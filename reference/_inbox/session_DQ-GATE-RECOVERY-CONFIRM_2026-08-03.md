=== SESSION LOG · 2026-08-03 · DQ-GATE-RECOVERY-CONFIRM ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `DQ-GATE-RECOVERY-CONFIRM` — замер, снялась ли блокировка `drift_check` сама после перехода
  расчётных суток на понедельник `2026-08-03`, и поправка состояния проекта.
- Сделано: тела `drift_check` за прогоны `17:00Z`/`18:00Z`/`19:00Z` (дословно, с ожиданием прогона
  `19:00Z` в фоне); проверка возобновления промоута (`0` заданий `MERGE`, независимый `COUNT(*)`
  совпал); проверка догона периода простоя (`core.fact_sales_profit`/`core.fact_purchases` по суткам
  `08-01…08-04`); состояние недельного потока (`core.fact_returns`); вердикт по гипотезе брифа
  (опровергнута); вердикт по `FACTS-BACKFILL` (не требуется, условно); подсчёт исторических блоков
  `drift_check` и их длительности. Артефакт: `reference/dq_gate_recovery_confirm_2026-08-03.md`.
  Эскалация архитектору: `reference/architect_review_queue_2026-08-03-2.md`.
- Команды/логи ключевые: `reference/_scratch_DQ-GATE-RECOVERY-CONFIRM_2026-08-03/` (`step1_run.log`,
  `step1b_run.log`, `step2_run.log`, `step3_run.log`, `step4_run.log`), не убираются (`ADR-043`).
- Отклонения от плана: старт сессии пришёлся на `2026-08-03T18:20:53Z` — до прогона `19:00Z`, брифом
  требуемого как третья точка замера. Ждал наступления прогона (~40 минут) фоновым скриптом
  (`step1b_wait_and_fetch_1900.sh`), затем снял тело. Отклонение процессное (момент старта), не по
  содержанию шагов.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `DQ-GATE-RECOVERY-CONFIRM`: строки в реестре и в таблице мандата на момент выдачи брифа НЕ
  было (`ADR-086 §3`, прецедент `PARALLEL-CHECK-NO-BRIEF`) → заводится и тем же коммитом закрывается
  DONE (задача одноразовая, измерение завершено этой сессией; прецедент одновременного
  заведения/закрытия — `INVOICES-STALENESS-PROBE`, 2026-08-02). Полный текст строки, годный для
  `07_ARCHIVE.md` дословно:
  `DQ-GATE-RECOVERY-CONFIRM` | (`DQ-GATE-RECOVERY-CONFIRM`, 2026-08-03) Замер: снялась ли блокировка
  `drift_check` сама после перехода на будничный порог `2026-08-03`. Результат — **нет**: `threshold`
  корректно переключился на `0,1`, но `yesterday_rev=191800` (из `staging`, независимо от блокировки
  промоута) держит `ratio=0,03` на двух последовательных часовых прогонах (`18:00Z`/`19:00Z`),
  идентично. Гипотеза брифа опровергнута наблюдением, не подтверждена. | задача | **DONE.** Полный
  разбор — `reference/dq_gate_recovery_confirm_2026-08-03.md`. Мандат класса B на восстановление потока
  не выдан этой сессией — эскалирован архитектору (`reference/architect_review_queue_2026-08-03-2.md`).
  Строка мандата (класс A, параллель нет, мандат постоянный на замер; Пишет:
  `reference/dq_gate_recovery_confirm_<date>.md`, `reference/_scratch_DQ-GATE-RECOVERY-CONFIRM_*/`,
  `reference/architect_review_queue_*.md`) заводится и убирается тем же коммитом парно (`ADR-090 §1`).
- Дописка к строке GAP-реестра `FACTS-BACKFILL` (текущий текст уже указывает «вердикт выносит
  `DQ-GATE-RECOVERY-CONFIRM` шаг 3» — эта сессия его выносит): **добавить предложением** «Вердикт этой
  сессией: НЕ требуется на момент замера (простой `≈50ч` < 7-суточного окна `MERGE`), но условно — если
  блокировка не снимется до истечения недели от `2026-08-01` (ориентировочно `2026-08-08`), вердикт
  устаревает и требует переизмерения. Статус строки `FACTS-BACKFILL` не меняется (остаётся `OPEN`,
  гейт — восстановление потока).»
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком):
  - Прошлый шаг: `DQ-GATE-RECOVERY-CONFIRM` закрыта DONE — гипотеза «блокировка снимется сама на буднях»
    опровергнута замером (`ratio=0,03 < 0,10` на двух прогонах подряд); артефакт
    `reference/dq_gate_recovery_confirm_2026-08-03.md`.
  - Где мы: блокировка `drift_check` продолжается непрерывно с `2026-08-01T18:00Z` (≥49ч), пережила
    переход на будничный порог; причина — не механизм гейта (`ADR-113` не опровергнут), а низкое
    значение `yesterday_rev` в источнике; реальность этого падения не проверена (класс B).
  - Следующий шаг: решение архитектора/владельца по эскалации `reference/architect_review_queue_2026-08-03-2.md`
    (мандат класса B на восстановление потока — вариант (а)/(б)/(в)); параллельно и без изменений —
    деплой `cf-facts` (`DEPLOY-PROCEDURE`, `ADR-115`), `INVOICES-BACKFILL`/`INVOICES-PARITY-RECHECK`.
  - Развилки на владельце: выбор формы восстановления потока (ручной промоут / правка порога-деплой /
    подтвердить падение выручки и не трогать гейт) — три варианта с плюсами/минусами в
    `reference/architect_review_queue_2026-08-03-2.md`.
  - Счётчик: пары реестра 2/7 сходятся (без изменений этой сессией) · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: **Замер `DQ-GATE-RECOVERY-CONFIRM` (2026-08-03) закрывает вопрос
  самовосстановления фактом, не расширяет `CONTEXT GAP`.** Три числа гейта на прогонах `18:00Z`/`19:00Z`
  `2026-08-03`: `target_date=2026-08-03` (сдвинулся верно), `threshold=0,1` (переключился на будничный
  верно), `yesterday_rev=191800` при `ma7=5533699` → `ratio=0,03` — НИЖЕ будничного порога, чек
  провалился ОБА раза с идентичными числами (значение в `staging` не растёт от часа к часу). Это первый
  наблюдаемый в репо случай, когда блок `drift_check` пережил календарный переход выходные→будни — все
  прежние наблюдавшиеся блоки (`2026-07-25→26`, недельные `06-14`/`06-28`) снимались проще. Промоут НЕ
  возобновился (`0` заданий `MERGE` с целью `core.fact_sales_profit` за окно замера, независимый
  `COUNT(*)` совпал). `core.fact_sales_profit` заморожена на `2026-08-01` (`36986` строк всего, `14` за
  `08-01`, `0` за `08-02…08-04`); `core.fact_purchases` — колонка даты `order_date`, не
  `transaction_date` (структурное отличие, не ошибка); последнее пополнение обеих — `2026-08-01
  17:02:1{4,6}`. `FACTS-BACKFILL`: вердикт «не требуется» условен — простой `≈50ч` меньше 7-суточного
  окна `MERGE`, но если блокировка проживёт дольше недели от `2026-08-01`, вердикт устаревает.
  `core.fact_returns` не пополнялась с `2026-07-19 01:11:56` (`max return_date=2026-07-01`, `24` строки);
  следующий недельный прогон — `2026-08-09T01:00Z`, ещё не наступил, восстановление возвратов остаётся
  ожиданием, а не фактом. Эскалация архитектору: открывается ли теперь мандат класса B на
  восстановление потока (три варианта с ценой каждого) — `reference/architect_review_queue_2026-08-03-2.md`.
  Полный расчёт, все тела логов и SQL — `reference/dq_gate_recovery_confirm_2026-08-03.md` и
  `reference/_scratch_DQ-GATE-RECOVERY-CONFIRM_2026-08-03/`, здесь не пересказываются.
- Новые открытые вопросы: нет нового `Q-` (это не гэп знания — это измеренный факт с известной
  причиной несоответствия предсказанию). Эскалация — файлом `architect_review_queue_2026-08-03-2.md`,
  без номера `Q-` (по прецеденту `CC-READONLY-PRINCIPAL-GEN-RETRO`, номер присваивается сессией, берущей
  пункт в разбор).
- Блокеры: нет (для этой сессии; для потока данных — блокировка `drift_check` продолжается, см. выше).
- updated_at: 2026-08-03
- обновил: исполнитель (сессия: DQ-GATE-RECOVERY-CONFIRM)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
