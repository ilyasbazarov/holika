=== SESSION LOG · 2026-08-08 · SALES-JULY-DAILY-RECON ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-JULY-DAILY-RECON — суточная сверка июльского остатка `+178 124,42` KGS против
  построчного эталона МойСклад, приёмка дословно `ADR-138 §3`
- Сделано:
  - Шаг (1) исполнен полностью: пересъём распределения `_loaded_at` по июлю с фильтром ветки
    `entity/demand` (исключены `Розница`/`Комиссия`). Последний массовый прогон ветки —
    `2026-07-19`, слепое окно до даты эталона `2026-08-07` — `19` суток. На отфильтрованной
    популяции пик `2026-08-07` (периметровый разовый прогон, `1830` строк в неотфильтрованном
    замере) исчезает (`58` строк) — `2026-07-19` остаётся единственным пиком без конкурента,
    дефект посылки `ADR-138 §1` этим подтверждён устранённым. Кандидаты в сироты на
    отфильтрованной популяции не изменились: `8` строк, `22 668,0108` KGS.
  - Шаг (2) исполнен и дал СТОП: контроль числа строк построчного эталона (PDF, стр. 1-7)
    сошёлся (`243` отгрузки + `1` возврат = `244`, независимо перепроверено по страницам). Контроль
    суммы отгрузок НЕ сошёлся: `108 989 733,92` (PDF, извлечено `pdftotext` двумя независимыми
    методами и Decimal-суммированием) против `108 989 733,88` (`oracle §1`), разница `+0,04` KGS.
    Сумма возвратов (`62 709,00`) сошлась точно — перекрёстная проверка.
  - Шаги (3)/(4) НЕ исполнялись — оба используют PDF как построчный эталон, а его контроль полноты
    не пройден; текст задачи (`ADR-138 §3`) требует стопа с предъявлением чисел, не округления.
- Команды/логи ключевые: `_scratch_SALES-JULY-DAILY-RECON_2026-08-08/step1_loaded_at_dist_filtered.sh`
  → `step1_run.log`; `step1b_candidates_filtered.sh` → `step1b_run.log`; `parse_pdf.py` над
  `pdf_pages_1-7.txt`/`pdf_pages_1-7_raw.txt` (два независимых `pdftotext`-извлечения). `date -u` и
  `gcloud auth list` первой и последней командой каждого bash-скрипта.
- Отклонения от плана: задача не доведена до конца по своему же стоп-условию — расхождение
  `0,04` KGS на контроле суммы отгрузок PDF-эталона. Артефакт `reference/sales_july_daily_recon_2026-08-08.md`
  фиксирует это как явный СТОП, не как гэп-догадку.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-JULY-DAILY-RECON: READY → PARTIAL (STOP на шаге (2), шаг (1) исполнен)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-JULY-DAILY-RECON` шаг (1) исполнен (кандидаты в сироты на отфильтрованной
    ветке `entity/demand` не изменились, `8` строк/`22 668,0108` KGS); шаг (2) дал СТОП —
    расхождение `0,04` KGS на контроле суммы построчного PDF-эталона против `oracle §1`, артефакт
    `reference/sales_july_daily_recon_2026-08-08.md`.
  - Где мы: остаток `+178 124,42` KGS июля остаётся нелокализованным по суткам — суточная сверка
    (шаги 3-4) заблокирована непройденным контролем полноты своего же входного эталона.
  - Следующий шаг: архитектору — решение по расхождению `0,04` KGS (принять как округление отчёта
    источника или завести отдельный discovery по причине); после решения — доисполнение шагов (3)/(4)
    `SALES-JULY-DAILY-RECON` на том же PDF, либо на переснятом.
  - Развилки на владельце: принять ли расхождение `0,04` KGS суммы отгрузок PDF-эталона как
    приемлемое округление отчёта МойСклад, или гейтить продолжение отдельным discovery.
  - Счётчик: пары реестра 7/7 (без изменений, июль вне зоны паритета) · Epic 1 — суточная локализация
    июльского остатка не завершена
- Подробности для модели: `SALES-JULY-DAILY-RECON` частично исполнена. Фильтр ветки `entity/demand`
  подтвердил дефект посылки `ADR-138 §1` устранённым — `2026-08-07` на отфильтрованной популяции
  несёт `58` строк, не `1830`; `2026-07-19` остаётся единственным пиком, слепое окно `19` суток.
  Кандидаты в сироты не изменились фильтром (`8` строк/`22 668,0108` KGS) — они уже лежали в ветке
  `entity/demand`. Контроль полноты PDF-эталона (`reference/oracle_july_2026_employees_2026-08-07.pdf`)
  дал расхождение по сумме отгрузок: `108 989 733,92` (PDF, двумя методами извлечения текста) против
  `108 989 733,88` (`oracle §1`), `+0,04` KGS — контроль числа строк сошёлся точно (`244`). Причина
  расхождения не установлена этой сессией (anti-improvisation) — не парсинг (воспроизведено двумя
  методами), возможно округление отчёта-источника при агрегации. Полный ход, SQL, сырой вывод и оба
  извлечения текстового слоя — `reference/sales_july_daily_recon_2026-08-08.md` +
  `reference/_scratch_SALES-JULY-DAILY-RECON_2026-08-08/`.
- Новые открытые вопросы: нет (это не новый гэп-кандидат — это стоп внутри уже заведённой задачи,
  решение по нему за архитектором/владельцем без новой Q-строки, пока не решено иначе)
- Блокеры: `SALES-JULY-DAILY-RECON` шаги (3)/(4) заблокированы расхождением `0,04` KGS на контроле
  полноты PDF-эталона (шаг (2))
- updated_at: 2026-08-08
- обновил: исполнитель (сессия: SALES-JULY-DAILY-RECON)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
