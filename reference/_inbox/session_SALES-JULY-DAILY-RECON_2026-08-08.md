=== SESSION LOG · 2026-08-08 · SALES-JULY-DAILY-RECON ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-JULY-DAILY-RECON — суточная сверка июльского остатка `+178 124,42` KGS против
  построчного эталона МойСклад, приёмка дословно `ADR-138 §3`
- Сделано (все 4 пункта приёмки исполнены):
  - (1) Пересъём распределения `_loaded_at` по июлю с фильтром ветки `entity/demand` (исключены
    `Розница`/`Комиссия`). Последний массовый прогон ветки — `2026-07-19`, слепое окно до даты
    эталона `2026-08-07` — `19` суток. На отфильтрованной популяции пик `2026-08-07` (периметровый
    разовый прогон, `1830` строк в неотфильтрованном замере) исчезает (`58` строк) — `2026-07-19`
    остаётся единственным пиком без конкурента, дефект посылки `ADR-138 §1` этим подтверждён
    устранённым. Кандидаты в сироты на отфильтрованной популяции не изменились: `8` строк,
    `22 668,0108` KGS.
  - (2) Контроль полноты PDF-эталона: число строк сошлось точно (`243` отгрузки + `1` возврат =
    `244`). Сумма отгрузок дала расхождение `+0,04` KGS (`108 989 733,92` против `108 989 733,88`,
    `oracle §1`) — формальный СТОП по тексту задачи. **Владелец снял стоп в чате 2026-08-08:**
    «0,04 сом это округление отчёта, продолжай на этом же PDF». Причина расхождения не
    установлена (не парсинг — воспроизведено двумя независимыми методами извлечения текста PDF).
  - (3) Суточный ряд PDF (только продажи, время пересчитано `−3ч` в UTC, `ADR-137 §3/§8`) против
    нашего суточного ряда «Оптовая + строки без канала». Итоговая сумма расхождений по суткам
    (`178 124,3801` KGS) сходится с целевым остатком `178 124,42` KGS с точностью до принятого
    округления `0,04` KGS.
  - (4) Вердикт: `25` из `30` суток дают расхождение `≈0` (округление отчёта). Весь содержательный
    остаток — на пяти сутках `07-02`/`07-11`/`07-13`/`07-20`/`07-21`/`07-27`/`07-29`/`07-30`
    (де-факто на восьми, но масштаб — на пяти последних). K1 (сироты) ПОДТВЕРЖДЁН вторым
    независимым методом — три даты сходятся день-в-день и до цента с кандидатами шага (1)
    (`22 668,0108` KGS, `12,73 %` остатка). Остаток `155 456,3693` KGS (`87,27 %`) локализован
    целиком на пяти датах (`56,5 %` всей задачи — на одной дате `07-30`), но поиском одного/пары
    документов PDF по всему июлю (`≈29 тыс.` пар) НЕ обнаружен ни на одной из пяти дат — K2 и K3
    этим методом неразличимы (оба предсказывают «документ отсутствует в июльском PDF»,
    `core.fact_sales_profit` не несёт `applicable`/`moment`/`demand_id`, `ADR-104 vi`). Разбивка по
    `agent_id` на пяти датах не даёт концентрации на одном контрагенте — K4 ослаблен как
    единственное объяснение крупнейшей даты (`07-30`), не проверен как частичный вклад на
    остальных. Необъяснённый остаток числом: `155 456,37` KGS.
- Команды/логи ключевые: `_scratch_SALES-JULY-DAILY-RECON_2026-08-08/`:
  `step1_loaded_at_dist_filtered.sh`→`step1_run.log`, `step1b_candidates_filtered.sh`→
  `step1b_run.log`, `step3_our_daily_wholesale.sh`→`step3_run.log`,
  `step4b_agent_breakdown.sh`→`step4b_run.log`, `parse_pdf.py`/`step4_diff.py`/
  `step4c_doc_match_search.py` над `pdf_pages_1-7.txt`/`pdf_pages_1-7_raw.txt` (два независимых
  `pdftotext`-извлечения). `date -u` и `gcloud auth list` первой и последней командой каждого
  bash-скрипта.
- Отклонения от плана: задача проходила через промежуточный СТОП на шаге (2) (расхождение `0,04`
  KGS), снятый прямым решением владельца в чате той же сессии — не отдельным заходом. Полная
  дифференциация K2/K3/K4 для оставшихся `155 456,37` KGS не достигнута — таблица
  `core.fact_sales_profit` не несёт полей, различающих эти три гипотезы; это названо явно как
  предмет следующего шага, не молчаливый остаток.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-JULY-DAILY-RECON: READY → DONE (все 4 пункта приёмки исполнены, вердикт вынесен;
  различение K2/K3/K4 на оставшихся `155 456,37` KGS выносится отдельным следующим шагом, не
  блокирует закрытие этой задачи — приёмка требовала «вердикт... и необъяснённый остаток назван
  числом», не полную идентификацию механизма)
- Задача SALES-JULY-RESIDUAL-SPLIT: PARTIAL → DONE (гейт снят результатом SALES-JULY-DAILY-RECON,
  `ADR-138 §3`)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-JULY-DAILY-RECON` завершена всеми 4 пунктами приёмки. Остаток
    `+178 124,42` KGS разложен: `22 668,01` KGS — сироты (К1, подтверждён дважды), `155 456,37` KGS
    — локализованы на 5 конкретных сутках (`56,5 %` всей задачи на одной дате `2026-07-30`), но
    механизм (K2 vs K3 vs K4) не различён доступными полями `core.fact_sales_profit`. Артефакт
    `reference/sales_july_daily_recon_2026-08-08.md`.
  - Где мы: июльский остаток целиком локализован (по суткам и по величине), но не целиком
    объяснён по механизму — открыт узкий вопрос по 5 датам вместо диффузного вопроса по месяцу.
  - Следующий шаг: архитектору — решение по различителю K2/K3/K4 (нужно поле `applicable`/`moment`
    в `core.fact_sales_profit` ЛИБО мандат класса B на прямой `GET` документов пяти локализованных
    суток, прецедент `ADR-100 §10`/`ADR-139 §1`); параллельно — `SALES-CHANNEL-NULL-DEMAND`
    (класс B, мандат `ADR-139`, не параллелится).
  - Развилки на владельце: заводить ли новую задачу-discovery на различитель K2/K3/K4 сейчас, или
    отложить до завершения текущей очереди (остаток вне зоны паритета, финиш не гейтит,
    `ADR-115 §11`).
  - Счётчик: пары реестра 7/7 (без изменений, июль вне зоны паритета) · Epic 1 — июльский остаток
    локализован по суткам, механизм различён частично (К1 из 4 кандидатов)
- Подробности для модели: `SALES-JULY-DAILY-RECON` завершена. Метод: суточный ряд построчного
  PDF-эталона (время `−3ч → UTC`, `ADR-137 §3/§8`) сопоставлен с суточным рядом
  `core.fact_sales_profit` (фильтр «Оптовая + без канала», возврат №00008 исключён — сверка только
  продаж). Итог по суткам сходится с месячным остатком без потерь (`178 124,38` против целевых
  `178 124,42`, разница `0,04` — принята владельцем как округление отчёта). К1 (сироты) подтверждён
  ВТОРЫМ независимым методом — три даты (`07-02`/`07-11`/`07-13`) совпадают день-в-день и до цента
  с кандидатами шага (1). Остаток `155 456,37` KGS локализован на пяти датах
  (`07-20`/`07-21`/`07-27`/`07-29`/`07-30`), `56,5 %` всей задачи — на одной `07-30`. Поиск
  одного/пары PDF-документов, дающих эту разницу (обязательный пункт приёмки), не нашёл совпадений
  ни на одной из пяти дат — метод не различает K2 (черновики) и K3 (замороженная дата): оба
  предсказывают «документ отсутствует в июльском PDF целиком», а `core.fact_sales_profit` не несёт
  `applicable`/`moment`/`demand_id` для прямой проверки (`ADR-104 vi`, схема таблицы — 22 колонки,
  зафиксирована `bq show --schema` в `_scratch/`). K4 проверен разбивкой по `agent_id` на пяти
  датах — на крупнейшей (`07-30`, `100 601,997` KGS) концентрации на одном контрагенте нет
  (крупнейший даёт `1 986 390` KGS, не совпадает с остатком), гипотеза ослаблена как единственное
  объяснение этой даты, но не проверена как частичный вклад на прочих четырёх. Полный ход, все SQL,
  сырой вывод и оба извлечения текстового слоя PDF — `reference/sales_july_daily_recon_2026-08-08.md`
  + `reference/_scratch_SALES-JULY-DAILY-RECON_2026-08-08/`.
- Новые открытые вопросы: нет (различитель K2/K3/K4 назван как следующий шаг в тексте выше и в
  `07_STATE §Открытые вопросы`, отдельная Q-строка не заводится этой сессией — решение о форме
  следующего шага за архитектором)
- Блокеры: нет
- updated_at: 2026-08-08
- обновил: исполнитель (сессия: SALES-JULY-DAILY-RECON)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
