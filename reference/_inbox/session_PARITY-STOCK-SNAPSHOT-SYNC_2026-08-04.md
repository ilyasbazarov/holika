=== SESSION LOG · 2026-08-04 · PARITY-STOCK-SNAPSHOT-SYNC ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `PARITY-STOCK-SNAPSHOT-SYNC` — единственная работа, закрывающая строку 21 реестра паритета («Остатки товаров»); шаг 1 (проба ретроспективного момента `report/stock/all`)
- Сделано: три read-only `GET report/stock/all` в одну минуту (контроль/проба с `moment=`/повтор контроля) с кандидат-параметром `moment` (аналогия с `report/profit/*`, формат `YYYY-MM-DD HH:MM:SS`), кандидат-значение взято из `_loaded_at` партиции `core.fact_inventory` недельной давности (`bq query`, read-only); построчная сверка проба vs контроль по всем 326 позициям `stock` — 0 расхождений при побайтовом различии тел (эхо параметра в `meta.href`); вердикт шага 1 — ОТРИЦАТЕЛЬНО (параметр проигнорирован сервером); дописана строка 21 `reference/parity_registry.md` (только колонка «Статус сходимости»); токен нигде не напечатан, проверка утечки (токен+grep одной командой) чистая
- Команды/логи ключевые: `reference/_scratch_PARITY-STOCK-SNAPSHOT-SYNC_2026-08-04/run_step1.log` (успешный прогон), `run_step1_attempt1_failed_gzip.log` (первый прогон упал на недекомпрессированном gzip-теле — баг скрипта, исправлен `curl --compressed`), сырые тела трёх вызовов и мета — тот же каталог
- Отклонения от плана: один технический перезапуск скрипта шага 1 из-за бага декомпрессии gzip в первой версии (первая попытка не дала содержательных данных, кроме `HTTP 200` на первом вызове); по существу шага отклонений нет — порядок и форма исполнены по `ADR-123 §8`/брифу дословно

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `PARITY-STOCK-SNAPSHOT-SYNC`: `READY` → `IN PROGRESS` (шаг 1 закрыт отрицательно; шаги 2-3 остаются, гейт — окно `21:00Z`, вторым заходом)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: шаг 1 `PARITY-STOCK-SNAPSHOT-SYNC` исполнен отрицательно — параметр `moment` эндпоинтом `report/stock/all` не учитывается (артефакт `reference/parity_stock_snapshot_sync_2026-08-04.md`).
  - Где мы: пять пар реестра из семи закрыты; у строки 21 требование синхронизации с окном `21:00Z` не снято, шаги 2-3 не исполнялись.
  - Следующий шаг: `PARITY-STOCK-SNAPSHOT-SYNC` шаги 2-3 (класс B, мандат выдан, тот же бриф) — вторым заходом в окне `21:00Z` UTC; `DQ-GATE-SCOPE-SPLIT-DEPLOY` и `FACTS-FLOW-RESUME-CONFIRM` идут независимо.
  - Развилки на владельце: окончательный ответ «умеет ли `report/stock/all` отдавать остатки задним числом вообще» (не только под именем `moment`) — публичная документация МойСклада инструменту недоступна.
  - Счётчик: пары реестра **5/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: Шаг 1 `PARITY-STOCK-SNAPSHOT-SYNC` (2026-08-04, класс B) дал чистый отрицательный результат по кандидат-параметру `moment`: `report/stock/all?...&moment=<дата на 7 суток раньше>` вернул `HTTP 200` с телом, побайтово идентичным контролю по всем 326 позициям `stock` (различие — только эхо параметра в `meta.href` верхнего уровня ответа). Дистанция кандидата (7 суток, на два порядка больше суточной амплитуды оборота ~5000 штук) исключает прочтение «остаток не менялся» — единственное состоятельное прочтение: сервер параметр `moment` под этим именем и в этом формате (`YYYY-MM-DD HH:MM:SS`) не учитывает. Это НЕ факт «API не умеет отдавать остатки задним числом» — другое имя параметра, другой формат значения и endpoint `report/stock/bystore` этой сессией не пробовались (один кандидат, одна попытка — по тексту брифа). Требование `ADR-123 §3` (синхронизация живого запроса с окном пересборки `cf-inventory-trigger`, `21:00Z` UTC = `03:00` Бишкек) остаётся единственной исполнимой формой измерения пары. Следующей сессии — шаги 2-3 того же брифа вторым заходом в окне `21:00Z`: пересверка построчным джойном против партиции того же прогона (ожидание — `Σ(движение)` близка к нулю, документы в `03:00` Бишкек не проводятся, значимое `Σ(движение)` опровергло бы механизм `ADR-123 §6`) и природа семи UUID-сирот из `reference/_scratch_PARITY-STOCK-ROWWISE_2026-08-04/step6_join_result.json` (`GET entity/product/<uuid>` по каждому). Технический гэп, снятый этой сессией не для строки реестра: скрипт `curl` с ручным заголовком `Accept-Encoding: gzip` без флага `--compressed` отдаёт нераспакованные байты, которые ломают `json.loads` — на будущее для любых `GET` к `api.moysklad.ru` с этим заголовком нужен `curl --compressed`, иначе первая попытка сгорает без данных (провенанс ошибки — `run_step1_attempt1_failed_gzip.log`, не переписывается).
- Новые открытые вопросы: нет
- Блокеры: нет (шаги 2-3 не блокированы, только гейтятся временны́м окном `21:00Z`)
- updated_at: 2026-08-04
- обновил: executor (сессия: PARITY-STOCK-SNAPSHOT-SYNC)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
