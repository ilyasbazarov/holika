=== SESSION LOG · 2026-08-12 · SALES-REFRESH-WINDOW-ROW-RESTORE ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-REFRESH-WINDOW-ROW-RESTORE` — вернуть в `core.fact_sales_profit` две строки,
  удалённые ошибочно боевым прогоном `job_id=1e698e3d-35c5-44c2-b90f-fbe852cf39a7`. Класс B,
  мандат выдан поимённо владельцем (`reference/sales_refresh_window_row_restore_mandate_2026-08-12.md §2`).
- Сделано:
  - предпроверка отдельным скриптом (`_scratch_.../precheck.sh`): оба `transaction_id`
    отсутствовали в цели (0/0); присутствовали в снимке `fact_sales_profit_snap_20260811_163306`
    ровно по одному разу (1/1); схемы снимка и цели идентичны колонка за колонкой (23 колонки,
    имена и типы совпали полностью); число строк цели ДО вставки зафиксировано — `42986`;
  - действие объявлено отдельным сообщением в чат (объект/операция/откат) ДО исполнения, владелец
    подтвердил;
  - вставка явным `INSERT (23 именованные колонки) SELECT (те же колонки по именам)` из снимка в
    `core.fact_sales_profit` по `WHERE transaction_id IN (...)` — `SELECT *` и позиционные
    конструкции не использовались (`C1`, `ADR-030`); `Number of affected rows: 2`;
  - приёмка тремя числами (`_scratch_.../acceptance.sh`):
    1. каждый `transaction_id` присутствует ровно один раз; сумма `revenue_kgs` по ним =
       `1980000.0` (ожидание `1 980 000,00` — сошлось); число строк выросло на `2`
       (`42986` → `42988` — сошлось);
    2. разность `SUM(revenue_kgs)` за `2026-05-01…2026-05-31` между снимком и живой таблицей =
       `1630239.808499962` (ожидание ТОЧНО `1 630 239,81` — сошлось в пределах представления
       `FLOAT64`, округление до копейки даёт точное совпадение);
    3. разность раскладывается ровно на `7` именованных `transaction_id` (сироты `ADR-100 §1`,
       даты `2026-05-07`/`2026-05-09`/`2026-05-26`), восьмого идентификатора не найдено.
  Полные логи и скрипты — `reference/_scratch_SALES-REFRESH-WINDOW-ROW-RESTORE_2026-08-12/`
  (`precheck.sh`, `precheck_run.log`, `insert_row_restore.sh`, `insert_run.log`,
  `acceptance.sh`, `acceptance_run.log`).
- Команды/логи ключевые: `bq query` (BigQuery), `date -u`/`gcloud auth list` первой и последней
  командой каждого из трёх скриптов — деградации авторизации не было ни в одном прогоне.
- Отклонения от плана: нет.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-REFRESH-WINDOW-ROW-RESTORE`: мандат выдан → **DONE**. Майский паритет
  восстановлен: `core.fact_sales_profit` теперь несёт обе строки документа «04235»
  (`1 980 000,00` KGS), остаток расхождения со снимком за май — ровно `7` законно удалённых
  сирот (`1 630 239,81` KGS), других расхождений нет.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-REFRESH-WINDOW-ROW-RESTORE` закрыта — две ошибочно удалённые строки
    (`1 980 000,00` KGS) вставлены обратно из снимка `fact_sales_profit_snap_20260811_163306`
    явным `INSERT (колонки) SELECT`; все три числа приёмки сошлись точно (`reference/_scratch_SALES-REFRESH-WINDOW-ROW-RESTORE_2026-08-12/`).
  - Где мы: майский паритет `core.fact_sales_profit` восстановлен по всем 36 строкам боевого
    прогона (`34` удалены законно, `2` вернулись); обслуживание по-прежнему стоит на
    `cf-facts-00011-mab` без ветки удаления — новых удалений не будет до следующего деплоя.
  - Следующий шаг: `SALES-REFRESH-WINDOW` — подготовка проверки полноты выгрузки в
    `helpers.py::paginate_entity` по образцу `cf-finance/invoices.py:85-122` (класс A, решение
    владельца §3 мандата); затем повторный деплой ветки удаления вместе с проверкой полноты
    одним патчем (класс B, мандат отдельным поимённым решением).
  - Развилки на владельце: нет новых.
  - Счётчик: строк списка закрытия осталось `8` из `9` (без изменения — эта сессия не входит в
    список закрытия, а закрывает найденный дефект).
- Подробности для модели: снимок `snap_20260811_163306` НЕ удалять (`ADR-161`, условие в силе до
  успешной приёмки следующего деплоя, `§5` мандата). Ветка код-репо
  `deploy/cf-facts-2026-08-11-refresh-window-v2` в `master` не сливается до того же деплоя. Форма
  приёмки (LEFT JOIN снимок→цель, фильтр по маю, `COUNT(*)` пропавших) переиспользуема для любой
  будущей сверки этой ветки — воспроизведена в третий раз без изменений, работает.
- Новые открытые вопросы: нет.
- Блокеры: нет.
- updated_at: 2026-08-12
- обновил: executor (сессия: SALES-REFRESH-WINDOW-ROW-RESTORE)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
