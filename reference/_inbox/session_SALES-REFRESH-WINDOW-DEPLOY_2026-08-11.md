=== SESSION LOG · 2026-08-11 · SALES-REFRESH-WINDOW-DEPLOY ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-REFRESH-WINDOW-DEPLOY (бриф `briefs/SALES-REFRESH-WINDOW-DEPLOY.md`, мандат `ADR-159`/`ADR-158`).
- Сделано:
  - Шаги 1-6 брифа исполнены штатно: дрейф не обнаружен (живая `cf-facts-00011-mab` побайтово
    совпала с `master`), патч перенесён по diff в ветку `deploy/cf-facts-2026-08-11-refresh-window`
    (код-репо `holika-prod`), гигиена/секреты чисты, снимок `core.fact_sales_profit_snap_20260811`
    снят ДО деплоя (`42 975` строк всего; май `3 411`/`96 844 445,61` KGS; июль `4 707`/
    `112 502 581,88` KGS), деплой дал ревизию `cf-facts-00012-ber` (`generation 1786437392633949`),
    read-back архива чист (11 файлов, sha256 совпал с веткой), секция записана в
    `reference/code/cf-facts/MANIFEST.md`. `window_days` первого прогона вычислен: `105`.
  - На шаге 8 (перед не-идемпотентным прогоном) перечитан `main.py` построчно и найден структурный
    гэп: `mode=weekly`/`mode=hourly` игнорируют `window_days` из тела запроса, окно выборки жёстко
    зашито (`90`/`7` суток). Сегодняшний `weekly`-вызов покрыл бы `2026-05-13…2026-08-11` — на `12`
    суток короче требуемого покрытия `2026-05-01`. Прогон НЕ запускался, вопрос передан владельцу.
  - Владелец передал решение архитектора: `reference/sales_refresh_window_rollback_adj_2026-08-11.md`
    (ветка `s/SALES-REFRESH-WINDOW-ROLLBACK-ADJ`, коммит `ed3f80c`) — подтверждён и прочитан с диска
    ДО исполнения (не принят на слово). Найден ВТОРОЙ, более тяжёлый дефект уже задеплоенного кода:
    обе ветки удаления `MERGE` (продажи/периметр) фильтруют только по дате, но пишут два независимых
    загрузчика с непересекающимися staging — недельный конвейер (воскресенье `2026-08-16 01:00 UTC`)
    удалил бы `6 922` строк оптовой торговли (`269 440 872,10` KGS) со стороны периметра и `5 471`
    строк (`17 056 819,29` KGS) со стороны продаж. Ущерба на момент находки не было (доказано снимком
    архитектора, разность `0` по всем каналам).
  - Мандат `ADR-159` приостановлен архитектором. Исполнено ровно одно разрешённое действие: перевод
    трафика Cloud Run `cf-facts` на ревизию `cf-facts-00011-mab` (100%), объявлено отдельным
    сообщением (`ADR-077 §6`), подтверждено владельцем в чате. Пересборка не требовалась.
  - Приёмка отката — три числа, все read-back:
    (i) `gcloud run services describe cf-facts` → трафик `cf-facts-00011-mab` `100%`;
    (ii) сопоставление снимка `_snap_20260811` с живой таблицей сразу после отката — `0` строк /
    `0,00` KGS по всем четырём каналам («(NULL)», «Комиссия», «Оптовая торговля», «Розница»);
    (iii) то же сопоставление ПОСЛЕ первого естественного часового `promote` на откаченной ревизии
    (execution `0be2309e-a6ed-409c-b64e-37cb51f69fda`, `10:00:02Z…10:06:2xZ`, `SUCCEEDED`,
    независимо подтверждено `revision_name=cf-facts-00011-mab` в логах Cloud Run) — снова `0` строк /
    `0,00` KGS по всем каналам. Откат подтверждён положительным контролем.
  - Собран `reference/sales_refresh_window_deploy_2026-08-11.md` — покрывает и деплой, и находку
    гэпа, и откат, все три числа приёмки.
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-DEPLOY_2026-08-11/`
  (`step1_run.log` … `step6_run.log`, `step_rollback_traffic_run.log`,
  `rollback_accept1_describe.json`, `rollback_accept2_diff.log`, `rollback_accept3_diff.log`).
- Отклонения от плана: **существенные.** Брифовые шаги 7-12 (первый боевой прогон, замер до/после,
  пересчёт разности, merge ветки в `master`) НЕ исполнены — мандат приостановлен ДО их начала.
  Вместо них исполнена процедура отката по прямому указанию владельца/архитектора. Ветка деплоя
  `deploy/cf-facts-2026-08-11-refresh-window` в `holika-prod` НЕ слита в `master` (условие мандата:
  merge — только при полном успехе шагов 8-11, которых не было). Ветка сохраняется как provenance.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-REFRESH-WINDOW, деплой: `ВЫДАН поимённо ADR-159` → **ПРИОСТАНОВЛЕН** (дополнение
  архитектора `reference/sales_refresh_window_rollback_adj_2026-08-11.md §5`, номер ADR присваивает
  сборка). Деплой исполнен и ОТКАЧЕН тем же днём: живая ревизия сейчас — `cf-facts-00011-mab` (та же,
  что была базой мандата), новая ревизия `cf-facts-00012-ber` существует, но трафика не несёт.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: деплой `SALES-REFRESH-WINDOW` исполнен и в тот же день откачен — найден дефект
    ветки удаления (независимые загрузчики продаж/периметра сносят чужие строки, `269 440 872,10`
    KGS риска), обслуживание вернулось на `cf-facts-00011-mab`, откат подтверждён тремя нулевыми
    сопоставлениями со снимком.
  - Где мы: майский паритет остаётся неисправленным (задача не продвинулась); дополнительно найден
    и предотвращён более тяжёлый латентный дефект, который сработал бы на недельном прогоне
    `2026-08-16`; форма фикса (три варианта, рекомендация — вариант 1) ждёт решения владельца.
  - Следующий шаг: владелец выбирает форму фикса (`reference/sales_refresh_window_rollback_adj_2026-08-11.md §6`);
    патч варианта 1 уже подготовлен (`s/SALES-REFRESH-WINDOW-ROLLBACK-ADJ`, коммит `a336700`) и
    требует независимой проверки ДРУГОЙ сессией и нового мандата, не переиспользования этого.
  - Развилки на владельце: выбор варианта фикса (1/2/3, `rollback_adj §6`); новый мандат класса B на
    его деплой, когда форма выбрана.
  - Счётчик: строк списка закрытия осталось `8` из `9` (без изменений — приостановленный деплой не
    засчитывается как продвижение строки `1`).
- Подробности для модели: **Деплой `SALES-REFRESH-WINDOW` откачен, не удалён.** Следующая сессия по
  этой задаче обязана знать: (i) снимок `core.fact_sales_profit_snap_20260811` СОХРАНЯЕТСЯ — не
  удалять, он единственное точное доказательство отсутствия ущерба и единственный точный откат
  данных на случай, если понадобится позже; (ii) ветка `deploy/cf-facts-2026-08-11-refresh-window`
  код-репо `holika-prod` тоже сохраняется как provenance, НЕ слита в `master`; (iii) структурный гэп
  `mode=weekly`/`mode=hourly` игнорируют `window_days` (найден этой сессией) остаётся нерешённым
  независимо от выбора формы фикса дефекта происхождения строки — при вариантах 1/2 расширенное окно
  первого прогона по-прежнему потребует правки `_run_weekly_load`, это войдёт в тот же будущий патч
  (`rollback_adj §7`); (iv) живая ревизия `cf-facts-00012-ber` существует в Cloud Run, но трафика не
  несёт — не путать с активной при следующем `describe`.
- Новые открытые вопросы: нет новых — все найденные факты уже отражены в `reference/sales_refresh_window_rollback_adj_2026-08-11.md`, заведённом архитекторской сессией `SALES-REFRESH-WINDOW-ROLLBACK-ADJ` (не этой).
- Блокеры: выбор формы фикса дефекта происхождения строки — на владельце.
- updated_at: 2026-08-11
- обновил: executor (сессия: SALES-REFRESH-WINDOW-DEPLOY)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет (дополнение к `ADR-159` подготовлено АРХИТЕКТОРСКОЙ сессией `SALES-REFRESH-WINDOW-ROLLBACK-ADJ`
  в `reference/sales_refresh_window_rollback_adj_2026-08-11.md §5`, эта сессия его не издаёт — только
  исполняет предписанное там действие и подтверждает приёмку).

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
