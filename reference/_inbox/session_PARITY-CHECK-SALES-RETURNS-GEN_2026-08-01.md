=== SESSION LOG · 2026-08-01 · PARITY-CHECK-SALES-RETURNS-GEN ===

## SESSION_LOG
- Задача: PARITY-CHECK-SALES-RETURNS-GEN — генерация брифа первой волны построчных сверок реестра
  паритета (пары «Продажи» и «Возвраты покупателей» одним заходом)
- Сделано: прочитан обязательный контекст (`_METHOD`, `00_CHARTER`, `04_ROADMAP` не потребовался —
  задача не в роадмапе, это GAP-реестр, `05_CONVENTIONS` целиком включая Часть III, `06_INDEX`,
  `07_STATE`), точечно подняты `ADR-085`, `ADR-088` (целиком, §1–§10), `ADR-089`, `ADR-079` §2/§6/§9,
  `ADR-076` §1/§5, `ADR-077` §6; собран бриф `briefs/PARITY-CHECK-SALES-RETURNS.md` по шаблону `08`
  со всеми пятью шагами приёмки, процитированными дословно из `07_STATE.md:182`
- Команды/логи ключевые: `bash tools/session_status.sh` (старт, RC 0, дерево чисто); чтение файлов
  с диска, живых команд к облаку/МойСкладу не исполнялось (роль генератора не читает секреты)
- Отклонения от плана: нет по содержанию брифа — бриф генерируется, но несёт явный гейт исполнения
  (мандат класса B на `PARITY-CHECK-SALES-RETURNS` не выдан, `_GENERATOR.md` п.4a): исполнение
  блокировано до отдельного ADR по прецеденту `ADR-079 §9`. Дополнительно в бриф внесено: гейт
  «разрез по контрагентам ждёт `PARITY-SCD2-ATTRIBUTION`» из `07_STATE.md:182` снят фактом —
  `ADR-089` уже закрыл эту задачу (действующий JOIN `scd2_is_current = TRUE` признан корректным),
  брифу это указано явно, чтобы исполнитель не ждал уже закрытого предусловия. **Процессное
  отклонение, пойманное на приёмке сборщиком:** первая версия этого файла несла `STATE_PATCH` по
  устаревшей форме `_METHOD.md §8` вместо действующей `05_CONVENTIONS.md` Часть III (`ADR-084`) —
  без блока «Стенд-ап» (пять строк, замена целиком), без «Подробности для модели», без поля
  «обновил». Причина: Часть III не была прочитана целиком перед составлением блока, только точечно
  через `grep` по таблице мандата. Исправлено этим же файлом до применения сборкой.

## STATE_PATCH
- Задача PARITY-CHECK-SALES-RETURNS: READY (бриф не собран) → READY (бриф собран,
  `briefs/PARITY-CHECK-SALES-RETURNS.md`; мандат класса B по-прежнему НЕ выдан — исполнение
  заблокировано до отдельного ADR)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: собран бриф первой волны построчных сверок реестра паритета
    (`PARITY-CHECK-SALES-RETURNS`, `briefs/PARITY-CHECK-SALES-RETURNS.md`); все пять шагов приёмки
    процитированы дословно из `07_STATE.md:182`, гейт по атрибуции контрагента снят фактом
    (`ADR-089`) и явно указан в брифе как закрытый
  - Где мы: бриф готов и ждёт мандата класса B; оба решения-предусловия построчных сверок
    (`ADR-088` — правило суток, `ADR-089` — атрибуция контрагента) уже приняты раньше
  - Следующий шаг: владелец/архитектор решает, выдавать ли мандат класса B на
    `PARITY-CHECK-SALES-RETURNS` поимённо, по прецеденту `ADR-079 §9`; параллельно доступны
    `INGEST-MOMENT-ZONE-FIX` (класс B, мандат не выдан) и четыре правки владельца в интерфейсе
    Looker Studio
  - Развилки на владельце: выдавать ли мандат класса B на `PARITY-CHECK-SALES-RETURNS` (да/нет/
    отложить) — не путать с `Q-92` (эталонные сутки паритета), который решается ПОСЛЕ исполнения
    самого брифа, не сейчас
  - Счётчик: пары реестра 1/7 сходятся · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
    (без изменений этой сессией — сессия только читала репо, ни одна пара не сверена)
- Подробности для модели: бриф `PARITY-CHECK-SALES-RETURNS` требует пяти шагов на исполнении —
  (1) различитель `Q-93` (зона границ `momentFrom`/`momentTo`, метод: узкое окно вокруг документа
  `entity/salesreturn` №00008 с известным `moment`); (2) замер ночных полос `ADR-088 §5/§6` за
  май-2026 по продажам и возвратам отдельно, плюс второй замер сдвига на майском документе
  (`ADR-088 §10`, факт «+3 часа» пока подтверждён только на одном июльском документе); (3)
  построчная сверка обеих пар с правилом суток каждой таблицы, документы ночных полос считаются
  ОТДЕЛЬНО от общего итога; (4) запись правила и результата в `reference/parity_registry.md` строки
  18–20 (только эти строки, остальной файл не трогать); (5) вопрос `Q-92` (эталонные сутки
  паритета) этой сессией НЕ задаётся ни при каком результате замера — он имеет цену только после
  того, как известно, пуста ли ночная полоса. Мандат класса B выдаётся отдельным ADR поимённо;
  единственное основание класса B — доступ к секрету `msklad-token` (`ADR-076 §1`), при выдаче
  действуют без изменений шесть оговорок `ADR-076 §5` и форма логирования `ADR-079 §6`.
- Новые открытые вопросы: нет
- Блокеры: мандат класса B на `PARITY-CHECK-SALES-RETURNS` — задача-специфичный ADR не издан
- updated_at: 2026-08-01
- обновил: генератор (сессия: PARITY-CHECK-SALES-RETURNS-GEN)

## NEW_DECISIONS
нет

## NEW_CONVENTIONS
нет

=== END SESSION ===
