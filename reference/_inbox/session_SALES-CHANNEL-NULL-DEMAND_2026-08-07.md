=== SESSION LOG · 2026-08-07 · SALES-CHANNEL-NULL-DEMAND ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-CHANNEL-NULL-DEMAND` — локализация точки потери `sales_channel_name` у 40 строк
  июля в `core.fact_sales_profit`. Исполнена БЕЗ брифа (`ADR-086 §1`), приёмка — строка
  `07_GAPS.md`.
- Сделано: read-only BQ-запросы (`reference/_scratch_SALES-CHANNEL-NULL-DEMAND_2026-08-07/`)
  подтвердили фактическое число строк (`40` / `821 523,00` KGS, совпадает с ожиданием
  `ADR-126 §2`), напечатали их построчно, распределение по суткам/контрагентам, проверили
  отсутствие меток периметра «Розница»/«Комиссия» (ветка `entity/demand` подтверждена) и
  прочитали `fetch_demands.py:104-106` с цитатой строк (`ADR-044`). Ключевая находка: у ВСЕХ
  40 строк `sales_channel_id` тоже `NULL`, не только `sales_channel_name` — это исключает
  механизм «промах кэша `channel_map`» и локализует потерю к пустому `sc_href` уже на входе
  парсинга (строка 104), то есть к ответу list-режима `entity/demand`, а не к нашему коду
  резолвинга справочника. Артефакт: `reference/sales_channel_null_demand_2026-08-07.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-CHANNEL-NULL-DEMAND_2026-08-07/step1_query.sh`
  → `run.log` (UTC-якорь + `gcloud auth list` первой и последней командой, `ADR-055 §4`;
  независимый `COUNT(*)` сверен с построчным выводом, `--max_rows=1000` против ожидаемых 40).
- Отклонения от плана: нет.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-CHANNEL-NULL-DEMAND`: READY → PARTIAL (механизм внутри кода локализован
  измерением — `channel_map`-кэш исключён, потеря сведена к пустому `salesChannel` на входе
  ответа МойСклада; корневая причина ответа источника — с нашей стороны неразличима, требуется
  живой `GET` одного документа, класс B, мандат не выдан, либо взгляд владельца на документ в
  интерфейсе).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-CHANNEL-NULL-DEMAND` — 40 строк июля с `NULL`-каналом подтверждены
    точно (`821 523,00` KGS), точка потери сужена измерением до пустого `salesChannel` в ответе
    `entity/demand` (не кэш справочника) — `reference/sales_channel_null_demand_2026-08-07.md`.
  - Где мы: счётчик реестра паритета `7/7`; из очереди `SALES-PERIMETER-QUEUE-ADJ` (`ADR-131`)
    остаются `SALES-DOCUMENT-OWNER-DEPLOY` (доработка `ADR-135 §4`/`ADR-136`) и
    `SALES-PERIMETER-PARITY-RECHECK` (мандат выдан, бриф готов, не взята); `SALES-JULY-RESIDUAL-SPLIT`
    и `SALES-CHANNEL-NULL-DEMAND` — обе READY-задачи `ADR-137` не взяты параллельно этой сессией.
  - Следующий шаг: `SALES-PERIMETER-PARITY-RECHECK` параллельно с доработкой
    `SALES-DOCUMENT-OWNER-INGEST`; по `SALES-CHANNEL-NULL-DEMAND` — решение владельца: живой `GET`
    (класс B, мандат не выдан) либо взгляд на документ в интерфейсе, форма фикса после этого.
  - Развилки на владельце: живой `GET` одного документа `entity/demand` (мандат класса B) ИЛИ
    владелец сам открывает пример-документ (`transaction_id d0b83f495f4580647aa83c403a7cce6e`,
    `2026-07-02`) в интерфейсе МойСклад и смотрит поле «Канал продаж».
  - Счётчик: пары реестра паритета 7/7 · Epic-1 очередь финиша — 2/4 задач `SALES-PERIMETER-QUEUE-ADJ`
    остаются открытыми; `SALES-JULY-ORACLE-ADJ` завела 2 задачи, 1 (`SALES-CHANNEL-NULL-DEMAND`)
    частично закрыта этой сессией, 1 (`SALES-JULY-RESIDUAL-SPLIT`) не взята.
- Подробности для модели: `channel_map` в `fetch_demands.py` строится ОДИН раз до цикла по
  документам (`_fetch_sales_channel_map`, строки 23-26/56) — это НЕ источник потери для этих 40
  строк (см. вывод ниже), но остаётся кандидатом для будущих расхождений другого вида (сам канал
  `sc_id` заполнен, а имя нет). Для настоящих 40 строк `sc_id` тоже `NULL` — значит `demand["salesChannel"]["meta"]["href"]`
  (строка 104 `fetch_demands.py`) был пуст на входе; ответ list-режима `GET entity/demand` не нёс
  поле `salesChannel` для этих документов, хотя источник (`reference/oracle_july_2026_2026-08-07.md §1`)
  подтверждает канал заполненным за июль. Различить «list-режим МойСклада систематически не отдаёт
  `salesChannel`» от «канал назначен ПОСЛЕ первой загрузки, повторный забор не задел эти сутки»
  нельзя без живого `GET` или взгляда владельца. Полный ход — `reference/sales_channel_null_demand_2026-08-07.md`.
- Новые открытые вопросы: нет (задача-строка `SALES-CHANNEL-NULL-DEMAND` уже существует, статус
  меняется на `PARTIAL` этим патчем, полный текст — `07_GAPS.md`).
- Блокеры: нет.
- updated_at: 2026-08-07
- обновил: исполнитель (сессия: SALES-CHANNEL-NULL-DEMAND)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
