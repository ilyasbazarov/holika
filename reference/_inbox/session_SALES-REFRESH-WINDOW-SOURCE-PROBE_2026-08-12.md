=== SESSION LOG · 2026-08-12 · SALES-REFRESH-WINDOW-SOURCE-PROBE ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-REFRESH-WINDOW-SOURCE-PROBE` — установить прямым запросом к МойСклад, живы ли
  в источнике 29 позиций, удалённых прогоном `job_id=1e698e3d-35c5-44c2-b90f-fbe852cf39a7`.
  Класс B, только чтение, мандат выдан поимённо владельцем
  (`reference/sales_refresh_window_rollback_probe_mandate_2026-08-12.md §3`).
- Сделано:
  - 16 пар (13 рабочих + 3 контрольных) запрошены формой ВПЕРЁД: `entity/demand` по широкому
    окну (`±2` дня от истинных бишкекских суток — обход незакрытого `Q-93`, точная граница
    `[D−1 18:00:00Z; D 18:00:00Z)` применена локально к полю `moment`, известному как UTC —
    `ADR-088 §1/§3`), совпавшие документы дочитаны `entity/demand/{id}/positions`, пересчитан
    `TO_HEX(MD5(demand_id|position_id))`.
  - Отклонение от буквы мандата объявлено ДО первого вызова (чат) и зафиксировано в артефакте
    §0: `expand=positions` заменён на подтверждённый в репо механизм (`fetch_demands.py`
    докстринг) — двухшаговый фетч без `expand`.
  - Положительный контроль (7 сирот `ADR-100 §1`) — все НЕ найдены. **PASS.**
  - Отрицательный контроль (25 заведомо живых позиций из `archive_106b.ndjson`) — все 25
    найдены (порог `≥10`). **PASS.**
  - Вердикт по 29: **2 строки** (`G1`, пара «К Глобал РФ Маркетплейсы», `2026-05-12`,
    `1 980 000,00` KGS) — живы в источнике, **дефект выгрузки (b1)**; **27 строк**
    (`427 220,42` KGS) — не найдены ни в одном документе пары, **удаление законно (b2)**.
  - `G9` (ранее неразличённая классом A) — подтверждена как ЗАКОННОЕ удаление: документов
    контрагента `356fb156…` за `2026-07-21` в источнике сейчас нет вовсе.
  - `G1` (ранее неразличённая) — опровергнута как «удаление законно»: документ `04235` жив,
    обе позиции присутствуют.
  - Полный поимённый список кандидатов на восстановление — артефакт §3.
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-SOURCE-PROBE_2026-08-12/`
  (`probe.py`, `run_probe.sh`, `run.log` — 0 ошибок/429/5xx, `date -u`+`gcloud auth list`
  первой и последней командой, `verdict.json`, 16 `raw_*_demand_headers.ndjson` +
  17 `raw_*_positions_*.ndjson` как провенанс). Секрет `msklad-token` не печатался (проверено
  `grep -i bearer` по логу и артефакту — 0 совпадений значения токена).
- Отклонения от плана: два, оба объявлены до исполнения и в артефакте §0 — замена
  `expand=positions` на двухшаговый фетч; обход незакрытого `Q-93` широким окном запроса с
  точной локальной фильтрацией. Форма проверки (пара → документы → позиции → хеш → вхождение)
  не изменена ни в одном из двух случаев.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-REFRESH-WINDOW-SOURCE-PROBE`: READY → DONE. Все 29 строк квалифицированы прямым
  запросом к источнику; оба контроля прошли; вердикт не оставляет неразличённых гипотез.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-REFRESH-WINDOW-SOURCE-PROBE` исполнена — прямой запрос к МойСклад по 16
    парам квалифицировал все 29 строк: 2 строки (`1 980 000,00` KGS, `G1`) дефект выгрузки, 27
    строк (`427 220,42` KGS) законное удаление; оба контроля (положительный/отрицательный)
    прошли; артефакт `reference/sales_refresh_window_source_probe_2026-08-12.md`.
  - Где мы: остаток `29` строк / `2 407 220,42` KGS полностью квалифицирован; из всего боевого
    прогона (`36` строк) осталось восстановить ровно `2` строки / `1 980 000,00` KGS.
  - Следующий шаг: `SALES-REFRESH-WINDOW-RESTORE` (селективное восстановление 2 строк `G1`,
    класс B, мандат отдельно) параллельно с `SALES-REFRESH-WINDOW-COMPLETENESS-CHECK` (проверка
    полноты выгрузки как предусловие повторного деплоя `cf-facts`, класс A) — обе ставит
    архитектор.
  - Развилки на владельце: нет.
  - Счётчик: см. предыдущий стенд-ап (настоящая сессия его не считает — читатель числа не
    менял).
- Подробности для модели: `SALES-REFRESH-WINDOW-SOURCE-PROBE` закрыта фактом прямого запроса,
  не квалификацией по прокси. Метод (широкое окно + точная локальная фильтрация по known-UTC
  `moment`) обошёл незакрытый `Q-93`, не закрыв его — `Q-93` остаётся открытым для любой
  будущей задачи, где ширина окна не так дёшева. Второе отклонение (замена `expand=positions`)
  стоит зафиксировать как общий факт о `entity/demand`: `expand` в списочном режиме этого
  эндпоинта неприменим, что уже было известно `fetch_demands.py`, но не было явно в
  `02_ERP_CONTRACTS §поведение API` — кандидат на перенос туда отдельной задачей.
- Новые открытые вопросы: нет новых. `Q-93` не закрывается настоящей сессией (см. выше).
- Блокеры: нет.
- updated_at: 2026-08-12
- обновил: executor (сессия: SALES-REFRESH-WINDOW-SOURCE-PROBE)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет (адъюдикация квалифицирующего вердикта и постановка шагов 3/4 — решение архитектора, не
  этой сессии; настоящая сессия только поставляет факт).

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет.

=== END SESSION ===
