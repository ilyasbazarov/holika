=== SESSION LOG · 2026-08-09 · INGEST-MOMENT-ZONE-FIX-DEPLOY ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `INGEST-MOMENT-ZONE-FIX-DEPLOY` — деплой патча зоны времени ингеста (мандат `ADR-147`)
- Сделано:
  - Объект A: `cf-facts` — три файла (`helpers.py`, `fetch_returns.py`, `fetch_purchases.py`)
    перенесены по diff в код-репо, задеплоены (`cf-facts-00010-mog` → `cf-facts-00011-mab`),
    read-back побайтовый, слито в `master` (коммит `508ca64`).
  - Функциональная проверка объекта A: `mode=returns` (window_days=100) и `mode=purchases`
    (полный рефреш) прогнаны на проде, подтверждены read-only по `_loaded_at` целевых таблиц
    (клиентский `gcloud` таймаутился на `purchases` — слепой retry не делался, факт проверен
    иначе).
  - Объект B: `transferConfig sq_marts_expenses` (`6a22a243-…`) — две строки `CAST(...AS DATE)`
    → `DATE(..., 'Asia/Bishkek')`, read-back побайтовый, ручной прогон (`bq mk --transfer_run`)
    подтверждён `SUCCEEDED`.
  - Замер «до/после» по трём сверенным парам реестра паритета (Условие 2 мандата): две пары
    (`marts.expenses` май-2026, `core.fact_returns` май-2026) сошедшиеся, без изменений.
- Команды/логи ключевые: `reference/_scratch_INGEST-MOMENT-ZONE-FIX-DEPLOY_2026-08-09/step1…step10_run.log`;
  полный разбор — `reference/ingest_moment_zone_fix_deploy_2026-08-09.md`,
  `reference/code/cf-facts/MANIFEST.md` §«Деплой `cf-facts` — дата документа по Asia/Bishkek…».
- Отклонения от плана:
  - Третья пара реестра (`core.fact_purchases`, `ADR-124`, `Δ=0`) не сверена дословно тем же
    позиционным запросом, которым её измеряла `PARITY-STOCK-INTRANSIT-RECHECK` — этого запроса
    не было в контексте сессии. Названо `CONTEXT GAP`, не додумано.
  - `Q-101` не закрыт этим деплоем (рекомендация мандата `§5(1)` дополнить тем же деплоем не
    выполнена — владелец подтверждал объём в три файла, расширение молча не делалось).
  - В процессе была допущена и исправлена ошибка: первая правка `MANIFEST.md` ушла в основное
    дерево вместо рабочего (`ADR-083 §6`) — откатана в main, перенесена в своё дерево до коммита.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `INGEST-MOMENT-ZONE-FIX, деплой`: `выдан поимённо (ADR-147)` → `DONE` — оба объекта
  задеплоены и функционально проверены, `reference/ingest_moment_zone_fix_deploy_2026-08-09.md`.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: `INGEST-MOMENT-ZONE-FIX` задеплоена целиком (`cf-facts` + `sq_marts_expenses`,
    мандат `ADR-147`) — `reference/ingest_moment_zone_fix_deploy_2026-08-09.md`.
  - Где мы: дефект зоны времени ингеста (`Q-77`/`ADR-088 §4`) устранён в коде и в SQL марта
    расходов; численный сдвиг в мае-2026 (зона паритета) не обнаружен ни по одной из двух
    проверенных пар реестра — третья (`fact_purchases`, `ADR-124`) ждёт точной сверки архитектором.
  - Следующий шаг: точная сверка пары `core.fact_purchases`/`ADR-124` тем же позиционным запросом,
    что `PARITY-STOCK-INTRANSIT-RECHECK` (класс A); параллельно — прежние классы A из очереди
    (`DQ-GATE-FAIL-OPEN-FIX` подготовка, `DQ-FRESHNESS-COVERAGE` подготовка, `INFRA-FACTS-LANDING`).
  - Развилки на владельце: нет.
  - Счётчик: пары реестра 7/7 (без изменений этой сессией) · Epic-1 без изменений счётчика фаз.
- Подробности для модели: `INGEST-MOMENT-ZONE-FIX` полностью закрыта (подготовка + деплой).
  Ревизия `cf-facts` теперь `cf-facts-00011-mab` (было `cf-facts-00010-mog`) — следующая сессия,
  берущая живой снапшот `cf-facts`, обязана снимать его заново, не полагаться на предыдущий
  `MANIFEST.md` как на текущее состояние без свежего `describe`. `transferConfig sq_marts_expenses`
  обновлён `updateTime: 2026-08-09T13:27:47Z` — прежний текст (родитель `a45837b`) остаётся
  провенансом отката. Открыт хвост: точная сверка `core.fact_purchases`/`ADR-124` — см.
  `reference/ingest_moment_zone_fix_deploy_2026-08-09.md §Что НЕ сделано`.
- Новые открытые вопросы:
  - `PURCHASES-INTRANSIT-RECHECK-VERIFY` — подтвердить, что деплой `INGEST-MOMENT-ZONE-FIX` не
    сдвинул пару «Заказы поставщикам в пути» (`ADR-124`, `Δ=0`), тем же позиционным запросом,
    что использовала `PARITY-STOCK-INTRANSIT-RECHECK`; без этого счётчик реестра `7/7` не
    подтверждён числом на новом коде, только предположением по аналогии с двумя другими парами.
- Блокеры: нет.
- updated_at: 2026-08-09
- обновил: executor (сессия: INGEST-MOMENT-ZONE-FIX-DEPLOY)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет — деплой не адъюдицирует правило-мост, только исполняет уже выданный мандат (`ADR-147`);
  адъюдикация «пара сошлась/не сошлась» по третьей паре остаётся за архитектором
  (`ADR-076 §4`, `ADR-091 §8`), этой сессией не выносится.

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
