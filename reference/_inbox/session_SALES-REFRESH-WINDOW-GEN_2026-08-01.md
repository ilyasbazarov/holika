=== SESSION LOG · 2026-08-01 · SALES-REFRESH-WINDOW-GEN ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-REFRESH-WINDOW-GEN` — генерация брифа подготовки (класс A) для `SALES-REFRESH-WINDOW`:
  варианты и текст патча снапшота `cf-facts`, закрывающие структурную находку `ADR-097 §5` (окно
  обновления 7/90 суток не покрывает якорь паритета `2026-05-01`, режима досева у продаж нет)
- Сделано: прочитан обязательный контекст (`_METHOD`, `00_CHARTER`, `04_ROADMAP`, `05_CONVENTIONS`,
  `06_INDEX`, `07_STATE`, `_GENERATOR.md`, `08_TASK_BRIEF_TEMPLATE.md`); проверка `ADR-054` по каждому
  файлу — совпало; класс (A) и параллель (нет) взяты дословно из `07_STATE §Мандат Claude Code: класс
  задач`, строка «SALES-REFRESH-WINDOW, подготовка» (~строка 344); поднят полный текст `ADR-097 §5` из
  `06_DECISIONS_LOG.md` и строка GAP-реестра `SALES-REFRESH-WINDOW` (~строка 433) — три требуемых
  варианта процитированы в бриф дословно; прочитан снапшот `reference/code/cf-facts/` (`config.py`,
  `main.py`, `bq_ops.py`, `fetch_demands.py`, `MANIFEST.md`) — найдено и в бриф внесено точное место
  ловушки задвоения (`bq_ops.py`, `ON`-клауза `_build_merge_sql`: `T.transaction_date >= DATE_SUB(
  CURRENT_DATE(), INTERVAL {window_days} DAY)`, тот же `window_days` несёт и окно выборки, и окно
  `MERGE`); бриф положен в `briefs/SALES-REFRESH-WINDOW.md`
- Команды/логи ключевые: `bash tools/session_status.sh` (старт, RC 0, дерево чисто); чтение файлов с
  диска, живых команд к облаку/МойСкладу не исполнялось (роль генератора не читает секреты, задача
  подготовки текста тоже их не требует)
- Отклонения от плана: нет — бриф собран прецедентно по строке мандата и по `ADR-097 §5`, задача не
  требует новой архитекторской адъюдикации (постановка уже принята `accepted`). В бриф явно внесено
  предупреждение не доверять снапшоту `cf-facts` как заведомо равному текущей задеплоенной ревизии:
  `MANIFEST.md` снят обходным путём (`gcloud functions list`/`run services describe`, не `functions
  describe`, тот вернул `403`), и для `cf-facts` (в отличие от `cf-finance`, `Q-95`/`DEPLOY-REVISION-PROBE`)
  в репо нет отдельного замера равенства снапшот↔деплой — патч готовится ПРОТИВ СНАПШОТА, это явно
  зафиксировано как ограничение метода, а не скрыто

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-REFRESH-WINDOW, подготовка`: разгейчен (постоянный мандат) → бриф готов
  (`briefs/SALES-REFRESH-WINDOW.md`), не взят
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: бриф `SALES-REFRESH-WINDOW` (подготовка, класс A) собран (`briefs/SALES-REFRESH-WINDOW.md`) — три варианта патча снапшота `cf-facts` под ловушку задвоения `ADR-097 §5`, готов к исполнению отдельной сессией
  - Где мы: финиш Epic-1 по-прежнему упирается в выбор моста продаж (`ADR-097 §6`, a/b/c); различитель `Q-98` добыт, адъюдикация H1–H4 не проведена; подготовка `SALES-REFRESH-WINDOW` больше не блокер для старта — бриф есть
  - Следующий шаг: архитекторская адъюдикация H1–H4 по результату шага 1 `PARITY-SALES-DISCRIMINATE`; параллельно исполнение `CODE-REPO-STANDUP` шаг 2a (класс A, бриф готов), исполнение `SALES-REFRESH-WINDOW` подготовки (класс A, бриф готов), тексты Looker Studio
  - Развилки на владельце: объект паритета продаж (a/b/c, `ADR-097 §6`) — после адъюдикации H1–H4, не раньше; мандат класса B на шаг 2 `PARITY-SALES-DISCRIMINATE` — после решения архитектора, нужен ли он ещё
  - Счётчик: пары реестра 1/7 сходятся · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
- Подробности для модели: Бриф `SALES-REFRESH-WINDOW.md` требует трёх вариантов текста патча (не
  выбора одного): (1) досев отдельным режимом с раздельными окнами выборки/`MERGE` — новый параметр
  вместо совмещённого `window_days`; (2) разовый пересев мая с явными датами вместо `DATE_SUB(
  CURRENT_DATE(), …)`; (3) плашка «датированный снимок» без правки кода. Для вариантов 1–2 бриф
  требует сохранить форму явного `INSERT (колонки)` (`ADR-030`, C1) и явно назвать открытым вопросом
  источник COGS (`cogs_source`) для диапазона шире текущих 7/90 суток — текущая логика (`window_days
  >= 90` → `STG_BYVARIANT`, иначе `CORE_BYVARIANT_BCK`) не рассчитана на произвольный исторический
  диапазон, генератор это значение не домысливает. Применение варианта 3 (плашка в
  `02_ERP_CONTRACTS.md`/`reference/parity_registry.md`) явно вынесено вне scope брифа — там готовится
  только текст-кандидат, не правка целевых файлов. Выбор одного из трёх вариантов и деплой
  (`07_STATE.md:345`, класс B, мандат НЕ выдан, гейтится `ADR-065`) в бриф не входят.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-01
- обновил: generator (сессия: SALES-REFRESH-WINDOW-GEN)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
