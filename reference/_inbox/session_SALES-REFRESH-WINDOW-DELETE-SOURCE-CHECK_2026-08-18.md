=== SESSION LOG · 2026-08-18 · SALES-REFRESH-WINDOW-DELETE-SOURCE-CHECK ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-REFRESH-WINDOW-DELETE-SOURCE-CHECK` — живое чтение МойСклада по 12 парам остатка
  различителя удалённых строк, закрытие вопроса «удаление — дефект или отражение источника»
- Сделано: положительный контроль (пара маркетплейса, `2026-05-12`) — ПРОЙДЕН, обе строки найдены
  тем же кодом. Все `27` строк остатка проверены по форме мандата (`entity/demand` + при
  необходимости `entity/retaildemand`/`entity/commissionreportin`) — все `27` НЕ НАЙДЕНЫ ни в одном
  из трёх эндпоинтов на дату+контрагент. Разложение по `27` строкам поимённое, вердикт по каждой —
  документ в МойСкладе сейчас отсутствует. Артефакт: `reference/sales_refresh_window_delete_source_check_2026-08-18.md`
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-DELETE-SOURCE-CHECK_2026-08-18/`
  (`run_check.sh`+`.log` — первый пакет, `49` вызовов остановлен на потолке `40` после `40`-го;
  `run_check_resume.sh`+`.log` — второй пакет, `9` вызовов, докрыл оставшиеся `5` дат;
  `result_final.json` — итоговое состояние)
- Отклонения от плана: потолок `40` вызовов был достигнут ДО закрытия всех `27` строк (после `27`
  вызовов демонд-ветки + `13` вызовов ветки отрицательного результата на `6` из `11` дат). Скрипт
  остановился сам, не превысив потолок; статус доложен владельцу отдельным сообщением; получено
  дословное «да, продолжай до закрытия всех 27» — второй пакет (`9` вызовов) исполнен тем же
  методом, тем же кодом. Итого `49` read-only вызовов за оба пакета, оба пакета объявлены владельцу
  до исполнения (первый — предусловие (iii) брифа, второй — по факту остановки на потолке).
  Первый скрипт не успел записать `result.json` (`sys.exit` внутри `api_get` на потолке `40`) —
  состояние первого пакета восстановлено в скрипте продолжения из `run_check.log` (закоммичен,
  тот же прогон, значения сверяемы построчно), не из памяти модели.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-REFRESH-WINDOW-DELETE-SOURCE-CHECK`: мандат ВЫДАН → ИСПОЛНЕНА. Вердикт: все `27`
  строк остатка (`427 220,42` KGS) НЕ НАЙДЕНЫ в источнике сейчас ни в `entity/demand`, ни в
  `entity/retaildemand`, ни в `entity/commissionreportin` — документы, породившие эти строки, в
  МойСкладе больше не существуют. Удаление отразило текущее состояние источника, не было потерей
  данных на уровне «документ пропал из нашей стороны, а не из МойСклада». Положительный контроль
  (пара маркетплейса) пройден тем же прогоном — метод исправен.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-REFRESH-WINDOW-DELETE-SOURCE-CHECK` закрыта — все `27` строк остатка
    (`427 220,42` KGS) не найдены живым чтением МойСклада ни в одном из трёх эндпоинтов, документы
    в источнике не существуют; артефакт `reference/sales_refresh_window_delete_source_check_2026-08-18.md`.
  - Где мы: `4` строки списка закрытия из `9` (закрылась строка различителя удалённых строк целиком).
  - Следующий шаг: строка `1` (ждёт прогон `2026-09-29`), строка `6` (два невыданных мандата),
    строка `5` (пакет передачи) — по доске `reference/close_list_board_<date>.md`.
  - Развилки на владельце: два мандата класса B строки `6`; ХОЛД формы показа возвратов
    `271 476,66`; решение — восстанавливать ли `27` строк (отдельный мандат, эта сессия не
    восстанавливала).
  - Счётчик: строк списка закрытия `4` из `9`; под-задач без изменений; срок `2026-09-29`, шесть
    недельных прогонов.
- Подробности для модели: `27` строк остатка различителя удалённых строк — НЕ дефект выгрузки,
  документы в МойСкладе отсутствуют сейчас во всех трёх эндпоинтах периметра продаж
  (`entity/demand`, `entity/retaildemand`, `entity/commissionreportin`). Полный расчёт, таблица по
  строкам и провенанс — `reference/sales_refresh_window_delete_source_check_2026-08-18.md`. Метка
  «удалённые» у `deleted_27_2026-08-18.csv` теперь верифицирована живым чтением (была неверифицирована
  до этой сессии, `DELETE-STAGE3-ADJ §3(iii)`). Различитель `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`
  может закрываться целиком следующим проходом сборки — все три критерия расследования (архив +
  постраничный предел + метка «удалённые») теперь проверены, последний — этой сессией.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-18
- обновил: исполнитель (сессия: SALES-REFRESH-WINDOW-DELETE-SOURCE-CHECK)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
