# FILE: session_SALES-REFRESH-WINDOW-DELETE-ADJ_2026-08-12.md

=== SESSION LOG · 2026-08-12 · SALES-REFRESH-WINDOW-DELETE-ADJ ===

> **Сборщику — порядок обработки (`_ASSEMBLER.md §5 п.3`, зависимость по содержанию):** блок
> `session_SALES-REFRESH-WINDOW-DEPLOY_2026-08-12.md` обрабатывается ПЕРВЫМ, этот — вторым.
> Этот блок адъюдицирует результат той сессии и переписывает часть её выводов.

## SESSION_LOG

- Задача: `SALES-REFRESH-WINDOW-DELETE-ADJ` — адъюдикация ненулевого третьего числа приёмки
  деплоя патча варианта 1. Архитектор, класс A, без брифа (`ADR-086 §1`). База `483eba6`,
  якорь `2026-08-12T05:56:38Z`. Дерево `worktrees/SALES-REFRESH-WINDOW-DELETE-ADJ`, ветка
  `s/SALES-REFRESH-WINDOW-DELETE-ADJ`. Строка задачи заводится этим блоком (`ADR-086 §3`).
- **Стоп-условие старта названо и обойдено по прямому запросу владельца:** `session_status.sh`
  вернул `RC 1` (неслитая ветка `s/SALES-REFRESH-WINDOW-DEPLOY`). Инцидент открыт, прецедент
  обхода — `SALES-REFRESH-WINDOW-ROLLBACK-ADJ` (2026-08-11). Ветка исполнителя не тронута.
- Сделано:
  - **Опознаны `7` из `36` удалённых строк.** Сверка списка `…deploy_v2…§8` с `ADR-100 §1`
    дала совпадение ТРЁХ независимых признаков одновременно: число строк `7`; сумма
    `1 596 538,5585 + 33 701,25 = 1 630 239,8085` против записанных `1 630 239,81` (разница с
    `1 630 240,60` есть известные `0,79`, `ADR-101 §1`); подпись по суткам `2 + 2 + 3` на
    `2026-05-07`/`05-09`/`05-26` — те же три даты, что записаны в `ADR-100 §1` как независимый
    признак. Это опознание, не правдоподобие. По этим семи строкам удаление есть исполнение
    назначенной работы, а не дефект.
  - **Признана мис-специфицированной приёмка, написанная мной** (`…deploy_mandate…§4` п.3,
    «удалённых строк оптовой торговли `0`»). Узкая форма фикса (`ADR-144 §8`) есть ровно ветка
    удаления; критерий «удалено ноль» требует от исправного патча бездействия и совместим с
    исправностью только при «за 106 суток из источника не исчезло ничего», тогда как проект
    измерил семь исчезновений за один май. Число перенесено мной из приёмки ОТКАТА `ADR-161`,
    где оно означало «ущерба нет». Класс — `05_CONVENTIONS` Часть I ★ «Критерий приёмки
    выводится из формулы, не из свойства данных» (`ADR-075 §3`). Верный критерий: «каждая
    удалённая строка отсутствует в источнике» — дословно содержание `Q-107` (`proposed`),
    написанного, но не применённого к тексту приёмки в том же документе.
  - Остановка исполнителя признана правильной: мандат исполнен буквально, приёмка не
    переписана под факт (`ADR-047`). Задержка отката решением владельца сохранила
    доказательную базу, без которой опознание было бы невозможно.
  - **Названа ошибка в оценке срочности, сообщённой владельцу:** снимок
    `core.fact_sales_profit_snap_20260811_163306` есть ТАБЛИЦА, а не окно, и не истекает
    (удалять её запрещает `ADR-161`); истекает только дублирующий её time travel (`168` ч).
    Единственный реальный срок — недельный конвейер `msklad-pipeline-weekly`, `0 1 * * 0`
    (`11_INFRA_FACTS.md:27`), то есть `2026-08-16T01:00Z`.
  - **Названо, что откат ДАННЫХ безопасным действием по умолчанию не является:**
    восстановление `36` строк вернёт семь подтверждённых сирот, то есть регресс пары
    «Продажи» (`ADR-119`, реестр `7/7`, финиш `ADR-140`). Единственное расхождение с
    исполнителем по существу; его фактура принята без правок. Откат КОДА этой оговоркой не
    затронут.
  - **Спроектирован различитель остатка (`29` строк, `2 407 220,42` KGS), весь класса A.**
    Уточнение формы: точного разреза по документу в `core.fact_sales_profit` нет —
    `transaction_id = TO_HEX(MD5(CONCAT(demand_id,'|',position_id)))` (`bq_ops.py:261`),
    обратно `demand_id` не достаётся. Решающим инструментом объявлен неизменяемый GCS-архив
    сырого ответа источника за 106-суточный прогон (прецедент `ADR-143`), а не снимок нашей
    таблицы: снимок таблицы есть снимок НАШЕЙ стороны, и подменять им снимок ИСТОЧНИКА
    запрещает `00_CHARTER §главный принцип`. Три ступени — §4a артефакта.
  - Владелец выбрал вариант 3 (различитель до отката) с жёсткой страховкой по времени
    (чат 2026-08-12).
- Команды/логи ключевые: `bash tools/session_status.sh` (`RC 1`, названо выше);
  `reference/_scratch_SALES-REFRESH-WINDOW-DELETE-ADJ_2026-08-12/orphan_match.py` +
  `orphan_match_run.log` — пересчёт трёх признаков опознания, все три `СОВПАЛО`;
  группировка остатка по паре дата+контрагент (`13` групп, `5` многострочных: `8`/`6`/`3`/`2`/`2`);
  `grep` расписания `msklad-pipeline-weekly`; чтение `bq_ops.py:248-300` (состав `MERGE`,
  формула `transaction_id`) и `bq_ops.py:49-71` (`STAGING_SCHEMA`, наличие `demand_id`).
  Облачных вызовов сессия не делала.
- Отклонения от плана: нет.

## STATE_PATCH

- Задача `SALES-REFRESH-WINDOW-DELETE-ADJ`: (не существовала) → DONE. Заводится и архивируется
  ОДНИМ проходом сборки (прецедент `INVOICES-STALENESS-PROBE`, `FINISH-QUEUE-ADJ`).
- Заводится `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE` — `READY`, класс A, гейтов нет.
  Вопрос: исчезли ли `29` неатрибутированных удалённых строк из источника (удаление корректно)
  или потеряны выгрузкой на длинном окне (дефект патча). Форма — три ступени §4a артефакта.
  Пишет: `reference/sales_refresh_window_delete_discriminate_<date>.md`,
  `reference/_scratch_SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE_<date>/`.
  Строка мандата заводится тем же коммитом (`ADR-090 §1`).
- Строка `SALES-REFRESH-WINDOW` (`07_GAPS.md`, индекс `07_STATE.md`): дописка — «Деплой
  варианта 1 исполнен `2026-08-11`, ревизия `cf-facts-00014-doh`, трафик `100 %`; откат НЕ
  исполнен по решению владельца. Боевой прогон (`106` суток) удалил `36` строк, из них `7`
  опознаны как сироты `ADR-100 §1` по трём совпавшим признакам — по ним патч сработал штатно.
  Третье число приёмки признано мис-специфицированным (`DELETE-ADJ`), верный критерий —
  «каждая удалённая строка отсутствует в источнике». Остаток `29` строк / `2 407 220,42` KGS
  разбирается задачей `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`. Предохранитель периметра
  сработал штатно и отказал в `MERGE` периметра (`500`) — периметрийные строки не тронуты.
  Ветка код-репо `…-v2` (`9ae9a84`) НЕ слита. Срок: недельный конвейер `2026-08-16T01:00Z`.»
- Строка мандата `SALES-REFRESH-WINDOW, деплой` (`07_STATE.md §Мандат Claude Code`): «ВЫДАН
  поимённо ЗАНОВО» → «ВЫДАН, ИСПОЛНЕН, приёмка НЕ подтверждена — третье число мис-специфицировано
  (`DELETE-ADJ`); откат приостановлен решением владельца; слияние ветки в `master` запрещено до
  закрытия `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`».
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: патч варианта 1 задеплоен и отработал широкий прогон; из `36` удалённых строк
    `7` опознаны как подтверждённые сироты, ради которых задача и заводилась, а условие приёмки
    «удалено ноль» признано мис-специфицированным архитектором.
  - Где мы: патч, судя по опознанию, работает — но `29` удалённых строк на `2 407 220,42` KGS
    ещё не объяснены; откат не исполнен, данные не тронуты, снимок восстановления не истекает.
  - Следующий шаг: `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE` (класс A, три ступени по
    GCS-архиву источника), затем решение о закрытии деплоя либо об откате кода.
  - Развилки на владельце: мандат класса B на единичные `GET` к складу по паре маркетплейса —
    только если различитель дойдёт до третьей ступени; апрув `Q-107` (`proposed`).
  - Счётчик: строк списка закрытия осталось `8` из `9`.
- Подробности для модели:
  - **Семь сирот `ADR-100 §1` УДАЛЕНЫ из ядра `2026-08-12T05:15:55Z`** прогоном
    `job_id=1e698e3d-35c5-44c2-b90f-fbe852cf39a7`. Опознание — три признака сразу (число, сумма
    до копейки, подпись `2+2+3` по суткам). Любая будущая сверка пары «Продажи» обязана это
    учитывать: разложение `ADR-119` (`Δ = M1 − |M2| + ε`) со стороны `M1` теперь пусто, потому
    что сироты физически убраны, а не только объяснены. Восстановление данных из снимка ВЕРНЁТ
    их и откатит пару назад.
  - **`transaction_id` в `core.fact_sales_profit` есть `TO_HEX(MD5(CONCAT(demand_id,'|',position_id)))`**
    (`bq_ops.py:261`) — идентификатора документа в таблице НЕТ, обратно из хеша он не
    достаётся. Любой разрез «по документу» на стороне ядра есть приближение через пару
    `transaction_date` + `agent_id`. Точный разрез берётся только из GCS-архива сырого ответа
    (там есть `demand_id` и `position_id`) либо из staging до её перезаписи.
  - **`stg_msklad.fact_sales_staging` перезаписывается `WRITE_TRUNCATE` каждым прогоном и
    run_id-скоупа не имеет** — staging широкого прогона затирается ближайшей часовой загрузкой
    (наблюдено дважды `2026-08-11`/`2026-08-12`). Диагностика, опирающаяся на staging, обязана
    сниматься в тот же час либо идти по GCS-архиву.
  - **Снимок `core.fact_sales_profit_snap_20260811_163306` не истекает** (это таблица), time
    travel датасета `core` — `168` ч (замерено). Оценка «оба пути восстановления ограничены по
    времени» неверна и в решениях не используется.
- Новые открытые вопросы: `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE` (см. выше).
- Блокеры: нет.
- updated_at: 2026-08-12
- обновил: архитектор (сессия: SALES-REFRESH-WINDOW-DELETE-ADJ)

## NEW_DECISIONS

- ADR-0XX: Третье число приёмки деплоя `SALES-REFRESH-WINDOW` мис-специфицировано — критерий
  требовал от исправной ветки удаления бездействия; семь из тридцати шести удалённых строк
  опознаны как сироты `ADR-100 §1` тремя совпавшими признаками; откат данных объявлен НЕ
  нейтральным действием; различитель остатка опирается на GCS-архив источника, а не на снимок
  нашей таблицы

  Контекст: деплой патча варианта 1 исполнен `2026-08-11`, боевой прогон на окне `106` суток
  удалил `36` строк, из них `28` канала «оптовая торговля». Мандат
  (`…deploy_mandate…§4` п.3) предписывал немедленный откат при любом ненулевом значении.
  Владелец откат приостановил и запросил адъюдикацию.

  Решение:
  §1. **Опознание.** `7` из `36` удалённых строк есть в точности сироты `ADR-100 §1`:
  совпали число (`7`), сумма (`1 630 239,8085` против `1 630 239,81`) и подпись по суткам
  (`2+2+3` на `2026-05-07`/`05-09`/`05-26`). По ним удаление есть исполнение назначенной
  работы; дефектом не является.
  §2. **Критерий приёмки признан неверным, авторство ошибки — архитектор.** «Удалено ноль»
  выведено из свойства данных под другой формулой (приёмка ОТКАТА `ADR-161`) и перенесено в
  контекст работающей ветки удаления, где то же число означает «работа не сделана». Класс —
  `05_CONVENTIONS` Часть I ★ (`ADR-075 §3`). Верный критерий: **каждая удалённая строка
  отсутствует в источнике**.
  §3. **Остановка исполнителя правильна.** Мандат исполнен буквально, приёмка не переписана
  под факт (`ADR-047`); задержка отката сохранила доказательную базу.
  §4. **Откат данных не нейтрален** — вернёт семь подтверждённых сирот и откатит назад пару
  «Продажи» (`ADR-119`). Откат КОДА нейтрален, обратим и проверен исполнением.
  §5. **Срочность переоценена:** снимок-восстановление есть таблица и не истекает; единственный
  реальный срок — недельный конвейер `2026-08-16T01:00Z` (`0 1 * * 0`, `11_INFRA_FACTS.md:27`).
  §6. **Различитель остатка (`29` строк) — класса A и опирается на GCS-архив сырого ответа
  источника** (прецедент `ADR-143`), не на снимок нашей таблицы: подмена снимка источника
  снимком собственной таблицы запрещена `00_CHARTER §главный принцип`. Три ступени — §4a
  артефакта; живой `GET` к складу (класс B) нужен только на третьей и только для пары
  маркетплейса.
  §7. **Страховка по времени:** если вопрос не закрыт к `2026-08-15T12:00Z` — откат КОДА без
  дальнейшего обсуждения; данные не трогаются ни при одном исходе.
  §8. Вариант 3 (различитель до отката) выбран владельцем (чат 2026-08-12).

  Последствия:
  - [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — заводится
    `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE` (`READY`); строка `SALES-REFRESH-WINDOW`
    дописывается по тексту `STATE_PATCH`.
  - [сейчас] `07_STATE.md §Мандат Claude Code` — строка `SALES-REFRESH-WINDOW, деплой`
    переписывается; заводится строка `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE` (класс A,
    параллель нет, мандат постоянный) и строка `SALES-REFRESH-WINDOW-DELETE-ADJ`
    (архивируется тем же проходом), парность `ADR-090 §1`.
  - [сейчас] `07_STATE.md §Подробности для модели` — четыре абзаца из `STATE_PATCH`.
  - [задачей SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE] закрытие вопроса по `29` строкам и
    переписывание третьего числа приёмки на верную формулировку §2.
  - [без правок] данные `core.fact_sales_profit` — не восстанавливаются и не трогаются;
    снимок `snap_20260811_163306` не удаляется (`ADR-161`).
  - [без правок] ветка код-репо `deploy/cf-facts-2026-08-11-refresh-window-v2` — в `master`
    не сливается до закрытия различителя.
  - [отдельным коммитом] `05_CONVENTIONS.md` — правка списка проверки готовности деплоя по
    `Q-107` приходит после апрува владельца (`ADR-081 §8`), этим решением не вводится.

  Статус: accepted (архитектор §1-§7; §8 — владелец, чат 2026-08-12)

## NEW_CONVENTIONS

- нет

=== END SESSION ===
