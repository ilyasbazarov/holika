=== SESSION LOG · 2026-08-12 · SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE` — различитель остатка 29 неатрибутированных
  удалённых строк боевого прогона `SALES-REFRESH-WINDOW-DEPLOY` (`job_id=1e698e3d…`).
- Сделано: исполнен точный трёхступенчатый различитель `…delete_adj…§4a` классом A целиком
  (ступени 0-2), плюс независимая перекрёстная проверка снимок/текущая таблица по всем `13`
  группам. Ступень 0: GCS-архив сырого ответа `weekly_run_salesrefreshwindowdeploy106b.ndjson.gz`
  найден, читается, `generation=1786510926867012`, создан `05:02:06Z` — до MERGE `05:15:55Z`.
  Ступень 1: пересчитан `transaction_id` по всем `7690` записям архива — ни один из `29`
  удалённых идентификаторов не найден. Ступень 2: по `agent_id`+дата (переведённой в
  `Asia/Bishkek` тем же правилом, что `bq_ops.py:230`) — в `11` из `13` групп (`19` строк,
  `427 220,42` KGS) документ контрагента в архиве ПРИСУТСТВУЕТ, конкретная позиция нет →
  подтверждённая потеря позиций на длинном окне, дефект. В `2` группах (`10` строк,
  `2 007 019,20` KGS: пара маркетплейса `1 980 000,00` + контрагент `356fb156` `27 019,20`)
  архив не содержит контрагента за дату вовсе — гипотеза (a) правдоподобна, но не различена
  классом A. Перекрёстная проверка (снимок vs текущая по `13` группам) дала численно
  ИДЕНТИЧНЫЙ разрез — расхождения между методами нет.
- Команды/логи ключевые: `step0_find_archive.sh` (Ступень 0), `step1_recompute_hash.sh` +
  `step1_check.py` (Ступень 1 + наивная 2), `step1c_refine_tz.py` (Ступень 2, Bishkek-дата),
  `step_crosscheck.sql`/`step2_crosscheck.sh` (перекрёстная проверка). Все логи и промежуточные
  файлы — `reference/_scratch_SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE_2026-08-12/`.
- Отклонения от плана: ступень 3 (класс B) не исполнялась — вне мандата этой сессии, секрет
  `msklad-token` не читался, живых вызовов к `api.moysklad.ru` не было. Найдена ВТОРАЯ группа
  (`356fb156`, `2026-07-21`, `27 019,20` KGS), нуждающаяся в ступени 3 — исходный текст
  `…delete_adj…§4a` п.3 называл только пару маркетплейса.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`: READY → частично закрыта (классы A
  ступеней 0-2 исполнены полностью; остаток — ступень 3, класс B, мандат не выдан)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: трёхступенчатый различитель `29` неатрибутированных строк исполнен классом A
    целиком; `19` строк (`427 220,42` KGS, `11` групп) — подтверждённый дефект выгрузки на
    длинном окне (документ в архиве есть, позиция нет); `10` строк (`2 007 019,20` KGS, `2`
    группы, включая пару маркетплейса) — документ в архиве отсутствует целиком, не различено.
  - Где мы: дефект патча на длинном окне ФАКТИЧЕСКИ ПОДТВЕРЖДЁН для большинства остатка —
    это меняет картину деплоя `SALES-REFRESH-WINDOW` (сейчас: «приёмка не подтверждена, третье
    число мис-специфицировано») на «приёмка не подтверждена, и часть удалений — доказанный
    дефект выборки, не легитимное удаление источника».
  - Следующий шаг: архитекторская адъюдикация — вывод о подтверждённом дефекте `11` из `13`
    групп решает вопрос деплоя `SALES-REFRESH-WINDOW` вперёд срока `2026-08-15T12:00Z`
    (`ADR-164 §7`), не дожидаясь его; параллельно — мандат класса B на ступень 3 по ДВУМ
    группам (пара маркетплейса + `356fb156`/`2026-07-21`), если архитектор сочтёт остаток
    значимым после решения по деплою.
  - Развилки на владельце: (i) решение по деплою `SALES-REFRESH-WINDOW` — откат кода раньше
    срока `ADR-164 §7`, учитывая, что дефект уже доказан на `19` из `29` строк, а не только
    предполагается; (ii) мандат класса B на ступень 3 по двум группам (объём — единицы
    `GET`-чтений); апрув `Q-107` (`proposed`) остаётся отдельным, деплой не блокирует.
  - Счётчик: строк списка закрытия осталось `8` из `9`.
- Подробности для модели: Полный разбор, все `13` групп с числами обоих независимых методов
  (архив источника + сравнение снимок/текущая) — `reference/sales_refresh_window_delete_discriminate_2026-08-12.md`.
  Ключевой факт для следующей сессии: `_PARSE_DATE` (`bq_ops.py:230`) конвертирует
  `transaction_date_raw` (UTC) в `Asia/Bishkek` без DST — сессия перепроверила различитель
  ступени 2 этим же правилом (наивная UTC-группировка совпала численно на этом наборе, но не
  предполагалась верной по умолчанию).
- Новые открытые вопросы: `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE-STAGE3` (рабочее имя, не
  заведено формально — решение по имени и мандату за архитектором): ступень 3 по двум группам,
  класс B, объём — единицы `GET`-запросов к `api.moysklad.ru`, секрет `msklad-token`.
- Блокеры: нет — вопрос передаётся архитектору решением, не техническим препятствием.
- updated_at: 2026-08-12
- обновил: executor (сессия: `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет — адъюдикация факта дефекта остаётся за архитектором, эта сессия классом A решений не
  принимает.

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
