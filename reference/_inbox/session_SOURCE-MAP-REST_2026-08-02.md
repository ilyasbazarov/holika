=== SESSION LOG · 2026-08-02 · SOURCE-MAP-REST ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SOURCE-MAP-REST` — доисполнение и закрытие остатка (ветка `customer_invoices`)
- Сделано: старт упёрся в СТОП-условие `ADR-081 §6` (забытое дерево/ветка `worktrees/SOURCE-MAP-REST`
  = `s/SOURCE-MAP-REST`, идентична `main`) — владелец решил переиспользовать дерево как есть, вместо
  завода нового `-2`. Прочитаны и проверены цитированием три артефакта, закрывшие `CONTEXT GAP`
  `§4` артефакта `reference/source_map_rest_2026-07-30.md` ПОСЛЕ 2026-07-30 (`SOURCE-MAP-INVOICES`,
  `INVOICES-STALENESS-PROBE`, `INVOICES-FIELD-MAP`). Не разведка — адъюдикация закрытия по чтению.
  Написан `reference/source_map_rest_close_2026-08-02.md`: закрытие пунктов (а)–(г) с адресом,
  подтверждённая гипотеза происхождения таблицы (ii — разовый ручной прогон, гипотезы (i)/(iii)
  отпали), вердикт по строке реестра, названный и адресованный остаток по конвертации валюты
  существующих 4058 строк.
- Команды/логи ключевые: нет (read-only чтение репозитория, облачные команды не исполнялись —
  прямое требование постановки)
- Отклонения от плана: дерево/ветка сессии — не новое `worktrees/SOURCE-MAP-REST-2`/`s/SOURCE-MAP-REST-2`,
  как называла постановка, а переиспользованное `worktrees/SOURCE-MAP-REST`/`s/SOURCE-MAP-REST` — по
  явному решению владельца после вопроса о СТОП-условии старта

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SOURCE-MAP-REST`: `PARTIAL` → `DONE`. Обоснование и адрес — `reference/source_map_rest_close_2026-08-02.md §3`.
  Строка реестра `SOURCE-MAP-REST` (`07_GAPS.md`) уезжает в `07_ARCHIVE.md` дословно (`ADR-064`), тем
  же коммитом сборки — строка мандата `SOURCE-MAP-REST` в таблице `07_STATE §Мандат Claude Code:
  класс задач` (`07_STATE.md:384`) убирается тем же коммитом (парность `ADR-090 §1`).
  `Q-82` статус НЕ меняется этим патчем — его собственный текст остаётся `OPEN`, дополнение
  `SOURCE-MAP-REST, 2026-07-30` («обслуживающая CF не установлена») в тексте `Q-82` устарело
  фактом (загрузчик теперь идентифицирован — разовый ручной прогон, не CF, `07_GAPS.md:52`) и
  подлежит переформулировке следующей квалифицирующей сессией/сборкой, не этим патчем (эта сессия
  правки `07_GAPS.md` не делает по постановке).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SOURCE-MAP-REST` закрыта DONE — остаток по ветке `customer_invoices` (пункты
    а-г `§4` артефакта 2026-07-30) снят чтением трёх уже исполненных артефактов
    (`SOURCE-MAP-INVOICES`, `INVOICES-STALENESS-PROBE`, `INVOICES-FIELD-MAP`), артефакт
    `reference/source_map_rest_close_2026-08-02.md`; исходный `reference/source_map_rest_2026-07-30.md`
    не менялся (`ADR-043`)
  - Где мы: восстановление потока данных остаётся приоритетом перед сверкой паритета «на сейчас»
    (`FACTS-WORKFLOW-STOP-DIAG`, `ADR-111`) — решение владельца о форме восстановления не получено;
    карта происхождения по всем четырём поверхностям паритета «Прочее» (закупки/остатки/возвраты/счета)
    теперь полностью снята — построчная численная сверка каждой пары остаётся впереди отдельными
    задачами (`Q-78`…`Q-82`, `INVOICES-PARITY-RECHECK` для счетов — гейт `INVOICES-BACKFILL`)
  - Следующий шаг: решение владельца о форме восстановления загрузки, затем `FACTS-BACKFILL`
    (класс B), затем `DQ-SOURCE-CAPTURE`; отдельно — решение владельца/архитектора по гэпу
    COGS-источника (§2 артефакта `reference/sales_merge_dryrun_2026-08-02.md`) перед выбором
    варианта `SALES-REFRESH-WINDOW`; параллельно без изменений — `DIM-METADATA-MAPPINGS-FROZEN`,
    `MARTS-BUILD-STAMP`, `FACT-SALES-BYVARIANT-BACKUP-OWNER`, `SALES-CONSIGNMENT-REVENUE`,
    `SALES-PERIMETER-EXTEND`, `FX-MAY-WINDOW-D1-TAIL`, `INGEST-CURRENCY-ASSERT` шаги 1-2,
    подготовка `INGEST-MOMENT-ZONE-FIX`, тексты Looker Studio; пункт «новый discovery-бриф на
    остаток `Q-82`» из прежнего стенд-апа снят — остаток закрыт этой сессией, не готовится
  - Развилки на владельце: решение о форме восстановления загрузки; первый `push` код-репо в
    `holika-prod`; ответ клиента по часовому поясу (`MSKLAD-TZ-PROPOSAL`); мандат класса B на
    `AUDIT-SNAPSHOT-FIX-EMPLOYEES`/`AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`; выбор варианта патча
    `SALES-REFRESH-WINDOW` — оба технических неизвестных сняты (одно фактом, одно явным
    `CONTEXT GAP`), решение можно принимать с учётом гэпа
  - Счётчик: пары реестра 2/7 сходятся · таблиц живых 15 из 26 (до восстановления, не
    переизмерялось) · строк реестра на критическом пути 23 из 63 · Epic M 5/7 фаз
- Подробности для модели: `SOURCE-MAP-REST` DONE закрывает карту происхождения по всем четырём
  поверхностям паритета «Прочее». Для будущих сессий, берущих `Q-82`/`INVOICES-PARITY-RECHECK`:
  правило пересчёта валюты для `entity/invoiceout` выведено и не требует повторного discovery
  (`reference/invoices_field_map_2026-08-02.md §4`) — `sum_kgs=(sum÷100)×rate.value`, `rate=null`
  тождественно валюте KGS; `unpaid_sum_kgs` — производная (`sum_kgs − payed_sum_kgs`), не поле API.
  Не смешивать со снапшотом `load_payments_cloudshell_2026-06-18.py` — его конвертер валюты
  дефектен (`÷100` без курса), образцом для нового загрузчика не является. Открытый остаток,
  адресованный именно `INVOICES-PARITY-RECHECK` (гейт `INVOICES-BACKFILL`), не этой строкой: не
  установлено и структурно не устанавливаемо изнутри хранилища, применил ли разовый ручной прогон
  `2026-06-05` конвертацию валюты к уже лежащим 4058 строкам (`core.fact_customer_invoices` не несёт
  ни валюты документа, ни курса, ни исходной суммы — тот же тупик, что `Q-20`); вопрос закрывается
  не установлением, а вытеснением — досев `INVOICES-BACKFILL` полностью перезапишет эти строки.
  Полный разбор — `reference/source_map_rest_close_2026-08-02.md §4`.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-02
- обновил: executor (сессия: SOURCE-MAP-REST)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
