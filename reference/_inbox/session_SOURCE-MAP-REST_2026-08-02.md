=== SESSION LOG · 2026-08-02 · SOURCE-MAP-REST ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SOURCE-MAP-REST — карта происхождения purchases/customer_invoices/inventory/counterparty_returns
- Сделано: старт по `CLAUDE.md` (SHA на старте `d20d9580b81561bfed1573495e9d8d8769a7d95c`), обязательный
  контекст прочитан, проверка `ADR-054` пройдена по каждому доку, хук установлен, `bash
  tools/hooks/selftest.sh` — пройдено 36, провалено 0. При переформулировке задачи обнаружено: тело
  брифа `briefs/SOURCE-MAP-REST.md` (датирован 2026-07-29) уже полностью исполнено предыдущей сессией
  `SOURCE-MAP-REST` от 2026-07-30 и слито в `main` до старта этого рабочего дерева — артефакт
  `reference/source_map_rest_2026-07-30.md`, слот `cf-inventory` в `11_INFRA_FACTS.md §CF`, строка
  GAP-реестра `SOURCE-MAP-REST` = **ЧАСТИЧНО DONE** (три поверхности из четырёх сняты, четвёртая —
  `CONTEXT GAP` по загрузчику `core.fact_customer_invoices`, дополнение к `Q-82`). Расхождение
  эскалировано владельцу отдельным блоком (варианты: закрыть без действий / довести BQ
  job-history дискавери / реконструировать поля по образцу `load_payments.py`) — владелец выбрал
  «закрыть без действий».
- Команды/логи ключевые: `bash tools/session_status.sh` (чисто), `bash tools/hooks/selftest.sh`
  (пройдено 36, провалено 0). Облачных команд эта сессия не исполняла — повторного дискавери не было.
- Отклонения от плана: да. Шаги 1–10 брифа не исполнялись — их работа уже сделана и закоммичена
  предыдущей сессией. Эта сессия не написала кода/скриптов/новых снапшотов, коммитит только этот
  session-блок. Остаток (`Q-82`, загрузчик `core.fact_customer_invoices`) сознательно не тронут —
  по решению владельца передаётся архитектору отдельным новым брифом.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SOURCE-MAP-REST: ЧАСТИЧНО DONE → без изменений (ЧАСТИЧНО DONE); повторного исполнения не
  производилось, статус подтверждён этой сессией.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SOURCE-MAP-REST` переподтверждена без нового исполнения — бриф уже полностью
    выполнен сессией 2026-07-30 (слито в `main`), эта сессия проверила состояние и по решению
    владельца закрылась без действий, артефакт `reference/source_map_rest_2026-07-30.md` не менялся
  - Где мы: восстановление потока данных остаётся приоритетом перед сверкой паритета «на сейчас»
    (`FACTS-WORKFLOW-STOP-DIAG`, `ADR-111`) — решение владельца о форме восстановления не получено;
    выбор варианта патча `SALES-REFRESH-WINDOW` теперь частично разгейчен (одно из двух неизвестных
    снято)
  - Следующий шаг: решение владельца о форме восстановления загрузки, затем `FACTS-BACKFILL` (класс
    B), затем `DQ-SOURCE-CAPTURE`; отдельно — решение владельца/архитектора по гэпу COGS-источника
    (§2 артефакта `reference/sales_merge_dryrun_2026-08-02.md`) перед выбором варианта
    `SALES-REFRESH-WINDOW`; параллельно без изменений — `DIM-METADATA-MAPPINGS-FROZEN`,
    `MARTS-BUILD-STAMP`, `FACT-SALES-BYVARIANT-BACKUP-OWNER`, `SALES-CONSIGNMENT-REVENUE`,
    `SALES-PERIMETER-EXTEND`, `FX-MAY-WINDOW-D1-TAIL`, `INGEST-CURRENCY-ASSERT` шаги 1-2, подготовка
    `INGEST-MOMENT-ZONE-FIX`, тексты Looker Studio; дополнительно — новый discovery-бриф на остаток
    `Q-82` (загрузчик `core.fact_customer_invoices`) готовится архитектором отдельно, эта сессия его
    не ставит
  - Развилки на владельце: решение о форме восстановления загрузки; первый `push` код-репо в
    `holika-prod`; ответ клиента по часовому поясу (`MSKLAD-TZ-PROPOSAL`); мандат класса B на
    `AUDIT-SNAPSHOT-FIX-EMPLOYEES`/`AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`; выбор варианта патча
    `SALES-REFRESH-WINDOW` — оба технических неизвестных сняты (одно фактом, одно явным
    `CONTEXT GAP`), решение можно принимать с учётом гэпа
  - Счётчик: пары реестра 2/7 сходятся · таблиц живых 15 из 26 (до восстановления, не
    переизмерялось) · строк реестра на критическом пути 23 из 63 · Epic M 5/7 фаз
- Подробности для модели: **Сессия `SOURCE-MAP-REST` (повтор, 2026-08-02, класс A, без нового
  исполнения).** Бриф `briefs/SOURCE-MAP-REST.md` (датирован 2026-07-29) описывает задачу как
  невыполненную с нуля; по факту она уже ЧАСТИЧНО DONE сессией 2026-07-30, артефакт и правки слиты в
  `main` до старта этого рабочего дерева. Эта сессия обнаружила расхождение, эскалировала владельцу
  (закрыть без действий / довести BQ job-history дискавери / реконструировать поля по образцу
  `load_payments.py`) — выбрано «закрыть без действий». Ничего не изменено, коммитов кроме этого
  session-блока нет. Для будущих сессий: если `briefs/SOURCE-MAP-REST.md` попадётся снова как
  назначение, сначала проверить `reference/source_map_rest_2026-07-30.md` и строку GAP-реестра
  `SOURCE-MAP-REST` (`07_STATE.md`) — повторное исполнение не требуется, требуется только новый
  discovery-бриф на остаток `Q-82`.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-02
- обновил: исполнитель (сессия: SOURCE-MAP-REST)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
