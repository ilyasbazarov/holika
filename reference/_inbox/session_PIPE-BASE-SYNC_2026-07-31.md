=== SESSION LOG · 2026-07-31 · PIPE-BASE-SYNC ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: PIPE-BASE-SYNC — пятая проверка старта (отставание `origin/main`) + починка счётчика буфера от git-игнорируемых файлов
- Сделано:
  - `tools/session_status.sh`: добавлена секция «отставание от origin/main» — `git fetch` плюс
    `git rev-list --count HEAD..origin/main`; счётчик печатается положительно (`ADR-044`) и входит
    в общий вердикт (RC 1 при N>0) в обоих режимах — «связанное» и «основное» дерево.
  - `tools/session_status.sh`: секция «буфер reference/_inbox» больше не считает git-игнорируемые
    файлы в счётчике «файлов кроме .gitkeep» (форма — `git check-ignore -q` на кандидатов из
    `find`); такой файл, если физически лежит в буфере, называется отдельной
    строкой-предупреждением «git-игнорируемых файлов в буфере (не входят в счётчик выше): N».
  - `CLAUDE.md §Старт` п.1 — вписана пятая проверка той же формулировкой, что несёт скрипт (четыре
    факта → пять фактов, добавлено предложение про отставание и ссылку `ADR-086 §4`).
  - `_ASSEMBLER.md §3 «Вход»` — добавлен пункт 1a про отставание от `origin/main` (`ADR-086 §4`)
    сразу после существующего пункта 1 про `git status`.
- Команды/логи ключевые:
  - `bash tools/hooks/selftest.sh` (после правки `CLAUDE.md`/`_ASSEMBLER.md`) →
    «итог: пройдено 28, провалено 0» (лог: `reference/_scratch_PIPE-BASE-SYNC_2026-07-31/selftest_after.log`).
  - `touch reference/_inbox/.DS_Store` → старый скрипт (сохранён как
    `reference/_scratch_PIPE-BASE-SYNC_2026-07-31/session_status.sh.before`) дал
    «файлов кроме .gitkeep: 1»; новый скрипт на том же входе дал «файлов кроме .gitkeep: 0» плюс
    строку «ВНИМАНИЕ: git-игнорируемых файлов в буфере (не входят в счётчик выше): 1». Тестовый
    файл убран после проверки, не входит в коммит.
  - `bash tools/session_status.sh` (живой прогон, реальное состояние дерева, до+после в одном
    логе `reference/_scratch_PIPE-BASE-SYNC_2026-07-31/run_after.log` / `run_before.log`) →
    новый скрипт дал RC 1, «отставание от origin/main: 1»; старый скрипт (сохранённая копия) на
    том же состоянии репозитория дал RC 0. Отставание — РЕАЛЬНОЕ, не смоделированное: во время
    этой сессии `origin/main` продвинулся PR-мержем (см. «Новые открытые вопросы» ниже), что и
    дало ненулевой счётчик без искусственной подготовки.
- Отклонения от плана: демонстрация пятой проверки на отставании (Return-this, п.4 брифа) сделана
  на РЕАЛЬНОМ, а не смоделированном отставании — бриф допускал любую форму («форма проверки на
  исполнителе»), а реальный факт сильнее синтетики (`ADR-074`). Причина отставания — вне scope этой
  задачи, зафиксирована отдельным открытым вопросом ниже, не адъюдицируется этой сессией.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача PIPE-BASE-SYNC: READY → DONE. Обе правки `tools/session_status.sh` внесены, формулировка
  синхронизирована в `CLAUDE.md`/`_ASSEMBLER.md`, регрессия хука зелёная (28/28), живой прогон
  подтверждён (диффы и логи — `reference/_scratch_PIPE-BASE-SYNC_2026-07-31/`). Строка полностью
  закрыта, переносится в `07_ARCHIVE.md` дословно тем же коммитом сборки (`ADR-064`).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: сессия `PIPE-BASE-SYNC` закрыла оба дефекта `tools/session_status.sh` (пятая
    проверка отставания от `origin/main`, починка счётчика буфера от git-игнорируемых файлов) и
    провела ту же формулировку в `CLAUDE.md §Старт` п.1 и `_ASSEMBLER.md §3`; регрессия хука 28/28.
  - Где мы: карта происхождения снята для 6 поверхностей из 7; численно сходится 1 пара реестра из
    7; построчных сверок не запускалось ни одной; дашборд Looker Studio разобран полностью — 5
    страниц, 11 источников, из них 5 Custom Query, все сняты в `reference/sql/`.
  - Следующий шаг: `SOURCE-MAP-INVOICES` (бриф готов); `STATE-COMPACT` хвостом; затем
    `REPORT-FIELDS` (нужна сессия `REPORT-FIELDS-GEN`, брифа нет).
  - Развилки на владельце: `LS-RETURNS-FX-HARDCODE` — чинить курс в Custom Query `fact_returns`
    сейчас или ждать закрытия политики конвертации (`Q-56`/`Q-66`/`Q-69`); НОВОЕ — расхождение
    `main` (локально) и `origin/main` (см. «Новые открытые вопросы», `Q-90`) требует решения о
    форме синхронизации, прежде чем какая-либо сессия попытается `git push`.
  - Счётчик: пары реестра 1/7 сходятся · карта 6/7 поверхностей · Epic M 5/7 фаз · реестр открытых
    вопросов 59 строк до уборки `STATE-COMPACT` (плюс `Q-90` этой сессией).
- Подробности для модели: Задача `PIPE-BASE-SYNC` закрыта строго по цитированной приёмке
  `07_STATE.md:164` (на момент чтения этой сессией): скрипт на отстающей базе печатает счётчик и
  возвращает RC 1 (подтверждено реальным отставанием, не смоделированным); файл, игнорируемый git,
  не входит в счётчик буфера и называется отдельной строкой-предупреждением (подтверждено
  `.DS_Store`-тестом); `bash tools/hooks/selftest.sh` даёт «провалено 0» (подтверждено после
  правки `CLAUDE.md`/`_ASSEMBLER.md`, не только до). Развилка владельца из брифа, поднятая сессией
  генерации `PIPE-BASE-SYNC-GEN` (вносить ли строку `PIPE-BASE-SYNC` в таблицу `07_STATE §Мандат
  Claude Code: класс задач`), закрывается фактом закрытия самой задачи: добавлять строку в таблицу
  мандата для уже DONE-задачи нет смысла, вопрос снимается без действия. Процессный дефект
  (таблица мандата не обновляется автоматически при заводке READY-задачи в GAP-реестре) остаётся
  открытым как `Q-89` (заведён сессией генерации, эта сессия его не трогает и не закрывает).
- Новые открытые вопросы:
  - Q-90 (наблюдение сессии PIPE-BASE-SYNC, 2026-07-31T12:12:59Z): во время исполнения этой
    сессии `origin/main` продвинулся мержем `https://github.com/ilyasbazarov/holika/pull/1`
    (ветка `s/PIPE-BASE-SYNC-GEN`, смёржена владельцем через веб-интерфейс GitHub,
    `mergedAt: 2026-07-31T12:00:47Z`, подтверждено `gh pr view 1 --json mergedBy,mergedAt`).
    Коммит `ed38f8c` принёс в `origin/main` файл
    `reference/_inbox/session_PIPE-BASE-SYNC-GEN_2026-07-31.md` **необработанным** — минуя проход
    сборки (`_ASSEMBLER.md`). Локально та же ветка была в ЭТОЙ ЖЕ сессии слита проходом сборки
    (агент `assembler`, коммит `55ccf20` поверх `76fac2c`), с корректным применением `STATE_PATCH`
    к `07_STATE.md` и удалением файла буфера. Итог: `main` (локально) и `origin/main` разошлись
    настоящим образом — подтверждено `git merge-base --is-ancestor` в обе стороны (`no`/`no`),
    общий предок `76fac2c`. Вопросы архитектору: (а) подпадает ли этот случай под запрет
    `ADR-086 §5` («доставка выхода архитектора через веб-интерфейс GitHub»), хотя здесь смёржена
    целиком ветка сессии генерации, а не точечная доставка архитектора; (б) как трактовать
    буфер-файл, лежащий в `origin/main` необработанным — обрабатывать его сборкой отдельно от
    уже обработанной локальной копии, или считать историческим дублем; (в) какой из двух `main`
    (локальный с применённым `STATE_PATCH`, или `origin/main` с сырым буфер-файлом) есть источник
    истины для будущего `push`, и как сшить историю без потери работы любой из сторон. Эта сессия
    ничего не чинит: вне мандата класса A этой задачи и вне списка файлов на запись брифа;
    `git push` не выполнялся.
- Блокеры: нет
- updated_at: 2026-07-31
- обновил: исполнитель (сессия: PIPE-BASE-SYNC)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
