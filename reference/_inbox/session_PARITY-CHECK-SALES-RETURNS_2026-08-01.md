=== SESSION LOG · 2026-08-01 · PARITY-CHECK-SALES-RETURNS ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: PARITY-CHECK-SALES-RETURNS — первая волна построчных сверок реестра паритета для пар «Продажи» и «Возвраты покупателей» (май-2026)
- Сделано:
  - Различитель `Q-93` снят однозначно: `entity/*` эндпоинты используют `filter=moment>=X;moment<=Y` (не `momentFrom`/`momentTo` — этот параметр на entity-эндпоинтах молча игнорируется, факт, а не гипотеза, подтверждено 5 контрольными окнами с идентичным содержимым при разных `momentFrom`/`momentTo`); границы фильтра интерпретируются в зоне API (той же, что несёт `moment`), верхняя граница включительна при `<=`
  - Ночные полосы (`ADR-088 §5/§6`) за май-2026 пусты по обеим сторонам, оба типа документов: продажи (`entity/demand`, 127 документов, без `applicable`-фильтра — подтверждено, что и с фильтром, и без него результат идентичен) — 0 в полосе `[18:00;21:00)` UTC; возвраты — `entity/salesreturn` (0 документов в мае вовсе) и `entity/retailsalesreturn` (8 документов, фактический источник `core.fact_returns` за май) — 0 в полосе `[21:00;24:00)` UTC у обоих
  - Второй замер сдвига (`ADR-088 §10`) на майском документе (`entity/demand` №04250): API `2026-05-15 08:13:00.000`, интерфейс (владелец, чат) `15.05.2026 11:13` — сдвиг +3ч подтверждён и для мая, переход на летнее время не проявился
  - Построчная сверка «Продажи»: `core.fact_sales_profit.revenue_kgs` (май) = 93 522 995,53 KGS vs `report/profit/byproduct.sellSum` (минорные единицы ÷100) = 93 234 205,80 KGS — расхождение 288 789,73 KGS (0,31%). Ночная полоса пуста → «без полос» = тот же итог. Причина расхождения НЕ установлена: проверка гипотезы «забыта конвертация мультивалютных документов» (в мае есть KGS/USD/RUB демand-документы) дала ТРЕТЬЕ число (91 892 755,66 KGS при пересчёте по `rate.value`), не совпадающее ни с одной из двух сравниваемых величин — расхождение осталось незакрытым как факт, не примирено
  - Построчная сверка «Возвраты покупателей»: назначенный оракул (`entity/salesreturn`) даёт 0 документов за май-2026 (0,00 KGS); `core.fact_returns` за май — 8 строк, ВСЕ `return_type=retailsalesreturn`, сумма 570,00 KGS, точно совпадающая с суммой этих 8 документов по `entity/retailsalesreturn`. Причина расхождения атрибутирована: оракул пары (`ADR-085 §2`) охватывает только `entity/salesreturn`, тогда как `core.fact_returns` (`fetch_returns.py`) грузит оба типа; для мая непуст только второй. Расширять ли оракул на `retailsalesreturn` — решение уровня моста, вне мандата исполнителя
  - Правило суток и оба результата внесены в `reference/parity_registry.md`, строки 18–20 (столбцы «Правило-мост» и «Статус сходимости + артефакт»), остальной файл не тронут (`git diff --stat` — 3 строки)
- Команды/логи ключевые: `reference/_scratch_PARITY-CHECK-SALES-RETURNS_2026-08-01/` — 9 скриптов + логи + сырые тела ответов (67 файлов), `date -u`/`gcloud auth list` первой и последней командой каждого; `grep -rF` по значению токена перед `git add` — 0 совпадений (RC=1)
- Отклонения от плана: два. (1) `momentFrom`/`momentTo` не работают на `entity/salesreturn` — пришлось перейти на `filter=moment>=X;moment<=Y`, брифом не предполагалось, снято попутно как часть различителя `Q-93`. (2) Ночная полоса для пары «Возвраты покупателей» считалась не только по назначенному оракулу (`entity/salesreturn`, пуст за май), но и по `entity/retailsalesreturn` — реальному источнику строк `core.fact_returns` за май; без этого расширения «полоса пуста» было бы неполным утверждением (оракул пуст тривиально, а не потому что полоса пуста)

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача PARITY-CHECK-SALES-RETURNS: IN PROGRESS (мандат `ADR-092`) → пять шагов брифа исполнены, но ОБЕ пары дали открытые расхождения, не «сошлось 0,00» — задача-строка остаётся открытой до адъюдикации архитектором двух новых фактов (расхождение 0,31% на продажах без установленной причины; несовпадение охвата оракула и core-таблицы на возвратах)
- Стенд-ап:
  - Прошлый шаг: первая волна построчных сверок реестра паритета исполнена для пар «Продажи»/«Возвраты покупателей» (май-2026); обе пары дали ненулевое расхождение с атрибутированной (возвраты) либо неустановленной (продажи) причиной, записано в `reference/parity_registry.md` строки 18–20
  - Где мы: различитель `Q-93` и ночные полосы закрыты фактом (все полосы пусты); финиш Epic-1 требует ещё адъюдикации двух новых находок и, вероятно, повторной сверки после неё
  - Следующий шаг: архитекторская адъюдикация двух новых гэпов (расхождение продаж 0,31%; охват оракула возвратов) — без брифа, решение уровня моста (`ADR-076 §4`); параллельно продолжаются `CODE-REPO-STANDUP` шаг 2a, `INFRA-FACTS-SWEEP`, подготовка текстов Looker Studio
  - Развилки на владельце: нет новых сверх уже известных (`Q-92` по-прежнему не задаётся — полосы пусты, но задача не про полосы)
  - Счётчик: пары реестра 1/7 сходятся (без изменений — обе новые пары НЕ сошлись 0,00) · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
- Подробности для модели: см. `SESSION_LOG` выше — оба числовых расхождения и их атрибуция изложены там полностью, здесь не дублируется. Артефакт `reference/_scratch_PARITY-CHECK-SALES-RETURNS_2026-08-01/` содержит все сырые тела ответов API и BigQuery-запросы, воспроизводящие оба числа.
- Новые открытые вопросы:
  - `Q-98` (рабочее имя, номер присваивает сборка): расхождение `core.fact_sales_profit.revenue_kgs` и `report/profit/byproduct.sellSum` за май-2026 — 288 789,73 KGS (0,31%); гипотеза «мультивалютная конвертация» проверена и отклонена (даёт третье число); причина не установлена
  - `Q-99` (рабочее имя): оракул пары «Возвраты покупателей» (`ADR-085 §2`, `entity/salesreturn`) не покрывает `entity/retailsalesreturn` — источник, реально формирующий `core.fact_returns` за май-2026 (8 из 8 строк); решение — расширять ли мост на розничные возвраты — уровня `ADR-068 §3`, не мандат исполнителя
- Блокеры: нет (обе находки — факты для адъюдикации, не блокируют прочую параллельную работу)
- updated_at: 2026-08-01
- обновил: исполнитель (сессия: PARITY-CHECK-SALES-RETURNS)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
