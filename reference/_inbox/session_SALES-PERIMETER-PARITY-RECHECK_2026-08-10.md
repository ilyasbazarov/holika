=== SESSION LOG · 2026-08-10 · SALES-PERIMETER-PARITY-RECHECK ===

## SESSION_LOG
- Задача: `T-SALES-PERIMETER-PARITY-RECHECK` — пересверка пары «Продажи» (строки 18-19 реестра
  паритета) против `report/profit/*` после деплоя периметра продаж (`cf-facts-00008-zen`).
- Сделано:
  - Закрыт нюанс окна (шаг 2 брифа): живой `GET` в разрыве `2026-05-01…2026-05-09` нашёл `0`
    документов `entity/retaildemand` и `2` документа `entity/commissionreportin`
    (`372 454,20` KGS, контрагенты «Bloom WB»/«UMAI WB»); досеяны прогоном `perimeter`
    (`window_days=99`) + `perimeter_promote`, оба подтверждены read-back прямым запросом, не по `rc`.
  - Снят датированный снимок эталона: `report/profit/byproduct`/`bycounterparty`, май-2026,
    `93 234 205,80` KGS — не изменился с `2026-08-04`.
  - Измерена `core.fact_sales_profit.revenue_kgs` за май (после досева): `96 844 445,61` KGS,
    `3411` строк.
  - `Δ = +3 610 239,81` KGS (`3,8722 %`), разложена без свободного остатка: `M1` (строки-сироты,
    пересчитано) `= +1 630 239,81`; новый компонент — документ `04235` (отгрузка комиссионеру,
    не удалён из `core` тем же дефектом `MERGE` без ветки `DELETE`, `ADR-100 §1`, что держит `M1`)
    `= +1 980 000,00`; `ε = 0,00` KGS.
  - Сравнение с прогнозом `sales_ingest_patch_2026-08-03.md §5` (`≈+1 630 239,81`): НЕ совпадает,
    разница `+1 980 000,00` KGS полностью объяснена (не подгонка) — прогноз предполагал устранение
    ВСЕХ трёх подкомпонентов `M2`, реализовано два из трёх.
  - Строки 18-19 `reference/parity_registry.md` дополнены (не переписаны) измеренным результатом.
  - Артефакт `reference/sales_perimeter_parity_recheck_2026-08-10.md` — полное разложение,
    провенанс, живые запросы (без значения токена).
- Команды/логи ключевые: `reference/_scratch_SALES-PERIMETER-PARITY-RECHECK_2026-08-10/*.sh` +
  `*_run.log` (каждый скрипт — `date -u`/`gcloud auth list` первой и последней командой).
  `grep -rF` по значению токена перед коммитом — `0` совпадений.
- Отклонения от плана:
  1. На старте сессии мандат `ADR-133 §1` (уже выданный) оказался ýже шага 2 брифа (запрещал
     `entity/*` GET и любую запись в `core`) — расхождение брифа с `07_STATE.md:1604`, стоп-условие
     `CLAUDE.md`. Владелец расширил объём в чате отдельным решением ДО исполнения (форма —
     `ADR-077 §6`, прецедент `ADR-092`/`ADR-108`/`ADR-117`/`ADR-118`). Полный текст расхождения и
     решения — `sales_perimeter_parity_recheck_2026-08-10.md §0`.
  2. Клиентский вызов `mode=perimeter` (`window_days=99`) оборвался `ReadTimeout` (300с) — известная
     ловушка (`ADR-133 §6`), успех сервера подтверждён read-only проверкой `staging`, слепого
     повтора не делалось.
  3. Найдена находка вне прямого предположения брифа: измеренная `Δ` расходится с прогнозом на
     ровно `+1 980 000,00` KGS — причина установлена (документ `04235` не удалён тем же дефектом,
     что `M1`), но форма записанного в реестре инварианта (`Δ = M1 − |M2| + ε`, где `M2` вычитается)
     эту механику больше не описывает корректно — остаток теперь того же знака, что `M1`, а не
     противовес. Это НЕ решено этой сессией (`ADR-133 §4`: адъюдикация правила-моста — за
     архитектором), названо как новый открытый вопрос.

## STATE_PATCH
- Задача `SALES-PERIMETER-PARITY-RECHECK`: `READY` → измерение выполнено, вердикт «сошлась»/
  «переоткрыта» НЕ вынесен этой сессией (`ADR-133 §4`) — ждёт адъюдикации архитектора.
- Текущий фокус: архитектору — адъюдицировать (а) форму инварианта строк 18-19 после расщепления
  `M2` на устранённую и неустранённую часть; (б) статус пары «Продажи» в свете найденного остатка
  `+1 980 000,00` KGS (документ `04235`); (в) снять/оставить мандат `ADR-133` (объём исчерпан —
  оба разрешённых `GET` сделаны, плюс расширенный владельцем объём `entity/*`/`perimeter*`
  исполнен и закрыт).
- Новые открытые вопросы:
  1. Форма правила-моста строк 18-19 реестра паритета не описывает измеренную механику `M2` после
     деплоя (`§6` артефакта `sales_perimeter_parity_recheck_2026-08-10.md`) — нужна адъюдикация
     архитектора, новый ADR или правка правила.
  2. Документ `04235` (отгрузка комиссионеру, `+1 980 000,00` KGS, `2026-05-12`) не удалён из
     `core.fact_sales_profit` дефектом `ADR-100 §1` (`MERGE` без ветки `DELETE`) — тот же адрес
     устранения, что у `M1` (`SALES-REFRESH-WINDOW`, не построен, либо точечная операция, класс B,
     вне scope этой задачи).
- Блокеры: нет.
- updated_at: 2026-08-10

## NEW_DECISIONS
- ADR-0XX: Расширение мандата класса B `SALES-PERIMETER-PARITY-RECHECK` на шаг 2 (read-only
  `entity/retaildemand`/`entity/commissionreportin`) и досев (`perimeter`/`perimeter_promote`)
  Контекст: мандат `ADR-133 §1`, выданный заранее, разрешал только два `GET report/profit/*`;
  шаг 2 брифа (закрытие нюанса окна `2026-05-01…2026-05-08`) требовал `entity/*` GET и, при находке,
  запись в `core` — вне объёма `ADR-133 §1`. Расхождение обнаружено на старте сессии, исполнение
  остановлено до решения владельца (`CLAUDE.md`, стоп-условие «расхождение брифа и мандата»).
  Решение: владелец расширил объём в чате 2026-08-10 двумя отдельными подтверждениями —
  (1) read-only `GET entity/retaildemand`/`entity/commissionreportin` с фильтром `moment` на
  разрыв `2026-05-01…2026-05-09`; (2) после находки — `mode=perimeter` (`window_days=99`) +
  `mode=perimeter_promote` тем же окном, запись в `core.fact_sales_profit`. Каждое действие
  объявлено отдельным сообщением (что/на каком объекте/чем откатывается) до подтверждения
  (`ADR-077 §6`), форма — прецедент `ADR-092`/`ADR-108`/`ADR-117`/`ADR-118`.
  Последствия: объём мандата `SALES-PERIMETER-PARITY-RECHECK` на эту сессию расширен и полностью
  исчерпан (оба этапа исполнены и закрыты фактом); дальнейшего действия по этому расширению не
  требуется. Полный лог обоих подтверждений — эта сессия (чат), провенанс — `reference/_scratch_
  SALES-PERIMETER-PARITY-RECHECK_2026-08-10/`.
  Статус: accepted (владелец подтвердил напрямую в чате перед каждым действием).

## NEW_CONVENTIONS
- нет
=== END SESSION ===
