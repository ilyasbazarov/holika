=== SESSION LOG · 2026-08-08 · SALES-DOCUMENT-OWNER-DEPLOY ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-DOCUMENT-OWNER-DEPLOY, деплойная половина (класс B)
- Сделано: шаги 1–2 брифа (класс A, read-only) исполнены и подтверждены фактом — патч (`bq_ops.py:320`)
  свежий, `py_compile` чисто; живая ревизия `cf-facts-00009-tul` (`generation 1786115536540209`,
  `updateTime 2026-08-07T15:13:10.253085704Z`) побайтово (sha256) равна `master` @ `7e039bd`
  (`holika-prod`, 11/11 файлов). Артефакт: `reference/sales_document_owner_deploy_2026-08-08.md`.
- Команды/логи ключевые: `gcloud functions describe cf-facts`, `gsutil cp .../function-source.zip#<generation>`,
  `shasum -a 256` по архиву и по `master`, `git clone` `holika-prod` (глубина 50) — все логи в
  `reference/_scratch_SALES-DOCUMENT-OWNER-DEPLOY_2026-08-08/`.
- Отклонения от плана: сессия остановлена ДО шага 3 брифа. Причина — НОВАЯ находка (не была известна
  на момент генерации брифа): снапшот `reference/code/cf-facts/bq_ops.py` сейчас несёт ДВА
  переплетённых недеплоенных патча в одних и тех же функциях `_build_merge_sql`/
  `_build_perimeter_merge_sql` — `document_owner_employee_id` (эта задача) и ветку `DELETE`
  `SALES-REFRESH-WINDOW` (`ADR-144 §8`, другая задача, класс B, мандат тоже не выдан, порядок деплоя
  относительно этой задачи не решён владельцем — развилка уже зафиксирована в `07_STATE.md` стенд-апе).
  Разделение патчей «по diff» (шаг 3) требует редакторского решения вне полномочий исполнителя. Помимо
  этого, независимо действует уже известный из шапки брифа блокер: мандат класса B на сам деплой
  `SALES-DOCUMENT-OWNER-DEPLOY` не выдан — шаг 5 и далее в любом случае не наступают. Полный разбор —
  `reference/sales_document_owner_deploy_2026-08-08.md`.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-DOCUMENT-OWNER-DEPLOY (деплой): OPEN → OPEN (без изменений статуса; уточнение блокера)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: шаги 1–2 `SALES-DOCUMENT-OWNER-DEPLOY` (класс A, read-only) подтверждены фактом — база
    деплоя (`cf-facts-00009-tul` = `master` `7e039bd`) верна; артефакт
    `reference/sales_document_owner_deploy_2026-08-08.md`.
  - Где мы: `SALES-DOCUMENT-OWNER-DEPLOY` (деплой) блокирована ДВОЙНО — (1) мандат класса B не выдан,
    (2) снапшот `bq_ops.py` несёт вперемешку эту задачу и `SALES-REFRESH-WINDOW`; шаг 3 не исполним без
    решения владельца/архитектора о разделении/порядке двух патчей.
  - Следующий шаг: владелец/архитектор решает порядок и форму двух патчей одного файла
    (`SALES-DOCUMENT-OWNER-DEPLOY` vs `SALES-REFRESH-WINDOW`) — раздельные ветки с ручным разделением,
    объединённый деплой одним архивом, либо иной порядок; затем — поимённый мандат класса B.
  - Развилки на владельце: как разделить/объединить деплой `SALES-DOCUMENT-OWNER-DEPLOY` и
    `SALES-REFRESH-WINDOW` в одном файле снапшота (эта же развилка уже стояла в предыдущем стенд-апе от
    `SALES-REFRESH-WINDOW-ADJ`, эта сессия подтвердила её как фактический блокер шага 3, не только
    предупреждение).
  - Счётчик: пары реестра 7/7 · карта — · Epic M — (Epic-1 завершён `ADR-140`, фаза передачи клиенту)
- Подробности для модели: `SALES-DOCUMENT-OWNER-DEPLOY` (деплой) — шаги 1–2 брифа исполнены и
  подтверждены: живая ревизия `cf-facts-00009-tul` побайтово равна `master` `7e039bd` (sha256, 11/11
  файлов), патч (`bq_ops.py:320`) свежий и валиден. Шаг 3 не начат — найдено, что снапшот
  `reference/code/cf-facts/bq_ops.py` несёт два переплетённых недеплоенных патча в одних функциях
  (`document_owner_employee_id` + `SALES-REFRESH-WINDOW`-ные ветки `DELETE`), разделение которых по
  diff требует решения владельца/архитектора о порядке/форме, не решаемо исполнителем механически.
  Независимо от этого мандат класса B на сам деплой `SALES-DOCUMENT-OWNER-DEPLOY` по-прежнему не выдан.
  Полный разбор с провенансом — `reference/sales_document_owner_deploy_2026-08-08.md`.
- Новые открытые вопросы: нет (развилка уже существовала строкой `SALES-REFRESH-WINDOW` в
  `07_GAPS.md`; эта сессия не заводит новую строку, а подтверждает, что развилка — фактический блокер
  шага 3 `SALES-DOCUMENT-OWNER-DEPLOY`, не только предупреждение)
- Блокеры: (1) мандат класса B на деплой `SALES-DOCUMENT-OWNER-DEPLOY` не выдан; (2) порядок/форма
  разделения двух патчей одного файла снапшота (`SALES-DOCUMENT-OWNER-DEPLOY` vs `SALES-REFRESH-WINDOW`)
  не решены владельцем/архитектором — оба блокируют шаг 3 и далее независимо друг от друга
- updated_at: 2026-08-08
- обновил: executor (сессия: SALES-DOCUMENT-OWNER-DEPLOY)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
