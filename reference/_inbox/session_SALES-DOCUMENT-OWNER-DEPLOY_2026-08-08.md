=== SESSION LOG · 2026-08-08 · SALES-DOCUMENT-OWNER-DEPLOY ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-DOCUMENT-OWNER-DEPLOY, деплойная половина (класс B). DONE.
- Сделано: полный цикл брифа (шаги 1-11). Найдена и эскалирована архитектору блокировка шага 3
  (два переплетённых недеплоенных патча в одном файле снапшота) — решена `ADR-145` (разделение
  механическое по хункам). Мандат класса B выдан `ADR-146 §1`. `ALTER TABLE` исполнен и подтверждён.
  Деплой `cf-facts-00010-mog` исполнен и подтверждён read-back'ом. `weekly`-прогон (`window_days=90`)
  выполнен со второй попытки (первая столкнулась с гонкой staging против штатного часового
  пайплайна — разобрано read-only, без слепого повтора, данные не пострадали). Ветка слита в `master`
  код-репо, `MANIFEST.md` пополнен. Артефакт: `reference/sales_document_owner_deploy_2026-08-08.md`.
- Команды/логи ключевые: `gcloud functions describe/deploy/call`, `bq query`/`bq show`, `gsutil cp`,
  `shasum -a 256`, `gcloud logging read` (диагностика гонки staging) — все логи в
  `reference/_scratch_SALES-DOCUMENT-OWNER-DEPLOY_2026-08-08/`.
- Отклонения от плана: (1) шаг 3 изначально заблокирован находкой, снят адъюдикацией архитектора
  вне брифа (`ADR-145`, `ADR-086 §1`), владелец поручил решать прямо в дереве, не через сборку. (2)
  Первая попытка `weekly`-прогона попала в гонку с штатным часовым пайплайном (общий staging,
  `WRITE_TRUNCATE`) — не порча данных (у патча этой задачи нет ветки удаления), исправлено повтором
  с немедленным `promote` следом. (3) По ошибке сессия скопировала `reference/code/cf-facts/
  MANIFEST.md` в клон код-репо `holika-prod`, где этот файл никогда не был частью репозитория —
  замечено и отменено ДО push, в код-репо ушли только 2 файла кода.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-DOCUMENT-OWNER-DEPLOY (деплой): OPEN → DONE
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-DOCUMENT-OWNER-DEPLOY` (деплой) закрыта DONE — `cf-facts-00010-mog` живой,
    `document_owner_employee_id` в `core.fact_sales_profit` заполнена у 2 912 из 4 707 июльских строк;
    артефакт `reference/sales_document_owner_deploy_2026-08-08.md`.
  - Где мы: разрез продаж по сотруднику через `document_owner_employee_id` физически возможен
    (данные в `core` есть); числовое схождение с эталоном МойСклада (`Q-78`/`ADR-128`) — следующий шаг,
    не эта задача.
  - Следующий шаг: числовая сверка разреза по сотруднику с эталоном МойСклада (новая задача, не
    заведена этой сессией — решение об имени и мандате за владельцем/архитектором); параллельно —
    `SALES-REFRESH-WINDOW` (деплой ветки удаления, класс B, мандат `ADR-145 §C` предусловие ещё не
    снято) остаётся в очереди своим порядком.
  - Развилки на владельце: нет новых.
  - Счётчик: пары реестра 7/7 · карта — · Epic M — (Epic-1 завершён `ADR-140`, фаза передачи клиенту)
- Подробности для модели: `SALES-DOCUMENT-OWNER-DEPLOY` (деплой) закрыта DONE. Живая ревизия
  `cf-facts-00010-mog` (`generation 1786194676108292`, `updateTime 2026-08-08T13:12:55.988390080Z`),
  подтверждена read-back'ом (sha256, 2 изменённых + 9 незатронутых файлов). `core.fact_sales_profit`
  несёт колонку `document_owner_employee_id` (`STRING`); после обязательного `weekly`-прогона
  (`window_days=90`) заполнена у 2 912 из 4 707 июльских строк (было 0). Майский снимок не сдвинулся
  (`3 300` строк / `96 471 991,41` KGS, разность `0,00` — условие 3 мандата `ADR-146 §5(3)` закрыто).
  Найден и подтверждён read-only класс риска: общий `stg_msklad.fact_sales_staging` между ручными
  вызовами сессии и штатным часовым расписанием может перезаписать данные до промоута (у этой задачи
  безвредно — нет ветки удаления; у `SALES-REFRESH-WINDOW`, когда её будут деплоить, тот же риск уже
  назван предусловием `ADR-145 §C`, и это сессия подтвердила его практикой, не только теорией). Полный
  провенанс — `reference/sales_document_owner_deploy_2026-08-08.md`,
  `reference/code/cf-facts/MANIFEST.md` §«Деплой `cf-facts` — сотрудник-владелец документа
  (2026-08-08)».
- Новые открытые вопросы: нет (гонка staging — не новый Q, она уже покрыта предусловием `ADR-145 §C`
  для `SALES-REFRESH-WINDOW`; эта сессия лишь подтвердила его практикой)
- Блокеры: нет
- updated_at: 2026-08-08
- обновил: executor (сессия: SALES-DOCUMENT-OWNER-DEPLOY)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет (мандат и разбор блокировки уже оформлены `ADR-145`/`ADR-146` отдельными сессиями архитектора)

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
