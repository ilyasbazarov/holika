=== SESSION LOG · 2026-08-01 · Q-96 ===

## SESSION_LOG
- Задача: Q-96 — пересчитана ли история `core.fact_payments` после правки, задеплоенной 2026-07-20
- Сделано: исполнена без брифа по `ADR-086 §1` (класс A; различитель и правило вывода процитированы
  дословно из строки `Q-96`; набор файлов на запись объявлен). Замер закрыт положительным
  результатом. Артефакт: `reference/q96_history_check_2026-08-01.md`
- Команды/логи ключевые: один скрипт с якорями `date -u` и `gcloud auth list` по краям
  (`09:24:17Z…09:24:35Z`), шесть read-only запросов `bq query` и один `bq show --schema`. Записи в
  облако нет. Провенанс: `reference/_scratch_Q-96_2026-08-01/`
- Отклонения от плана: одно, названо явно и вынесено в артефакт §2. Строка `Q-96` назначает
  различителем сравнение с ЖИВЫМ API; живой `GET` есть класс B (`ADR-076 §1`) и мандат на него не
  выдан — сама строка мандата `Q-96` разводит read-only часть по `core` (класс A, идёт сейчас) и
  живой `GET` (класс B, поимённо). Взят эквивалентный внутренний различитель: сравнение с
  ЗАПИСАННЫМИ эталонными значениями по двум конкретным документам
  (`reference/recon_vyvod_pribyli_2026-05.md §2`, слой A = live API, прогон 2026-07-14). Он сильнее
  агрегатного сравнения (расхождение локализовано до двух сумм), но не даёт свежести эталона —
  ограничение названо в артефакте

## STATE_PATCH
- `Q-96`: ОТКРЫТ → **ЗАКРЫТ ФАКТОМ, результат положительный.** История пересчитана, бэкфилл НЕ нужен.
  Оба документа, составлявшие всё расхождение мая (`8837df1b-50e5-11f1-0a80-0bfd003170f9` USD и
  `585c8eaf-5f66-11f1-0a80-132d0026fc3b` RUB), несут эталонные значения до копейки: 900 000,00 и
  937,50 против 10 000,00 и 750,00 на 2026-07-14. Весь май (244 строки `paymentout`) переписан одним
  прогоном `2026-07-31 21:00:25…21:11:28 UTC`. `marts.expenses` за май = 10 232 903,20 — точное
  совпадение с записанной целевой величиной эталона (`ADR-057 §5`)
- Задача E1-T3-MECH-FX: статус НЕ меняется этой сессией (замер не адъюдицирует, `_METHOD §11`).
  В строку вносится дописка с фактом и с развилкой владельца
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: замером закрыт `Q-96` — история `core.fact_payments` пересчитана, бэкфилл не нужен;
    оба ранее расходившихся документа мая сошлись с записанным эталоном до копейки, инвесторский март
    мая точно равен целевой величине 10 232 903,20 (`reference/q96_history_check_2026-08-01.md`)
  - Где мы: содержательный результат `E1-T3-MECH-FX` достигнут (правка на проде, история пересчитана,
    числа сошлись), формальная приёмка задачи — нет: ни staging dry-compare, ни свежий live re-diff
    не исполнялись
  - Следующий шаг: развилка владельца по `E1-T3-MECH-FX` (закрыть по записанному эталону либо
    потребовать свежий live re-diff классом B); параллельно доступны `CODE-REPO-STANDUP` шаг 2a,
    `PARITY-CHECK-SALES-RETURNS`, `INFRA-FACTS-SWEEP` и подготовка текстов четырёх запросов Looker Studio
  - Развилки на владельце: чем закрывать `E1-T3-MECH-FX` (записанный эталон или свежий live re-diff);
    создание удалённого репозитория кода и первый `push` — исходящее действие, инструменту не отдаётся;
    `Q-94` (машинная проверка языка ответов) — замерять или нет
  - Счётчик: пары реестра 1/7 сходятся · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
    (без изменений этой сессией — сверок реестра паритета не производилось)
- Подробности для модели: **Замер `Q-96` (2026-08-01) закрыл вопрос положительно.** Метод:
  сравнение не с живым API (класс B, мандата нет), а с ЗАПИСАННЫМИ эталонными значениями двух
  конкретных документов из `reference/recon_vyvod_pribyli_2026-05.md §2` — там расхождение
  `Δ(A↔B) = 890 187,50` было локализовано ровно до двух документов из 25, остальные 23 совпадали до
  копейки. Сегодня оба сошлись: USD-документ `8837df1b…` = 900 000,00 (был 10 000,00), RUB-документ
  `585c8eaf…` = 937,50 (был 750,00). `_loaded_at` всех 244 майских строк `paymentout` лежит в одних
  сутках `2026-07-31`, окно `21:00:25…21:11:28 UTC` — то есть весь май переписан целиком одним
  прогоном через 11 дней после деплоя правки. Окно совпадает с документированным расписанием
  `finance-daily-update` (`0 3 * * *` `Asia/Bishkek` = `21:00 UTC` предыдущих суток), а у соседнего
  джоба задокументировано окно загрузки `2020-01-01 → завтра`; если и здесь так, полный пересчёт
  истории идёт ежесуточно — **тело вызова этим замером не читалось, это согласующаяся гипотеза, не
  факт** (`ADR-021 §2`). `marts.expenses` за май = 10 232 903,20 при 149 строках — точное совпадение
  с целевой величиной эталона. **Что сознательно НЕ сопоставлялось:** агрегат мая по всем
  `paymentout` (244 документа, 8 600 919,01) против числа слоя B эталона (25 документов,
  3 355 976,16) — замер 2026-07-14 был сужен до статьи «Вывод прибыли», множества разные, примирение
  их чисел было бы подгонкой. **Ограничение метода:** эталон датирован 2026-07-14, а МойСклад
  пересчитывает историю при правках задним числом (`ADR-067 §3`), поэтому совпадение проверяет
  соответствие июльскому снимку эталона, а не сегодняшнему состоянию источника.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-01
- обновил: исполнитель (сессия: Q-96)
- Дописка к строке задачи `E1-T3-MECH-FX` (оба вхождения — таблица Epic 1 и GAP-реестр), в конец
  текста статуса:

  **ФАКТ ЗАМЕРА (`Q-96`, 2026-08-01): история пересчитана, числа сошлись.** Оба документа, составлявшие
  всё расхождение мая, несут эталонные значения до копейки (900 000,00 и 937,50 против 10 000,00 и
  750,00 на 2026-07-14); весь май переписан прогоном `2026-07-31 21:00…21:11 UTC`; `marts.expenses`
  за май = 10 232 903,20, точное совпадение с целевой величиной эталона (`ADR-057 §5`). Провенанс:
  `reference/q96_history_check_2026-08-01.md`. **Содержательный результат задачи достигнут, формальная
  приёмка — нет:** ни staging dry-compare (`Q-41`, стадия пройдена, артефакта уже не будет), ни свежий
  прод re-diff против ЖИВОГО API не исполнялись; сравнение шло с записанным эталоном от 2026-07-14.
  **Развилка владельца:** закрыть задачу по записанному эталону либо потребовать свежий live re-diff —
  последний есть класс B и требует поимённого мандата. Переквалификация замером не производится
  (`_METHOD §11`).

## NEW_DECISIONS
нет — замер фактов не адъюдицирует (`_METHOD §11`). Переквалификация `E1-T3-MECH-FX` вынесена
владельцу и архитектору отдельной развилкой.

## NEW_CONVENTIONS
нет

=== END SESSION ===
