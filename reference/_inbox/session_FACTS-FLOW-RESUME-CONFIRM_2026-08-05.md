# FILE: reference/_inbox/session_FACTS-FLOW-RESUME-CONFIRM_2026-08-05.md

=== SESSION LOG · 2026-08-05 · FACTS-FLOW-RESUME-CONFIRM ===

## SESSION_LOG

- Задача: `FACTS-FLOW-RESUME-CONFIRM` — контрольная точка `ADR-121 §5`. Класс A, мандат постоянный,
  гейтов нет. Исполнено без брифа по `ADR-086 §1` (все четыре условия выполнены: класс A; приёмка и
  определение записаны дословно строкой `07_GAPS.md`; набор файлов на запись объявлен полем `Пишет:`
  строки мандата; определение процитировано дословно в артефакте).
- База: `65e90058e80c3df97dd6cdbafd48727e95b89200`. Дерево/ветка:
  `worktrees/FACTS-FLOW-RESUME-CONFIRM` / `s/FACTS-FLOW-RESUME-CONFIRM`.
- UTC-окно: `2026-08-04T18:24:57Z…18:25:08Z`. Личность в начале и в конце совпала.
- **Исход ПОЛОЖИТЕЛЬНЫЙ по обоим критериям строки задачи.** Строки за `08-01` (14 / `25 920,90`),
  `08-03` (18 / `191 799,80`) и `08-04` (67 / `4 625 728,33`) в `core.fact_sales_profit` есть;
  `MAX(_loaded_at)` сдвинулся `2026-08-01 17:02:14` → `2026-08-04 18:02:15`. Блокировку снял прогон
  `cb2c9bae-…` конвейера `msklad-pipeline-hourly`, старт `2026-08-04T18:00:02.699Z`, `SUCCEEDED`;
  пять предыдущих часовых прогонов того же дня — `FAILED`.
- Артефакт: `reference/facts_flow_resume_confirm_2026-08-05.md`; провенанс —
  `reference/_scratch_FACTS-FLOW-RESUME-CONFIRM_2026-08-05/check_attempt2.sh` + `run_attempt2.log`
  (файлы попытки 1 сохранены рядом, не переписаны, `ADR-043`).
- Отклонения от плана: один шаг сверх буквы строки задачи — `gcloud workflows executions list`
  (read-only, тот же класс), мотив объявлен ДО исполнения внутри скрипта: без него отрицательный
  исход был бы неотличим «гейт заблокировал снова» от «прогон ещё идёт», то есть повторил бы
  неинтерпретируемость попытки 1.

## STATE_PATCH

- Задача `FACTS-FLOW-RESUME-CONFIRM`: `OPEN` → **DONE**. Полный текст строки переносится в
  `07_ARCHIVE.md` дословно тем же коммитом (`ADR-064`), строка убирается из `07_GAPS.md` и из индекса
  `07_STATE §Открытые вопросы`; строка мандата `FACTS-FLOW-RESUME-CONFIRM` убирается тем же коммитом
  (парность `ADR-090 §1`).
- Задача `FACTS-BACKFILL`: статус **НЕ меняется** (остаётся `OPEN`). В колонку «как закрывается»
  дописка: «условность вердикта `ADR-121 §6` разряжена В ЧАСТИ ПРОДАЖ — промоут прошёл
  `2026-08-04T18:00:02Z`, `08-01` и `08-03` в ядре, дыры нет
  (`reference/facts_flow_resume_confirm_2026-08-05.md`); закупки и возвраты этим замером НЕ
  измерялись, поэтому строка не закрывается».
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - **Прошлый шаг:** контрольная точка `FACTS-FLOW-RESUME-CONFIRM` дала положительный исход —
    промоут продаж возобновился на прогоне `2026-08-04T18:00:02Z`, блокировка DQ Gate снялась сама,
    как и предсказывал `ADR-121` (`reference/facts_flow_resume_confirm_2026-08-05.md`).
  - **Где мы:** пять пар реестра из семи закрыты; поток продаж восстановлен без вмешательства в
    живой конвейер, мандат класса B на восстановление не понадобился; строка 21 реестра ждёт
    единственного допустимого измерения, проба ретроспективного момента дала отрицательный ответ.
  - **Следующий шаг:** `PARITY-STOCK-SNAPSHOT-SYNC` шаги 2-3 в окне `21:00Z` (класс B, мандат выдан,
    бриф готов); `DQ-GATE-SCOPE-SPLIT-DEPLOY` (класс B, мандат выдан, бриф готов) идёт независимо.
  - **Развилки на владельце:** нет новых.
  - **Счётчик:** пары реестра **5/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели (append в одноимённый подраздел `07_STATE`):

  **Поток продаж восстановился сам, прогноз `ADR-121` подтверждён по дате прогона
  (`FACTS-FLOW-RESUME-CONFIRM`, 2026-08-05).** Полный ход и провенанс —
  `reference/facts_flow_resume_confirm_2026-08-05.md`, здесь не пересказывается. Что обязана знать
  следующая сессия. (i) **Пустая дата `2026-08-02` в ядре дефектом НЕ является и работы под себя не
  требует** — за это воскресенье в источнике ноль документов продажи (`ADR-121 §4`, числа `14`/`0`/`18`
  за `08-01`/`08-02`/`08-03` против обычных `170–220`, независимо подтверждено `yesterday_rev = 0`
  того дня). Наша сторона воспроизводит источник точно, включая ноль; пустая дата в середине
  диапазона есть ровно тот вид наблюдения, который читается как пропуск загрузки и порождает лишнюю
  задачу. (ii) **Прирост таблицы замыкается без остатка:** `36 986 → 37 071`, разность `85` в
  точности равна `18 + 67` новых суток — прогон доехал целиком, а не частично; проверка независима от
  самого факта появления строк. (iii) **Вердикт ограничен продажами.** `core.fact_purchases` и
  `core.fact_returns` этим замером не запрашивались вовсе; успешный прогон конвейера есть основание
  ожидать, что закупки поехали, но ожидание не есть измерение — поэтому `FACTS-BACKFILL` не
  закрывается. Возвраты не могли восстановиться по построению: недельный конвейер, ближайший прогон
  `2026-08-09T01:00Z`. (iv) **Следствие для строки 22 реестра, стоит проверки одним запросом:**
  `ADR-120 §4` объявил пару «Заказы в пути» неизмеримой при остановленной загрузке; загрузка,
  судя по успешному прогону, возобновилась, то есть гейт этой строки вероятно снят — различитель
  есть свежесть `_loaded_at` в `core.fact_purchases`, настоящей сессией не снималась (вне строки
  задачи). (v) Крайний срок вмешательства `2026-08-07` (`ADR-121 §5`) не наступил и больше не
  применяется; ручной промоут, вариант (а), не понадобился.

- Новые открытые вопросы: нет.
- Блокеры: нет.
- updated_at: 2026-08-05
- обновил: исполнитель (сессия: `FACTS-FLOW-RESUME-CONFIRM`)

## NEW_DECISIONS

- нет. Контрольная точка исполнена по уже принятому `ADR-121 §5`; новых решений не требует, статусов
  сверх названных в `STATE_PATCH` не двигает.

## NEW_CONVENTIONS

- нет.

=== END SESSION ===
