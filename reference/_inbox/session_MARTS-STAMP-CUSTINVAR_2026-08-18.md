=== SESSION LOG · 2026-08-18 · MARTS-STAMP-CUSTINVAR ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `MARTS-STAMP-CUSTINVAR` — применение отметки времени сборки на `transferConfig`
  `sq_marts_customer_invoices_ar` (одна из трёх строк применения `MARTS-BUILD-STAMP-PREP`, объект
  `7г1`)
- Сделано: предусловие (i) свежий `bq show --transfer_config` сверен со снимком репо (побайтовое
  совпадение, `diff rc=0`); предусловие (ii) `bq query --dry_run` подготовленного текста, `rc=0`,
  `totalBytesProcessed=1422244`; объявление действия владельцу, дословное «да» получено; первый
  вызов `bq update --transfer_config` НЕВЕРНО заменил весь объект `params`, стерев три поля режима
  записи (`destination_table_name_template`, `partitioning_field`, `write_disposition`) —
  дефекта не было у `weight_flow`, где `params` изначально нёс только `query`; первый ручной прогон
  упал (`errorStatus`: «A destination table must be set with SELECT statements»); инцидент объявлен
  владельцу отдельным сообщением, read-only снятие текущего состояния, повторный `bq update` с
  полным `params` (текст запроса + три восстановленных поля), read-back подтвердил все четыре
  поля; второй ручной прогон `SUCCEEDED`; приёмка трёх пунктов на исправленном состоянии — read-back
  побайтово равен (sha256 совпадает), обе колонки в схеме, обе колонки непустые на всех `586`
  строках
- Команды/логи ключевые: `reference/_scratch_MARTS-STAMP-CUSTINVAR_2026-08-18/step1_run.log` …
  `step12_run.log`; полный разбор инцидента и приёмки — `reference/marts_stamp_custinvar_2026-08-18.md`
- Отклонения от плана: ДА — первый вызов `bq update --transfer_config` стёр три поля `params` не
  из-за решения, а из-за ошибки формы вызова (передан только `query`, инструмент заменяет объект
  целиком, а не сливает поля). Исправлено тем же ходом сессии с явным подтверждением владельца
  перед исправлением. Итоговое состояние соответствует мандату (только текст запроса изменён
  относительно исходного). Урок записан в артефакт для следующих сессий строки `7`
  (`sq_marts_expenses`): перед подменой `query` снимать живой `params` целиком.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `MARTS-BUILD-STAMP-PREP`: остаётся `PARTIAL` — применение на объекте
  `sq_marts_customer_invoices_ar` (`7г1`) исполнено и принято (после исправления инцидента);
  применение на `sq_marts_expenses` (`7г2`) остаётся не исполненным, отдельная сессия, мандат
  выдан (`ADR-193 §5`)
- Дописка к строке реестра `MARTS-BUILD-STAMP-PREP` (добавить фактом к существующей дописке от
  `MARTS-STAMP-WEIGHTFLOW`, не переписывая её): «Применение на `sq_marts_customer_invoices_ar`
  (`7г1`) ИСПОЛНЕНО и ПРИНЯТО (`MARTS-STAMP-CUSTINVAR`, 2026-08-18) — с оговоркой: первый вызов
  `bq update` ошибочно стёр три служебных поля `params` (`destination_table_name_template`,
  `partitioning_field`, `write_disposition`), инцидент исправлен тем же ходом сессии с апрувом
  владельца, итоговое состояние соответствует мандату (только текст запроса изменён). Read-back
  побайтово равен, обе колонки в схеме, обе непусты на `586`/`586` строках. Полный разбор —
  `reference/marts_stamp_custinvar_2026-08-18.md`. **Урок для `sq_marts_expenses`:** перед подменой
  `query` снять живой `params` целиком и включить в payload все его поля — `bq update
  --transfer_config --params` заменяет объект целиком, не сливает. Остаётся `7г2`
  (`sq_marts_expenses`) — мандат выдан, применение не исполнено, отдельная сессия (`ADR-115 §5`)».
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: отметка времени сборки применена и принята на `sq_marts_customer_invoices_ar`
    (`MARTS-STAMP-CUSTINVAR`, `reference/marts_stamp_custinvar_2026-08-18.md`) — второй из трёх
    объектов строки `7`; в процессе исправлен инцидент с потерянными полями `params`
  - Где мы: строка `7` (`MARTS-BUILD-STAMP`) — `2` объекта применения из `3` исполнены
    (`weight_flow`, `customer_invoices_ar`); строка `7` целиком не закрывается, пока не исполнен
    `sq_marts_expenses`
  - Следующий шаг: применение отметки времени сборки на `sq_marts_expenses` (мандат выдан,
    `ADR-193 §5`; перед подменой `query` снять живой `params` целиком — урок этой сессии) ·
    ступень 3 различителя `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE` (мандат класса B выдан)
  - Развилки на владельце: нет
  - Счётчик: строк списка закрытия — `4` из `9` (без изменений этой сессией — строка `7` не
    закрыта целиком) · пары реестра `7`/`7` · Epic M `7` фаз (без изменений)
- Подробности для модели: `sq_marts_expenses` — ЕДИНСТВЕННЫЙ оставшийся объект строки `7`.
  Критический урок этой сессии: `bq update --transfer_config --params='{"query": ...}'` заменяет
  ВЕСЬ объект `params`, а не только переданный ключ. У `weight_flow` `params` нёс только `query` —
  дефект не проявился. У `customer_invoices_ar` `params` нёс дополнительно
  `destination_table_name_template`/`partitioning_field`/`write_disposition` — их подмена только
  `query` стёрла и уронила прогон. Перед правкой `sq_marts_expenses` ОБЯЗАТЕЛЬНО снять живой
  `params` целиком (не только сравнить `query`) и включить в payload обновления ВСЕ поля, которые
  там есть, с новым текстом запроса вместо старого. Read-only снятие полного `params`, а не только
  текста — новый обязательный под-шаг предусловия (i) для этого объекта.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-18
- обновил: исполнитель (сессия: `MARTS-STAMP-CUSTINVAR`)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
