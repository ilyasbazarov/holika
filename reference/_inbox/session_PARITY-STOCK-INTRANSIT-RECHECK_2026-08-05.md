=== SESSION LOG · 2026-08-05 · PARITY-STOCK-INTRANSIT-RECHECK ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `PARITY-STOCK-INTRANSIT-RECHECK` — первое допустимое измерение строки 22 реестра паритета
  («Заказы поставщикам в пути») после восстановления загрузки `core.fact_purchases`
- Сделано: свежесть снята (`16` мин до живого запроса, в пределах часового окна); наша сторона по
  собственному фильтру моста (`16` заказов / `251` позиция / `78 184 949,42-43` KGS); живой
  `GET entity/purchaseorder` к источнику по собственному признаку (`17` заказов / `341` позиция /
  `251` ненулевых / `78 184 949,43` KGS); построчная сверка в обе стороны — `Δ = 0,00` без остатка
  (`251=251` совпавших ключей, `0` сирот с любой стороны); судьба всех четырёх заказов прежнего
  расхождения установлена поимённо на обеих сторонах; предсказание `ADR-120 §2` подтверждено
  измерением. Артефакт: `reference/parity_intransit_recheck_2026-08-05.md`. Строка 22 реестра
  дописана (`reference/parity_registry.md`, только эта строка), статус сходимости НЕ проставлен.
- Команды/логи ключевые: `reference/_scratch_PARITY-STOCK-INTRANSIT-RECHECK_2026-08-05/`
  (`step1_freshness.sh`, `step2_our_side.sh`, `step3_source_side.sh`, `step5_prior_tails_our_side.sh`
  и соответствующие `run_step*.log`)
- Отклонения от плана: нет

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `PARITY-STOCK-INTRANSIT-RECHECK`: READY → DONE (измерение выполнено; вердикт по паре — не
  этой задачей)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: измерена строка 22 реестра после восстановления загрузки закупок — `Δ → 0,00`,
    предсказание `ADR-120 §2` подтверждено (`reference/parity_intransit_recheck_2026-08-05.md`).
  - Где мы: пять пар реестра сходятся; строка 22 измерена и не расходится, но статус сходимости
    ставит архитектор (`PARITY-STOCK-INTRANSIT-CLOSE`); у строки 21 остаётся один шаг из трёх.
  - Следующий шаг: `PARITY-STOCK-INTRANSIT-CLOSE` — адъюдикация вердикта строки 22 по этому
    измерению (архитектор); `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 (строка 21) идёт независимо;
    `DQ-GATE-SCOPE-SPLIT-DEPLOY` идёт независимо.
  - Развилки на владельце: нет новых.
  - Счётчик: пары реестра **5/7** сходятся, строка 22 измерена (вердикт не мой) · измерено 7/7 ·
    Epic M 6/7 фаз.
- Подробности для модели: **Измерение строки 22 закрыто числом, вердикт архитектора остаётся
  открытым.** Ключевое для следующей сессии: `Δ = 0,00` подтверждён построчно (`251` позиций,
  ноль расхождений, ноль сирот в обе стороны, обе стороны по собственному признаку). Механизм
  прежнего расхождения `+2 234 268,75` KGS подтверждён как заморозка, а не структурный дефект —
  оба заказа одностороннего хвоста (`08bfe70e-…`, `e7d7a18b-…`) на момент этого замера несут статус
  «Прибыл» на ОБЕИХ сторонах (источник и наша сторона согласны), то есть выпали из целевого фильтра
  симметрично, а не «исчезли». Побочное наблюдение, требующее внимания при следующей работе с этой
  парой: множество заказов `core.fact_purchases` (объект паритета) разошлось с `marts.in_transit`
  на момент замера (`1` заказ только в `core`, `2` только в марте) — похоже на задержку пересборки
  витрины относительно часового прогона ядра; не расследовалось, вне scope этой задачи. Также не
  расследовано: `in_transit_sum_kgs` на нашей стороне не обнуляется у заказов со статусом «Прибыл»
  (оба разрешённых заказа хвоста несут прежние ненулевые суммы при новом статусе) — на измерение не
  влияет (заказы вне целевого фильтра на обеих сторонах), но это наблюдение о поведении загрузчика,
  которое стоит иметь в виду, если кто-то возьмётся за смежную задачу по `cf-facts`/закупкам. Полный
  ход, числа и провенанс — `reference/parity_intransit_recheck_2026-08-05.md`, здесь не пересказано.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-05
- обновил: исполнитель (сессия: `PARITY-STOCK-INTRANSIT-RECHECK`)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
