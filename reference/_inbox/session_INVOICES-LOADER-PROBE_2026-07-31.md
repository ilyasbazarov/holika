=== SESSION LOG · 2026-07-31 · INVOICES-LOADER-PROBE ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `INVOICES-LOADER-PROBE` — снять замером вопрос, который `07_STATE §Стенд-ап` (строка
  «Следующий шаг», сборка 2026-07-31) ставил прямым вопросом владельцу: существует ли где-то скрипт
  или файл `2026-06-05`, сформировавший исходный NDJSON для `core.fact_customer_invoices`.
  Архитектор-сессия по прямому указанию владельца; работала в дереве `worktrees/STATE-COMPACT-GEN`
  и коммитила в ветку `s/STATE-COMPACT-GEN` (своего дерева не заводила — сессия не по брифу).
  Класс A: только чтение, записи в облако нет.
- Повод: `ADR-056 §5` — вопрос, который роль может снять своей работой, владельцу не задаётся.
  Часть вопроса снималась замером и была задана напрасно; вторая претензия владельца — язык строки
  (нечитаема без открывания репо).
- Сделано:
  - Локальная машина владельца (macOS): поиск `*.ndjson`/`*.jsonl` по всему домашнему каталогу — ноль
    (кроме служебных логов Claude Code); файлы с mtime 2026-06-04…06 — только выгрузки Google Drive;
    поиск по содержимому `Desktop`/`Downloads`/`Documents` по `invoiceout`, `fact_customer_invoices`,
    `bq load`, `ndjson` — только документация проекта и витринный `sq_marts_customer_invoices_ar.sql`.
    Загрузчика нет.
  - Диск Cloud Shell (`gcloud cloud-shell ssh`, read-only, два прогона): `*.ndjson`/`*.jsonl` во всём
    `$HOME` — ноль; файлы с mtime 2026-06-05 — только четыре debug-лога npm; файлы со словом `invoice`
    в имени — один, витринный SQL; `.bash_history` (576 строк) — ни одной строки с `bq load`,
    `ndjson`, `invoice`, `load_`. Загрузчика нет.
  - **Найден соседний загрузчик того же класса: `~/load_payments.py` (Cloud Shell, mtime 2026-06-18).**
    Полный текст снят в репо: `reference/_scratch_STATE-COMPACT-GEN_2026-07-31/load_payments_cloudshell_2026-06-18.py`.
- Артефакт: `reference/_scratch_STATE-COMPACT-GEN_2026-07-31/FINDING.md` (разбор + провенанс),
  коммит `c8bf382` в `s/STATE-COMPACT-GEN`.
- Команды/логи ключевые: `cloudshell_probe.sh` + `remote_probe.sh` → `run.log`;
  `cloudshell_probe2.sh` + `remote_probe2.sh` → `run2.log`. Оба прогона несут `date -u` и
  `gcloud auth list` первой И последней командой (`ADR-055/063`): активный принципал
  `ilyasbazarov4@gmail.com` на входе и на выходе, `RC_SSH=0`, деградации авторизации нет.
  Хук: `.git/hooks/pre-commit` установлен в общем git-каталоге (един на все деревья, `ADR-080 §5i`);
  `bash tools/hooks/selftest.sh` → «пройдено 28, провалено 0».
- Отклонения от плана: замер расширен вторым прогоном (снятие текста `load_payments.py` и поиск
  архивов/истории) после того, как первый прогон обнаружил соседний загрузчик. Основание расширения —
  тот же read-only метод, тот же класс A, и находка прямо меняет квалификацию `Q-82`.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `STATE-COMPACT`: READY → READY (не исполнялась; бриф выдан сессией `STATE-COMPACT-GEN`)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: сессия `STATE-COMPACT-GEN` выдала бриф `briefs/STATE-COMPACT.md` (уборка реестра
    открытых вопросов); следом замером `INVOICES-LOADER-PROBE` снят вопрос о происхождении счетов
    покупателям — скрипта, которым 5 июня вручную загружена таблица `core.fact_customer_invoices`,
    нет ни на машине владельца, ни на диске Cloud Shell, зато найден соседний загрузчик той же
    механики `load_payments.py` (Cloud Shell, 18 июня), текст снят в репо. Артефакт:
    `reference/_scratch_STATE-COMPACT-GEN_2026-07-31/FINDING.md`
  - Где мы: происхождение данных выяснено для 6 поверхностей из 7; сходится числом 1 пара реестра
    паритета из 7; построчных сверок не запускалось ни одной. По счетам покупателям соответствие
    «поле МойСклада → колонка» по-прежнему не выведено, но теперь есть рабочий образец того же
    автора и того же периода, по которому его можно восстановить
  - Следующий шаг: исполнить бриф уборки реестра `briefs/STATE-COMPACT.md` (своим деревом
    `worktrees/STATE-COMPACT`), затем снять имена полей на стороне МойСклада — задача
    `REPORT-FIELDS`, которой нужна отдельная сессия генерации брифа `REPORT-FIELDS-GEN`
  - Развилки на владельце: (1) `LS-RETURNS-FX-HARDCODE` — жёстко зашитый курс валюты в запросе
    возвратов, прежняя формулировка без изменений; (2) заводить ли отдельную задачу на восстановление
    соответствия полей для счетов покупателям по найденному образцу — сам вопрос «сохранился ли где-то
    исходный код» СНЯТ замером и владельцу больше не задаётся
  - Счётчик: пары реестра 1/7 сходятся · карта происхождения 6/7 поверхностей · Epic M 5/7 фаз ·
    реестр открытых вопросов 58 строк до уборки задачей `STATE-COMPACT` (число пересчитано сессией
    `STATE-COMPACT-GEN` построчно; «49» из текста строки-задачи устарело)
- Подробности для модели: Замер `INVOICES-LOADER-PROBE` (2026-07-31, `13:48:25Z…13:52:54Z`) уточняет
  наблюдение `reference/source_map_invoices_2026-07-30.md §4`. Там отсутствие поля `sourceUris` в
  конфигурации `LOAD`-задания было прочитано как «`bq load` ЛОКАЛЬНОГО NDJSON-файла». Найденный
  соседний загрузчик `load_payments.py` показывает механику точнее: он вызывает
  `bigquery.Client.load_table_from_json(records, core.fact_*_stg, NEWLINE_DELIMITED_JSON,
  WRITE_TRUNCATE)`, то есть сериализует список словарей из памяти и грузит его потоком — картина
  задания та же (`sourceUris` нет, `inputFiles=1`), но **NDJSON при этом никогда не существует как
  файл на диске**. Если счета грузились тем же приёмом — а форма задания, `_loaded_at`,
  проставленный до загрузки, и `MERGE` с явным списком колонок совпадают, — то поиск файла NDJSON
  бессмыслен по построению. Отсутствие таких файлов на обеих проверенных машинах с этим объяснением
  согласуется. Пределы замера названы явно: проверены только локальная машина владельца и диск Cloud
  Shell; Colab, Google Drive и прочие устройства не проверялись, а `.bash_history` Cloud Shell
  ограничен по длине и до начала июня не достаёт — отсутствие следа там доказательством отсутствия
  события не является. Следствие для `Q-82`: ветка «найти исходный код счетов» практически исчерпана,
  ветка «восстановить соответствие полей заново» перестаёт быть работой с нуля — `load_payments.py`
  даёт форму соответствия «поле ответа МойСклада → колонка `core.fact_*`» от того же автора и того же
  периода. Отдельно: `load_payments.py` использует `INSERT` с явным списком колонок, то есть `C1`
  (`ADR-030`) им не нарушается.
- Новые открытые вопросы: одна строка (номер `Q-` присваивает сборка) — формулировка «один `LOAD`-job
  из локального NDJSON (не GCS)» присутствует в `07_ARCHIVE.md` (строка `SOURCE-MAP-INVOICES`) и в
  `11_INFRA_FACTS.md §Ручные/разовые загрузки (не CF)`; замером она уточнена (файла на диске не было,
  загрузка потоком из памяти). Правка не вносится тихо: `07_ARCHIVE` по `ADR-064` несёт перенесённый
  дословно текст, поэтому способ внесения уточнения — решение сборки, а не сессии.
- Блокеры: нет
- updated_at: 2026-07-31
- обновил: архитектор (сессия: INVOICES-LOADER-PROBE)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
