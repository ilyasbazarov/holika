=== SESSION LOG · 2026-08-12 · SALES-REFRESH-WINDOW-CODE-ROLLBACK ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-REFRESH-WINDOW-CODE-ROLLBACK — вернуть обслуживание `cf-facts` на ревизию без ветки
  удаления, класс B, мандат выдан поимённо (`reference/sales_refresh_window_rollback_probe_mandate_2026-08-12.md §2`).
  База: `320731f` (`main` после сборки буфера 2026-08-12, второй проход).
- Сделано:
  - Старт сессии по CLAUDE.md пройден: `tools/session_status.sh` показал два незавершённых
    рабочих дерева/ветки сессий (`SALES-REFRESH-WINDOW-DEFECT-ADJ`, `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`)
    — стоп-условие; владелец подтвердил проход сборки; сборка слила обе ветки, присвоила
    `ADR-165`/`ADR-166`, вычистила деревья (SHA после сборки `320731f`). Дерево/ветка сессии
    заведены только после этого.
  - Хук pre-commit подтверждён общим на все деревья, `selftest.sh` — 36/36, провалено 0.
  - Обязательный контекст + весь `context-to-load` брифа-мандата прочитаны и проверены по
    ADR-054 (первая строка = имя файла), включая полный текст мандата (`§1-§6`),
    `…defect_adj…§6/§7`, `…deploy_v2…§2/§3/§7`, строку `SALES-REFRESH-WINDOW` в `07_GAPS.md`.
  - Диагностика ДО действия (read-only, `step0_pretraffic.sh`): подтверждён факт — расхождение
    живой таблицы `core.fact_sales_profit` со снимком `snap_20260811_163306` уже составляло `36`
    строк (`6`+`28`+`2` по трём каналам), совпадает с известным числом удалённых строк.
  - Действие объявлено отдельным сообщением в чат до исполнения (`ADR-077 §6`).
  - Исполнена ровно одна не-идемпотентная команда (`step1_traffic_rollback.sh`):
    `gcloud run services update-traffic cf-facts --to-revisions=cf-facts-00011-mab=100`.
  - Read-back 1 (сразу после перевода, часть `step1`): `status.traffic` = `cf-facts-00011-mab`, `100%`.
  - Read-back 2 (`step2_readback2.sh`): расхождение со снимком осталось `36`, ни одной новой
    строки сверх известных.
  - Read-back 3 (`step3_readback3.sh`, после первого ЕСТЕСТВЕННОГО часового прогона `~09:00Z`,
    ручных `mode=*` не запускалось): расхождение со снимком осталось `36`, ни одной новой строки;
    Cloud Logging подтвердил — запросы `cf-facts` после перевода обслужены только ревизией
    `cf-facts-00011-mab` (`15` записей с этим `revision_name`), ни одной записи `cf-facts-00014-doh`.
  - Ни одна ревизия не удалена; данные `core.fact_sales_profit` не тронуты; снимок
    `snap_20260811_163306` не тронут; ветка код-репо `deploy/cf-facts-2026-08-11-refresh-window-v2`
    в `master` не слита; `transferConfig`/Scheduler/Workflows не тронуты.
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-CODE-ROLLBACK_2026-08-12/`
  (`step0_pretraffic.sh`+`step0_run.log`, `step1_traffic_rollback.sh`+`step1_run.log`,
  `step2_readback2.sh`+`step2_run.log`, `step3_readback3.sh`+`step3_run.log`).
- Отклонения от плана: диагностический запрос `step0` первой попыткой содержал ошибку
  (`COUNT(l.transaction_id)` после фильтра `l.transaction_id IS NULL` — тривиальный `0`,
  инстанс правила «успех инструмента ≠ факт»); исправлен на `COUNT(*)` до принятия числа за
  факт, лог первой (бракованной) попытки не сохранён — перезапись файла до коммита, второй
  запуск дал содержательный результат, совпавший с известными `36`.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-REFRESH-WINDOW-CODE-ROLLBACK: READY → DONE. Трафик `cf-facts` на `cf-facts-00011-mab`,
  все три read-back мандата чисты, новых удалённых строк не найдено.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: откат кода `cf-facts` на ревизию без ветки удаления исполнен и подтверждён тремя
    read-back (трафик, сразу после перевода, после первого естественного часового прогона) — ни
    одной новой удалённой строки; артефакт `reference/_scratch_SALES-REFRESH-WINDOW-CODE-ROLLBACK_2026-08-12/`.
  - Где мы: живой трафик безопасен (ветка удаления снята с прода); `29` строк остаются
    неквалифицированными; недельный прогон `2026-08-16T01:00Z` больше не несёт риска новых
    неконтролируемых удалений, потому что код без ветки удаления снова на трафике.
  - Следующий шаг: `SALES-REFRESH-WINDOW-SOURCE-PROBE` (класс B, только чтение, мандат выдан
    `ADR-166 §2`) — установить, живы ли в источнике `29` удалённых позиций.
  - Развилки на владельце: нет новых; прежние (`SOURCE-PROBE` мандат) уже выданы и остаются в силе.
  - Счётчик: строк списка закрытия осталось `8` из `9` (без изменения — откат кода был шагом
    внутри списка, не отдельной строкой закрытия).
- Подробности для модели: диагностический запрос сопоставления `core.fact_sales_profit` со
  снимком `snap_20260811_163306` — рабочая форма для будущих read-back этой ветки:
  `LEFT JOIN … USING (transaction_id) WHERE l.transaction_id IS NULL`, считать `COUNT(*)`,
  НЕ `COUNT(l.transaction_id)` (тот столбец после такого фильтра тривиально `NULL`/`0`).
  Полный текст запросов и все четыре лога — в `reference/_scratch_SALES-REFRESH-WINDOW-CODE-ROLLBACK_2026-08-12/`.
- Новые открытые вопросы: нет.
- Блокеры: нет.
- updated_at: 2026-08-12
- обновил: executor (сессия: SALES-REFRESH-WINDOW-CODE-ROLLBACK)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет.

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет.

=== END SESSION ===
