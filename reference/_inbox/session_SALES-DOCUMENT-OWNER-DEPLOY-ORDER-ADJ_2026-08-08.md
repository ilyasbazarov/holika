=== SESSION LOG · 2026-08-08 · SALES-DOCUMENT-OWNER-DEPLOY-ORDER-ADJ ===

> **ВНИМАНИЕ СБОРЩИКУ.** Этот блок УЖЕ ПРИМЕНЁН его автором по прямому указанию владельца
> («решить внутри рабочего дерева, не откладывая до прохода сборки»): `ADR-145` присвоен и внесён
> в `06_DECISIONS_LOG.md`/`06_INDEX.md`, правки `07_STATE.md`/`07_GAPS.md` внесены тем же коммитом
> `s/SALES-DOCUMENT-OWNER-DEPLOY`. Отступление от `ADR-080 §4` названо явно. **Повторно применять
> `STATE_PATCH` и `NEW_DECISIONS` НЕ нужно; номер ADR не переприсваивать.** Блок лежит в буфере как
> провенанс сессии, а не как задание.

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-DOCUMENT-OWNER-DEPLOY-ORDER-ADJ` — порядок и форма деплоя двух патчей одного файла
  `reference/code/cf-facts/bq_ops.py` (класс A, архитектор, без брифа, `ADR-086 §1`; постановка —
  вопрос исполнителя `SALES-DOCUMENT-OWNER-DEPLOY`, шаг 3 брифа заблокирован).
- Сделано: дифф снапшота против архива код-репо перегенерирован независимо от артефакта сессии и
  разобран по хункам — посылка «патчи переплетены, разделение редакторское» опровергнута; найден
  девятый хунк, не принадлежащий ни одной задаче; найдено отсутствие предохранителя от
  рассогласования окна staging и окна `MERGE`, из-за которого у ветки удаления нет работающего
  отката; порядок деплоя решён четырьмя независимыми основаниями; объединённый архив отклонён.
  Артефакт — `reference/sales_document_owner_deploy_order_adj_2026-08-08.md`, решение — `ADR-145`.
- Команды/логи ключевые: `bash tools/session_status.sh` (RC 1, стоп снят указанием владельца),
  `git rev-parse HEAD` = `25669a0` (merge-base ветки совпадает с `HEAD` `main`),
  `bash tools/hooks/selftest.sh` (провалено 0), `diff -u` архива код-репо против снапшота
  (9 хунков, воспроизведено точно), чтение `main.py`/`bq_ops.py`/`workflow_weekly.yaml` снапшота,
  `11_INFRA_FACTS.md:27`. Живых вызовов к облаку и к МойСкладу нет, `bq`/`gcloud` не запускались,
  код не правился.
- Отклонения от плана: два. (1) Стоп-условие старта (три неслитых ветки и их деревья) снято прямым
  указанием владельца. (2) `ADR` и `STATE_PATCH` применены самой сессией, а не сборкой — тем же
  указанием; см. врезку выше.

## STATE_PATCH  (→ ПРИМЕНЕНО ЭТОЙ СЕССИЕЙ, сборщику повторно не применять)
- Задача `SALES-DOCUMENT-OWNER-DEPLOY`: `OPEN` → `OPEN` (шаг 3 разблокирован, статус не меняется —
  остаётся невыданный мандат класса B на сам деплой).
- Задача `SALES-REFRESH-WINDOW`: `OPEN` → `OPEN` (добавлено предусловие деплоя).
- Задача `SALES-DOCUMENT-OWNER-DEPLOY-ORDER-ADJ`: заводится и закрывается DONE одним проходом
  (прецедент `INVOICES-STALENESS-PROBE`, `FINISH-QUEUE-ADJ`, `DQ-GATE-DEPLOY-ADJ`); отдельной строки
  реестра не получает — предмет целиком покрыт двумя уже существующими строками, новая строка
  запрещена `ADR-140 §4` и не нужна (`ADR-143 §8` прецедент).
- Стенд-ап — заменён целиком (применено).
- Подробности для модели — добавлен один абзац (применено).
- Новые открытые вопросы: нет.
- Блокеры: `SALES-DOCUMENT-OWNER-DEPLOY` — мандат класса B на деплой (не выдан);
  `SALES-REFRESH-WINDOW` — предусловие `ADR-145 §4` плюс мандат класса B (не выдан).
- updated_at: 2026-08-08
- обновил: архитектор (сессия: SALES-DOCUMENT-OWNER-DEPLOY-ORDER-ADJ)

## NEW_DECISIONS  (→ ПРИМЕНЕНО ЭТОЙ СЕССИЕЙ, сборщику повторно не применять)
- `ADR-145` — полный текст внесён в `06_DECISIONS_LOG.md` этим же коммитом, строка в `06_INDEX.md`
  тем же коммитом. Заголовок: «Два патча `cf-facts/bq_ops.py` разделяются механически по хункам, а
  не редакторским решением; порядок — сначала патч владельца документа; деплой ветки удаления
  получает предусловие, которого у него не было». Статус: §1-§5 accepted (архитектор).

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
