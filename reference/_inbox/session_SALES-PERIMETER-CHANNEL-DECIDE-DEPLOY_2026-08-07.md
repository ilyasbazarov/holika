=== SESSION LOG · 2026-08-07 · SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY — деплой метки канала периметра продаж в `cf-facts`
  (класс B), гейт в шапке брифа: исполнение блокировано до поимённого ADR о мандате.
- Сделано: свежая проверка мандата (07_STATE — НЕ выдан, подтверждено); шаг 1 (py_compile патча
  ADR-134, свежий прогон — OK); шаг 2 (снапшот живой ревизии `cf-facts-00008-zen`, побайтовая сверка
  с `master` код-репо — полное совпадение, дрейфа нет); шаг 3 (ветка
  `deploy/cf-facts-2026-08-07-channel` от `master`, патч перенесён по diff, изолирован от посторонней
  правки `document_owner_employee_id`, обнаруженной в локальном снапшоте `bq_ops.py` — см. находку
  ниже; `.gcloudignore` уже чист, секретов не найдено); шаг 4 (коммит `84d6f71`, push ветки
  подтверждён владельцем отдельным вопросом в чате, выполнен). Шаг 5 и далее — не начаты, брифом
  запрещено начинать без мандата.
- Находка: снапшот `reference/code/cf-facts/bq_ops.py` в рабочем дереве нёс смешанный недеплоенный
  патч двух задач — `ADR-134` (эта задача) и `document_owner_employee_id` (`SALES-DOCUMENT-OWNER-
  DEPLOY`, отдельный мандат, вне scope). Перенос по diff (не копированием) поймал это до коммита;
  патч в ветку деплоя перенесён вручную, хунк за хунком, только по `sales_channel_id`/
  `sales_channel_name`. Полный разбор — `reference/sales_perimeter_channel_decide_deploy_2026-08-07.md
  §Находка`.
- Команды/логи ключевые: `reference/_scratch_SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY_2026-08-07/
  step2_run.log` (снапшот+сверка), `step3_work/step3_final_*.diff` (изолированный патч),
  `step3_work/repo` (ветка, коммит `84d6f71`, запушена на `origin`).
- Отклонения от плана: перенос патча `bq_ops.py` сделан вручную по хункам вместо прямого `git apply`
  полного diff — необходимо для исключения посторонней правки (см. «Находка» выше); в остальном план
  брифа (шаги 1–4) выполнен буквально.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-PERIMETER-CHANNEL-DECIDE, деплой: READY (мандат не выдан) → READY, ветка деплоя
  подготовлена и запушена (`deploy/cf-facts-2026-08-07-channel`, коммит `84d6f71`), мандат
  по-прежнему НЕ выдан — статус мандата этой сессией не менялся и не может меняться.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: ветка деплоя `cf-facts` для метки канала периметра (`ADR-134`) подготовлена,
    патч перенесён по diff и изолирован от посторонней правки, закоммичена и запушена
    (`deploy/cf-facts-2026-08-07-channel`, `84d6f71`) — `reference/sales_perimeter_channel_decide_deploy_2026-08-07.md`.
    Деплой не выполнялся, мандат класса B не выдан.
  - Где мы: правка готова и лежит в запушенной ветке код-репо; прод `cf-facts` не менялся, стоит на
    `cf-facts-00008-zen`. Не хватает ровно поимённого ADR, выдающего мандат на шаги 5–10.
  - Следующий шаг: выдать поимённый ADR на мандат класса B для `SALES-PERIMETER-CHANNEL-DECIDE,
    деплой` (форма — прецедент `ADR-092`/`ADR-108`/`ADR-117`/`ADR-118`/`ADR-126 §6`), затем исполнить
    шаги 5–10 брифа `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY` поверх уже подготовленной ветки; отдельно
    и параллельно — `SALES-PERIMETER-PARITY-RECHECK`.
  - Развилки на владельце: выдача мандата класса B на деплой `cf-facts` этим патчем — да/нет/когда.
  - Счётчик: без изменений этой сессией (подготовка деплоя не пара реестра паритета).
- Подробности для модели: база деплоя подтверждена свежим замером — `cf-facts-00008-zen`,
  `generation 1786093276804812`, побайтовое совпадение с `master` (HEAD `0c5f68e`) на момент этой
  сессии. Ветка `deploy/cf-facts-2026-08-07-channel` (коммит `84d6f71`) готова к деплою без
  дополнительной подготовки — следующая сессия с мандатом начинает сразу с шага 5 брифа, не с шага 1.
- Новые открытые вопросы: нет.
- Блокеры: мандат класса B на деплой `cf-facts` (задача `SALES-PERIMETER-CHANNEL-DECIDE, деплой`) не
  выдан — единственный блокер завершения этого брифа.
- updated_at: 2026-08-07
- обновил: исполнитель (сессия: SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
