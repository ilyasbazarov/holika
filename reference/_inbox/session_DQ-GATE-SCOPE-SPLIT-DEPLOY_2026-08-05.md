=== SESSION LOG · 2026-08-05 · DQ-GATE-SCOPE-SPLIT-DEPLOY ===

## SESSION_LOG
- Задача: DQ-GATE-SCOPE-SPLIT-DEPLOY — деплой готового патча периметра DQ Gate на два живых Cloud Workflows
- Сделано:
  - Шаг 0-1: предусловия чисты, мандат/класс совпали с брифом; свежий снимок живых `msklad-pipeline-hourly`/`msklad-pipeline-weekly` снят, `revisionId` совпал с `000003-f02`/`000003-fa9`, sha256 совпал со снимком `2026-08-02` — дрейфа нет
  - Шаг 2-3: seed исходных ревизий в `master` код-репо `holika-prod` (коммит `f293555`, push подтверждён владельцем); патч перенесён по diff в ветку `deploy/workflows-2026-08-05-dq-scope-split` (коммит `e79bef6`, push подтверждён владельцем); sha256 файлов ветки точно совпал с заранее объявленным sha256 патча; поиск секретов — 0 совпадений, напечатано явно
  - Шаг 4: действие объявлено отдельным сообщением перед каждым не-идемпотентным шагом (push seed, push ветки, деплой, merge) — все четыре подтверждены владельцем по одному
  - Шаг 5: деплой выполнен (сначала hourly, затем weekly) — новые ревизии `000004-5fc`/`000004-6bf`, `state ACTIVE`
  - Шаг 6: read-back побайтово подтверждён для обоих объектов через `--format=json`+извлечение поля (найдено: `--format="value(sourceContents)"` добавляет свой перевод строки — не факт о контенте, метод read-back исправлен по ходу)
  - Шаг 7: живой прогон hourly (`execution a28be854…`, `06:00:02Z…06:05:57Z`, `SUCCEEDED`) показал по HTTP-логам `cloud_run_revision`, что вызов `cf-facts`, соответствующий позиции `step_purchases`, завершается ровно к началу вызова `cf-dq` — то есть до `step_dq`; прикладной лог `mode=` от `cf-facts` отсутствовал в окне — назван как гэп наблюдения, не примирён тихо
  - Шаг 8: `merge --no-ff` ветки в `master`, push подтверждён владельцем — коммит `6a581bf`; запись «ревизия ↔ коммит» в `reference/code/cf-facts/MANIFEST.md`
- Команды/логи ключевые: `reference/_scratch_DQ-GATE-SCOPE-SPLIT-DEPLOY_2026-08-05/` (step1…step7, все с `date -u`/`gcloud auth list` в начале и конце скриптов деплоя); артефакт `reference/dq_gate_scope_split_deploy_2026-08-05.md`
- Отклонения от плана: метод read-back Шага 6 скорректирован по ходу (json-извлечение вместо `value(sourceContents)`) — расхождение форматтера `gcloud`, не самого объекта; на конечный результат и приёмку не повлияло, sha256 совпали точно

## STATE_PATCH
- Задача DQ-GATE-SCOPE-SPLIT-DEPLOY: READY → DONE
- Задача DQ-GATE-SCOPE-SPLIT (родительская): OPEN → CLOSED (закрыта деплоем; подготовка и деплой оба исполнены)
- Стенд-ап:
  - Прошлый шаг: патч периметра DQ Gate задеплоен на оба живых Cloud Workflows (`msklad-pipeline-hourly` → `000004-5fc`, `msklad-pipeline-weekly` → `000004-6bf`), read-back побайтово подтверждён, функциональная проверка на живом прогоне hourly пройдена (`reference/dq_gate_scope_split_deploy_2026-08-05.md`)
  - Где мы: пять пар реестра из семи закрыты (без изменений этой сессией — сессия не про реестр паритета); периметр DQ Gate больше не гасит закупки/возвраты при провале чека продаж
  - Следующий шаг: `DQ-GATE-SCOPE-CONFIRM` (подтверждение на первом реальном провале `drift_check`, класс A, параллель да); `PARITY-STOCK-SNAPSHOT-SYNC` шаги 2-3 идёт независимо (класс B, мандат выдан, бриф готов)
  - Развилки на владельце: нет новых
  - Счётчик: пары реестра 5/7 сходятся · измерено 7/7 · Epic M 6/7 фаз
- Подробности для модели: Найден методический факт для будущих деплоев Cloud Workflows: `gcloud workflows describe --format="value(sourceContents)"` добавляет собственный завершающий перевод строки к выводу — это артефакт форматтера `gcloud`, не свойство самого объекта; побайтовый read-back Workflows следует делать через `--format=json` и программное извлечение поля `sourceContents`, иначе сверка даёт ложное расхождение в 1 байт на КАЖДОМ объекте (не только на weekly, где байтовое отличие уже было ожидаемым по другой причине — Вход 4 брифа). Это относится к процедуре `05_CONVENTIONS §Процедура деплоя … вариант Б` в части «форма для Workflows» — следующая сессия, деплоящая Workflows, не должна заново натыкаться на это как на неожиданность.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-05
- обновил: исполнитель (сессия: DQ-GATE-SCOPE-SPLIT-DEPLOY)

## NEW_DECISIONS
нет

## NEW_CONVENTIONS
нет

=== END SESSION ===
