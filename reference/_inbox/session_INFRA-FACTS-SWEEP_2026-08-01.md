=== SESSION LOG · 2026-08-01 · INFRA-FACTS-SWEEP ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: INFRA-FACTS-SWEEP — закрытие хвоста семи read-only гэпов (`Q-6`, `Q-11`, `Q-12`, `Q-13`, `Q-14`, `Q-47`, `Q-55`)
- Сделано:
  - `Q-6`: актуальная ревизия `cf-dq` = `cf-dq-00007-hot`, `updateTime 2026-07-30T10:04:58Z` — новее `cf-dq-00006-lac` (предшествовавшей T-1-фиксу 2026-06-24)
  - `Q-12`: `cf-alert` — полные факты (URL, ревизия `cf-alert-00001-bej`, секреты `telegram-bot-token`/`telegram-chat-id`)
  - `Q-13`: `msklad-pipeline-weekly` — полный состав шагов снят (`sourceContents`): `dim → fx → facts(weekly) → dq → check_dq → promote(90d) → purchases → returns(90d) → done`
  - `Q-14`: read-only разведка — код снапшотов пишет в `GCS_RAW`, но не читает обратно (0 совпадений по чтению); история BigQuery LOAD-заданий в `core.*` ограничена окном `2026-04-29…2026-05-21` (бутстрап), после мая — 0 LOAD-заданий в `core`. Процедура `RUNBOOK_v8 §11.2/§11.3` зафиксирована как не документированная нигде и не восстановленная этим замером (не гейт, не догадка)
  - `Q-47`: `core.fact_loss` = 129 строк (было 128 на 2026-07-23), не-KGS = 0. Артефакт уложен, DEFER-статус НЕ переоткрыт
  - `Q-55`: три `COUNTIF` по `core.fact_loss` (129 строк) — все три = 0. Условие переоткрытия `ADR-036` не наступило, `ADR-036` не тронут
  - `Q-11`: свежая выгрузка `marts.abc_xyz` — X=28/Y=164/Z=338 (было X=39/Y=148/Z=439 на 2026-06-05); ABC A=112/B=151/C=267; расхождение зафиксировано как факт, не сглажено
- Команды/логи ключевые: `reference/_scratch_INFRA-FACTS-SWEEP_2026-08-01/step0…step4` (все read-only: `gcloud functions/workflows describe`, `bq show`, `bq query` над `INFORMATION_SCHEMA.JOBS_BY_PROJECT` и `core.fact_loss`/`marts.abc_xyz`); сводный артефакт — `reference/infra_facts_sweep_2026-08-01.md`
- Отклонения от плана: первая попытка Q-14 (`step2_q14_bq_jobs.sh`, поиск по тексту запроса `LIKE '%gcs%'`) дала пустую выдачу — методически неверна для LOAD-заданий (BigQuery не кладёт `sourceUris` в поле `query`), опознано как гэп наблюдения по `ADR-021 §2`/`ADR-044`, не как факт «заданий нет»; исправлено фильтром `job_type = "LOAD"` (`step3`). Оставлено в провенансе как найденный методический дефект, не удалено (`ADR-043`)

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача INFRA-FACTS-SWEEP: READY → DONE
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` целиком, ровно пять строк):
  - Прошлый шаг: хвост из семи мелких read-only гэпов закрыт одной сессией (`Q-6`, `Q-11`, `Q-12`, `Q-13`, `Q-14`, `Q-47`, `Q-55`), артефакт `reference/infra_facts_sweep_2026-08-01.md`
  - Где мы: финиш Epic-1 по-прежнему упирается в вопрос периметра документов у нашей таблицы и у отчёта источника; инфра-хвост, отвлекавший от него, закрыт
  - Следующий шаг: `PARITY-SALES-DISCRIMINATE` шаг 1 (класс A, без нового мандата); параллельно `CODE-REPO-STANDUP` шаг 2a, подготовка текстов Looker Studio
  - Развилки на владельце: объект паритета продаж — после замера, не раньше (`ADR-088 §6/§7`); мандат класса B на запрос розничных продаж — после шага 1
  - Счётчик: пары реестра 1/7 сходятся · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
- Подробности для модели: (см. `SESSION_LOG` выше — числа и провенанс не дублируются) `07_STATE.md` GAP-реестр несёт устаревшие цифры X/Y/Z (2026-06-05: 39/148/439) против свежих (2026-08-01: 28/164/338, `marts.abc_xyz` теперь 530 строк вместо 626) — расхождение НЕ адъюдицировано этой сессией (класс A, только выгрузка факта в `/reference`; обновление контрольных цифр `07_STATE §контрольные цифры` — решение сборки/архитектора). `core.fact_loss` выросло с 128 до 129 строк между 2026-07-23 и 2026-08-01 — причина роста не устанавливалась (вне метода строк Q-47/Q-55). Узел `cf-alert` (`01_ARCHITECTURE.md §топология`, placeholder `GAP Q-12`) и weekly-DAG (`01_ARCHITECTURE.md §DAG`) теперь имеют полные факты в `reference/infra_facts_sweep_2026-08-01.md` — перенос в `01_ARCHITECTURE.md`/`11_INFRA_FACTS.md` не сделан этой сессией (класс A пишет только `/reference`), это задача следующего шага сборки/архитектора.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-01
- обновил: executor (сессия: INFRA-FACTS-SWEEP)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
