=== SESSION LOG · 2026-08-04 · PARITY-STOCK-INTRANSIT ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `PARITY-STOCK-INTRANSIT` — зонтичный заход класса B на два read-only замера пар реестра паритета («Остатки товаров», «Заказы поставщикам в пути»)
- Сделано:
  - Стоп на старте: шапка брифа несла «мандат класса B не выдан», а `07_STATE.md:645`/`07_GAPS.md:88` — «мандат ВЫДАН поимённо (`ADR-116 §3`)»; расхождение брифа и `07_STATE` (стоп-условие `CLAUDE.md §Мандат сессий`) снято прямым подтверждением владельца в чате
  - `PARITY-STOCK-SAME-DAY`: `MAX(date_snapshot)` нашей стороны снят, немедленно вслед — живой `GET report/stock/all`, затем `bq query` той же партиции; окно `2026-08-04T05:29:51Z…05:30:00Z`
  - `PARITY-INTRANSIT-ROWWISE`: живой `GET entity/purchaseorder` (17 заказов «В пути»/«Прибыл частично» на сегодня) + позиции каждого заказа, пересчёт по формуле ингеста, `bq query` `core.fact_purchases`/`marts.in_transit` по тем же id, полный внешний джойн по `(purchase_order_id, position_id)`
  - Найдена и закрыта ловушка метода: первый `bq query` без `-n` молча обрезал результат до дефолтных 100 строк (`341`/`241` фактически) — пойман сопоставлением с независимо известным числом позиций источника, оба запроса перезапущены с `-n 5000`
  - Обе строки реестра (21, 22) обновлены; артефакт `reference/parity_stock_intransit_2026-08-04.md`
- Команды/логи ключевые:
  - `PARITY-STOCK-SAME-DAY`: источник `report/stock/all` 302 строки / `SUM(stock)=121 874,00`; наша сторона `core.fact_inventory` (`date_snapshot='2026-08-04'`) 303 строки / `SUM(stock)=121 878,00`; разность `4,00 (0,0033%)` — на два порядка меньше прежнего невалидного замера `0,12%` (дрейф суток снят, остаток не объяснён)
  - `PARITY-INTRANSIT-ROWWISE`: источник 17 заказов / 341 позиция / `SUM(in_transit_sum_kgs)=76 514 274,30`; `core.fact_purchases` те же 17 заказов — 341 строка, та же сумма; построчный джойн — 0 сирот источника, 0 сирот нашей стороны, 0 расхождений сумм на 341 совпавшем ключе (то же для `marts.in_transit`, 241 строка); построчное совпадение подтверждено отдельно от совпадения агрегата
  - Токен: прочитан в `MSKLAD_TOKEN`, не напечатан; `grep -rF -- "$MSKLAD_TOKEN"` по каталогу сессии — 0 совпадений (`grep` rc=1), проверка зафиксирована в `grep_token_check.log`
- Отклонения от плана: нет (сама ловушка лимита строк — дефект метода замера, закрыта внутри сессии, не отклонение от шагов брифа)

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `PARITY-STOCK-INTRANSIT`: READY → DONE (обе подзадачи `PARITY-STOCK-SAME-DAY`/`PARITY-INTRANSIT-ROWWISE` исполнены; строки реестра 21/22 обновлены)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `PARITY-STOCK-INTRANSIT` закрыта DONE — «Остатки товаров» пересняты одним окном (разность упала с `0,12%` до `0,0033%`, эффект дрейфа суток снят), «Заказы в пути» сверены построчно (0 сирот, 0 расхождений на 341 позиции), `reference/parity_stock_intransit_2026-08-04.md`.
  - Где мы: пара «в пути» подтверждена не только по агрегату, но и построчно — расхождений нет; пара «остатки» сузилась до остатка `0,0033%`, причина не установлена (не гейтит остальное).
  - Следующий шаг: `INVOICES-PERIMETER-DIFF` (пара «Счета покупателям», строка 23 реестра) — единственная неизмеренная построчно пара из семи; параллельно — `PARITY-SALES-CLOSE` по очереди `ADR-116 §1`.
  - Развилки на владельце: нет.
  - Счётчик: пары реестра 2/7 сходятся (эта сессия не меняла статус сходимости ни одной пары на «сошлась» — только уточняла величину и состав уже открытых строк) · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: Найдена ловушка метода, полезная для любой будущей сессии с `bq query --format=prettyjson` по таблицам с потенциально >100 строк: инструмент `bq` по умолчанию (`-n,--max_rows`) отдаёт РОВНО 100 строк без предупреждения и без ошибки — валидный JSON, `rc=0`, тихо неполный. В этой сессии пойман только потому, что число строк источника было известно независимо (341 позиция) и не совпало со 100; при отсутствии независимого счётчика для сравнения результат ушёл бы в отчёт как факт. Любая будущая сессия, строящая построчную сверку или экспорт через `bq query --format=...` без явного `-n`/`--max_rows`, обязана либо задать лимит заведомо больше ожидаемого числа строк, либо предварительно снять `COUNT(*)` тем же запросом и сверить. `core.fact_purchases` полностью перезаписывается (`WRITE_TRUNCATE`) каждый прогон `cf-facts` без окна дат (`reference/source_map_rest_2026-07-30.md §3`), поэтому таблица в любой момент несёт текущее состояние ВСЕХ заказов, не только «в пути» — построчная сверка по `purchase_order_id IN (...)` без дополнительного статусного фильтра корректна и не даёт ложных сирот от смены статуса.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-04
- обновил: executor (сессия: PARITY-STOCK-INTRANSIT)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- proposed: любой `bq query --format=json|prettyjson`, чей результат может превышать 100 строк, обязан нести явный `-n`/`--max_rows` с запасом либо предварённый `COUNT(*)` тем же условием — дефолтный лимит `bq` (100 строк) даёт синтаксически валидный, но тихо неполный результат (тот же класс, что `★ Успех инструмента ≠ факт`, но специфичен для `bq`, не назван в существующем тексте правила).

=== END SESSION ===
