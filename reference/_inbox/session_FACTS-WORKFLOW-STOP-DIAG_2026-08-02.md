=== SESSION LOG · 2026-08-02 · FACTS-WORKFLOW-STOP-DIAG ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: FACTS-WORKFLOW-STOP-DIAG — диагностика остановки загрузки фактов (продажи/закупки/возвраты)
- Сделано: установлен механизм отказа — DQ Gate (`cf-dq`), чек `drift_check`, шаг `raise_dq_failed`
  в обоих workflow (`msklad-pipeline-hourly`, `msklad-pipeline-weekly`), блокирующий `step_promote`/
  `step_purchases`/`step_returns` штатной логикой `check_dq`. Подтверждено ДВУМЯ независимыми путями
  (идентичный шаг отказа + идентичный провалившийся чек в execution-логах обоих workflow) плюс
  отсутствием заданий BigQuery дальше по цепочке. Исключена гипотеза деплоя (Шаг 3: ни `cf-facts`,
  ни `cf-dq` не передеплоивались вблизи моментов остановки). Установлено: отказ ПРОДОЛЖАЕТСЯ на
  момент замера (`2026-08-02T13:49:47Z`), часовой поток стоит 20ч 47м, недельный (возвраты) — 14
  суток / два пропущенных воскресенья. Разница во времени первого проявления (возвраты `07-19→07-26`,
  продажи/закупки `08-01 17:00→18:00`) объяснена разной частотой опроса ОДНОГО механизма, не двумя
  разными отказами. Артефакт: `reference/facts_workflow_stop_diag_2026-08-02.md`.
- Команды/логи ключевые: `reference/_scratch_FACTS-WORKFLOW-STOP-DIAG_2026-08-02/` (7 скриптов,
  соответствующие `*_run.log`) — свежесть BQ-таблиц/заданий, Cloud Scheduler/Workflows describe и
  executions list, Cloud Logging execution-логи workflow (текст ошибки дословно), ревизии Cloud Run
  `cf-facts`/`cf-dq` с историей деплоев.
- Отклонения от плана: нет — все шесть шагов брифа исполнены по порядку; шаг 1 расширен одним
  дополнительным скриптом (`step1b`), когда первый прогон показал, что `fact_sales_staging`
  продолжает грузиться, что сузило диагностику раньше срока (не отклонение по существу, дополнение
  метода в рамках того же шага).

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача FACTS-WORKFLOW-STOP-DIAG: READY → DONE (диагностика завершена; механизм назван,
  восстановление не выполнялось — класс B, мандат не выдан, вне scope)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: диагностика остановки фактов завершена (`FACTS-WORKFLOW-STOP-DIAG`) — механизм
    установлен: DQ Gate `drift_check` блокирует `step_promote`/`step_purchases`/`step_returns` в
    обоих workflow, отказ живой на момент замера, деплой исключён как причина
  - Где мы: восстановление потока данных остаётся приоритетом перед сверкой паритета «на сейчас»;
    механизм назван, решение о форме восстановления (класс B) — за владельцем; исторические майские
    измерения паритета в силе
  - Следующий шаг: решение владельца о форме восстановления (снять блокировку конкретного прогона,
    либо править `drift_check`/порог в `cf-dq`, либо подтвердить падение выручки как реальное), затем
    `FACTS-BACKFILL` (класс B), затем `DQ-SOURCE-CAPTURE` (снять исходник `cf-dq`, закрывает
    `CONTEXT GAP` этой сессии); параллельно без изменений — `DIM-METADATA-MAPPINGS-FROZEN`,
    `MARTS-BUILD-STAMP`, `FACT-SALES-BYVARIANT-BACKUP-OWNER`, `SALES-CONSIGNMENT-REVENUE`,
    `SALES-PERIMETER-EXTEND`, `SALES-REFRESH-WINDOW`, `FX-MAY-WINDOW-D1-TAIL`,
    `INGEST-CURRENCY-ASSERT` шаги 1-2, подготовка `INGEST-MOMENT-ZONE-FIX`, тексты Looker Studio
  - Развилки на владельце: решение о форме восстановления загрузки (см. «Следующий шаг») — новое,
    добавляется к прежним; первый `push` код-репо в `holika-prod`; ответ клиента по часовому поясу
    (`MSKLAD-TZ-PROPOSAL`); мандат класса B на `AUDIT-SNAPSHOT-FIX-EMPLOYEES`/
    `AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`
  - Счётчик: пары реестра 2/7 сходятся · таблиц живых 15 из 26 (до восстановления, не переизмерялось)
    · строк реестра на критическом пути 23 из 63 · Epic M 5/7 фаз
- Подробности для модели: <append>
  **Механизм остановки фактов установлен, не починен (`FACTS-WORKFLOW-STOP-DIAG`, 2026-08-02, класс
  A).** Полный разбор с execution ID, дословными логами и провенансом —
  `reference/facts_workflow_stop_diag_2026-08-02.md`, здесь не пересказывается. Что обязана знать
  следующая сессия: (i) причина остановки — не крах кода и не деплой (обе CF стабильны с
  06-2026), а штатный `check_dq`/`raise_dq_failed` в `workflow.yaml` обоих pipeline, блокирующий
  дальнейшие шаги при провале чека `drift_check` в `cf-dq` (выручка «вчера» против 7-дневного
  среднего ниже порога `0,03` для выходных); (ii) `yesterday_rev`/`ma7` в логе провала НЕ меняются
  на протяжении минимум 20 часовых прогонов подряд — правдоподобная, но НЕ подтверждённая чтением
  кода гипотеза: чек читает «вчера» из `core.fact_sales_profit`, промоут в которую сам же блокирует,
  то есть отказ может быть самоподдерживающимся (`CONTEXT GAP`, закрывается `DQ-SOURCE-CAPTURE`);
  (iii) возвраты и продажи/закупки — ОДИН механизм (не гипотеза архитектора, а подтверждённый факт),
  разное время первого проявления — следствие разной частоты вызова `step_dq` (раз в час vs раз в
  неделю), не два независимых отказа; (iv) восстановление — три варианта, все класс B, ни один не
  выполнялся: снять блокировку конкретного прогона / поправить порог или логику `drift_check` в
  `cf-dq` / подтвердить падение выручки `2026-08-01` как реальное и не гейтить им дальше; (v) исходник
  `cf-dq` по-прежнему не снят (`Q-6`) — без него нельзя проверить гипотезу (ii) и нельзя оценить,
  адекватен ли порог `0,03` сезонности выходных.
- Новые открытые вопросы: нет (уточняет существующие строки `FACTS-WORKFLOW-STOP-DIAG`, `Q-6`,
  `DQ-SOURCE-CAPTURE`, `FACTS-BACKFILL` — статусы правит сборка)
- Блокеры: нет новых
- updated_at: 2026-08-02
- обновил: исполнитель (сессия: FACTS-WORKFLOW-STOP-DIAG)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
