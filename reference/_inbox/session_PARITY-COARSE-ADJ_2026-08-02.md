# FILE: reference/_inbox/session_PARITY-COARSE-ADJ_2026-08-02.md

=== SESSION LOG · 2026-08-02 · PARITY-COARSE-ADJ ===

## SESSION_LOG

- Задача: `PARITY-COARSE-ADJ` — адъюдикация урожая грубого замера трёх пар, поправка к срезу
  `PROCESS-FREEZE`, пересборка критического пути, заводка задач на результат.
- Класс задачи: **A** (чтение репо, запись в `/reference`, коммит в локальную ветку; облачных
  вызовов ноль). Строки задачи в `07_STATE` на момент старта не было — заведена требованием
  владельца в чате 2026-08-02. Седьмой зафиксированный инстанс класса `ADR-086 §3`; отдельный
  `Q` не заводится (`ADR-051 §2`).
- База: `b86f0eba0d8843e17789f1b85f4cdcb531f95f36`. Дерево `worktrees/PARITY-COARSE-ADJ`,
  ветка `s/PARITY-COARSE-ADJ`.
- **Отклонение от старта, названное явно.** Ветка `s/PARITY-COARSE-TOTALS` не слита — стоп-условие
  `ADR-081 §6`. Сборку владелец запускает отдельным чатом (правило проекта, названо им в чате
  2026-08-02); ожидание прохода означало бы потерю хода. Работа велась в собственном дереве, файлов
  чужой ветки не касалась, её блок в буфере не трогала. Буфер получит два блока; порядок разбора —
  по дате имени (`_ASSEMBLER.md §5`), обе даты равны, конфликта по файлам нет: блоки правят разные
  строки реестра.
- Сделано:
  - **Поправка факта.** `reference/process_freeze_2026-08-02.md §1` утверждал «две правки
    подготовлены»; проверка файлами показала обратное — подготовленных патчей нет ни одного,
    задачи `READY` и не взяты, снапшот кода не тронут, три файла `reference/sql/*.sql` суть снимки
    живых ДЕФЕКТНЫХ запросов, а не исправленные версии. Разбор — `§1` артефакта.
  - **Адъюдикация трёх пар** (`§3` артефакта): пара «в пути» признана сошедшейся на уровне (ii)
    с записанной оговоркой; пара «остатки» признана НЕ измеренной корректно (нарушено правило
    одновременности `ADR-079 §3`); пара «счета» разделена на сравнимую недостачу документов и
    несравнимые суммы.
  - **Гипотеза с одноразовым различителем** по счетам: число `4058` совпадает с замороженной
    цифрой источника `PR-27` до единицы — кандидат в «таблица не пополняется вовсе».
  - **Шесть задач на результат** с проверяемой приёмкой и порядком (`§4`).
  - **Пересборка критического пути** (`§5`): шаг 2b код-репо назван единственным гейтом всего
    деплойного потока.
- Команды/логи: `git log/show`, `grep`, чтение артефакта замера и `reference/source_map_rest_2026-07-30.md`.
  Облачных вызовов нет ни одного.
- Отклонения от плана: одно, названо выше.

## STATE_PATCH

- Задача `PARITY-COARSE-ADJ`: (новая) → **DONE**. Строка в реестре не заводится: задача заведена и
  закрыта одним блоком (прецедент `PROCESS-FREEZE`, `REGISTRY-COMPACT-ADJ`).
- **Счётчик пар паритета: `1/7` → `2/7`** (сошлась пара «Заказы поставщикам в пути»,
  `ADR-0NN §1`). Строка 22 `reference/parity_registry.md` — правится задачей, не сборкой.
- **Пять новых задача-строк** в `07_STATE` (индекс) и `07_GAPS.md` (полный текст), плюс парные
  строки в `§Мандат Claude Code: класс задач` (`ADR-090 §1`):

  1. `INVOICES-STALENESS-PROBE` · `READY` · Пополняется ли `core.fact_customer_invoices` вообще:
     число счетов у нас совпало с замороженной цифрой `PR-27` до единицы · гейтов нет.
     Мандат: `A | да | постоянный | один read-only `bq query`. Пишет: `reference/invoices_staleness_probe_<date>.md``
  2. `INVOICES-FIELD-MAP` · `READY` · Соответствие полей `entity/invoiceout` → `core.fact_customer_invoices`
     и правило пересчёта валюты не выведены · гейт: результат `INVOICES-STALENESS-PROBE` меняет объём.
     Мандат: `A | да | постоянный | чтение снапшота кода и живой схемы. Пишет: `reference/invoices_field_map_<date>.md``
  3. `PARITY-STOCK-SAME-DAY` · `READY` · Повторный замер пары остатков обеими сторонами в одном
     окне · гейт: мандат класса B не выдан.
     Мандат: `B | нет | НЕ выдан | живой `GET` `report/stock/all` с `msklad-token``
  4. `SALES-MERGE-DRYRUN` · `READY` · Два технических неизвестных патча продаж: допустим ли
     `UPDATE` партиционирующей колонки внутри `MERGE`; источник COGS шире 7/90 суток · гейтов нет.
     Мандат: `A | да | постоянный | `bq query --dry_run` (в `allow`) и чтение `fetch_byvariant.py`. Пишет: `reference/sales_merge_dryrun_<date>.md``
  5. `INVOICES-PERIMETER-DIFF` · `READY` · Разложение недостачи 468 счетов по статусам, валютам и
     датам · гейт: мандат класса B не выдан; идёт одним заходом с `PARITY-STOCK-SAME-DAY`.
     Мандат: `B | нет | НЕ выдан | живой `GET` `entity/invoiceout` с `msklad-token``

- **`CODE-REPO-STANDUP` шаг 2b — приоритет повышен до высшего.** Строка реестра и строка мандата
  существуют, класс и мандат не меняются (`B`, НЕ отдаётся, владелец); в колонку гейта дописывается:
  «**Единственный гейт всего деплойного потока (`ADR-0NN §5`): без него не деплоится ни одна правка
  конвейера — продажи, ингест, зона времени, валютная проверка.**»
- **Строка `SALES-REFRESH-WINDOW`** — уточнение статуса без смены `READY`: дописка «подготовка
  выдала ТРИ варианта, ни один не выбран; два технических неизвестных снимаются
  `SALES-MERGE-DRYRUN`, после чего выбор — владелец».
- **`§Контрольные цифры`**: строка «`core.fact_customer_invoices`: 4058 записей (PR-27)» получает
  дописку «**то же число измерено 2026-08-02 живым запросом к витрине — совпадение до единицы,
  кандидат в признак незаполняемой таблицы, различитель `INVOICES-STALENESS-PROBE`**».
- Блокеры: без изменений.
- Новые открытые вопросы: нет — все пять находок заведены задачами, а не вопросами
  (`ADR-051 §2`: у каждой назван исполнитель и приёмка).
- Стенд-ап: **берётся из блока `PARITY-COARSE-TOTALS`, если тот разбирается в этой же сборке**
  (он ближе к предмету замера); настоящий блок стенд-ап НЕ подаёт, чтобы не конкурировать.
  Если этот блок разбирается один — стенд-ап составляет сборка по `§5` артефакта.
- Подробности для модели: append одним абзацем (критерий `ADR-105 §1`) —

  **Урожай грубого замера адъюдицирован (`PARITY-COARSE-ADJ`, 2026-08-02, `ADR-0NN`).** Полный
  разбор — `reference/parity_coarse_adj_2026-08-02.md`, здесь не пересказывается. Что обязана
  знать следующая сессия: (i) **подготовленных патчей в проекте НЕТ ни одного** — «разгейчено» не
  равно «готово», признак подготовки механический: артефакт в `reference/` с именем задачи, нет
  артефакта — нет подготовки; (ii) числа `7,61 %`/`12,55 %`/`12,81 %` по счетам расхождением НЕ
  являются и в реестр не идут — сторона источника сложена через валюты по номиналу, правило
  пересчёта для этой ветки не выведено (`Q-82`, обслуживающая CF не установлена); сравнима только
  недостача `468` документов из `4526`; (iii) разность `0,12 %` по остаткам не интерпретируется —
  нарушено правило одновременности `ADR-079 §3`, наша сторона суточной давности; (iv) шаг 2b
  `CODE-REPO-STANDUP` гейтит ВЕСЬ деплойный поток, а не одну строку; (v) число `4058` в
  `§Контрольные цифры` совпало с живым замером до единицы — до вердикта
  `INVOICES-STALENESS-PROBE` считать поверхность «Операционка» подозреваемой в устаревании.

## NEW_DECISIONS

- **ADR-0NN: Адъюдикация урожая грубого замера; пара «в пути» сошлась; суммы по счетам
  расхождением не объявляются; шаг 2b код-репо — единственный гейт деплойного потока.**
  Контекст: `PARITY-COARSE-TOTALS` измерила три пары, ни разу не измерявшиеся
  (`reference/parity_coarse_totals_2026-08-02.md`); владелец потребовал превращения находок в
  задачи на результат и объяснения, какой готовый код не деплоится.
  Решение:
  §1 **Пара «Заказы поставщикам в пути» признаётся сошедшейся** (`0,00` при `78 748 543,05`),
  уровень эталона (ii): сторона источника получена применением опубликованного правила к СЫРЫМ
  документам источника, что `ADR-098 §7` не запрещает (запрещено сравнение нашей таблицы с нашей
  же таблицей). Оговорка записывается в строку 22 реестра: ноль доказывает верность
  воспроизведения источника ингестом и НЕ доказывает совпадения с числом, отображаемым интерфейсом.
  Счётчик пар `1/7` → `2/7`.
  §2 **Пара «Остатки товаров» остаётся «не сверялось».** Разность `0,12 %` не интерпретируется:
  наша сторона — снимок за предыдущие сутки, источник живой, правило одновременности
  (`ADR-079 §3`) нарушено, слагаемые «дрейф склада» и «расхождение» не разделены. Закрывается
  задачей `PARITY-STOCK-SAME-DAY`.
  §3 **Пара «Счета покупателям» разделяется на два факта.** Сравнима и принимается недостача
  `468` документов из `4526` (`10,3 %`). Суммы **несравнимы и расхождением не объявляются**:
  `43,4 %` документов источника не в KGS, сложены по номиналу, правило пересчёта для этой ветки
  не выведено (`Q-82`, обслуживающая Cloud Function не установлена —
  `reference/source_map_rest_2026-07-30.md §4`). Проценты `7,61`/`12,55`/`12,81` в реестр не
  вносятся; вносится недостача документов и пометка о несравнимости сумм.
  §4 **Заводятся пять задач на результат** с проверяемой приёмкой и порядком:
  `INVOICES-STALENESS-PROBE`, `INVOICES-FIELD-MAP`, `PARITY-STOCK-SAME-DAY`, `SALES-MERGE-DRYRUN`,
  `INVOICES-PERIMETER-DIFF` (составы — `reference/parity_coarse_adj_2026-08-02.md §4`).
  §5 **Шаг 2b `CODE-REPO-STANDUP` объявляется единственным гейтом всего деплойного потока** и
  получает высший приоритет. Класс и мандат не меняются (`B`, владелец). Пока он открыт, подготовка
  патчей увеличивает запас недоставленной работы, а не приближает финиш `ADR-067`.
  §6 **Поправка факта к `reference/process_freeze_2026-08-02.md §1`:** подготовленных патчей в
  проекте нет ни одного; «разгейчено» было прочитано как «готово». Признак подготовки назначается
  механический — артефакт в `reference/` с именем задачи (`ADR-021 §2`).
  Последствия: `[сейчас]` `06_DECISIONS_LOG.md` + `06_INDEX.md` — ADR и строка;
  `[сейчас]` `07_STATE.md` + `07_GAPS.md` — пять новых задача-строк, дописки к
  `CODE-REPO-STANDUP` и `SALES-REFRESH-WINDOW`, дописка в `§Контрольные цифры`, абзац в
  `§Подробности для модели`, шапка; `[сейчас]` `07_STATE.md §Мандат` — пять парных строк
  (`ADR-090 §1`); `[задачей]` строки 21, 22, 23 `reference/parity_registry.md` правят сами задачи,
  не сборка; `[без правок]` `ADR-107` — заморозка не нарушается, все решения настоящего ADR
  предметные, а не о методе; `[без правок]` `ADR-065`, `ADR-079 §3`, `ADR-098 §7` — применяются,
  не пересматриваются.
  Статус: **accepted** (архитектор — §1–§4, §6; §5 — приоритет, подтверждён требованием владельца
  в чате 2026-08-02 «эти задачи возьмём с наивысшим приоритетом»).

## NEW_CONVENTIONS

- нет.

=== END SESSION ===
