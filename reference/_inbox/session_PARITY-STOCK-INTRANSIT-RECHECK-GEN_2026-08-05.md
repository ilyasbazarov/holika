# FILE: reference/_inbox/session_PARITY-STOCK-INTRANSIT-RECHECK-GEN_2026-08-05.md

=== SESSION LOG · 2026-08-05 · PARITY-STOCK-INTRANSIT-RECHECK-GEN ===

## SESSION_LOG

- Задача: `PARITY-STOCK-INTRANSIT-RECHECK-GEN` — завести задача-строку
  `PARITY-STOCK-INTRANSIT-RECHECK` и собрать под неё бриф. Поручение владельца в чате 2026-08-05
  («заводи строку и собирай бриф»). Класс A, мандат постоянный. Задача НЕ исполнялась.
- База: `1683cc4`. Дерево/ветка: `worktrees/PARITY-STOCK-INTRANSIT-RECHECK-GEN` /
  `s/PARITY-STOCK-INTRANSIT-RECHECK-GEN`.
- Повод: до финиша Epic-1 остаются две несошедшиеся пары реестра (строки 21 и 22). Работа, закрывающая
  строку 22, в репо не существовала ни строкой, ни брифом — притом что `ADR-120 §4` прямо назвал её
  («закрытие строки 22 гейтится восстановлением потока и пересверкой»). Работа без идентификатора
  есть незакрытая постановка (`ADR-084 §5`).
- Сделано: собран `briefs/PARITY-STOCK-INTRANSIT-RECHECK.md` по шаблону `08` и `_GENERATOR.md §4a/§5`
  (класс B, гейт исполнения по отсутствию мандата проставлен явно, параллель `нет`, файлы на запись
  маркированным списком). Заведение строк — в `STATE_PATCH` ниже.
- **Замер при сборке (класс A, read-only):** `core.fact_purchases` —
  `MAX(_loaded_at) = 2026-08-05 06:02:02`, `211` заказов, `4 398` строк, снято `2026-08-05T06:07:57Z`.
  До восстановления таблица стояла на `2026-08-01T17:02:16Z`. **Гейт `ADR-120 §4` («восстановление
  потока») снят фактом, а не ожиданием.**
- Команды/логи ключевые: `bash tools/session_status.sh` (RC 0), `git worktree add`, один
  `bq query` (read-only, свежесть закупок), сплошные поиски `grep -rn` по репо с печатью совпавших
  строк.
- Отклонения от плана: нет.

## STATE_PATCH

- **Заводится задача-строка `PARITY-STOCK-INTRANSIT-RECHECK`** — в `07_GAPS.md`, в индекс
  `07_STATE §Открытые вопросы` и в `07_STATE §Мандат Claude Code: класс задач` ОДНИМ коммитом
  (парность `ADR-090 §1`):
  - Индекс: `| PARITY-STOCK-INTRANSIT-RECHECK | READY | Первое допустимое измерение строки 22 реестра после восстановления загрузки закупок; проверяет утверждение ADR-120 §2 о причине расхождения | мандат класса B не выдан; бриф собран |`
  - Полный текст (`07_GAPS.md`): `| PARITY-STOCK-INTRANSIT-RECHECK | (PARITY-STOCK-INTRANSIT-RECHECK-GEN, 2026-08-05, заведена по поручению владельца в чате; основание — ADR-120 §4, назвавший работу, но не давший ей идентификатора) Пересверка строки 22 реестра паритета («Заказы поставщикам в пути», Q-81) после восстановления загрузки core.fact_purchases: обе стороны по собственному признаку, разложение Δ, судьба четырёх заказов прежних хвостов | задача (класс B, мандат не выдан) | **READY.** Гейт «восстановление потока» (ADR-120 §4) СНЯТ замером 2026-08-05T06:07:57Z: core.fact_purchases MAX(_loaded_at) = 2026-08-05 06:02:02, 211 заказов, 4398 строк (до восстановления — 2026-08-01T17:02:16Z). Окно допустимого замера здесь ЧАСОВОЕ, не ночное: таблица перезаписывается WRITE_TRUNCATE каждым часовым прогоном cf-facts. Задача несёт проверяемое предсказание: если причиной расхождения +2 234 268,75 KGS была заморозка (ADR-120 §2), полная перезапись обязана убрать два заказа одностороннего хвоста и Δ схлопнется к нулю; значимая Δ означает, что ADR-120 §2 опровергнут и механизм структурный — оба исхода суть результат. Бриф briefs/PARITY-STOCK-INTRANSIT-RECHECK.md собран, не взят. Мандат класса B выдаётся поимённо владельцем перед заходом (форма ADR-115 §12). Вердикт о сходимости выносит архитектор задачей PARITY-STOCK-INTRANSIT-CLOSE, не эта сессия. |`
  - Мандат: `| PARITY-STOCK-INTRANSIT-RECHECK | B | нет | НЕ выдан | живой GET entity/purchaseorder с msklad-token (ADR-076 §1); задача только читает, ни одной записи, откат не требуется; оговорки ADR-076 §5 и форма логирования ADR-079 §6 обязательны; окно замера — в пределах часа от MAX(_loaded_at) core.fact_purchases. Пишет: reference/parity_registry.md только строка 22, reference/parity_intransit_recheck_<date>.md, reference/_scratch_PARITY-STOCK-INTRANSIT-RECHECK_<date>/ |`
- Задача `PARITY-STOCK-INTRANSIT-CLOSE`: статус остаётся `PARTIAL`; в колонке гейта «строка 22 —
  восстановление потока» заменяется на «строка 22 — `PARITY-STOCK-INTRANSIT-RECHECK`» (гейт
  восстановления снят замером, работа получила имя).
- `Q-81`: статус остаётся `PARTIAL`; в колонке гейта «восстановление потока плюс пересверка»
  заменяется на «`PARITY-STOCK-INTRANSIT-RECHECK` — пересверка (восстановление потока подтверждено
  замером 2026-08-05T06:07:57Z)».
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - **Прошлый шаг:** заведена задача-строка `PARITY-STOCK-INTRANSIT-RECHECK` и собран бриф под неё;
    попутно замером подтверждено, что загрузка закупок возобновилась
    (`core.fact_purchases`, `2026-08-05 06:02:02`).
  - **Где мы:** пять пар реестра из семи закрыты; у обеих оставшихся строк теперь есть и правило, и
    названная работа с брифом — не хватает только двух измерений, оба класса B.
  - **Следующий шаг:** `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 в окне `21:00Z` (мандат выдан) и
    `PARITY-STOCK-INTRANSIT-RECHECK` в пределах часа от прогона `cf-facts` (мандат НЕ выдан);
    `DQ-GATE-SCOPE-SPLIT-DEPLOY` идёт независимо.
  - **Развилки на владельце:** мандат класса B на `PARITY-STOCK-INTRANSIT-RECHECK` (только чтение,
    живой `GET entity/purchaseorder`, откат не требуется).
  - **Счётчик:** пары реестра **5/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели (append в одноимённый подраздел `07_STATE`):

  **Работа, закрывающая строку 22, получила имя; гейт восстановления снят замером
  (`PARITY-STOCK-INTRANSIT-RECHECK-GEN`, 2026-08-05).** Полный текст постановки —
  `briefs/PARITY-STOCK-INTRANSIT-RECHECK.md`, здесь не пересказывается. Что обязана знать сессия,
  берущая задачу. (i) **Окно допустимого замера строки 22 — ЧАСОВОЕ, не ночное.**
  `core.fact_purchases` перезаписывается `WRITE_TRUNCATE` каждым часовым прогоном `cf-facts`
  (`reference/source_map_rest_2026-07-30.md §3`), поэтому допустимое окно наступает каждый час, сразу
  после прогона. Переносить сюда форму соседней строки 21 (разовый суточный снимок, окно `21:00Z`,
  `ADR-123 §3`) по аналогии — ошибка, которая стоила бы ночного захода без нужды. (ii) **Гейт
  `ADR-120 §4` снят фактом:** `MAX(_loaded_at) = 2026-08-05 06:02:02`, `211` заказов, `4 398` строк
  против стояния на `2026-08-01T17:02:16Z`; свежесть на момент СВОЕГО прогона всё равно снимается
  заново. (iii) **Задача несёт проверяемое предсказание, и это её главная ценность:** `ADR-120 §2`
  объявил причиной расхождения `+2 234 268,75` KGS заморозку нашей стороны, честно пометив это
  «достаточным объяснением», а не измеренной причиной. После восстановления загрузки полная
  перезапись обязана убрать два заказа одностороннего хвоста; `Δ → ~0` подтверждает причину
  измерением, значимая `Δ` опровергает `ADR-120 §2` и означает структурный механизм. Оба исхода —
  результат, приёмка покрывает оба намеренно. (iv) **Имя `PARITY-STOCK-INTRANSIT-RECHECK` уже
  встречается в истории репо и до сих пор было пустым:** сессия
  `PARITY-STOCK-INTRANSIT-RECHECK-GEN` от 2026-08-04 (сборка `5bc00a7`) прошла под этим именем, но
  выпустила бриф ДРУГОЙ задачи (`briefs/PARITY-STOCK-ROWWISE.md`) и строки реестра не завела — то
  есть имя жило в записях, а задачи за ним не было. Расхождение названо здесь и закрыто заведением
  строки; отдельной строки реестра под него не заводится (`ADR-051 §2`, `ADR-107 §4`).

- Новые открытые вопросы: `PARITY-STOCK-INTRANSIT-RECHECK` (задача-строка, READY, класс B).
- Блокеры: нет.
- updated_at: 2026-08-05
- обновил: генератор (сессия: `PARITY-STOCK-INTRANSIT-RECHECK-GEN`)

## NEW_DECISIONS

- нет. Постановка выведена из уже принятого `ADR-120 §3/§4`; настоящая сессия даёт ей идентификатор,
  строку и бриф, значений принятых решений не двигает. Мандат класса B — решение владельца, здесь не
  предвосхищается.

## NEW_CONVENTIONS

- нет.

=== END SESSION ===
