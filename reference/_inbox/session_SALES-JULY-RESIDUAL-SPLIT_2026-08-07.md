=== SESSION LOG · 2026-08-07 · SALES-JULY-RESIDUAL-SPLIT ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-JULY-RESIDUAL-SPLIT — разложение июльского остатка `+178 124,42` KGS
  (`core.fact_sales_profit` против эталона МойСклада, `ADR-137 §3`)
- Сделано: воспроизведён метод `ADR-100 §1` на июльских данных. «Последний массовый прогон»
  идентифицирован как `2026-07-19` (`msklad-pipeline-weekly`, `11_INFRA_FACTS.md:27`, дата —
  воскресенье, совпадает с расписанием `0 1 * * 0`; независимо подтверждено пиком загрузки
  `1113`/`4707` строк в этот день). Четыре измерения: (1) распределение `_loaded_at` по июлю —
  два дня старше `2026-07-19` (`2026-07-09`×1, `2026-07-15`×7); (2) кандидаты в сироты —
  `8` строк, `22 668,0108` KGS; (3) все 8 кандидатов несут метку `«Оптовая торговля»`
  (не `«Розница»`/`«Комиссия»`) — подтверждена локализация в ветке `entity/demand`, вне периметра;
  (4) суточный разрез — 8 кандидатов приходятся на бизнес-даты `2026-07-02`/`2026-07-11`/`2026-07-13`.
  Артефакт: `reference/sales_july_residual_split_2026-08-07.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-JULY-RESIDUAL-SPLIT_2026-08-07/step{1,2,3,4}_*.sh`
  + `step{1,2,3,4}_run.log`, `date -u`/`gcloud auth list` первой и последней командой каждого.
- Отклонения от плана: нет.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-JULY-RESIDUAL-SPLIT: READY → PARTIAL (объяснено `22 668,0108` из `178 124,42` KGS,
  `12,73 %`; необъяснённый остаток `155 456,4092` KGS уходит архитектору как новый `CONTEXT GAP` —
  строки-сироты подтверждены измерением, но не единственный и не достаточный механизм)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: `SALES-JULY-RESIDUAL-SPLIT` измерена — строки-сироты объясняют `12,73 %`
    июльского остатка (`178 124,42` KGS), `87,27 %` не объяснены; артефакт
    `reference/sales_july_residual_split_2026-08-07.md`
  - Где мы: пара «Продажи» реестра паритета закрыта (`5/7`→`7/7` по совокупности прежних сессий,
    `ADR-129`); июльский остаток вне зоны паритета (май-2026, `ADR-115 §11`) — диагностика вдогонку,
    финиш не гейтит
  - Следующий шаг: архитектору — назвать следующий кандидатный механизм для необъяснённых
    `155 456,41` KGS (задача не задана этой сессией, anti-improvisation); параллельно
    `SALES-CHANNEL-NULL-DEMAND` (READY, независима)
  - Развилки на владельце: нет
  - Счётчик: пары реестра 7/7 · Epic M — не относится (Epic-1)
- Подробности для модели: «Последний массовый прогон» для `core.fact_sales_profit` — это
  `msklad-pipeline-weekly` (`0 1 * * 0`, полный 90-суточный reload, `03_PIPELINE_SPEC.md:17`);
  для июля это `2026-07-19`, установлено и датой (воскресенье), и профилем загрузки (пик
  `1113`/`4707` строк). Метод `ADR-100 §1` (кандидаты = строки со `_loaded_at` старше этой даты)
  воспроизводим на любой месяц тем же приёмом — при условии что для него есть подтверждённая
  дата последнего полного прогона. Полный ход — `reference/sales_july_residual_split_2026-08-07.md`.
- Новые открытые вопросы: необъяснённый остаток `155 456,4092` KGS июльской пары (вне зоны
  паритета, май-фиксация не расширяется) — новый кандидатный механизм не назван, решает архитектор
- Блокеры: нет
- updated_at: 2026-08-07
- обновил: исполнитель (сессия: SALES-JULY-RESIDUAL-SPLIT)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
