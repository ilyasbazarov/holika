=== SESSION LOG · 2026-08-02 · CC-READONLY-PRINCIPAL-GEN ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: CC-READONLY-PRINCIPAL-GEN — генерация брифа задачи `CC-READONLY-PRINCIPAL`, шаг 1 (замер D1-D3)
- Сделано: `briefs/CC-READONLY-PRINCIPAL-1STSTEP.md` собран прецедентно по строке GAP-реестра
  (`07_STATE.md:441`) и строке таблицы мандата (`07_STATE.md:358`); класс A / параллель нет / мандат
  постоянный процитированы дословно, расхождения между брифом, строкой мандата и строкой реестра нет.
  Оговорка `reference/mandate_table_audit_2026-08-01.md §4a` учтена явно: различитель D1 («вывод
  `/permissions`») неисполним в десктопном приложении — бриф передаёт исполнителю выбор одного из двух
  путей (интерактивный `claude` в терминале либо чтение `.claude/settings.json` плюс один наблюдённый
  запуск `bash script.sh`), с обязательной фиксацией выбора в артефакте. Шаги 2-3 (правка
  `.claude/settings.json`) явно вынесены вне scope брифа (класс B, `07_STATE.md:359-360`).
- Команды/логи ключевые: только чтение с диска (`_METHOD`, `00_CHARTER`, `04_ROADMAP`,
  `05_CONVENTIONS`, `06_INDEX`, `07_STATE`, `08_TASK_BRIEF_TEMPLATE`, `06_DECISIONS_LOG` ADR-077 §1-§8,
  `reference/mandate_table_audit_2026-08-01.md`, `.claude/settings.json`); никаких записывающих команд
  к облаку не исполнялось. Бриф закоммичен отдельным коммитом `6773eb5`.
- Отклонения от плана: одно техническое — сессия пересекла полночь по местному времени между началом
  работы (контекст стартовал с датой 2026-08-01) и коммитом брифа; пути артефактов, которые исполнитель
  брифа будет создавать (`reference/cc_readonly_principal_<date>.md`,
  `reference/_scratch_CC-READONLY-PRINCIPAL_<date>/`, session-блок исполнителя), оставлены в брифе с
  плейсхолдером `<date>` вместо зашитой даты генерации, чтобы не нести неверную дату при фактическом
  исполнении в другой день.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача CC-READONLY-PRINCIPAL, шаг 1: READY → READY, бриф готов к исполнению
  (`briefs/CC-READONLY-PRINCIPAL-1STSTEP.md`)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: бриф `CC-READONLY-PRINCIPAL` шаг 1 (замер D1-D3) собран
    (`briefs/CC-READONLY-PRINCIPAL-1STSTEP.md`); оговорка неисполнимости различителя D1 в десктопном
    режиме (`reference/mandate_table_audit_2026-08-01.md §4a`) учтена, выбор пути замера передан
    исполнителю
  - Где мы: финиш Epic-1 по-прежнему упирается в выбор моста продаж (`ADR-097 §6`, a/b/c);
    `SALES-ORPHAN-CONFIRM` не взята; выбор варианта `SALES-REFRESH-WINDOW` остаётся владельцу;
    `CC-READONLY-PRINCIPAL` шаг 1 теперь готов к исполнению отдельной сессией, не исполнен
  - Следующий шаг: `SALES-ORPHAN-CONFIRM` (класс A, без брифа, `ADR-086 §1`); выбор одного из трёх
    вариантов `SALES-REFRESH-WINDOW` — решение владельца/архитектора; исполнение готового брифа
    `CC-READONLY-PRINCIPAL` шаг 1 (класс A, постоянный мандат); следом шаг 2
    `PARITY-SALES-DISCRIMINATE` (класс B, мандат выдан `ADR-100 §10`, очередь после
    `SALES-ORPHAN-CONFIRM`); параллельно `CODE-REPO-STANDUP` шаг 2b (владелец), тексты Looker Studio
  - Развилки на владельце: выбор варианта патча `SALES-REFRESH-WINDOW` (1/2/3, не ранжированы);
    допустимость `UPDATE transaction_date` в BigQuery `MERGE` — требует dry-run перед деплоем любого
    варианта, трогающего код; источник COGS для диапазона шире 7/90 суток — не назван; объект паритета
    продаж (a/b/c, `ADR-097 §6`) — после шага 2 `PARITY-SALES-DISCRIMINATE`; место хостинга код-репо и
    первый `push` — решение владельца
  - Счётчик: пары реестра 1/7 сходятся · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
- Подробности для модели: (→ append в одноимённый подраздел `07_STATE`) Генерация
  `CC-READONLY-PRINCIPAL-GEN` (2026-08-02) собрала бриф прецедентно по двум совпадающим строкам —
  GAP-реестра (`07_STATE.md:441`) и таблицы мандата (`07_STATE.md:358`) — обе цитируют класс A,
  параллель «нет», мандат постоянный, расхождения не было. Шаги 1-3 брифа адресуют ровно три
  неизвестных `ADR-077 §3` (D1 механизм трения, D2 impersonation `bq`, D3 отделимость записи
  `marts.expenses_staging` от `marts.expenses`); проектирование состава ролей принципала (шаги 2-3
  задачи) этим брифом сознательно не производится — по `ADR-077 §4` состав выводится из провенанса
  исполненного замера, а не назначается заранее. Бриф явно называет текущий состав
  `.claude/settings.json` (процитирован дословно) и наблюдение генератора «список не содержит правила
  вида `Bash(bash *)`» как непроверенную гипотезу, которую обязана подтвердить сама сессия-исполнитель,
  а не принять на слово.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-02
- обновил: generator (сессия: CC-READONLY-PRINCIPAL-GEN)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
