=== SESSION LOG · 2026-08-08 · SALES-JULY-K2K3-DISCRIMINATE ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-JULY-K2K3-DISCRIMINATE — различитель K2 (непроведённые документы) / K3 (`moment`
  вне июля) остатка `155 456,37` KGS на пяти локализованных сутках (`07-20`/`07-21`/`07-27`/
  `07-29`/`07-30`), приёмка дословно `ADR-141 §5`.
- Сделано:
  1. Живой `GET entity/demand` без фильтра `applicable` по пяти суткам (границы — правило
     `ADR-088`/`ADR-137 §8`) — `69` документов, у ВСЕХ `applicable=true`.
  2. Контроль совпадения популяции: `bq query` за те же пять суток («Оптовая + без канала»)
     воспроизвёл `sales_july_daily_recon_2026-08-08.md §3` строка-в-строку (5/5 дат, точное
     совпадение строк и сумм) — СТОП не сработал.
  3. Сопоставление всех `69` документов с построчным эталоном (PDF, `243` отгрузки, разбор уже
     лежал в репо, не переснимался) по номеру документа — `0` из `69` отсутствуют в эталоне.
  4. Вердикт числами: K2 = `0,00` KGS (опровергнут фактом — непроведённых документов на этих
     сутках нет вообще), гейт фильтра `INGEST-DEMAND-APPLICABLE-FILTER` снимается без правки по
     условию, заранее заданному в `ADR-141 §5(4)`. K3 измерению этим методом не поддаётся (та же
     граница метода, что и в `SALES-JULY-DAILY-RECON`) — весь остаток `155 456,37` KGS остаётся
     необъяснённым механизмом.
- Команды/логи ключевые: `reference/_scratch_SALES-JULY-K2K3-DISCRIMINATE_2026-08-08/`
  (`run_step1.log`, `run_step2.log`, `run_step3.log`, `raw_<date>.json`, `summary.json`,
  `step2_control.json`, `step3_missing_from_oracle.json`); полный разбор —
  `reference/sales_july_k2k3_discriminate_2026-08-08.md`.
- Отклонения от плана: нет.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-JULY-K2K3-DISCRIMINATE: READY → DONE (измерение исполнено полностью, все четыре
  пункта приёмки закрыты фактом; K2 опровергнут, K3 не измерен методом этой задачи).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: различитель `SALES-JULY-K2K3-DISCRIMINATE` исполнен — K2 (непроведённые
    документы) опровергнут фактом на всех пяти сутках (`0` из `69`), K3 методом текущего `GET`
    не измеряется; артефакт `reference/sales_july_k2k3_discriminate_2026-08-08.md`.
  - Где мы: гейт `INGEST-DEMAND-APPLICABLE-FILTER` готов к закрытию архитектором — условие
    снятия без правки, заданное в `ADR-141 §5(4)`, выполнено фактом измерения; K3 остаётся
    незакрытым вопросом того же класса, что и в `SALES-JULY-DAILY-RECON`.
  - Следующий шаг: архитекторская адъюдикация результата — формальное закрытие
    `INGEST-DEMAND-APPLICABLE-FILTER` (снятие без правки кода) и решение по K3 (новый
    discovery-бриф с историей `moment` документа, либо `DEFER` остатка).
  - Развилки на владельце: нет новых (решение по K3 — на архитекторе, не на владельце, форма
    ещё не готова).
  - Счётчик: без изменений (задача не входит в реестр паритета, финиш `7/7` не затрагивается).
- Подробности для модели: `SALES-JULY-K2K3-DISCRIMINATE` (`ADR-141 §5`, DONE) исчерпала мандат
  измерением: живой `GET entity/demand` по пяти сутках без фильтра `applicable` вернул `69`
  документов, ВСЕ `applicable=true` — K2 опровергнут фактом, а не порогом (0 из 69, не «мало»).
  Все `69` документов также найдены в построчном эталоне на тех же датах (0 отсутствующих) —
  живых «лишних» документов, объяснимых черновиком или сдвигом `moment` внутрь месяца, не
  обнаружено. K3 (`moment` документа сместился за пределы июля ПОСЛЕ первой загрузки,
  `transaction_date` заморожена и не обновляется при `MERGE ... WHEN MATCHED`) методом
  списочного `GET` по ТЕКУЩЕМУ состоянию источника принципиально не проверяется: документ,
  сместившийся с одной из пяти дат, по построению не попадёт в сегодняшний список для этой
  даты — нужна история `moment` либо снимок на момент первой загрузки, вне мандата задачи.
  Итог: из `155 456,37` KGS остатка K2 = `0,00` (опровергнут), K3 не измерен, весь остаток
  необъяснён механизмом. Полный текст — `reference/sales_july_k2k3_discriminate_2026-08-08.md`.
- Новые открытые вопросы: нет новых Q-строк заведено этой сессией — измерение отвечает на уже
  существующий `CONTEXT GAP` `ADR-138 §2` частично (случаев для проверки не нашлось, вопрос
  остаётся открытым по существу МойСклада, но решения по этой задаче больше не блокирует).
- Блокеры: нет.
- updated_at: 2026-08-08
- обновил: executor (сессия: SALES-JULY-K2K3-DISCRIMINATE)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет — адъюдикация результата (закрытие гейта, решение по K3) вне мандата исполнителя
(`ADR-076 §4`, уточнено `ADR-091 §8`), передаётся архитектору.

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
