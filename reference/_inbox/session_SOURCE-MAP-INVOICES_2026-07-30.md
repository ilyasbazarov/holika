=== SESSION LOG · 2026-07-30 · SOURCE-MAP-INVOICES ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SOURCE-MAP-INVOICES — принципал/механизм загрузки `core.fact_customer_invoices`
- Сделано:
  - Шаг 1: граница `_loaded_at` — 4058 строк, один и тот же `_loaded_at` (`2026-06-05 09:06:57`),
    `distinct_load_dates=1` (`step1_run.log`).
  - Шаг 2: `INFORMATION_SCHEMA.JOBS_BY_PROJECT` по цели `core.fact_customer_invoices` — два задания,
    оба `user_email=ilyasbazarov4@gmail.com`, `2026-06-05` (`step2_run.log`).
  - Шаг 3: `bq show -j` (с `--location=asia-east1`, первая попытка без локации дала `Not found` —
    гэп наблюдения, закрыт повтором) — полный DDL `CREATE TABLE` и полный `MERGE` (явный
    `INSERT (колонки)`) (`step3_run.log`).
  - Шаг 4: инвентарь 8 живых CF — все на `etl-sa@msklad-bi-prod.iam.gserviceaccount.com`, ни одна не
    совпадает с найденным `user_email` (`step4_run.log`).
  - Расширение (в рамках read-only/класса A, обоснование в артефакте §6): Шаг 5/6 — происхождение
    staging-таблицы `core.fact_customer_invoices_stg`: один `LOAD`-job, тот же `user_email`, локальный
    NDJSON-файл (без `sourceUris` в конфиге — не GCS) (`step5_run.log`, `step6_run.log`).
  - Квалификация: исход (3) — разовый ручной прогон личного аккаунта владельца, не CF, не сервис-аккаунт.
  - Артефакт `reference/source_map_invoices_2026-07-30.md` написан целиком.
  - `11_INFRA_FACTS.md` дополнен новым разделом `§Ручные/разовые загрузки (не CF)` — найден конкретный
    факт (принципал+механизм), условие правки файла выполнено.
- Команды/логи ключевые: `reference/_scratch_SOURCE-MAP-INVOICES_2026-07-30/step{1..6}_*.sh` +
  `step{1..6}_run.log` (полные, неусечённые).
- Отклонения от плана: Шаг 4 брифа сформулирован условно («если Шаг 3 назвал сервис-аккаунт»); найден
  НЕ сервис-аккаунт, а личный аккаунт — Шаг 4 всё равно исполнен, вывод переформулирован как исключение
  всех 8 CF классом принципала (структурное несовпадение), не как «сверка не нашла совпадения». Плюс
  добавлены Шаг 5/6 сверх буквальных шагов брифа (обоснование — read-only, тот же класс A, прямо служит
  формулировке Цели про «исходный GCS-объект для LOAD-заданий»; не выводит карту полей и не открывает
  код — см. артефакт §6). Ложный стоп в начале сессии по ADR-054 на `05_CONVENTIONS.md`: первое чтение
  инструментом вернуло текст без строки `# FILE: 05_CONVENTIONS.md` — при повторной проверке (`head`,
  `git show HEAD:...`, повторный Read) строка на месте, файл корректен; расхождение было артефактом
  одного конкретного вызова инструмента чтения в этой сессии, не файла репозитория.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SOURCE-MAP-INVOICES: READY → DONE
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком):
  - Прошлый шаг: `SOURCE-MAP-INVOICES` установила фактом, что `core.fact_customer_invoices` загружена
    ОДНИМ ручным прогоном личного аккаунта владельца 2026-06-05 (не CF, не сервис-аккаунт) — артефакт
    `reference/source_map_invoices_2026-07-30.md`, факт добавлен в `11_INFRA_FACTS.md §Ручные/разовые
    загрузки`.
  - Где мы: карта происхождения снята для 6 поверхностей из 7 (без изменений этой сессией — карта полей
    для invoices этой задачей не выводилась, невозможна без кода); численно сходится 1 пара реестра из 7;
    построчных сверок не запускалось ни одной.
  - Следующий шаг: прямой вопрос владельцу — существует ли где-то (Cloud Shell/локальная машина/ноутбук)
    скрипт/файл 2026-06-05, сформировавший исходный NDJSON для этой таблицы (см. артефакт §7); параллельно
    — `STATE-COMPACT` хвостом, затем `REPORT-FIELDS` (нужна сессия `REPORT-FIELDS-GEN`).
  - Развилки на владельце: (1) `LS-RETURNS-FX-HARDCODE` — не тронуто этой сессией, прежняя формулировка
    остаётся; (2) НОВОЕ — есть ли где-то исходный скрипт/ноутбук для ручной загрузки invoices 2026-06-05;
    если нет — `Q-82` в части этой таблицы движется к решению «код навсегда утерян» (см. `NEW_DECISIONS`
    ниже, proposed).
  - Счётчик: пары реестра 1/7 сходятся · карта 6/7 поверхностей (без изменений — invoices остаётся без
    карты полей, только принципал/механизм) · Epic M 5/7 фаз · реестр открытых вопросов 59 строк до
    уборки `STATE-COMPACT` (без изменений числа этой сессией — `Q-82` дополняется текстом, не заводится
    новой строкой).
- Подробности для модели: Полный ход сессии, включая обоснование расширения за буквальные Шаг 5/6, и
  таблицу всех job_id/user_email/creation_time — см. `reference/source_map_invoices_2026-07-30.md`
  целиком (артефакт самодостаточен, здесь не пересказывается). Ключевое наблюдение для будущих сессий:
  `bq show -j` без `--location` даёт `Not found` для заданий, исполненных в `asia-east1` (не `US`) —
  тот же класс гэпа наблюдения, что уже встречался (`ADR-021 §2`), закрывается явным `--location`, не
  выводом «задание исчезло». Также: staging-таблицы вида `<table>_stg` в датасете `core` можно
  происхождать тем же приёмом (`INFORMATION_SCHEMA.JOBS_BY_PROJECT` по имени `_stg`-таблицы) — полезно
  для будущих SOURCE-MAP-задач, если встретится похожий паттерн MERGE-из-staging без известного CF.
- Новые открытые вопросы: нет (дополнение к существующему `Q-82`, не новая строка).
- Блокеры: нет.
- updated_at: 2026-07-31
- обновил: исполнитель (сессия: SOURCE-MAP-INVOICES)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет (решение о судьбе кода-загрузчика invoices как «навсегда утерян» — не эта сессия; предлагается
  как открытый вопрос владельцу в артефакте §7, не как ADR-кандидат — недостаточно фактов, чтобы
  формулировать решение, только вопрос)

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
