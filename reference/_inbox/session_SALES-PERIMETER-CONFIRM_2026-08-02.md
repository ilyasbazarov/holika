=== SESSION LOG · 2026-08-02 · SALES-PERIMETER-CONFIRM ===

**База сессии:** `git rev-parse HEAD` = `0cc54b1ee4f336ae6512853b9223278049cfd25f`
(дерево `worktrees/SALES-PERIMETER-CONFIRM`, ветка `s/SALES-PERIMETER-CONFIRM`).
**Роль:** архитектор в режиме исполнения (`ADR-096 §3` — закрепление модели за ролью не запрещает
архитектору исполнять в пределах класса задач; поручение владельца, чат 2026-08-02).
**Класс задачи:** A (read-only чтение BigQuery, запись только в `/reference`).
**Запуск без брифа** (`ADR-086 §1`): класс A; приёмка процитирована дословно в артефакте; набор файлов
на запись объявлен ниже; определение задачи взято из session-блока
`reference/_inbox/session_PARITY-SALES-H1H4-ADJ_2026-08-02.md` (ветка `s/PARITY-SALES-H1H4-ADJ`,
коммиты `11b25d3`/`59fd150`), который на момент этой сессии лежит в буфере и сборкой не разобран —
это названо, а не умолчано.
**Файлы на запись:** `reference/sales_perimeter_confirm_2026-08-02.md`,
`reference/_scratch_SALES-PERIMETER-CONFIRM_2026-08-02/`,
`reference/_inbox/session_SALES-PERIMETER-CONFIRM_2026-08-02.md`.

## SESSION_LOG

- Задача: `SALES-PERIMETER-CONFIRM`, часть A — подтверждение приписки семи документов остатка стороны
  отчёта отчётам комиссионера плюс замер исторического масштаба дефекта признания выручки по отгрузке
  комиссионеру.
- Сделано:
  - **Приёмка части A выполнена полностью, все четыре бита.** За май-2026 в
    `core.fact_commissionreportin` ровно `7` документов; множество `agent_id` в точности равно трём
    ожидаемым UUID; разбивка по документам `3 / 3 / 1`; контрагентов сверх этих трёх нет ни одного.
  - **Независимое подтверждение полноты множества:** сумма `reward + overhead` по трём контрагентам за
    май = `438 729,41` KGS против контрольной цифры `438 729,42` (`07_STATE.md:520`), верифицированной
    против оракула сессией `E1-T1-MECH-INGEST`. Расхождение `0,01` — округление.
  - **Масштаб дефекта признания выручки замерен по всей зоне паритета** (`2026-05-01` … `2026-08-01`,
    `7 120` строк, `293 247 739,41` KGS): ровно **один** документ — отгрузка ООО «РВБ» `2026-05-12` на
    `1 980 000,00` KGS (2 строки позиций). У UMAI WB и Bloom WB отгрузок в зоне нет вовсе. Доля дефекта —
    `0,675 %` зоны паритета, `2,1 %` мая-2026.
  - Полный расчёт и провенанс — `reference/sales_perimeter_confirm_2026-08-02.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-PERIMETER-CONFIRM_2026-08-02/step_a.sh`/`run.log`
  (прогон 1, `2026-08-01T20:50:31Z`…`2026-08-01T20:50:53Z`), `step_b.sh`/`run_b.log` (прогон 2,
  `2026-08-01T20:52:36Z`…`2026-08-01T20:52:48Z`), `live_schema.log` (`bq show --schema`).
  `date -u` и `gcloud auth list` первой И последней командой обоих скриптов (`ADR-055 §3/§4`,
  `ADR-063 §4`); личность `ilyasbazarov4@gmail.com` совпадает начало/конец. Все запросы `SELECT`,
  записей в источники ноль. Один скрипт на шаг (`ADR-077 §6`).
- Отклонения от плана: первый прогон упал на трёх запросах из шести (`Unrecognized name: demand_id`) —
  схема бралась из снапшота кода, а там `STAGING_SCHEMA` (`reference/code/cf-facts/bq_ops.py:46-63`,
  таблица `stg_msklad.fact_sales_staging`), не `core.fact_sales_profit`. Отказ громкий, вердикта на
  неполных данных не выносилось; запросы переписаны под живую схему вторым скриптом. Разбор ошибки и
  следствие для будущих сессий — `reference/sales_perimeter_confirm_2026-08-02.md §5`.

## STATE_PATCH

- Задача `SALES-PERIMETER-CONFIRM`: заведена (буфер `PARITY-SALES-H1H4-ADJ`) → **часть A DONE**,
  часть B остаётся открытой той же строкой (класс B, мандат не выдан). Задача целиком НЕ закрывается.
- `Q-98`: остаётся ОТКРЫТ, **сужен** — три бита приписки из четырёх подтверждены живыми данными;
  закрытие гейтится частью B (сама сумма `2 133 028,14`).
- Задача `SALES-CONSIGNMENT-REVENUE`: масштаб замерен (см. `Подробности для модели`), гейт замера снят,
  задача разгейчена по фактам и ждёт формы фикса.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - **Прошлый шаг:** подтверждающий замер `SALES-PERIMETER-CONFIRM` часть A исполнен
    (артефакт `reference/sales_perimeter_confirm_2026-08-02.md`) — приписка семи документов отчётам
    комиссионера подтверждена тремя битами из четырёх, масштаб дефекта признания выручки по зоне
    паритета замерен и равен одному документу на `1 980 000,00` KGS
  - **Где мы:** объект паритета продаж выбран владельцем — число `report/profit/*` (вариант (a)); причина
    расхождения `0,31 %` установлена и подтверждена с обеих сторон; впереди две правки нашей стороны —
    признание выручки по отгрузке комиссионеру и расширение периметра ингеста на розницу и продажи
    маркетплейсов; обе класса A на подготовке, деплой гейтится правилом «деплой только из код-репо»
  - **Следующий шаг:** `SALES-CONSIGNMENT-REVENUE` (подготовка, класс A, разгейчена) и
    `SALES-PERIMETER-EXTEND` (подготовка, класс A, разгейчена); параллельно класс A —
    `SALES-REFRESH-WINDOW` (вариант 3), `FX-MAY-WINDOW-D1-TAIL`, `METHOD-PROMOTE-LOCAL-RULES`,
    `INGEST-CURRENCY-ASSERT` шаги 1-2, подготовка `INGEST-MOMENT-ZONE-FIX`, тексты Looker Studio;
    владелец — `MSKLAD-TZ-PROPOSAL` и `CODE-REPO-STANDUP` шаг 2b
  - **Развилки на владельце:** мандат класса B на `SALES-PERIMETER-CONFIRM` часть B (один `GET`,
    закрывает `Q-98` окончательно); ответ клиента по часовому поясу аккаунта (`MSKLAD-TZ-PROPOSAL`,
    закрывает `Q-92`); мандат класса B на `AUDIT-SNAPSHOT-FIX-EMPLOYEES` и
    `AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`; допустимость `UPDATE transaction_date` и источник COGS шире
    7/90 суток — при возврате к вариантам 1/2 `SALES-REFRESH-WINDOW`; место хостинга код-репо и первый
    `push`; правка `_GENERATOR.md` по ретроспективе
  - **Счётчик:** пары реестра 1/7 сходятся · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
- Подробности для модели (append):

  **`SALES-PERIMETER-CONFIRM` часть A (2026-08-02, класс A).** Полный расчёт и провенанс —
  `reference/sales_perimeter_confirm_2026-08-02.md`, здесь не пересказывается. Ключевое для следующих
  сессий. (i) Комиссионный канал за май-2026 исчерпывается тремя контрагентами
  (`0276f431-2ff5-11ef-0a80-11d40019917f` — 3 документа, `3c080755-03ff-11f0-0a80-0c2c00104bbb` — 3,
  `31d135bc-4df8-11f1-0a80-1c8a0053c5b4` — 1), и это не предположение: сумма `reward + overhead` по ним
  сходится с уже верифицированной против оракула контрольной цифрой `438 729,42` (`07_STATE.md:520`) с
  точностью `0,01`. (ii) Дефект признания выручки по отгрузке комиссионеру в зоне паритета единичен —
  один документ, `1 980 000,00` KGS, `2026-05-12`, две строки позиций; `0,675 %` выручки зоны,
  `2,1 %` выручки мая. (iii) У двух из трёх контрагентов отгрузок в зоне паритета нет вовсе при
  наличии продаж в отчёте — товар передавался до `2026-05-01` либо не документом `entity/demand`;
  вопрос не устанавливался, вывода не делается. (iv) **Ловушка для будущих сессий:** схема
  `core.fact_sales_profit` в снапшоте кода отсутствует — `bq_ops.py:46-63` это `STAGING_SCHEMA`
  таблицы `stg_msklad.fact_sales_staging` (`bq_ops.py:82`/`113`). Живая core-таблица не несёт
  `demand_id`/`position_id`, ключ строки — `transaction_id = MD5(demand_id + position_id)`,
  необратимый хэш; форма таблицы документирована в `02_ERP_CONTRACTS.md:36-49` и брать её надо оттуда,
  а снапшот кода использовать для поведения загрузчика. Следствие: число ДОКУМЕНТОВ по контрагенту из
  `core` не получается в принципе, только строки/сутки/сумма.

- Новые открытые вопросы: нет (`SALES-PERIMETER-CONFIRM` часть B уже стоит строкой той же задачи).
- Блокеры: нет.
- updated_at: 2026-08-02
- обновил: архитектор в режиме исполнения (сессия: `SALES-PERIMETER-CONFIRM`)

## NEW_DECISIONS

- нет. Замер подтверждающий, новых решений не порождает; квалификация его результата уже стоит в
  решении сессии `PARITY-SALES-H1H4-ADJ` (буфер, `§4`/`§6`/`§7`).

## NEW_CONVENTIONS

- нет

=== END SESSION ===
