=== SESSION LOG · 2026-08-05 · PIPELINE-MODE-OBSERVATION ===

## SESSION_LOG

- Задача: `PIPELINE-MODE-OBSERVATION` — запись прецедента сквозной сессии как режима конвейера плюс
  замер её стоимости. Класс A, роль архитектор, без брифа, по прямому поручению владельца в чате
  2026-08-05. Строки в реестре на момент старта не было и она НЕ заводится: это процессное
  наблюдение, а не предметная задача (`ADR-107 §4`). База — `76645fb`.
- Сделано:
  - Артефакт `reference/pipeline_mode_observation_2026-08-05.md`: разбор прецедента с поправкой к
    прочтению владельца, рамка режима (что поглощает, что не поглощает), правило остановки, замер
    стоимости с названными пределами, перечень того, чего не хватает для решения.
  - **Замер стоимости исполнен по транскриптам сессий** (тот же источник, что служил доказательством
    в `ADR-080 §1`). Выборка — 85 сессий проекта с числом обменов `≥ 20`. В репо вынесены ТОЛЬКО
    агрегаты и имена задач; тела реплик и промптов не извлекались.
  - **Главный результат замера: экономии токенов нет, и гипотеза о «налоге за контекст» опровергнута
    фактом.** Свежий вход сквозной сессии — `420` токенов из почти `80` миллионов, то есть контекст
    и в коротких сессиях переиспользуется через кэш, а не читается заново; экономить нечего. При
    самом благоприятном прочтении (замещены пять чатов) сквозная сессия дороже на `32 %` по выходу и
    вровень по чтению кэша.
  - **Контроль, снимающий обвинение с режима:** три ОДНОЗАДАЧНЫЕ сессии истории сопоставимы по длине
    и дороже либо сравнимы по выходу (`DEPLOY-REVISION-PROBE` — `539 665`, `PARITY-STOCK-SNAPSHOT-SYNC`
    — `458 833`, `REGISTRY-COMPACT-ADJ` — `440 816` против `499 021` этой сессии). Дорогой сессию
    делает глубина работы, а не число поглощённых шагов.
  - Установлено, что естественная граница режима не выдумывается, а проступает сама, тремя способами:
    сборка не поглощается физически (git), закрытие пары реестра не поглощается по правилу
    (`ADR-076 §4`/`ADR-091 §8`), класс B не поглощается без отдельного объявления (`ADR-077 §6`).
    Прецедент границу НЕ нарушил — проверено поимённо по каждому из четырёх ходов.
  - **Второй ход (по прямому поручению владельца, чат 2026-08-05): подготовлен пакет запуска
    `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 в НОВОЙ форме.** Артефакт —
    `reference/stock_sync_step2_launch_2026-08-05.md`. Владелец прямо запретил исполнение задачи в
    настоящем чате; исполнения и не было — ни одного вызова к облаку, ни одного чтения секретов.
    **Найденный факт, снимающий зависимость замера от ночного расписания:** загрузчик остатков
    идемпотентен по построению (`reference/code/cf-inventory/main.py`, шаг 5 удаляет партицию
    текущих бишкекских суток, шаг 6 пишет заново), поэтому окно пересборки нашей стороны можно
    создать принудительно в любой час, и требование `ADR-123 §3` исполняется точнее ночного
    варианта — элапс между сторонами становится равен времени работы функции, а не интервалу до
    расписания. **Проверен и снят главный риск формы:** внутренняя проверка данных функции стоит ДО
    удаления партиции (шаг 3 против шага 5), то есть её провал ядро не трогает вовсе — установлено
    чтением кода, а не докстринга. Собраны фактура объекта с адресами, шесть поимённо названных
    рисков, разграничение «что из брифа остаётся / что заменяется», рекомендация по роли и модели и
    готовый стартовый промпт.
- Команды/логи ключевые: `bash tools/session_status.sh` (ЧИСТО); `bash tools/hooks/selftest.sh`
  («провалено 0»); `git worktree add worktrees/PIPELINE-MODE-OBSERVATION -b s/PIPELINE-MODE-OBSERVATION HEAD`;
  три версии замера в `reference/_scratch_PIPELINE-MODE-OBSERVATION_2026-08-05/`.
- Отклонения от плана: **две версии замера отброшены как негодные и это зафиксировано, а не
  заглажено.** Автоклассификация сессий по ролям через поиск по тексту промпта дала заведомо ложные
  группы дважды (`70` и `72` сессии из `85` отнесены к сборке), потому что вывод `git log` в любой
  сессии содержит строку «docs: сборка буфера». Классификация отброшена целиком, сравнение ведётся
  против всей популяции, и это названо пределом замера, а не обойдено. Третий инстанс класса
  «автовердикт текстового поиска — не факт» (`ADR-044`) за один день.

## STATE_PATCH

- Строк реестра под ПЕРВЫЙ ход эта сессия не заводит и не закрывает: процессное наблюдение строки
  под себя не получает (`ADR-107 §4`). Парность `ADR-090 §1` не затрагивается — строки мандата тоже
  нет.
- Строка `PARITY-STOCK-SNAPSHOT-SYNC` (второй ход): статус `PARTIAL` НЕ меняется, в текст вносится
  дописка о новой форме шага 2 — **принудительный прогон `cf-inventory` через
  `cf-inventory-trigger` плюс собственный живой `GET` сразу за ним, в любой час, вместо ожидания
  окна `21:00Z`.** Основание: загрузчик идемпотентен по построению (`main.py` шаги 5-6), поэтому окно
  пересборки создаётся по требованию; требование `ADR-123 §3` не ослабляется, а исполняется точнее
  (элапс равен времени работы функции). Цена названа: сохранённый снимок суток смещается с `03:00`
  на момент прогона и восстановлению не подлежит. **Мандат класса B на ЗАПИСЬ по-прежнему НЕ выдан**
  — прежний (владелец, чат 2026-08-04) покрывал только чтение; новый выдаётся владельцем поимённо в
  тексте запуска. Полный пакет запуска, фактура объекта, шесть рисков и стартовый промпт —
  `reference/stock_sync_step2_launch_2026-08-05.md`.
  **Дописка обязана лечь ЭТИМ проходом сборки:** иначе бриф (клауза «окно `21:00Z`») разойдётся со
  строкой реестра по форме шага, а расхождение брифа с реестром есть стоп-условие сессии.
- Строка мандата `PARITY-STOCK-SNAPSHOT-SYNC` в таблице `§Мандат Claude Code: класс задач`: в поле
  оснований дописывается, что мандат покрывает ТОЛЬКО чтение, а действие записи (принудительный
  прогон) требует отдельного поимённого мандата. Класс, параллель и поле «Пишет:» не меняются.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: записан прецедент сквозной сессии с рамкой и правилом остановки, исполнен замер её
    стоимости — экономии токенов нет, размен лежит в контроле качества
    (`reference/pipeline_mode_observation_2026-08-05.md`).
  - Где мы: шесть пар реестра из семи сходятся; до формального финиша остаётся строка 21 (замер плюс
    вердикт), до снятия претензии клиента — выкатка готового патча и отдельно разрез по сотрудникам.
  - Следующий шаг: `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 в форме «принудительный прогон `cf-inventory`
    плюс замер сразу за ним» (класс B, мандат на запись НЕ выдан — прежний покрывает только чтение);
    `SALES-EMPLOYEE-ATTRIBUTION` (класс A, ждёт отчёта клиента); `SALES-INGEST-PATCH-DEPLOY`
    (класс B, мандат владельцем дан в чате, требует объявления действия и брифа);
    `INTRANSIT-SURFACE-LAG` (класс A).
  - Развилки на владельце: мандат класса B на принудительный прогон `cf-inventory` (меняет момент
    суточного снимка на клиентской витрине); какое правило разреза по сотруднику считать правильным;
    правка `04_ROADMAP`, переставшего описывать очередь.
  - Счётчик: пары реестра **6/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели:

  **Сквозная сессия как режим конвейера: прецедент записан, замер исполнен, решение отложено до
  снятия заморозки (`PIPELINE-MODE-OBSERVATION`, 2026-08-05).** Полный разбор —
  `reference/pipeline_mode_observation_2026-08-05.md`, здесь не пересказывается. Что обязана знать
  следующая сессия. (i) **Экономии токенов у режима НЕТ, и довод «пять чатов платят налог за
  контекст пять раз» опровергнут фактом:** свежий вход длинной сессии — `420` токенов из `79,6` млн,
  то есть контекст переиспользуется через кэш и в коротких сессиях; при замещении пяти чатов
  сквозная дороже на `32 %` по выходу. Не повторять этот довод в планировании. (ii) **Дорожает
  каждый обмен:** выход на обмен `2 248` против медианных `884`, чтение кэша на обмен `358 529`
  против `169 084` — механика большого контекста, а не случайность. (iii) **Режим не является
  причиной дорогой сессии:** три однозадачные сессии истории сопоставимы по длине и дороже либо
  сравнимы. (iv) **Настоящий размен — потеря свежего взгляда**, и у проекта есть измеренная цена
  этой потери: `ADR-120 §1` перевернул вердикт предыдущей сессии, найдя, что «построчная сверка с
  нулями» была односторонней по построению. Смягчение, стоящее ноль: самопроверка численным
  замыканием вместо самопроверки суждением — там, где вывод замыкается арифметически, свежий взгляд
  заменяется проверкой; там, где вывод есть суждение, — не заменяется ничем. (v) **Граница режима**
  (сборка не поглощается физически, закрытие пары не поглощается по правилу, класс B не поглощается
  без объявления) и **правило остановки** («сессия кончается перед шагом, где ей пришлось бы судить
  собственный результат») сформулированы в `§2` артефакта как КАНДИДАТ, не как принятое правило.
  (vi) **Чего решению не хватает:** второго и третьего прецедента — один заход не выборка; разумно
  провести в режиме ещё две-три задачи, фиксируя каждый раз, случалось ли нарушение границы.

  **Ловушка замера, стоившая двух отброшенных версий и годная для любой будущей работы с
  транскриптами.** Классифицировать сессию по роли поиском по тексту стартового промпта НЕЛЬЗЯ:
  любая сессия печатает `git log`, а в нём есть строка «docs: сборка буфера», поэтому признак
  «сборка» ловится где угодно (`70` и `72` ложных из `85` в двух версиях). Надёжный признак —
  имя ветки либо рабочего дерева (`s/<TASK>`, `worktrees/<TASK>`): его заводит сама сессия, и
  посторонняя сессия его не печатает. Третий за день инстанс класса `ADR-044` («автовердикт
  текстового поиска — не факт»).

- Новые открытые вопросы: нет (наблюдение строк реестра не заводит, `ADR-107 §4`).
- Блокеры: нет.
- updated_at: 2026-08-05
- обновил: архитектор (сессия: PIPELINE-MODE-OBSERVATION)

## NEW_DECISIONS

- нет. Настоящая сессия решений не принимает: процессный слой заморожен (`ADR-107 §1`), исключение
  по `§2` не заявляется — ни одна задача паритета без этого решения не встаёт, а сам режим применим
  по решению владельца в каждом конкретном запуске уже сегодня. Кандидат решения (рамка, правило
  остановки, фактура замера) подготовлен в `reference/pipeline_mode_observation_2026-08-05.md` и
  ждёт снятия заморозки при сходимости реестра (`ADR-107 §7`).

## NEW_CONVENTIONS

- нет.

=== END SESSION ===
