=== SESSION LOG · 2026-08-17 · SALES-REFRESH-WINDOW-STAGE3-CHECK ===

## SESSION_LOG
- Задача: `SALES-REFRESH-WINDOW-STAGE3-CHECK` — проверка ступени 3 приёмки
  `SALES-REFRESH-WINDOW-DEPLOY-FINAL` (первый недельный прогон `2026-08-16T01:00Z`), класс A,
  без брифа (`ADR-086 §1`), read-only.
- Сделано: снят `gcloud workflows executions describe` исполнения `d5921070-…` (`FAILED`,
  `01:00:01Z…01:18:52Z`); изолированы BQ-джобы MERGE ветки продаж (`deleted=0, inserted=0,
  updated=7133`, окно 90 суток) и ветки периметра (не исполнилась — предохранитель `ADR-145 §4`);
  снята свежесть `stg_msklad.fact_sales_perimeter_staging` (`max_date=2026-08-10` при запрошенном
  диапазоне до `2026-08-16`); снят май-2026 (`3404` строк / `95 214 205,80` KGS) и разбиение
  живая/снимок (`43270`/`42995`). Полный разбор — `reference/sales_refresh_window_stage3_2026-08-17.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-STAGE3_2026-08-17/`
  (`step1_run.log`, `step2_run.log`, `step3_run.log`).
- Отклонения от плана: нет — задача была read-only проверкой по уже заданному критерию
  (`…deploy_final_2026-08-12.md §5`, `…deploy_final_adj_2026-08-12.md §4`), не исполнением брифа.

## STATE_PATCH
- Задача `SALES-REFRESH-WINDOW-DEPLOY-FINAL`: PARTIAL → PARTIAL (без изменения статуса; ступень 3
  проверена и остаётся ОТКРЫТОЙ — не закрыта этой сессией)
- Задача `SALES-REFRESH-WINDOW`: OPEN → OPEN (без изменения; гейт уточнён)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` целиком, ровно пять строк):
  - Прошлый шаг: ступень 3 `SALES-REFRESH-WINDOW-DEPLOY-FINAL` проверена read-only
    (`reference/sales_refresh_window_stage3_2026-08-17.md`) — недельный прогон `2026-08-16T01:00Z`
    упал на новом предохранителе, не на отказе полноты
  - Где мы: ветка продаж патча испытана на 90-суточном окне впервые и чисто (`0` удалений);
    ветка периметра всё ещё ни разу не исполнилась — второй раз подряд по НОВОЙ причине
  - Следующий шаг: адъюдикация архитектора — маршрут (a) ждать `2026-08-23` как новую попытку
    периметра, либо (b) считать `2026-08-10`-разрыв дефектом `fetch_perimeter.py` и чинить раньше
  - Развилки на владельце: нет — вопрос адресован архитектору
  - Счётчик: без изменений этой сессией (read-only проверка, не движение по счётчику паритета)
- Подробности для модели: **Недельный прогон `2026-08-16T01:00Z` упал на предохранителе
  `ADR-145 §4` (свежесть), не на проверке полноты `ADR-171`.** Ветка продаж (`_build_merge_sql`,
  окно 90 суток, впервые захватывает часть мая `2026-05-18…`) исполнилась чисто — `job_id
  39293e3f-4baa-4fae-8311-b9fdf2f0e311`, `inserted=0 deleted=0 updated=7133`. Ветка периметра
  (`_build_perimeter_merge_sql`) остановлена ДО MERGE: `stg_msklad.fact_sales_perimeter_staging`
  свежая (загружена в рамках этого же прогона, `_loaded_at 2026-08-16 01:18:21`), запрошенный
  диапазон выборки правильный (`fetch_perimeter.py::_run_perimeter_load`, `date_from=date_to-90`),
  но фактически полученные документы (`entity/retaildemand`+`entity/commissionreportin`) не идут
  дальше `2026-08-10` — разрыв `6` суток между запрошенным и полученным верхним краем. Причина
  разрыва (лаг источника / дефект пагинации-фильтра запроса) НЕ установлена — требует живого `GET`
  к МойСклад, класс B, мандат не выдан. Прецедент отличия: прогон `2026-08-09` падал на другом шаге
  (`raise_dq_failed`), до периметра не доходил — это первый раз, когда именно свежесть-предохранитель
  периметра сработал в проде. Полный текст — `reference/sales_refresh_window_stage3_2026-08-17.md`.
- Новые открытые вопросы:
  - `SALES-PERIMETER-STAGING-LAG` (кандидатное имя, номер присваивает сборка): почему
    `fetch_perimeter.py` за запрошенный диапазон `2026-05-18…2026-08-16` фактически получает
    документы `entity/retaildemand`+`entity/commissionreportin` только по `2026-08-10`
    включительно — лаг источника или дефект выборки/пагинации; блокирует испытание ветки удаления
    периметра (второй половины патча `SALES-REFRESH-WINDOW`) на реальном недельном прогоне; гейт —
    discovery (класс A: чтение кода `fetch_perimeter.py`/`helpers.py::paginate_entity` дальше уже
    прочитанного; класс B: живой `GET entity/retaildemand`/`entity/commissionreportin` с широким
    диапазоном для проверки, есть ли документы после `2026-08-10` в источнике).
- Блокеры: ступень 3 `SALES-REFRESH-WINDOW-DEPLOY-FINAL` не закрыта; слияние ветки код-репо
  `deploy/cf-facts-2026-08-12-completeness-and-delete` в `master` остаётся запрещённым
  (решение архитектора, чат 2026-08-13, подтверждено фактом — периметр всё ещё не испытан).
- updated_at: 2026-08-17
- обновил: исполнитель (сессия: `SALES-REFRESH-WINDOW-STAGE3-CHECK`)

## NEW_DECISIONS
нет

## NEW_CONVENTIONS
нет

=== END SESSION ===
