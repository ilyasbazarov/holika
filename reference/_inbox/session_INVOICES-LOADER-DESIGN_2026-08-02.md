=== SESSION LOG · 2026-08-02 · INVOICES-LOADER-DESIGN ===

## SESSION_LOG

- Задача: `INVOICES-LOADER-DESIGN` — проект загрузчика счетов покупателям (T2 программы `ADR-110`)
- Класс: A. Запуск без брифа по `ADR-086 §1`; четыре условия проверены и напечатаны в артефакте §0.
- База на старте: `d20d9580b81561bfed1573495e9d8d8769a7d95c`. Дерево `worktrees/INVOICES-LOADER-DESIGN`,
  ветка `s/INVOICES-LOADER-DESIGN`. `tools/session_status.sh` RC 0 («ЧИСТО для сессии в своём дереве»),
  `tools/hooks/selftest.sh` — `пройдено 36, провалено 0`.
- Сделано: восемь предметов постановки закрыты решением с обоснованием либо явным `CONTEXT GAP`.
  Артефакт `reference/invoices_loader_design_2026-08-02.md` — самодостаточный вход для T3.
  Облачных вызовов ноль: вся фактура снята чтением файлов репо.
- Принятые решения проекта (полный текст и обоснование — в артефакте, здесь только перечень):
  режим `invoices` внутри `cf-finance` с диспетчером по полю `mode` и умолчанием `payments`;
  вызов отдельным Cloud Scheduler job'ом `invoices-daily-update`, `0 4 * * *` `Asia/Bishkek`;
  полный обход `entity/invoiceout` явным `offset`/`limit=100` с `expand=state,agent,salesChannel,rate.currency`;
  правило суток `DATE(M+6ч)` в SQL, не в Python; `payment_planned` — то же правило, решение принято,
  замер не заказан; staging `stg_msklad.fact_customer_invoices_staging` на 19 колонок с сырыми
  минорными единицами, курсом и кодом валюты; `MERGE` по ключу `invoice_id` с явным `INSERT (колонки)`,
  с `T.moment` в `UPDATE SET` и с веткой `WHEN NOT MATCHED BY SOURCE THEN DELETE`; fallback-ветка курса
  по `ADR-010`/`ADR-029` обязательна; черновики фильтруются в базе, не на выгрузке (`ADR-034`).
- Находки сверх постановки, три:
  (1) в список `expand` обязан входить `rate.currency` — без него нет `isoCode` (`ADR-027`) и
      fallback-ветка не может назвать валюту; постановка называла только три `expand`;
  (2) `core.fact_customer_invoices` не партиционирована и не кластеризована — открытый вопрос
      `SALES-MERGE-DRYRUN` про `UPDATE` партиционирующей колонки к этой таблице не относится вовсе;
  (3) пятое условие готовности программы (`ADR-110 §4`, наблюдаемость) не достигается задачами
      `T2`–`T4` и гейтится `DQ-GATE-SCOPE-SPLIT` — ребро графа, которого нет в декомпозиции `ADR-110 §3`.
- Команды/логи ключевые: `bash tools/session_status.sh` (RC 0); `bash tools/hooks/selftest.sh`
  (провалено 0); два скрипта провенанса с `date -u` первой и последней командой —
  `reference/_scratch_INVOICES-LOADER-DESIGN_2026-08-02/design_provenance.log` (восемь греп-вердиктов
  с напечатанными строками, `ADR-044`) и `…/design_selfcheck.log` (машинный разбор текста `MERGE`:
  13 присваиваний `UPDATE`, 14 имён = 14 значений `INSERT`, `INSERT ROW` в исполнимом SQL — ноль).
- Отклонения от плана: нет.

## STATE_PATCH

- Задача `INVOICES-LOADER-DESIGN`: OPEN → **DONE** (артефакт `reference/invoices_loader_design_2026-08-02.md`).
  Строка уезжает в `07_ARCHIVE.md` дословно тем же коммитом (`ADR-064`), вместе с парной строкой
  таблицы `§Мандат Claude Code: класс задач` (`ADR-090 §1`).
- Задача `INVOICES-LOADER-BUILD`: гейт `INVOICES-LOADER-DESIGN` СНЯТ, задача разгейчена и исполнима сейчас.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: `INVOICES-LOADER-DESIGN` исполнена — восемь предметов проекта закрыты решением либо
    явным гэпом, текст `MERGE` приведён полностью и машинно проверен на `C1`, артефакт
    `reference/invoices_loader_design_2026-08-02.md` самодостаточен для постройки кода
  - Где мы: восстановление потока данных остаётся приоритетом перед сверкой паритета «на сейчас»
    (`FACTS-WORKFLOW-STOP-DIAG`, `ADR-111`) — решение владельца о форме восстановления не получено;
    отдельная ветка счетов дошла до готового проекта и упирается дальше в постройку кода, а затем в
    первый выгруз код-репозитория (`ADR-065`)
  - Следующий шаг: решение владельца о форме восстановления загрузки, затем `FACTS-BACKFILL` (класс B);
    параллельно разгейчена `INVOICES-LOADER-BUILD` (класс A, гейтов больше нет); отдельно — решение
    владельца/архитектора по гэпу COGS-источника перед выбором варианта `SALES-REFRESH-WINDOW`;
    параллельно без изменений — `DIM-METADATA-MAPPINGS-FROZEN`, `MARTS-BUILD-STAMP`,
    `FACT-SALES-BYVARIANT-BACKUP-OWNER`, `SALES-CONSIGNMENT-REVENUE`, `SALES-PERIMETER-EXTEND`,
    `FX-MAY-WINDOW-D1-TAIL`, `INGEST-CURRENCY-ASSERT` шаги 1-2, подготовка `INGEST-MOMENT-ZONE-FIX`,
    тексты Looker Studio
  - Развилки на владельце: решение о форме восстановления загрузки; первый `push` код-репо в
    `holika-prod`; ответ клиента по часовому поясу (`MSKLAD-TZ-PROPOSAL`); мандат класса B на
    `AUDIT-SNAPSHOT-FIX-EMPLOYEES`/`AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`; выбор варианта патча
    `SALES-REFRESH-WINDOW`
  - Счётчик: пары реестра 2/7 сходятся · таблиц живых 15 из 26 (до восстановления, не переизмерялось) ·
    строк реестра на критическом пути 23 из 63 · Epic M 5/7 фаз
- Подробности для модели:

  **`INVOICES-LOADER-DESIGN` закрыта.** Полный проект — `reference/invoices_loader_design_2026-08-02.md`
  (самодостаточен для `INVOICES-LOADER-BUILD`, здесь не пересказывается). Четыре факта, без которых
  следующая сессия по этой ветке пойдёт вслепую. (i) **Снапшот `reference/code/cf-finance/` не годится
  как база патча:** ревизия `00006-piv` против живой `00012-cik`, и диспетчера режимов в нём нет вовсе
  — `grep -n "mode" reference/code/cf-finance/main.py` даёт 0 совпадений, точка входа есть
  `def main(request): run_etl(); return "OK", 200` (`main.py:138-140`). T3 обязан снять свежий снапшот
  ПЕРВЫМ шагом, до написания патча; проект задаёт целевую форму диспетчера, а не правку по номерам строк.
  (ii) **`core.fact_customer_invoices` не партиционирована и не кластеризована** — в `bq show`
  отсутствуют `timePartitioning`/`rangePartitioning`/`clustering`/`requirePartitionFilter` (проверено
  программно по уже лежащему в репо
  `reference/_scratch_INVOICES-STALENESS-PROBE_2026-08-02/bq_show_fact_customer_invoices.json`).
  Следствие: открытый вопрос `SALES-MERGE-DRYRUN` о допустимости `UPDATE` партиционирующей колонки
  внутри `MERGE` к счетам не относится в принципе, ловушка (iii) `ADR-100 §7` здесь неприменима.
  (iii) **Список `expand` шире, чем называла постановка:** обязателен четвёртый компонент
  `rate.currency`, иначе `currency_code` из `isoCode` (`ADR-027`) недоступен и fallback-ветка курса
  (`ADR-029`) не может назвать валюту, для которой запрашивает текущий курс. Приём четырёх `expand`
  при `limit=100` на `entity/invoiceout` не пронаблюдён — проверяется T3 одним read-only `GET`;
  провал означает тихую деградацию `expand` (`02_ERP_CONTRACTS.md:297`) и лечится двумя проходами.
  (iv) **Пятое условие готовности программы недостижимо задачами `T2`–`T4`.** `ADR-110 §4` требует,
  чтобы проверка свежести покрывала таблицу; проект даёт текст такой проверки (техническая свежесть
  по `_loaded_at`, порог 48 часов, выведенный из формы уже принятого правила `03_PIPELINE_SPEC.md:86`),
  но её РАЗМЕЩЕНИЕ уходит в `DQ-GATE-SCOPE-SPLIT`, и не ради чистоты: добавление седьмого чека в
  `cf-dq` до разделения гейта заставило бы провал по счетам гасить промоут продаж, закупки и возвраты
  — то есть расширило бы уже известный радиус поражения (`07_GAPS.md:87`). Декомпозиция `ADR-110 §3`
  этой зависимости не называет; решение о ней — архитектора.

  **Побочно, меняет цену двух других задач.** Проект тянет из источника ВСЁ без фильтра по дате
  (прецедент `03_PIPELINE_SPEC.md:45`, полный re-fetch платежей) и держит в staging полный набор
  документов вместе с флагом проведённости (`ADR-034`: тянуть всё, фильтровать в базе). Отсюда два
  следствия, ни одно из которых настоящей сессией не реализуется и не меняет статусов:
  `INVOICES-BACKFILL` (T5) сводится к проверке, потому что первый же успешный прогон подтягивает
  историю целиком; `INVOICES-PERIMETER-DIFF` частично удешевляется с класса B до запроса к
  собственной staging, потому что недостачу `468` документов можно будет разложить по статусам и
  флагу проведённости без живого `GET` к МойСкладу.

  **Расхождение репо о публичном доступе `cf-finance`, названное и не исправленное.**
  `01_ARCHITECTURE.md:25` несёт пометку `⚠️ allow-unauthenticated`, тогда как
  `11_INFRA_FACTS.md:28/149` фиксируют IAM-lockdown `2026-07-20` (`ADR-022`): привязка `allUsers`
  снята, анонимный запрос `403`, вызов Scheduler `200`. То есть пометка описывает состояние до
  `2026-07-20`, а оговорка `ADR-110 §5(i)`, на неё опирающаяся, вероятно уже погашена. Живой IAM
  сессией не перепроверялся, `01_ARCHITECTURE.md` не правился (STABLE, только через ADR).
  Не блокирует ничего; адресат — архитектор.

- Новые открытые вопросы: нет. Семь `CONTEXT GAP` артефакта §11 не заводят строк реестра — шесть из
  них адресованы уже существующим задачам (`INVOICES-LOADER-BUILD`, `INVOICES-PERIMETER-DIFF`,
  `INVOICES-PARITY-RECHECK`, `DQ-GATE-SCOPE-SPLIT`), седьмой (расхождение о публичном доступе) есть
  наблюдение для архитектора. Процессное наблюдение о классе живых `GET` с секретом записано одной
  строкой в артефакт §12 и строки под себя не получает (`ADR-107 §4`).
- Блокеры: нет.
- updated_at: 2026-08-02
- обновил: исполнитель (сессия: `INVOICES-LOADER-DESIGN`)

## NEW_DECISIONS

нет.

Проектные решения настоящей задачи живут в артефакте `reference/invoices_loader_design_2026-08-02.md`
и нового ADR не требуют: место жительства уже решено `ADR-110 §5` (владелец, вариант A), а остальные
семь предметов выводятся из уже принятых решений (`ADR-088`, `ADR-010`, `ADR-027`, `ADR-029`,
`ADR-030`/`C1`, `ADR-034`, `ADR-100`/`C2`, `ADR-007`) — они применяются, а не переопределяются.
Заводить ADR под применение действующего канона значило бы дублировать журнал.

## NEW_CONVENTIONS

нет.

=== END SESSION ===
