=== SESSION LOG · 2026-08-04 · INVOICES-PARITY-RECHECK ===

## SESSION_LOG
- Задача: INVOICES-PARITY-RECHECK — повторная сверка пары «Счета покупателям» после досева
- Сделано: мандат класса B получен постфактум в чате владельца (форма прецедента `ADR-092`/`ADR-117`);
  действие объявлено отдельным сообщением до запуска (`ADR-077 §6`); один живой построчный `GET
  entity/invoiceout` (пагинация `limit=100`, `expand=state,agent,salesChannel,rate.currency`, форма —
  дословно из `reference/code/cf-finance/invoices.py`) плюс один `SELECT` к
  `marts.customer_invoices_ar`; конвертация валют выполнена впервые для этой пары (в отличие от
  грубого замера `PARITY-COARSE-TOTALS`, где она пропускалась); разность сведена без остатка к одной
  причине (асинхронность суточного снимка); строка 23 `reference/parity_registry.md` заполнена мостом
  и статусом; артефакт `reference/invoices_parity_recheck_2026-08-04.md`
- Команды/логи ключевые: `reference/_scratch_INVOICES-PARITY-RECHECK_2026-08-04/run.log` (якоря
  `date -u`/`gcloud auth list` в начале `06:33:23Z` и в конце `06:44:20Z`, личность вызывающего не
  менялась); `grep -rF` по значению токена — 0 совпадений (дважды: по каталогу сессии сразу после
  прогона и по всему `reference/` перед коммитом)
- Отклонения от плана: первый запуск скрипта (`bt3w1emdp`) ошибочно принят за зависший и убит через
  ~6 минут — диагностика (`curl`/голый `requests.get`) показала: сеть исправна, просто каждая страница
  с `expand` из четырёх компонентов весит ~1,6 МБ и обрабатывается API ~14с, то есть полный обход
  46 страниц занимает ~11 минут; повторный запуск (`bikcoiuve`) отработал штатно за 11 минут. Слепого
  retry не было — сначала read-only диагностика (`ADR-055 §2`), затем чистый повторный прогон

## STATE_PATCH
- Задача INVOICES-PARITY-RECHECK: OPEN → DONE
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком):
  - Прошлый шаг: `INVOICES-PARITY-RECHECK` закрыта DONE — пара «Счета покупателям» сошлась с точностью
    до правила одновременности (разность `0,022%`/`0,0034%`/`0,00%`/`0,030%`, полностью объяснена одним
    новым документом за ~8,5ч между суточной загрузкой и снятием эталона), `reference/invoices_parity_recheck_2026-08-04.md`.
  - Где мы: условие 4 готовности программы «Загрузчик счетов покупателям» выполнено вариантом
    «расхождение закрыто правилом» (`ADR-068 §1`); пара «оплачено» сошлась до копейки, что исключает
    ошибку конвертации/периметра на загруженном множестве.
  - Следующий шаг: `INVOICES-PERIMETER-DIFF` больше не единственная неизмеренная построчно пара — три
    из четырёх пар «второй волны» (остатки/в пути/счета) теперь измерены; параллельно —
    `PARITY-SALES-CLOSE` по очереди `ADR-116 §1`.
  - Развилки на владельце: нет.
  - Счётчик: пары реестра **3/7** сходятся (эта сессия перевела «Счета покупателям» из «не сверялось»
    в «сошлась с оговоркой») · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: <нет — весь ход и провенанс самодостаточны в
  `reference/invoices_parity_recheck_2026-08-04.md`, дублировать нечего. Единственное, что может
  сломать следующую сессию, если не прочитать: пара «Счета покупателям» сходится ТОЛЬКО при
  одновременном снятии обеих сторон (загрузчик суточный, `04:00` Бишкек) — любой будущий live-замер
  этой пары обязан либо запускаться сразу после суточной загрузки, либо принимать плавающую разность,
  пропорциональную интервалу, как норму, а не как регресс>
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-04
- обновил: исполнитель (сессия: INVOICES-PARITY-RECHECK)

## NEW_DECISIONS
- ADR-0XX: Мандат класса B на `INVOICES-PARITY-RECHECK` выдан постфактум владельцем в чате 2026-08-04
  Контекст: сессия остановилась на `CONTEXT GAP` (мандат не выдан, `07_STATE.md:658`/`07_GAPS.md:80`);
  владелец выдал мандат постфактум в чате той же сессии (форма прецедента `ADR-092 §1`/`ADR-117`),
  действие было объявлено отдельным сообщением ДО запуска (`ADR-077 §6`): объект (read-only `GET
  entity/invoiceout` + read-only `SELECT marts.customer_invoices_ar`), объём (один постраничный обход
  без фильтра по дате, форма — дословно из `reference/code/cf-finance/invoices.py`), откат (не
  требуется, ничего не пишется)
  Решение: мандат класса B на `INVOICES-PARITY-RECHECK` считается выданным этим постфактум-решением;
  шесть оговорок `ADR-076 §5` и форма логирования `ADR-079 §6` соблюдены (токен не печатался,
  `grep -rF` дал 0 совпадений дважды, якоря `date -u`/`gcloud auth list` в начале и в конце, форма
  запроса не изобретена — взята из снапшота боевого кода, эталон датирован снимком)
  Последствия: [сейчас] `07_STATE.md:658` — строка мандата `INVOICES-PARITY-RECHECK` меняется с «НЕ
  выдан» на номер этого ADR; [сейчас] `07_GAPS.md:80` — статус строки `OPEN` → `CLOSED`, полный текст
  переносится в `07_ARCHIVE.md`; [без правок] `08_TASK_BRIEF_TEMPLATE.md`, `05_CONVENTIONS.md`,
  `CLAUDE.md` — форма мандата постфактум уже описана прецедентом `ADR-117`, нового правила не вводится
  Статус: accepted (владелец, чат 2026-08-04)

## NEW_CONVENTIONS
- нет

=== END SESSION ===
