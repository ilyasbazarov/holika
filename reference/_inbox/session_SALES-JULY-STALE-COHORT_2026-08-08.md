=== SESSION LOG · 2026-08-08 · SALES-JULY-STALE-COHORT ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-JULY-STALE-COHORT` — проверка разложения остатка `155 456,37` KGS и различение
  подслучая A по GCS-архиву (класс A, без брифа, приёмка дословно `ADR-143 §5`).
- Сделано: шаг 0 (листинг GCS) — объекты для всех трёх покрывающих прогонов и объекты старше
  `2026-07-19` есть, результат положительный; шаг 1 (`bq query` дословно из `§2.7`) — все `7` строк
  совпали с предсказанием точно, модель `ADR-143 §4/§2.2` подтверждена измерением; шаг 1b — единой
  свежей даты `_loaded_at` по пяти суткам нет, 90-суточного пересбора между `2026-07-19` и
  `2026-08-08` не было; шаг 2 — скачано `387` объектов GCS, вычислен `transaction_id` по каждой
  строке архива, сопоставлен с `11` целевыми `transaction_id` подслучая A и отдельно с их
  `demand_id`; сверка с живым списком `69` документов. Вердикт: `0,00` KGS исход (а), `80 730,00`
  KGS (`2` строки) исход (б), `46 891,20` KGS (`9` строк) исход (в); сумма сходится точно с
  `127 621,20`.
- Команды/логи ключевые: `gcloud storage ls -l` (387+ объектов, полный листинг в `_scratch`),
  один `bq query` (текст `§2.7`, `-n 1000`), два read-only `bq query` за `transaction_id`, `gcloud
  storage cp -I` (387 объектов), python-скрипт сопоставления по MD5-хэшу. Артефакт —
  `reference/sales_july_stale_cohort_2026-08-08.md`.
- Отклонения от плана: нет — оба шага (0 и 2) прошли положительным исходом, предсказанным брифом
  как основной путь.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-JULY-STALE-COHORT`: READY → DONE. Разложение `155 456,37` KGS подтверждено
  измерением (не гипотезой): подслучай B (`27 835,20` KGS) самоизлечим ближайшим 90-суточным
  пересбором до `2026-09-29` (пересбора между `2026-07-19` и `2026-08-08` не было); подслучай A
  (`127 621,20` KGS) различён поимённо на три исхода — `0,00` / `80 730,00` / `46 891,20` KGS.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: остаток `155 456,37` KGS июльской выручки различён числами без единого обращения к
    МойСкладу — `82,1 %` (`127 621,20` KGS) разложены на `80 730,00` KGS удалённых позиций живых
    документов и `46 891,20` KGS документов/позиций, исчезнувших из источника без следа; остаток
    `17,9 %` подтверждён самоизлечимым до `2026-09-29` — `reference/sales_july_stale_cohort_2026-08-08.md`.
  - Где мы: у остатка июльской выручки больше нет неизмеренной части — каждый цент назван механизмом;
    решение по форме фикса (явный `DELETE` для `46 891,20` + `80 730,00` KGS) на владельце.
  - Следующий шаг: `SALES-REFRESH-WINDOW` — переоценка варианта 3 (плашка без правки) по факту
    измеренной цены `127 621,20` KGS/месяц (`ADR-143 §10`); параллельно `INFRA-FACTS-LANDING`,
    `HANDOVER-PACKAGE`.
  - Развилки на владельце: остаётся ли `SALES-REFRESH-WINDOW` вариантом 3 (плашка) при известной
    ежемесячной цене, или переходит к варианту 1 (фикс `MERGE`) — решение владельца по факту этого
    замера (`ADR-143 §10`).
  - Счётчик: пары реестра `7/7` (финиш объявлен) · очередь передачи `1/26` закрыта, блокирующих `0/9`.
- Подробности для модели: механизм различения (архив GCS + `transaction_id`-хэш + сверка с живым
  списком) воспроизводим для любого будущего подобного остатка — см. `reference/sales_july_stale_cohort_2026-08-08.md`.
  Метод не различает «документ удалён» от «`moment` уехал за пределы всех наблюдённых окон» — это
  граница метода, а не гэп (нет источника, который бы это разрешил дешевле повторного живого
  замера).
- Новые открытые вопросы: нет.
- Блокеры: нет.
- updated_at: 2026-08-08
- обновил: executor (сессия: SALES-JULY-STALE-COHORT)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
