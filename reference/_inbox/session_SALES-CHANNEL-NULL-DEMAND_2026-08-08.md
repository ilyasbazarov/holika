=== SESSION LOG · 2026-08-08 · SALES-CHANNEL-NULL-DEMAND, остаток ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-CHANNEL-NULL-DEMAND`, остаток — исполнение различителя `ADR-139 §2/§3` (мандат
  класса B выдан поимённо `ADR-139 §1`, владелец, чат 2026-08-08)
- Сделано: 5 живых `GET` к `entity/demand` (из разрешённых шести) по составу `ADR-139 §2`; один
  документ (`cd1f2cac-75d5-11f1-0a80-114600c229cb`, 2026-07-02, контрагент
  `178ba7b4-87c0-11ef-0a80-1568002f4222`) точно опознан совпадением суммы (`61 938,00` KGS) с
  исходной `NULL`-строкой; `salesChannel` у него и у трёх контрольных документов присутствует
  СЕЙЧАС и в списочном, и в одиночном режиме. Гипотеза «list-режим систематически теряет поле»
  опровергнута; подтверждена вторая ветвь — канал назначен документу ПОСЛЕ первой загрузки
  (`2026-07-19`). Артефакт: `reference/sales_channel_null_demand_remainder_2026-08-08.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-CHANNEL-NULL-DEMAND_2026-08-08/run.sh` →
  `run.log` (UTC-якорь + `gcloud auth list` первой и последней командой; проверка утечки токена
  `grep -rF` — чисто).
- Отклонения от плана: нет — исполнено ровно по составу `ADR-139 §2`, шестой `GET` не понадобился.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `SALES-CHANNEL-NULL-DEMAND`, остаток: мандат выдан/исполним → **исполнен**; статус
  строки `SALES-CHANNEL-NULL-DEMAND` в `07_GAPS.md`/`07_STATE §Открытые вопросы` остаётся
  `PARTIAL` — механизм полностью локализован (не потеря list-режимом, назначение канала
  постфактум), но форма операционного фикса НЕ назначена этой сессией (`_METHOD §6`) и требует
  решения владельца/архитектора (задачи по циклу переприменения документов с постфактум-каналом).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: `SALES-CHANNEL-NULL-DEMAND`, остаток закрыт — различитель `ADR-139 §3` измерен
    живыми `GET`, механизм назван (канал назначается документу постфактум, не теряется
    list-режимом); артефакт `reference/sales_channel_null_demand_remainder_2026-08-08.md`.
  - Где мы: 40 июльских строк без канала объяснены полностью на уровне механизма; форма
    операционного фикса (что делать с документами, у которых канал меняется после загрузки) —
    открытое решение владельца/архитектора, не код-баг.
  - Следующий шаг: решение владельца/архитектора о форме фикса для постфактум-назначенных каналов
    (новая задача не заведена этой сессией — имя и мандат определяются владельцем); параллельно —
    сверка разреза продаж по сотруднику с эталоном (`Q-78`/`ADR-128`, названа предыдущей сессией) и
    `SALES-REFRESH-WINDOW` остаются в очереди своим порядком.
  - Развилки на владельце: форма фикса для канала, назначаемого документу ПОСЛЕ первой загрузки
    (дозагрузка по расписанию / ручной триггер / игнорировать как несущественный объём — `0,006 %`
    по `reference/parity_sales_july_diff_2026-08-05.md`).
  - Счётчик: пары реестра `7/7` · карта — · Epic M — (Epic-1 завершён `ADR-140`, фаза передачи
    клиенту).
- Подробности для модели: живой `GET entity/demand/{id}` на `cd1f2cac-75d5-11f1-0a80-114600c229cb`
  (2026-08-08) показывает `salesChannel` заполненным (id `4a07eb32-2e47-11ef-0a80-0f4200606f40`,
  предположительно «ОФИС» по независимому совпадению сумм в
  `reference/parity_sales_july_diff_2026-08-05.md §2`, имя не резолвилось отдельным вызовом — вне
  состава `ADR-139 §2`); при загрузке `2026-07-19` тот же документ дал `NULL`. Вывод: у части
  документов канал назначается ПОСЛЕ первого захвата загрузчиком, наш парсер и кэш каналов
  отработали корректно на том, что получили. Полный ход —
  `reference/sales_channel_null_demand_remainder_2026-08-08.md`.
- Новые открытые вопросы: форма операционного фикса для постфактум-назначенных каналов
  (владелец/архитектор определяют имя задачи).
- Блокеры: нет.
- updated_at: 2026-08-08
- обновил: executor (сессия: SALES-CHANNEL-NULL-DEMAND)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
