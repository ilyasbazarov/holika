=== SESSION LOG · 2026-08-07 · SALES-EMPLOYEE-ATTRIBUTION (второй ход) ===

**База:** `git rev-parse HEAD` на старте этого хода = `2fa9c8e` (первый ход этой же сессии,
`docs: SALES-EMPLOYEE-ATTRIBUTION разбор разреза по сотруднику на данных клиента`), многоходовая
сессия (`ADR-021 §1`) — блок адресуется этому SHA, самодостаточен.
**Вход:** решение владельца в чате 2026-08-07 — вариант 1 развилки
`reference/sales_employee_attribution_2026-08-07.md §5` («Давай через правку загрузчика»), плюс просьба
дать название следующей задаче и вложить в репо всю фактуру для будущей сессии-генератора брифа.

## SESSION_LOG

- Задача: `SALES-EMPLOYEE-ATTRIBUTION` (продолжение) — зафиксировать выбор владельца по развилке §5 и
  завести следующую задачу под правку загрузчика.
- Сделано:
  1. Развилка `reference/sales_employee_attribution_2026-08-07.md §5` закрыта решением владельца:
     вариант 1 (правка загрузчика).
  2. Заведена новая задача **`SALES-DOCUMENT-OWNER-INGEST`** — написать (не деплоить) патч `cf-facts`,
     добавляющий сотрудника-владельца ДОКУМЕНТА (`entity/demand.owner`) в `core.fact_sales_profit`.
     Класс A (текст патча в снапшот, ничего не деплоит, живых `GET` не требует — состав поля уже
     подтверждён офлайн). Деплой — отдельная задача, заводится по факту готовности патча текстом
     (урок `ADR-126 §6`: деплойная работа получает собственную строку сразу, не прячется фразой в
     очереди).
  3. Задача-предшественник `SALES-EMPLOYEE-ATTRIBUTION` (диагностика + проект правки) закрывается
     `DONE`: диагностика исполнена первым ходом этой сессии, проект правки (состав полей/файлов) —
     там же, `§3`. Дальнейшая работа продолжается новой задачей, не тем же именем — по прецеденту
     `SALES-INGEST-PATCH` → `SALES-INGEST-PATCH-DEPLOY` (`ADR-126 §6`).
  4. Вся фактура для будущей сессии-генератора брифа `SALES-DOCUMENT-OWNER-INGEST-GEN` уже в репо
     этой веткой: состав правки файл-за-файлом, подтверждённый формат поля `owner`, риски и открытый
     техвопрос — всё в `reference/sales_employee_attribution_2026-08-07.md §3` (артефакт первого хода,
     не переписывается заново).
- Артефакты: этот session-блок (второй файл буфера); правок кода/SQL/новых `/reference`-файлов
  этот ход не создаёт — вся фактура уже лежит в артефакте первого хода.
- Команды/логи ключевые: нет (чистое архитекторское решение, без облачных вызовов).
- Отклонения от плана: нет.

## STATE_PATCH

- Задача `SALES-EMPLOYEE-ATTRIBUTION`: `OPEN` → `DONE` (архивируется).
- Задача `SALES-DOCUMENT-OWNER-INGEST`: заводится `OPEN`, класс A, параллель да, мандат постоянный.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: разрез по сотруднику разобран на данных клиента и закрыт решением владельца — правка
    загрузчика, задача `SALES-DOCUMENT-OWNER-INGEST` заведена с полной фактурой в `/reference`
    (`reference/sales_employee_attribution_2026-08-07.md`).
  - Где мы: шесть пар реестра из семи сходятся; строка 21 ждёт `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2;
    остаток `Q-78` теперь закрывается через `SALES-DOCUMENT-OWNER-INGEST` (текст патча) плюс её будущий
    деплой, не диагностикой — диагностика завершена.
  - Следующий шаг: сессия-генератор `SALES-DOCUMENT-OWNER-INGEST-GEN` собирает бриф из
    `reference/sales_employee_attribution_2026-08-07.md §3` и этой строки `07_GAPS`;
    `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 (класс B, мандат на запись не выдан); `SALES-INGEST-PATCH-DEPLOY`
    (класс B, мандат дан в чате, требует объявления действия и брифа); `INTRANSIT-SURFACE-LAG` (класс A).
  - Развилки на владельце: мандат класса B на принудительный прогон `cf-inventory`; правка `04_ROADMAP`,
    переставшего описывать очередь.
  - Счётчик: пары реестра **6/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: **Название следующей задачи — `SALES-DOCUMENT-OWNER-INGEST`.** Вся фактура
  для генератора брифа уже в репо, повторного discovery не требуется: (1) подтверждённый формат поля —
  `reference/_scratch_PARITY-CHECK-SALES-RETURNS_2026-08-01/demand_page_0.json`, ключ `owner.meta.href`,
  формат идентичен уже читаемому `agent.meta.href`; (2) состав правки файл-за-файлом (`fetch_demands.py`,
  `bq_ops.py` — новая колонка `document_owner_employee_id`, C1 `ADR-030`, `02_ERP_CONTRACTS.md`) —
  `reference/sales_employee_attribution_2026-08-07.md §3`; (3) `sq_marts_sales_overview.sql` явно ВНЕ
  scope этой задачи — правка марта под новое поле является отдельным решением ПОСЛЕ того, как поле
  появится в `core`, генератор не должен включать её в бриф; (4) открытый техвопрос, не блокирующий
  проектирование, но обязательный к упоминанию в брифе как риск: снимочный ли характер `owner` документа
  при правках задним числом — непроверено, `reference/sales_employee_attribution_2026-08-07.md §3`
  последний абзац; (5) отдельно висит `SALES-EMPLOYEE-ATTRIBUTION-MAY-SUM-GAP` (нестыковка `sum`
  документа vs `Σ revenue_kgs` его позиций за май) — НЕ относится к этой задаче напрямую (она про
  ИСТОЧНИК данных для офлайн-проверки, не про форму патча), но если приёмка `SALES-DOCUMENT-OWNER-INGEST`
  захочет офлайн-сверку на мае — этот гэп встанет на пути первым, генератору стоит не гейтить им бриф,
  а просто назвать риск.
- Новые открытые вопросы: нет (закрывающий этот блок вопрос про GENERATOR не заводится как GAP — это
  штатный следующий шаг метода, `_METHOD.md §9`).
- Блокеры: нет.
- updated_at: 2026-08-07
- обновил: архитектор (сессия: `SALES-EMPLOYEE-ATTRIBUTION`, второй ход)

## NEW_DECISIONS

- ADR-0XX: Развилка разреза по сотруднику закрыта владельцем — вариант 1 (правка загрузчика); задача
  `SALES-DOCUMENT-OWNER-INGEST` заведена с полной фактурой для генерации брифа
  Контекст: `reference/sales_employee_attribution_2026-08-07.md §5` предъявил владельцу три варианта
  (правка загрузчика / документирование как легитимного отступления / гибрид) с рисками каждого и
  рекомендацией архитектора (вариант 1, не срочно). Владелец выбрал вариант 1 в чате 2026-08-07.
  Решение: (1) вариант 1 принят: правка загрузчика `cf-facts`, добавляющая
  `document_owner_employee_id` в `core.fact_sales_profit` по полю `owner` документа `entity/demand`;
  (2) следующая задача называется `SALES-DOCUMENT-OWNER-INGEST` (класс A — текст патча, не деплой;
  деплой получает отдельную строку по факту готовности патча, урок `ADR-126 §6` применяется
  превентивно, не постфактум); (3) `SALES-EMPLOYEE-ATTRIBUTION` закрывается `DONE` — её мандат
  (диагностика + проект) исполнен полностью первым ходом этой же сессии; (4) вся фактура для
  сессии-генератора брифа уже лежит в `reference/sales_employee_attribution_2026-08-07.md §3`,
  повторный discovery не требуется, `SALES-DOCUMENT-OWNER-INGEST-GEN` при старте читает эту секцию
  и строку `07_GAPS.md`; (5) правка `sq_marts_sales_overview.sql` явно исключена из scope новой задачи
  — форма разреза на марте есть решение ПОСЛЕ появления поля в `core`, не часть этой правки.
  Последствия:
  - [сейчас] `07_GAPS.md` — строка `SALES-EMPLOYEE-ATTRIBUTION` переносится в `07_ARCHIVE.md` дословно
    с пометкой «закрыта решением владельца по развилке §5, продолжение — `SALES-DOCUMENT-OWNER-INGEST`»;
    новая строка `SALES-DOCUMENT-OWNER-INGEST` заводится текстом из `STATE_PATCH §Подробности для
    модели` этого блока.
  - [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — строка `SALES-EMPLOYEE-ATTRIBUTION`
    убирается (`ADR-090 §1`); новая строка `SALES-DOCUMENT-OWNER-INGEST` (класс A, да, постоянный,
    описание — как в `STATE_PATCH`) добавляется тем же коммитом (парность `ADR-090 §1` соблюдена).
  - [сейчас] `07_STATE.md §Открытые вопросы` — строка `SALES-EMPLOYEE-ATTRIBUTION` (была `OPEN`)
    заменяется строкой `SALES-DOCUMENT-OWNER-INGEST` (`OPEN`), текст — из `STATE_PATCH`.
  - [задачей SALES-DOCUMENT-OWNER-INGEST-GEN] Сборка брифа задачи `SALES-DOCUMENT-OWNER-INGEST` из
    фактуры, названной выше — следующий шаг метода (`_METHOD.md §9`, механический бриф либо бриф с
    суждением по усмотрению генератора), не заводится этим ADR как отдельная задача реестра.
  Статус: accepted (архитектор — постановка и форма задачи; выбор варианта — владелец, чат 2026-08-07).

## NEW_CONVENTIONS

- нет

=== END SESSION ===
