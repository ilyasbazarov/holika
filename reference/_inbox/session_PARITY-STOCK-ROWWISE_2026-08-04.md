=== SESSION LOG · 2026-08-04 · PARITY-STOCK-ROWWISE ===

## SESSION_LOG
- Задача: `PARITY-STOCK-ROWWISE` — построчная сверка `stock`-компонента строки 21 реестра паритета («Остатки товаров»), установление причины разности `4,00` KGS-штук
- Сделано: мандат класса B получен поимённо от владельца в чате («даю мандат»); живой `GET report/stock/all` (298 строк) + построчная выгрузка `core.fact_inventory` (303 строки, партиция `2026-08-04`) + полный внешний джойн по ключу `product_id`; расхождение агрегата (`5 355,00` KGS-штук в этом замере, не `4,00`) реконсилировано без остатка на три множества (271 совпало / 25 разошлись `+5 291,00` / 6+1 сироты `108,00`/`44,00`); установлена причина: разность есть функция элапса между разовым суточным снимком `core.fact_inventory` (`_loaded_at` партиции `2026-08-04` = `2026-08-03T21:00:04Z`) и непрерывно меняющимся `report/stock/all` — величина растёт с элапсом за счёт обычного оборота, не константа и не фиксированный дефект; артефакт `reference/parity_stock_rowwise_2026-08-04.md`; строка 21 реестра дополнена результатом
- Команды/логи ключевые: `reference/_scratch_PARITY-STOCK-ROWWISE_2026-08-04/` (все сырые тела, bq-выгрузки, джойн, проверка токена)
- Отклонения от плана: буквальная цель брифа («установить причину разности `4,00`») оказалась неисполнима буквально — эта конкретная величина не воспроизводима (момент прошёл, живой источник не хранит историю); установлена причина ЯВЛЕНИЯ, которое порождает любую такую разность (включая `4,00` тогда и `5 355,00` сейчас), с полной построчной реконсиляцией. Найдена и исправлена ловушка метода: переменная окружения токена не переживает переход между отдельными вызовами инструмента — первый прогон `grep`-проверки токена шёл с пустым паттерном (ложное «всё совпало»), перезапущен токен+grep одной командой, результат чистый.

## STATE_PATCH
- Задача `PARITY-STOCK-ROWWISE`: OPEN, мандат не выдан → причина явления установлена построчным замером; статус строки 21 реестра паритета — механизм известен, адъюдикация правила-моста и финальное закрытие статуса остаются архитектору
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком):
  - Прошлый шаг: сессия `PARITY-STOCK-ROWWISE` построчно сверила `stock`-компонент строки 21 и установила причину разности — асинхронность разового суточного снимка `core.fact_inventory` и непрерывного `report/stock/all` (не фиксированный дефект, величина растёт с элапсом с момента снимка), `reference/parity_stock_rowwise_2026-08-04.md`.
  - Где мы: пять пар реестра из семи закрыты; строка 21 («Остатки товаров») получила установленную причину и ждёт архитекторской адъюдикации правила-моста (константа `4,00` невоспроизводима, нужен выбор формы закрытия — датированный снимок обеих сторон одним моментом либо явное правило допуска); строка 22 («Заказы в пути») ждёт восстановления потока данных.
  - Следующий шаг: архитекторская адъюдикация правила-моста строки 21 по этому артефакту (форма закрытия строки — датированный снимок vs правило допуска на элапс); `FACTS-FLOW-RESUME-CONFIRM` (проверка утром `2026-08-05`, класс A) идёт параллельно, независимо.
  - Развилки на владельце: нет новых (форма закрытия строки 21 — решение архитектора по уже установленному факту, не владельца).
  - Счётчик: пары реестра 5/7 сходятся (без изменений этой сессией — причина установлена, статус строки не закрыт) · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: `core.fact_inventory` пополняется ОДИН раз в сутки одним батчем (наблюдено этой сессией: все 303 строки партиции `2026-08-04` несут одинаковый `_loaded_at = 2026-08-03 21:00:04 UTC`, то есть снимок берётся около бишкекской полуночи, не непрерывно). Это делает ЛЮБОЙ более поздний в течение того же дня живой `GET report/stock/all` заведомо расходящимся с этой партицией на сумму реального оборота с момента снимка — расхождение растёт с элапсом (замер `PARITY-STOCK-INTRANSIT` в 05:29Z элапс ≈8ч30м дал `4,00` KGS-штук; этот замер в 09:06Z элапс ≈12ч06м дал `5 355,00`). Для будущей адъюдикации правила-моста строки 21 это означает: «сходимость 0,00» на этой паре структурно невозможна при живом запросе позже момента снимка; вариант закрытия — либо переопределить эталон как «снимок в тот же момент, что и `cf-inventory`» (нужен способ синхронизировать живой запрос с расписанием CF, не решено этой сессией), либо принять именованный допуск, растущий с элапсом. Природа 7 строк-сирот (1 у источника `44,00`, 6 у нас `108,00`, вместе `2,8%` расхождения) не установлена — не отличено, дрейф ли это (товар ушёл в ноль/архив между снимком и запросом) или иной механизм; полные UUID и величины — `reference/_scratch_PARITY-STOCK-ROWWISE_2026-08-04/step6_join_result.json`.
- Новые открытые вопросы: `PARITY-STOCK-ROWWISE-BRIDGE` — адъюдикация правила-моста строки 21 реестра паритета: как формулировать эталонный момент сравнения `stock`, если единственный наш источник — разовый суточный снимок, а эталон МойСклада — непрерывный текущий остаток; кандидаты — (a) синхронизировать живой контрольный запрос с моментом следующего запуска `cf-inventory`, (b) принять именованный допуск как функцию элапса, (c) переопределить объект паритета на другую величину МойСклада с историей (если такая существует). Гейт: архитектор, без дополнительного мандата (read+`/reference`, класс A).
- Блокеры: нет
- updated_at: 2026-08-04
- обновил: исполнитель (сессия: PARITY-STOCK-ROWWISE)

## NEW_DECISIONS
- нет

## NEW_CONVENTIONS
- нет

=== END SESSION ===
