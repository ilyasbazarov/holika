=== SESSION LOG · 2026-08-11 · SALES-REFRESH-WINDOW-MANDATE-ADJ ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `SALES-REFRESH-WINDOW-MANDATE-ADJ` — ответ на запрос исполнителя о мандате класса B:
  проверка готовности по существу, четыре решения, текст мандата (архитектор, класс A, без брифа —
  `ADR-086 §1`; строка задачи заводится этим блоком, `ADR-086 §3`). База `c4e291b`.
- Сделано:
  - **Готовность проверена самим архитектором, не по отчёту** (`ADR-146 §4`): импорт
    `from datetime import date, timedelta` (`bq_ops.py:25`) на месте — `py_compile` его отсутствия
    не поймал бы; вызов предохранителя безусловен и стоит до построения текста `MERGE`
    (`:460`, `:668`); граница окна в обеих ветках удаления одинакова (`:379`, `:647`).
  - **Решение 1 — живая проверка отказа предохранителя НЕ проводится.** Оба предложенных варианта
    требуют создать ровно то состояние, от которого защищаемся. Вместо них предикат проверен
    read-only на живых данных обеих staging: опасный сценарий (часовой staging + `window_days=90`)
    даёт ОТКАЗ, оба штатных — проход. В приёмку внесено положительное подтверждение из журнала
    первого прогона.
  - **Решение 2 — предусловие `ADR-145 §5` снято фактом, не предохранителем.** Замерена глубина
    time travel датасета `core`: `maxTimeTravelHours = 168`; плюс в мандат внесён обязательный
    снимок таблицы до деплоя (`CREATE SNAPSHOT TABLE … CLONE`). Откат становится двухсоставным и
    полным.
  - **Решение 3 — допуск трое суток.** Вероятность ложного отказа измерена (замер `§C`, 180 суток
    ядра): `12` из `181` суток без продаж (`6,63 %`), окон из трёх подряд нулевых суток — `0`.
    Строгое сравнение дало бы ложный отказ примерно в одном прогоне из пятнадцати.
  - **Решение 4 — риск `R1` мандат не гейтит**, пересверка входит в приёмку деплоя, новая строка
    списка закрытия не заводится.
  - **Найдена третья дыра предохранителя, в запросе отсутствующая:** он проверяет только НИЖНИЙ
    край окна. Устаревший верхний край staging так же смертелен — ветка удаления снесёт всё, что
    попало в окно и отсутствует в staging. Состояние не гипотетическое: staging периметра сейчас
    несёт `max_date = 2026-08-03` при `2026-08-11`, отставание восемь суток.
  - Артефакт `reference/sales_refresh_window_mandate_adj_2026-08-11.md` — четыре решения, три
    обязательные правки, готовый текст мандата для решения владельца.
- Команды/логи ключевые: один скрипт на шаг (`ADR-077 §6`)
  `reference/_scratch_SALES-REFRESH-WINDOW-MANDATE-ADJ_2026-08-11/step1_rollback_and_guard_facts.sh`
  → `step1_run.log`; `bq show` датасета плюс три запроса на чтение; `RC=0`; якоря
  `2026-08-11T05:32:53Z` и `2026-08-11T05:33:04Z`, принципал один и тот же. Ни `MERGE`, ни
  `promote`, ни деплоя, ни вызовов к МойСкладу. `bash tools/hooks/selftest.sh` →
  `пройдено 36, провалено 0`.
- Отклонения от плана: нет.

## STATE_PATCH  (→ применить к 07_STATE.md)

- Задача `SALES-REFRESH-WINDOW-MANDATE-ADJ`: заводится и закрывается **DONE** тем же проходом
  сборки (прецедент `FINISH-QUEUE-ADJ`, `DQ-GATE-DEPLOY-ADJ`, `SALES-PARITY-VERDICT-ADJ`); строка
  мандата по тому же прецеденту не заводится.
- Строка `SALES-REFRESH-WINDOW`: предусловие мандата (`ADR-145 §4/§5`) **ВЫПОЛНЕНО** — предохранитель
  внесён исполнителем (`d62c587`), отсутствие отката данных снято замером глубины time travel
  (`168` часов) плюс обязательным снимком таблицы в тексте мандата. Остаток до деплоя: три правки
  предохранителя (класс A, `reference/sales_refresh_window_mandate_adj_2026-08-11.md §3`) и решение
  владельца о выдаче мандата (текст готов, `§4` того же артефакта).
- Строка `SALES-PERIMETER-PARITY-RECHECK` и строка `Q-78` — подтверждены закрытыми, переоткрытия
  нет (сессия-исполнитель 2026-08-11 проверила первоисточник, изменений не вносила, session-блока
  не создавала).
- Счётчик списка закрытия: **`8` из `9`** без изменений — строка `1` не исполнена, пока не сделаны
  три правки и не выдан мандат.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: запрос исполнителя о мандате на деплой ветки удаления рассмотрен — четыре решения
    приняты, три обязательные правки предохранителя названы, текст мандата подготовлен
    (`reference/sales_refresh_window_mandate_adj_2026-08-11.md`).
  - Где мы: предусловие мандата закрыто; предохранитель существует, но проверяет только нижний край
    окна и берёт даты из разных часов — до выдачи мандата это чинится.
  - Следующий шаг: три правки предохранителя (класс A, короткая сессия), параллельно — подготовка
    обоих патчей `cf-dq` (класс A, строка `2` списка).
  - Развилки на владельце: выдать ли мандат класса B текстом `§4` артефакта (после трёх правок);
    мандат на выезд `cf-dq`.
  - Счётчик: **строк списка закрытия осталось `8` из `9`**.
- Подробности для модели: сессия, вносящая три правки предохранителя, обязана знать — (i) допуск
  `3` суток выведен из замера (`6,63 %` пустых суток, ноль окон из трёх подряд), это не назначенное
  число; (ii) обе даты (минимум staging и начало окна) обязаны вычисляться в ОДНОМ запросе
  BigQuery, иначе предохранитель и `MERGE` берут время из разных часов; (iii) проверять надо ОБА
  края окна — верхний не менее опасен, живой пример отставания staging периметра на восемь суток
  приведён в артефакте.
- Новые открытые вопросы: **нет и не заводятся** (фиксация объёма, `ADR` блока `DQ-GATE-FORM-ADJ`,
  `§5`). Три правки предохранителя — не новая строка, а остаток строки `1` списка закрытия.
- Блокеры: нет.
- updated_at: 2026-08-11
- обновил: архитектор (сессия: `SALES-REFRESH-WINDOW-MANDATE-ADJ`)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)

- ADR-0XX: Готовность `SALES-REFRESH-WINDOW` к деплою подтверждена с тремя обязательными правками
  предохранителя; живая проверка отказа отклонена; предусловие отката снято замером и снимком;
  допуск предохранителя выведен из замера
  Контекст: исполнительская сессия внесла предохранитель `ADR-145 §4/§5` и запросила рассмотрение
  мандата, назвав четыре нерешённых вопроса (`reference/sales_refresh_window_mandate_request_2026-08-11.md §2`).
  Решение: (1) живая проверка отказа предохранителя НЕ проводится ни до, ни после деплоя — она
  требует создать состояние, от которого защищаемся; вместо неё предикат проверен read-only на
  живых данных обеих staging (опасный сценарий даёт отказ, оба штатных — проход), а кодовая
  половина доказана чтением места вызова; в приёмку внесено положительное подтверждение из журнала;
  (2) предусловие `ADR-145 §5` снято не предохранителем, а фактом отката: `maxTimeTravelHours = 168`
  для датасета `core` плюс обязательный снимок таблицы до деплоя, внесённый в текст мандата;
  (3) метод предохранителя принимается с допуском `3` суток — при строгом сравнении ложный отказ
  ожидается примерно в одном прогоне из пятнадцати (`12` из `181` суток без продаж), при допуске
  требует трёх подряд пустых суток, которых за 180 суток истории не было ни разу; цена допуска —
  три суток глубины против класса ошибки в `83` суток; (4) риск `R1` мандат не гейтит, пересверка
  входит в приёмку; (5) до выдачи мандата обязательны три правки: допуск константой в `config.py`,
  вычисление обеих дат в одном запросе BigQuery вместо питоновских часов, проверка ВЕРХНЕГО края
  окна (найдено этой сессией, в запросе отсутствовало; живой пример — staging периметра отстаёт на
  восемь суток).
  Последствия: [задачей `<правки предохранителя, класс A>`] `reference/code/cf-facts/bq_ops.py`
  (условие отказа, источник дат, второй край) и `reference/code/cf-facts/config.py` (константа
  допуска с обоснованием). [задачей `<выдача мандата, владелец>`] текст мандата готов —
  `reference/sales_refresh_window_mandate_adj_2026-08-11.md §4`. [задачей `<деплой>`] снимок
  таблицы шагом 3 и расширенное окно первого прогона шагом 5 — обязательные пункты, не
  рекомендации. [без правок] `07_GAPS.md` строка `SALES-REFRESH-WINDOW` сохраняется до исполнения
  деплоя; новых строк реестра не заводится.
  Статус: accepted (архитектор). Выдача мандата класса B — решение владельца, здесь не принимается.

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
