# FILE: session_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2_2026-08-18.md

=== SESSION LOG · 2026-08-18 · SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2 ===

## SESSION_LOG

- Задача: `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY` (второй заход) — выезд `cf-facts` с
  guard-fix ф3/ф4 (`bq_ops.py` `a5c6a36`, `main.py` `56d40c1`), мандат класса B, редакция 2
  (`reference/guard_fix_deploy_mandate_2026-08-18_rev2.md`), база `543b6c1`.
- Сделано: все шесть предусловий (П2, П6, П3, П4, П7, П5) закрыты фактами по порядку из
  текста запуска; ветка `deploy/cf-facts-2026-08-18-guard-fix-f3-v2` заведена от `543b6c1`
  (не `master`); патч перенесён дословно из замороженного снапшота (0 строк правки); деплой
  исполнен с подтверждения владельца, новая ревизия `cf-facts-00019-tip`; read-back подтвердил
  побайтовое совпадение архива с веткой (`ALL_MATCH=1`, мусора `0`); трафик переведён на новую
  ревизию с ОТДЕЛЬНОГО повторного подтверждения владельца (первая попытка была заблокирована
  классификатором разрешений инструмента, не относится к мандату); `reference/code/cf-facts/MANIFEST.md`
  дополнен записью «ревизия ↔ коммит». Полный разбор — `reference/sales_refresh_window_guard_fix_deploy_2_2026-08-18.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2_2026-08-18/step1_run.log`
  … `step11_run.log` (11 скриптов, по одному на шаг, `date -u`+`gcloud auth list` первой и
  последней командой каждого).
- Отклонения от плана: ветка код-репо **НЕ слита в `master`** — мандат редакции 1 §4 п.6
  предписывает слияние после успешного read-back, но это прямо конфликтует с явным запретом
  `07_STATE.md` на слияние лежащего в основе коммита `543b6c1` в `master` до ступени 3
  (`2026-08-23`); ветка этой сессии — прямой потомок `543b6c1` и несёт его целиком. Исполнитель
  не адъюдицирует мандат (`_METHOD §2`) — конфликт вынесен владельцу/архитектору, ветка
  остаётся неслитой, `master` не тронут (`sales_refresh_window_guard_fix_deploy_2_2026-08-18.md §6`).
  Ручную проверку `promote` не делал (прямой запрет мандата). Код не правил ни строкой.

## STATE_PATCH

- Задача `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY`: ВЫДАН/НЕ ИСПОЛНЕН (redaction 1, СТОП на П3) →
  **ИСПОЛНЕН, read-back подтверждён, слияние в `master` НЕ решено (вынесено владельцу)**.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: guard-fix ф3/ф4 задеплоен вторым заходом от исправленной базы `543b6c1`
    (ревизия `cf-facts-00019-tip`), все шесть предусловий закрыты, read-back подтверждён
    побайтово, трафик переведён на новую ревизию.
  - Где мы: деплой закрыт; критический путь к сроку `2026-09-29` теперь в двух звеньях —
    слияние ветки в `master` (решение владельца/архитектора) и ступень 3 (`2026-08-23`); до
    ступени 3 пять суток.
  - Следующий шаг: решение владельца/архитектора по слиянию ветки
    `deploy/cf-facts-2026-08-18-guard-fix-f3-v2`; параллельно, класса A: правка порядка
    `CHECKS` под `DQ-FRESHNESS-WIRE`; сопоставление множеств `transaction_id` под
    `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`.
  - Развилки на владельце: слияние ветки `deploy/cf-facts-2026-08-18-guard-fix-f3-v2` в
    `master` до ступени 3 — новое исключение из запрета `07_STATE.md`, или запрет транзитивен
    (новое, `sales_refresh_window_guard_fix_deploy_2_2026-08-18.md §6`); плюс прежние — шесть
    невыданных мандатов класса B, два действия в интерфейсе Looker Studio, один ХОЛД формы
    показа возвратов.
  - Счётчик: строк списка закрытия остаётся `7` из `9` (`ADR-156 §6`, деплой не закрывает
    ЦЕЛУЮ строку 1 — ступень 3 и слияние ещё открыты); доска `reference/close_list_board_2026-08-18.md`
    строка `1к` протухла (деплой исполнен, доска этого не отражает — правка вне «Пишет» этой
    сессии); срок `2026-09-29`, шесть недельных прогонов.
- Подробности для модели: Полный числовой разбор, read-back, sha256-таблицы — `reference/sales_refresh_window_guard_fix_deploy_2_2026-08-18.md`
  и `reference/code/cf-facts/MANIFEST.md` (новая секция «Деплой `cf-facts` — guard-fix ф3/ф4,
  второй заход»). Ключевое для следующей сессии: (1) обслуживающая ревизия `cf-facts` теперь
  `cf-facts-00019-tip`, НЕ `cf-facts-00017-jon` — любой будущий деплой берёт эту ревизию как
  текущую базу-прокси; (2) ветка `deploy/cf-facts-2026-08-18-guard-fix-f3-v2` существует на
  `origin` (код-репо `holika-prod`), не слита — не путать с провенансом, брать как факт «ветка
  есть, решение по слиянию не принято»; (3) `gcloud run services update-traffic` (и, вероятно,
  другие прямые мутирующие вызовы прода) может быть заблокирован классификатором разрешений
  самого инструмента Claude Code НЕЗАВИСИМО от того, что мандат класса B выдан и владелец уже
  подтвердил действие в чате — это техническое ограничение среды, не сигнал о нарушении
  предусловий; при блокировке нужно явно перепросить подтверждение именно на заблокированное
  действие, не считать его частью уже полученного согласия и не делать слепой retry без
  read-only проверки состояния (см. `NEW_CONVENTIONS` ниже).
- Новые открытые вопросы: слияние ветки `deploy/cf-facts-2026-08-18-guard-fix-f3-v2` в `master`
  — новое исключение из запрета `07_STATE.md` (слияние `543b6c1`-линии до ступени `3`,
  `2026-08-23`), или запрет распространяется транзитивно на любую ветку-потомка `543b6c1`;
  разбор и оба чтения — `reference/sales_refresh_window_guard_fix_deploy_2_2026-08-18.md §6`;
  снимается решением владельца/архитектора, не измерением; ID присваивает сборка.
- Блокеры: нет.
- updated_at: 2026-08-18
- обновил: исполнитель (сессия: `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2`)

## NEW_DECISIONS

нет — исполнитель не адъюдицирует мандат класса B и не выносит решение по конфликту слияния
(`_METHOD §2`); конфликт зафиксирован как открытый вопрос выше, решение остаётся владельцу/архитектору.

## NEW_CONVENTIONS

- proposed: `05_CONVENTIONS §Процедура деплоя Cloud Function` дополнить пунктом о том, что
  прямые мутирующие вызовы прода (`gcloud run services update-traffic` и класс аналогичных)
  могут быть отклонены классификатором разрешений среды исполнения Claude Code независимо от
  выданного мандата класса B и уже полученного в чате подтверждения владельца на действие в
  целом. Отклонение классификатора — не факт о состоянии предусловий или прода; сессия обязана
  (i) read-only проверкой убедиться, что заблокированное действие НЕ выполнилось частично,
  (ii) назвать это конкретное действие владельцу отдельным сообщением ещё раз и получить явное
  повторное подтверждение ИМЕННО на него, а не считать общее согласие на «деплой» достаточным
  для обхода блокировки. Живой прецедент — эта сессия, `step11` (`переключение трафика на
  cf-facts-00019-tip`), первая попытка отклонена классификатором, вторая исполнена после
  отдельного «Разрешаю, переключай трафик.».

=== END SESSION ===
