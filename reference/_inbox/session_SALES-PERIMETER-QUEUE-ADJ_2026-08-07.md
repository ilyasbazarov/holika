=== SESSION LOG · 2026-08-07 · SALES-PERIMETER-QUEUE-ADJ ===

> **Роль:** архитектор (`_ARCHITECT.md`, `ADR-083 §8`, `ADR-091 §8`). Работа в основном дереве,
> ничего не исполнялось, ни одного облачного вызова, ни одного коммита этой сессией.
> **База:** `git rev-parse HEAD` = `b754e30fad7089bb27fefcb46bbd4aa19e8a091f`.
> **Указание сборщику:** блок несёт ТРИ ADR-кандидата (`accepted`). Номера присваивает сборка по
> порядку; внутри блока они названы «первый / второй / третий ADR этого блока», сквозных ссылок по
> номеру между ними нет намеренно — подстановка номеров не требует правки перекрёстных ссылок.

## SESSION_LOG

- Задача: `SALES-PERIMETER-QUEUE-ADJ` — адъюдикация очереди после деплоя периметра продаж:
  заведение четырёх задач, устранение дефекта парности пятой, выдача двух мандатов класса B,
  приземление эскалации по расхождению на дашборде.
- Сделано:
  - Установлено фактом, что счётчик реестра паритета — `7/7`, а не `6/7`: строка 21 закрыта
    `ADR-129` тем же проходом сборки 2026-08-07, расхождение стенд-апа уже названо в
    `07_STATE.md:1041`. Задач, гейтящих седьмую пару, не существует.
  - **Находка, ранее в репо не зафиксированная:** режимы `perimeter` и `perimeter_promote`
    (`reference/code/cf-facts/main.py:103-106`) не вызываются НИ ОДНИМ расписанием. Сплошной поиск
    по `reference/code/cf-facts/workflow_hourly.yaml`, `workflow_weekly.yaml` и
    `deploy_and_workflow.sh` даёт 0 совпадений по подстроке `perimeter`; инвентарь Cloud Scheduler
    (`11_INFRA_FACTS.md:27`, пять джобов) их не называет. Следствие: прогон 2026-08-07 был разовым
    ручным, новые розничные и комиссионные продажи в ядро больше не поедут.
  - **Вторая находка:** у всех 5467 строк периметра `sales_channel_name` жёстко `NULL` —
    `CAST(NULL AS STRING) AS sales_channel_name` в `_build_perimeter_merge_sql`
    (`reference/code/cf-facts/bq_ops.py:483-486`). На витрине они станут «Не указан» и смешаются с
    40 строками опта без канала (`ADR-126 §7`).
  - Дефект парности `ADR-090 §1`: бриф `briefs/SALES-PERIMETER-PARITY-RECHECK.md` закоммичен
    (`82137c3`), а строк задачи нет ни в `07_GAPS.md`, ни в таблице мандата. Бриф это сам и
    предсказывал («заводится проходом сборки одновременно с этим брифом») — не заведено.
  - Разобран стоп исполнителя на `SALES-PERIMETER-LANDING-CHECK`: исполнитель прав по букве
    `ADR-086 §3`, определение задачи из чата не берётся. Лечение — не исключение из правила, а
    заведение строки, что и делает этот блок (прецедент формы — `07_STATE.md:221`, «правило
    удовлетворимо ровно тем, что сессия заводит строку своим же блоком»).
- Команды/логи ключевые: только чтение с диска и `grep`; облачных вызовов нет.
- Отклонения от плана: нет.

## STATE_PATCH

- Задача `SALES-PERIMETER-LANDING-CHECK`: нет строки → `READY` (новая строка, заводится в
  `07_GAPS.md` + индекс + таблица мандата одним коммитом).
- Задача `SALES-PERIMETER-CADENCE`: нет строки → `OPEN` (новая строка, там же).
- Задача `SALES-PERIMETER-CHANNEL-DECIDE`: нет строки → `READY` (новая строка, там же).
- Задача `SALES-DOCUMENT-OWNER-DEPLOY`: нет строки → `OPEN` (новая строка, там же).
- Задача `SALES-PERIMETER-PARITY-RECHECK`: бриф без строки → `READY`, мандат класса B выдан
  (устранение дефекта парности `ADR-090 §1`).

- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - **Прошлый шаг:** очередь после деплоя периметра продаж адъюдицирована — заведены
    `SALES-PERIMETER-LANDING-CHECK`, `SALES-PERIMETER-CADENCE`, `SALES-PERIMETER-CHANNEL-DECIDE`,
    `SALES-DOCUMENT-OWNER-DEPLOY`, восстановлена парность `SALES-PERIMETER-PARITY-RECHECK`;
    выданы два мандата класса B.
  - **Где мы:** реестр паритета сошёлся полностью (`7/7`, строка 21 закрыта `ADR-129`); открыт не
    счётчик, а клиентская картина — периметр задеплоен и разово промоутнут, но ни одним расписанием
    не вызывается, канал у его строк пуст, сотрудник документа в ядро не попадает.
  - **Следующий шаг:** `SALES-PERIMETER-LANDING-CHECK` (класс A, без брифа) и
    `SALES-DOCUMENT-OWNER-INGEST` (класс A, бриф готов) параллельно, затем
    `SALES-PERIMETER-CADENCE` и `SALES-PERIMETER-CHANNEL-DECIDE` (класс A), затем
    `SALES-PERIMETER-PARITY-RECHECK` (класс B, мандат выдан) и `SALES-DOCUMENT-OWNER-DEPLOY`
    (класс B, мандат по факту готовности патча).
  - **Развилки на владельце:** форма каденции периметра, если замер стоимости покажет часовую
    неприемлемой; метка канала для розницы и комиссии, если поле `salesChannel` в источнике
    отсутствует; выдача мандата на деплой патча сотрудника документа по факту его готовности.
  - **Счётчик:** пары реестра 7/7 сходятся · измерено 7/7 · Epic M 6/7 фаз.

- Подробности для модели (append):

**Очередь после деплоя периметра заведена, две находки о самом деплое (`SALES-PERIMETER-QUEUE-ADJ`,
2026-08-07, архитектор).** Что обязана знать следующая сессия. (i) **Счётчик реестра — `7/7`**;
любой стенд-ап или бриф, называющий `6/7`, устарел — строка 21 закрыта `ADR-129` тем же проходом
сборки, расхождение уже названо (`07_STATE.md:1041`). Задач под седьмую пару нет; открыта не
сходимость реестра, а клиентская картина (`ADR-125 §1`). (ii) **Режимы `perimeter`/`perimeter_promote`
не вызываются ничем** — 0 совпадений подстроки `perimeter` в `workflow_hourly.yaml`,
`workflow_weekly.yaml`, `deploy_and_workflow.sh`; инвентарь Cloud Scheduler (`11_INFRA_FACTS.md:27`)
их не называет. Прогон 2026-08-07 (`MANIFEST.md`, шаг 9) был разовым ручным: `+5 467` строк,
`+12 702 994,13` KGS за 90 суток. Без каденции число снова разойдётся, и разойдётся тихо. Оговорка
метода: инвентарь шедулеров датирован 2026-07-25, снапшот workflow — 2026-08-05 (read-back
`DQ-GATE-SCOPE-SPLIT-DEPLOY`); отсутствие в снапшоте не есть факт о живом объекте
(`ADR-021 §2`) — задача `SALES-PERIMETER-CADENCE` обязана переснять оба живыми read-only вызовами
ПЕРВЫМ шагом и закрыться фактом, если каденция уже подключена. (iii) **У строк периметра нет
канала** — `CAST(NULL AS STRING) AS sales_channel_name` (`bq_ops.py:483-486`); на витрине они
попадут в «Не указан» и смешаются с 40 строками опта без канала (`ADR-126 §7`), то есть корзина
«Не указан» перестаёт быть однородной. (iv) **Разрез по сотруднику от этого деплоя не сдвинется по
построению** — разрыв есть переатрибуция, а не потеря (`ADR-126 §3`, `ADR-127`); единственный сдвиг,
которого стоит ожидать, — новые строки периметра получат менеджеров СВОИХ контрагентов, что
величину по конкретному менеджеру подвинет, а механизм расхождения не тронет. (v) **Ловушка
досева для будущего патча сотрудника документа:** новая колонка заполнится у СТАРЫХ строк только
если она внесена в ветку `WHEN MATCHED THEN UPDATE SET` (`bq_ops.py:240-256`), иначе `MERGE`
обновит 15 перечисленных колонок и новую оставит `NULL` навсегда; глубина досева задаётся окном
режима (`7` суток hourly, `90` weekly), июль внутри `90` до `2026-09-29`
(`ADR-125 §5`). (vi) **Стоп исполнителя на задаче из разговора отработал верно** (`ADR-086 §3`):
определение задачи не берётся из чата, лечение — заведение строки, а не исключение из правила;
четвёртый зафиксированный инстанс класса (`07_STATE.md:221`), новой строки реестра под него не
заводится (`ADR-051 §2`, `ADR-107 §4`).

- Новые открытые вопросы:
  - `SALES-PERIMETER-LANDING-CHECK` (задача, READY)
  - `SALES-PERIMETER-CADENCE` (задача, OPEN)
  - `SALES-PERIMETER-CHANNEL-DECIDE` (задача, READY)
  - `SALES-DOCUMENT-OWNER-DEPLOY` (задача, OPEN)
  - `SALES-PERIMETER-PARITY-RECHECK` (задача, READY — восстановление парности, не новая работа)
- Блокеры: нет.
- updated_at: 2026-08-07
- обновил: архитектор (сессия: `SALES-PERIMETER-QUEUE-ADJ`)

---

### Патч 1 — `07_GAPS.md`, append пять строк в конец таблицы

*Операция: append. Формат — четыре ячейки (`ID | Вопрос | Тип | Как закрывается`), `ADR-106 §1`.*

| ID | Вопрос | Тип | Как закрывается |
|---|---|---|---|
| SALES-PERIMETER-LANDING-CHECK | (`SALES-PERIMETER-QUEUE-ADJ`, 2026-08-07) После деплоя `cf-facts-00008-zen` и разового промоута периметра (`+5 467` строк / `+12 702 994,13` KGS за 90 суток) не установлено, доехал ли периметр до июля-2026, до витрины `marts.sales_overview` и до клиентской страницы; владелец наблюдает прежнюю картину расхождения | задача | **READY.** Класс A, read-only BigQuery, брифа не требует (`ADR-086 §1`): определение и приёмка записаны здесь дословно и цитируются в тексте запуска без пересказа. **Приёмка — артефакт `reference/sales_perimeter_landing_check_<date>.md`, несущий шесть измеренных величин с SQL и сырым выводом по каждой:** (1) `MAX(_mart_refreshed_at)` в `marts.sales_overview` и `MAX(_loaded_at)` в `core.fact_sales_profit`; (2) `COUNT(*)` и `SUM(revenue_kgs)` за `2026-07-01…2026-07-31` в `core.fact_sales_profit`; (3) те же две величины за тот же период в `marts.sales_overview`; (4) помесячный разрез `COUNT(*)`/`SUM(revenue_kgs)` в `stg_msklad.fact_sales_perimeter_staging` по `DATE(PARSE_TIMESTAMP('%Y-%m-%d %H:%M:%E3S', transaction_date_raw), 'Asia/Bishkek')` с разбивкой по `source_doc_type`; (5) разрез июля по `sales_channel_name` в `marts.sales_overview` — сколько суммы приходится на «Не указан»; (6) разрез июля по `manager_name` в `marts.sales_overview` — величина корзины сотрудника, названного клиентом. **Вердикт артефакта — ровно один из двух:** периметр доехал до июля (назвать величину прироста и назвать, какие из перечисленных гипотез он закрывает) либо не доехал (назвать, на каком слое обрывается: ядро / витрина / страница). **Оговорки, обязательные к соблюдению:** `bq query --format=json\|prettyjson` без явного `-n`/`--max_rows` тихо режет результат до 100 строк (`PARITY-STOCK-INTRANSIT`, 2026-08-04) — на любом разрезе задавать лимит заведомо больше ожидаемого числа строк либо сверять с независимым `COUNT(*)`; сравнение ядра и витрины корректно только при известном `_mart_refreshed_at` (витрина пересобирается `every 2 hours`, `11_INFRA_FACTS.md:114`), поэтому величина (1) снимается ПЕРВОЙ и записывается в артефакт как окно наблюдения. Живых вызовов к МойСкладу нет, в `core.*`/`marts.*` не пишет. Пишет: `reference/sales_perimeter_landing_check_<date>.md`, `reference/_scratch_SALES-PERIMETER-LANDING-CHECK_<date>/`. |
| SALES-PERIMETER-CADENCE | (`SALES-PERIMETER-QUEUE-ADJ`, 2026-08-07) Режимы `perimeter`/`perimeter_promote` (`reference/code/cf-facts/main.py:103-106`) не вызываются ни одним расписанием: 0 совпадений подстроки `perimeter` в `workflow_hourly.yaml`, `workflow_weekly.yaml`, `deploy_and_workflow.sh`; инвентарь Cloud Scheduler (`11_INFRA_FACTS.md:27`, пять джобов) их не называет. Прогон 2026-08-07 был разовым ручным — новые розничные и комиссионные продажи в ядро больше не поедут | задача | **OPEN.** Класс A (подготовка патча снапшота), деплой Cloud Workflows — класс B, мандат выдан вторым ADR блока `SALES-PERIMETER-QUEUE-ADJ`. **Шаг 1 обязателен и read-only:** переснять живыми вызовами `gcloud scheduler jobs list` и `gcloud workflows describe` обоих pipeline, сверить с репо — снапшот датирован 2026-08-05, инвентарь шедулеров 2026-07-25, отсутствие в них фактом о живом объекте не является (`ADR-021 §2`); если каденция уже подключена, задача закрывается фактом и шаги 2-3 не исполняются. **Шаг 2 — позиция шагов, назначена архитектурно, не выбирается сессией:** `step_perimeter` (`mode: perimeter`) ставится в `workflow_weekly.yaml` ПОСЛЕ `step_facts` и ДО `step_dq` — он пишет только в staging и под гейт продаж не подпадает по тому же основанию, по которому туда перенесены закупки и возвраты (`DQ-GATE-SCOPE-SPLIT`, `ADR-122`); `step_perimeter_promote` (`mode: perimeter_promote`) ставится ПОСЛЕ `step_promote` — он пишет в `core.fact_sales_profit`, то есть в ту самую таблицу, которую гейт защищает, и выносить его из-под гейта запрещено. **Основание недельной, а не часовой каденции:** прогон `perimeter` занял `368,9с` при окне 90 суток (`MANIFEST.md`, шаг 9а), а `PERIMETER_WINDOW_DAYS = WEEKLY_WINDOW_DAYS = 90` задано конструкцией (`config.py:44`); часовой цикл этого не вынесет, и отдельная каденция без основания не заводится. **Названная цена решения:** розница и комиссия будут отставать от опта до семи суток на клиентской поверхности — это осознанный размен, а не дефект; пересмотр в сторону часовой каденции требует замера стоимости по образцу `INTRANSIT-CADENCE-COST-PROBE` и решения владельца. **Шаг 3 — деплой** по процедуре `05_CONVENTIONS` Часть II вариант Б, с оговоркой формы для Workflows: read-back через `--format=json` и программное извлечение `sourceContents`, иначе `gcloud` добавляет свой перевод строки и даёт ложное расхождение в один байт (`DQ-GATE-SCOPE-SPLIT-DEPLOY`, 2026-08-05). Пишет: `reference/code/cf-facts/workflow_weekly.yaml`, `reference/sales_perimeter_cadence_<date>.md`, `reference/_scratch_SALES-PERIMETER-CADENCE_<date>/`. |
| SALES-PERIMETER-CHANNEL-DECIDE | (`SALES-PERIMETER-QUEUE-ADJ`, 2026-08-07) У всех 5467 строк периметра `sales_channel_name` жёстко `NULL` (`CAST(NULL AS STRING)`, `reference/code/cf-facts/bq_ops.py:483-486`); на витрине они станут «Не указан» и смешаются с 40 строками опта без канала (`ADR-126 §7`), после чего корзина «Не указан» перестаёт быть однородной и разрез по каналу перестаёт быть полным | задача | **READY.** Класс A (разбор и текст правки), деплой — класс B, мандат НЕ выдан, заводится по факту готовности правки. **Шаг 1 — различитель офлайн, живого `GET` НЕ требует:** несут ли `entity/retaildemand` и `entity/commissionreportin` поле `salesChannel` — проверяется по уже лежащему в репо дампу `retaildemand_page_0.json` (упомянут в `07_STATE.md:532`) и по ключам ответа, зафиксированным `reference/sales_perimeter_confirm_2026-08-02.md §6`; если дампа по `commissionreportin` в репо нет, это `CONTEXT GAP`, закрываемый одним живым `GET` (класс B) — назвать, не домысливать. **Шаг 2 — форма правки по исходу шага 1:** поле есть в источнике → правка `fetch_perimeter.py` по образцу уже читаемых `sales_channel_id`/`sales_channel_name` в `fetch_demands.py`; поля нет → константная метка типа документа вместо `NULL`, потому что «Не указан» уже занят другим смыслом и слияние двух разных причин в одну корзину есть тихая потеря различения. **Рекомендация архитектора, если решает владелец:** метить константой в обоих случаях — разрез по каналу на клиентской странице обязан различать опт, розницу и комиссию, а не показывать «Не указан» на треть суммы. Правка `sq_marts_sales_overview.sql` в scope НЕ входит: `COALESCE(f.sales_channel_name, 'Не указан')` (`reference/sql/sq_marts_sales_overview.sql:58`) корректен при любом исходе, чинить надо ингест, не витрину (`00_CHARTER §главный принцип`, фикс-форвард в нужный слой). Пишет: `reference/code/cf-facts/`, `reference/sales_perimeter_channel_decide_<date>.md`, `reference/_scratch_SALES-PERIMETER-CHANNEL-DECIDE_<date>/`. |
| SALES-DOCUMENT-OWNER-DEPLOY | (`SALES-PERIMETER-QUEUE-ADJ`, 2026-08-07) Деплой патча `SALES-DOCUMENT-OWNER-INGEST` (`document_owner_employee_id` в `core.fact_sales_profit`) — вторая половина работы, названная `ADR-128` как «отдельная задача, заводится по факту готовности патча»; без неё поле остаётся в снапшоте и разрез по сотруднику на клиентской странице не меняется | задача | **OPEN.** Класс B (деплой `cf-facts`), **мандат НЕ выдан** — выдаётся отдельным поимённым ADR по факту готовности патча, форма «один поимённый ADR на один деплой» не меняется (`DEPLOY-ORDER-ADJ`, 2026-08-03 §iv). Гейт: `SALES-DOCUMENT-OWNER-INGEST` DONE с артефактом в `reference/` (механический признак готовности, `ADR-109 §1`). **Три требования к приёмке, названные заранее, чтобы деплой не пришлось повторять.** (1) `ALTER TABLE core.fact_sales_profit ADD COLUMN` исполняется ДО деплоя кода, иначе первый же `MERGE` упадёт на несуществующей колонке. (2) Новая колонка обязана присутствовать в ветке `WHEN MATCHED THEN UPDATE SET` (`bq_ops.py:240-256`, сегодня 15 колонок), иначе у уже загруженных строк она останется `NULL` навсегда — тот же класс дефекта, что оставил `transaction_date` на дате первой записи (`ADR-100 §2`). (3) Глубина досева равна окну режима: `7` суток hourly, `90` weekly; июль-2026 внутри `90` только до `2026-09-29` (`ADR-125 §5`), после чего штатным путём не досеется — при постановке назвать, каким режимом и с каким `window_days` идёт первый прогон. Процедура — `05_CONVENTIONS` Часть II вариант Б, read-back по закреплённому `generation`. Пишет: `reference/sales_document_owner_deploy_<date>.md`, `reference/code/cf-facts/MANIFEST.md`, `reference/_scratch_SALES-DOCUMENT-OWNER-DEPLOY_<date>/`. |
| SALES-PERIMETER-PARITY-RECHECK | (`SALES-PERIMETER-QUEUE-ADJ`, 2026-08-07, восстановление парности `ADR-090 §1`) Бриф `briefs/SALES-PERIMETER-PARITY-RECHECK.md` закоммичен `82137c3` без строк реестра и мандата — бриф это сам предсказывал («заводится проходом сборки одновременно с этим брифом»), не заведено. Предмет: пересверка пары «Продажи» (строки 18-19 реестра) против `report/profit/*` после деплоя `cf-facts-00008-zen` | задача | **READY, мандат класса B ВЫДАН** третьим ADR блока `SALES-PERIMETER-QUEUE-ADJ`. Определение, шаги и приёмка — в брифе, здесь не дублируются. Разрешённый объём вызовов назван в самом ADR и сужать/расширять его сессии запрещено. **Ожидание, записанное до прогона:** видимая разность растёт с `0,31 %` до `≈1,75 %` (риск `R1`, `ADR-101`) — это предсказанное поведение, пара при этом остаётся закрытой правилом (`ADR-119 §2`), строки 18/19 не переоткрываются и правка не откатывается. Переоткрытие возможно только при появлении СВОБОДНОГО остатка, то есть суммы, не относимой ни к строкам-сиротам, ни к периметру типов документов. Пишет: `reference/parity_registry.md` (строки 18-19), `reference/sales_perimeter_parity_recheck_<date>.md`, `reference/_scratch_SALES-PERIMETER-PARITY-RECHECK_<date>/`. |

### Патч 2 — `07_STATE.md` §Открытые вопросы / GAP-реестр, append пять строк индекса

*Операция: append в конец таблицы индекса. Четыре ячейки, словарь статусов закрытый (`ADR-106 §1`).*

| ID | Статус | Вопрос | Гейт |
|---|---|---|---|
| `SALES-PERIMETER-LANDING-CHECK` | READY | Доехал ли периметр продаж до июля-2026, до витрины и до клиентской страницы после разового промоута | нет; класс A, брифа не требует — приёмка записана дословно в `07_GAPS.md` |
| `SALES-PERIMETER-CADENCE` | OPEN | Режимы периметра не вызываются ни одним расписанием — деплой без каденции даёт разовый эффект | шаг 1 read-only переснимает живые объекты; деплой Workflows — мандат класса B выдан |
| `SALES-PERIMETER-CHANNEL-DECIDE` | READY | У строк периметра пустой канал продаж, корзина «Не указан» перестаёт быть однородной | подготовка класс A; деплой правки — мандат класса B не выдан |
| `SALES-DOCUMENT-OWNER-DEPLOY` | OPEN | Деплой патча сотрудника-владельца документа в `core.fact_sales_profit` | готовность патча `SALES-DOCUMENT-OWNER-INGEST`; мандат класса B выдаётся отдельным поимённым ADR |
| `SALES-PERIMETER-PARITY-RECHECK` | READY | Пересверка пары «Продажи» против отчёта прибыльности после деплоя периметра | нет; мандат класса B выдан, бриф готов |

### Патч 3 — `07_STATE.md` §Мандат Claude Code: класс задач, append пять строк

*Операция: append в конец таблицы. Парность `ADR-090 §1` соблюдена — те же пять ID, тот же коммит.*

| Задача | Класс | Параллель | Мандат | Основание |
|---|---|---|---|---|
| SALES-PERIMETER-LANDING-CHECK | A | да | постоянный | read-only запросы к `core`/`marts`/`stg_msklad`, живых вызовов к МойСкладу нет, в прод-таблицы не пишет. Исполняется БЕЗ брифа по `ADR-086 §1`: приёмка записана дословно строкой `07_GAPS.md`, текст запуска обязан процитировать её без пересказа. Пишет: `reference/sales_perimeter_landing_check_<date>.md`, `reference/_scratch_SALES-PERIMETER-LANDING-CHECK_<date>/` |
| SALES-PERIMETER-CADENCE, шаг 1 (съём живых объектов) + шаг 2 (патч снапшота) | A | да | постоянный | read-only `gcloud scheduler jobs list` / `gcloud workflows describe` и правка снапшота, ничего не деплоит. Пишет: `reference/code/cf-facts/workflow_weekly.yaml`, `reference/sales_perimeter_cadence_<date>.md`, `reference/_scratch_SALES-PERIMETER-CADENCE_<date>/` |
| SALES-PERIMETER-CADENCE, шаг 3 (деплой Workflows) | B | нет | выдан поимённо вторым ADR блока `SALES-PERIMETER-QUEUE-ADJ` | деплой `msklad-pipeline-weekly`; процедура `05_CONVENTIONS` Часть II вариант Б, форма read-back для Workflows — `--format=json` плюс программное извлечение `sourceContents` |
| SALES-PERIMETER-CHANNEL-DECIDE, подготовка | A | да | постоянный | офлайн-разбор дампов ответа и текст правки `fetch_perimeter.py`, ничего не деплоит, живых `GET` не требует. Пишет: `reference/code/cf-facts/`, `reference/sales_perimeter_channel_decide_<date>.md`, `reference/_scratch_SALES-PERIMETER-CHANNEL-DECIDE_<date>/` |
| SALES-PERIMETER-CHANNEL-DECIDE, деплой | B | нет | НЕ выдан | деплой `cf-facts`; выдаётся по факту готовности правки, отдельным поимённым ADR |
| SALES-DOCUMENT-OWNER-DEPLOY | B | нет | НЕ выдан | деплой `cf-facts` плюс `ALTER TABLE` живой `core.fact_sales_profit`; выдаётся отдельным поимённым ADR по факту готовности патча (`ADR-109 §1` — артефакт в `reference/` с именем задачи) |
| SALES-PERIMETER-PARITY-RECHECK | B | нет | выдан поимённо третьим ADR блока `SALES-PERIMETER-QUEUE-ADJ` | живые `GET` к `report/profit/*` с секретом `msklad-token`; объём вызовов ограничен текстом того ADR, оговорки `ADR-076 §5` обязательны |

## NEW_DECISIONS

- **ADR-0XX (первый ADR этого блока): очередь после деплоя периметра продаж заведена четырьмя
  задачами; счётчик реестра — `7/7`; эскалация по расхождению на клиентской странице приземлена
  задачами, а не наблюдением**

  **Статус: accepted** (владелец подтвердил заведение строк прямым решением в чате 2026-08-07).

  **Контекст.** Деплой `cf-facts-00008-zen` (2026-08-07) состоялся, периметр промоутнут разово
  (`+5 467` строк, `+12 702 994,13` KGS за 90 суток, `reference/code/cf-facts/MANIFEST.md` шаг 9б).
  Владелец сообщил, что картина расхождения на странице продаж не изменилась, и запросил разбор.
  Одновременно исполнитель остановился на задаче, определение которой существовало только в
  разговоре, — по `ADR-086 §3` это правильная остановка, а не сбой.

  Установлено чтением репо, без единого облачного вызова:

  (i) **Счётчик реестра паритета — `7/7`, а не `6/7`.** Строка 21 закрыта `ADR-129` тем же проходом
  сборки 2026-08-07; расхождение стенд-апа уже названо в `07_STATE.md:1041` как содержание чужого
  блока. Задач, гейтящих седьмую пару, не существует. Открыт не счётчик, а клиентская приёмка —
  разрез, который `ADR-125 §1` назвал заранее.

  (ii) **Режимы `perimeter` и `perimeter_promote` не вызываются ни одним расписанием.** Сплошной
  поиск подстроки `perimeter` даёт 0 совпадений в `reference/code/cf-facts/workflow_hourly.yaml`,
  `workflow_weekly.yaml` и `deploy_and_workflow.sh`; инвентарь Cloud Scheduler
  (`11_INFRA_FACTS.md:27`) называет пять джобов, ни один из них периметр не вызывает. Деплой
  добавил способность, но не поведение.

  (iii) **У строк периметра нет канала продаж** — `CAST(NULL AS STRING) AS sales_channel_name`
  (`bq_ops.py:483-486`), витрина превратит их в «Не указан» (`sq_marts_sales_overview.sql:58`) и
  смешает с 40 строками опта без канала (`ADR-126 §7`).

  (iv) **Разрез по сотруднику от этого деплоя не сдвигается по построению** — разрыв есть
  переатрибуция, а не потеря (`ADR-126 §3`, подтверждено данными клиента `ADR-127`); чинится только
  патчем загрузчика и его деплоем.

  (v) **Дефект парности `ADR-090 §1`:** бриф `briefs/SALES-PERIMETER-PARITY-RECHECK.md` закоммичен
  `82137c3`, строк задачи нет ни в `07_GAPS.md`, ни в таблице мандата; бриф сам предсказывал их
  заведение сборкой, заведения не произошло.

  **Решение.**

  §1 Заводятся четыре задачи — `SALES-PERIMETER-LANDING-CHECK`, `SALES-PERIMETER-CADENCE`,
  `SALES-PERIMETER-CHANNEL-DECIDE`, `SALES-DOCUMENT-OWNER-DEPLOY` — полным текстом определения и
  приёмки в `07_GAPS.md`, строкой индекса в `07_STATE`, строкой мандата в той же таблице, одним
  коммитом (`ADR-090 §1`). Восстанавливается парность пятой, `SALES-PERIMETER-PARITY-RECHECK`.

  §2 **Эскалация по расхождению на клиентской странице приземляется задачами, а не наблюдением.**
  Каждая гипотеза о причине получает задачу-различитель, и ни одна не остаётся в тексте ответа:
  «смотрели до пересборки витрины либо на кэш» и «периметр лёг не на июль» закрывает
  `SALES-PERIMETER-LANDING-CHECK` величинами (1)-(4) его приёмки; «новые строки не видны в разрезе
  по каналу» — величина (5) той же задачи плюс `SALES-PERIMETER-CHANNEL-DECIDE`; «эффект разовый и
  завтра разойдётся снова» — `SALES-PERIMETER-CADENCE`; «разрез по сотруднику не сдвинулся» — уже
  объяснён (iv) и закрывается парой `SALES-DOCUMENT-OWNER-INGEST` + `SALES-DOCUMENT-OWNER-DEPLOY`.
  Гипотеза без задачи-различителя в этом проекте есть та же догадка, только длиннее.

  §3 **Порядок.** `SALES-PERIMETER-LANDING-CHECK` и `SALES-DOCUMENT-OWNER-INGEST` идут первыми и
  параллельно (обе класс A; перед одновременным запуском обязателен
  `bash tools/parallel_check.sh SALES-PERIMETER-LANDING-CHECK SALES-DOCUMENT-OWNER-INGEST`,
  `ADR-082 §1`). Затем `SALES-PERIMETER-CADENCE` и `SALES-PERIMETER-CHANNEL-DECIDE`. Затем два
  класса B: `SALES-PERIMETER-PARITY-RECHECK` и `SALES-DOCUMENT-OWNER-DEPLOY`. Основание порядка:
  замер, показывающий, где обрывается периметр, меняет содержание всех последующих правок, а
  стоит один заход read-only.

  §4 **Учёт `ADR-119 §4` соблюдён:** ни одна из четырёх задач не объявляет пару реестра сошедшейся
  и счётчика не двигает — реестр уже `7/7`. Задача-замер и задача-вердикт остаются разными
  работами; вердикт по итогам `SALES-PERIMETER-PARITY-RECHECK` (если он понадобится) есть
  адъюдикация архитектора, сессии-исполнителю не отдаваемая (`ADR-076 §4`, `ADR-091 §8`).

  §5 **Заморозка `ADR-107` не нарушена:** все пять строк предметные, ни одна не вводит и не меняет
  правило метода. Стоп исполнителя на задаче из разговора — четвёртый инстанс уже
  зарегистрированного класса (`07_STATE.md:221`), строки реестра под себя не получает
  (`ADR-051 §2`, `ADR-107 §4`).

  **Последствия:**
  - [сейчас] `07_GAPS.md`: append пять строк (Патч 1 этого блока, дословно).
  - [сейчас] `07_STATE.md` §Открытые вопросы: append пять строк индекса (Патч 2, дословно).
  - [сейчас] `07_STATE.md` §Мандат Claude Code: append семь строк — пять задач, из которых две
    разрезаны по шагам (Патч 3, дословно); парность `ADR-090 §1` соблюдена.
  - [сейчас] `07_STATE.md` §Стенд-ап: заменяется целиком, счётчик `7/7`.
  - [сейчас] `07_STATE.md` §Подробности для модели: append абзац этого блока.
  - [сейчас] `06_INDEX.md`: три строки на три ADR этого блока.
  - [задачей SALES-PERIMETER-LANDING-CHECK] проверка, доехал ли периметр до июля, витрины и страницы.
  - [задачей SALES-PERIMETER-CADENCE] подключение периметра к недельному конвейеру.
  - [задачей SALES-PERIMETER-CHANNEL-DECIDE] метка канала для розницы и комиссии.
  - [задачей SALES-DOCUMENT-OWNER-DEPLOY] деплой поля сотрудника-владельца документа.
  - [без правок] `reference/code/cf-facts/*` — этой сессией не трогались; `reference/parity_registry.md`
    правит только `SALES-PERIMETER-PARITY-RECHECK`; `04_ROADMAP.md` — расхождение с фактической
    очередью уже названо (`ADR-125 §6`), настоящим решением не чинится.

  Введено сессией `SALES-PERIMETER-QUEUE-ADJ` (2026-08-07, архитектор).

---

- **ADR-0XX (второй ADR этого блока): мандат класса B на деплой Cloud Workflows в рамках
  `SALES-PERIMETER-CADENCE`**

  **Статус: accepted** (владелец выдал мандат прямым решением в чате 2026-08-07).

  **Контекст.** Деплой периметра дал способность без поведения: режимы существуют, вызывать их
  некому (первый ADR блока, (ii)). Подключение — правка YAML недельного pipeline, то есть деплой
  Cloud Workflows, класс B. Форма «один поимённый ADR на один деплой» действует
  (`DEPLOY-ORDER-ADJ`, 2026-08-03 §iv), пакетный мандат отклонён и не переоткрывается.

  **Решение.** Мандат выдан поимённо на задачу `SALES-PERIMETER-CADENCE`, шаг 3.

  §1 **Разрешено ровно одно действие:** развёртывание нового текста `msklad-pipeline-weekly`
  (`gcloud workflows deploy`) с двумя добавленными шагами — `step_perimeter` между `step_facts` и
  `step_dq`, `step_perimeter_promote` после `step_promote`. Никаких иных объектов, никаких правок
  `msklad-pipeline-hourly`, никаких новых джобов Cloud Scheduler, никаких правок самой функции
  `cf-facts` этим мандатом не разрешено.

  §2 **Предусловие исполнения:** шаг 1 задачи (read-only съём живых объектов) исполнен и записан;
  при обнаружении уже подключённой каденции мандат не используется вовсе.

  §3 **Обязательный порядок и откат.** Процедура — `05_CONVENTIONS` Часть II вариант Б: снимок
  задеплоенного текста как seed, отдельная ветка, слияние после read-back. Read-back для Workflows
  делается через `--format=json` и программное извлечение `sourceContents` — форматтер `gcloud`
  добавляет свой перевод строки и даёт ложное расхождение в один байт
  (`DQ-GATE-SCOPE-SPLIT-DEPLOY`, 2026-08-05). Откат — повторный деплой снятого до правки текста;
  текст обязан быть сохранён в scratch до деплоя, а не восстанавливаться из памяти.

  §4 **Оговорка `ADR-077 §6` обязательна:** перед исполнением сессия отдельным сообщением в чат
  называет, что именно будет развёрнуто, на каком объекте и чем откатывается. Отсутствие такого
  сообщения делает подтверждение владельца неотличимым от рутинного запуска скрипта.

  §5 **Мандат не покрывает первый прогон нового шага вручную.** Каденция включается расписанием;
  принудительный внеплановый вызов `mode=perimeter_promote` есть запись в живую `core.fact_*` и
  требует отдельного решения — прецедент формы `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2.

  **Последствия:**
  - [сейчас] `07_STATE.md` §Мандат Claude Code: строка `SALES-PERIMETER-CADENCE, шаг 3` несёт
    «выдан поимённо» со ссылкой на этот ADR.
  - [задачей SALES-PERIMETER-CADENCE] исполнение деплоя и запись провенанса в
    `reference/code/cf-facts/MANIFEST.md`.
  - [без правок] `reference/code/cf-facts/workflow_hourly.yaml` — часовой pipeline этим решением не
    затрагивается; `11_INFRA_FACTS.md §SQ` — пополняется задачей, не этим ADR.

  Введено сессией `SALES-PERIMETER-QUEUE-ADJ` (2026-08-07, архитектор).

---

- **ADR-0XX (третий ADR этого блока): мандат класса B на живые вызовы `report/profit/*` в рамках
  `SALES-PERIMETER-PARITY-RECHECK`**

  **Статус: accepted** (владелец выдал мандат прямым решением в чате 2026-08-07).

  **Контекст.** Пара «Продажи» закрыта правилом (`ADR-119`), но после деплоя периметра ни одна
  сторона не пересчитывалась: предсказанный рост видимой разности до `≈1,75 %` остаётся ожиданием,
  не измерением (`07_STATE.md:1080-1083`). Бриф задачи готов, строк реестра не имел — заводятся
  первым ADR блока.

  **Решение.** Мандат выдан поимённо на задачу `SALES-PERIMETER-PARITY-RECHECK`.

  §1 **Разрешённый объём вызовов, шире которого сессия не идёт:** `GET` к
  `report/profit/byproduct` и `report/profit/bycounterparty` с границами `momentFrom` /
  `momentTo`, покрывающими зону паритета мая-2026, с пагинацией `limit ≤ 100`
  (`02_ERP_CONTRACTS §поведение API`). Ни один иной endpoint, ни один `POST`/`PUT`/`DELETE`, ни
  одна запись в любую таблицу проекта этим мандатом не разрешены.

  §2 **Эталон датируется** (`ADR-067 §3`): сырые тела ответов сохраняются в scratch задачи и
  коммитятся как провенанс; сравнение идёт со снятым снимком, а не с повторным запросом в момент
  вывода. Наша величина берётся из `core.fact_sales_profit` и НЕ перевычисляется из API в момент
  сравнения — иначе проверка вырождается в сравнение источника с самим собой (`ADR-098 §7`).

  §3 **Правило суток обеих сторон называется явно** (`ADR-088 §3`) вместе с окном UTC; окно
  запроса выводится из правила, а не назначается на глаз.

  §4 **Критерий вывода — инвариант разложения, не порог** (`ADR-119 §2`): разность обязана
  разложиться на строки-сироты и периметр типов документов без свободного остатка. Рост видимой
  разности до `≈1,75 %` есть предсказанное поведение (риск `R1`, `ADR-101`); он не является
  регрессом, не переоткрывает строки 18/19 и не служит основанием откатывать деплой.
  **Объявление пары сошедшейся либо её переоткрытие сессии-исполнителю не отдаётся**
  (`ADR-076 §4`, `ADR-091 §8`) — сессия предъявляет разложение, вердикт выносит архитектор.

  §5 **Оговорки `ADR-076 §5` обязательны;** перед первым живым вызовом — отдельное сообщение в чат
  по `ADR-077 §6`. Форма логирования — `ADR-079 §6`.

  §6 **Ловушка, снятая заранее:** `curl` с заголовком `Accept-Encoding: gzip` без флага
  `--compressed` отдаёт нераспакованные байты и ломает разбор ответа
  (`PARITY-STOCK-SNAPSHOT-SYNC`, 2026-08-04); `bq query` без явного `--max_rows` тихо режет вывод
  до 100 строк (`PARITY-STOCK-INTRANSIT`, 2026-08-04).

  **Последствия:**
  - [сейчас] `07_STATE.md` §Мандат Claude Code: строка `SALES-PERIMETER-PARITY-RECHECK` несёт
    «выдан поимённо» со ссылкой на этот ADR.
  - [задачей SALES-PERIMETER-PARITY-RECHECK] измерение и правка строк 18-19
    `reference/parity_registry.md`.
  - [без правок] `briefs/SALES-PERIMETER-PARITY-RECHECK.md` — бриф не переписывается, мандат
    добавляется строкой реестра и этим ADR.

  Введено сессией `SALES-PERIMETER-QUEUE-ADJ` (2026-08-07, архитектор).

## NEW_CONVENTIONS

нет

=== END SESSION ===
