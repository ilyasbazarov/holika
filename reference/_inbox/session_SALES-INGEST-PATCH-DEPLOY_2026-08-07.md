=== SESSION LOG · 2026-08-07 · SALES-INGEST-PATCH-DEPLOY ===

## SESSION_LOG
- Задача: SALES-INGEST-PATCH-DEPLOY — деплой готового патча периметра продаж в `cf-facts`
- Сделано: шаг 1 (живой `GET entity/retaildemand/positions`, состав полей подтверждён по всем
  четырём предположенным полям); шаг 2 (свежий снапшот `cf-facts-00007-xir`/`generation
  1782334223015697`, побайтовая сверка с `master` код-репо `6a581bf` — расхождения нет); шаг 3
  (ветка `deploy/cf-facts-2026-08-07-perimeter` от `master`, патч перенесён по diff, `.gcloudignore`
  добавлен, сплошной поиск секретов — 0 значений, C1/C2/`py_compile` подтверждены, коммит `bfa74d4`
  локально). Артефакт — `reference/sales_ingest_patch_deploy_2026-08-07.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-INGEST-PATCH-DEPLOY_2026-08-07/step1_run.log`,
  `step2_run.log`, `step3_branch_diff.patch`, `step3_branch_commit_log.txt`.
- Отклонения от плана: сессия остановлена владельцем на шаге 4 (push ветки) — прямой вопрос
  получил ответ «Нет, остановиться». Шаги 4–10 не выполнены. Прод `cf-facts` не тронут.

## STATE_PATCH
- Задача SALES-INGEST-PATCH-DEPLOY: READY → READY (без изменения статуса; подготовка шагов 1–3
  исполнена, деплой не начат)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-INGEST-PATCH-DEPLOY` шаги 1–3 исполнены (состав полей `retaildemand`
    подтверждён, снапшот сверен с `master` без расхождения, патч перенесён в ветку
    `deploy/cf-facts-2026-08-07-perimeter`, коммит `bfa74d4`, НЕ запушена) — владелец остановил
    сессию перед push, деплой не стартовал.
  - Где мы: шесть пар реестра из семи сходятся; до формального финиша остаётся строка 21; готовый
    патч периметра продаж на один шаг ближе к деплою (ветка готова, не выложена), окно пересборки
    июля `2026-09-29` не истекло.
  - Следующий шаг: `SALES-INGEST-PATCH-DEPLOY` продолжение с шага 4 (push ветки — требует
    подтверждения владельца) → шаг 5 (объявление действия + мандат класса B на сам деплой);
    `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 (класс B, мандат на запись не выдан); `SALES-EMPLOYEE-ATTRIBUTION`
    (класс A, ждёт отчёта клиента); `INTRANSIT-SURFACE-LAG` (класс A).
  - Развилки на владельце: подтверждение push ветки `deploy/cf-facts-2026-08-07-perimeter` (снято
    этой сессией — «нет»,открыт заново в следующей); выдача поимённого мандата класса B на сам
    деплой; мандат на принудительный прогон `cf-inventory`; правило разреза по сотруднику; правка
    `04_ROADMAP`.
  - Счётчик: пары реестра 6/7 сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: локальный коммит `bfa74d4` (ветка `deploy/cf-facts-2026-08-07-perimeter`,
  от `master` `6a581bf`) существует ТОЛЬКО в рабочем клоне внутри
  `reference/_scratch_SALES-INGEST-PATCH-DEPLOY_2026-08-07/step2_work/master_repo/repo/` (не
  запушен, не закоммичен в holika — это внешний код-репозиторий). Следующая сессия ПЕРЕД
  повторным переносом патча по diff обязана проверить, не сдвинулся ли `master` код-репо с
  `6a581bf` (если сдвинулся — снапшот шага 2 устарел, переносить заново). Текст диффа
  продублирован как текстовый провенанс в `reference/_scratch_SALES-INGEST-PATCH-DEPLOY_2026-08-07/step3_branch_diff.patch`
  на случай недоступности рабочего клона. Мандат на push и на сам деплой отдельные — push этой
  сессией запрошен и получил «нет», мандат класса B на деплой (шаг 5+) вообще не запрашивался.
- Новые открытые вопросы: нет
- Блокеры: push ветки `deploy/cf-facts-2026-08-07-perimeter` не подтверждён владельцем — блокирует
  шаги 4–10 брифа
- updated_at: 2026-08-07
- обновил: executor (сессия: SALES-INGEST-PATCH-DEPLOY)

## NEW_DECISIONS
- нет

## NEW_CONVENTIONS
- нет

=== END SESSION ===
