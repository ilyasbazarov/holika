=== SESSION LOG · 2026-08-07 · SALES-INGEST-PATCH-DEPLOY ===

## SESSION_LOG
- Задача: SALES-INGEST-PATCH-DEPLOY — деплой готового патча периметра продаж в `cf-facts`
- Сделано: полный цикл процедуры деплоя (вариант Б), шаги 1–10. Живой `GET
  entity/retaildemand/positions` подтвердил состав полей; снапшот сверен с `master` (расхождения
  нет); патч перенесён по diff в ветку `deploy/cf-facts-2026-08-07-perimeter` (коммит `bfa74d4`),
  запушена по согласию владельца; деплой выполнен (новая ревизия `cf-facts-00008-zen`); read-back
  подтвердил побайтовое соответствие архива ветке; функциональные проверки — `hourly` (без ошибок,
  лог исключения не наблюдался в этом окне за отсутствием повода — см. артефакт), `perimeter`
  (staging: 506 док/1441 стр `retaildemand`, 24 док/4026 стр `commissionreportin`), затем, после
  отдельного согласия владельца, `perimeter_promote` (MERGE в `core.fact_sales_profit`: `7062→12529`
  строк, `+12 702 994,13` KGS, арифметика и джойн по `transaction_id` подтверждены прямым запросом).
  Ветка слита в `master` (`fbf351f`) и запушена; `MANIFEST.md` обновлён записью ревизия↔коммит по
  результату read-back. Артефакт — `reference/sales_ingest_patch_deploy_2026-08-07.md`.
- Команды/логи ключевые: `reference/_scratch_SALES-INGEST-PATCH-DEPLOY_2026-08-07/step1_run.log`
  … `step9b_*` (полный провенанс всех десяти шагов).
- Отклонения от плана: клиент `gcloud functions call` дважды упирался в собственный таймаут (300с)
  на длинных вызовах (`perimeter`, окно 90 суток) — сервер оба раза уже завершил обработку успешно
  к моменту проверки; слепой повтор не делался, состояние подтверждалось `gcloud logging read`
  перед тем как считать шаг пройденным. Каждое не-идемпотентное действие (push ветки, деплой,
  MERGE в `core`, слияние в `master`) запрашивалось владельцем отдельно, в его собственном
  сообщении — сессия не связывала диагностику с действием.

## STATE_PATCH
- Задача SALES-INGEST-PATCH-DEPLOY: READY → DONE (деплой выполнен и подтверждён read-back +
  функциональными проверками; MANIFEST.md несёт запись ревизия↔коммит)
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `SALES-INGEST-PATCH-DEPLOY` доведена до конца — `cf-facts` обновлён до ревизии
    `cf-facts-00008-zen`, периметр продаж расширен (`entity/retaildemand` +
    `entity/commissionreportin`), отгрузка комиссионеру исключена из выручки; ветка слита в
    `master` (`fbf351f`).
  - Где мы: шесть пар реестра из семи сходятся; готовый патч периметра продаж задеплоен —
    `SALES-CONSIGNMENT-REVENUE` и `SALES-PERIMETER-EXTEND` теряют гейт деплоя фактом; полная
    пересверка `core` vs эталона МойСклад после этого деплоя ЕЩЁ НЕ снята (см. «Развилки» и
    артефакт).
  - Следующий шаг: пересверка расхождения продаж с `report/profit/*` после деплоя (ожидаемый
    временный рост с `0,31 %` до `≈1,75 %`, риск `R1`, не регресс) и/или `SALES-REFRESH-WINDOW`
    (строки-сироты MERGE); `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 (класс B, мандат на запись не
    выдан); `SALES-EMPLOYEE-ATTRIBUTION` (класс A, ждёт отчёта клиента); `INTRANSIT-SURFACE-LAG`
    (класс A).
  - Развилки на владельце: назначить/подтвердить задачу пересверки продаж после этого деплоя;
    финальный статус строк реестра `SALES-CONSIGNMENT-REVENUE`/`SALES-PERIMETER-EXTEND`
    (`READY` → закрыть или оставить открытыми до пересверки — решение не этой сессии); выбор формы
    `SALES-REFRESH-WINDOW`; мандат на принудительный прогон `cf-inventory`; правило разреза по
    сотруднику; правка `04_ROADMAP`.
  - Счётчик: пары реестра 6/7 сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: деплой `cf-facts` (`cf-facts-00008-zen`, `generation
  1786093276804812`) состоялся 2026-08-07, запись — `reference/code/cf-facts/MANIFEST.md §Деплой
  cf-facts — периметр продаж (2026-08-07)`. `core.fact_sales_profit` за последние 90 суток
  выросло с `7062`/`293 325 096,84` до `12529`/`306 028 090,97` KGS (проверено прямым запросом,
  не по отчёту команды). Полная пересверка с оракулом МойСклада (`report/profit/*`) этой сессией
  НЕ проводилась — только предсказанный формулой эффект (`sales_ingest_patch_2026-08-03.md §5`)
  остаётся в силе как ожидание, не как измеренный факт после ЭТОГО конкретного прогона. Прикладной
  лог исключения комиссионных отгрузок не наблюдался в тестовом прогоне `hourly` — не дефект,
  в тестовом окне не было подходящих данных (подтверждено прямым просмотром сырого ответа
  МойСклада), следующая сессия НЕ должна считать это признаком того, что исключение не работает.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-07
- обновил: executor (сессия: SALES-INGEST-PATCH-DEPLOY)

## NEW_DECISIONS
- нет

## NEW_CONVENTIONS
- нет

=== END SESSION ===
