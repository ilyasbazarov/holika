=== SESSION LOG · 2026-08-08 · FACTS-BACKFILL ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `FACTS-BACKFILL` — read-only замер: осталась ли в `core.fact_purchases`/`core.fact_returns`
  дыра от простоя `drift_check` (`2026-08-01T18:00Z…2026-08-04T18:00:02Z`)
- Сделано:
  - Шаг 1: execution `msklad-pipeline-weekly` за `2026-07-27T00:00Z…момент замера` подняты дословно
    (список + describe); ERROR-уровневых логов провала `step_purchases`/`step_returns` — ноль.
    Прикладные логи `cf-facts` (`log.info`) в Cloud Logging не найдены ни в одном из четырёх
    проверенных окон (включая контрольное заведомо успешное) — гэп наблюдаемости, зафиксирован
    поимённо в артефакте, не расследован (вне scope).
  - Шаг 2: независимый источник истины — фактические задания BigQuery
    (`INFORMATION_SCHEMA.JOBS_BY_PROJECT`, `region-asia-east1`) вместо логов. Найдено: помимо
    недельного `step_purchases`, `core.fact_purchases` грузится ЕЖЕЧАСНО через `step_purchases`
    `workflow_hourly.yaml` — файл не входил в context-to-load брифа, прочитан по факту находки в
    задании BigQuery. Прямым замером установлен разрыв успешных заданий `fact_purchases`
    `2026-08-01 17:05Z → 2026-08-04 18:05Z` (≈73ч); причина разрыва (0 заказов от источника или
    проглоченное исключение) не различима без прикладных логов — `CONTEXT GAP`, не блокирует
    вердикт. `core.fact_returns`: 2 задания с `2026-07-27`, оба `2026-08-09`; первое совпадает по
    времени с execution `df58f54f…` (`step_returns` успел исполниться до провала на `step_dq` тем
    же execution); источник второго не установлен, read-only по природе, не расследован.
  - Шаг 3: текущее состояние обеих таблиц снято явным `COUNT(*)` (не `GROUP BY`, там где выдача
    могла быть пустой по двум причинам сразу). `fact_purchases`: без окна дат, полный снимок,
    `MAX(_loaded_at)=2026-08-10 11:01:56`. `fact_returns`: окно 90 суток от последнего успешного
    прогона (`2026-08-09`), покрывает `2026-05-11…2026-08-09` — период простоя `08-01…08-04`
    целиком внутри окна.
  - Шаг 4: раздельный вердикт по обеим таблицам — **досев НЕ требуется**, обоснование от механизма
    загрузки (`WRITE_TRUNCATE` без накопления истории — покрытие последним успешным прогоном
    физически исключает «дыру в середине истории», возможна только свежесть/несвежесть снимка).
  - Шаг 5 (запись) — НЕ исполнялся: гейт класса B в силе, вердикт Шага 4 делает запись не нужной
    по существу для обеих таблиц.
- Команды/логи ключевые: `reference/_scratch_FACTS-BACKFILL_2026-08-08/` (`step1…step3b_run.log`,
  10 скриптов+логов, `date -u`/`gcloud auth list` первой и последней командой каждого).
- Отклонения от плана: да — контекст брифа не включал `workflow_hourly.yaml` (только
  `workflow_weekly.yaml`), но именно часовой файл объясняет наблюдаемые ежечасные задания
  `fact_purchases`; поднят по факту находки в BigQuery, не выдуман. Приёмка Шага 1 брифа
  («состояние step_purchases/step_returns по логам») дополнена/частично замещена Шагом 2 (задания
  BigQuery) — прикладные логи `cf-facts` оказались недоступны в Cloud Logging (гэп наблюдаемости),
  workflow-логи несут только CRITICAL/ERROR-события, не подтверждения успеха шагов.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `FACTS-BACKFILL`: `OPEN` → **`DONE`** (закрывается фактом — обе половины реестра, закупки
  и возвраты, дали вердикт «досев не требуется»; артефакт
  `reference/facts_backfill_2026-08-08.md`). Строка переносится в `07_ARCHIVE.md` целиком по
  правилу компактности (`ADR-064`).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `FACTS-BACKFILL` закрыта DONE read-only замером — ни `core.fact_purchases`, ни
    `core.fact_returns` не несут дыры от простоя `drift_check` (`2026-08-01…08-04`); механизм
    `WRITE_TRUNCATE` без накопления истории самозалечивает простой первым успешным прогоном;
    артефакт `reference/facts_backfill_2026-08-08.md`.
  - Где мы: реестр паритета остаётся `7/7`; финиш Epic-1 не затронут (эта задача была частью
    финального остатка `CORE-FRESHNESS-SWEEP`/`FACTS-WORKFLOW-STOP-DIAG`, не паритетной пары).
  - Следующий шаг: без изменений от предыдущего стенд-апа — `DQ-GATE-FAIL-OPEN-FIX` подготовка,
    `DQ-FRESHNESS-COVERAGE` подготовка, `INFRA-FACTS-LANDING`.
  - Развилки на владельце: без изменений от предыдущего стенд-апа — предусловие мандата
    `SALES-REFRESH-WINDOW`; апрув `ERP-CONTRACTS-DAY-RULE-DOC`; подпись момента пересборки на
    странице «в пути» (исполнителя нет).
  - Счётчик: пары реестра `7/7` · обязательных задач передачи `10` (из них с выданным мандатом `3`)
    · Epic M — (Epic-1 завершён `ADR-140`).
- Подробности для модели:
  - `ADR-148 §4` поднимал вопрос разреза строки `FACTS-BACKFILL` на класс A (проверка) и класс B
    (досев), если проверка найдёт дыру. Разрез не понадобился: обе половины замера дали «не
    требуется», записи в `core.fact_*` не будет вовсе — задача полностью закрывается на классе A,
    без открытия класса B. Архитектору стоит закрыть этот пункт `ADR-148 §4` явной ссылкой на этот
    факт, а не оставлять как незакрытое действие.
  - Найдено попутно (не входило в context-to-load брифа): `reference/code/cf-facts/workflow_hourly.yaml`
    содержит `step_purchases` (`mode=purchases`, до `step_dq`, non-blocking `except`/`WARNING`) —
    `core.fact_purchases` грузится ежечасно, не только недельно. Это меняет фактическую картину
    относительно предположения брифа (только недельная загрузка) и объясняет высокую частоту
    заданий BigQuery, но НЕ меняет структурный вывод (полный снимок без окна дат самозалечивает
    любой простой короче времени жизни заказа).
  - Прикладные логи `cf-facts` (`log.info`) не обнаружены в Cloud Logging ни в одном из четырёх
    проверенных этой сессией окон, включая заведомо успешное контрольное окно
    (`2026-08-04T18:00-18:06Z`, `FACTS-FLOW-RESUME-CONFIRM`). Это гэп наблюдаемости приложения,
    отдельный от предмета этого брифа — стоит завести отдельной строкой (кандидат:
    `CF-FACTS-LOG-VISIBILITY`), не заводится этой сессией (класс A read-only, без мандата на
    заведение новых строк вне своей задачи).
  - Два непроверенных до конца наблюдения не блокируют вердикт, но названы поимённо в артефакте
    (§«Что не установлено»): причина 73-часового разрыва заданий `fact_purchases`; триггер второго
    задания `fact_returns` `2026-08-09T11:14:59Z`.
- Новые открытые вопросы:
  - `CF-FACTS-LOG-VISIBILITY` (кандидат, не заведена этой сессией) — прикладные логи `cf-facts` не
    появляются в Cloud Logging; решение архитектора, заводить ли строкой.
- Блокеры: нет.
- updated_at: 2026-08-08
- обновил: исполнитель (сессия: FACTS-BACKFILL)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
