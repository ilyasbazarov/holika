=== SESSION LOG · 2026-08-03 · DEPLOY-ORDER-ADJ ===

## SESSION_LOG

- Задача: `DEPLOY-ORDER-ADJ` — порядок и форма мандатов класса B на накопившиеся деплои (архитектор,
  класс A, без брифа, `ADR-086 §1/§3`, заведена из разговора владельца 2026-08-03).
- Сделано:
  - Подтверждён удалённым репозиторием факт снятия гейта `ADR-065` для всех трёх функций:
    `git ls-remote` → `refs/heads/master = 81812f4d06fccb5ea0500b565269b07755831fb0`, совпадает с
    локальным SHA из `reference/code_repo_seed_rest_2026-08-03.md`.
  - Сплошной проверкой по механическому признаку `ADR-109 §1` (артефакт в `reference/` с именем
    задачи) плюс `git log --all --grep` установлено: из семи единиц деплойной очереди построена ОДНА
    (`INVOICES-LOADER-BUILD`); `SALES-REFRESH-WINDOW` имеет текст вариантов без применения к снапшоту;
    `INGEST-MOMENT-ZONE-FIX`, `INGEST-CURRENCY-ASSERT` шаг 2, `DQ-GATE-SCOPE-SPLIT`,
    `SALES-CONSIGNMENT-REVENUE`, `SALES-PERIMETER-EXTEND` — не начаты.
  - Найдена незакрытая развилка, гейтящая ЛЮБОЙ первый деплой: форма деплой-процедуры «только из
    код-репо» (`ADR-065`, owner-gated), названная открытой в `ADR-094 §7`,
    `reference/code_repo_standup_d2_2026-08-01.md:100`, `reference/deploy_revision_probe_2026-08-01.md:121`
    и не имеющая строки ни в одном реестре.
  - Разобраны все три вопроса владельца; артефакт `reference/deploy_order_adj_2026-08-03.md`.
  - Названы четыре расхождения реестра с адресами (`§7` артефакта).
  - **Второй ход (по указанию владельца).** Форма деплой-процедуры выбрана владельцем — вариант Б
    (отдельная ветка, слияние в основную после успешной проверки развёрнутого архива). Вопрос о
    каденции пересъёма эталона, ошибочно заданный владельцу как вход, снят собственным замером
    (`05_CONVENTIONS` Часть I п.5): по 127 майским документам `entity/demand` посчитана кривая
    застывания — медиана `lag = 29,39` суток, `81,89 %` к концу +30 суток, `99,21 %` к `2026-07-26`;
    возвраты застывают в тот же день (`lag = 0,00` у всех 8). Найдено, что единственная точка
    выполнимости правила одновременности для мая (`2026-07-26`) совпала с ПЕРВЫМ провалом DQ Gate,
    и три майских документа общей стоимостью `12 997 537,72` KGS правились после последнего
    успешного обновления нашей стороны и штатным путём не догонятся никогда. Каденция вынесена
    развилкой трёх вариантов с рекомендацией (`§10` артефакта).
  - **Третий ход (по указанию владельца).** Каденция принята — вариант 1. Записаны оба решения:
    полный текст процедуры деплоя — `reference/deploy_procedure_2026-08-03.md` (новый артефакт,
    шаги 0-9, инварианты, чек-лист); короткая форма правила — `05_CONVENTIONS.md` Часть II отдельным
    именованным коммитом (`ADR-081 §8`); каденция с календарём, замером-основанием и обязательным
    запасным путём — новый раздел `reference/parity_registry.md`. Тексты-кандидаты для
    `02_ERP_CONTRACTS.md` (STABLE, вне списка `ADR-081 §8`) подготовлены и адресованы сборке
    (`§12` артефакта).
  - **Четвёртый ход (по указанию владельца).** Гейт закрытия мая снят ОФЛАЙН, живой запрос не
    понадобился: наша сторона по маю заморожена состоянием `2026-07-19`, сторона источника скачана
    `2026-08-01` — то есть уже после правок, поэтому посуточная разность двух уже лежащих в репо
    замеров (`step1b_by_date.json` против `step2_by_day.json`) показывает денежный эффект правок
    напрямую. Результат: `2026-05-03` расходится на `−0,02` KGS, `2026-05-10` на `+0,01` —
    округление; `2026-05-09` расходится на `+1 462 797,00`, но это одна из четырёх позиций,
    атрибутированных строкам-сиротам ещё `ADR-100 §1` (`_loaded_at = 2026-05-14`, за два месяца до
    правок). Решающее — арифметика замыкается: суммарная разность `1 630 239,87` против суммы семи
    сирот `1 630 240,60`, остаток `−0,73` KGS, свободного места под денежный эффект июльских правок
    не остаётся. **Три недосягаемые правки денег не меняли.** Три варианта закрытия мая с
    рекомендацией — `§14` артефакта.
  - **Пятый ход (по указанию владельца).** Вариант 1 принят — май закрыт датированным снимком по
    состоянию `2026-07-19T01:11:56Z`. Записан `reference/may_zone_close_2026-08-03.md`: посуточная
    таблица всех 26 суток обеих сторон (`1363` строки / `93 522 995,53` KGS против `127` документов /
    `91 892 755,66` KGS, разность `1 630 239,87`), три рассинхронизированных документа поимённо,
    доказательство неденежности, четыре границы записи и перечень того, что записью НЕ закрыто.
    Числа воспроизведены независимо и совпали до копейки с уже записанными в репо ранее.
    `reference/parity_registry.md` — абзац о состоянии мая заменён записью о закрытии.
- Команды/логи ключевые: `date -u` → `2026-08-03T10:20:56Z`; `git ls-remote
  https://github.com/ilyasbazarov/holika-prod`; `ls reference/` + `git log --all --grep` по семи именам
  задач. Ни одного вызова к облаку проекта, ни одного обращения к секретам, ни одной записи в
  `core.*`/`marts.*`.
- Отклонения от плана: нет.

## STATE_PATCH

- Задача `DEPLOY-ORDER-ADJ`: (строки не было) → **DONE**, закрывается тем же проходом сборки
  (открыта и закрыта одним блоком; прецедент формы — `FACTS-STOP-ADJ`, `INVOICES-STALENESS-PROBE`).
- Задача `DEPLOY-PROCEDURE`: (строки не было) → **PARTIAL** — форма выбрана владельцем (вариант Б),
  текст процедуры записан (`reference/deploy_procedure_2026-08-03.md` + `05_CONVENTIONS.md` Часть II);
  остаток — первое применение вместе с первым деплоем, класс B, мандат не выдан.
- Задача `PARITY-ZONE-SNAPSHOT-DECISION`: READY → **DONE** — оба условия закрытия строки выполнены:
  каденция назначена (решение владельца 2026-08-03, вариант 1) и плашка появилась в
  `reference/parity_registry.md`; парная плашка в `02_ERP_CONTRACTS.md` приходит тем же коммитом
  сборки текстом-кандидатом (`reference/deploy_order_adj_2026-08-03.md §12`).
- Задача `MAY-ZONE-CLOSE`: (строки не было) → **DONE** — май закрыт датированным снимком
  `2026-07-19` (вариант 1, решение владельца 2026-08-03); артефакт
  `reference/may_zone_close_2026-08-03.md`, запись в `reference/parity_registry.md`. Открыта и
  закрыта одним блоком.
- Задача `CODE-REPO-SEED-REST`: READY → **DONE** (остаток `push` исполнен и подтверждён удалённым
  репозиторием, см. `SESSION_LOG`).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: `DEPLOY-ORDER-ADJ` закрыта DONE — процедура деплоя и каденция пересъёма записаны,
    май закрыт датированным снимком (`reference/deploy_order_adj_2026-08-03.md`,
    `reference/deploy_procedure_2026-08-03.md`, `reference/may_zone_close_2026-08-03.md`).
  - Где мы: живой поток ждёт бесплатного замера сегодня вечером; деплойный путь открыт впервые —
    процедура есть, построен ровно один патч из семи.
  - Следующий шаг: `DQ-GATE-RECOVERY-CONFIRM` (после `2026-08-03T18:00Z`); затем
    `INVOICES-LOADER-DEPLOY` — первый деплой по новой процедуре, мандат класса B поимённо.
  - Развилки на владельце: апрув `GATE-STATE-VERIFIABILITY`.
  - Счётчик: пары реестра 2/7 сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели:

  **Деплойная очередь разобрана, мандат класса B не является следующим шагом (`DEPLOY-ORDER-ADJ`,
  2026-08-03, архитектор; номер ADR присваивает сборка).** Полный разбор с адресами `файл:строка` —
  `reference/deploy_order_adj_2026-08-03.md`, здесь не пересказывается. Что обязана знать следующая
  сессия: (i) **из семи единиц деплойной очереди построена ровно одна** — `INVOICES-LOADER-BUILD`;
  проверка механическая (`ADR-109 §1`: артефакт в `reference/` с именем задачи) и воспроизводимая, у
  `INGEST-MOMENT-ZONE-FIX`, `INGEST-CURRENCY-ASSERT` шага 2, `DQ-GATE-SCOPE-SPLIT`,
  `SALES-CONSIGNMENT-REVENUE`, `SALES-PERIMETER-EXTEND` артефакта нет и коммита нет; **статус `READY`
  в реестре означает «исполнимо сейчас» (`ADR-106 §2`), а не «готово к деплою»** — читать его как
  готовность есть воспроизводимая ошибка; (ii) **форма деплой-процедуры «только из код-репо» никогда
  не назначалась** и гейтит любой первый деплой: код-репо сегодня есть зеркало ЗАДЕПЛОЕННЫХ ревизий
  (оба коммита — seed из `function-source.zip`), а патч счетов лежит только в
  `reference/code/cf-finance/`, то есть первый деплой обязан развернуть направление инварианта
  «ревизия ↔ коммит»; пять конкретных неотвеченных вопросов перечислены в `§3.2` артефакта;
  (iii) восстановление живого потока **деплоем не является и мандата не требует** — `ADR-113` снял
  тезис о самоподдерживающемся отказе, два пути самоснятия следуют из кода и стоят ноль, различает их
  `DQ-GATE-RECOVERY-CONFIRM` (класс A, после `2026-08-03T18:00Z`); класс B открывается только при
  опровергающем исходе и тогда идёт одиночным заходом впереди всего; (iv) **пакетный мандат отклонён
  не по форме, а по отсутствию наполнения** — прецедент `ADR-101` волны 2 объединял пять готовых
  однородных read-only замеров, здесь предметы разнородны и несут разные откаты; действующая форма
  «один поимённый ADR на один деплой» (`ADR-092`/`ADR-100 §6`/`ADR-104 §1`/`ADR-108`) не меняется;
  (v) **выбор варианта `SALES-REFRESH-WINDOW` сделан владельцем 2026-08-02** (`ADR-101 §10`,
  `07_GAPS.md:70`): вариант 3 (плашка, без кода) к исполнению, вариант 1 как цель, вариант 2 отклонён —
  открытым остаётся только источник COGS для диапазона шире 90 суток, а вопрос допустимости `UPDATE`
  партиционирующей колонки в `MERGE` ЗАКРЫТ прошедшим dry-run (`SALES-MERGE-DRYRUN`), и требование
  «нужен dry-run перед деплоем» устарело.

  **Каденция пересъёма эталона получила замер, а не догадку (`DEPLOY-ORDER-ADJ`, второй ход,
  2026-08-03).** Полный расчёт, таблицы и скрипты — `reference/deploy_order_adj_2026-08-03.md §9-§10`
  и `reference/_scratch_DEPLOY-ORDER-ADJ_2026-08-03/`; здесь не пересказывается. Что обязана знать
  следующая сессия: (i) **месяц не закрыт ни через неделю, ни через месяц** — по 127 майским
  документам `entity/demand` застывание составляет `30,71 %` на +7 суток, `81,89 %` на +30,
  `95,28 %` на +45 и `99,21 %` к `2026-07-26`; медиана `lag = updated − moment` — `29,39` суток,
  максимум `80,10`; поэтому снимок «сразу после закрытия месяца» расходится с источником по
  построению, и это артефакт момента съёма, а не дефект пайплайна; (ii) возвраты ведут себя
  ПРОТИВОПОЛОЖНО (все 8 майских — `lag = 0,00`), то есть единая каденция на семь пар реестра обязана
  опираться на худшую пару, а не на среднюю; (iii) окно 90 суток даёт **ровно одну точку в месяц**,
  где правило одновременности (`ADR-079 §3`) выполнимо буквально — последнее воскресенье, при котором
  весь месяц ещё внутри окна (май → `2026-07-26`, июнь → `2026-08-30`, июль → `2026-09-27`,
  август → `2026-10-25`); (iv) **для мая эта точка упущена**: `2026-07-26` есть ровно тот прогон,
  который первым провалился в текущей аварии DQ Gate, последний успешный — `2026-07-19` при
  застывании `95,28 %`; шесть майских документов правились после него, три из них (стоимостью
  `12 997 537,72` KGS, `13,9 %` майской выручки) имеют `moment` раньше `2026-05-11` и штатным путём
  не догонятся уже никогда; **(v) денежного эффекта эти правки НЕ имели** — установлено офлайн
  сравнением двух уже лежащих в репо замеров (наша сторона заморожена `2026-07-19`, сторона источника
  скачана `2026-08-01`, то есть после правок): двое суток сходятся на уровне копеек, третьи расходятся
  ровно на позицию, атрибутированную строкам-сиротам ещё `ADR-100 §1`, и суммарная арифметика
  замыкается с остатком `−0,73` KGS, не оставляя места под эффект правок; утрата синхронизации по маю
  есть утрата НЕденежных атрибутов, и **что именно в трёх документах поменялось, не установлено —
  установлено только то, что не поменялось**; (vi) следствие для очереди: постоянный режим досева
  (`topoff`, целевой вариант `ADR-101 §10`) возвращается в роль страховки на случай упущенного
  месяца и требованием закрытия мая НЕ является — вывод предыдущего хода о выросшей срочности снят
  собственным замером.

- Новые открытые вопросы: нет. `MAY-ZONE-CLOSE` открыта и закрыта этим же блоком (см. выше).
  Отдельной строки под каденцию НЕ заводится (`ADR-051 §2`): вопрос закрыт решением владельца
  2026-08-03 и записан в `reference/parity_registry.md`.
- Блокеры: нет новых. `B-7` не трогался.
- updated_at: 2026-08-03
- обновил: архитектор (сессия: `DEPLOY-ORDER-ADJ`)

## NEW_DECISIONS

- ADR-0XX: Мандат класса B не является следующим шагом деплойной очереди; первым идёт форма
  деплой-процедуры, а восстановление потока деплоем не является

  **Контекст:** владелец запросил порядок и форму мандатов класса B на накопившиеся деплои, назвав
  четыре кандидата и предположив, что три из них имеют готовые патчи, а восстановление живого потока
  требует вмешательства класса B. Обе посылки проверены и обе не подтвердились: тезис о
  самоподдерживающемся отказе снят `ADR-113` двумя днями ранее, а механическая проверка готовности
  (`ADR-109 §1`) находит артефакт подготовки ровно у одной задачи из семи. Одновременно найдена
  развилка, названная открытой трижды и не имеющая строки ни в одном реестре, — форма деплой-процедуры
  «только из код-репо» (`ADR-065`, owner-gated).

  **Решение:**

  §1 Гейт `ADR-065` снят для всех трёх функций — подтверждено read-only вызовом к удалённому
  репозиторию (`refs/heads/master = 81812f4…`), а не сообщением в чате. Строка `CODE-REPO-SEED-REST`
  закрывается `DONE`. accepted (архитектор).

  §2 Готовность к деплою определяется механическим признаком `ADR-109 §1` и на сегодня есть у ОДНОЙ
  задачи (`INVOICES-LOADER-DEPLOY`). Статус `READY` в реестре означает «исполнимо сейчас»
  (`ADR-106 §2`) и готовностью патча не является; прочтение `READY` как готовности зафиксировано как
  воспроизводимая ошибка чтения реестра. accepted (архитектор).

  §3 **Восстановление живого потока идёт первым и деплоем не является.** До результата замера
  `DQ-GATE-RECOVERY-CONFIRM` (класс A, после `2026-08-03T18:00Z`) вмешательство класса B основания не
  имеет (`ADR-113 §5` подтверждается и не пересматривается). При опровергающем исходе восстановление
  становится задачей класса B, идёт ОДИНОЧНЫМ заходом впереди всей очереди, и форма его выводится из
  наблюдённой причины, а не назначается заранее. accepted (архитектор).

  §4 **Форма деплой-процедуры выбрана владельцем — вариант Б** (чат 2026-08-03), закрывая развилку,
  оставленную открытой `ADR-065` и трижды переподтверждённую как неназначенную (`ADR-094 §7`,
  `reference/code_repo_standup_d2_2026-08-01.md:100`, `reference/deploy_revision_probe_2026-08-01.md:121`).
  Правило: **ещё не задеплоенный патч ложится в ОТДЕЛЬНУЮ ветку код-репо; слияние в основную ветку
  происходит только после успешной проверки развёрнутого архива.** Основная ветка тем самым в любой
  момент равна проду, что и есть содержание инварианта «ревизия ↔ коммит». Соответствие фиксируется
  записью в `MANIFEST.md` соответствующей функции по результату read-back архива (`ADR-040`,
  `ADR-017 §2`), а не сообщением команды деплоя (`ADR-021 §2`). В процедуру входят без изменений:
  чистый `.gcloudignore` как условие деплоя (`ADR-040`), раздельность диагностики и действия
  (`ADR-055 §2`), объявление действия отдельным сообщением до подтверждения (`ADR-077 §6`), и явный
  запрет секретов в репозитории (он публичный, `CODE-REPO-VISIBILITY`) — сегодня это верно по факту,
  но правилом не записано. Запись процедуры текстом — задача `DEPLOY-PROCEDURE`, класс A.
  accepted (владелец — форма; архитектор — состав встраиваемых условий).

  §5 **Пакетный мандат класса B на несколько деплоев отклоняется.** Прецедент `ADR-101` волны 2
  применим к готовым однородным единицам работы; здесь предметы разнородны, готовность разная, и откат
  у каждого свой, поэтому пакет связал бы откаты между собой. Действующая форма — один поимённый ADR
  на один деплой (`ADR-092`, `ADR-100 §6`, `ADR-104 §1`, `ADR-108`) — сохраняется без изменений;
  обязательный минимум его содержания (объект и sha256 патча, чем откатывается, read-back по
  `ADR-040`, раздельность диагностики и действия по `ADR-055 §2`, объявление действия отдельным
  сообщением по `ADR-077 §6`) собирается из уже принятых правил и не изобретается. accepted
  (архитектор).

  §6 **Первым деплоем новой эры назначается `INVOICES-LOADER-DEPLOY`** — он же приёмка процедуры.
  Основание: единственный построенный патч; не трогает работающий поток (новый режим против правки
  формулы в живом ингесте); откат тривиален (предыдущая ревизия `cf-finance` без режима `invoices`).
  accepted (архитектор); выдача самого мандата — владелец, отдельным ADR после `§4`.

  §7 **Выбор варианта `SALES-REFRESH-WINDOW` не открыт** — он сделан владельцем 2026-08-02
  (`ADR-101 §10`): вариант 3 к исполнению, вариант 1 как цель, вариант 2 отклонён. Противоречие двух
  строк реестра снимается в пользу более позднего решения. Вопрос допустимости `UPDATE`
  партиционирующей колонки внутри `MERGE` закрыт прошедшим dry-run (`SALES-MERGE-DRYRUN`); открытым
  остаётся только источник COGS для диапазона шире 90 суток, и он бьёт исключительно по варианту 1 в
  момент его постройки. accepted (архитектор).

  §8 Заморозка `ADR-107` на `DEPLOY-PROCEDURE` не распространяется по двум независимым основаниям:
  процедура деплоя Cloud Function есть предметный контур пайплайна (тот же разбор, что у
  `DQ-GATE-SCOPE-SPLIT`), а при строгом прочтении исключение обосновано поимённо — без процедуры
  встают `INVOICES-PARITY-RECHECK` (строка 23 реестра паритета) и ветка досева зоны паритета.
  accepted (архитектор).

  §9 **Каденция пересъёма эталона выведена из двух измеренных границ и выбрана владельцем —
  вариант 1** (чат 2026-08-03). Снизу каденцию держит скорость застывания документов в источнике,
  сверху — окно обновления 90 суток, после которого выравнивать стороны нечем; между ними остаётся
  практически одна точка в месяц, и правило её называет: **эталон месяца снимается ОДНОКРАТНО, в
  последнее воскресенье, при котором весь месяц ещё целиком внутри окна 90 суток; наша сторона
  снимается в тот же день, сразу после недельного прогона; пересъём после этого не производится.**
  Календарь: май → `2026-07-26`, июнь → `2026-08-30`, июль → `2026-09-27`, август → `2026-10-25`.
  **Запасной путь обязателен и есть часть правила:** сорванный целевой прогон уносит окно
  безвозвратно, тогда месяц закрывается либо датированным снимком по последнему успешному прогону с
  поимённо названной потерей документов, либо досевом после постройки постоянного режима. Замер-
  основание и его предел — `reference/deploy_order_adj_2026-08-03.md §9`. Варианты 2 и 3 отклонены:
  второй удваивает стоимость ради заведомо расходящегося раннего снимка (`30,71 %` застывания на
  +7 суток), третий небезопасен по построению — после выхода месяца из окна наша сторона перестаёт
  меняться, и триггер молчит навсегда, а источник продолжает двигаться. accepted (владелец — выбор;
  архитектор — фактура, границы и запасной путь).

  §10 **Размещение обоих решений.** Процедура деплоя: полный текст —
  `reference/deploy_procedure_2026-08-03.md`, короткая форма правила — `05_CONVENTIONS.md` Часть II
  рядом с гигиеной деплоя (`ADR-040`), внесена сессией отдельным именованным коммитом (`ADR-081 §8`).
  Каденция: `reference/parity_registry.md`, раздел «Датированный снимок и каденция пересъёма» —
  внесён сессией (класс A, `/reference`). Плашка в `02_ERP_CONTRACTS.md` (STABLE, вне списка
  `ADR-081 §8`) приходит СБОРКОЙ по текстам-кандидатам
  `reference/deploy_order_adj_2026-08-03.md §12`. Тем самым закрываются оба условия строки
  `PARITY-ZONE-SNAPSHOT-DECISION`. accepted (архитектор).

  §11 **Гейт закрытия мая снят офлайн; правки денег не меняли.** Прежняя квалификация («требует
  живого запроса к источнику, класс B») была ошибкой: различающие данные уже лежали в репо, и
  посуточное сравнение замороженной нашей стороны с более поздним снимком источника отвечает на
  вопрос напрямую. Расхождение по маю целиком объясняется строками-сиротами `ADR-100 §1`
  (арифметика замыкается с остатком `−0,73` KGS), свободного места под денежный эффект июльских
  правок нет. Утрата по маю — неденежная. Границы вердикта названы в
  `reference/deploy_order_adj_2026-08-03.md §13.3`: сравнение посуточное, подокументное невозможно по
  схеме таблицы; что именно поменялось в трёх документах, не устанавливалось. Три варианта закрытия
  мая, их цена и рекомендация (вариант 1 — датированный снимок) — `§14` того же артефакта. **Владелец
  выбрал вариант 1 (чат 2026-08-03); май закрыт** записью `reference/may_zone_close_2026-08-03.md` и
  абзацем в `reference/parity_registry.md`. Разность `1 630 239,87` KGS этим НЕ закрыта — она есть
  отдельный дефект `MERGE` без ветки удаления (`ADR-100 §1`, `C2`). Вывод `§9`-хода о выросшей срочности постоянного режима досева снимается: режим
  возвращается в роль страховки. accepted (архитектор — фактура и квалификация; владелец — выбор
  варианта 1, чат 2026-08-03).

  **Последствия:**
  - [сейчас] `07_STATE §Открытые вопросы` и `07_GAPS.md` — заводятся строки `DEPLOY-PROCEDURE`
    (PARTIAL: текст записан, остаток — первое применение); строка `MAY-ZONE-CLOSE` заводится и
    архивируется тем же проходом (открыта и закрыта одним блоком), одним коммитом в оба файла
    (`ADR-106 §2`).
  - [сейчас] `07_STATE §Мандат Claude Code: класс задач` — три строки: `DEPLOY-PROCEDURE, подготовка`
    (A, параллель нет, постоянный, Пишет: `reference/deploy_procedure_<date>.md`,
    `05_CONVENTIONS.md` отдельным коммитом);
    `DEPLOY-PROCEDURE, первое применение` (B, НЕ выдан — исполняется вместе с первым деплоем);
    `MAY-ZONE-CLOSE` (A, параллель нет, постоянный — заводится и убирается тем же коммитом вместе
    с задача-строкой, `ADR-090 §1`. Пишет: `reference/parity_registry.md`,
    `reference/may_zone_close_2026-08-03.md`).
  - [сейчас] `07_GAPS.md` строка `PARITY-ZONE-SNAPSHOT-DECISION` → `DONE`, перенос в `07_ARCHIVE.md`
    дословно; парная строка таблицы мандата убирается (`ADR-090 §1`).
  - [сейчас] `02_ERP_CONTRACTS.md §core.fact_sales_profit` — плашка датированного снимка по
    тексту-кандидату 1 (`reference/deploy_order_adj_2026-08-03.md §12`).
  - [сейчас] `02_ERP_CONTRACTS.md §оракул`, строка `reference/parity_registry.md` — дописка про
    однократный съём по каденции, текст-кандидат 2 там же; существующий текст ячейки не переписывается.
  - [сейчас] `05_CONVENTIONS.md` Часть II — подстановка присвоенного номера ADR вместо формулировки
    «номер подставляется сборкой» (прецедент `ADR-106 §7`).
  - [отдельным коммитом] `05_CONVENTIONS.md` Часть II, раздел о процедуре деплоя — **внесён этой
    сессией** коммитом `1f8cf3c` (`ADR-081 §8`).
  - [сейчас] `07_STATE`/`07_GAPS.md`/`07_ARCHIVE.md` — строка `DEPLOY-ORDER-ADJ` заводится и
    архивируется тем же проходом (открыта и закрыта одним блоком).
  - [сейчас] `07_GAPS.md` строка `CODE-REPO-SEED-REST` → `DONE`, перенос в `07_ARCHIVE.md` дословно;
    парная строка таблицы мандата убирается (`ADR-090 §1`).
  - [сейчас] `07_GAPS.md` строка `SALES-REFRESH-WINDOW` — снимается хвост «выбор варианта — владелец»,
    заменяется дословной ссылкой на решение `ADR-101 §10` (вариант 3 сейчас, вариант 1 цель);
    указывается, что вопрос `UPDATE transaction_date` закрыт dry-run.
  - [сейчас] `07_GAPS.md` строка `INGEST-MOMENT-ZONE-FIX` — снимается дописка «подготовка ТОЛЬКО после
    закрытия `Q-92`» (`ADR-091 §7`, superseded `ADR-101 §6`), приводится в соответствие со строкой
    таблицы мандата.
  - [сейчас] `07_GAPS.md` строка `INVOICES-LOADER-DEPLOY` — дописывается предусловие: патч обязан быть
    в код-репо, процедура переноса назначается `DEPLOY-PROCEDURE`.
  - [сейчас] `07_ARCHIVE.md` — строка `MAY-ZONE-CLOSE` дословно (DONE тем же блоком).
  - [задачей INVOICES-LOADER-DEPLOY] первое применение процедуры, оно же её приёмка.
  - [задачей DQ-GATE-RECOVERY-CONFIRM] вердикт о восстановлении потока и о необходимости
    `FACTS-BACKFILL`; настоящим решением не предвосхищается.
  - [без правок] `06_DECISIONS_LOG.md` прошлые ADR — `ADR-065` не пересматривается, его развилка
    исполняется, а не отменяется; `ADR-113` подтверждается и не меняется.
  - [без правок] `reference/dq_gate_behavior_probe_2026-08-02.md`,
    `reference/sales_refresh_window_2026-08-01.md` — датированы, не переписываются (`ADR-043`).

  Статус: accepted (архитектор — §1-§3, §5-§8, §10, §11-фактура; §4 — владелец, вариант Б,
  чат 2026-08-03; §9 — владелец, вариант 1, чат 2026-08-03, фактура и запасной путь — архитектор;
  §11 — владелец, вариант 1, чат 2026-08-03)

## NEW_CONVENTIONS

нет

=== END SESSION ===
