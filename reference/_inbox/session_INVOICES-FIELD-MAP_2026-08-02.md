=== SESSION LOG · 2026-08-02 · INVOICES-FIELD-MAP ===

## SESSION_LOG
- Задача: `INVOICES-FIELD-MAP` (T1, программа `ADR-110`) — соответствие полей `entity/invoiceout` → `core.fact_customer_invoices` + правило пересчёта валюты
- Сделано:
  - Подтверждено: ни один снапшот `reference/code/` не парсит `entity/invoiceout` (ожидаемо, `ADR-110 §0`)
  - Живой `GET` (класс A, `msklad-token` резолвлен inline): `entity/invoiceout?limit=1` (28 top-level полей) и `?limit=5&expand=state,agent,salesChannel` (форма `state`/`agent`/`salesChannel`, разброс `rate`)
  - Выведено построчное соответствие всех 14 колонок `core.fact_customer_invoices` с провенансом
  - Правило суток `moment` названо как функция UTC (`DATE(M+6ч)` по Бишкеку, по образцу верной ветки продаж), payment_planned — то же правило по аналогии, с пониженной уверенностью, явно названной
  - Правило пересчёта валюты подтверждено: `ADR-010` применим к `entity/invoiceout` без модификации; `rate=null` на выборке встретился только у документов в базовой валюте (KGS), что согласуется с каноном, не противоречит
  - Найдено и задокументировано: ни `load_payments.py` (образец стиля), ни живой снапшот `reference/code/cf-finance/main.py` — НЕ образец конвертера валюты, оба несут дефектную форму `÷100` без `× rate.value`; актуальная задеплоенная ревизия `cf-finance-00012-cik` эту дефектную форму не несёт (`DEPLOY-REVISION-PROBE`, `06_DECISIONS_LOG.md:1946`), но снапшот в репо не обновлён с ревизии `00006-piv`
  - `unpaid_sum_kgs` распознан как производная величина (`sum_kgs − payed_sum_kgs`), не отдельное поле API
  - Артефакт: `reference/invoices_field_map_2026-08-02.md`
  - Процессная находка вынесена архитектору: `reference/architect_review_queue_2026-08-02-3.md` (устаревший снапшот `reference/code/cf-finance/`)
- Команды/логи ключевые: `reference/_scratch_INVOICES-FIELD-MAP_2026-08-02/step1_run.log`, `step2_run.log` (оба несут `date -u`/`gcloud auth list` первой и последней командой); `grep -rF "$TOKEN" reference/_scratch_INVOICES-FIELD-MAP_2026-08-02/` → 0 совпадений (токен не закоммичен)
- Отклонения от плана: нет — все шаги брифа выполнены, ни один `CONTEXT GAP` не потребовал остановки; открытые пункты названы явно в артефакте §6, ни один не блокирует T1

## STATE_PATCH
- Задача `INVOICES-FIELD-MAP`: READY → DONE
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: `INVOICES-FIELD-MAP` (T1 программы `ADR-110`) закрыта — соответствие 14 полей `entity/invoiceout`→`core.fact_customer_invoices` и правило пересчёта валюты выведены (`reference/invoices_field_map_2026-08-02.md`), `ADR-010` подтверждён применимым без модификации
  - Где мы: восстановление потока фактов остаётся приоритетом перед сверкой паритета «на сейчас» (`FACTS-WORKFLOW-STOP-DIAG`, `ADR-111`) — решение владельца о форме восстановления не получено; параллельно программа `ADR-110` продвинулась на один шаг — `INVOICES-LOADER-DESIGN` (T2) разгейчена
  - Следующий шаг: `INVOICES-LOADER-DESIGN` (T2, вход готов — этот артефакт); решение владельца о форме восстановления загрузки фактов (см. предыдущий стенд-ап), затем `FACTS-BACKFILL`, затем `DQ-SOURCE-CAPTURE`; параллельно без изменений — прочие READY-задачи `07_STATE §Мандат`, включая `SALES-MERGE-DRYRUN` (готов, класс A)
  - Развилки на владельце: решение о форме восстановления загрузки фактов; первый `push` код-репо в `holika-prod` (гейтит весь деплойный поток, включая T4 этой программы); ответ клиента по часовому поясу (`MSKLAD-TZ-PROPOSAL`)
  - Счётчик: пары реестра 2/7 сходятся · таблиц живых 15 из 26 (до восстановления, не переизмерялось) · строк реестра на критическом пути 23 из 63 · Epic M 5/7 фаз
- Подробности для модели: `INVOICES-FIELD-MAP` закрыта. Полный ход, провенанс, все находки — только
  `reference/invoices_field_map_2026-08-02.md` (артефакт самодостаточен для `INVOICES-LOADER-DESIGN`),
  здесь не пересказывается. Единственный факт, без которого сломается T2: **не брать
  `reference/code/cf-finance/main.py` (текущий снапшот в репо) и `load_payments.py` за образец
  конвертера валюты** — оба несут дефектную форму `÷100` без `× rate.value`; конвертер строить прямо
  по канону `ADR-010` (формула `reference/invoices_field_map_2026-08-02.md §4.2`), опционально по
  образцу `cf-loss-commission/main.py:79-94` (`_rate_and_currency`, тот же канон с fallback). Снапшот
  `reference/code/cf-finance/` устарел (ревизия `00006-piv` вместо актуальной `00012-cik`) — находка
  вынесена архитектору (`reference/architect_review_queue_2026-08-02-3.md`), T2 не блокирует.
  Второй факт: `unpaid_sum_kgs` — не поле API, вычисляется как `sum_kgs − payed_sum_kgs` после
  конвертации обеих величин одним курсом документа. Третий факт: правило суток для `payment_planned`
  — рабочая гипотеза по аналогии с `moment`, не подтверждено отдельным замером; T2 может принять как
  есть или запросить замер.
- Новые открытые вопросы: нет (пункты §6 артефакта не блокируют и не заводятся отдельными `Q`/задачами — они адресованы либо T2 напрямую, либо архитекторской очередью выше)
- Блокеры: нет
- updated_at: 2026-08-02
- обновил: исполнитель (сессия: INVOICES-FIELD-MAP)

## NEW_DECISIONS
- нет

## NEW_CONVENTIONS
- нет

=== END SESSION ===
