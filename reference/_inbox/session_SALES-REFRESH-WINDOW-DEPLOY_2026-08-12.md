=== SESSION LOG · 2026-08-12 · SALES-REFRESH-WINDOW-DEPLOY ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-REFRESH-WINDOW-DEPLOY — деплой патча варианта 1 в `cf-facts` + боевой прогон
  расширенного окна
- Сделано:
  - Пять предусловий `§3` мандата пройдены (П1-П5), все read-only, логи в
    `reference/_scratch_SALES-REFRESH-WINDOW-DEPLOY_2026-08-11/`.
  - Ветка код-репо `deploy/cf-facts-2026-08-11-refresh-window-v2` заведена от `master`, патч (4
    файла) перенесён, sha256 сверен, коммит `9ae9a84`, push подтверждён владельцем.
  - Деплой исполнен (подтверждён владельцем): новая ревизия `cf-facts-00014-doh`, read-back
    архива подтверждён побайтово (11 файлов, мусора нет). Трафик переведён на новую ревизию
    (подтверждено владельцем отдельно от деплоя) — живой на конец сессии.
  - `maxTimeTravelHours` датасета `core` замерен: `168` часов.
  - Боевой прогон исполнен в два захода: первый (`window_days=105`) оборвался на клиенте (владелец
    сообщил причину — ноутбук ушёл в сон посреди операции), но фактически довыполнился на сервере
    (staging загружен, впоследствии естественно перезаписан штатным конвейером до второго захода —
    ущерба не было, подтверждено сверкой с П2 ПЕРЕД вторым заходом, 0 расхождений по всем каналам).
    Второй заход (`window_days=106`, пересчитан на фактическую дату) исполнил все 4 вызова:
    `weekly`/`perimeter` — успех; `promote` (продажи) — успех, точная статистика MERGE из
    `INFORMATION_SCHEMA.JOBS`: `inserted=27, deleted=36, updated=7709`; `perimeter_promote` —
    предохранитель `ADR-145 §4` штатно отказал (периметрийный источник не покрывает глубину окна),
    MERGE не исполнился, периметр не тронут.
  - Из 36 удалённых строк сравнения по каналам: `28` — «оптовая торговля» (`460 921,67` KGS), `6` —
    без канала, `2` — «К Глобал РФ Маркетплейсы». Третье число приёмки мандата (ожидание `0` по
    оптовой торговле) — **не выполнено**.
  - Владелец явно распорядился НЕ откатывать немедленно (как предписывает мандат при ненулевом
    третьем числе), а собрать полную фактуру для архитектора, включая причину обрыва первой попытки
    (сон ноутбука). Собран и закоммичен провенанс-артефакт
    `reference/sales_refresh_window_deploy_v2_2026-08-12.md` (5 предусловий, деплой, оба захода
    прогона, точная MERGE-статистика, построчный список 36 удалённых строк, наблюдение о том, что
    штатный часовой прогон в это же окно времени дал `0` удалений, открытый вопрос архитектору).
    `reference/code/cf-facts/MANIFEST.md` дополнен секцией второго захода.
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-DEPLOY_2026-08-11/` (все скрипты
  и логи сессии), `reference/sales_refresh_window_deploy_v2_2026-08-12.md`.
- Отклонения от плана: да — приёмка не подтверждена, откат по мандату НЕ исполнен (прямое указание
  владельца, отступление от предписанного мандатом действия при ненулевом третьем числе). Ветка
  код-репо `deploy/cf-facts-2026-08-11-refresh-window-v2` НЕ слита в `master`. Трафик `cf-facts`
  остаётся на пропатченной ревизии `cf-facts-00014-doh` на момент закрытия сессии — открытый живой
  риск, требует решения архитектора/владельца в следующей сессии.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-REFRESH-WINDOW, деплой: мандат ВЫДАН/исполнялся → ИСПОЛНЕН ЧАСТИЧНО, приёмка
  провалена (третье число ненулевое), откат отложен владельцем до адъюдикации архитектора.
  Статус для таблицы мандата: не менять класс/мандат — задача НЕ закрыта, требует продолжения
  (архитекторская сессия адъюдикации, затем либо откат, либо иное решение).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком):
  - Прошлый шаг: деплой `cf-facts` (патч варианта 1, ревизия `cf-facts-00014-doh`) исполнен и
    подтверждён read-back; боевой прогон расширенного окна дал ненулевое третье число приёмки (28
    удалённых строк «оптовая торговля»); откат НЕ исполнен по прямому указанию владельца — материал
    передан архитектору (`reference/sales_refresh_window_deploy_v2_2026-08-12.md`).
  - Где мы: живой риск открыт — пропатченный код на 100% трафика, ветка код-репо не слита,
    причина 28 удалений не установлена (гипотезы: легитимное изменение источника vs дефект выборки
    на длинных окнах — не различены).
  - Следующий шаг: архитекторская сессия адъюдикации `SALES-REFRESH-WINDOW-DEPLOY-ADJ` (или
    аналогичное имя) — решить между (1) откат немедленно, (2) короткое расследование через прямой
    запрос к API МойСклад по конкретным `transaction_id`, затем откат, (3) иное. Прочитать полный
    провенанс до решения.
  - Развилки на владельце: да — уже частично снята прямым указанием (не откатывать до адъюдикации);
    финальное решение (откат vs расследование vs иное) — на архитекторе по этому указанию.
  - Счётчик: без изменений этим проходом (не задача этой сессии).
- Новые открытые вопросы: см. ниже (GAP-реестр).
- Блокеры: живой риск — трафик `cf-facts` на пропатченной ревизии, штатный часовой конвейер
  продолжает исполняться до решения; снимок `core.fact_sales_profit_snap_20260811_163306` и
  time-travel (`168`ч) — единственные пути восстановления `36` удалённых строк, оба стареют.
- updated_at: 2026-08-12
- обновил: исполнитель (сессия: SALES-REFRESH-WINDOW-DEPLOY)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет — решение по причине и дальнейшему действию делегировано архитектору явно, эта сессия ADR не
принимает.

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

---

## Новая строка GAP-реестра (для 07_STATE §Открытые вопросы + 07_GAPS, заводится сборкой)

**ID:** предлагается `Q-109` (номер присваивает сборка)
**Статус:** OPEN
**Вопрос одной фразой:** боевой прогон `SALES-REFRESH-WINDOW-DEPLOY` (окно 106д) удалил 28 строк
канала «оптовая торговля» (460 921,67 KGS) — причина не установлена (легитимное изменение источника
vs дефект выборки на длинных окнах), откат отложен владельцем до адъюдикации архитектора.
**Гейт:** блокирует слияние ветки `deploy/cf-facts-2026-08-11-refresh-window-v2` в `master` и
дальнейшие боевые прогоны расширенного окна; НЕ блокирует штатный часовой конвейер (по факту
единственного наблюдавшегося часового прогона на пропатченном коде — `0` удалений).
**Полный текст (для `07_GAPS`):** см. `reference/sales_refresh_window_deploy_v2_2026-08-12.md`
целиком, особенно `§5-§9`.

=== END SESSION ===
