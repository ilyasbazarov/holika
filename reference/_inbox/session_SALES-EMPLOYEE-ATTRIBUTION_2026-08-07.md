=== SESSION LOG · 2026-08-07 · SALES-EMPLOYEE-ATTRIBUTION ===

**База:** `git rev-parse HEAD` на старте = `5c72708` (`docs: сборка буфера 2026-08-06`).
**Найдено при старте:** дерево `worktrees/PARITY-STOCK-SNAPSHOT-SYNC` / ветка `s/PARITY-STOCK-SNAPSHOT-SYNC`
не слиты (стоп-условие `ADR-081 §6`); владелец явно распорядился игнорировать и продолжить — сессия пошла
поверх этого решения, ветка/дерево этой сессией не трогались.

## SESSION_LOG

- Задача: `SALES-EMPLOYEE-ATTRIBUTION` — разбор разреза продаж по сотруднику, закрытие остатка `Q-78`.
  Исполнена без брифа по `ADR-086 §1`, определение процитировано дословно из `07_GAPS.md` в тексте
  запуска.
- Сделано:
  1. Посеян датированный снимок эталона уровня (iii) — отчёт МойСклад по Турдалиевой за июль, разрез по
     контрагентам, предоставлен владельцем — `reference/oracle_sales_july_2026-08-05.md`.
  2. Сверка множества контрагентов «источник vs наша корзина Турдалиевой»: 23 совпадают точно (та же
     сумма с точностью до округления), 7 расходятся (у нас — за другим менеджером либо без менеджера).
  3. Гипотеза `ADR-126 §3` («источник режет по документу, мы — по контрагенту») подтверждена частично:
     множества контрагентов предсказывают друг друга, но перестановка семи расхождений по деньгам не
     закрывает разрыв в ноль (перелёт на `1 194 360,00` KGS при недостаче `1 177 927,90` KGS) — то есть
     правило тоньше уровня контрагента, необходим разрез по документу.
  4. Состав правки загрузчика спроектирован (не применён): поле `owner.meta.href` у `entity/demand`
     подтверждено чтением сырых майских тел без сети; правка — по аналогии с уже читаемым `agent_id`.
  5. Офлайн-проверка на мае предпринята: поле `owner` населено на всех 127 майских документах, но
     обнаружена и НЕ примирена нестыковка суммы `sum` документа против `Σ revenue_kgs` его позиций —
     зафиксирована как новый `CONTEXT GAP`, не как опровержение гипотезы.
  6. Развилка для владельца предъявлена (§5 артефакта): правка загрузчика / документирование как
     легитимного отступления / гибрид — решение НЕ принято этой сессией.
- Артефакты: `reference/oracle_sales_july_2026-08-05.md`,
  `reference/sales_employee_attribution_2026-08-07.md`,
  `reference/_scratch_SALES-EMPLOYEE-ATTRIBUTION_2026-08-07/` (два скрипта, два лога, JSON-выдачи,
  построчный дамп xls).
- Команды/логи ключевые: `step1_compare.sh` (июльская сверка, UTC `18:41:29Z…18:42:59Z`),
  `step2_may_check.sh` (майская офлайн-проверка, UTC `18:46:29Z…18:46:39Z`); оба — auth
  `ilyasbazarov4@gmail.com`, совпала в начале и в конце (`ADR-055 §4`).
- Отклонения от плана: пункт 4 брифа-постановки («проверяемо ли офлайн на мае») исполнен ЧАСТИЧНО —
  проверка предпринята, но не завершена из-за новой нестыковки данных (см. артефакт §4); решение по
  развилке §5 не принято (не входило в мандат этой сессии — постановка прямо запрещала выбирать
  правило).

## STATE_PATCH

- Задача `SALES-EMPLOYEE-ATTRIBUTION`: `OPEN` → `OPEN` (диагностика и проект исполнены; деплой
  правки — отдельное решение владельца + класс B, мандат не выдан).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: разрез по сотруднику разобран поимённо на данных клиента — гипотеза `ADR-126 §3`
    подтверждена частично, состав правки загрузчика спроектирован, развилка предъявлена владельцу
    (`reference/sales_employee_attribution_2026-08-07.md`).
  - Где мы: шесть пар реестра из семи сходятся; строка 21 («Остатки товаров») ждёт `PARITY-STOCK-SNAPSHOT-SYNC`
    шаг 2; остаток `Q-78` (разрез по сотруднику) ждёт решения владельца по развилке правила разреза.
  - Следующий шаг: владелец выбирает вариант развилки (§5 артефакта); `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2
    (класс B, мандат на запись не выдан); `SALES-INGEST-PATCH-DEPLOY` (класс B, мандат дан в чате,
    требует объявления действия и брифа); `INTRANSIT-SURFACE-LAG` (класс A).
  - Развилки на владельце: какой вариант разреза по сотруднику выбрать (правка загрузчика / легитимное
    отступление / гибрид, `reference/sales_employee_attribution_2026-08-07.md §5`); мандат класса B на
    принудительный прогон `cf-inventory`; правка `04_ROADMAP`, переставшего описывать очередь.
  - Счётчик: пары реестра **6/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели: Офлайн-проверка на мае (задача 4 постановки) наткнулась на нестыковку:
  Σ поля `sum` 127 майских документов `entity/demand` (`52 647 606,84` KGS) не пропорциональна
  Σ`revenue_kgs` тех же документов на уровне позиций (`93 522 995,53` KGS, `ADR-097 §1`) — коэффициент
  различается по сотрудникам-владельцам документа от `23%` до `127%`, единым множителем (курс/налог/
  округление) не объясняется. Вероятная причина — `sum` документа и Σ его позиций считаются по разным
  основаниям (в сохранённых JSON `positions` не развёрнуты, только ссылка), но это не подтверждено
  измерением. Следующая сессия, которой понадобится майский разрез по сотруднику документа, ПЕРЕД
  использованием обязана либо получить снимок с `expand=positions` (класс B), либо иначе подтвердить
  сравнимость `sum` и `Σ revenue_kgs` — иначе рискует унаследовать невалидное сравнение.
  Полный ход и все числа — `reference/sales_employee_attribution_2026-08-07.md` (артефакт самодостаточен,
  здесь не пересказывается).
- Новые открытые вопросы: `SALES-EMPLOYEE-ATTRIBUTION-MAY-SUM-GAP` — нестыковка `sum` документа vs
  `Σ revenue_kgs` позиций за май (см. выше), блокирует майскую офлайн-проверку разреза по сотруднику,
  не блокирует июльский вывод (тот подтверждён иначе, по множеству контрагентов, не по сумме документа).
- Блокеры: нет (развилка §5 — решение владельца, не блокер диагностики).
- updated_at: 2026-08-07
- обновил: архитектор (сессия: `SALES-EMPLOYEE-ATTRIBUTION`)

## NEW_DECISIONS

- ADR-0XX: Разрез продаж по сотруднику — гипотеза `ADR-126 §3` подтверждена частично данными клиента;
  простая перестановка контрагентов не закрывает разрыв в ноль; необходим разрез на уровне документа
  Контекст: `ADR-126 §3/§4` оставил версию «источник режет по документу, мы — по контрагенту» гипотезой
  с названным различителем (сверка отчёта Турдалиевой по контрагентам). Владелец предоставил этот отчёт
  2026-08-06/07.
  Решение: (1) множество контрагентов у источника и у нас совпадает на 23 из 30 точно, семь расходятся
  по закреплению менеджера — гипотеза подтверждена НАПРАВЛЕНИЕМ; (2) перестановка семи расхождений по
  деньгам даёт перелёт на `1 194 360,00` KGS против недостачи `1 177 927,90` KGS — правило источника
  режет тоньше уровня контрагента, воспроизвести его без данных документа нельзя; (3) состав правки
  загрузчика (поле `owner.meta.href` документа `entity/demand`, паттерн идентичен уже читаемому
  `agent_id`) спроектирован, не применён; (4) офлайн-проверка на мае частично исполнена, поле `owner`
  подтверждено населённым, но сравнение по сумме упёрлось в новый неразрешённый gap (заведён отдельной
  строкой); (5) выбор правила разреза (правка загрузчика / легитимное отступление моста / гибрид) —
  развилка владельца, этим решением НЕ закрывается.
  Последствия:
  - [сейчас] `reference/oracle_sales_july_2026-08-05.md`,
    `reference/sales_employee_attribution_2026-08-07.md`,
    `reference/_scratch_SALES-EMPLOYEE-ATTRIBUTION_2026-08-07/` — внесены сессией в ветке.
  - [сейчас] `07_GAPS.md` строка `SALES-EMPLOYEE-ATTRIBUTION` — дописка о частичном подтверждении
    гипотезы и ссылка на артефакт; статус остаётся `OPEN` (решение развилки не принято).
  - [сейчас] `07_GAPS.md` строка `Q-78` — дописка: различитель `ADR-126 §3` исполнен, направление
    гипотезы подтверждено, точного схождения в ноль на уровне контрагента не существует по построению;
    статус остаётся `PARTIAL` до решения развилки.
  - [сейчас] `07_STATE.md §Открытые вопросы` — новая строка `SALES-EMPLOYEE-ATTRIBUTION-MAY-SUM-GAP`
    (факт, не решение; закрывается независимым замером `expand=positions` либо подтверждением
    сравнимости `sum`/`Σ revenue_kgs`).
  - [задачей владельца] Выбор варианта развилки §5 артефакта — не заводится отдельной задачей этим
    решением; следующая сессия, которой владелец сообщит выбор, заводит либо правку загрузчика, либо
    правку реестра паритета (легитимное отступление), по факту выбора.
  Статус: accepted (архитектор — §1-§4 фактура и проект; §5 развилка не решена, остаётся открытой).

## NEW_CONVENTIONS

- нет

=== END SESSION ===
