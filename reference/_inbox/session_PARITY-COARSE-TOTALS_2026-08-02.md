=== SESSION LOG · 2026-08-02 · PARITY-COARSE-TOTALS ===

**SHA старта:** `b86f0eba0d8843e17789f1b85f4cdcb531f95f36` · **Дерево/ветка:** `worktrees/PARITY-COARSE-TOTALS` / `s/PARITY-COARSE-TOTALS`
**Класс задачи:** B, мандат выдан поимённо `ADR-108 §1` (`reference/process_freeze_2026-08-02.md §5`).

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `PARITY-COARSE-TOTALS` — грубые итоги трёх пар реестра паритета, не измерявшихся ни разу (остатки товаров, заказы поставщикам «в пути», счета покупателям)
- Сделано:
  - Шаг 1: зерно пары «Счета покупателям» установлено по тексту SQL — снимок «на сейчас» без фильтра по дате (подтверждено `03_PIPELINE_SPEC.md:240`)
  - Шаг 2: наша сторона всех трёх витрин снята одним заходом BigQuery (окно `2026-08-02T09:20:24Z…09:20:54Z` + добавочный запрос числа счетов `09:29:05Z…09:29:08Z`)
  - Шаг 3: живые `GET` к МойСкладу — `report/stock/all` (313 строк), `entity/purchaseorder` фильтр по статусам (19 заказов, позиционная формула из `reference/code/cf-facts/fetch_purchases.py`), `entity/invoiceout` (4526 документов, 46 страниц, без конвертации валют) — окно `2026-08-02T09:24:27Z…09:28:04Z`
  - Шаг 4: три разности посчитаны и записаны
  - Шаг 5: артефакт `reference/parity_coarse_totals_2026-08-02.md` записан; строки 21–23 `reference/parity_registry.md` (только колонка «Статус сходимости») обновлены — diff файла подтверждён (3 строки изменены, остальные не тронуты)
- Команды/логи ключевые: `reference/_scratch_PARITY-COARSE-TOTALS_2026-08-02/{step1_grain,step2_our_side,step2b_invoice_count,step3_source_side}.{sh,py,log}` + сырые тела ответов API + `grep_token_check.log` (0 совпадений токена перед `git add`)
- Отклонения от плана: нет. Один процессный нюанс: Шаг 2 дополнен коротким read-only запросом `step2b_invoice_count.sh` (SUM(invoice_count) на нашей стороне) — не отдельный шаг брифа, а добавка к уже исполненному Шагу 2 для сопоставимости числа счетов; облачных операций не-идемпотентного характера в добавке нет.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `PARITY-COARSE-TOTALS`: READY → DONE
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком):
  - Прошлый шаг: грубые итоги всех трёх неизмеренных пар реестра паритета получены одним заходом класса B (`ADR-108`); артефакт `reference/parity_coarse_totals_2026-08-02.md`, строки 21–23 `reference/parity_registry.md` обновлены
  - Где мы: из семи пар реестра паритета закрыта одна, измерено шесть (три построчно/структурно, три — грубым итогом); ни одна из шести измеренных пар не сходится к 0,00 кроме пары «Заказы в пути» (сошлась 0,00 грубым итогом); построчная сверка трёх новых пар и адъюдикация правил-мостов — следующий шаг
  - Следующий шаг: архитекторская адъюдикация урожая волны 2 (`07_STATE §Текущий фокус`, волна 3) — правила-мосты `Q-80`/`Q-81`/`Q-82` и проверка гипотезы «одна причина на несколько пар»; параллельно класс A — задачи, названные предыдущим стенд-апом, не тронутые этой сессией
  - Развилки на владельце: без изменений (см. предыдущий стенд-ап — `CODE-REPO-STANDUP` шаг 2b, `MSKLAD-TZ-PROPOSAL`, мандат на `AUDIT-SNAPSHOT-FIX-EMPLOYEES`/`AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`)
  - Счётчик: пары реестра 1/7 сходятся (формально; пара «в пути» дала грубый итог `0,00`, но это не построчная сверка и не адъюдикация — статус «сходится» этой сессией не присваивается) · измерено 7/7 (все пары реестра хотя бы раз измерены — три структурно/построчно, три грубым итогом) · строк реестра на критическом пути 23 из 63 (без изменений, не трогалось этой сессией) · Epic M 5/7 фаз
- Подробности для модели:
  Три числа по каждой паре (источник / наша сторона / разность), окно UTC и провенанс — только в
  `reference/parity_coarse_totals_2026-08-02.md`, не пересказывается здесь. Критическое для
  следующей сессии: (i) пара «Заказы поставщикам в пути» сошлась **0,00** на грубом итоге —
  позиционная формула `reference/code/cf-facts/fetch_purchases.py` (rate.value-конвертация),
  применённая к свежим данным API, дала сумму, побитово совпавшую с `marts.in_transit`; это
  сильный позитивный сигнал для `Q-81` (гейт «прогон сверки против `entity/purchaseorder`»),
  но НЕ построчная сверка (только 19 заказов агрегатом) и статус `Q-81` этой сессией не менялся.
  (ii) Пара «Счета покупателям» держит три расхождения одного порядка (7,6–12,8%) и заметно
  меньшее число документов на нашей стороне (4058 из 4526 источника, `−10,3%`) — совпадает с уже
  известной цифрой `07_STATE §Контрольные цифры` («`core.fact_customer_invoices`: 4058 записей»),
  то есть перекос не новый факт, а давно известный, просто впервые сопоставленный с живым
  источником. `43,4%` документов источника — не в KGS, конвертация не применялась (`Q-82`,
  загрузчик не установлен) — часть расхождения по суммам может быть эффектом именно этого, гипотеза
  НЕ проверялась. (iii) Пара «Остатки товаров» разошлась на `0,12%` (138 штук из 115 887) при
  расхождении числа строк на 1 (313 источник vs 312 наша сторона) и при устаревшей на сутки
  витрине (снимок `2026-08-01` против живого источника `2026-08-02`) — витрина не пересобиралась
  ради замера (ловушка L3 брифа), это часть результата.
- Новые открытые вопросы: нет (все три пары уже несут строки `Q-80`/`Q-81`/`Q-82` в GAP-реестре; эта сессия добавляет измерение, не новый вопрос)
- Блокеры: нет
- updated_at: 2026-08-02
- обновил: исполнитель (сессия: PARITY-COARSE-TOTALS)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
