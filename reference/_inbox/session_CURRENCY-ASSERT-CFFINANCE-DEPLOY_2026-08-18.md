=== SESSION LOG · 2026-08-18 · CURRENCY-ASSERT-CFFINANCE-DEPLOY ===

## SESSION_LOG

- Задача: `CURRENCY-ASSERT-CFFINANCE-DEPLOY` — выезд `cf-finance` с валютной детекцией
  (под-задача `6в` списка закрытия). **Класс B, мандат выдан владельцем поимённо ЗАНОВО после
  правки** (чат 2026-08-18), форма — `reference/currency_assert_cffinance_launch_2026-08-18.md §1`.
  База `17d830d`.
- Сделано: ревизия **`cf-finance-00014-gub`** выложена и обслуживает `100 %` трафика; приёмка
  закрыта по всем четырём пунктам; запись «ревизия ↔ коммит» внесена в
  `reference/code/cf-finance/MANIFEST.md` по read-back архива. Артефакт —
  `reference/currency_assert_cffinance_deploy_2026-08-18.md`.
  - Прод-коммит `e6b9627` установлен ПОБАЙТОВОЙ сверкой скачанного архива обслуживающей ревизии
    с коммитами код-репо, а не по записи манифеста; голова `master` по `cf-finance` равна архиву —
    дрейфа нет.
  - Все семь предусловий плюс `П10` (`is-ancestor`) закрыты фактами ДО деплоя.
  - Первый прогон новой проверки дал `34` предупреждения — находка, разобрана ниже.
- Команды/логи ключевые: `step1_preconditions.sh` (живой `describe`, скачивание архива прода,
  клон код-репо, поиск прод-коммита), `step2_branch_and_checks.sh` (ветка, перенос одного файла,
  `git diff --stat` = `1 file`, `is-ancestor` истина, секретов `0` при диффе `84` строки, импорт
  по ветке `ALL IMPORTS OK`), `step3_baseline.sh` (`5175` строк до), `step4_deploy.sh` (оборван
  таймаутом инструмента), `step5_diagnose.sh` (read-only: прод не задет), `step6_deploy_retry.sh`
  (доисполнение хвоста), `step7_readback.sh` (`ALL_MATCH=1`, трафик `100 %`),
  `step8_payments_call.sh` (вызов, оборван таймаутом инструмента), `step9`/`step10`/`step11`
  (read-only: журнал и таблица). `bash tools/hooks/selftest.sh` — `пройдено 36, провалено 0`.
- Отклонения от плана: два, оба названы и оба разобраны read-only ДО любого повтора.
  (1) Первый заход деплоя оборван таймаутом инструмента (2 минуты) — облако не задето, ревизия
  прежняя, новых сборок нет; хвост доисполнен без повторного `push`. (2) Вызов режима `payments`
  оборван таймаутом инструмента (10 минут) при собственном таймауте функции `1800` с; повторный
  вызов НЕ делался, исход снят журналом и таблицей. Следствие: HTTP-код ответа не снят — форма
  пункта `3` приёмки отличается от записанной в мандате, закрыт более сильным свидетельством.
  (3) Клон код-репо при первом коммите лёг в индекс ССЫЛКОЙ на внешний репозиторий
  (`160000`), то есть его содержимое в наш репозиторий не попало бы, и провенанс ветки деплоя
  оказался бы невосстановим — в отличие от прошлых сессий, где клон лежит файлами. Поймано
  предупреждением `git` и проверкой `git ls-files -s`, исправлено ДО конца сессии: ссылка убрана
  из индекса, содержимое ветки деплоя положено плоскими файлами
  (`deployed_branch_content/` — три файла плюс `BRANCH_COMMIT.txt`; `sha256` `main.py` совпадает
  с выложенным архивом), сам клон вынесен за пределы репозитория.

## STATE_PATCH

- Задача `INGEST-CURRENCY-ASSERT`, строка «шаг 3, выезд `cf-finance`» таблицы
  `§Мандат Claude Code`: мандат ВЫДАН ЗАНОВО → **ИСПОЛНЕН**. Ревизия `cf-finance-00014-gub`,
  приёмка закрыта по четырём пунктам. Строка убирается из таблицы тем же коммитом, что и
  соответствующая часть строки реестра (`ADR-090 §1`).
- Задача `INGEST-CURRENCY-ASSERT` (строка реестра, `07_GAPS.md:57` + индекс `07_STATE.md`):
  PARTIAL → PARTIAL, остаток сужается. Новый текст остатка: «шаги 1-2 DONE (2026-08-13); выезд
  `cf-finance` ИСПОЛНЕН `2026-08-18` (ревизия `cf-finance-00014-gub`, детекция работает в проде);
  остаток — ОДИН выезд `cf-facts`, класс B, мандат НЕ выдан, порядок — после ступени `3`
  `SALES-REFRESH-WINDOW-DEPLOY-FINAL`». **Клауза «дополнительно гейтится `ADR-065`» снимается как
  протухшая** (гейт снят `ADR-115 §2`) — во всех трёх якорях сплошным поиском (`ADR-083 §2`).
- Под-задача `6в` списка закрытия — ЗАКРЫТА. Строка `6` списка: `1` из `3` → `2` из `3`; строка
  целиком НЕ закрывается (остаётся `6б`). Официальный счётчик (`ADR-156 §6`) не двигается:
  осталось `3` строки из `9`.
- Задача `CURRENCY-ASSERT-CFFINANCE-DEPLOY`: заводится и архивируется ОДНИМ проходом сборки
  вместе с парной строкой мандата (класс B, параллель `нет`, мандат ВЫДАН и ИСПОЛНЕН,
  Пишет: `reference/currency_assert_cffinance_deploy_<date>.md`,
  `reference/_scratch_CURRENCY-ASSERT-CFFINANCE-DEPLOY_<date>/`, `reference/code/cf-finance/MANIFEST.md`,
  `reference/handover_limitations.md`).
- **Открытое действие владельца:** слияние ветки `deploy/cf-finance-2026-08-18-currency-assert` в
  `master` код-репо. Read-back успешен, то есть условие процедуры выполнено; запрета, аналогичного
  `cf-facts` (`Q-121`), у `cf-finance` НЕТ — линия `cf-facts` этой веткой не затрагивается.
  Слияние ждёт подтверждения владельца и держит инвариант «основная ветка = прод» открытым.
- Подробности для модели:
  - **Первый прогон детекции дал `34` предупреждения с ОДНИМ и тем же идентификатором валюты и
    `iso=None`** — идентификатора нет в справочнике `entity/currency`. Ложным срабатыванием из-за
    недоступности справочника это не является (тогда печаталось бы «детекция НЕ выполнялась»), и
    дефектом патча тоже: правило `ADR-101 §5` отработало буквально. Из `22` идентификаторов хвоста
    журнала `21` найден в ядре: `2024-06-18`…`2024-09-17`, `378 517,09` сом — **весь объём вне
    окна паритета** (`с 2026-05-01`, `ADR-069`). Записано в реестр известных ограничений
    (`ADR-156 §2`), задачей не заводится.
  - **`trigger_marts()` отдаёт `403 PermissionDenied`** — задокументированное существующее
    поведение (`03_PIPELINE_SPEC §cf-finance`, non-fatal). Патчем не создано; всякая будущая
    сессия, увидев его в логе, не должна принимать это за след своей правки.
  - **Таймаут инструмента исполнителя — не таймаут облака.** Два обрыва этой сессии (деплой на
    2-й минуте, вызов на 10-й) оба были на стороне инструмента при живой операции в облаке.
    Форма, которая спасла: read-only диагностика ДО повтора, и доисполнение только недостающего
    хвоста. Долгие вызовы `cf-finance` (`timeout=1800` с) в лимит инструмента не укладываются
    by construction — исход снимается журналом и таблицей, не кодом ответа.
- Новые открытые вопросы: нет (`ADR-156 §5`).
- Блокеры: нет.
- updated_at: 2026-08-18
- обновил: исполнитель (сессия: `CURRENCY-ASSERT-CFFINANCE-DEPLOY`)

## NEW_DECISIONS

- нет (решений уровня ADR не принималось: мандат выдан ранее, форма патча адъюдицирована ранее,
  находка закрыта реестром ограничений по действующему правилу `ADR-156 §2`)

## NEW_CONVENTIONS

- нет

=== END SESSION ===
