=== SESSION LOG · 2026-08-07 · INTRANSIT-SURFACE-LAG ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `INTRANSIT-SURFACE-LAG` — замер отставания клиентской витрины «в пути» от ядра +
  находка по обнулению `in_transit_sum_kgs` у прибывших заказов (`ADR-124 §6`)
- Сделано:
  - Снят `date -u` первой командой сессии (`2026-08-06T19:15:36Z`, все дальнейшие времена — от него)
  - Вопрос 1(а): элапс `MAX(_loaded_at)` ядра против `MAX(_mart_refreshed_at)` витрины = `5 ч 52 мин
    56 сек`; возраст снимка на момент замера = `6 ч 07 мин 19 сек`
  - Вопрос 1(б): KGS-эффект на клиентской странице прямо сейчас = `0,00` (обе стороны
    `78 184 949,425` KGS, `251`/`251` ключей, симметрическая разность пуста); эффект зависит от фазы
    суточного цикла на момент замера, структурный риск до `~24` ч остаётся
  - Вопрос 2 (только данные BQ, без живого `GET`): у статуса «Прибыл» `94,5 %` строк (`3 835` из
    `4 057`) НЕ обнуляют `quantity_in_transit`/`in_transit_sum_kgs`, сумма незанулённого остатка
    `957 627 883,14` KGS; поведение источника по тем же позициям — остаток, класс B, не проверялось
  - Заполнена строка `sq_marts_in_transit` в `11_INFRA_FACTS.md §SQ`, свежее подтверждение якоря
    `~13:09Z` добавлено в колонку «Состояние»
  - Собран артефакт `reference/intransit_surface_lag_2026-08-07.md`
- Команды/логи ключевые: `reference/_scratch_INTRANSIT-SURFACE-LAG_2026-08-07/step{2,3,4}_*.sh/.log`
  (три read-only скрипта, `bq query --use_legacy_sql=false`, UTC-якорь и `gcloud auth list` в начале
  и в конце каждого)
- Отклонения от плана: нет

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `INTRANSIT-SURFACE-LAG`: OPEN → замерено, форма фикса по-прежнему НЕ выбрана (owner-gated,
  `ADR-124 §6`); закрытие строки — решение владельца/архитектора при разборе находки, не этой сессии
- Стенд-ап: не формирую (не архитектор; `07_STATE`/`06_DECISIONS_LOG`/`06_INDEX` не правлю —
  `ADR-080`, бриф §Роль)
- Подробности для модели: замер `INTRANSIT-SURFACE-LAG` (2026-08-07) даёт архитектору два числа для
  выбора формы фикса — на конкретном окне `~6` ч KGS-эффект нулевой (периметр и суммы идентичны), но
  это свойство МОМЕНТА замера (ранняя фаза суточного цикла), не доказательство отсутствия эффекта в
  худшей фазе (ближе к `24` ч); повторный замер перед следующей пересборкой (`~13:09Z`) даст более
  информативную верхнюю границу. Отдельно: находка про обнуление `in_transit_sum_kgs` у статуса
  «Прибыл» (`94,5 %` строк не обнуляются, `957 627 883,14` KGS вне периметра паритета) — факт о ядре,
  не о МойСкладе; различитель требует живого `GET` (класс B), не входил в мандат. Полный ход и числа
  — `reference/intransit_surface_lag_2026-08-07.md`.
- Новые открытые вопросы: нет (задача уже существовала строкой `INTRANSIT-SURFACE-LAG`, GAP не
  заводится; наблюдение про фазозависимость KGS-эффекта — не отдельный `Q`, это уточнение той же
  строки, `ADR-051 §2`)
- Блокеры: нет
- updated_at: 2026-08-07
- обновил: исполнитель (сессия: INTRANSIT-SURFACE-LAG)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
