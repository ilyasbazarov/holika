# FILE: reference/_inbox/session_PARITY-STOCK-SNAPSHOT-SYNC_2026-08-05.md

=== SESSION LOG · 2026-08-05 · PARITY-STOCK-SNAPSHOT-SYNC ===

## SESSION_LOG

- Задача: `PARITY-STOCK-SNAPSHOT-SYNC`, **шаг 3** (природа семи строк-сирот). Класс B, мандат
  `ADR-123 §8` (владелец, чат 2026-08-04). Бриф `briefs/PARITY-STOCK-SNAPSHOT-SYNC.md`.
  Шаг 3 от окна `21:00Z` не зависит и исполняется в любой час — так задан вариант А.
- База: `1683cc4`. Дерево/ветка: `worktrees/PARITY-STOCK-SNAPSHOT-SYNC` / `s/PARITY-STOCK-SNAPSHOT-SYNC`.
- UTC-окно: `2026-08-05T04:30:50Z…04:31:57Z`. Личность снята в начале и в конце каждого из двух
  скриптов, все четыре снятия совпали. Действие объявлено владельцу отдельным сообщением до
  исполнения (`ADR-077 §6`).
- Сделано: семь UUID извлечены программно из `step6_join_result.json`; по каждому — read-only
  `GET entity/product/<uuid>`. **Все семь карточек существуют (`200`), ни одна не архивирована.**
  Гипотеза «архивация карточки» (`ADR-123`, одна из двух названных веток) опровергнута фактом.
- **Ложный след пойман собственным контролем.** Все семь сирот несут `updated` одной и той же
  миллисекунды (`2026-08-03 15:15:17.683`). Вывод не сделан: три товара из СОВПАВШЕГО множества
  опрошены теми же запросами и несут ту же отметку — значит это массовая правка справочника
  целиком, а не свойство сирот. Признак снят до попадания в вывод.
- Шаг 2 НЕ исполнялся: окно `21:00Z` упущено (элапс на момент сессии `7ч 30м`, та же зона, что у
  двух прежних недопустимых замеров). Подтверждено прямым запросом: слепок партиции `2026-08-05`
  снят `2026-08-04T21:00:04Z`.
- Артефакт: `reference/parity_stock_snapshot_sync_2026-08-05.md`; провенанс —
  `reference/_scratch_PARITY-STOCK-SNAPSHOT-SYNC_2026-08-05/`.
- Отклонения от плана: два. (1) Добавлен контрольный опрос трёх не-сирот — сверх буквы шага 3,
  того же класса и только чтение; без него совпадение отметок ушло бы в вывод как находка.
  (2) Первый прогон контроля вернул НОЛЬ идентификаторов при `rc=0` — извлекатель не знал формы
  элемента (массив, а не строка/словарь). Поймано только потому, что скрипт печатал форму элемента;
  исправлено и перезапущено, оба лога сохранены.

## STATE_PATCH

- Задача `PARITY-STOCK-SNAPSHOT-SYNC`: остаётся **`PARTIAL`**. Дописка в колонку «как закрывается»:
  «**Шаг 3 исполнен 2026-08-05** (`reference/parity_stock_snapshot_sync_2026-08-05.md`): все семь
  карточек существуют, ни одна не архивирована — ветка «архивация» опровергнута фактом; природа
  сирот НЕ установлена, различитель перенесён в шаг 2 (повторится ли множество сирот на свежем
  замере). Остался только шаг 2 — пересверка в окне `21:00Z`; шаги 1 и 3 закрыты».
- `Q-80`: статус **НЕ меняется** (`PARTIAL`). Числовой сверки шаг 3 не даёт, статус строки 21 не
  двигает.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - **Прошлый шаг:** исполнен шаг 3 задачи `PARITY-STOCK-SNAPSHOT-SYNC` — семь строк-сирот опрошены
    поимённо, ветка «архивация карточки» опровергнута фактом
    (`reference/parity_stock_snapshot_sync_2026-08-05.md`).
  - **Где мы:** пять пар реестра из семи закрыты; у строки 21 закрыты два шага задачи из трёх,
    остался единственный — допустимое измерение в окне съёмки слепка, которого не было ещё ни разу.
  - **Следующий шаг:** `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 в окне `2026-08-05T21:00Z` (класс B,
    мандат выдан, бриф готов); `DQ-GATE-SCOPE-SPLIT-DEPLOY` (класс B, мандат выдан, бриф готов)
    идёт независимо.
  - **Развилки на владельце:** нет новых.
  - **Счётчик:** пары реестра **5/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели (append в одноимённый подраздел `07_STATE`):

  **Семь сирот строки 21: одна ветка опровергнута, природа не установлена
  (`PARITY-STOCK-SNAPSHOT-SYNC` шаг 3, 2026-08-05).** Полный ход — артефакт, здесь не
  пересказывается. Что обязана знать сессия, берущая шаг 2. (i) **Карточки всех семи товаров живы и
  не архивированы** — из двух веток, названных `ADR-123`, «архивация карточки» закрыта фактом;
  осталась вторая (переход в ноль между снимком и запросом либо иной критерий включения строки в
  отчёт). Карточка товара состояния «попадает ли позиция в `report/stock/all`» не несёт по
  построению — это свойство отчёта, не товара, поэтому опросом карточек вторая ветка не
  различается в принципе. (ii) **Различитель лежит в шаге 2 и стоит ноль дополнительных вызовов:**
  повторится ли множество сирот на свежем замере. Пусто либо ДРУГИЕ товары — прежние семь были
  преходящими, что согласуется с механизмом `ADR-123 §6`; те же семь снова — механизм структурный,
  и это отдельная находка. (iii) **Ловушка, уже сработавшая и снятая:** все семь сирот несут
  `updated` одной миллисекунды (`2026-08-03 15:15:17.683`), что выглядит следом операции ровно над
  ними; контроль на трёх НЕ-сиротах дал ту же отметку — это массовая правка справочника целиком.
  Наблюдение, объясняющее ровно изучаемое множество, обязано проверяться на неизучаемом, иначе оно
  объясняет всё и не различает ничего. (iv) **Слепок снимается секунда в секунду:** `21:00:04Z`
  трое суток подряд; окно шага 2 планировать по этой отметке, а не по расписанию «примерно в
  девять». (v) Побочно: за сутки `08-04` → `08-05` склад вырос на `30 748` штук (`121 878` →
  `152 626`) — крупный приход; величина расхождения, измеренная в дневные часы, на таком фоне
  неинформативна тем более.

- Новые открытые вопросы: нет.
- Блокеры: нет.
- updated_at: 2026-08-05
- обновил: исполнитель (сессия: `PARITY-STOCK-SNAPSHOT-SYNC`)

## NEW_DECISIONS

- нет. Шаг исполнен по уже принятому `ADR-123 §8`; новых решений не требует.

## NEW_CONVENTIONS

- нет.

=== END SESSION ===
