=== SESSION LOG · 2026-08-02 · DQ-SOURCE-CAPTURE ===

## SESSION_LOG
- Задача: `DQ-SOURCE-CAPTURE` — снять исходники `cf-dq`, установить периметр проверки свежести, путь сигнала
- Сделано:
  - Снят исходник задеплоенной ревизии `cf-dq-00007-hot` прямым путём (`gcloud functions describe` + `gsutil cp`,
    без `403`) → `reference/code/cf-dq/` + `MANIFEST.md` (sha256 всех 5 файлов с диска)
  - Периметр всех 6 чеков выписан дословно с номерами строк: только `stg_msklad.fact_sales_staging`,
    `core.fact_sales_profit`, `core.dim_products`; `core.fact_customer_invoices` не входит ни в один чек
    (двойное основание: таблица не адресуется, и колонка `transaction_date_raw` в ней структурно отсутствует)
  - Путь сигнала прослежен до конца: `cf-dq` никого не уведомляет сам; оба Cloud Workflow на провале только
    логируют `CRITICAL` и роняют исполнение; из 4 alert-policy специфичная `msklad-dq-gate-failed` — мёртвая
    проводка (метрика ссылается на `service_name=cf-dq`+`severity>=ERROR`, которых реальный лог не несёт —
    0 совпадений точным фильтром за 90 суток); общая `msklad-workflow-execution-failed` реально срабатывает
    (20 совпадений за последние часы, тот же фильтр)
  - Порог выходных `0,03` реконструирован по 9 субботам за 70 суток (read-only BQ, формула чека
    воспроизведена и сверена с фактически залогированными числами для двух сработавших суббот —
    совпадение до копейки): 7 из 9 проходят с запасом ×4,5 и выше над порогом, 2 (07-25, 08-01) проваливаются
    не впритык, а на два порядка ниже нормы — порог не выглядит неоткалиброванным
  - Расхождение «ревизия старше правки» снято чтением кода: `patch_dq.py` внутри архива деплоя переписывает
    `check_drift` в T-1-логику, тело патча побайтово совпадает с задеплоенным `main.py`, `mtime` обоих файлов
    и `createTime` ревизии — один и тот же день (`2026-06-18`); дата `2026-06-24` в прежней редакции доков не
    соответствовала НИ ОДНОЙ ревизии `cf-dq` и была атрибуцирована неверно (спутана с `cf-facts` того же дня)
- Команды/логи ключевые: `reference/_scratch_DQ-SOURCE-CAPTURE_2026-08-02/step1…step12_*.sh` + `*_run.log`
  (`date -u`/`gcloud auth list` первой и последней командой каждого скрипта)
- Отклонения от плана: бриф не предполагал реконструкцию суббот BQ-запросом и трассировку Cloud
  Monitoring/log-based metrics — оба добавлены владельцем в чате как обязательные пункты сверх брифа;
  выполнены в рамках класса A (read-only), новых прав не запрашивалось

## STATE_PATCH
- Задача `DQ-SOURCE-CAPTURE`: не начата → DONE
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: `DQ-SOURCE-CAPTURE` закрыта — снапшот `cf-dq` в репозитории, периметр DQ-Gate установлен
    дословно (только продажи; счета покупателям и четыре других фактовых таблицы ядра вне периметра),
    путь сигнала прослежен до Cloud Monitoring (специфичный DQ-алерт нерабочий, общий workflow-алерт рабочий),
    расхождение ревизии/патча снято чтением кода — `reference/dq_source_capture_2026-08-02.md`
  - Где мы: восстановление потока данных остаётся приоритетом перед сверкой паритета «на сейчас»
    (`FACTS-WORKFLOW-STOP-DIAG`, `ADR-111`) — решение владельца о форме восстановления не получено;
    новый факт для этого решения — порог `0,03` не является причиной блокировки (реконструкция 9 суббот)
  - Следующий шаг: решение владельца о форме восстановления загрузки (снять блокировку конкретного
    прогона, либо править `drift_check`/порог, либо подтвердить падение выручки как реальное — порог
    исключён из подозреваемых этой сессией), затем `FACTS-BACKFILL` (класс B), затем `DQ-GATE-SCOPE-SPLIT`
    (периметр гейта уже заведён, теперь с полным списком непокрытых таблиц) и `DQ-GATE-BEHAVIOR-PROBE`
    (глубже 30 прогонов); параллельно без изменений — `DIM-METADATA-MAPPINGS-FROZEN`, `MARTS-BUILD-STAMP`,
    `FACT-SALES-BYVARIANT-BACKUP-OWNER`, `SALES-CONSIGNMENT-REVENUE`, `SALES-PERIMETER-EXTEND`,
    `SALES-REFRESH-WINDOW`, `SALES-MERGE-DRYRUN` (бриф готов, не взят), `FX-MAY-WINDOW-D1-TAIL`,
    `INGEST-CURRENCY-ASSERT` шаги 1-2, подготовка `INGEST-MOMENT-ZONE-FIX`, тексты Looker Studio
  - Развилки на владельце: решение о форме восстановления загрузки (см. «Следующий шаг»); чинить ли
    мёртвую проводку `msklad-dq-gate-failed` (новая находка, форма фикса не назначается этой сессией —
    архитектурное решение); первый `push` код-репо в `holika-prod`; ответ клиента по часовому поясу
    (`MSKLAD-TZ-PROPOSAL`); мандат класса B на `AUDIT-SNAPSHOT-FIX-EMPLOYEES`/`AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`
  - Счётчик: пары реестра 2/7 сходятся · таблиц живых 15 из 26 (до восстановления, не переизмерялось) ·
    строк реестра на критическом пути 23 из 63 · Epic M 5/7 фаз
- Подробности для модели: **`DQ-SOURCE-CAPTURE` закрыта, снапшот и периметр `cf-dq` — в
  `reference/code/cf-dq/MANIFEST.md` и `reference/dq_source_capture_2026-08-02.md` (самодостаточны,
  здесь не пересказываются).** Три факта, без которых следующая сессия по DQ-Gate или по восстановлению
  потока данных пойдёт вслепую: (1) DQ-Gate проверяет ТОЛЬКО домен продаж (`fact_sales_staging`/
  `fact_sales_profit`/`dim_products`) — ни одна из пяти остальных фактовых таблиц ядра (`fact_returns`,
  `fact_purchases`, `fact_inventory`, `fact_payments`, `fact_customer_invoices`, `fact_commissionreportin`)
  не имеет автоматического наблюдателя свежести/дрифта вообще, заморозка любой из них повторит историю
  счетов незаметно; (2) специфичный Cloud Monitoring алерт `msklad-dq-gate-failed` (email+Telegram, выглядит
  полностью настроенным) структурно не может сработать — его лог-метрика ждёт `service_name="cf-dq"` +
  `severity>=ERROR`, а провал реально логируется CRITICAL на ресурсе Workflow, не `cf-dq` (0 совпадений
  точным фильтром за 90 суток, проверено запросом, не предположением); работает только более общая
  политика `msklad-workflow-execution-failed`, которая не называет причину падения; (3) порог выходных
  `0,03` реконструирован по 9 субботам (read-only BQ) и не выглядит виновником — обе несработавшие субботы
  провалились на порядки, а не впритык, семь остальных прошли с запасом ×4,5+; это исключает калибровку
  порога из списка причин самоподдерживающегося отказа `ADR-101`, сужая его до механизма, уже названного
  `facts_stop_diag_adj_2026-08-02.md §2/§4`. Расхождение «ревизия cf-dq старше T-1-фикса» снято: фикс
  внутри текущей ревизии (`patch_dq.py` в архиве деплоя, побайтовое совпадение с `main.py`, общий `mtime`
  с `createTime` ревизии `2026-06-18`); дата `2026-06-24` в старой редакции доков была ошибочной
  атрибуцией (принадлежит деплою `cf-facts` того же дня), не фактом о `cf-dq`.

## Новые открытые вопросы
- нет (все три дополнительных вопроса владельца закрыты фактом внутри этой сессии; `Q-3`/остаток
  неверсионированных исходников `cf-dq` и вопрос его актуальной ревизии закрываются этой же сессией
  дословно — см. `Acceptance` брифа, п. «Закрывается попутно»)

## Блокеры
- нет

- updated_at: 2026-08-02
- обновил: исполнитель (сессия: DQ-SOURCE-CAPTURE)

## NEW_DECISIONS
- нет

## NEW_CONVENTIONS
- нет

=== END SESSION ===
