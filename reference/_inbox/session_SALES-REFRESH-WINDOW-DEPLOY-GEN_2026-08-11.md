=== SESSION LOG · 2026-08-11 · SALES-REFRESH-WINDOW-DEPLOY-GEN ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: SALES-REFRESH-WINDOW-DEPLOY-GEN — генерация деплой-брифа `SALES-REFRESH-WINDOW-DEPLOY` из
  выданного мандата класса B (`ADR-159`/`ADR-158`, 2026-08-11).
- Сделано:
  - Обнаружено и остановлено расхождение: исходное задание ссылалось на `briefs/SALES-REFRESH-WINDOW.md`
    как на «деплойную сессию класса B», но этот файл — подготовительный бриф класса A от 2026-08-01,
    явно исключающий деплой из scope. Владельцу задан вопрос по форме `ADR-056 §4`, получен ответ:
    деплой-бриф ещё предстоит сгенерировать ролью `generator`.
  - Прочитаны: `06_DECISIONS_LOG.md` (`ADR-159`, `ADR-158`, `ADR-160`), `06_INDEX.md` (строки `ADR-144`–
    `ADR-160`), `07_STATE.md` (Текущий фокус, строка мандата `SALES-REFRESH-WINDOW, деплой` ~1610,
    GAP-строка `SALES-REFRESH-WINDOW` ~1708), `reference/sales_refresh_window_mandate_adj_2026-08-11.md`
    целиком, `_GENERATOR.md`, `08_TASK_BRIEF_TEMPLATE.md`.
  - Подтверждено чтением снапшота (`reference/code/cf-facts/config.py`, `bq_ops.py`), что три
    обязательные правки предохранителя (`ADR-158 §3` мандатного артефакта: допуск `GUARD_TOLERANCE_DAYS`,
    единый BQ-запрос для обеих дат, проверка обоих краёв окна) уже внесены в снапшот — деплой-сессии не
    нужно патчить код, только перенести снапшот в ветку код-репо.
  - Собран `briefs/SALES-REFRESH-WINDOW-DEPLOY.md`: класс B (мандат `ADR-159`), параллель «нет», полный
    список файлов на запись, 12 шагов (сверка дрейфа → ветка деплоя → гигиена/секреты → снимок → деплой
    → read-back → расчёт `window_days` → боевой прогон → замер до/после → пересчёт разности → merge →
    сборка провенанс-артефакта), критерии приёмки и return-this дословно по мандату `§4`-`§7`.
- Команды/логи ключевые: только чтение файлов с диска (`Read`/`Bash grep`), запись — только в
  `briefs/` и `reference/_inbox/`. Ни одного `bq`/`gcloud`/сетевого вызова.
- Отклонения от плана: нет — задача выполнена ролью `generator` (Read/Grep/Glob/Write), прод-код и
  деплой не трогались, как предписано `.claude/agents/generator.md`.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача SALES-REFRESH-WINDOW, деплой: мандат выдан (`ADR-159`), брифа не было → бриф готов
  (`briefs/SALES-REFRESH-WINDOW-DEPLOY.md`), задача НЕ взята (генерация ≠ исполнение).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: деплой-бриф `SALES-REFRESH-WINDOW-DEPLOY` собран из мандата `ADR-159`/`ADR-158`,
    расхождение исходного задания с текстом `briefs/SALES-REFRESH-WINDOW.md` (класс A, деплой вне
    scope) поймано и снято ответом владельца.
  - Где мы: мандат класса B на деплой `cf-facts` выдан и исполним немедленно; бриф готов, но не взят —
    майский паритет остаётся неисправленным до фактического деплоя.
  - Следующий шаг: исполнение `briefs/SALES-REFRESH-WINDOW-DEPLOY.md` отдельной сессией-исполнителем
    (снимок → деплой из ветки код-репо → первый широкий прогон → замеры мая/июля → пересчёт разности);
    параллельно подготовка обоих патчей `cf-dq` (класс A, строка `2`, как в прежнем стенд-апе).
  - Развилки на владельце: подтверждение каждого не-идемпотентного шага деплой-сессии по мере
    исполнения (снимок / деплой / боевой прогон) — форма `ADR-077 §6`, уже встроена в шаги брифа;
    мандат на выезд `cf-dq` (текста ещё нет, строка `2` не подготовлена) остаётся отдельно.
  - Счётчик: строк списка закрытия осталось `8` из `9` (без изменений — генерация брифа не есть
    исполнение мандата, счётчик двигается по факту приёмки деплоя, `ADR-159` последствие §2).
- Подробности для модели: **Деплой-бриф существует и не взят** —
  `briefs/SALES-REFRESH-WINDOW-DEPLOY.md`. Сессия-исполнитель обязана знать: (i) код-патч (три правки
  предохранителя) уже В СНАПШОТЕ `reference/code/cf-facts/` — деплой-сессия НЕ патчит код, только
  переносит снапшот в ветку `deploy/cf-facts-<date>-refresh-window` код-репо и сверяет `sha256`; (ii)
  `window_days` первого боевого прогона считается по формуле мандата на ФАКТИЧЕСКУЮ дату прогона, не
  по ориентирам брифа (`106`/`113`/`119` — только примеры для дат `12.08`/`19.08`/`25.08`); (iii) оба
  промоута (продажи И периметр) идут ОДНИМ прогоном с одним `window_days`, staging периметра тоже
  обязана быть загружена тем же прогоном (на `2026-08-11` отставала на 8 суток); (iv) лог обязан
  печатать «предохранитель пройден» с обеими датами для КАЖДОГО из двух промоутов — отсутствие есть
  провал приёмки, а не мелочь; (v) ненулевое расхождение с числовым ожиданием мандата (`§6(ii)`) не
  примиряется исполнителем, уходит архитектору.
- Новые открытые вопросы: нет (все известные гэпы — источник COGS шире 90 суток, сверка пары «Заказы
  поставщикам в пути» — уже названы в `07_STATE`/`MANIFEST.md`, этой сессией не открывались заново).
- Блокеры: нет.
- updated_at: 2026-08-11
- обновил: generator (сессия: SALES-REFRESH-WINDOW-DEPLOY-GEN)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
