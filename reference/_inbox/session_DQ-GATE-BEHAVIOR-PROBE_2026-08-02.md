=== SESSION LOG · 2026-08-02/03 · DQ-GATE-BEHAVIOR-PROBE ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: DQ-GATE-BEHAVIOR-PROBE — снимется ли блокировка `drift_check` сама, или нужно вмешательство
  владельца (три read-only замера, класс A).
- Сделано: замер 3 исполнен (первый прогон часового workflow после смены суток `2026-08-02→03`:
  `target_date=2026-08-02, yesterday_rev=0` — сигнатура самоподдерживающегося отказа, названная
  брифом заранее). Независимо перепроверена классификация пяти `FAILED`-прогонов часового workflow
  (`07-14/18/21/23/28`) — подтверждено, что это инфраструктурные отказы (`raise_dim`/`raise_facts`),
  не `drift_check`; единственный настоящий блок DQ Gate до текущей аварии — разрыв
  `2026-07-25T18:00Z…2026-07-26T18:00Z` (24 прогона). Артефакт `reference/dq_gate_behavior_probe_2026-08-02.md`
  переписан консолидированной финальной версией (замеры 1–3, вердикт, `CONTEXT GAP`).
- Команды/логи ключевые: `reference/_scratch_DQ-GATE-BEHAVIOR-PROBE_2026-08-02/step3_run.log`
  (тело провала `2026-08-02T18:02:18Z`, `target_date=2026-08-02, yesterday_rev=0, ma7=4774584,
  ratio=0.00`); `step2f_run.log` (`gcloud workflows executions describe` × 5, подтверждение шагов
  `raise_dim`/`raise_facts`); `step2g_run.log` (`promote_result` восстанавливающего прогона
  `2026-07-26T18:00:02Z`, `affected_rows=691`).
- Отклонения от плана: сессия стартовала в дереве с непустым буфером `reference/_inbox/` (2 файла)
  и отставанием от `origin/main` на 2 коммита — владелец явно подтвердил продолжать без сборки и
  без синхронизации (задача read-only, конфликта не создаёт).

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача DQ-GATE-BEHAVIOR-PROBE: PARTIAL (замеры 1–2 закрыты, замер 3 открыт) → **DONE** (все три
  замера закрыты, вердикт дан).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: `DQ-GATE-BEHAVIOR-PROBE` закрыта DONE — блокировка `drift_check` сама не снимется
    (`target_date=2026-08-02, yesterday_rev=0`, `reference/dq_gate_behavior_probe_2026-08-02.md`).
  - Где мы: три read-only замера дали окончательный ответ; решение о форме восстановления (ручной
    промоут vs правка порога/деплой `cf-dq`) — на владельце, класс B, эта сессия его не принимает.
  - Следующий шаг: решение владельца о форме восстановления → затем `FACTS-BACKFILL` (досев периода
    простоя); параллельно в очереди `DQ-SOURCE-CAPTURE` (чтение кода `cf-dq`, закрывает оставшийся
    `CONTEXT GAP` о механизме восстановления `2026-07-26`) и `DQ-GATE-SCOPE-SPLIT` (периметр гейта).
  - Развилки на владельце: форма восстановления — (а) ручной промоут конкретного прогона, (б) правка
    /калибровка `drift_check`/порога в `cf-dq` с передеплоем, (в) владелец подтверждает падение
    выручки реальным и решает вопрос отдельно от механизма гейта.
  - Счётчик: без изменений этой сессией (задача точечная, не часть парного/картового счёта).
- Подробности для модели: `drift_check` читает `core.fact_sales_profit` (подтверждено независимой
  сверкой чисел — `yesterday_rev`/`ma7` воспроизводятся ядром, не `stg_msklad.fact_sales_staging`;
  последняя структурно не может быть источником MA7, так как загружается `WRITE_TRUNCATE`
  (`reference/code/cf-facts/bq_ops.py:7,115`) и не хранит фиксированное календарное окно). Журнал
  заданий BigQuery к `core.fact_sales_profit` за `2026-07-15…2026-08-02` (369 заданий) не содержит
  НИ ОДНОГО задания не от `etl-sa@msklad-bi-prod.iam.gserviceaccount.com` — версия «сняли руками»
  опровергнута фактом. Единственный настоящий блок DQ Gate до текущей аварии — `2026-07-25T18:00Z
  …2026-07-26T18:00Z` (24 прогона); механизм ЕГО восстановления не объясняется буквальным чтением
  «источник — уже промёрженный core» (промоут дня `07-26` исполнился ПОСЛЕ прохождения чека в том же
  прогоне) — остаётся `CONTEXT GAP`, закрывается `DQ-SOURCE-CAPTURE`. Замер 3
  (`2026-08-02T18:00:02Z`, execution `5a36322f-76fa-428f-b2be-62bc2e1b98d5`) дал `target_date=2026-08-02,
  yesterday_rev=0, ratio=0.00` — данные за этот день физически отсутствуют в ядре (промоут
  заблокирован с `2026-08-01T18:00Z`), самоподдержание отказа подтверждено практически, независимо
  от того, разрешён ли механизм восстановления `07-26` до конца. `freshness` уже `lag_days=2`
  (порог `≤3`) — начнёт падать примерно `2026-08-05` по Бишкеку (арифметика, не прогноз).
- Новые открытые вопросы: нет новых сверх уже заведённых (`DQ-SOURCE-CAPTURE`,
  `DQ-GATE-SCOPE-SPLIT` — обе уже в реестре).
- Блокеры: нет для этой задачи; решение о форме восстановления (класс B) — на владельце, вне
  мандата любой read-only сессии.
- updated_at: 2026-08-03
- обновил: исполнитель (сессия: DQ-GATE-BEHAVIOR-PROBE)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
