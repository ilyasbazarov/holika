=== SESSION LOG · 2026-08-02 · DQ-GATE-BEHAVIOR-PROBE ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: `DQ-GATE-BEHAVIOR-PROBE` — снимется ли блокировка загрузки фактов сама, или нужно
  вмешательство владельца (класс A, read-only, три замера)
- Сделано:
  - Замер 1: снята схема `core.fact_sales_profit`/`stg_msklad.fact_sales_staging`
    (`INFORMATION_SCHEMA.COLUMNS`), посчитана выручка `2026-08-01` и среднедневная за
    `2026-07-25…2026-07-31` в обеих таблицах. `core.fact_sales_profit` воспроизводит оба числа
    гейта (`yesterday_rev=25921`, `ma7=4780222`); `stg_msklad.fact_sales_staging` не воспроизводит
    недельное среднее (расхождение ~0,5%).
  - Замер 2: полная история выполнений часового workflow за `2026-07-11…2026-08-02` (500 прогонов,
    лимит поднят с прежних 30) и недельного (50 прогонов). Найден необнаруженный прежней
    диагностикой блок из 24 последовательных отказов `raise_dq_failed` (`2026-07-25T18:00Z…
    2026-07-26T17:00Z`), плюс пять несвязанных однократных инфраструктурных сбоев. Журнал заданий
    BigQuery к `core.fact_sales_profit` за тот же период (369 заданий) — все от
    `etl-sa@msklad-bi-prod.iam.gserviceaccount.com`, ни одного от личного аккаунта; разрыв заданий
    точно совпадает с блоком 24 отказов. Объяснение (в) «сняли руками» — опровергнуто фактом.
    Объяснение (a) «часовой/недельный считают по-разному» — не подтверждено (оба видят идентичные
    цифры в один день). Объяснение (б) — не подтверждено чтением кода (вне scope), но хронология
    восстановления `2026-07-26` математически не согласуется со строгим постфактум-чтением
    заблокированного `core`, поэтому ближе к истине, чем буквальное прочтение доки; итог оставлен
    как `CONTEXT GAP`, закрывается `DQ-SOURCE-CAPTURE`.
  - Замер 3: **НЕ исполнен** — брифом предписано исполнять после `2026-08-02T18:00Z` (полночь по
    Бишкеку), сессия стартовала в `14:55:37Z`, то есть более чем за 3 часа до открытия окна.
    Read-only сессия не располагает безопасным способом дождаться этого момента внутри одного
    прогона. Предсказание по фактам замера 2 зафиксировано в артефакте.
  - Артефакт: `reference/dq_gate_behavior_probe_2026-08-02.md` (ответ владельцу, оба закрытых
    замера, предсказание по замеру 3, поимённые `CONTEXT GAP`).
- Команды/логи ключевые: `reference/_scratch_DQ-GATE-BEHAVIOR-PROBE_2026-08-02/` — `step1_*`
  (схема + выручка), `step2_*` (история выполнений + job-журнал), `step2b_*`/`step2c_*`/`step2d_*`
  (детальный разбор окна восстановления `2026-07-26`), `step2e_*` (классификация пяти изолированных
  инфраструктурных отказов).
- Отклонения от плана: замер 3 не исполнен по причине, названной выше (не отклонение по существу,
  а следствие временнОго условия, зафиксированного самим брифом). Ни одного действия над живым
  конвейером не совершено.

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `DQ-GATE-BEHAVIOR-PROBE`: READY → IN PROGRESS (замеры 1–2 закрыты, артефакт
  `reference/dq_gate_behavior_probe_2026-08-02.md`; замер 3 остаётся открытым до первого прогона
  часового workflow после `2026-08-02T18:00Z` — доисполняется тем же брифом/деревом без нового
  брифа, класс и мандат не меняются)
- Текущий фокус: без изменений в части «Развилки на владельце» — открытые пункты стенд-апа этой
  правкой не переформулируются; добавление — предсказание по замеру 3 (см. артефакт) не отменяет
  необходимости решения владельца о форме восстановления, названного в прежнем стенд-апе
- Новые открытые вопросы: нет новой строки реестра; уточнение существующего понимания — источник
  `yesterday_rev`/`target_date` в `cf-dq` не обязательно строго `core.fact_sales_profit` по
  буквальному календарному T-1 (хронология восстановления `2026-07-26` этому противоречит); вопрос
  остаётся закрываемым только `DQ-SOURCE-CAPTURE`, отдельной строки не заводится (тот же объект)
- Блокеры: замер 3 блокирован временем (не средой) — открывается `2026-08-02T18:00Z`
- updated_at: 2026-08-02

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md; "нет" если нет)
- нет

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
- нет

=== END SESSION ===
