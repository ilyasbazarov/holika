=== SESSION LOG · 2026-08-01 · DEPLOY-REVISION-PROBE ===

## SESSION_LOG
- Задача: DEPLOY-REVISION-PROBE — различить, несёт ли задеплоенная ревизия `cf-finance` валютную
  правку, и какая ревизия обслуживает трафик сейчас
- Сделано: исполнена без брифа по `ADR-086 §1` (класс A; приёмка процитирована дословно из строки
  задачи в `07_STATE`; полный набор файлов на запись объявлен; определение процитировано, не
  пересказано). Все шесть пунктов приёмки закрыты, кроме одной колонки таблицы (диск Cloud Shell) —
  причина названа явно. Артефакт: `reference/deploy_revision_probe_2026-08-01.md`
- Команды/логи ключевые: два скрипта с якорями `date -u` и `gcloud auth list` по краям
  (`08:05:46Z…08:06:06Z` и `08:06:56Z…08:06:59Z`), аккаунт и проект между якорями не менялись.
  `gcloud run services describe`, `gcloud run revisions list`, `gcloud functions describe --gen2`,
  `gcloud storage cp` архива по generation, `gcloud builds list`. Всё только чтение; ни одной записи
  в облако, ни одного деплоя. Провенанс целиком в
  `reference/_scratch_DEPLOY-REVISION-PROBE_2026-08-01/`
- Отклонения от плана: один второй заход скрипта, причина содержательная и вынесена в артефакт §5.
  Греп первого захода напечатал «совпадений нет» по задеплоенному файлу при фактическом наличии
  умножения на курс — шаблон `rate\.value` не покрывает форму `(row.get("rate") or {}).get("value")`.
  Ложный отрицательный дал бы ВЕРДИКТ, ПРОТИВОПОЛОЖНЫЙ ИСТИННОМУ. Поймано печатью контекста строк,
  которую требует приёмка. Первый лог не удалён и не переписан: оба захода лежат в провенансе

## STATE_PATCH
- Задача DEPLOY-REVISION-PROBE: READY → **DONE** (артефакт `reference/deploy_revision_probe_2026-08-01.md`)
- `Q-95`: ОТКРЫТ → **ЗАКРЫТ ФАКТОМ.** Ревизия `cf-finance-00012-cik` обслуживает 100 % трафика и её
  архив исходников содержит умножение на курс (строки 66–69, напечатаны целиком). Оба исходных
  наблюдения верны, противоречия между ними нет — противоречие было между ними и статусом задачи
  `E1-T3-MECH-FX`
- Задача E1-T3-MECH-FX: статус НЕ меняется этой сессией. Замер даёт факт, переквалификация задачи
  есть решение архитектора (`_METHOD §11`: DISCOVER ≠ CAPTURE ≠ решение). В строку задачи вносится
  дописка с фактом и с явным перечнем того, что замером НЕ проверено
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - Прошлый шаг: замером закрыт `Q-95` — валютная правка УЖЕ на проде: ревизия `cf-finance-00012-cik`
    держит 100 % трафика с 2026-07-20, её архив исходников несёт `× rate.value`
    (`reference/deploy_revision_probe_2026-08-01.md`)
  - Где мы: `E1-T3-MECH-FX` числится IN PROGRESS с предстоящим cutover, но деплой фактически
    состоялся 2026-07-20 — до прохождения обоих своих гейтов; корректность чисел после правки и
    судьба исторических строк НЕ проверены
  - Следующий шаг: переквалификация `E1-T3-MECH-FX` архитектором (что осталось: не деплой, а
    проверка чисел и вопрос о пересчёте истории); затем `CODE-REPO-STANDUP` шаг 2a от ревизии
    `cf-finance-00012-cik`; параллельно `PARITY-CHECK-SALES-RETURNS`, `INFRA-FACTS-SWEEP` и
    подготовка текстов четырёх запросов Looker Studio
  - Развилки на владельце: создание удалённого репозитория и первый `push` кода — исходящее
    действие, инструменту не отдаётся; `Q-94` (машинная проверка языка ответов) — замерять или нет
  - Счётчик: пары реестра 1/7 сходятся · карта происхождения 7/7 поверхностей · Epic M 5/7 фаз
    (без изменений этой сессией — сверок не производилось)
- Подробности для модели: **Замер `DEPLOY-REVISION-PROBE` (2026-08-01) закрыл `Q-95` фактом.**
  Ревизия `cf-finance-00012-cik` обслуживает 100 % трафика (`status.traffic` напечатан), сборка за
  ней — `31cf7c79-4b2d-45e5-b8b7-4530dc57eec5` от `2026-07-20T15:20:44Z`, ревизия создана через 29
  секунд. Архив исходников скачан ПО GENERATION (`1784560843778541`), то есть та самая версия
  объекта, из которой собрана ревизия, а не текущее содержимое пути. В `main.py` этого архива
  умножение на курс присутствует (строки 66–69). Место вычисления `sum_kgs` в файле одно, обходных
  путей записи нет. Снимок `reference/code/cf-finance/main.py` (`sha256 7a5d8dcd…181d`) оказался
  ПОБАЙТОВО равен файлу `main.py.pre-e1t3-mech-fx.bak`, лежащему внутри того же задеплоенного архива
  — то есть снимок в репо есть в точности до-фиксовая версия, и это подтверждено двумя независимыми
  путями. Разница задеплоенного и снимка — ровно два ханка: удалённый комментарий и сам фикс.
  **Дефект метода, пойманный на себе (артефакт §5):** первый заход напечатал «совпадений нет» по
  задеплоенному файлу, потому что шаблон грепа `rate\.value` не покрывает форму
  `(row.get("rate") or {}).get("value")`; ложный отрицательный дал бы вердикт, противоположный
  истинному. Поймано требованием приёмки печатать строку конвертера целиком, а не метку
  (`ADR-021 §2`, `ADR-044`). Третий случай класса в проекте (`Q-45`, `ADR-073 §9`), первый, где
  ложный вердикт был бы решающим; отдельный `Q` не заводится (`ADR-051 §2`). **Побочные факты без
  интерпретации:** (i) архив задеплоенной сборки содержит четыре мусорных файла
  (`main.py.bak`, `main.py.pre-e1t3-mech-fx.bak`, `patch_main_finance.py`, `__pycache__/`), то есть
  деплой сделан из рабочего каталога как есть — это подтверждает и расширяет `RQ-3`, и это ставит
  вопрос к `ADR-094 §4`: побайтовое равенство первого коммита архиву несовместимо с исключением
  мусора, оговорка разрешается при постановке шага 2a; (ii) `updateTime` функции —
  `2026-07-30T10:04:58Z` при ревизии и сборке от `2026-07-20`, новых ревизий нет, механизм не
  установлен и не интерпретируется, на вердикт не влияет.
- Новые открытые вопросы: `Q-96` (см. строку реестра) — пересчитаны ли исторические строки
  `core.fact_payments`, загруженные до `2026-07-20`
- Блокеры: нет
- updated_at: 2026-08-01
- обновил: исполнитель (сессия: DEPLOY-REVISION-PROBE)
- Дописка к строке задачи `E1-T3-MECH-FX` (обе вхождения — и в `07_STATE §Статусы задач (Epic 1)`,
  и в GAP-реестре), в конец текста статуса:

  **ФАКТ ЗАМЕРА (`DEPLOY-REVISION-PROBE`, 2026-08-01): фикс УЖЕ НА ПРОДЕ.** Ревизия
  `cf-finance-00012-cik` держит 100 % трафика с `2026-07-20T15:21:13Z`, архив исходников её сборки
  несёт `× rate.value` (`reference/deploy_revision_probe_2026-08-01.md §1/§4`). Деплой состоялся ДО
  прохождения обоих объявленных гейтов: артефакт Step 4 в репо отсутствует (`Q-41`), а `ADR-065`
  (деплой только из код-репо) принят `2026-07-27`, то есть позже самого деплоя. **Замером НЕ
  проверено и потому НЕ утверждается:** корректность чисел после правки (прод re-diff `core` против
  живого API = 0,00 не мерился); пересчёт исторических строк, загруженных до `2026-07-20` (`Q-96`);
  охват `cashout` (отдельная линия `Q-30`). **Переквалификация задачи — решение архитектора, этой
  сессией не производится** (`_METHOD §11`): по факту осталось не «выкатить», а «проверить числа и
  решить судьбу истории».

- Новая строка GAP-реестра (вставить после строки `Q-95`):

| Q-96 | (`DEPLOY-REVISION-PROBE`, 2026-08-01) Валютная правка на проде с `2026-07-20`, но действует ли она только вперёд или история `core.fact_payments` пересчитана — не проверено. Загрузчик пишет `WRITE_TRUNCATE` в staging и затем `MERGE` в `core` по окну запроса; окно берётся из тела вызова планировщика, поэтому строки, загруженные ДО `2026-07-20`, могли остаться с не-конвертированными суммами | факт (не измерено) | **ОТКРЫТ, различитель назначен и дёшев.** Различитель: выборка не-KGS документов мая-2026 из `core.fact_payments` против того же множества из живого API (метод уже отработан сессией `E1-T3`, граница «38/38 подлежащих загрузке не-KGS документов мая» зафиксирована там же). Совпало — история пересчитана; разошлось — нужен бэкфилл, и это отдельная задача класса B. **Гейтит:** закрытие `E1-T3-MECH-FX` (без ответа нельзя сказать, что задача выполнена), и через него — часть инвесторских чисел за период до `2026-07-20`. НЕ гейтит: `PARITY-CHECK-SALES-RETURNS`, `CODE-REPO-STANDUP`, задачи Looker Studio |

- Новая строка таблицы мандата (вставить после строки `DEPLOY-REVISION-PROBE`):

| Q-96 (сверка истории `fact_payments`) | A | да | постоянный | чтение `core` и живого API; API требует `msklad-token` ⇒ **при живом `GET` задача становится классом B** и мандат выдаётся поимённо. Read-only часть по `core` (что лежит в таблице) идёт классом A сейчас |

## NEW_DECISIONS
нет — замер фактов не адъюдицирует (`_METHOD §11`). Переквалификация `E1-T3-MECH-FX` и разрешение
оговорки `ADR-094 §4` (мусор в архиве против побайтового равенства) вынесены архитектору отдельно.

## NEW_CONVENTIONS
нет

=== END SESSION ===
