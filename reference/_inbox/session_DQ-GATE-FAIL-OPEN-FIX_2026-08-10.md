=== SESSION LOG · 2026-08-10 · DQ-GATE-FAIL-OPEN-FIX ===

**Адресовано SHA:** `ceafa0a` (последний коммит этой сессии, ветка `s/DQ-GATE-FAIL-OPEN-FIX`).
Блок заменяет собой предыдущую версию файла (та же сессия, тот же task-id, `ADR-021 §1`) —
многоходовая сессия вышла за пределы одного брифа (подготовка + деплой) по прямому указанию
владельца в чате.

## SESSION_LOG
- Задача: `DQ-GATE-FAIL-OPEN-FIX` — закрытие fail-open дефекта ветки `ma7 == 0` в `check_drift`
  (снапшот `reference/code/cf-dq/main.py`), подготовка (класс A) **плюс деплой (класс B)**, мандат
  на который выдан и приземлён этой же сессией по прямому указанию владельца.
- Сделано (подготовка, коммит `2a9c9a4`):
  - Стартовая проверка (`tools/session_status.sh`) вернула СТОП по `ADR-086 §4`: локальная база
    отставала от `origin/main` на 6 коммитов (дерево было заведено от устаревшего `HEAD fceb62f`).
    По решению владельца дерево и ветка `s/DQ-GATE-FAIL-OPEN-FIX` пересозданы от свежего
    `origin/main` (`e30c2f7`); бриф (untracked, не в `origin/main`) сохранён и восстановлен.
  - Различитель внесён в ветку `ma7 == 0` функции `check_drift`: `SELECT COUNT(*) FROM
    core.fact_sales_profit` без окна различает «первый запуск проекта» (`COUNT(*)=0` →
    `passed=True` с обоснованием) от «заморозка ядра при непустой истории» (`COUNT(*)>0` →
    `passed=False` с обоснованием, ранее — тихий `passed=True` безусловно).
  - Синтаксис проверен, ветвление подтверждено офлайн-симуляцией на реальных числах адъюдикации
    (`yesterday_rev=191800`). Артефакт — `reference/dq_gate_fail_open_fix_2026-08-10.md`.
  - Диффом подтверждено: единственный изменённый файл и диапазон — тело `if ma7 == 0:`; остальные
    пять чеков и `CHECKS`-реестр не тронуты.
- Сделано (мандат и деплой, коммиты `86c1079`/`a70787f`/`c40559c`/`ceafa0a`):
  - Владелец запросил мандат на деплой; запущена read-only архитекторская подсессия (агент), которая
    нашла блокирующую находку: в снапшоте `cf-dq` лежит ВТОРАЯ, чужая заготовка (`DQ-FRESHNESS-COVERAGE`,
    подготовка 2026-08-09, свой мандат на деплой не выдан) — при переносе файла целиком в прод уехали
    бы обе правки. Владелец подтвердил объём: только `ma7`, без соседней заготовки.
  - `ADR-152` приземлён этой сессией по прямому указанию владельца (отступление от буфера `ADR-080 §4`,
    форма прецедента `ADR-145`/`ADR-146`, сборщику повторно не применять): `06_DECISIONS_LOG.md` +
    `06_INDEX.md` + `07_STATE.md` (две строки мандата, строка открытых вопросов) + `07_GAPS.md` (строка
    `DQ-GATE-FAIL-OPEN-FIX` дополнена, строка `DQ-FRESHNESS-COVERAGE` дополнена перекрёстной ссылкой,
    заведена новая строка `DQ-GATE-BLOCK-BOUNDED`).
  - Предусловия `ADR-152 §6` исполнены (read-only, класс A): (i) живая ревизия `cf-dq-00007-hot`
    сверена с `master` код-репо `holika-prod` — дрейфа нет, sha256 всех пяти файлов совпали; (ii)
    обнаружен гэп — `.gcloudignore` для `cf-dq/` в код-репо отсутствовал ВООБЩЕ (ни в корне, ни в
    каталоге функции), из-за чего `patch_dq.py` и вложенный архивный мусор годами уезжали бы в прод;
    создан `cf-dq/.gcloudignore` по прецеденту формы `cf-facts/.gcloudignore`.
  - Ветка деплоя `deploy/cf-dq-2026-08-10-fail-open` собрана вручную поверх ЧИСТОЙ `master`-версии
    `main.py` (побайтово равна живому архиву, без заготовки `DQ-FRESHNESS-COVERAGE` — та в код-репо
    никогда не попадала) плюс идентичный хунк `ma7`; секретный сплошной поиск — 0 совпадений.
  - Владелец подтвердил push ветки отдельным сообщением → отправлено.
  - Владелец подтвердил деплой отдельным сообщением → `gcloud functions deploy cf-dq` из выкаченной
    ветки. Команда превысила 120с и ушла в фон инструмента (не обрыв — не ретраилась вслепую);
    по завершении — новая ревизия `cf-dq-00008-cev`, `state=ACTIVE`.
  - Read-back архива (класс A, read-only): sha256 всех четырёх файлов нового архива совпали с веткой
    побайтово; `patch_dq.py` и три вложенных самореференциальных архива (застарелая аномалия прежних
    ревизий) в новом архиве отсутствуют — впервые чистый архив у `cf-dq`. Условие приёмки 1/3
    (`ADR-152 §5`) закрыто.
  - Владелец подтвердил слияние ветки в `master` отдельным сообщением → `git merge --no-ff` +
    `git push origin master`, merge-коммит `5ad9544ce65c72278c6d02a8b16f727cc8ffa4e8`. Инвариант
    «master код-репо = то, что задеплоено» восстановлен.
  - Владелец попросил дождаться первого планового часового прогона и сообщить результат. Скрипт
    (read-only, класс A) дождался прогона `msklad-pipeline-hourly` и прочитал `audit.dq_runs`:
    `run_id=1786374002.7999175`, `checked_at=2026-08-10 15:05:55 UTC`. `drift_check` вернул штатный
    формат (`yesterday_rev=756397, ma7=5459004, ratio=0.14, threshold=0.03 (weekend)`) — `ma7≠0`,
    новая ветка на здоровых данных не исполнялась, как и ожидалось. Все шесть чеков `passed=true`.
    Условия приёмки 2/3 закрыты полностью, синтетика не использовалась.
- Команды/логи ключевые: все скрипты и логи — `reference/_scratch_DQ-GATE-FAIL-OPEN-FIX_2026-08-10/`
  (`verify_branch.py`, `deploy/step1_predusloviya.sh…step4_verify_hourly.sh` + `*_run.log`).
  `date -u`/`gcloud auth list` — первой и последней командой каждого облачного скрипта (`ADR-055 §4`).
- Отклонения от плана: сессия вышла за пределы одного брифа (подготовка → мандат → деплой → приёмка)
  по прямому указанию владельца в чате на каждом шаге отдельно (`ADR-077 §6`); дерево/ветка сессии
  пересозданы посреди старта (СТОП-условие `ADR-086 §4`, подтверждено владельцем перед действием).

## STATE_PATCH
- Задача `DQ-GATE-FAIL-OPEN-FIX, подготовка`: OPEN → DONE.
- Задача `DQ-GATE-FAIL-OPEN-FIX, деплой`: НЕ выдан → ВЫДАН (`ADR-152`) → ИСПОЛНЕН → ПРИЁМКА ЗАКРЫТА
  ПОЛНОСТЬЮ. Строка `DQ-GATE-FAIL-OPEN-FIX` (`07_GAPS.md`) готова к переносу в `07_ARCHIVE.md`
  (`ADR-064`) — решение и перенос за проходом сборки, не этой сессией.
- Стенд-ап (замена блока `07_STATE.md §Текущий фокус §Стенд-ап` целиком, ровно пять строк):
  - Прошлый шаг: `DQ-GATE-FAIL-OPEN-FIX` — fail-open в `check_drift` закрыт, задеплоен
    (`cf-dq-00008-cev`, `master` `holika-prod` @ `5ad9544`), приёмка подтверждена живым прогоном
    (`audit.dq_runs`, `2026-08-10 15:05:55 UTC`), не отчётом инструмента; попутно закрыт застарелый
    гэп — у `cf-dq` не было `.gcloudignore`.
  - Где мы: счётчик реестра паритета остаётся `7/7`; из очереди DQ закрыта задача, добавлявшая
    fail-open риск в гейт; открыты две смежные строки — `DQ-GATE-BLOCK-BOUNDED` (проектирование) и
    `DQ-FRESHNESS-COVERAGE` (деплой, мандат не выдан).
  - Следующий шаг: `INFRA-FACTS-LANDING` (бриф готов) — правит и `11_INFRA_FACTS.md §cf-dq` новой
    ревизией; параллельно — `DQ-GATE-METRIC-REDESIGN` подготовка (форма не назначена) или
    `DQ-GATE-BLOCK-BOUNDED` подготовка (класс A, форма не назначена).
  - Развилки на владельце: форма `DQ-GATE-BLOCK-BOUNDED` (ограничение блокировки по времени и/или
    ack-механизм) — не решена этой сессией, только заведена; предусловие мандата `SALES-REFRESH-WINDOW`;
    апрув `ERP-CONTRACTS-DAY-RULE-DOC`; подпись момента пересборки «в пути»; дефект `_loaded_at` в
    `cf-finance`/`cf-loss-commission` (архитектору из `DQ-FRESHNESS-COVERAGE`).
  - Счётчик: пары реестра `7/7` · обязательных задач передачи `10` (из них с выданным мандатом
    ранее `3`, эта сессия исполнила и закрыла ещё одну поимённо выданным мандатом) · Epic M —
    (Epic-1 завершён `ADR-140`).
- Подробности для модели: ревизия `cf-dq` теперь `cf-dq-00008-cev` (была `cf-dq-00007-hot`) —
  `11_INFRA_FACTS.md §cf-dq` НЕ обновлён этой сессией (вне её файлов на запись по брифу), обновление
  вменено `INFRA-FACTS-LANDING` либо отдельной строкой. Код-репо `holika-prod` `master` теперь на
  `5ad9544` (было `508ca64`). У `cf-dq` впервые чистый архив (`.gcloudignore` создан) — тот же класс
  гигиены, что уже применялся к `cf-facts` (`ADR-146 §4`); стоит сплошным поиском проверить остальные
  функции (`cf-finance`, `cf-fx`, `cf-inventory` — по факту этой сессии `.gcloudignore` у них тоже
  нет) при их следующем деплое, отдельная работа не заводится.
- Новые открытые вопросы:
  - Требование `dq_gate_fail_open_adj_2026-08-03.md §7` пункт 1 («блокирующее действие ограничено по
    длительности и/или имеет путь подтверждения-и-пропуска») подтверждено `grep`-проверкой как НЕ
    имеющее строки-адресата. Заведена строка `DQ-GATE-BLOCK-BOUNDED` (`07_GAPS.md`, `07_STATE.md`)
    этим же приземлением `ADR-152`; принимаемый остаточный риск деплоя назван явно в `ADR-152 §7`.
  - `cf-finance`/`cf-fx`/`cf-inventory` в код-репо `holika-prod` тоже не имеют `.gcloudignore`
    (найдено попутно при клонировании репо для деплоя `cf-dq`, не проверялось целенаправленно) —
    наблюдение, не задача; фиксируется как находка для следующего деплоя любой из этих функций.
- Блокеры: нет.
- updated_at: 2026-08-10
- обновил: исполнитель (сессия: DQ-GATE-FAIL-OPEN-FIX, деплой и приземление ADR-152 — по прямому
  указанию владельца)

## NEW_DECISIONS
- ADR-152: Мандат класса B на `DQ-GATE-FAIL-OPEN-FIX, деплой` выдан поимённо; объём — один хунк
  одного файла; чужой неразвёрнутый патч `DQ-FRESHNESS-COVERAGE` в ветку деплоя не переносится
  Контекст: см. §Контекст полного текста ADR (`06_DECISIONS_LOG.md`, уже приземлён этой сессией).
  Решение: §1-§7, полный текст — `06_DECISIONS_LOG.md`.
  Последствия: `[сейчас]` — уже приземлены этой же сессией в `07_STATE.md`/`07_GAPS.md`/`06_INDEX.md`
  тем же коммитом, что и сам ADR (`86c1079`); дальнейшие последствия деплоя (read-back, слияние,
  приёмка) приземлены последующими коммитами той же сессии (`a70787f`/`c40559c`/`ceafa0a`).
  Статус: accepted (§1 владелец, §2-§7 архитектор — уже приземлено, повторно сборкой НЕ применять).

## NEW_CONVENTIONS
нет

=== END SESSION ===
