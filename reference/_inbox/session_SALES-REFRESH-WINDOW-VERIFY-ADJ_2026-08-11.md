# FILE: session_SALES-REFRESH-WINDOW-VERIFY-ADJ_2026-08-11.md

=== SESSION LOG · 2026-08-11 · SALES-REFRESH-WINDOW-VERIFY-ADJ ===

## SESSION_LOG

- Задача: `SALES-REFRESH-WINDOW-VERIFY-ADJ` — адъюдикация двух находок независимой проверки
  патча варианта 1 (`SALES-REFRESH-WINDOW-SCOPE-VERIFY`), гейт `Q-108` перед новым мандатом
  класса B. Архитектор, класс A, без брифа (`ADR-086 §1`; определение процитировано дословно
  из гейта `Q-108`, `07_STATE.md:1755`). База `bd8c733`. Дерево `worktrees/SALES-REFRESH-WINDOW-VERIFY-ADJ`,
  ветка `s/SALES-REFRESH-WINDOW-VERIFY-ADJ`. Строка задачи заводится этим блоком (`ADR-086 §3`).
- Сделано:
  - Находка 1 подтверждена независимо И расширена: ложное утверждение живёт в ДВУХ местах,
    не в одном. Проверяющая сессия назвала артефакт (`…rollback_adj…:167`); сплошной поиск
    (`ADR-083 §2`, печать строк — `ADR-044`) нашёл второе вхождение в **докстринге
    `reference/code/cf-facts/main.py:175-176`** — то есть в снапшоте кода, который поедет в
    деплой и который читает следующая сессия.
  - Утверждение разложено на три: «умолчание сохраняет прежнее поведение» — верно;
    «конвейер передаёт 90 явно (`workflow_weekly.yaml:70`)» — ложно (шаг `step_facts`,
    `:63-71`, несёт только `run_id` и `mode`); «расписание не затрагивается» — верно, но
    следует из первого, не из второго. Класс `ADR-075 §3`: верный вывод из неверной посылки.
  - **Развилка `Q-108` снята замером, а не решена вкусом.** Защиту ветки удаления несёт не
    согласованность объявленных окон, а `_assert_staging_covers_merge_window` (`bq_ops.py:403`),
    вызываемый ДО обоих `MERGE` (`:518` продажи, `:731` периметр): он сравнивает фактическое
    покрытие staging с обоими краями окна и ОТКАЗЫВАЕТ (`raise ValueError`), а не предупреждает;
    происхождение числа `window_days` ему безразлично. Явная передача в workflow не добавляет
    безопасности ни на единицу.
  - Найден живой контрпример явности в тех же файлах: `workflow_hourly.yaml:84-85` передаёт
    `window_days: 90` шагу `mode: "purchases"`, а `_run_purchases()` (`main.py:246`) аргументов
    не принимает вовсе — значение парсится, логируется и выбрасывается. Поле `window_days` в
    YAML контрактом не является.
  - **Решение: `Q-108` CLOSED — правится текст, явная передача в `workflow_weekly.yaml` НЕ
    добавляется** (три основания — §3 артефакта). Гейт перед новым мандатом класса B снят.
  - Докстринг `main.py` приведён к коду тем же коммитом (прецедент `ADR-146` — комментарий,
    противоречащий коду, правится до шага деплоя): ложная посылка убрана, назван реальный
    механизм подстановки и реальный носитель защиты. Один хунк одного файла — разделение
    патчей по хункам (`ADR-145 §1`) не нарушено. `py_compile main.py rc=0`.
  - Датированный провенанс-артефакт сессии-автора (`…rollback_adj…:167`) НЕ правится
    (`ADR-021 §4` — ретро-правка выпущенного запрещена); опровержение живёт в артефакте
    проверки, в `Q-108` и в §1 артефакта адъюдикации.
  - Находка 2 (числовой ориентир `42975 = 5578+37397` против `42995 = 5578+37417`) —
    правки не требует: приёмка предиката есть инвариант «сумма частей = итог, ноль коллизий»,
    а не конкретное число, и инвариант выполнен на ОБОИХ замерах (форма прецедента `ADR-119`,
    `ADR-123 §4`, `ADR-124`). Названо применимое действующее правило: счётчик живой таблицы
    есть временнóй факт и обязан нести якорь UTC (`ADR-046 §2`) — без якоря «расхождение на 20»
    неотличимо от «предикат течёт», из-за чего проверке пришлось тратить отдельный запрос на
    исключение дефекта различителя. Применение действующего правила, не новое правило —
    заморозка `ADR-107` не задевается.
  - **Находка сверх двух (найдена этой сессией, проверяющей не называлась):** после патча
    два режима несут противоположные контракты по одному полю тела запроса. `main.py:88`
    парсит `window_days`, `main.py:90` его ЛОГИРУЕТ, `main.py:96` (`weekly`) передаёт дальше,
    `main.py:94` (`hourly`) и `main.py:100` (`purchases`) выбрасывают. Ловушка лежит прямо на
    пути задачи: патч требует ручного первого прогона с расширенным окном, и вызов
    `{"mode":"hourly","window_days":90}` покажет в логе `90`, а загрузит `7` суток — лог чистый
    на вид и ложный (`05_CONVENTIONS` Часть I `★ Успех инструмента ≠ факт`). Код не трогаем
    (второй хунк в файле деплоя = смешивание патчей, `ADR-145 §1`); требование уходит в
    деплой-бриф: расширенный первый прогон идёт ТОЛЬКО `mode=weekly`. Предупреждение внесено
    в тот же докстринг.
- Команды/логи ключевые: `bash tools/session_status.sh` (ЧИСТО, RC 0, база `bd8c733`);
  `bash tools/hooks/selftest.sh` — `пройдено 36, провалено 0`;
  `reference/_scratch_SALES-REFRESH-WINDOW-VERIFY-ADJ_2026-08-11/evidence.sh` →
  `evidence_run.log` (UTC-якорь `2026-08-11T14:59:56Z` в начале и в конце, облачных вызовов нет:
  весь замер по локальному клону, поэтому `gcloud auth list` неприменим);
  `python3 -m py_compile main.py` rc=0; контрольный `grep` докстринга — `передаёт 90 явно`: 0.
- Отклонения от плана: нет.

## STATE_PATCH

- Задача `SALES-REFRESH-WINDOW-VERIFY-ADJ`: (не существовала) → DONE. Заводится и архивируется
  ОДНИМ проходом сборки (прецедент `INVOICES-STALENESS-PROBE`, `FINISH-QUEUE-ADJ`,
  `DQ-GATE-DEPLOY-ADJ`, `FACTS-FLOW-RESTORE-ADJ`).
- `Q-108`: OPEN → **CLOSED** (решение архитектора по гейту «решение владельца/архитектора»).
  Строка уезжает из `07_GAPS.md` в `07_ARCHIVE.md` дословно, тем же коммитом снимается из
  индекса `07_STATE.md §Открытые вопросы`.
- Строка `SALES-REFRESH-WINDOW` (`07_GAPS.md:52`, индекс `07_STATE.md:1723`): гейт
  переписывается. Было — «гейт: решение владельца/архитектора по `Q-107` (текст страховки
  против кода)». Стало — «`Q-108` закрыт (`VERIFY-ADJ`, 2026-08-11): текст правится, явная
  передача `window_days` в workflow не добавляется, докстринг `main.py` приведён к коду тем же
  проходом; независимая проверка патча пройдена. Новый мандат класса B на деплой патча
  варианта 1 НЕ выдан — остаётся запросить у владельца. `Q-107` (`proposed`) деплой технически
  не блокирует».
- Строка мандата `SALES-REFRESH-WINDOW, деплой` (`07_STATE.md:1625`): текст основания
  дополняется — «гейт `Q-108` СНЯТ (`VERIFY-ADJ`, 2026-08-11); мандат по-прежнему НЕ выдан,
  требуется поимённое решение владельца».
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: обе находки независимой проверки патча продаж адъюдицированы — ложная ссылка на
    страховку окна найдена вторым вхождением прямо в коде и там исправлена, числовой ориентир
    признан не требующим правки; артефакт `reference/sales_refresh_window_verify_adj_2026-08-11.md`.
  - Где мы: патч варианта 1 проверен независимо и текстово чист; все технические гейты перед
    деплоем сняты, майский паритет по-прежнему не исправлен — ждёт ровно одного решения владельца.
  - Следующий шаг: владелец выдаёт (или не выдаёт) новый поимённый мандат класса B на деплой
    патча варианта 1 `SALES-REFRESH-WINDOW`; отдельно — апрув `Q-107` (предложение конвенции).
  - Развилки на владельце: новый мандат класса B на деплой патча варианта 1; апрув `Q-107`
    (`proposed`, деплой технически не блокирует).
  - Счётчик: строк списка закрытия осталось `8` из `9`.
- Подробности для модели:
  - **Защиту ветки удаления от рассогласования окон несёт ЗАМЕР, а не объявление.**
    `_assert_staging_covers_merge_window` (`bq_ops.py:403`) вызывается до обоих `MERGE`
    (`:518` продажи, `:731` периметр), сравнивает `MIN`/`MAX(transaction_date)` staging с обоими
    краями окна и отказывает при недостаче (допуск `GUARD_TOLERANCE_DAYS = 3`, `config.py:56`).
    Откуда пришло `window_days` — из тела запроса или из умолчания `main()` — ему безразлично.
    Любой будущий разговор «добавить явную передачу окна ради страховки» обязан начинаться с
    этого факта: явность в YAML безопасности не добавляет. Живой контрпример там же —
    `workflow_hourly.yaml:84-85` передаёт `window_days: 90` режиму `purchases`, который
    аргументов не принимает (`main.py:246`).
  - **`window_days` из тела запроса принимает ТОЛЬКО `mode=weekly`.** `main.py:88` парсит,
    `main.py:90` логирует, `main.py:96` (`weekly`) передаёт, `main.py:94` (`hourly`) и
    `main.py:100` (`purchases`) выбрасывают. Ручной широкий прогон, которого требует патч,
    идёт только `mode=weekly`; вызов с `mode=hourly` покажет в логе запрошенное окно и
    загрузит `7` суток. Предупреждение внесено в докстринг `_run_weekly_load`.
  - **Правка докстринга обязана попасть в новую ветку деплоя** код-репо `holika-prod` вместе
    с патчем варианта 1 — она часть снапшота, а не отдельная задача. Существующая ветка
    `deploy/cf-facts-2026-08-11-refresh-window` несёт ОТКАЧЕННЫЙ код и сохраняется как
    провенанс (`ADR-161`), в неё правка не переносится.
  - **Пункт (iii) прежних деталей устарел в недельной половине.** Записанное «при варианте 1
    расширенное окно первого прогона по-прежнему потребует правки `_run_weekly_load`» неверно:
    правка уже сделана коммитом `a336700` (`main.py:166` — параметр с умолчанием
    `WEEKLY_WINDOW_DAYS`, `main.py:96` — передача). Верна только часовая половина (см. абзац
    выше). Сборщику: заменить абзац, а не дописать рядом.
  - Числа разбиения `core.fact_sales_profit` привязаны к моменту замера и дрейфуют штатно:
    `42975 = 5578 + 37397` (подготовка патча) и `42995 = 5578 + 37417`
    (`2026-08-11T11:18:38Z`). Приёмка предиката — инвариант «сумма частей = итог, ноль
    коллизий», не число. Счётчик живой таблицы есть временнóй факт и записывается с якорем
    UTC (`ADR-046 §2`).
- Новые открытые вопросы: нет.
- Блокеры: нет.
- updated_at: 2026-08-11
- обновил: архитектор (сессия: SALES-REFRESH-WINDOW-VERIFY-ADJ)

## NEW_DECISIONS

- ADR-0XX: `Q-108` закрыт правкой текста, а не правкой конвейера — названная в патче страховка
  окна не является носителем защиты; ложное утверждение найдено вторым вхождением в докстринге
  деплоимого снапшота и исправлено; асимметрия контракта `window_days` по режимам названа как
  ловушка ручного прогона

  Контекст: независимая проверка патча варианта 1 (`SALES-REFRESH-WINDOW-SCOPE-VERIFY`) прошла
  по всем восьми пунктам и оставила две находки по тексту патча. `Q-108` предлагал развилку —
  поправить текст либо добавить явную передачу `window_days: 90` в `workflow_weekly.yaml`,
  «чтобы страховка перестала зависеть от совпадения двух независимых умолчаний». Развилка
  построена на посылке, что защиту ветки удаления несёт согласованность объявленных окон.

  Решение:
  §1. Посылка развилки опровергнута замером: защиту несёт `_assert_staging_covers_merge_window`
  (`bq_ops.py:403`), вызываемый ДО обоих `MERGE` (`:518`, `:731`); он сравнивает фактическое
  покрытие staging с обоими краями окна и ОТКАЗЫВАЕТ, а не предупреждает, и к происхождению
  числа `window_days` безразличен. Явная передача в workflow не добавляет безопасности.
  §2. Явность поля `window_days` в этих YAML контрактом не является — живой контрпример
  `workflow_hourly.yaml:84-85` (`window_days: 90` режиму `purchases`, `_run_purchases()` без
  аргументов, `main.py:246`). Третий экземпляр числа `90` увеличил бы площадь дрейфа.
  §3. `Q-108` закрывается правкой текста; явная передача в `workflow_weekly.yaml` НЕ
  добавляется. Три основания: (i) названная страховка не носитель защиты; (ii) правка YAML
  есть деплой Cloud Workflows, класс B (прецедент `ADR-132`), а форма патча утверждена узкой
  решением владельца (`ADR-144 §8`); (iii) явность в этом файле уже доказала, что вводит в
  заблуждение.
  §4. Ложное утверждение живёт в ДВУХ местах, и второе существеннее первого: докстринг
  `reference/code/cf-facts/main.py:175-176` едет в задеплоенный архив и объясняет механизм
  безопасности следующей сессии. Правится по прецеденту `ADR-146` (комментарий, противоречащий
  коду, — условие приёмки). Датированный провенанс-артефакт `…rollback_adj…:167` НЕ правится
  (`ADR-021 §4`).
  §5. Числовой ориентир разбиения правки не требует: приёмка предиката есть инвариант
  «сумма частей = итог, ноль коллизий», выполненный на обоих замерах (форма `ADR-119`,
  `ADR-123 §4`, `ADR-124`). Счётчик живой таблицы есть временнóй факт и обязан нести якорь UTC
  (`ADR-046 §2`) — применение действующего правила, не новое правило, заморозка `ADR-107` не
  задевается.
  §6. Асимметрия контракта названа: `window_days` из тела запроса принимает только
  `mode=weekly`; `mode=hourly` и `mode=purchases` парсят, логируют и выбрасывают. Код не
  правится (второй хунк в файле деплоя есть смешивание патчей, `ADR-145 §1`); требование
  уходит в деплой-бриф — расширенный первый прогон идёт только `mode=weekly`.
  §7. Гейт `Q-108` перед новым мандатом класса B снят. Незакрытым остаётся `Q-107`
  (`proposed`, апрув владельца), деплой технически не блокирующий.

  Последствия:
  - `[сейчас]` `07_STATE.md §Открытые вопросы` — строка `Q-108` снимается (CLOSED);
    `07_GAPS.md` — строка `Q-108` уезжает в `07_ARCHIVE.md` дословно.
  - `[сейчас]` `07_GAPS.md:52` и индекс `07_STATE.md:1723` (`SALES-REFRESH-WINDOW`) — гейт
    переписывается по тексту `STATE_PATCH`.
  - `[сейчас]` `07_STATE.md:1625` (`SALES-REFRESH-WINDOW, деплой`) — основание дополняется
    «гейт `Q-108` СНЯТ», класс и статус мандата не меняются.
  - `[сейчас]` `07_STATE.md §Мандат Claude Code` — заводится и тем же проходом архивируется
    строка `SALES-REFRESH-WINDOW-VERIFY-ADJ` (класс A, параллель нет, мандат постоянный;
    Пишет: `reference/sales_refresh_window_verify_adj_<date>.md`, `reference/code/cf-facts/main.py`,
    `reference/_scratch_SALES-REFRESH-WINDOW-VERIFY-ADJ_<date>/`), парность `ADR-090 §1`.
  - `[сейчас]` `07_STATE.md §Подробности для модели` — четыре абзаца из `STATE_PATCH`;
    абзац про пункт (iii) ЗАМЕНЯЕТ устаревший, не дописывается рядом.
  - `[без правок]` `reference/sales_refresh_window_rollback_adj_2026-08-11.md` — датированный
    провенанс, ретро-правка запрещена (`ADR-021 §4`).
  - `[без правок]` `reference/code/cf-facts/workflow_weekly.yaml`,
    `reference/code/cf-facts/workflow_hourly.yaml` — явная передача `window_days` не
    добавляется (§3), асимметрия `step_facts` остаётся стилевой (`ADR-051 §2`).
  - `[без правок]` `05_CONVENTIONS.md` — нового правила не вводится, §5 есть применение
    `ADR-046 §2`.
  - `[задачей SALES-REFRESH-WINDOW]` требование «расширенный первый прогон только
    `mode=weekly`» вносится в деплой-бриф при его пересборке; правка докстринга переносится
    в новую ветку деплоя код-репо вместе с патчем.
  - Уже исполнено сессией отдельным коммитом ветки: `reference/code/cf-facts/main.py`
    (докстринг `_run_weekly_load`), `reference/sales_refresh_window_verify_adj_2026-08-11.md`,
    `reference/_scratch_SALES-REFRESH-WINDOW-VERIFY-ADJ_2026-08-11/`.

  Статус: accepted (архитектор — по гейту `Q-108` «решение владельца/архитектора»)

## NEW_CONVENTIONS

- нет

=== END SESSION ===
