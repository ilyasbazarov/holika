# FILE: reference/_inbox/session_PARITY-STOCK-SNAPSHOT-SYNC-GEN_2026-08-04.md

=== SESSION LOG · 2026-08-04 · PARITY-STOCK-SNAPSHOT-SYNC-GEN ===

## SESSION_LOG

- Задача: `PARITY-STOCK-SNAPSHOT-SYNC-GEN` — сборка брифа под `PARITY-STOCK-SNAPSHOT-SYNC`
  (`ADR-083 §7`: режим генерации берёт имя `<TASK>-GEN` для дерева, ветки и task-ID блока).
  Класс A, мандат постоянный, задача НЕ исполнялась — только собран бриф.
- База: `961a77199119235500f74e4db70dc13f3b9f4099`. Дерево/ветка:
  `worktrees/PARITY-STOCK-SNAPSHOT-SYNC-GEN` / `s/PARITY-STOCK-SNAPSHOT-SYNC-GEN`.
- Повод: попытка исполнить `PARITY-STOCK-SNAPSHOT-SYNC` напрямую по строке задачи остановлена —
  `ADR-086 §1` разрешает исполнение без брифа только классу A, а задача класса B. Три из четырёх
  условий §1 при этом выполнены (определение и приёмка записаны дословно строкой, набор файлов
  объявлен); не выполнено ровно условие класса. Владелец выбрал собрать бриф (чат 2026-08-04,
  «идём вариантом А, собирай бриф»), а не снимать требование для одной задачи.
- Сделано: собран `briefs/PARITY-STOCK-SNAPSHOT-SYNC.md` по шаблону `08` и правилам
  `_GENERATOR.md §4a/§5`. Класс проставлен из таблицы мандата (`B`, мандат ВЫДАН — гейт исполнения
  по `_GENERATOR.md §4a` НЕ ставится), параллель `нет`, файлы на запись — маркированным списком в
  машиночитаемой форме (`ADR-083 §1`).
- Три находки независимого перечитывания постановки (ради них и собирался бриф — см. `STATE_PATCH`).
- Команды/логи ключевые: облачных вызовов НЕТ. `bash tools/session_status.sh` (RC 0),
  `git worktree add`, чтение репо, сплошные поиски `grep -rn` с печатью совпавших строк.
- Отклонения от плана: нет.

## STATE_PATCH

- Задача `PARITY-STOCK-SNAPSHOT-SYNC`: статус остаётся **`READY`**, дописка в колонку «как
  закрывается» (`07_GAPS.md`) и в колонку гейта индекса (`07_STATE`): «бриф
  `briefs/PARITY-STOCK-SNAPSHOT-SYNC.md` собран (`PARITY-STOCK-SNAPSHOT-SYNC-GEN`, 2026-08-04), не
  взят».
- Строка мандата `PARITY-STOCK-SNAPSHOT-SYNC` (`07_STATE §Мандат Claude Code: класс задач`) — та же
  дописка «бриф собран»; класс, параллель, мандат и поле `Пишет:` не меняются.
- Отдельной задача-строки под `PARITY-STOCK-SNAPSHOT-SYNC-GEN` НЕ заводится — прецедент всех прежних
  генераций (`SALES-REFRESH-WINDOW-GEN`, `INVOICES-BACKFILL-GEN`, `DQ-GATE-SCOPE-SPLIT-GEN` и др.):
  генерация фиксируется дописью «бриф собран» в строку родительской задачи, парность `ADR-090 §1`
  этим не затрагивается.
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, ровно пять строк):
  - **Прошлый шаг:** собран бриф `briefs/PARITY-STOCK-SNAPSHOT-SYNC.md` — работы, закрывающей
    строку 21 реестра паритета («Остатки товаров»).
  - **Где мы:** пять пар реестра из семи закрыты; у строки 21 есть полное правило-моста, выданный
    мандат и готовый бриф — не хватает единственного допустимого измерения, которого не было ни разу.
  - **Следующий шаг:** `PARITY-STOCK-SNAPSHOT-SYNC` шаг 1 (класс B, мандат выдан, бриф готов, проба
    ретроспективного момента, любой час); `DQ-GATE-SCOPE-SPLIT-DEPLOY` (класс B, мандат выдан, бриф
    готов) и `FACTS-FLOW-RESUME-CONFIRM` идут независимо.
  - **Развилки на владельце:** остаток шага 1 — публичная документация МойСклада инструменту
    недоступна, окончательный ответ «умеет ли API отдавать остатки задним числом» закрывает владелец.
  - **Счётчик:** пары реестра **5/7** сходятся · измерено 7/7 · Epic M 6/7 фаз.
- Подробности для модели (append в одноимённый подраздел `07_STATE`):

  **Бриф `PARITY-STOCK-SNAPSHOT-SYNC` собран; независимое перечитывание постановки дало три
  находки, которых не было в `ADR-123 §8` (`PARITY-STOCK-SNAPSHOT-SYNC-GEN`, 2026-08-04).** Полный
  текст — `briefs/PARITY-STOCK-SNAPSHOT-SYNC.md`, здесь не пересказывается. Что обязана знать
  сессия-исполнитель. (i) **Имени параметра ретроспективного момента у `report/stock/all` в репо
  НЕТ, и бриф его не утверждает.** Кандидат `moment` выведен по аналогии с соседними отчётами
  (`report/profit/*` принимают `momentFrom`/`momentTo` в формате `YYYY-MM-DD HH:MM:SS`,
  `reference/report_fields_2026-07-31.md §2/§3`) — это аналогия, не документированный факт. Отсюда
  жёсткая граница вывода: отрицательный исход пробы означает «параметр С ЭТИМ ИМЕНЕМ не сработал», а
  НЕ «МойСклад не умеет отдавать остатки задним числом»; окончательное закрытие требует публичной
  документации API, инструменту недоступной, и остаётся за владельцем. Молчаливое игнорирование
  неизвестного параметра неотличимо от его отсутствия — поэтому проба обязана логировать сырое тело
  ответа целиком, включая массив `errors`, и брать ретроспективный момент заведомо далёким (несколько
  суток, при суточном движении в тысячи штук), иначе «ответы совпали» неотличимо от «склад не
  менялся». (ii) **Ловушка партиции, способная тихо испортить весь ночной замер:** `date_snapshot`
  есть дата по БИШКЕКУ (`cf-inventory/main.py:151-156`), а прогон идёт в `21:00Z`, то есть в `03:00`
  следующих бишкекских суток. Партиция прогона `21:00Z` числа `<D>` несёт `date_snapshot = <D+1>`;
  взятая «по сегодняшней дате» партиция окажется суточной давности, и замер повторит ровно ту ошибку,
  ради устранения которой затевается. (iii) `_loaded_at` штампуется ДО выборки (`main.py:157` против
  `main.py:168`), то есть это отметка начала прогона; при `PAGE_SIZE = 1000` против ~300 строк весь
  ответ умещается в одну страницу и разница исчисляется секундами, но в артефакт окно пишется как
  наблюдённое, а не как предполагаемое.

  **Строчные адреса в брифах устаревают молча — найден живой инстанс.** `briefs/PARITY-STOCK-ROWWISE.md`
  цитирует `reference/code/cf-finance/main.py:39` как каноническую форму заголовка авторизации; по
  этому адресу сегодня лежит `for entity_type in ["paymentout", "cashout"]:` — снапшот `cf-finance`
  обновлялся после написания брифа (`INVOICES-LOADER-DEPLOY`, 2026-08-03). Действующие адреса формы:
  `reference/code/cf-inventory/helpers.py:51-55` (заголовок) и `helpers.py:20-25` (получение токена);
  для задач по остаткам брать именно их — это код самого производителя нашей стороны. Новый бриф
  цитирует `cf-inventory`, а не `cf-finance`, и называет причину. Отдельной строки реестра не
  заводится (`ADR-107 §4`): процессный слой заморожен, ни одна задача паритета без правила о цитатах
  не встаёт.

- Новые открытые вопросы: нет (`CONTEXT GAP` про имя параметра назван внутри строки
  `PARITY-STOCK-SNAPSHOT-SYNC` и в `ADR-123 §7`, собственного `Q`-номера не получает, `ADR-051 §2`).
- Блокеры: нет.
- updated_at: 2026-08-04
- обновил: генератор (сессия: `PARITY-STOCK-SNAPSHOT-SYNC-GEN`)

## NEW_DECISIONS

- нет. Постановка задачи уже принята `ADR-123 §8`; настоящая сессия её не пересматривает, а
  переносит в форму брифа и добавляет три исполнительские оговорки (см. `STATE_PATCH`), ни одна из
  которых не двигает значений принятого решения.

## NEW_CONVENTIONS

- нет.

=== END SESSION ===
