=== SESSION LOG · 2026-08-02 · INVOICES-LOADER-BUILD ===

## SESSION_LOG
- Задача: `INVOICES-LOADER-BUILD` (T3 программы `ADR-110`) — построение кода загрузчика счетов
  покупателям внутри снапшота `cf-finance`, по проекту `reference/invoices_loader_design_2026-08-02.md`.
- Сделано:
  - Снят свежий снапшот живого `cf-finance` (ревизия `00012-cik`), sha256 `main.py` и архива
    совпали побайтово с независимой записью `reference/code_repo_standup_d2_2026-08-01.md`.
    Подтверждено: диспетчер режимов в живом коде отсутствовал (гэп 1 design §11.1 закрыт фактом).
  - Реализован диспетчер режимов (`mode="payments"|"invoices"`, `run_etl()` не тронут) —
    `reference/code/cf-finance/main.py`.
  - Реализован новый модуль `reference/code/cf-finance/invoices.py`: пагинация явным
    `offset`/`limit`, `G1`/`G2` полноты, конвертация валюты (три ветви + обязательный fallback,
    без хардкода `1.0`), схема staging на 19 колонок, текст `MERGE` дословно из design §7.5
    (ключ `invoice_id`, явный `INSERT(колонки)`, ветка `WHEN NOT MATCHED BY SOURCE THEN DELETE`),
    тексты обеих проверок свежести, печать счётчиков прогона.
  - Добавлена зависимость `tenacity` в `requirements.txt`.
  - Прогнаны все проверки чек-листа `design §13`, достижимые без секрета `msklad-token` (10
    пунктов, 8 полных + 2 частичных с явно названным остатком) — все вердикты числами, не метками.
  - Создана и использована непрод-таблица `stg_msklad.fact_customer_invoices_core_test_staging`
    (имя оканчивается на `_staging`, попадает под явно разрешённый мандатом шаблон) для
    проверки идемпотентности/ветки удаления `MERGE` БЕЗ единой записи в живую
    `core.fact_customer_invoices` — решение зафиксировано и обосновано в артефакте.
  - Собран `reference/invoices_loader_build_2026-08-02.md` — все числовые вердикты, провенанс,
    самодостаточность для `T4`.
- Команды/логи ключевые: `reference/_scratch_INVOICES-LOADER-BUILD_2026-08-02/` (снапшот,
  скрипты проверок и их логи, `cf_finance_describe.json`, `cf_finance_logs_30d.json`).
- Отклонения от плана: одно, названо явно и обосновано в артефакте §2 (п.7/8) — идемпотентность
  и ветка удаления `MERGE` проверены против непрод-копии core-схемы, а не буквально против
  живой `core.fact_customer_invoices`, потому что бриф одновременно требует реального
  исполнения `MERGE` (чек-лист) и запрещает запись в `core.*` (граница мандата) — оба
  требования выполнены одновременно только так.

## STATE_PATCH
- Задача `INVOICES-LOADER-BUILD`: не взят → DONE (артефакт `reference/invoices_loader_build_2026-08-02.md`,
  самодостаточен для `T4`; живая `core.fact_customer_invoices` не изменялась, только читана).
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком):
  - Прошлый шаг: `INVOICES-LOADER-BUILD` (T3) закрыт DONE — код загрузчика счетов построен и
    проверен всеми достижимыми без секрета проверками, артефакт `reference/invoices_loader_build_2026-08-02.md`
  - Где мы: восстановление потока данных остаётся приоритетом перед сверкой паритета «на
    сейчас» (`FACTS-WORKFLOW-STOP-DIAG`, `ADR-111`); программа загрузчика счетов дошла до
    построенного кода (T3 DONE), T4/T5/T6 остаются классом B без мандата
  - Следующий шаг: мандат класса B на `INVOICES-LOADER-DEPLOY` (T4) — единственное основание,
    доступ к секрету `msklad-token` для закрытия гэпа 3 (проверка `expand` на живом документе) и
    самого деплоя; решение владельца о форме восстановления загрузки
    (`FACTS-WORKFLOW-STOP-DIAG`/`ADR-111`) остаётся приоритетным параллельно; отдельно решение
    владельца/архитектора по гэпу COGS-источника (`SALES-REFRESH-WINDOW`)
  - Развилки на владельце: решение о форме восстановления загрузки; первый `push` код-репо в
    `holika-prod`; ответ клиента по часовому поясу (`MSKLAD-TZ-PROPOSAL`); мандат класса B на
    `AUDIT-SNAPSHOT-FIX-EMPLOYEES`/`AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`; выбор варианта патча
    `SALES-REFRESH-WINDOW`; мандат класса B на `INVOICES-LOADER-DEPLOY` (T4, новое — доступ к
    секрету для гэпа 3 плюс деплой)
  - Счётчик: пары реестра 2/7 сходятся · таблиц живых 15 из 26 (до восстановления, не
    переизмерялось) · строк реестра на критическом пути 23 из 63 · Epic M 5/7 фаз
- Подробности для модели: `T3` программы `ADR-110` закрыт кодом, не деплоем — построенный
  `reference/code/cf-finance/invoices.py` + патч `main.py` (провенанс и sha256 —
  `reference/code/cf-finance/MANIFEST.md`) готовы к переносу на `T4`, но требуют СВОЕГО свежего
  снапшота на момент переноса (ревизия могла измениться). Ключевое числовое наблюдение сессии:
  текущий суточный прогон платежей (`finance-daily-update`) занимает в среднем `824с` (17 прогонов
  за 30 суток, `785…874с`) против потолка `1800с` — задел для нового прогона счетов трёхзначный,
  замер сделан read-only чтением Cloud Logging, без секрета и без деплоя (метод —
  `reference/_scratch_INVOICES-LOADER-BUILD_2026-08-02/checks/cf_finance_logs_30d.json`).
  Тестовая непрод-таблица `stg_msklad.fact_customer_invoices_core_test_staging` создана этой
  сессией под шаблон `*_staging` и самоочистится TTL-политикой датасета (14 суток) — отдельного
  удаления не требует, но если следующая сессия увидит её раньше TTL, это провенанс этой
  сессии, не мусор.
- Новые открытые вопросы: остаток раздела брифа «Живые вызовы к МойСкладу — граница класса» НЕ
  снят этой сессией (ожидаемо под классом A) — гэп 3 design-документа (`§11.3`): проверка
  `expand=state,agent,salesChannel,rate.currency` на реальном документе МойСклада требует живой
  `GET` с секретом `msklad-token`, что по `ADR-076 §1` есть класс B; мандат строки
  `07_STATE.md:432` секрета не упоминает. Design-документ называет это расхождение и явно НЕ
  решает его («работа архитектора»); эта сессия тоже его не решает — anti-improvisation запрещает
  подставить «наверное можно один раз». Тот же секрет гейтит и деплойную часть `T4`. Отдельный
  `Q`-номер не заводится (объект тот же, что уже назван design §12 «Процессное наблюдение» и
  требует решения архитектора о классе `INVOICES-LOADER-DEPLOY`, не новую находку).
- Блокеры: `T4` (`INVOICES-LOADER-DEPLOY`) не может стартовать без мандата класса B — единственное
  основание мандата уже названо (доступ к секрету `msklad-token`), решение — архитектора/владельца,
  прецедент формы — `ADR-092`/`ADR-100 §9` (мандат поимённо на одну сессию).
- updated_at: 2026-08-02
- обновил: исполнитель (сессия: INVOICES-LOADER-BUILD)

## NEW_DECISIONS
нет

## NEW_CONVENTIONS
нет

=== END SESSION ===
