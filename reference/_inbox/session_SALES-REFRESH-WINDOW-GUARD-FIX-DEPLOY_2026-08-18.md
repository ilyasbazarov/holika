# FILE: session_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY_2026-08-18.md

=== SESSION LOG · 2026-08-18 · SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY ===

## SESSION_LOG

- Задача: `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY` — деплой `cf-facts` (форма гард-фикса ф3/ф4,
  `run_id`-обвязка), класс B, мандат выдан поимённо (`guard_fix_deploy_mandate_2026-08-18.md`)
- Сделано:
  - Прогон предусловия П1 (контракт `run_id` в живых `sourceContents` `msklad-pipeline-hourly`
    и `msklad-pipeline-weekly`) — ЗАКРЫТО фактом, строки с номерами напечатаны
  - Прогон предусловия П4 (ровно два файла в diff против `master`, `.gcloudignore`, сплошной
    поиск секретов) — ЗАКРЫТО фактом, ветка деплоя `deploy/cf-facts-2026-08-18-guard-fix-f3`
    подготовлена ЛОКАЛЬНО (коммит `82c5027`, НЕ запушена)
  - Прогон предусловия П2 (обслуживающая ревизия `cf-facts-00017-jon`, `percent=100`) —
    ЗАКРЫТО фактом
  - Прогон предусловия П3 (дрейф `master` ↔ обслуживающая ревизия) — **ПРОВАЛЕНО**: sha256
    пяти файлов (`bq_ops.py`, `config.py`, `fetch_perimeter.py`, `helpers.py`, `main.py`)
    архива `cf-facts-00017-jon` не совпадает со свежим `master` код-репо; причина установлена
    двумя независимыми способами (git-история код-репо + маркеры кода doc-репо vs master) —
    полный разбор `reference/sales_refresh_window_guard_fix_deploy_2026-08-18.md §5`
  - **Деплой НЕ исполнялся.** `git push` и `gcloud functions deploy` не вызывались ни разу
- Команды/логи ключевые: `reference/_scratch_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY_2026-08-18/
  step1_run.log` … `step4_run.log` (UTC-якорь и `gcloud auth list` первой и последней командой
  каждого скрипта)
- Отклонения от плана: да — мандат предписывал деплой из ветки, заведённой от `master`; факт
  показал, что `master` не несёт три уже задеплоенных патча (`cf-facts-00012-ber`/`-00014-doh`/
  `-00017-jon`), поэтому деплой по буквальному тексту мандата откатил бы прод молча.
  Предусловие П3 сработало как задумано и остановило сессию ДО push/deploy

## STATE_PATCH

- Задача `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY`: мандат выдан, деплой начат → **СТОП на
  предусловии П3, деплой НЕ исполнен**; ветка мандата `ADR-189` остаётся действующей, но
  инструкция «от `master`» (`§1`) требует пересмотра владельцем/архитектором прежде, чем
  сессия сможет продолжить
- Стенд-ап (пять строк):
  - Прошлый шаг: предусловие П3 деплоя `cf-facts` guard-fix ф3/ф4 провалено фактом —
    `master` код-репо не несёт три уже задеплоенных патча `SALES-REFRESH-WINDOW`
    (`reference/sales_refresh_window_guard_fix_deploy_2026-08-18.md`)
  - Где мы: критический путь к недельному прогону `2026-08-23` заблокирован — деплой
    гард-фикса не может исполниться по тексту текущего мандата без риска молча откатить
    три живых патча прода
  - Следующий шаг: развилка §7 артефакта — владелец/архитектор выбирает базу ветки деплоя
    (`deploy/cf-facts-2026-08-12-completeness-and-delete` вместо `master`, либо дождаться
    слияния ступени 3); новый текст мандата/уточнение — задачей архитектора, не этой сессией
  - Развилки на владельце: да — выбор базы ветки деплоя гард-фикса (§7 артефакта)
  - Счётчик: строк списка закрытия осталось `7` из `9` (без изменений — деплой не исполнен);
    критический путь остаётся тем же одним звеном, срок `2026-08-23` не сдвинут
- Подробности для модели: `master` код-репо `holika-prod` НЕ равен обслуживающей ревизии
  `cf-facts` начиная с `cf-facts-00012-ber` (три коммита `47b3a45`/`9ae9a84`/`543b6c1` не
  являются предками `master` — намеренно, слияние гейтится ступенью 3, `2026-08-23`). Любой
  будущий деплой `cf-facts`, ведомый буквально «от `master`» без явной проверки предусловия
  П3, повторит эту же ловушку. Полный разбор — `reference/sales_refresh_window_guard_fix_
  deploy_2026-08-18.md`
- Новые открытые вопросы: нет (развилка §7 — не GAP, а решение владельца/архитектора по
  уже названным вариантам, не требует discovery)
- Блокеры: `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY` заблокирована решением владельца/
  архитектора по базе ветки деплоя (§7 артефакта)
- updated_at: 2026-08-18
- обновил: исполнитель (сессия: `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY`)

## NEW_DECISIONS

нет

## NEW_CONVENTIONS

нет

=== END SESSION ===
