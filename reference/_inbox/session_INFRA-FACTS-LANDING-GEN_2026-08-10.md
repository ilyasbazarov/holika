=== SESSION LOG · 2026-08-10 · INFRA-FACTS-LANDING-GEN ===

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)
- Задача: T-INFRA-FACTS-LANDING-GEN — генерация брифа задачи `INFRA-FACTS-LANDING` по `08_TASK_BRIEF_TEMPLATE`
- Сделано:
  - собран `briefs/INFRA-FACTS-LANDING.md`: класс A и параллель «да» взяты из `07_STATE §Мандат Claude Code`
    (строка `INFRA-FACTS-LANDING`), поле «Файлы на запись» — маркированным списком, шесть путей
  - при чтении входов найдены две ловушки устаревания, обе вынесены владельцу блоком вопросов и решены им
    2026-08-10; оба решения перенесены в бриф с основанием
  - поле «Файлы на запись» проверено механически: `bash tools/parallel_check.sh INFRA-FACTS-LANDING
    MARTS-BUILD-STAMP` → `6 путей · класс A · источник: briefs/INFRA-FACTS-LANDING.md`, RC 0
- Команды/логи ключевые: `bash tools/session_status.sh` (RC 0, буфер 0, отставание 0, HEAD `fceb62f`);
  `bash tools/hooks/selftest.sh` → `пройдено 36, провалено 0`; `shasum -a 256` двух снимков конвейеров;
  сплошные `grep -rn` по `cf-alert`, `pipeline-weekly`, `X=39\|Y=148\|Z=439`
- Отклонения от плана: рабочее дерево и ветка сессии генерации носят имя `INFRA-FACTS-LANDING`, а не
  `INFRA-FACTS-LANDING-GEN` (`ADR-083 §7`). Дерево и ветку заводил владелец до старта сессии; переименование
  сессией не производилось. Task-ID настоящего блока взят по `ADR-083 §7` (`-GEN`). Следствие названо в
  «Подробности для модели»

## STATE_PATCH  (→ применить к 07_STATE.md)
- Задача `INFRA-FACTS-LANDING`: READY (гейтов нет) → READY, **бриф собран, не взят**
- Строка `07_STATE §Мандат Claude Code`, задача `INFRA-FACTS-LANDING`: поле `Пишет:` дополнить
  `03_PIPELINE_SPEC.md` (решение владельца 2026-08-10, основание — в брифе §Входы п.3). Строка мандата
  снимается вместе с задача-строкой при закрытии задачи (`ADR-090 §1`), поэтому дополнение живёт ровно
  до её закрытия
- Стенд-ап (ЗАМЕНЯЕТ блок `### Стенд-ап` в `07_STATE` целиком, НЕ дописывается, ровно пять строк):
  - Прошлый шаг: собран бриф `briefs/INFRA-FACTS-LANDING.md` — перенос трёх групп уже измеренных
    инфра-фактов в живые документы; при сборке найдено, что часть фактов замера 2026-08-01 успела
    устареть, источники переноса заменены на актуальные снимки репозитория
  - Где мы: счётчик реестра паритета остаётся `7/7`; из очереди передачи готов к запуску ещё один
    документный шаг класса A, облачных вызовов не требующий
  - Следующий шаг: `INFRA-FACTS-LANDING` (бриф готов), `DQ-GATE-FAIL-OPEN-FIX` подготовка,
    `DQ-FRESHNESS-COVERAGE` подготовка
  - Развилки на владельце: предусловие мандата `SALES-REFRESH-WINDOW`; апрув `ERP-CONTRACTS-DAY-RULE-DOC`;
    подпись момента пересборки на странице «в пути» (исполнителя нет); решение по дефекту `_loaded_at`
    в `cf-finance`/`cf-loss-commission`, оставленное архитектору сессией `DQ-FRESHNESS-COVERAGE`
  - Счётчик: пары реестра `7/7` · обязательных задач передачи `10` (из них с выданным мандатом `3`) ·
    Epic M — (Epic-1 завершён `ADR-140`)
- Подробности для модели:
  **Бриф `INFRA-FACTS-LANDING` собран (сессия `INFRA-FACTS-LANDING-GEN`, 2026-08-10), не взят. Класс A,
  параллель «да», шесть путей на запись.** Что обязана знать сессия-исполнитель и чего нет в самой
  строке задачи. (i) **Факты `Q-13` (состав шагов недельного прогона) из `reference/infra_facts_sweep_2026-08-01.md`
  УСТАРЕЛИ** — после того замера недельный конвейер разворачивали дважды: `DQ-GATE-SCOPE-SPLIT-DEPLOY`
  (2026-08-05, закупки и возвраты переставлены ДО гейта) и `SALES-PERIMETER-CADENCE-DEPLOY` (2026-08-07,
  добавлены `step_perimeter`/`step_perimeter_promote`). Источник переноса заменён решением владельца
  2026-08-10 на снимки `reference/code/cf-facts/workflow_weekly.yaml` (`sha256 a1a58a2f…`, ревизия
  `000005-124`) и `workflow_hourly.yaml` (`sha256 821edbee…`, ревизия `000004-5fc`), чьи суммы побайтово
  сверены с read-back'ом живых сервисов (`reference/code/cf-facts/MANIFEST.md §Cloud Workflows`).
  Тем же решением в scope добавлено исправление порядка ЧАСОВОГО прогона в `01_ARCHITECTURE.md:84`:
  он неверен с 2026-08-05 и стоит в том же абзаце, куда ляжет недельный блок. (ii) **Вывод «DQ-алерт —
  мёртвая проводка» (`reference/dq_source_capture_2026-08-02.md §5`) СНЯТ** `ADR-149`
  (`DQ-ALERT-FILTER-FIX`, DONE, `07_ARCHIVE.md:216`) — перенос его в живой док запрещён брифом явным
  пунктом-ловушкой. (iii) **`Q-11` в доме уже закрыт, остаток в другом файле** — свежие числа
  (`X=28`, `Y=164`, `Z=338`, `530` строк) стоят в `07_STATE §Контрольные цифры` с 2026-08-01, а
  `03_PIPELINE_SPEC.md:250` несёт указатель на дом со СТАРЫМИ числами `39/148/439`; решением владельца
  2026-08-10 файл включён в набор на запись (документы исполнения правятся session-блоком, ADR не нужен),
  поэтому набор брифа шире поля `Пишет:` строки мандата на один путь. (iv) **Правка `01_ARCHITECTURE.md`
  (STABLE) нового ADR не требует** — она уже назначена `ADR-142 §4` и его блоком последствий, строкой
  «[задачей `INFRA-FACTS-LANDING`] перенос трёх групп фактов». (v) **Дерево и ветка этой сессии генерации
  названы `INFRA-FACTS-LANDING`, без суффикса `-GEN`** (`ADR-083 §7`): сессия-исполнитель не сможет завести
  дерево и ветку под своим именем, пока проход сборки не сольёт и не уберёт эти. Порядок: сначала сборка
  этого буфера, потом запуск исполнения.
- Новые открытые вопросы: нет
- Блокеры: нет
- updated_at: 2026-08-10
- обновил: генератор (сессия: INFRA-FACTS-LANDING-GEN)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR; "нет" если нет)
нет — сессия генерации решений не принимает. Два решения по scope приняты владельцем в чате 2026-08-10
и зафиксированы в брифе и в `STATE_PATCH` выше: (а) `03_PIPELINE_SPEC.md` включён в набор файлов на
запись; (б) состав шагов переносится из актуальных снимков репозитория, оба конвейера, без облачных вызовов.

## NEW_CONVENTIONS  (→ предложение правки 05; "нет" если нет)
нет

=== END SESSION ===
