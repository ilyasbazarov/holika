=== SESSION LOG · 2026-07-29 · SOURCE-MAP-SALES-GEN ===

> Имя файла несёт суффикс `-GEN` намеренно: это блок ГЕНЕРАТОРА брифа, а не блок исполнителя задачи
> `SOURCE-MAP-SALES`. Исполнитель положит свой блок как `session_SOURCE-MAP-SALES_<date>.md`, и при
> совпадении дат два блока столкнулись бы именем, что противоречит мотиву `ADR-080 §4ii`
> («имя уникально по построению»). Идентификатор латиницей (`ADR-041 §6`).

## SESSION_LOG  (→ append в /docs, опц. CHANGELOG)

- Задача: `SOURCE-MAP-SALES-GEN` — генерация task-brief задачи `SOURCE-MAP-SALES` по роли `_GENERATOR.md`
  и шаблону `08_TASK_BRIEF_TEMPLATE.md` версии 2.0.
- Режим запуска: Claude Code, отдельное рабочее дерево `.claude/worktrees/SOURCE-MAP-SALES`, ветка
  `s/SOURCE-MAP-SALES`, по решению владельца. `09_RUNBOOK §2` (режим Г) допускает генерацию в основном
  дереве; запуск в дереве — выбор владельца, отклонением от правила не является, но имеет следствие,
  вынесенное ниже отдельным пунктом.
- SHA на старте: `git rev-parse HEAD` = `79905d2934ee8b844c9898410ca80e10f00580eb`.
- Стоп-условия старта проверены и не наступили: дерево чистое (`git status --short` пуст),
  `reference/_inbox/` содержал только `.gitkeep`, `git worktree list` — два дерева (основное + это),
  `git branch --list 's/*'` — одна ветка (эта). Проверка `ADR-054` пройдена по всем прочитанным докам:
  `_METHOD`, `00_CHARTER`, `01_ARCHITECTURE`, `02_ERP_CONTRACTS`, `03_PIPELINE_SPEC`, `04_ROADMAP`,
  `05_CONVENTIONS`, `06_DECISIONS_LOG`, `06_INDEX`, `07_STATE`, `07_ARCHIVE`, `08_TASK_BRIEF_TEMPLATE`,
  `09_RUNBOOK`, `11_INFRA_FACTS`, `_GENERATOR` — первая строка каждого совпала с его именем/номером.
- Сделано:
  - Прочитан обязательный контекст плюс точечно `ADR-079` (целиком), `ADR-080`, `ADR-081`, `ADR-082`
    из `06_DECISIONS_LOG.md`.
  - Собран и записан `briefs/SOURCE-MAP-SALES.md` по шаблону `08` версии 2.0: заполнены новые поля
    «Класс задачи», «Параллель», «Файлы на запись»; raw-URL в «Context-to-load» не приводятся
    (`ADR-082 §2`); секция «Роль» — под самостоятельное исполнение.
  - Класс `A` и признак параллелимости `да` взяты ДОСЛОВНО из строки `SOURCE-MAP-SALES` таблицы
    `07_STATE §Мандат Claude Code: класс задач`; расхождения между брифом и таблицей нет.
  - Задача квалифицирована как discovery-бриф (`_GENERATOR §Тип`, `_METHOD §11`): return-this —
    обогащение репо (`/reference`-артефакт + снапшот кода + слот в `11`), не прод-код.
  - **Гэп `Q-83` обнаружен, вынесен владельцу и ЗАКРЫТ в пределах этой же сессии.** Владелец восстановил
    текст строки из закрытого рабочего чата `PARITY-REGISTRY-BRIDGE`. Все пять фактических утверждений
    формулировки проверены по репо на SHA `79905d2` и подтверждены: описание `revenue_kgs` как «Нетто
    выручка KGS» (`02_ERP_CONTRACTS.md` строки 47–49); повторный вычет в марте
    (`reference/sql/sq_marts_sales_overview.sql:85`); LEFT JOIN на `core.fact_returns` (там же, строки
    18–29 и 123–126); шапка SQL, утверждающая, что блок не подключён и поля нулевые, при действующем
    незакомментированном блоке в теле (строки 6–10); `03_PIPELINE_SPEC.md:125`, документирующий join как
    действующий. Существование всех колонок различителя подтверждено (`02_ERP_CONTRACTS.md:88–101`,
    `core.fact_returns`). Вводных достаточно: различитель исполним запросом без единого допущения.
    Шаг 4в брифа переписан из гейта в обязательный замер с назначенной ЗАРАНЕЕ таблицей разбора четырёх
    исходов — чтобы вердикт не подгонялся под полученные цифры.
- Команды/логи ключевые: `git status`/`rev-parse`/`worktree list`/`branch --list`;
  `grep -rn "Q-83" . --exclude-dir=.git` (три совпадения, все — ссылки, определения нет);
  `git log --oneline -S "ADR-079" -- 07_STATE.md` → `20b01da` («Add files via upload»);
  `bash tools/hooks/selftest.sh` → «пройдено 28, провалено 0».
- Отклонения от плана: гэп по `Q-83` (см. выше) — обнаружен, вынесен владельцу полным блоком, закрыт его
  ответом в пределах сессии; бриф доработан, гейт снят. Содержание различителя сессией не сочинялось
  ни на одном этапе (`05` Часть I ★ Anti-improvisation).

## STATE_PATCH  (→ применить к 07_STATE.md)

- Задача `SOURCE-MAP-SALES`: READY (брифа нет) → **READY, бриф готов** (`briefs/SOURCE-MAP-SALES.md`
  @ ветка `s/SOURCE-MAP-SALES`). Статус самой задачи не меняется — она не исполнялась.
- Текущий фокус: без изменений — `SOURCE-MAP-SALES` остаётся первой в очереди; дальше режим С (сборка),
  затем режим И (исполнение брифа).
- Новые открытые вопросы: **завести строку `Q-83` в `07_STATE §Открытые вопросы` — текстом ниже,
  дословно.** Строки в репо на SHA `79905d2` НЕТ, хотя раздел «Последствия» `ADR-079` предписывает её
  завести; `Q-83` упоминается трижды и все три раза как ссылка на несуществующую строку
  (`06_DECISIONS_LOG.md:1106`, `06_DECISIONS_LOG.md:1117`, `07_STATE.md:109`). `ADR-079` применён
  коммитом `20b01da` («Add files via upload»), этот пункт его blast radius не исполнен.
  **Провенанс текста:** предоставлен владельцем 2026-07-29 в чате настоящей сессии, восстановлен им из
  закрытого рабочего чата сессии `PARITY-REGISTRY-BRIDGE`. Настоящей сессией текст НЕ сочинён и не
  дополнен — приведён как получен. Все пять фактических утверждений формулировки сверены с репо на
  SHA `79905d2` и подтверждены (см. `SESSION_LOG` выше и таблицу сверки в разделе `Q-83` брифа).

  Строка для вставки:

  > | Q-83 | (`PARITY-REGISTRY-BRIDGE`, 2026-07-29) `02 §схемы core` описывает
  > `core.fact_sales_profit.revenue_kgs` как «Нетто выручка KGS» при наличии отдельных `sell_sum_kgs` и
  > `return_sum_kgs`, а `reference/sql/sq_marts_sales_overview.sql` вычитает возвраты повторно:
  > `net_revenue_kgs = ROUND(f.revenue_kgs - COALESCE(r.return_sum_kgs, 0), 2)` по LEFT JOIN на
  > `core.fact_returns`. Если описание в `02` верно, возвраты вычитаются дважды. Дополнительно шапка того
  > же SQL утверждает, что блок возвратов не подключён и поля нулевые, тогда как `03 §marts` документирует
  > join как действующий: одно из двух устарело | факт (кандидат в дефект) | **ОТКРЫТ, блокирует мост пары
  > «Инвестор»/«Операционка»:** до решения не определено, какая колонка есть объект паритета
  > (`sell_sum_kgs`, `revenue_kgs` или `net_revenue_kgs`). Различитель, класс A, внутри `SOURCE-MAP`:
  > сравнить `SUM(sell_sum_kgs)`, `SUM(revenue_kgs)`, `SUM(return_sum_kgs)` в `core.fact_sales_profit` и
  > `SUM(sum_kgs)` в `core.fact_returns` за май-2026. Починка (если факт подтвердится) трогает живую
  > инвесторскую витрину, то есть класс B и отдельный ADR; этой строкой не назначается |

  **Порядок применения для сборки:** строка заводится этим патчем; результат различителя допишет в неё
  сессия-исполнитель `SOURCE-MAP-SALES` своим блоком. Если оба блока попадут в один проход сборки —
  завести строку и сразу дописать результат, а не две строки.
- Блокеры: новых нет. `Q-83` блокером задачи `SOURCE-MAP-SALES` не является — он в неё входит как
  обязательный шаг 4в. Мост пары «Инвестор»/«Операционка» он блокирует до решения архитектора о том,
  какая колонка есть объект паритета.
- updated_at: 2026-07-29
- обновил: генератор (сессия: `SOURCE-MAP-SALES-GEN`)

## NEW_DECISIONS  (→ append в 06_DECISIONS_LOG.md как ADR)

нет.

Решений уровня `06` эта сессия не принимала. Два наблюдения ниже — процессные факты для владельца, не
кандидаты в ADR; они не требуют решения, если владелец с ними согласен, и внесены ради того, чтобы не
всплыть тихо позже.

1. **`08_TASK_BRIEF_TEMPLATE.md` внутренне противоречив после правок `ADR-082 §2`.** Шапка документа
   (строки 6–8) гласит: «Raw-URL в «context-to-load» НЕ приводятся (`ADR-082 §2`)». Раздел «Правила
   заполнения» (строка 86) сохранил прежнюю формулировку: «Context-to-load — всегда с литеральным raw-URL
   каждого пункта (пиннить по SHA при перечитывании)». `ADR-082` §Последствия перечисляет «четыре правки»
   в `08`; эта, по-видимому, пятая и не была сделана. Бриф собран по шапке (она несёт номер ADR и дату
   решения), строка 86 прочитана как остаток. Правка `08` — предмет ADR (`05` Часть I: STABLE-доки только
   через `06`), поэтому настоящей сессией не производится.
2. **Ветка и путь дерева `SOURCE-MAP-SALES` уже заняты этой генератор-сессией.** Владелец запустил
   генерацию в `.claude/worktrees/SOURCE-MAP-SALES` на ветке `s/SOURCE-MAP-SALES`. Команда режима И
   (`09_RUNBOOK §3`) — `git worktree add ".claude/worktrees/$T" -b "s/$T" HEAD` при `T=SOURCE-MAP-SALES` —
   упадёт, пока это дерево и ветка не убраны проходом сборки (`ADR-081 §7`). Отказ громкий, ущерба нет;
   порядок «сначала режим С, потом режим И» его снимает штатно.

## NEW_CONVENTIONS  (→ предложение правки 05; не применять без апрува)

нет.

=== END SESSION ===
