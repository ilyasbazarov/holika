# MANIFEST · /reference/code/cf-finance/ — снапшот исходника (E1-T3-D-CFSRC)

**Тип:** discovery-снапшот (`_METHOD §11`), не оракул, не прод-код.
**Источник-путь (on-disk):** `/home/ilyasbazarov4/cf-finance` (Cloud Shell persistent disk, `msklad-bi-prod`).
**Дата извлечения:** 2026-07-14 (`cf_extract.log`, запуск владельцем в Cloud Shell).
**Извлечено разработчиком, закоммичено человеком** (`_METHOD §10` — модель не пишет напрямую в репо).

---

## Ревизия и провенанс (`gcloud functions describe cf-finance --gen2 --region=asia-east1 --project=msklad-bi-prod`)

| Поле | Значение |
|---|---|
| `serviceConfig.revision` | `cf-finance-00006-piv` (совпадает с задокументированной в `11_INFRA_FACTS §CF`) |
| `buildConfig.entryPoint` | `main` |
| `buildConfig.source.storageSource` | `gs://gcf-v2-sources-420804682491-asia-east1/cf-finance/function-source.zip` (generation `1782378822395935`) |
| `updateTime` | `2026-06-25T09:14:37.746529153Z` |
| `state` | `ACTIVE` |

## Сверка disk vs deployed — ВЫПОЛНЕНА, зафиксирована

`diff -rq` между on-disk-деревом (искл. `.git`, `__pycache__`) и распакованным `function-source.zip` деплоя `00006-piv`:

```
Only in /home/ilyasbazarov4/cf-finance: .gcloudignore
```

**Результат:** `main.py`, `patch_main_finance.py`, `requirements.txt` — **байт-в-байт идентичны** deployed-исходнику (никаких строк `"Files … and … differ"` в выводе `diff -rq`, только одна строка `"Only in …"`). Единственное расхождение — файл `.gcloudignore` присутствует on-disk, но **отсутствует** в задеплоенном zip. Это управляющий build-файл (не код), не влияет на логику `run_etl()`/конвертер; корректный вывод — деплой `00006-piv` был собран без него (либо `.gcloudignore` появился на диске позже деплоя, либо сам себя исключает — содержимое `.gcloudignore` в этой сессии не дампилось, вне scope шага 4 брифа).

**Побочное наблюдение (не расхождение, факт для контекста):** `diff -rq` НЕ пометил `main.py.bak` как «Only in disk» — то есть этот бэкап-файл присутствует и в задеплоенном zip тоже (идентичного содержимого, иначе была бы строка `differ`). Значит на момент сборки `00006-piv` в директории уже лежал `main.py.bak`, и `.gcloudignore` на тот момент либо отсутствовал, либо не исключал `.bak`-файлы — задеплоенный артефакт содержит непреднамеренный backup-файл. Не блокирует находки по конвертеру, но стоит на заметке для гигиены деплоя (не в scope этой задачи, не создаю отдельный Q без запроса).

**⚠️ Дополнительный факт вне scope брифа, зафиксирован как есть:** `ls -la` на шаге 1 показал каталог `.git` внутри `/home/ilyasbazarov4/cf-finance` (7 записей, изменён 2026-06-25 09:05). Формулировка `Q-3`/брифа («код нигде не под контролем версий») **частично не подтверждена фактом** — локальный git-репозиторий на диске существует, но неизвестно, есть ли у него remote/push куда-либо (не проверялось — не в scope шагов брифа, содержимое `.git` не читалось). Это прямо релевантно decision-половине Q-3 (каноническое место хранения) — вынесено пунктом в `architect_review_queue`, не решается здесь.

## sha256-дерево on-disk источника (полное, из `cf_extract.log`, шаг 1)

| Файл | sha256 |
|---|---|
| `.gcloudignore` | `9c20b803e45cd91612bcc0113d5e925422cd5c90686feaa4487e0349ae0951b2` |
| `main.py` | `7a5d8dcd7866a887eb940accab709487eb27cba42a3446478b83678c67fa181d` |
| `main.py.bak` | `86d743e5e85d8162f8d4314b5ef1c8f33b2fce67ba084e275cfb6e8147ac0eed` |
| `patch_main_finance.py` | `fc07feeeab11a015569eca3a9782adacb2cd3b964e801164a16b0c429fe6ba47` |
| `requirements.txt` | `5b9dfa54877cc7b48885be34cd1bf4c125435bb9f19f1e0dd856def14e4b6d92` |

**Верификация транскрипции файлов в этом снапшоте (`main.py`, `patch_main_finance.py`, `requirements.txt` в этой директории — реконструированы из текстового дампа `cf_extract.log` шаг 4, т.к. модель не имеет прямого доступа к Cloud Shell диску):**
- `main.py` — sha256 реконструкции **совпадает** с sha256-деревом (`7a5d8dcd…181d`). ✅ Байт-в-байт подтверждено.
- `requirements.txt` — sha256 реконструкции **совпадает** (`5b9dfa54…6d92`). ✅ Байт-в-байт подтверждено.
- `patch_main_finance.py` — sha256 реконструкции **НЕ совпадает** с деревом (ожидалось `fc07feee…6ba47`, реконструкция даёт другой хэш; размер файла в `ls -la` шага 1 указан `2615` байт, реконструкция — `2612` байт). **Расхождение зафиксировано явно, не примирено** (`_METHOD §6` — untrusted до устранения): вероятна мелкая потеря пробелов/спецсимвола при копипасте лога в чат. Файл в этом снапшоте — рабочая, читаемая копия для целей findings (конвертер её не касается), но **не байт-верифицирована**. Для точной байт-копии рекомендуется отдельно приложить `patch_main_finance.py` файлом (upload) либо перекачать `function-source.zip` напрямую в репо-коммит вместо ручной транскрипции. Этот файл не участвует в root-cause находке (см. `cf_finance_converter_findings`) — он не про конвертер, а про `try/except` на `trigger_marts()`.

**Итог по критерию приёмки «исходник извлечён и закоммичен с манифестом провенанса»:** выполнено для всего дерева (sha256 всех 5 файлов зафиксирован); полнотекстовая транскрипция подтверждена байт-в-байт для 2 из 3 дампнутых файлов; для `patch_main_finance.py` — расхождение явно задокументировано, не скрыто.
