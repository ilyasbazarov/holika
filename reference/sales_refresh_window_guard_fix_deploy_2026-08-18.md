# FILE: sales_refresh_window_guard_fix_deploy_2026-08-18.md

# `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY` — СТОП на предусловии П3 (дрейф), деплой не исполнялся

**Сессия:** `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY` (исполнитель, класс B, мандат выдан
поимённо — `reference/guard_fix_deploy_mandate_2026-08-18.md`)
**Дата:** 2026-08-18 (Бишкек) · **База:** `183e90d`
**Дерево/ветка:** `worktrees/SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY` /
`s/SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY`
**Провенанс:** `reference/_scratch_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY_2026-08-18/step1_run.log`
… `step4_run.log`

---

## §1. Вердикт

**СТОП. Деплой НЕ исполнялся.** Предусловие П3 (`guard_fix_deploy_mandate_2026-08-18.md §3`,
`05_CONVENTIONS §Процедура деплоя` п.1) провалено: свежий `master` код-репо `holika-prod`
**не совпадает** с кодом обслуживающей ревизии `cf-facts-00017-jon`. Расхождение — не
косметика: `master` не несёт три патча, которые сейчас реально работают в проде
(`GUARD_TOLERANCE_DAYS`, обе ветки `WHEN NOT MATCHED BY SOURCE … THEN DELETE`, константы
канала периметра). Ветка деплоя, ЗАВЕДЁННАЯ ОТ `master` буквально по тексту мандата
(`guard_fix_deploy_mandate_2026-08-18.md §1`), стёрла бы эти три патча молча при деплое —
ровно тот сценарий, от которого предусловие защищает.

Ни `git push`, ни `gcloud functions deploy` этой сессией **не вызывались**. Единственное
изменение — локальный коммит `82c5027` в клоне код-репо внутри
`reference/_scratch_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY_2026-08-18/holika-prod/` (не
запушен, подтверждено `git ls-remote`).

## §2. П1 — контракт `run_id` (ЗАКРЫТО)

Живые `sourceContents` обоих Cloud Workflow сняты (`step1_run.log`). Строки с номерами:

- `msklad-pipeline-hourly`, `step_promote` (строка 139): `body: {run_id: ${run_id}, mode:
  "promote", window_days: 7}`.
- `msklad-pipeline-weekly`, `step_promote` (строка 215): `body: {run_id: ${run_id}, mode:
  "promote", window_days: 90}`.
- `msklad-pipeline-weekly`, `step_perimeter_promote` (строка 242): `body: {run_id: ${run_id},
  mode: "perimeter_promote", window_days: 90}`.

Оба promote-шага обоих Workflow несут `run_id` в `body`. Предусловие закрыто.

## §3. П4 — ровно два файла, гигиена ветки (ЗАКРЫТО)

Ветка `deploy/cf-facts-2026-08-18-guard-fix-f3` заведена от `master` (коммит `9a08b5a`),
ровно два файла скопированы из проверенного снапшота doc-репо (== коммит `56d40c1`,
последний, трогавший `bq_ops.py`/`main.py`):

```
git diff --stat master
 cf-facts/bq_ops.py | 188 +++++++++++++++++++++++++++++++++++++++++++++++++----
 cf-facts/main.py   |  89 +++++++++++++++++++++----
 2 files changed, 253 insertions(+), 24 deletions(-)
```

`.gcloudignore` несёт `*.bak`, `__pycache__/`, `*.pyc`, `patch_*.py`, `src.zip`, `.DS_Store`
(`ADR-040`, полный текст — `step3_run.log`). Сплошной поиск секретов по диффу — 2 совпадения
на слово `token`, оба — имя переменной (`token = get_token()`, передача параметром), не
литеральное значение. Предусловие закрыто.

## §4. П2 — обслуживающая ревизия (ЗАКРЫТО)

```
gcloud run services describe cf-facts --format="value(status.traffic)"
{'percent': 100, 'revisionName': 'cf-facts-00017-jon'}
```

Совпадает с ожиданием мандата. Предусловие закрыто.

## §5. П3 — дрейф (СТОП)

Архив обслуживающей ревизии (`generation=1786546688061530`, закреплённый, скачан по
`storageSource#generation`) распакован; sha256 пяти файлов, которые патч `SALES-REFRESH-
WINDOW` менял (`bq_ops.py`, `config.py`, `fetch_perimeter.py`, `helpers.py`, `main.py`),
сверен со свежим `master` (`step4_run.log`):

| Файл | sha256 архива (`cf-facts-00017-jon`) | sha256 `master` | Итог |
|---|---|---|---|
| `bq_ops.py` | `3be740b7…` | `e8bb0659…` | **MISMATCH** |
| `config.py` | `475a785d…` | `56e77eff…` | **MISMATCH** |
| `fetch_perimeter.py` | `07ddd1a0…` | `f609d565…` | **MISMATCH** |
| `helpers.py` | `661cc5ae…` | `c1d5f059…` | **MISMATCH** |
| `main.py` | `40fee815…` | `97b9ce3a…` | **MISMATCH** |

Все пять — расхождение, `ALL_MATCH=0`.

**Причина, установленная по git-истории код-репо** (`step2_run.log` + прямая проверка
`merge-base --is-ancestor`):

- `master` несёт мерж `508ca64` (`INGEST-MOMENT-ZONE-FIX`, `cf-facts-00011-mab`) — это
  последний коммит, трогавший `cf-facts/` на `master`.
- Три более поздних коммита `SALES-REFRESH-WINDOW` **не являются предками `master`**
  (проверено прямо):
  - `47b3a45` (узкая форма, `cf-facts-00012-ber`) — `is-ancestor` → **NO**
  - `9ae9a84` (v2, `cf-facts-00014-doh`) — не мержился (см. `MANIFEST.md`, «НЕ слита»)
  - `543b6c1` (deploy-final, **текущая обслуживающая** `cf-facts-00017-jon`) — `is-ancestor`
    → **NO**
- `reference/code/cf-facts/MANIFEST.md` документирует это как **осознанное** решение:
  слияние ветки `deploy/cf-facts-2026-08-12-completeness-and-delete` в `master` отложено до
  ступени 3 (первый недельный прогон `2026-08-23`), решение архитектора
  (`sales_refresh_window_deploy_final_adj_2026-08-12.md §8`). Это не чужой незакоммиченный
  дрейф — это ЗАФИКСИРОВАННОЕ состояние проекта, но предусловие П3 его тоже квалифицирует
  как дрейф, и практическое следствие то же самое.

**Подтверждение независимым способом.** Снапшот doc-репо `reference/code/cf-facts/`, на
котором построен и отревьюен патч guard-fix-ф3/ф4 (`a5c6a36`/`56d40c1`), уже несёт маркеры
`SALES-REFRESH-WINDOW` (`GUARD_TOLERANCE_DAYS` — `config.py`, `WHEN NOT MATCHED BY SOURCE` —
`bq_ops.py`, дважды). В свежем `master` код-репо этих маркеров нет вовсе (`grep` — 0
совпадений). То есть патч ф3/ф4 писан и проверен ПОВЕРХ кода уровня `cf-facts-00017-jon`, а
не поверх `master`.

**Практическое следствие, если бы деплой продолжился буквально по тексту мандата.** Ветка
`deploy/cf-facts-2026-08-18-guard-fix-f3`, заведённая от `master` (как велит §1 мандата),
содержала бы патч ф3/ф4 **поверх кода без ветки удаления, без допуска
`GUARD_TOLERANCE_DAYS` и без констант канала периметра**. Деплой такой ветки заменил бы
сейчас работающий в проде код (`cf-facts-00017-jon`) на код, откатывающий три предыдущих
патча — молча, под видом «маленького гард-фикса». `Откат`, названный в мандате (трафик на
`cf-facts-00017-jon`), в этом сценарии стал бы фактическим НАПРАВЛЕНИЕМ, а не аварийным
путём.

## §6. Что сделано, что не сделано

Сделано: П1/П2/П4 закрыты фактами; П3 проверен и провален, причина установлена двумя
независимыми способами (git-история код-репо + сравнение маркеров кода doc-репо vs master).
Локальный коммит `82c5027` создан в клоне код-репо **только внутри scratch-каталога этой
сессии**, не запушен.

НЕ сделано: `git push` в код-репо не вызывался; `gcloud functions deploy` не вызывался;
`git diff --stat master` для веток, содержащих актуальную ревизию, не пересобирался (решение
о правильной базе — не мне принимать, это выбор владельца/архитектора); код doc-репо не
правился ни строкой (патч заморожен `ADR-170 §3`, это ограничение не нарушено).

## §7. Развилка, вынесенная владельцу/архитектору

Мандат §1 предписывает вести ветку деплоя от `master`. Факт показывает, что `master`
устарел относительно того, что реально работает в проде, и это устаревание — зафиксированное
осознанное решение (ступень 3 гейтит слияние), а не забытая правка. Патч ф3/ф4 рецензирован
поверх кода уровня `cf-facts-00017-jon`, не поверх `master`.

**Варианты (называю, не выбираю — выбор владельца/архитектора):**

1. Вести ветку деплоя от `deploy/cf-facts-2026-08-12-completeness-and-delete` (коммит
   `543b6c1`, тот самый, что сейчас в проде), а не от `master`. Патч ф3/ф4 копируется поверх
   ЭТОЙ ветки, а не поверх `master`. `git diff --stat` против ЭТОЙ ветки (не против `master`)
   тогда обязан показать ровно два файла.
2. Дождаться ступени 3 (`2026-08-23`) и слияния `deploy/cf-facts-2026-08-12-completeness-
   and-delete` в `master` штатным проходом, и только после этого вести ветку гард-фикса от
   `master` буквально по тексту текущего мандата.

Ни один вариант не выбран этой сессией — исполнитель не адъюдицирует мандат класса B.
