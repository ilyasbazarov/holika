# FILE: sales_document_owner_deploy_2026-08-08.md

# SALES-DOCUMENT-OWNER-DEPLOY — деплой, 2026-08-08

**Первой фразой:** ЗАДЕПЛОЕНО. Колонка `document_owner_employee_id` стоит в `core.fact_sales_profit`
(тип `STRING`, подтверждено `bq show`). Обязательный `weekly`-прогон (`window_days=90`) выполнен —
`2 912` из `4 707` июльских строк получили значение владельца (было `0`). Майский снимок (условие 3
мандата) не сдвинулся: `3 300` строк / `96 471 991,41` KGS до и после, разность `0,00`.

**Задача:** `SALES-DOCUMENT-OWNER-DEPLOY`, деплойная половина (класс B).
**Статус:** DONE. Мандат — `ADR-146 §1` (владелец, чат 2026-08-08).

---

## Ход сессии (в порядке, для провенанса)

**Шаги 1–2 (класс A, read-only).** Патч свежий (`bq_ops.py:320`), `py_compile` чисто. Живая ревизия
`cf-facts-00009-tul` подтверждена побайтово равной `master` `7e039bd` (11/11 файлов, sha256).

**Находка (шаг 3 изначально не исполним).** Снапшот `reference/code/cf-facts/bq_ops.py` нёс
вперемешку эту задачу и незадеплоенный `SALES-REFRESH-WINDOW` (`ADR-144 §8`) в одних функциях.
Вопрос эскалирован архитектору без брифа (`ADR-086 §1`) — решение `ADR-145`: разделение оказалось
МЕХАНИЧЕСКИМ (девять хунков, ни один не смешивает патчи), не редакторским; порядок — сначала эта
задача. Полный разбор — `reference/sales_document_owner_deploy_order_adj_2026-08-08.md`.

**Мандат.** Выдан `ADR-146 §1` (владелец, чат) — объект (`ALTER TABLE` + деплой), объём, откат, три
условия приёмки: (1) правка комментария, противоречащего коду (перенесена в ветку деплоя ДО шага 7);
(2) промежуточная таблица руками не трогается — называется явно в объявлении шага 5; (3) обязательный
`weekly`-прогон затрагивает май — эффект измеряется прямым запросом до/после.

**Шаги 3–4.** Ветка `deploy/cf-facts-2026-08-08-document-owner` отведена от `master`, патч перенесён
механически по 5 хункам `bq_ops.py` (согласно `ADR-145`) + весь `fetch_demands.py` (3 места правки,
однозначно этой задаче). Постторонний хунк `T.discount` (`ADR-145 §2`) в ветку не попал — уже был
откачен архитектором в каноническом снапшоте (`ADR-146`, коммит `412f469`). Ветка перегенерирована
от актуального снапшота (после правки комментария), запушена (`3248e62`).

**Шаг 5 (объявление).** Дано отдельным сообщением: объект, откат, условие 2 (судьба staging).

**Шаг 6a-6b.** `bq show` ДО — колонки нет (22 колонки). `ALTER TABLE core.fact_sales_profit ADD
COLUMN document_owner_employee_id STRING` исполнен. `bq show` ПОСЛЕ — колонка есть, тип `STRING`
(23 колонки).

**Шаг 7 (деплой).** `gcloud functions deploy cf-facts --source=<ветка>/cf-facts ...` (флаги — из
`describe` шага 2). Новая ревизия `cf-facts-00010-mog`, `generation 1786194676108292`,
`updateTime 2026-08-08T13:12:55.988390080Z`.

**Шаг 8 (read-back).** Архив новой ревизии скачан по `generation`, sha256 всех 11 файлов сверен:
2 изменённых файла (`bq_ops.py`, `fetch_demands.py`) — побайтово равны ветке деплоя; 9 незатронутых —
идентичны предыдущей ревизии. Мусора в архиве нет.

**Шаг 9 (функциональная проверка, существующие режимы).** `hourly` — `status=ok`,
`staging_rows_loaded=350`, все 350 строк несут `document_owner_employee_id`. Прочие режимы
(`returns`/`purchases`/`perimeter`/`perimeter_promote`) не прогонялись отдельно — их файлы кодово не
менялись (sha256 идентичен).

**Шаг 10 (обязательный `weekly`, `window_days=90`) — с осложнением, разобрано read-only, без слепого
повтора.** Первая попытка: клиент `gcloud` оборвался по таймауту (`ReadTimeout`, 300с); read-only
диагностика по `httpRequest`-логам показала, что сервер отработал `status=200`/`latency=465.97s`, но
пока сессия разбиралась, штатный часовой пайплайн (расписание `0 * * * *`) перезаписал общий staging
(`WRITE_TRUNCATE`) раньше промоута — гонка (предсказанный `ADR-145 §2` класс риска), не порча данных
(у этого патча нет ветки удаления). Вторая попытка: тот же клиентский таймаут, сервер подтверждён
`status=200`/`latency=460.15s` (`receiveTimestamp 14:22:47Z`); staging проверен read-only ДО
промоута — `7 033` строки, окно `2026-05-10…2026-08-08`, все с владельцем. `promote`
(`window_days=90`) исполнен немедленно вслед — `status=ok`, `affected_rows=7033`.

**Приёмка (прямые запросы до/после):**
- Июль-2026: `document_owner_employee_id` заполнен `0` → **`2 912`** из `4 707` строк.
- Май-2026 (условие 3 мандата): `3 300` строк / `96 471 991,41` KGS — **без изменений**, разность
  `0,00`.
- Свод `core.fact_sales_profit` после: `42 789` строк / `691 534 086,33` KGS.

**Шаг 11 (слияние, MANIFEST).** Ветка слита в `master` код-репо (`merge --no-ff`, коммит `e9a4cbb`),
запушена. `reference/code/cf-facts/MANIFEST.md` пополнен секцией «Деплой `cf-facts` — сотрудник-
владелец документа (2026-08-08)». Побочная находка: по ошибке скопировал `MANIFEST.md` и в клон
код-репо `holika-prod` — там ему не место (никогда не был частью код-репо, только `holika`); отменено
до push (`git reset` + удаление файла), в код-репо ушли только 2 файла кода.

## Что вне scope (не переисполнено, не проверено)

- Числовое схождение разреза продаж по сотруднику с эталоном МойСклада — отдельная задача.
- `Q-105` (версионируется ли `owner` документа отдельно) — не закрыт.
- `02_ERP_CONTRACTS.md`, клиентские витрины — не правились.
- `_build_perimeter_merge_sql` / источники периметра на предмет поля `owner` — не проверялись.
- Ничего из `SALES-REFRESH-WINDOW` (ветка удаления) в этот деплой не попало.

## Провенанс

`reference/_scratch_SALES-DOCUMENT-OWNER-DEPLOY_2026-08-08/`: `describe_cf-facts.json`,
`step2_*.log`, `step3_snapshot_vs_master.diff`, `patch_document_owner_hunks1-5.diff`,
`step6a_check_column_absent.sh`/`.log`, `step6b_alter_table.sh`/`.log`, `step7_deploy.sh`/`.log`,
`step8_readback.log`, `step9_hourly.sh`/`.log`, `step9b_staging_check.sh`/`.log`,
`step10a_before.sh`/`.log` … `step10l_after.sh`/`.log` (полная цепочка диагностики и повтора шага 10),
`describe_cf-facts_after.json`. Скачанные архивы/клон `master` оставлены на диске, не закоммичены
(бинарники и вложенный `.git`).
