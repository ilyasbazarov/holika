# FILE: sales_perimeter_channel_decide_deploy_2026-08-07.md

# `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY` — задеплоено, прод `cf-facts` стоит на ревизии `cf-facts-00009-tul`

**Задеплоено.** Мандат класса B на шаги 5–10 выдан поимённо (`ADR-135`, владелец, чат 2026-08-07);
предыдущая сессия исполнила шаги 1–4 (диагностика, ветка `deploy/cf-facts-2026-08-07-channel`,
коммит `84d6f71`, push) — эта сессия их не переделывала, только повторила один read-only `describe`
(шаг 0 ниже), как предписано, чтобы убедиться в отсутствии дрейфа между сессиями.

## Что сделано (шаги 5–10 брифа)

**Шаг 0 (повтор части шага 2, read-only).** Свежий `gcloud functions describe cf-facts` перед
деплоем — ревизия та же, `cf-facts-00008-zen` (`generation 1786093276804812`,
`updateTime 2026-08-07T09:02:36Z`), дрейфа между сессиями нет.

**Шаг 5.** Объявление действия отдельным сообщением в чат (что/на каком объекте/чем откатывается) —
владелец подтвердил деплой.

**Шаг 6 (деплой).** Одна команда, скриптом-файлом, `date -u`/`gcloud auth list` в начале и в конце.
`cf-facts-00008-zen` → **`cf-facts-00009-tul`**, `generation` **`1786115536540209`**,
`updateTime` **`2026-08-07T15:13:10.253085704Z`**.

**Шаг 7 (read-back).** Архив новой ревизии скачан по закреплённому `generation`, sha256 всех 11
файлов побайтово сверен с веткой `deploy/cf-facts-2026-08-07-channel` — совпадение полное (кроме
`.gcloudignore`, закономерно отсутствующего в развёрнутом архиве); мусора
(`.bak`/`__pycache__`/`.DS_Store`/`src.zip`/разовые patch-скрипты) нет.

**Шаг 8 (незатронутые режимы).** Живой прогон `hourly` (`run_id=verify_deploy_2026-08-07_channel_hourly`)
— `status=ok`, `demand_positions_fetched=403`, `staging_rows_loaded=403`, ни одной ошибки. sha256
`config.py`/`main.py`/`fetch_demands.py`/`fetch_purchases.py`/`fetch_returns.py`/`helpers.py` не
изменился против прошлой ревизии — кодово не тронуты.

**Шаг 9а (`perimeter`, staging, ПЕРВЫМ).** Клиент `gcloud` оборвался по клиентскому таймауту, сервер
отработал успешно — подтверждено прямым read-only опросом `stg_msklad.fact_sales_perimeter_staging`
(слепой повтор вызова НЕ делался). Загружено: `retaildemand` — 506 документов / 1441 строка /
`1 188 422,00` KGS; `commissionreportin_sale` — 24 документа / 4026 строк / `11 514 572,13` KGS.
**Точное совпадение** с прошлым измеренным прогоном (`MANIFEST.md §«Деплой cf-facts — периметр
продаж (2026-08-07)» §9а`) — периметр отбора документов не изменился, эта задача его не расширяет,
только метит канал. `sales_channel_name` = «Розница»/«Комиссия» по типу документа,
`sales_channel_id` = `NULL` у обоих — подтверждено прямым запросом к staging.

**Шаг 9б (`perimeter_promote`, core, ТОЛЬКО после 9а).** До MERGE: `core.fact_sales_profit` итого —
`42 784` строки / `691 376 392,83` KGS; строк периметра (join по
`transaction_id = TO_HEX(MD5(CONCAT(doc_id,'|',position_id)))` против снимка staging этого
прогона) — `5 467`, у всех `sales_channel_name = NULL` (наследие прошлого прогона
`SALES-INGEST-PATCH-DEPLOY`, когда метки ещё не было). Команда отчиталась `affected_rows=5467`.
Read-back ПРЯМЫМ запросом (не по отчёту команды): после MERGE итого `core.fact_sales_profit` НЕ
изменился (те же `42 784` строки / `691 376 392,83` KGS — `MERGE` только обновил существующие
строки, новых не добавил).

**ГЛАВНОЕ ЧИСЛО.** Разрез `sales_channel_name` по периметру (5 467 строк) ПОСЛЕ `MERGE`:
`4 026` строк «Комиссия» + `1 441` строка «Розница» = `5 467` — **`NULL` упал с 5 467 до 0**.
Сумма периметра `12 702 994,13` KGS не изменилась до/после (проверено отдельным запросом).

**Шаг 10 (слияние).** `deploy/cf-facts-2026-08-07-channel` (коммит `84d6f71`) слит в `master`
код-репо `holika-prod` через `merge --no-ff` — коммит **`7e039bd`**, запушено в `origin/master`
ПОСЛЕ успешного read-back и всех функциональных проверок. Запись ревизия↔коммит —
`reference/code/cf-facts/MANIFEST.md` §«Деплой `cf-facts` — метка канала периметра продаж
(2026-08-07)».

## Приёмка (сверка с критериями брифа)

- Побайтовая сверка снапшота с `master` перед деплоем — совпало, база подтверждена
  `cf-facts-00008-zen`.
- `.gcloudignore` — отсутствие `.bak`/`__pycache__`/`.DS_Store`/`src.zip` подтверждено (унаследован
  чистым от подготовки предыдущей сессии).
- Read-back: имя ревизии (`cf-facts-00009-tul`), `generation` (`1786115536540209`), sha256 всех
  файлов, отсутствие грязи — напечатаны выше и зафиксированы в `MANIFEST.md`.
- Незатронутые режимы (шаг 8) не изменили поведение — подтверждено и sha256, и логом прогона
  `hourly`.
- `perimeter` (staging) и `perimeter_promote` (core) проверены раздельно, в этом порядке; число
  документов/строк/сумма периметра НЕ изменилось относительно прошлого измерения; `sales_channel_name`
  заполнено константой по типу документа; `NULL` в затронутом периметре упал до 0 — числа напечатаны
  с арифметикой.
- `MANIFEST.md` несёт запись ревизия↔коммит по результату read-back, не по сообщению команды деплоя.
- `master` код-репо содержит патч ТОЛЬКО после успешного read-back (шаг 7) — слияние было последним
  действием (шаг 10), после всех проверок.
- Ни одной записи/`MERGE` в `core.fact_sales_profit` до проверки staging-режима `perimeter` (9а) — не
  нарушено: `perimeter_promote` вызван строго после подтверждённого 9а.

## Откат (не понадобился)

Повторный деплой именем ревизии `cf-facts-00008-zen`. Данные откатывать не нужно — патч только
проставляет значения двух колонок (`sales_channel_id`/`sales_channel_name`) у уже существующих
строк периметра, не создаёт новых строк; при необходимости — точечный `UPDATE` тех же строк обратно
в `NULL` (запрос — в `MANIFEST.md`).

## Вне scope (не тронуто этим деплоем)

- `SALES-DOCUMENT-OWNER-DEPLOY` (`document_owner_employee_id`) — отдельная задача, отдельный мандат,
  в эту ветку не переносилась (подтверждено предыдущей сессией по diff).
- `sq_marts_sales_overview.sql` — не правился, витрина уже корректна при любом исходе.
- Каденция (`SALES-PERIMETER-CADENCE`) — уже подключена отдельной задачей ранее 2026-08-07, этой
  сессией не трогалась.

## Провенанс

Скрипты и сырые логи — `reference/_scratch_SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY_2026-08-07/`
(добавлены этой сессией: `step0_*`, `branch_check/`, `step6_*`, `step7_*`, `step8_*`, `step9a_*`,
`step9b_*`).
