# FILE: sales_perimeter_channel_decide_deploy_2026-08-07.md

# `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY` — деплой НЕ выполнен, прод `cf-facts` стоит на ревизии `cf-facts-00008-zen`

**Задача не завершена.** Мандат класса B на сам деплой (шаг 5 и далее процедуры
`reference/deploy_procedure_2026-08-03.md`) НЕ выдан на момент этой сессии — свежая проверка
`07_STATE.md §Мандат Claude Code` (строка `SALES-PERIMETER-CHANNEL-DECIDE, деплой`) подтвердила
статус «НЕ выдан», как и было объявлено в шапке брифа. Прод `cf-facts` остаётся на ревизии
`cf-facts-00008-zen` (`generation 1786093276804812`) — той же, что до этой сессии. Выполнены только
шаги 1–4 брифа (диагностика, ветка, коммит, push ветки) — они мандата не требуют по прямому тексту
брифа. Шаг 5 (объявление действия и получение мандата), деплой (6), read-back (7), функциональная
проверка (8–9) и слияние (10) — НЕ исполнялись.

## Что сделано (класс A / read-only + подготовка ветки)

**Шаг 1.** Свежий `py_compile` `reference/code/cf-facts/fetch_perimeter.py`/`bq_ops.py` в рабочем
дереве этой сессии — `COMPILE OK`. Патч ADR-134 подтверждён в файлах (константы
`sales_channel_name="Розница"`/`"Комиссия"` в `fetch_perimeter.py`, чтение из staging вместо
`CAST(NULL AS STRING)` в `bq_ops.py`).

**Шаг 2.** Свежий снапшот живой ревизии `cf-facts` снят `gcloud functions describe` +
`gcloud storage cp` по закреплённому `generation`. Ревизия — `cf-facts-00008-zen`,
`generation 1786093276804812`, `updateTime 2026-08-07T09:02:36.835840920Z` — совпадает с базой,
объявленной брифом (Вход 4). Побайтовая сверка (`shasum -a 256`, 11 файлов) живого архива с
`cf-facts/` в `master` код-репо (`holika-prod`, HEAD `0c5f68e`) — **полное совпадение**, единственное
отличие — `.gcloudignore`, которого в развёрнутом архиве закономерно нет. Дрейфа не найдено, деплой
безопасен по критерию шага 1 процедуры. Полный лог —
`reference/_scratch_SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY_2026-08-07/step2_run.log`.

**Шаг 3.** Ветка `deploy/cf-facts-2026-08-07-channel` заведена от свежего `master` (`0c5f68e`).

**Находка, важная для честности переноса патча:** снапшот `reference/code/cf-facts/bq_ops.py` в
рабочем дереве ЭТОЙ сессии нёс не только патч `ADR-134`, но и посторонние правки
`document_owner_employee_id` (задача `SALES-DOCUMENT-OWNER-DEPLOY`, явно вне scope этого брифа,
собственный мандат ещё не выдан). Копирование файла целиком перенесло бы чужой недеплоенный патч в
ветку деплоя канала — именно тот риск, из-за которого процедура требует переноса **по diff**, а не
копированием. Патч перенесён вручную, хунк за хунком, только по `sales_channel_id`/
`sales_channel_name` (schema `PERIMETER_STAGING_SCHEMA`, `SELECT` и `WHEN MATCHED UPDATE SET` в
`_build_perimeter_merge_sql`, докстринг). Итоговый `git diff` против `master` — 2 файла,
38 insertions(+), 2 deletions(-), `document_owner_employee_id` в диффе отсутствует (проверено
`grep`). `fetch_perimeter.py` перенесён целиком — в нём загрязнения не найдено (файл идентичен
локальному снапшоту). Полные диффы —
`reference/_scratch_SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY_2026-08-07/step3_work/step3_final_*.diff`.

`.gcloudignore` в ветке уже несёт `*.bak`, `__pycache__/`, `.pyc`, `.DS_Store`, `src.zip`,
`patch_*.py` — соответствует `ADR-040`, правка не понадобилась. Сплошной поиск по патчу на
секреты/токены/ключи (`git diff | grep -inE 'api[_-]?key|secret|token|password|bearer|-----BEGIN'`) —
единственное совпадение слова `token` относится к имени переменной HTTP-авторизации МойСклада, не к
значению секрета. Свежий `py_compile` обоих файлов в ветке — `COMPILE OK`.

**Шаг 4.** Коммит `84d6f71` в ветку `deploy/cf-facts-2026-08-07-channel`
(`cf-facts: метка канала периметра продаж — константа по типу документа (задача
SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY, решение ADR-134)`). `master` код-репо не тронут. Push ветки
подтверждён владельцем отдельным сообщением в чате — выполнен, ветка на `origin`.

## Что НЕ сделано и почему

Шаг 5 брифа (объявление действия отдельным сообщением и получение поимённого мандата класса B) не
наступил: без предварительно выданного поимённого ADR, устанавливающего мандат на этот деплой, бриф
прямо предписывает остановиться до этого шага, не пытаясь получить мандат явочным порядком в моменте.
Соответственно не исполнялись: деплой (шаг 6), read-back новой ревизии (шаг 7), функциональная
проверка незатронутых режимов (шаг 8), функциональная проверка `perimeter`/`perimeter_promote`
(шаг 9), слияние ветки в `master` и запись в `MANIFEST.md` (шаг 10).

**Прод `cf-facts` не изменился этой сессией** — стоит на `cf-facts-00008-zen`, той же ревизии, что и
до сессии.

## Следующий шаг

Готовность правки зафиксирована (`ADR-134`), диагностика и ветка деплоя подготовлены и запушены
(коммит `84d6f71`, `deploy/cf-facts-2026-08-07-channel`). Не хватает ровно одного: поимённого ADR,
выдающего мандат класса B на деплой `cf-facts` этим патчем (форма — прецедент `ADR-092`/`ADR-108`/
`ADR-117`/`ADR-118`/`ADR-126 §6`). По факту его выдачи следующая сессия исполняет шаги 5–10 этого же
брифа (или его переиздание) поверх уже подготовленной и запушенной ветки — начинать сборку патча
заново не требуется.

## Файлы

- `reference/code/cf-facts/fetch_perimeter.py`/`bq_ops.py` — снапшот патча, не менялся этой сессией
  (только читался и сверялся).
- `reference/_scratch_SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY_2026-08-07/` — скрипты и логи шагов 1–4,
  включая клон `master` (`step2_work/master_repo/`) и рабочий клон ветки деплоя
  (`step3_work/repo/`, коммит `84d6f71`, запушен на `origin/deploy/cf-facts-2026-08-07-channel`).
- Код-репозиторий `holika-prod`: ветка `deploy/cf-facts-2026-08-07-channel`, коммит `84d6f71`,
  запушена. `master` не тронут.
