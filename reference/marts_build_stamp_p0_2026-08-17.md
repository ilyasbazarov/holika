# FILE: marts_build_stamp_p0_2026-08-17.md

# `MARTS-BUILD-STAMP-P0` — предусловие П0: сверка трёх живых transferConfig со снапшотами

**Дата:** 2026-08-17 (Бишкек) · **Задача:** `MARTS-BUILD-STAMP-P0` · **Класс:** A, без брифа
(в рамках существующей строки `MARTS-BUILD-STAMP`, `ADR-156 §2`).
**База:** `git rev-parse HEAD` на старте — `c37f28e88ad2d8624b53825ca08462eee4d3f4c9`.
**Дерево/ветка:** `worktrees/MARTS-BUILD-STAMP-P0` / `s/MARTS-BUILD-STAMP-P0`.
**Постановка:** `reference/close_list_escalation_adj_2026-08-17.md §3` (предусловие П0 строки `7`
списка закрытия).

**Зачем.** `MARTS-BUILD-STAMP-PREP` (`reference/marts_build_stamp_prep_2026-08-13.md`) собрала три
готовых текста запроса от снапшотов `reference/sql/sq_marts_*.sql` выгрузки `2026-07-07` — на
момент этой сессии базе более шести недель. Живые конфигурации трёх витрин с тех пор не сверялись
ни разу. Соседняя задача `SALES-OVERVIEW-OWNER-SWITCH` (2026-08-15) замерила дрейф живого
`transferConfig` от снапшота на своём объекте (26 строк декоративного диффа). Мандаты класса B на
применение трёх подготовленных текстов не выдаются, пока живая база не сверена.

**Что делала эта сессия:** только чтение (`bq show --transfer_config`) и сравнение файлов. Ни один
`transferConfig` не тронут, витрины не пересобраны.

---

## §1 Провенанс

- Скрипт снятия: `reference/_scratch_MARTS-BUILD-STAMP-P0_2026-08-17/step1_fetch_live_configs.sh`
- Лог (`date -u`/`gcloud auth list` первой и последней командой, `ADR-055 §3/§4`):
  `reference/_scratch_MARTS-BUILD-STAMP-P0_2026-08-17/step1_run.log` — аккаунт
  `ilyasbazarov4@gmail.com` идентичен на входе и выходе, деградации авторизации не было.
- Живые конфигурации (`bq show --transfer_config --format=prettyjson`, `rc=0` у всех трёх,
  непустой парсимый JSON — критерий «успех инструмента ≠ факт» удовлетворён):
  - `reference/_scratch_MARTS-BUILD-STAMP-P0_2026-08-17/step1_customer_invoices_ar_config.json`
  - `reference/_scratch_MARTS-BUILD-STAMP-P0_2026-08-17/step1_expenses_config.json`
  - `reference/_scratch_MARTS-BUILD-STAMP-P0_2026-08-17/step1_weight_flow_config.json`
- Извлечённый текст поля `params.query` каждого — `step1_<name>_live_query.sql` в том же каталоге.
- Полные диффы: `step2_<name>_diff.txt` в том же каталоге.

## §2 Сверка — по каждому объекту

### `sq_marts_customer_invoices_ar` (Config ID `6a23f3ea-0000-2952-853d-582429be7ecc`)

`diff` живого текста и `reference/sql/sq_marts_customer_invoices_ar.sql` — **0 строк расхождения**,
байтовые длины совпадают (`925` байт у обоих). **Вердикт: расхождений нет.** Подготовленный текст
`sq_marts_customer_invoices_ar_stamped_2026-08-13.sql` годен к применению по критерию П0.

### `sq_marts_expenses` (Config ID `6a22a243-0000-20fd-a458-883d24f4cad4`)

`diff` живого текста и `reference/sql/sq_marts_expenses.sql` — **0 строк расхождения**, байтовые
длины совпадают (`3182` байт у обоих). **Вердикт: расхождений нет.** Подготовленный текст
`sq_marts_expenses_stamped_2026-08-13.sql` годен к применению по критерию П0.

### `sq_marts_weight_flow` (Config ID `6a1f9418-0000-276f-a1e4-d4f547ee7418`)

`diff` живого текста и `reference/sql/sq_marts_weight_flow.sql` — **19 строк диффа** (`git diff`
формат), два места:

1. Комментарий в шапке запроса: `«…в МойСклад.»` (снапшот) vs `«…в МойСкладе.»` (живой) — правка
   грамматического падежа внутри комментария, вне SQL-логики.
2. Отсутствие финального перевода строки в снапшоте (`\ No newline at end of file` с обеих сторон
   диффа, по концу файла и по концу первого изменённого блока).

Байтовые длины: снапшот `2545` байт, живой `2546` байт — расхождение ровно на один символ,
соответствует единственной букве `е`, добавленной в слово «МойСкладе». SQL-код (все `SELECT`,
`CREATE OR REPLACE TABLE`, CTE, `JOIN`, `GROUP BY`, `ORDER BY`) — идентичен посимвольно за пределами
этих двух мест.

**Вердикт: расхождение декоративное** (правка формы одного слова в комментарии + отсутствие
финального перевода строки) — тот же класс, что `SALES-OVERVIEW-OWNER-SWITCH §1 П1` (длина
полосок-разделителей, финальный перевод строки). Подготовленный текст
`sq_marts_weight_flow_stamped_2026-08-13.sql` годен к применению по критерию П0.

## §3 Итог по всем трём

| Объект | Config ID | Диффов строк | Вердикт |
|---|---|---|---|
| `sq_marts_customer_invoices_ar` | `6a23f3ea-0000-2952-853d-582429be7ecc` | 0 | расхождений нет |
| `sq_marts_expenses` | `6a22a243-0000-20fd-a458-883d24f4cad4` | 0 | расхождений нет |
| `sq_marts_weight_flow` | `6a1f9418-0000-276f-a1e4-d4f547ee7418` | 19 (декоративный) | декоративное |

**Предусловие П0 (`close_list_escalation_adj_2026-08-17.md §3`) закрыто по всем трём объектам:**
исход (i) для всех трёх — три подготовленных текста
(`reference/sql/sq_marts_customer_invoices_ar_stamped_2026-08-13.sql`,
`sq_marts_expenses_stamped_2026-08-13.sql`, `sq_marts_weight_flow_stamped_2026-08-13.sql`) годны к
применению класса B без пересборки. Ни один текст этой сессией не пересобирался — не потребовалось
(исход (ii) не наступил ни на одном объекте).

**Что остаётся держать в уме, вне scope этой сессии:** `sq_marts_weight_flow` несёт отдельный,
ранее заведённый `CONTEXT GAP` (`marts_build_stamp_prep_2026-08-13.md §3`) — не про совпадение
текста запроса (закрыто здесь), а про поведение прямого подключения Looker Studio к таблице при
появлении двух новых колонок. Этот гэп П0 не закрывает и не открывает заново.

## §4 Что этой сессией не делалось

- Ни один `transferConfig` не изменён (`bq update` не вызывался) — только `bq show`.
- Ни одна витрина не пересобрана.
- Мандаты класса B на применение трёх объектов этой сессией не выдаются и не запрашиваются —
  выдача мандатов вне scope класса A.
