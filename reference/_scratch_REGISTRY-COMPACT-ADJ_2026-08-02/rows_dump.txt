### 01  стр.349  [Q-3]  тип: референс-артефакт + решение  (1243 симв.)
  В: **Исходный код пайплайна нигде не версионирован**: cf-finance на persistent-диске Cloud Shell, `load_invoices.py` в эфемерном `/tmp`, остальные CF — не названы
  К: **partial-close (cf-finance-часть, сессия E1-T3-D-CFSRC 2026-07-14):** код cf-finance извлечён, sha256-верифицирован, disk==deployed для кода подтверждён (снапшот `/reference/code/cf-finance/` + `MANIFEST.md`; расходится только build-файл `.gcloudignore`); decision-половина (каноническое место хранения) — остаётся `proposed` (AD
  …хвост: полнение (2026-07-23):** cf-loss-commission провенанс захвачен (снапшот `/reference/code/cf-loss-commission/` готов, SHA256 верифицирован disk==deployed), **требует коммита в holika** (см. B-9, Q-46).

### 02  стр.350  [Q-6]  тип: факт  (289 симв.)
  В: Актуальная ревизия cf-dq после T-1-фикса (TD-DQ-REVISION)
  К: `gcloud functions describe cf-dq` **Дописка `ADR-101 §5` (2026-08-02):** факт снимается шагом 1 задачи `INGEST-CURRENCY-ASSERT` вместе с текстом DQ-проверки `currency_normalization`; отдельного захода не требует.

### 03  стр.351  [Q-7]  тип: факт  (129 симв.)
  В: TTL JWT-токена Bakai (TD-BAKAI-01)
  К: запрос банку / эмпирическая фиксация (рабочая политика DEFER есть в RB §17)

### 04  стр.352  [Q-9]  тип: факт  (321 симв.)
  В: UUID статей расходов на `entity/loss` (ре-скоуп: декомпозиция вынесена в Q-23/CLOSED — остаётся ЧИСТЫЙ UUID-резолвинг)
  К: **DEFER** (rationale ADR-009 §3 — future defense-in-depth, не блокер Epic-1; E1-T2 работал по имени, UUID не резолвлены); API-запрос по методу PR (РБ, строки 711–721), когда актуально

### 05  стр.353  [Q-11]  тип: референс-артефакт  (156 симв.)
  В: Бизнес-метрики (X/Y/Z распределение, покрытие) датированы 2026-06-05 — устарели
  К: свежая выгрузка метрик из BQ → `/reference`

### 06  стр.354  [Q-12]  тип: факт  (167 симв.)
  В: `cf-alert` на схеме архитектуры, отсутствует в PR; URL/конфиг/Telegram — не документированы
  К: `gcloud functions describe cf-alert` + конфиг алертинга

### 07  стр.355  [Q-13]  тип: факт  (152 симв.)
  В: `msklad-pipeline-weekly`: расписание известно, состав шагов — не задокументирован
  К: `gcloud workflows describe msklad-pipeline-weekly`

### 08  стр.356  [Q-14]  тип: факт  (138 симв.)
  В: Процедура полной пересборки core из GCS raw (RB §11, шаги 11.2–11.3) — заглушки
  К: восстановить процедуру (связано с Q-3)

### 09  стр.357  [Q-20]  тип: решение  (1990 симв.)
  В: DQ-порог `currency_normalization` (`avg_revenue_kgs < 10M`, `03_PIPELINE_SPEC.md` §DQ) — только верхняя граница (ловит «забыли `/100`, остались тыйыны»); НЕ ловит регрессию «забыли `×rate`» (сумма занижена ~90x, а не завышена) — тот же класс бага, что и TD-REC
  К: решение владельца + (при подтверждении) патч DQ-конфигурации `cf-dq`; coverage-gap подтверждён архитектором (M-P5-AP), конкретное значение порога — не назначается заранее (anti-improvisation), требует эмпирической калибровки **Дописка `ADR-101 §5` (2026-08-02, архитектор): ПЕРЕФОРМУЛИРОВАН.** Вопрос «каким должен быть нижний пор
  …хвост: аёт около `6,86` млн — НИЖЕ порога `10` млн, то есть проверка не сработала бы и на собственной цели; утверждение условное — текста проверки в репо нет (`CONTEXT GAP`, снимается шагом 1 той же задачи).

### 10  стр.358  [E1-T2-MECH-TAX]  тип: решение  (388 симв.)
  В: Инъекция 2-го источника «Налоги» (начисление) + подавление cash-строки
  К: форма staging-first (как E1-T1-MECH); гейт E1-T2 шаг-5; ops — владелец числа + каденс помесячно **Дописка сборки 2026-08-01 (`ADR-091 §9`):** блокирует не класс задачи, а отсутствующий вход владельца (число начисления и каденс); после его получения staging-часть становится классом A.

### 11  стр.359  [Q-28]  тип: факт  (363 симв.)
  В: `Regional Access Boundary` ошибка при `gcloud secrets versions access` в логе прогона E1-T2 (не заблокировала выполнение)
  К: **DEFER** — задокументирована в `11_INFRA_FACTS.md §IAM`. Триггер промоушена из DEFER: повтор ИЛИ появление в не-интерактивном/service-account прогоне (ошибка ссылается на личный gmail `ilyasbazarov4@…`, не на `etl-sa@…`)

### 12  стр.360  [Q-30]  тип: факт  (909 симв.)
  В: Полный масштаб currency-conversion бага (весь март / другие месяцы / `cashout`) не измерен сессией E1-T3, вне scope E1-T3
  К: МАГНИТУДНЫЙ замер (месяцы × все core.fact_* × cashout) — DEFER до после структурной картины (Q-3) и посадки expense-фикса; СТРУКТУРНЫЙ охват снимается в Q-3, не отдельной кампанией. Owner-gated; бласт-радиус потенциально project-wide, если converter общий для `core.fact_*` (не подтверждено — Q-3). **Структурная половина — CAPTUR
  …хвост: удный замер (месяцы/cashout/sales) — по-прежнему DEFER **Дополнение триггера (ADR-069 §3):** промоушен из DEFER при расширении паритета на период до 2026-05 — замер месяца входит в процедуру включения

### 13  стр.361  [RQ-3]  тип: факт (гигиена деплоя)  (182 симв.)
  В: Задеплоенная ревизия `cf-finance-00006-piv` содержит `main.py.bak`
  К: не адъюдицировано этой сессией; DEFER — не блокер, убрать при следующем деплое

### 14  стр.362  [CODE-REPO-STANDUP]  тип: задача (промоутнута в критический путь,   (2577 симв.)
  В: Отдельный код-репо для живого VC исходников пайплайна (ADR-017 §4/§5)
  К: **Шаг 1 DONE (сессия CODE-REPO-STANDUP-D1, 2026-07-28, класс A):** `.git` в `/home/ilyasbazarov4/cf-finance/` **существует, но несёт 0 коммитов** («No commits yet», `git rev-list --count --all` = 0); `main.py` staged (в индексе), но рабочая копия уже разошлась со staged-версией (`modified, not staged`); untracked — `.gcloudignor
  …хвост: с архивом не найдено, второй коммит не потребовался. Артефакт: `reference/code_repo_standup_d2_2026-08-01.md`. Шаг 2b (удалённый репозиторий, `push`) остаётся владельцу, задача целиком не закрывается.

### 15  стр.363  [Q-34]  тип: решение  (654 симв.)
  В: Наблюдается ли состояние прогонов SQ-флота (покрывает ли `cf-alert`/мониторинг падения BQ DTS) и нужен ли постоянный механизм
  К: **DEFER (ADR-020 §3).** Гэп реален (дефект прожил 45 сут невидимым; `03 §DQ` и ADR-007 наблюдают только `staging`/`core`; прецедент RB-42), но: эмпирика опровергла риск для инвесторской поверхности (`sq_marts_expenses` — 0 падений за всю историю); механизм Q-34 не поймал бы Q-38 (отказ выглядит как `SUCCEEDED`); Epic-1 не закрыт

### 16  стр.364  [Q-35]  тип: референс-артефакт  (485 симв.)
  В: `10_OPS_PLAYBOOK.md` объявлен в `00_CHARTER §карта документов` стр.52 и `MIGRATION_MAP` §Целевой набор; на него ссылаются `11 §секреты`, `03 §DQ`, `ADR-008 §4`, `02:241` — файла в репо @ `4fce6a8` НЕТ (404); M-P4 закрыт DONE 22/22 без него
  К: **DEFER (ADR-020 §Последствия)** — не блокер. Висячий адресат: ответы Q-34 и `ADR-008 §4` (retention `audit`) класть некуда. Триггер промоушена: выход Q-34 из DEFER ЛИБО первая правка, требующая §инцидентный слой

### 17  стр.365  [Q-37]  тип: факт  (479 симв.)
  В: Латентный дефект актива: по `DATE(snapshot_at) = 2026-05-07` лежат ТРИ идентичных среза (Q-36) — наивный запрос по этой дате утроит строки
  К: **DEFER (ADR-019 §6)** — не блокер, потребителя нет (`ADR-008 §2`). Срабатывает при промоушене таблицы в consumer-facing роль (`ADR-008 §3`): тогда либо дедуп по `MIN(snapshot_at)` в пределах суток, либо `DELETE` двух лишних срезов — решать ADR того момента. Не чинить превентивно; `AUDIT-SNAPSHOT-FIX` этого не касается

### 18  стр.366  [Q-38]  тип: факт (зафиксирован) + решение (форма)  (908 симв.)
  В: `sq_marts_gmroi_by_folder` (Config `6a004e88-…`) читает `marts.gmroi` (Config `6a006664-…`) — единственная март-на-март зависимость флота (проверены все 13 SQL @ `4fce6a8`). Порядок 08:03 → 12:02 UTC верный, но не задан ничем: оба `every 24 hours` от собственн
  К: **DEFER (ADR-020 §4)** — не блокер Epic-1, `gmroi_by_folder` вне Трека A/B. Риск: нарушение порядка даёт `SUCCEEDED` на вчерашних данных; `03 §DQ` не видит; `_mart_refreshed_at = CURRENT_TIMESTAMP()` штампует свежесть агрегации, а не данных. Форма фикса (оркестрация · assert на `_mart_refreshed_at` источника · слияние в один SQ)

### 19  стр.367  [Q-39]  тип: решение  (2142 симв.)
  В: `sq_audit_dim_counterparties_snapshot` / `sq_audit_dim_employees_snapshot`: тот же паттерн `INSERT … SELECT *, CURRENT_TIMESTAMP()`, что сломал `dim_products` — две взведённые мины того же класса (зелены лишь потому, что источники ещё не получали новых колонок
  К: **DEFER (ADR-020 §5)** — оптовое распространение фикса ADR-019 ОТКЛОНЕНО: паттерн один, ценность активов разная. `core.dim_counterparties` несёт SCD2 нативно (`02 §схемы dim`) ⇒ история уже в ядре, довод `ADR-008 §3` неприменим, вопрос смещается с «как чинить» на «зачем он» (ближе к ретайру). Решать отдельно и по каждому, не опт
  …хвост:  встречается; ограничение метода — проверены оба известных репо инвентаря, потребитель вне них таким поиском не виден. Строка закрывается по исполнении ОБЕИХ задач. `Q-37` не трогается (`ADR-019 §6`).

### 20  стр.368  [Q-40]  тип: решение  (1458 симв.)
  В: Промоушен правила «успех инструмента ≠ факт» (`05` Часть I, ADR-021 §2) в канон `_METHOD §6`
  К: **DEFER (ADR-021 §4)** — `05` Часть I есть доменно-нейтральное зеркало `_METHOD §6`; правило введено как holika-локальное уточнение, что создаёт контролируемое расхождение с каноном. Правка `_METHOD` задевает проекты за пределами holika (методология переносимая) — owner-gated, blast radius вне этого репо. Триггер промоушена: пра
  …хвост: Фактура, добавленная этой сессией: цена варианта (b) «оставить локальным» РАСТЁТ — `ADR-044` добавил к правилу второй абзац (греп-вердикт), которого в каноне нет, то есть расхождение уже не одна фраза

### 21  стр.369  [Q-47]  тип: факт (бизнес-правило)  (1009 симв.)
  В: (E1-T1-MECH-INGEST, сессия 2026-07-23) Loss: 0 из 128 документов за всю историю аккаунта в не-KGS валютах. Это известный факт о бизнесе (loss всегда KGS) или потенциальный гэп в данных, требующий переспроверки?
  К: **DEFER** (`E1-T1-INGEST-ADJ`, 2026-07-23). Бизнес-факт не назначается — дока, утверждающего «loss всегда KGS», в репо нет (догадка запрещена, `_METHOD §6`). НИЧЕГО НЕ ГЕЙТИТ: по прецеденту `ADR-016 §3` («охват `cashout` — из глобального scope `ADR-010`, forward-корректность, НЕ из замера») ингест обязан нести `× rate.value` нез
  …хвост:  положить SQL/лог замера в `/reference` вместе с провенанс-коммитом. **Триггер промоушена:** появление первого не-KGS `loss`-документа ЛИБО расхождение по «Списаниям»/«Прочим расходам» против оракула.

### 22  стр.370  [Q-55]  тип: факт  (1328 симв.)
  В: (E1-T1-MECH-PREP, 2026-07-26) В `core.fact_loss` поля `project_id`, `sales_channel_id`, `agent_name` = NULL во всех строках
  К: **Механизм установлен фактом 2026-07-26:** `cf-finance` строка 38 запрашивает `expand=expenseItem,agent,project,salesChannel` и проект с каналом получает; `cf-loss-commission` запрашивает `expand=expenseItem,agent,rate.currency` — без `project` и `salesChannel`, поэтому читает NULL из неразвёрнутых ссылок. Один и тот же API, раз
  …хвост: ся НОВЫМ ADR **до** правки SQL: `'Не указан'` перестаёт быть «честным отражением источника» и становится затиранием данных. Переоткрытие УЗКОЕ — `ADR-032` (17 колонок, имена, порядок) не затрагивается

### 23  стр.371  [Q-62]  тип: факт (процесс, доставка)  (2060 симв.)
  В: (E1-T1-MECH-PREP-ADJ, 2026-07-26, доставка артефактов) Диагностический блок, доставленный как многострочный `bash -c '…'` с вложенным квотингом и `sleep 900` внутри, при вставке в интерактивный Cloud Shell наложился строками: задвоенный блок, оборванный SQL, н
  К: **ОТКРЫТ.** `ADR-014 §3` (редирект в файл) присутствовал и не помог: он лечит наложение echo, а причина здесь — глубина вложенности квотинга и длина блока. **Последствие подтверждено фактом, а не осталось риском:** из искажённого паста успела уйти строка `gcloud scheduler jobs run loss-commission-daily-update` — `lastAttemptTime
  …хвост: command not found`, `syntax error`). Разрушительных действий не произошло — все строки упали. Смягчение, применённое дальше в той же сессии: подача команд по одной строке за раз. `Q-62` НЕ закрывается

### 24  стр.372  [Q-68]  тип: факт (процесс, транспорт)  (2472 симв.)
  В: (FX-OUTBOUND-COVERAGE-ADJ, 2026-07-26, доставка артефактов) Прямая вставка copy-paste bash-блока в **интерактивный Cloud Shell** дважды подряд обрывала терминал целиком — потеря сессии без следа, не наложение строк как в `Q-62`. Обход (heredoc-файл + `nohup`) 
  К: **ОТКРЫТ, путь закрытия назначен, приоритет ниже `Q-62`.** Причина НЕ установлена и не выводится (`ADR-051 §1`): наблюдения изнутри консоли нет, гипотезы (объём паста, глубина блока, лимит буфера терминала, обрыв WebSocket Cloud Shell) не различены. Класс — «интерактивный Cloud Shell как ненадёжный транспорт», НЕ под-класс налож
  …хвост:  ГРОМКИЙ, в отличие от тихой деградации до `401` в инциденте 2026-07-26. Ущерба нет: данные уже были получены скриптовым прогоном с якорями `date -u`. Провенанс: `/reference/bq_jobs_2026-07-28.md §7`.

### 25  стр.373  [Q-69]  тип: факт (две неразличённые гипотезы)  (3605 симв.)
  В: (FX-FORWARD-FILL-ADJ, 2026-07-26) Значение курса в `core.dim_fx_rates` меняется в 3–37 % дней месяца; май-2026 — ни одного изменения за 31 день (`87.4205`). Причина НЕ установлена: **(A)** `cf-fx`/его источник теряет изменения либо **(B)** официальный курс НБК
  К: **ОТКРЫТ, различитель назначен.** Замер `/reference/fx_forward_fill_2026-07-26.md §7` между гипотезами не выбирает — смотрит только внутрь таблицы. Догадкой не закрывается (`_METHOD §6`; прецедент `ADR-045 §1`). `ADR-058 §2`: различитель — GCS-архив ответа источника `gs://msklad-raw-msklad-bi-prod/fx-rates/bakai_{YYYY-MM-DD}.jso
  …хвост:  `2026-05-01…2026-06-02` вынесен в `Q-70`. (5) `ADR-058 §3` — ИСЧЕРПАН (`ADR-059 §5`). (6) `ADR-058 §2` — ИСПОЛНЕН. (7) `Q-69` больше НЕ гейтит `ADR-039 §2` (`ADR-061 §2`); политика выбрана `ADR-062`.

### 26  стр.374  [Q-70]  тип: факт (гипотеза (A) переформулирована, ра  (4358 симв.)
  В: (FX-RATE-FIDELITY-ADJ, 2026-07-26; продвинут сессией FX-MAY-WINDOW, 2026-07-28) Механизм застывшего значения `87.4205` в окне `2026-05-01…2026-06-02` (33 дня, весь май-2026 внутри) не установлен полностью. forward-fill как объяснение окна ОПРОВЕРГНУТ (`ADR-059
  К: **СУЖЕН, НЕ закрыт (сессия FX-MAY-WINDOW, 2026-07-28, `/reference/fx_may_window_2026-07-28.md`).** (D2) ИСПОЛНЕН фактом: `INFORMATION_SCHEMA.JOBS_BY_PROJECT` за окно вернула 13 заданий с полным текстом SQL — 6 forward-fill `INSERT` между `2026-05-07` и `2026-06-02`, буквально копирующих последний известный курс; 5 из 6 исполнены
  …хвост: ть выбором правдоподобной половины (`_METHOD §6`, прецедент `ADR-045 §1`). Остаток адресуется задачей `FX-MAY-WINDOW-D1-TAIL` (класс A, гейтов нет): файл источника уже лежит в репо, читатель известен.

### 27  стр.375  [PARITY-REGISTRY]  тип: задача (discovery+реестр)  (1818 симв.)
  В: (ADR-067 §5, разгейчен ADR-068/069) Реестр паритета: пары «страница/отчёт МойСклад ↔ витрина/страница LS», по паре — правило-мост (ADR-068 §3), покрытие-с-даты (якорь 2026-05-01, ADR-069), статус сходимости; + discovery report-API МойСклада (endpoint'ы ↔ стран
  К: **discovery-часть DONE (сессия PARITY-REGISTRY, 2026-07-28, класс B, мандат `ADR-076 §3a`).** Артефакты: `reference/parity_registry.md` (7 пар в таблице + раздел «вне реестра» на 5 поверхностей PR-30 + `marts.weight_flow` + плашка «вне паритета»), `reference/parity_report_api_2026-07-28.md` (18 живых GET, провенанс `reference/_s
  …хвост: ПОЛНИТЕЛЮ, но адъюдицируется архитекторской сессией, в том числе внутри Claude Code (`ADR-083 §8`); прежняя формулировка «не отдаётся» читалась как запрет инструменту вообще и была фактически неверна.

### 28  стр.376  [ORACLE-AUTOMATION]  тип: задача  (573 симв.)
  В: (ADR-067 §5c, 2026-07-27) Скрипт помесячного посева эталона (report-API/entity-API → `/reference/pnl_<YYYY-MM>.md` и далее по реестру паритета) вместо ручной выгрузки
  К: бриф после PARITY-REGISTRY (форма эталона определяется реестром); приёмка: эталон нового месяца сеется одной командой, формат идентичен ручному seed **Дописка сборки 2026-08-01 (`ADR-091 §6`):** разрезана — написание скрипта посева есть класс A и доступно сейчас; первый живой прогон с секретом есть класс B, мандат НЕ выдан, выда

### 29  стр.377  [SOURCE-MAP-REST]  тип: задача (discovery, класс A)  (980 симв.)
  В: (`ADR-079 §7b`, 2026-07-29) То же по `core.fact_purchases` (`marts.in_transit`), `core.fact_customer_invoices` (`marts.customer_invoices_ar`), `stock`-компоненту `marts.inventory_health`, плюс съём SQL custom query `msklad_counterparty_returns` (снимка в `refe
  К: **ЧАСТИЧНО DONE** (TODO → ЧАСТИЧНО DONE, сессия SOURCE-MAP-REST, 2026-07-30). Три из четырёх поверхностей карты сняты полностью (purchases→in_transit, inventory→stock-компонент inventory_health, msklad_counterparty_returns — исход (b) discovery исчерпан). Четвёртая (customer_invoices→customer_invoices_ar) — **CONTEXT GAP**: обсл
  …хвост:  Не выдаётся за полное закрытие. Артефакт: `reference/source_map_rest_2026-07-30.md`. Закрывает нашу сторону `Q-79`, `Q-80` (остаток), `Q-81`; `Q-82` остаётся открыт (дополнен, не закрыт — см. `Q-82`)

### 30  стр.378  [CC-READONLY-PRINCIPAL]  тип: задача (замер + инфра)  (1416 симв.)
  В: (`ADR-077`, 2026-07-28) Перевод класс-A сессий Claude Code на принципал с правами по факту использования: снять подтверждение с каждого запуска скрипта, не превращая ask-гейты в декорацию
  К: **READY, шаг 1 (класс A).** Замер трёх неизвестных (`ADR-077 §3`): D1 механизм трения (`/permissions` + один наблюдённый запуск); D2 уважает ли `bq` impersonation, иначе цена варианта растёт на управление файлом-ключом; D3 отделима ли запись в `marts.expenses_staging` от живой `marts.expenses` при общем датасете `marts`, и если 
  …хвост: а. Отдельно: различитель D1 («вывод `/permissions`») в десктопном режиме неисполним как записан, два пути названы в `reference/mandate_table_audit_2026-08-01.md §4a`, выбор за сессией, берущей задачу.

### 31  стр.379  [Q-76]  тип: [0-9]{2}_).*\.md$` не покрывает `CLAUDE.  (2504 симв.)
  В: (CC-HOOK-REVIEW, 2026-07-27) Охват механических проверок хука не замерялся: (а) набор гомоглифов `[МСЕАРОТНКВХ]` в проверке №2 — какие кириллические подделки НЕ ловятся, не установлено; (б) фильтр проверки №1 `^(_[A-Z]\
  К: факт (охват не замерен)

### 32  стр.380  [Q-75]  тип: факт (не подтверждён замером) + решение   (2330 симв.)
  В: (CC-PILOT-BOOTSTRAP, 2026-07-27) В `.claude/settings.json` правило `Bash(bq query:*)` стоит в `ask`, `Bash(bq query --dry_run*)` — в `allow`. По документации инструмента правила проверяются в порядке deny → ask → allow, первое совпадение решает, и `ask` побежд
  К: **ОТКРЫТ, решение отложено до замера (`ADR-071 §6`, выбор владельца), НЕ блокер.** Утверждение взято из документации, не из наблюдения на этой установке ⇒ фактом не является (`ADR-021 §2`, `05` Часть I ★). Различитель: Шаг 0 — вывод `/permissions` плюс первый фактический `bq query --dry_run` в сессии. Риск не в безопасности (пер
  …хвост: ётся ОТКРЫТЫМ: состав прав не изменён. **Триггер закрытия переформулирован:** исполнение замера и последующая правка прав, ЛИБО отрицательный результат D2/D3, при котором владелец пересматривает объём

### 33  стр.381  [Q-78]  тип: факт (мост не адъюдицирован)  (591 симв.)
  В: (`PARITY-REGISTRY`, 2026-07-28) Мост «Инвестор»/«Операционка» (`marts.sales_overview`) не выведен: поля `revenue_kgs`/`cogs_kgs`/`margin_kgs` не сверены построчно с `report/profit/byproduct`/`report/profit/bycounterparty` (оба эндпоинта подтверждены живым `200
  К: Закрывается: построчная сверка полей + предложение правила от архитектора (адъюдикация вне мандата этой сессии, `ADR-076 §4`)

### 34  стр.382  [Q-79]  тип: факт + референс-артефакт (не посеян)  (575 симв.)
  В: (`PARITY-REGISTRY`, 2026-07-28) `msklad_counterparty_returns` (Трек A, «Операционка») — ни SQL-снапшот, ни правило-мост не найдены в репо; источник — LS custom query без снимка в `reference/sql/`. Эталон `reference/recon_2026-05.md` не посеян (`02 §оракул`, `A
  К: Закрывается посевом эталона (`ORACLE-AUTOMATION`, `ADR-067 §5c`, идёт после `PARITY-REGISTRY`) + добычей SQL-снапшота custom query для `msklad_counterparty_returns`

### 35  стр.383  [Q-80]  тип: факт (мост частично не адъюдицирован)  (695 симв.)
  В: (`PARITY-REGISTRY`, 2026-07-28) Мост `marts.inventory_health` частично не выведен: `stock` не сверен построчно с `report/stock/all`/`report/stock/bystore` (оба подтверждены живым `200`, `reference/parity_report_api_2026-07-28.md`); расчётные флаги (`coverage_d
  К: Закрывается: построчная сверка `stock`-компонента; расчётные флаги остаются вне паритета решением архитектора (не ORACLE-величина) — фиксируется явным ADR, не молчаливым исключением

### 36  стр.384  [Q-81]  тип: факт (мост выведен, не сверен численно)  (422 симв.)
  В: (`PARITY-REGISTRY`, 2026-07-28) Мост `marts.in_transit` («Склад»/«Закупки в пути») выведен из SQL (`status_name IN ('В пути','Прибыл частично') AND in_transit_sum_kgs > 0`), но численная сверка с `entity/purchaseorder` (подтверждён живым `200`) не производилас
  К: Закрывается прогоном сверки суммы остатка к поставке против `entity/purchaseorder` за один фильтр периода

### 37  стр.385  [Q-82]  тип: факт (мост не адъюдицирован)  (1024 симв.)
  В: (`PARITY-REGISTRY`, 2026-07-28) Мост `marts.customer_invoices_ar` («Операционка») не выведен: соответствие `sum_kgs`/`payed_sum_kgs`/`unpaid_sum_kgs` полям `entity/invoiceout` (`sum`/`payedSum`/`state.name`, эндпоинт подтверждён живым `200`) не проверено постр
  К: Закрывается построчной сверкой полей `entity/invoiceout` против агрегата SQL. **Дополнение (`SOURCE-MAP-REST`, 2026-07-30):** обслуживающая CF (или иной загрузчик) для `core.fact_customer_invoices` не установлена среди живых функций проекта — case-insensitive grep `invoice

### 38  стр.386  [Q-84]  тип: решение  (676 симв.)
  В: (`PROCESS-DEBT`, 2026-07-29) Промоушен правила «пункт блока последствий несёт маркер времени прихода» (`ADR-083 §3`) из holika-локального `05` Часть III в канон `_METHOD §8`
  К: **DEFER, owner-gated.** `_METHOD.md` доменно-нейтрален и дистиллирован из эталона `ilyasbazarov/docs` @ `cb33fb4`; его правка задевает проекты за пределами holika — тот же blast radius и то же основание, что у `Q-40` (`ADR-021 §4`). Настоящим ADR правится ТОЛЬКО `05` Часть III, расхождение с каноном названо сознательно, а не ост

### 39  стр.387  [LAUNCH-RUNBOOK]  тип: задача (процесс)  (1377 симв.)
  В: (`ADR-082`, 2026-07-29) Пусковые сценарии сессий: чат-архитектор, генерация брифа, одна задача, параллель, сборка. До этого решения порядок запуска нигде не был записан, ссылки на роли владелец прикладывал вручную каждый раз, а `08_TASK_BRIEF_TEMPLATE` требова
  К: **DONE фаза «правила» (2026-07-29).** Введён `09_RUNBOOK.md` (пять режимов, готовые тексты первого сообщения, шпаргалка); в `08` добавлены поля «Параллель» и «Файлы на запись», убраны требование raw-URL и запрет на самостоятельное исполнение; в `_GENERATOR.md` разведены запуск с диска и запуск в чате; заведены агенты `assembler`
  …хвост: (`§4`), скрытый каталог деревьев (`§5`), режим Г в своём дереве (`§6`), коллизия имён генерации и исполнения (`§7`). Остаток `ADR-082 §3` (агенты не проверены наблюдением) настоящим ADR не закрывается

### 40  стр.388  [Q-85]  тип: решение  (615 симв.)
  В: (`STATE-STANDUP`, 2026-07-30) Правило «стенд-ап заменяется, детали дописываются» введено как holika-локальное в `05_CONVENTIONS` Часть III, тогда как канон `_METHOD §8` по-прежнему несёт одну строку «Текущий фокус»
  К: **DEFER, owner-gated.** `_METHOD.md` доменно-нейтрален и дистиллирован из эталона `ilyasbazarov/docs` @ `cb33fb4`; его правка задевает проекты за пределами holika. Основание и форма идентичны `Q-40` и `Q-84`. **Триггер промоушена:** тот же дефект (дописывание вместо замены) воспроизведён в другом проекте ЛИБО владелец выравнивае

### 41  стр.389  [LS-COUNTERPARTY-RETURNS-FIX]  тип: задача (класс B, инструменту не отдаётся  (872 симв.)
  В: (`ADR-085 §9/§10`, 2026-07-30) Правка живого Custom Query `msklad_counterparty_returns`: возвраты за период дашборда вместо скользящих 365 дней, агрегация в зерне строки вместо размножения по каналам и проектам, `transaction_date` переименовывается по смыслу (
  К: **READY** (`BLOCKED` снят сборкой 2026-08-01: `LS-QUERY-SNAPSHOT` DONE 2026-07-30, `07_ARCHIVE.md:113`). Приёмка дословно в `ADR-085 §10`. Исполняет владелец в интерфейсе Looker Studio, read-back обязателен **Дописка сборки 2026-08-01 (`ADR-091 §3`):** разрезана — подготовка текста в `reference/sql/msklad_counterparty_returns.sq

### 42  стр.390  [LS-RETURNS-FX-HARDCODE]  тип: задача (класс B, инструменту не отдаётся  (1283 симв.)
  В: (`LS-QUERY-SNAPSHOT` плюс `ADR-087 §5`, 2026-07-30) Custom Query `fact_returns` (страница «Executive summary»): курс KGS→USD вписан литералом `87.4` минуя `core.dim_fx_rates` и незакрытую политику конвертации (`Q-56`/`Q-66`/`Q-69`); поле `rate_kgs_per_usd` по 
  К: **READY.** Одна правка одного запроса закрывает три дефекта. Приёмка: литерала курса в тексте запроса нет; поле-обманка удалено либо несёт курс; период соответствует объявленному в инвентаре; read-back обязателен. Развилка владельца по курсу: чинить до закрытия политики конвертации либо ждать её. Поведение периода определено `AD
  …хвост: `ADR-091 §3`):** разрезана — подготовка текста запроса в `reference/sql/fact_returns.sql` есть класс A и отдаётся инструменту; вставка в интерфейс и read-back за владельцем. Приёмка не переписывается.

### 43  стр.391  [LS-PERIOD-CONTRACT]  тип: задача (класс B, инструменту не отдаётся  (944 симв.)
  В: (`ADR-087`, 2026-07-30) Контракт периода Custom Query: параметры дашборда по умолчанию, снимок «на момент» единственное исключение и объявляется в инвентаре плюс подписью на странице, скользящее окно литералом запрещено
  К: **READY.** Правило принято `ADR-087`, осталось исполнение: (а) объявить исключения для `msklad_customer_invoices_ar` (страница «Операционка») и `msklad_inventory_latest` (страница «Склад») строкой в инвентаре — исполнимо сейчас; (б) подписи на этих двух страницах дашборда, страницы известны; (в) `fact_returns` в этой задаче НЕ ч
  …хвост: 026-08-01 (`ADR-091 §3`):** разрезана — пункт (а) (строки-исключения в `reference/ls_custom_queries_2026-07-30.md`) есть класс A и отдаётся инструменту; пункт (б) (подписи на страницах) за владельцем.

### 44  стр.392  [LS-INVENTORY-EXPLICIT-COLUMNS]  тип: задача (класс B, инструменту не отдаётся  (823 симв.)
  В: (`ADR-087 §7`, 2026-07-30) Замена `SELECT *` в Custom Query `msklad_inventory_latest` полным явным списком колонок `marts.inventory_health`, список снимается с живой схемы в момент правки
  К: **READY.** Приёмка: в тексте запроса нет `SELECT *`; число колонок в списке равно числу колонок таблицы на момент правки; ни один график страницы «Склад» не потерял поле (проверяется после сохранения); снимок в `reference/sql/msklad_inventory_latest.sql` обновлён тем же днём. Исполняет владелец в интерфейсе Looker Studio, read-b
  …хвост: а сборки 2026-08-01 (`ADR-091 §3`):** разрезана — съём списка колонок с живой схемы и новый текст в `reference/sql/msklad_inventory_latest.sql` есть класс A; вставка и проверка графиков за владельцем.

### 45  стр.393  [Q-92]  тип: решение (owner-gated)  (1689 симв.)
  В: (`PARITY-MOMENT-ZONE`, 2026-07-31) Эталонные сутки паритета: московские сутки интерфейса, бишкекские сутки бизнеса, либо объявленное отступление (`ADR-068 §2`)
  К: **ОТКРЫТ, гейт `ADR-088 §6` СНЯТ фактом** (ночные полосы за май-2026 пусты по обеим парам: 0 из 127 документов `entity/demand`, 0 из 8 `entity/retailsalesreturn`), прежняя формулировка «НЕ задаётся до замера» устарела и вводила в заблуждение. **Вопрос сужен до одного бита — исключается ли вариант (a)** (московские сутки интерфей
  …хвост: я мая-2026, а не навсегда — первый документ в полосе `[18:00;24:00)` UTC внутри зоны паритета возвращает вопросу численную цену. Варианты и их цена без изменений в `ADR-088 §7` (статус там `proposed`)

### 46  стр.394  [Q-93]  тип: факт (не измерено)  (354 симв.)
  В: (`PARITY-MOMENT-ZONE`, 2026-07-31) В какой зоне МойСклад интерпретирует границы `momentFrom`/`momentTo` и включает ли верхнюю границу — не проверено
  К: Различитель: запрос узким окном вокруг документа с известным `M`, исполняется первым шагом задачи `PARITY-CHECK-SALES-RETURNS` вместе с замером ночных полос (`ADR-088 §8`)

### 47  стр.395  [INGEST-MOMENT-ZONE-FIX]  тип: задача (класс B)  (753 симв.)
  В: (`ADR-088 §4`, 2026-07-31) Приведение четырёх веток — возвраты и закупки в `cf-facts`, списания и комиссии в `cf-finance` — к явному правилу суток; ветка продаж НЕ трогается, она корректна
  К: **READY, мандат НЕ выдан.** Целевое правило суток гейтится `Q-92`; деплой CF дополнительно гейтится `ADR-065`. Про `core.fact_payments.moment` (уже DATE на приёме) правило суток не установлено и не предполагается — устанавливается отдельно при постановке задачи **Дописка сборки 2026-08-01 (`ADR-091 §7`):** подготовка патча в сна

### 48  стр.396  [PARITY-CHECK-SALES-RETURNS]  тип: задача (сверка, класс B)  (2698 симв.)
  В: (`ADR-088 §5/§6/§8`, 2026-07-31) Первая волна построчных сверок реестра паритета: пара «Продажи» (`core.fact_sales_profit.revenue_kgs` против `report/profit/byproduct` и `report/profit/bycounterparty` — две строки реестра, страницы LS «Инвестор» и «Операционка
  К: **READY, бриф собран (`briefs/PARITY-CHECK-SALES-RETURNS.md`, сессия `PARITY-CHECK-SALES-RETURNS-GEN` 2026-08-01), мандат класса B по-прежнему НЕ выдан.** Приёмка по шагам: (1) различитель `Q-93` — зона интерпретации `momentFrom`/`momentTo` и включение верхней границы, ДО содержательных замеров; (2) замер ночных полос `ADR-088 §
  …хвост: — оракул пары не покрывает `entity/retailsalesreturn`, реальный источник `core.fact_returns` за май (`Q-99`). Задача-строка остаётся открытой до архитекторской адъюдикации обеих находок (`ADR-076 §4`)

### 49  стр.397  [Q-101]  тип: факт (дефект классификации, денежных вел  (700 симв.)
  В: (`PARITY-GAPS-ADJ`, 2026-08-01, побочный дефект §5 `ADR-098`) `fetch_returns.py:70` определяет `has_basis` по наличию ключа `demand`, а не по его значению; у всех восьми розничных возвратов мая ключ присутствует, поэтому колонка `core.fact_returns.has_basis` =
  К: **DEFER, ничего не гейтит.** Закрывается вместе с деплоем `INGEST-MOMENT-ZONE-FIX` — отдельной задачи и отдельной строки мандата не заводится, потому что отдельного деплоя не требует. Триггер промоушена: любая задача, опирающаяся на `has_basis` как на признак

### 50  стр.398  [SALES-CONSIGNMENT-REVENUE]  тип: задача  (912 симв.)
  В: (`ADR-103 §7`, 2026-08-02) Отгрузка комиссионеру признаётся выручкой BI: товар, переданный на реализацию, попадает в `core.fact_sales_profit.revenue_kgs` как выручка месяца отгрузки, тогда как МойСклад продажей его не считает. За май-2026 — один документ `enti
  К: **READY, гейт замера снят (`ADR-104 §5`): масштаб дефекта замерен по всей зоне паритета и оказался единичным** — ровно один документ, доля `0,675 %` зоны паритета. Форма фикса не назначена: зависит от того, есть ли у отгрузок комиссионерам различающий признак в уже загружаемых полях. Класс подготовки — A (правка снапшота `refere

### 51  стр.399  [SALES-PERIMETER-EXTEND]  тип: задача  (661 симв.)
  В: (`ADR-103 §8`, 2026-08-02) Расширение периметра ингеста продаж на `entity/retaildemand` и на продажи по `entity/commissionreportin` — следствие выбора владельцем варианта (a) объекта паритета продаж
  К: **READY, оба гейта сняты.** Гейт решения снят выбором владельца (a) (`ADR-103 §8`); гейт подтверждающего замера снят (`ADR-104 §2`). Тем же выбором адресована правка правила-моста `reference/parity_registry.md` строки 18-19: объект паритета продаж = число `report/profit/*`. Форма — ОТДЕЛЬНЫЙ ингест по прецеденту `ADR-024`, НЕ ра

### 52  стр.400  [SALES-REFRESH-WINDOW]  тип: задача  (2805 симв.)
  В: (`ADR-097 §5`, 2026-08-01) Окно обновления `cf-facts` (7/90 суток скользящим) не покрывает якорь паритета `2026-05-01`, режима досева у продаж нет
  К: **Класс A:** подготовка ОДНИМ патчем снапшота `reference/code/cf-facts/` по трём дефектам одной и той же операции `MERGE` (`ADR-100 §7`, объединение принято владельцем 2026-08-01): (1) окно — досев отдельным режимом с раздельными окнами выборки и `MERGE`; (2) отсутствие ветки удаления (`bq_ops.py:237-305`) — строки, исчезнувшие 
  …хвост: `cogs_source` для диапазона шире 7/90 суток и допустимость `UPDATE transaction_date` в `MERGE` остаются открытыми, не домысливались. Выбор варианта и деплой — класс B, мандат не выдан, гейт `ADR-065`.

### 53  стр.401  [Q-94]  тип: факт (не измерено)  (790 симв.)
  В: (`MANDATE-TABLE-AUDIT`, 2026-08-01) Можно ли проверить язык ответа владельцу машинно. У инструмента есть механизм перехвата на конце хода, который в принципе способен прочитать текст ответа и заругаться на внутренний идентификатор без расшифровки (`ADR-056 §1`
  К: **DEFER, не гейтит ничего.** Из документации не берётся (`ADR-021 §2`, `05` Часть I ★): нужен замер на этой установке. Правило `ADR-093 §1-§3` от замера не зависит и держится на ревью владельца. Основание держать вопрос открытым — `ADR-093 §5`: три причины из четырёх сняты мерами, конкуренцию проверяемого правила с непроверяемым

### 54  стр.402  [Q-97]  тип: факт (две неразличённые гипотезы)  (1416 симв.)
  В: (`E1-T3-CLOSE`, 2026-08-01) Единственный документ зоны паритета, не переписанный после деплоя валютной правки: №05662, `23167c6f-76d1-11f1-0a80-0556001964ca`, `moment 2026-07-03`, `_loaded_at 2026-07-07 21:01:30`, контрагент ООО «ВАЛЕН СЕРВИС», статья «Бухгалт
  К: **ОТКРЫТ, оба различителя названы, цена мала.** Гипотеза (A): документ не-KGS и его сумма занижена — величина ошибки равна `30 000 × (rate−1)`; при рублёвом курсе порядка 1,25 это около 7 500 KGS, при KGS ошибки нет вовсе. Гипотеза (B): документ перестал приходить из источника (удалён либо изменён в МойСкладе), то есть перед нам
  …хвост: t/23167c6f-76d1-11f1-0a80-0556001964ca` — класс B (`msklad-token`), мандат НЕ выдан. **Гейтит:** ничего. Величина максимум порядка 7 500 KGS против майского эталона 10 232 903,20, то есть менее 0,08 %

### 55  стр.403  [FX-MAY-WINDOW-D1-TAIL]  тип: задача (discovery, класс A)  (1092 симв.)
  В: (`ADR-101 §4`, 2026-08-02) Остаток различителя D1 по `Q-70`: источник проверен только за `2026-04-25…2026-05-10`, тогда как окно застывания курса — `2026-05-01…2026-06-02`; отрезок `2026-05-11…2026-06-02` (23 суток), в котором D2 нашёл шесть ручных forward-fil
  К: **READY, гейтов нет.** Метод: прочитать лист `'2026'` файла `reference/_scratch_fx_may_window_2026-07-28/nbkr_dailyrus_raw.xls` (лист брать ПО ИМЕНИ `wb.sheet_by_name('2026')` — `sheet_by_index(0)` даёт лист `'2010'`, грабли уже наступлены) за `2026-05-11…2026-06-02` и сверить построчно с `core.dim_fx_rates` за те же даты одним 
  …хвост: я окна, любое расхождение означает (A) и фикс-форвард в слой курсов, НЕ в марты (`ADR-062 §7`). Квалификация результата — архитектору (`ADR-058 §7`). Пишет: `reference/fx_may_window_d1_tail_<date>.md`

### 56  стр.404  [AUDIT-SNAPSHOT-FIX-EMPLOYEES]  тип: задача  (961 симв.)
  В: (`ADR-101 §3`, 2026-08-02) `audit.dim_employees_snapshots` несёт `snapshot_at` последней колонкой при `INSERT … SELECT *, CURRENT_TIMESTAMP()` — та же конструкция, что сломала снимок товаров и прожила 45 суток невидимой (`ADR-019`); `core.dim_employees` версио
  К: **READY, форма фикса задана и не изобретается: вариант E `ADR-019 §3`** — пересоздать `audit.dim_employees_snapshots` с `snapshot_at` ПЕРВОЙ колонкой, запрос переписать на `SELECT CURRENT_TIMESTAMP() AS snapshot_at, * FROM core.dim_employees`. Подготовка DDL и текста запроса — класс A. Исполнение (пересоздание таблицы, правка жи
  …хвост:  НЕ выдан. Осознанный размен варианта E назван в `ADR-019 §3` и здесь не переоткрывается. Пишет: `reference/sql/sq_audit_dim_employees_snapshot.sql`, `reference/audit_snapshot_fix_employees_<date>.md`

### 57  стр.405  [AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE]  тип: задача  (947 симв.)
  В: (`ADR-101 §3`, решение владельца 2026-08-02) Ежедневная копия `core.dim_counterparties` в `audit.dim_counterparties_snapshots` дублирует историю, которая в ядре уже есть нативно (SCD2), потребителя не имеет и несёт ту же взведённую мину позиционного `INSERT … 
  К: **READY, решение принято — ретайр.** Приёмка: `transferConfig` снапшота контрагентов отключён либо удалён, факт подтверждён read-back; отсутствие потребителя переподтверждено НА МОМЕНТ ИСПОЛНЕНИЯ (не наследуется из сессии `UNGATED-QUEUE-ADJ`) сплошным поиском чтений `audit.` по `reference/sql/*.sql` и по инвентарю Custom Query с
  …хвост:  НЕ удаляются — ретайр останавливает дальнейшее накопление, а не стирает накопленное (решение об удалении данных отдельное и здесь не принимается). Класс B (правка живой конфигурации), мандат НЕ выдан

### 58  стр.406  [INGEST-CURRENCY-ASSERT]  тип: задача  (1070 симв.)
  В: (`ADR-101 §5`, 2026-08-02) Класс ошибки «забыли умножить на курс» непроверяем в хранилище по построению: таблицы фактов не несут ни валюты документа, ни суммы в минорных единицах, ни применённого курса — проверка обязана стоять в слое загрузки, где эти величин
  К: **READY по шагам 1-2.** Шаг 1 (класс A): снять исходник и актуальную ревизию `cf-dq`, напечатать фактический текст проверки `currency_normalization` — закрывает названный `CONTEXT GAP` и `Q-6`, попутно сужает residual `Q-3`. Шаг 2 (класс A): подготовить проверку в снапшоте `reference/code/` — для каждой позиции либо валюта докум
  …хвост: ится `ADR-065`. Действующий агрегатный порог `avg_revenue_kgs < 10M` не снимается, но перестаёт считаться покрытием этого класса. Пишет: `reference/ingest_currency_assert_<date>.md`, `reference/code/`

### 59  стр.407  [METHOD-PROMOTE-LOCAL-RULES]  тип: задача (класс A)  (880 симв.)
  В: (`ADR-101 §9`, решение владельца 2026-08-02) Продвижение трёх holika-локальных правил в `_METHOD.md` помеченным расширением: «успех инструмента ≠ факт» (`Q-40`, в `§6`), «маркер времени прихода пункта последствий» (`Q-84`, в `§8`), «стенд-ап заменяется целиком
  К: **READY, гейтов нет.** Форма задана прецедентом `_METHOD.md §11 [РАСШИРЕНИЕ]`: помеченный блок плюс провенанс-строка «за пределами эталонного baseline». Эталон `ilyasbazarov/docs @ cb33fb4` НЕ правится, чужие пины не ломаются. Приёмка: три правила присутствуют в `_METHOD.md` с пометкой расширения; `05_CONVENTIONS` Часть I/III и 
  …хвост: е переписываются (локальные редакции остаются, расхождение с каноном исчезает); строки `Q-40`, `Q-84`, `Q-85` закрываются этой задачей. Правка идёт отдельным именованным коммитом сессии (`ADR-081 §8`)

### 60  стр.408  [MSKLAD-TZ-PROPOSAL]  тип: задача (владелец)  (742 симв.)
  В: (`ADR-101 §6`, решение владельца 2026-08-02) Передача клиенту предложения переключить часовой пояс аккаунта МойСклад с московского на бишкекский; исполняет владелец, инструменту недоступно ни в каком классе
  К: **READY, текст готов** — `reference/msklad_timezone_proposal_2026-08-02.md`. Включает обратимую пятиминутную проверку ретроактивности (пункт 5), которая попутно закрывает несделанный второй замер сдвига `ADR-088 §10`. Исходы: согласие означает закрытие `Q-92` вариантом (b), пояс аккаунта переключён, единственная сошедшаяся пара 

### 61  стр.409  [PARITY-ZONE-SNAPSHOT-DECISION]  тип: решение  (1237 симв.)
  В: (`ADR-101 §10`, решение владельца 2026-08-02) Чем является зона паритета во времени — непрерывно обновляемой областью или датированным снимком. Сегодня фактически второе и не по решению, а по умолчанию: окно `cf-facts` скользящее 90 суток, `2026-05-01`/`05-02`
  К: **РЕШЕНО владельцем 2026-08-02: вариант (3) сейчас, вариант (1) как цель.** Сейчас зона паритета объявляется датированным снимком с назначенной каденцией пересъёма; режим досева произвольного исторического периода становится целью после закрытия шага 2b `CODE-REPO-STANDUP` (деплой гейтится `ADR-065`); вариант (2) (разовый пересе
  …хвост: `: формулировка финишной черты сужена, `ADR-101 §10` несёт `superseded-in-part`. Строка закрывается по факту появления плашки в `02_ERP_CONTRACTS`/`reference/parity_registry.md` и назначенной каденции

### 62  стр.410  [STATE-DETAILS-BUDGET]  тип: задача (класс A, документная + правка ху  (836 симв.)
  В: (`STATE-DETAILS-ADJ`, 2026-08-02, `ADR-105 §4`) Назначить числовой бюджет полю «Подробности для модели» в session-блоке и максимальную длину строки GAP-реестра, плюс механическую проверку в `tools/hooks/pre-commit` с кейсами в `tools/hooks/selftest.sh`. **Гейт
  К: закрывается ADR с числом, выведенным из замера, плюс правки хука и его самотестов

