# Architect review queue — 2026-07-26 (FX-OUTBOUND-COVERAGE)

## п.1 — min_date аномалия
Контекст: core.dim_fx_rates покрыт с 2025-04-29, документированный источник
(Bakai OpenBanking, 02 §core.dim_fx_rates) — с 2026-06-03. Расхождение 13 месяцев.
Вопрос: второй/более ранний источник наполнения, ручной бэкафилл, или доки
02 неполны? Варианты: (a) обновить 02 фактом о более раннем источнике —
нужен discovery; (b) DEFER в 07_STATE как открытый Q.
Рекомендация разработчика (не решение): DEFER с новым Q-номером, не блокирует
ADR-039 §2 (предусловие §4 снято — май покрыт полностью).
Блокирует: ничего немедленно.

## п.2 — blast radius ADR-039 §5 шире ожидаемого
Контекст: помимо expenses/in_transit, latest_fx (в разных формах) найден в
abc_xyz, gmroi, inventory_health (полностью) и supplier_price_history (fallback).
Вопрос: политика ADR-039 §2, если принята как (b), должна применяться ко всем
5+1 файлам одинаково, или по типу метрики (§3 факт-подсказка уже в ADR-039)?
Варианты: единая политика на все / политика per-метрика (состояние vs поток).
Рекомендация: решение остаётся owner-gated как и заявлено в ADR-039 §2 — этот
пункт лишь расширяет её область применения фактом, не предрешает выбор.
Блокирует: решение владельца по ADR-039 §2 (уже blocked этим).

## п.3 — класс инцидента: интерактивный шелл полностью роняет сессию
Контекст: прямая вставка copy-paste bash-блока в интерактивный Cloud Shell
дважды подряд обрывала терминал целиком (не наложение строк, как в Q-62, а
потеря сессии без следа). Workaround (heredoc-файл + nohup) сработал.
Вопрос: новый под-класс к Q-62, или отдельный факт про интерактивный Cloud
Shell конкретно (объём/глубина блока)? Причина не установлена — не было
доступа к самой consоли для диагностики (человек, не я, наблюдал падение).
Рекомендация: DEFER, фиксировать как наблюдение; ADR-014 §3 (редирект в
файл) уже отчасти покрывает это дефолтной формой доставки — heredoc-паттерн
стоит рассмотреть как следующее расширение ADR-014, аналогично ADR-043/044.
Блокирует: ничего в этой задаче — уже обойдено практически.
