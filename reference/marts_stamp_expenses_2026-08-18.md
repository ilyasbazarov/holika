# FILE: marts_stamp_expenses_2026-08-18.md

# Отметка времени сборки применена на `sq_marts_expenses` — приёмка закрыта тремя пунктами,
# урок предыдущей сессии применён с первого раза

**Сессия:** `MARTS-STAMP-EXPENSES` (класс B, мандат выдан поимённо владельцем, чат 2026-08-18,
`reference/dq_deploy_accept_adj_2026-08-18.md §5`; форма/предусловия/приёмка/откат — там же).
**Дата:** 2026-08-18 (Бишкек). **База:** `9485dac`. Это ТРЕТИЙ и последний из трёх объектов строки
`7` (`MARTS-BUILD-STAMP`).

**Объект.** `transferConfig` `sq_marts_expenses`, Config ID `6a22a243-0000-20fd-a458-883d24f4cad4`,
целевая таблица `msklad-bi-prod.marts.expenses`.

---

## Что сделано

Поле `params.query` живой конфигурации подменено на подготовленный текст
`reference/sql/sq_marts_expenses_stamped_2026-08-13.sql`. **Урок сессии `MARTS-STAMP-CUSTINVAR`
применён с первого захода:** `params` этого объекта, как и у `customer_invoices_ar`, несёт три
служебных поля сверх `query` (`destination_table_name_template`, `partitioning_field`,
`write_disposition`) — предусловие (i) сразу сняло их значения, правка передала `params` целиком
(новый текст запроса + сохранённые три поля). Read-back прошёл сразу, без исправлений. Расписание,
адресат, права — не тронуты.

Один ручной прогон (`bq mk --transfer_run`), объявленный владельцу тем же сообщением, что и
правка. Завершился `SUCCEEDED` с первой попытки.

## Предусловия — оба пройдены до правки

**(i)** Живой запрос сверен со снимком репо `reference/sql/sq_marts_expenses.sql` — **побайтовое
совпадение** (`diff rc=0`, `3182`/`3182` байт). Полный `params` снят и сохранён ДО правки —
`live_params_full_2026-08-18.json` (эталон отката вместе с текстом запроса
`live_query_extracted_2026-08-18.sql`). Лог — `step1_run.log`.

**(ii)** `bq query --dry_run` подготовленного текста — `rc=0`, `totalBytesProcessed=1544366`
(`PRECISE`). Лог — `step2_run.log`.

**(iii)** Действие объявлено владельцу отдельным сообщением до `ask` — состав `params`, чем
откатывается, ручной прогон; дословное «да» получено.

## Приёмка — три пункта, все закрыты фактами, без инцидентов

**1. Read-back.** `bq show --transfer_config` после правки отдаёт текст запроса, побайтово равный
подготовленному файлу: `diff rc=0`, sha256 совпадает
(`8de7ebd348f3bc2fe0ae372bd047df4e520c5ec4b20ffbd3a48d435cbd3ad1f7`); все три служебных поля на
месте с прежними значениями (`destination_table_name_template=expenses`, `partitioning_field=''`,
`write_disposition=WRITE_TRUNCATE`). Лог — `step4_run.log`.

**2. Схема.** Обе колонки присутствуют в схеме `marts.expenses` после пересборки:
`_marts_built_at`, `_source_max_loaded_at` (полный состав — семнадцать прежних полей плюс эти
две). Лог — `step7_run.log`, `table_schema_2026-08-18.json`.

**3. Состав живого результата.** Замер по всей таблице после ручного прогона (`3302` строки):

| Колонка | Непустых строк | Мин | Макс |
|---|---|---|---|
| `_marts_built_at` | `3302` из `3302` | `2026-08-18 08:58:06` | `2026-08-18 08:58:06` |
| `_source_max_loaded_at` | `3302` из `3302` | `2026-08-17 21:00:12` | `2026-08-17 21:00:12` |

Ноль пустых значений по обеим колонкам. Лог — `step7_run.log`, `data_check_2026-08-18.json`.

## Что НЕ делалось

- Расписание, адресат, права `transferConfig` не менялись.
- Текст запроса не пересобирался сверх подстановки подготовленного файла целиком.
- Никаких записей в `marts.*` руками, кроме результата штатного прогона `transferConfig`.
- Инцидентов не было — предусловие (i) заранее сняло полный состав `params`, повторения дефекта
  `MARTS-STAMP-CUSTINVAR` не произошло.

## Откат (не потребовался)

`bq update --transfer_config` прежним полным `params` из
`reference/_scratch_MARTS-STAMP-EXPENSES_2026-08-18/live_params_full_2026-08-18.json` (снят до
правки, содержит прежний текст запроса и все три служебных поля). Пересборка витрины при откате
не требуется.

## Значение для проекта

Это последний из трёх объектов строки `7` (`MARTS-BUILD-STAMP`) списка закрытия. Со всеми тремя
объектами (`weight_flow`, `customer_invoices_ar`, `expenses`) отметка времени сборки применена и
принята — строка `7` может закрываться целиком следующим проходом сборки при подтверждении
конечного состояния всех трёх дописок в реестре.

## Адреса логов

Все скрипты и логи — `reference/_scratch_MARTS-STAMP-EXPENSES_2026-08-18/`: `step1`–`step2`
(предусловия, включая полный снимок `params`), `step3`/`step4` (правка и read-back, с первого
раза), `step5`/`step6` (ручной прогон — `SUCCEEDED` с первой попытки), `step7` (приёмка 2 и 3).
