# FILE: dq_freshness_wire_checks_order_2026-08-18.md

# DQ-FRESHNESS-WIRE-CHECKS-ORDER — перенос блока `CHECKS` ниже определений, 2026-08-18

**Задача:** доисполнение остатка (1) строки реестра `DQ-FRESHNESS-WIRE` (`07_GAPS.md`) —
правка класса A, класс подтверждён `07_STATE.md §Мандат Claude Code`. Брифа нет и не нужен
(`ADR-086 §1`). Класс — A: правка снапшота `reference/code/cf-dq/main.py`, ничего не
деплоится, живых вызовов к облаку нет.

**База:** `5b2d8df` (голова `main` после сборки буфера 2026-08-18, седьмой проход).
**Дерево/ветка:** `worktrees/DQ-FRESHNESS-WIRE-CHECKS-ORDER` / `s/DQ-FRESHNESS-WIRE-CHECKS-ORDER`.

## Что сделано

Блок `CHECKS = [...]` (19 пар) перенесён из позиции `main.py:215-235` (выше определений
двенадцати функций `check_freshness_*`, что давало `NameError` при импорте модуля в проде,
`2026-08-18`) в позицию ниже определения `check_freshness_invoices_business` и выше `def
main(request):`. Ни одна строка внутри блока не изменена (см. `git diff` ниже — удалённый и
добавленный блок побайтово идентичны), ни одна функция не тронута. Больше в файле не менялось
ничего.

## Приёмка — четыре пункта

**(1) Позиция блока.** `grep -n` подтверждает: `check_freshness_invoices_business` определена
строкой `477`, `CHECKS = [` — строкой `490`, закрывающая `]` — строкой `510`, `def main(request):`
— строкой `513`. Блок лежит строго между концом последней функции `check_freshness_*` и точкой
входа.

**(2) Импорт модуля — факт-проверка исполнением.** Локальный интерпретатор дерева — Python
`3.9.6` (`Clang 17.0.0`), в котором `helpers.py`/`main.py` несут аннотацию `dict | None`
(PEP 604), не поддерживаемую рантаймом `3.9` — это свойство МАШИНЫ, не патча (`TypeError:
unsupported operand type(s) for |`, наблюдалось на ОБОИХ модулях одинаково, до и не зависимо
от переноса `CHECKS`). Найден альтернативный интерпретатор на той же машине — Homebrew
`python@3.14` (`/opt/homebrew/opt/python@3.14/bin/python3.14`, версия `3.14.6`), на котором
импорт `main` (транзитивно `helpers`, `config`) прошёл `rc=0`:

```
интерпретатор: 3.14.6 (main, Jun 10 2026, 10:03:53) [Clang 17.0.0 (clang-1700.6.4.2)]
ok   импорт main
len(CHECKS)=19
```

Сторонние зависимости (`google.cloud.bigquery`, `functions_framework`) подставлены
снисходительными стабами (форма — `reference/_scratch_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-
2_2026-08-18/import_check.py`): любой атрибут — вызываемый объект, любой вызов возвращает ещё
один такой же объект; `functions_framework.http` переопределён identity-декоратором (обязан
вернуть саму функцию `main`, иначе последующий вызов не пойман тестом). Полный лог —
`reference/_scratch_DQ-FRESHNESS-WIRE-CHECKS-ORDER_2026-08-18/import_check_main_run.log`.

Файл `patch_dq.py` (четвёртый `.py` в каталоге) в проверку НЕ входит: это одноразовый локальный
скрипт правки, читающий `main.py` по ОТНОСИТЕЛЬНОМУ пути от текущей директории (`open("main.py")`);
он не импортируется `main.py`, не декорирован `functions_framework.http`, не часть графа
исполнения CF и не деплоится. Первый прогон универсального скрипта (импортирующего все `.py`
каталога подряд, `import_check_run.log`) корректно поймал этот файл как `FileNotFoundError` —
это свойство скрипта, не дефект патча; повторный целевой прогон (`import_check_main_run.log`)
проверяет ровно то, что реально исполняется в проде — модуль `main`.

**(3) `git diff --stat` и состав правки.** `git diff --stat` показывает РОВНО один файл:

```
reference/code/cf-dq/main.py | 44 ++++++++++++++++++++++----------------------
 1 file changed, 22 insertions(+), 22 deletions(-)
```

Полный `git diff` (`reference/_scratch_DQ-FRESHNESS-WIRE-CHECKS-ORDER_2026-08-18/git_diff_full.log`)
показывает удаление блока `CHECKS` на старом месте и добавление БУКВАЛЬНО ТОГО ЖЕ блока (19 строк,
побайтово идентичны) на новом месте; единственная соседняя правка — снятие одной лишней пустой
строки, образовавшейся между функцией и блоком при переносе (форматирование, не содержание). Ни
одна функция `check_*` не тронута.

**(4) Логи файлами.** Все логи — `reference/_scratch_DQ-FRESHNESS-WIRE-CHECKS-ORDER_2026-08-18/`:
`import_check.py` (универсальный скрипт проверки), `import_check_run.log` (первый прогон, все
4 `.py`, поймал ожидаемый `FileNotFoundError` у `patch_dq.py`), `import_check_main_run.log`
(целевой прогон модуля `main`, `rc=0`, `len(CHECKS)=19`), `git_diff_stat.log`, `git_diff_full.log`.

## Вне scope (не сделано, по инструкции)

Деплой; любые живые вызовы `gcloud`/`bq`; правка `config.py`, `helpers.py`, порогов; любые
другие правки `main.py`; слияние веток код-репо; `master` код-репо. Новых строк реестра эта
работа не заводит (`ADR-186 §6`, `ADR-156 §2/§5`) — доисполняет существующую строку
`DQ-FRESHNESS-WIRE`.

## Следующий шаг

Строка `DQ-FRESHNESS-WIRE` (`07_GAPS.md`) переходит к шагу (2) её собственного порядка —
повторное ревью архитектора с факт-проверкой исполнением (эта проверка её частично
предвосхищает, но не заменяет — ревью адъюдицирует архитектор, не эта сессия); шаг (3) —
деплой класса B по заново выданному мандату (`ADR-186 §7`).
