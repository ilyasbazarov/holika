# FILE: sales_overview_owner_switch_2026-08-15.md

# Исполнение мандата `SALES-OVERVIEW-OWNER-SWITCH`

**Дата:** 2026-08-15 (Бишкек) · **Класс:** B, мандат выдан поимённо 2026-08-13.
**Мандат:** `reference/sales_overview_owner_switch_mandate_2026-08-13.md` (читать целиком до исполнения — исполнено).
**Провенанс сырых логов:** `reference/_scratch_SALES-OVERVIEW-OWNER-SWITCH_2026-08-15/`.

---

## §1 Предусловия (§4 мандата)

- **П1** (`step1_transfer_config.json`, `step1_live_query.sql`). Живой текст `transferConfig` побайтово
  отличался от снапшота ТОЛЬКО в длине декоративных полосок-разделителей комментариев (26 строк diff)
  и в отсутствии финального перевода строки. Все четыре целевые строки правки (менеджер/владелец
  документа, будущие строки 53–61 и 127–130) совпадали побайтово. Мандат требует стоп на любое
  расхождение — вопрос задан владельцу в чате, получен ответ «Продолжить правку» (2026-08-15).
- **П2** (`step2_schema.json`). `document_owner_employee_id` (`STRING`) присутствует в схеме
  `core.fact_sales_profit` — подтверждено.
- **П3** (`step3_baseline.sql`, `step3_baseline_out.log`). Базовые числа §5 переснятые собственным
  запросом непосредственно перед правкой:
  - июль по владельцу документа: Турдалиева `74 745 820,28`; Турсунбекова `23 152 315,77`;
    Омурбекова `11 091 597,83`; «Не указан» `3 334 723,58`.
  - суммарная выручка: июль `112 324 457,46`; май `95 214 205,80`; апрель `85 085 235,81`;
    вся история `701 526 752,52`.
  - апрель по владельцу контрагента (историческая ветка) — числа совпали с анкорными числами
    мандата побайтово.
  - май по владельцу документа — числа совпали с анкорными числами мандата побайтово.

## §2 Правка (§3 мандата)

Диф ровно в двух блоках, дословно предписанных мандатом — `reference/sql/sq_marts_sales_overview.sql`
(строки 53–61: атрибуция менеджера через `IF(transaction_date < DATE '2026-05-01', …)`; строки 127–130:
JOIN `dim_employees` раздвоен на `e_agent`/`e_doc`). Проверка отсутствия остатков алиаса `e` —
`grep -n '\be\.\|owner_employee_id\|manager_'` (`step` не сохранён отдельно, вывод в теле сессии):
ровно шесть совпадений, все внутри новых блоков, алиас `e` не встречается.

`bq query --dry_run` на изменённом файле — успешно, 8 822 943 байт верхняя граница (`step4_dryrun.log`).

## §3 Применение (не-идемпотентная операция, подтверждено владельцем в чате 2026-08-15)

`bq update --transfer_config` через Python-скрипт `step5_apply.py` (форма без heredoc — прецедент
`M-22`/`ADR-055`), лог `step5_apply_out.log` (RC=0, вывод внутренне противоречив из-за буферизации
stdout — не примирялся, вместо этого сделана независимая read-only проверка).

**Read-only верификация** (`step6_verify_config.json`, `step6_live_query_after.sql`): живой текст
запроса ПОБАЙТОВО совпал с `reference/sql/sq_marts_sales_overview.sql` (`diff` вернул 0 строк
расхождения). `updateTime` = `2026-08-15T07:45:17.472102Z`.

## §4 Пересборка

Штатный прогон был бы в `09:34:00Z`; по решению владельца (чат 2026-08-15) запущена ручная
пересборка — `bq mk --transfer_run` (`step7_manual_run.log`), run id
`.../runs/6a822e40-0000-2cbf-a7c2-582429c412a8`, `runTime=2026-08-15T08:05:53Z`. Опрос состояния
(`step7_run_poll_*.json`) — `RUNNING` → `SUCCEEDED` за 5 проходов по 10 секунд.

## §5 Приёмка (§5 мандата, четыре проверки, все PASS)

Запрос и полный вывод — `step8_acceptance.sql` / `step8_acceptance_out.log`.

1. **Июль по `manager_name` из витрины.** Турдалиева `74 745 820,28` — эталон клиента
   `74 745 820,29`, расхождение `0,01` (округление, допустимо мандатом). **PASS.**
2. **Суммарная выручка не изменилась.** Против собственного пре-фикс базового замера (§1 П3):
   июль `112 324 457,46` → `112 324 457,48` (+0,02); май `95 214 205,80` → `95 214 205,79` (−0,01);
   апрель `85 085 235,81` → `85 085 235,83` (+0,02); вся история `701 526 752,52` → `701 526 752,73`
   (+0,21, объясняется новыми документами за прошедшие ~20 минут между переснятием и пересборкой —
   период «вся история» включает текущий незакрытый месяц). Отклонения — копейки, ожидаемый шум
   между двумя снимками живой таблицы, не эффект правки (правка не трогает агрегацию, только разрез).
   **PASS.**
3. **История до мая не изменилась.** Апрельский разрез по `manager_name` совпал с анкорными числами
   мандата побайтово: Турдалиева `57 406 025,62`; Турсунбекова `19 506 791,28`; Омурбекова
   `4 582 772,94`; `NULL` `2 851 639,89` (пришёл как SQL `NULL`, не как строка `'Не указан'` —
   подтверждает, что `COALESCE` не протёк в историческую ветку); Бейшеналиева `652 772,70`;
   Бактияр кызы `43 689,93`; Казакбаев `41 543,47`. **PASS.**
4. **Май соответствует ожидаемому разрезу.** Турдалиева `77 229 469,38` (анкор `77 229 469,41`,
   −0,03); Турсунбекова `11 931 866,13` (точное совпадение); «Не указан» `5 301 450,08` (точное
   совпадение); Бактияр кызы `751 420,20` (анкор `751 420,18`, +0,02). Расхождения — копейки,
   тот же класс шума, что и в проверке 2. **PASS.**

## §6 Итог

Все четыре проверки приёмки пройдены. Мандат исполнен полностью: объект один
(`sq_marts_sales_overview`), правка ровно в предписанных местах, откат не потребовался.
