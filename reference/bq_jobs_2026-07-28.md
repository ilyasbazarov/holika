# FILE: bq_jobs_2026-07-28.md

# Замер заданий BigQuery 13–28 июля · закрытие остатка `Q-64`, сужение `Q-78`

**Дата (Бишкек):** 2026-07-28 · **Роль:** владелец (Cloud Shell + терминал macOS) + архитектор-чат
**Основание:** `ADR-074 §2` (форма замера — цель, не флаг), остаток `Q-64`, различитель `Q-78`.
**Класс:** read-only замер. Команд записи нет.

---

## §1 Якоря и личность вызывающего (`ADR-055 §3`, `ADR-063 §4`)

| Прогон | Среда | Начало (`date -u`) | Конец (`date -u`) | Аккаунт начала = конца |
|---|---|---|---|---|
| 1 (задания `marts.*`) | Cloud Shell, bq 2.1.34 | `2026-07-28T05:27:21Z` | `2026-07-28T05:27:34Z` | `ilyasbazarov4@gmail.com` |
| 2 (принципалы) | терминал macOS | `2026-07-28T05:34:35Z` | `2026-07-28T05:34:38Z` | `ilyasbazarov4@gmail.com` |

Расхождений личности внутри прогонов нет. Оба прогона исполнены скриптом-файлом с редиректом
в лог (`ADR-055 §1`).

## §2 Смена инструмента — основание

Замер выполнен через `INFORMATION_SCHEMA.JOBS_BY_PROJECT` (регион `asia-east1`), а не `bq ls -j`.
Основание — двукратный отказ прежнего инструмента на непарсимой выдаче: `ADR-021 §3`
(два JSON-документа при усечении) и «Extra data: line 6» в приёмке cutover
(`sq_cadence_2026-07-27.md §6`). `INFORMATION_SCHEMA` возвращает таблицу и несёт `user_email`
построчно. Версии `bq` в проекте расходятся: 2.1.33 (`ADR-021 §3`), 2.1.34 (Cloud Shell),
2.1.35 (macOS) — подтверждение `ADR-074 §2`: фиксируется цель замера, не флаг.

## §3 Задания с целью в `marts.*`, окно `2026-07-13T00:00:00Z` … прогон

Всего **319** заданий, фактическое окно `2026-07-13T01:34:01Z … 2026-07-28T03:34:03Z`.
Горизонт хранения метаданных нижней границы фильтра не достиг — задания есть на самой границе.

| Целевая таблица | Заданий |
|---|---|
| `sales_overview` | 182 |
| `expenses` | 16 |
| `gmroi`, `abc_xyz`, `customer_invoices_ar`, `inventory_health`, `gmroi_by_folder`, `supplier_price_history`, `in_transit`, `weight_flow` | по 15 |
| `expenses_staging` | 1 |

182 ≈ 12/сут × 15,2 сут — подтверждает `every 2 hours` у `sq_marts_sales_overview` (`§4`
артефакта `sq_cadence_2026-07-27.md`). По 15 у восьми витрин — суточная каденция флота.
Единственный `expenses_staging` — ручной прогон 26.07 19:39, совпадает с
`sq_cadence_2026-07-27.md §3`.

## §4 `marts.expenses` поимённо — 16 заданий

| Дата | Время UTC | job_id |
|---|---|---|
| 2026-07-13 | 11:10:00 | `scheduled_query_6a54d30a-…` |
| 2026-07-14 | 11:10:04 | `scheduled_query_6a56f2a2-…` |
| 2026-07-15 | 11:10:04 | `scheduled_query_6a57ad50-…` |
| 2026-07-16 | 11:10:02 | `scheduled_query_6a58ba70-…` |
| 2026-07-17 | 11:10:01 | `scheduled_query_6a5b265d-…` |
| 2026-07-18 | 11:10:03 | `scheduled_query_6a5b66bf-…` |
| 2026-07-19 | 11:10:04 | `scheduled_query_6a61544d-…` |
| 2026-07-20 | 11:10:05 | `scheduled_query_6a5dfca7-…` |
| 2026-07-21 | 11:10:01 | `scheduled_query_6a5f3c29-…` |
| 2026-07-22 | 11:10:05 | `scheduled_query_6a616976-…` |
| 2026-07-23 | 11:10:03 | `scheduled_query_6a6134eb-…` |
| 2026-07-24 | 11:10:04 | `scheduled_query_6a63a23e-…` |
| 2026-07-25 | 11:10:09 | `scheduled_query_6a62946b-…` |
| 2026-07-26 | 11:10:03 | `scheduled_query_6a65b16c-…` |
| 2026-07-27 | 11:10:01 | `scheduled_query_6a66c595-…` |
| **2026-07-27** | **15:53:48** | **`scheduled_query_6a6770e8-0000-2b13-9b3e-089e082b4104`** — ручная пересборка cutover |

Пятнадцать суточных прогонов подряд, без пропусков, разброс `11:10:00`–`11:10:09`.
Все под `ilyasbazarov4@gmail.com` (владелец конфигурации, `ADR-045 §1`).
Прогон `2026-07-28T11:10Z` на момент замера ещё не наступил.

**Положительный контроль инструмента.** Строка 15:53:48 — прогон, запущенный
`StartManualTransferRunsRequest` в ходе cutover; `job_id` совпадает с зафиксированным в
`sq_cadence_2026-07-27.md §6`. Ручной прогон конфигурации в этой выдаче **виден и неотличим по
форме от суточного** ⇒ инструмент способен показать пересборку, инициированную не расписанием.

**Следствие для `Q-78`.** Второй путь `ADR-038` (`cf-finance.trigger_marts()` в конце ночного
прогона) дал бы вторую пересборку в сутки, то есть ≈30 заданий за окно вместо 16, той же формы.
Их 16. Гипотеза «пинок уходит, но замером не виден» **опровергнута положительным контролем**.
Остаётся одна: пинок не доходит. Причина настоящим артефактом не устанавливается —
различитель прежний, логи `cf-finance` за ночной слот.

## §5 Окно 14:30–15:00 UTC 2026-07-26 — остаток `Q-64`

| creation_time | user_email | statement_type | ds | tbl |
|---|---|---|---|---|
| `2026-07-26 14:46:58` | `ilyasbazarov4@gmail.com` | SELECT | `_12584c14be40fb92791e8b32a747ff27b3c7bae7` | `anonb65986…` |
| `2026-07-26 14:47:01` | `ilyasbazarov4@gmail.com` | SELECT | `_12584c14be40fb92791e8b32a747ff27b3c7bae7` | `anon9b9188…` |

Заданий в `14:41` нет. Оба соседних — анонимные `SELECT` во временные таблицы служебного
датасета, в `marts` не пишут. Наблюдение `Q-64` («`marts.expenses` в 14:41 UTC») инструментом,
видящим обоих принципалов, **не воспроизводится**. Причина исходной записи не назначается
(`_METHOD §6`): вопрос стоял о наличии пересборки вне слота, ответ — её не было.

## §6 Видимость чужих принципалов — снятие оговорки `ADR-074 §3`

Запрос без фильтра по датасету, окно с `2026-07-27T00:00:00Z`:

| user_email | заданий | first_seen | last_seen |
|---|---|---|---|
| `etl-sa@msklad-bi-prod.iam.gserviceaccount.com` | 760 | `2026-07-27 00:00:03` | `2026-07-28 05:05:14` |
| `ilyasbazarov4@gmail.com` | 102 | `2026-07-27 01:34:03` | `2026-07-28 05:34:04` |

`INFORMATION_SCHEMA.JOBS_BY_PROJECT` показывает задания сервисного аккаунта наравне со своими.
Оговорка `ADR-074 §3` снимается **наблюдением**, а не выводом из `ADR-045 §1`.
Замер `§3`/`§4` показал 319 из 319 заданий с целью в `marts` под аккаунтом владельца — это
факт о `marts`, не артефакт фильтрации: `etl-sa` в `marts` не пишет вовсе.

## §7 Транспортный инцидент — громкий отказ, класс `Q-68`

После скриптовых прогонов два запроса были повторены **вручную россыпью** в Cloud Shell и
вернули `ERROR: (bq) You do not currently have an active account selected` дважды подряд —
Cloud Shell потерял креды между вызовами. Ущерба нет: данные уже были получены скриптом.

Класс — `Q-68` (интерактивный Cloud Shell как ненадёжный транспорт), но с отличием в лучшую
сторону: отказ **громкий**, в отличие от тихой деградации до `401` в инциденте 2026-07-26
(`ADR-063 §2`). Скриптовый прогон с якорями `date -u` по краям отработал целиком и остался
достоверным; посыпались ровно те вызовы, что шли вне формы `ADR-055 §1`.
Отрицательный и положительный пример правила получены в одной сессии.

## Провенанс

Логи прогонов: `~/holika_jobs_20260728T052721Z.log` (Cloud Shell),
`~/holika_who_20260728T053435Z.log` (macOS); сырая выдача `~/marts_jobs.json` (319 записей).
