# FILE: guard_fix_deploy_mandate_2026-08-18_rev2.md

# Мандат класса B на `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY` — редакция 2 (исправлена база)

**Заменяет:** `reference/guard_fix_deploy_mandate_2026-08-18.md` **в части базы ветки деплоя и
двух зависящих от неё проверок**. Всё остальное того файла остаётся в силе дословно и здесь не
переписывается: объект по составу файлов, откат, запреты, приёмка.

**Основание правки:** `reference/guard_fix_deploy_base_adj_2026-08-18.md` (СТОП исполнителя признан
правильным; вариант ожидания основной ветки опровергнут циклом; дефект — в редакции 1 мандата).
**Подтверждение владельца:** чат 2026-08-18, «база подтверждена».

---

## §1. Что изменилось — ровно три пункта

| # | Было (редакция 1) | Стало (редакция 2) |
|---|---|---|
| 1 | Ветка деплоя заводится **от `master`** | Ветка деплоя заводится **от коммита `543b6c1`** (`deploy/cf-facts-2026-08-12-completeness-and-delete`) — того, чей код равен обслуживающей ревизии `cf-facts-00017-jon` |
| 2 | П3: архив обслуживающей ревизии сверяется **с `master`** | П3: архив сверяется **с базой ветки деплоя** (`543b6c1`); ожидание — побайтовое совпадение всех файлов |
| 3 | П4: `git diff --stat master` = ровно два файла | П4: `git diff --stat 543b6c1` = ровно два файла |

**Не изменилось ничем:** объект по составу (`cf-facts/bq_ops.py`, `cf-facts/main.py`), запрет
переноса четырёх файлов `fetch_*.py`, откат (трафик на `cf-facts-00017-jon`), приёмка из шести
пунктов, все прочие предусловия.

## §2. Добавлены две механические проверки (`05_CONVENTIONS §Процедура деплоя` п.9(iv), п.10)

Обе — предусловия, обе отказывают ДО облачных вызовов:

- **П6 — инвариант базы.** `git merge-base --is-ancestor 543b6c1 <HEAD ветки деплоя>` обязан
  вернуть истину. Ложь есть СТОП: ветка отменила бы то, что работает в проде. Печатать результат,
  не метку.
- **П7 — факт-проверка исполнением на СОДЕРЖИМОМ ВЕТКИ.** Импорт всех модулей `cf-facts` из
  каталога ветки деплоя (не из `reference/code/`): стабы внешних зависимостей в `sys.modules`,
  версия интерпретатора называется в логе. Ожидание — модулей с провалом импорта `0`. На
  редакции 1 эта проверка упала бы с `ImportError` на `GUARD_TOLERANCE_DAYS` и
  `PERIMETER_CHANNEL_NAMES` — то есть поймала бы дефект базы механически.

## §3. Что переигрывать не нужно

Предусловия **П1** (контракт `run_id` в обоих Cloud Workflow) и **П2** (обслуживающая ревизия
`cf-facts-00017-jon`, `percent=100`) закрыты фактами прошлым заходом
(`reference/sales_refresh_window_guard_fix_deploy_2026-08-18.md §2/§4`) и от базы не зависят.
Снять их заново дешевле, чем доказывать неизменность, поэтому П2 снимается повторно как часть
штатной процедуры; П1 переиспользуется по артефакту, повторный съём не обязателен.

Переигрываются: **П3** и **П4** (обе зависят от базы) плюс новые **П6** и **П7**.

## §4. Текст запуска исполнительской сессии (редакция 2)

Дерево и ветка: прежняя сессия остановилась чисто и её ветка `s/SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY`
уходит в сборку. Новая сессия берёт имя `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2`:

```
T=SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2
git worktree add "worktrees/$T" -b "s/$T" HEAD
```

> Ты — исполнитель проекта holika (`_METHOD.md §2`).
>
> Пройди обязательный старт сессии по `CLAUDE.md`.
>
> **Задача:** `SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY`, второй заход. **Класс задачи: B**, мандат
> ВЫДАН поимённо владельцем (чат 2026-08-18), база подтверждена владельцем повторно (чат
> 2026-08-18, после исправления). Брифа нет намеренно (`ADR-086 §1`).
>
> **Определение задачи:** `reference/guard_fix_deploy_mandate_2026-08-18_rev2.md` (эта редакция —
> она главная) плюс `reference/guard_fix_deploy_mandate_2026-08-18.md` в части, которую rev2 не
> переписывает. Приёмку процитируй в session-блоке дословно.
>
> **Первый заход остановился на предусловии П3 и был прав** — прочитай
> `reference/sales_refresh_window_guard_fix_deploy_2026-08-18.md` (что уже проверено) и
> `reference/guard_fix_deploy_base_adj_2026-08-18.md` (почему база была неверной). Не повторяй
> закрытые предусловия без нужды, но П3/П4 переигрываются от НОВОЙ базы.
>
> **База ветки деплоя — коммит `543b6c1`, не `master`.** Это главное отличие от первого захода.
>
> **Context-to-load:** `reference/guard_fix_deploy_mandate_2026-08-18_rev2.md`;
> `reference/guard_fix_deploy_mandate_2026-08-18.md`; `reference/guard_fix_deploy_base_adj_2026-08-18.md`;
> `reference/sales_refresh_window_guard_fix_deploy_2026-08-18.md`;
> `reference/guard_fix_prep2_review_2026-08-18.md`; `05_CONVENTIONS §Процедура деплоя Cloud Function`
> (десять элементов); `reference/code/cf-facts/MANIFEST.md`; `07_STATE §Мандат Claude Code`.
>
> **Порядок предусловий:** П2 (обслуживающая ревизия) → П6 (инвариант базы) → П3 (дрейф против
> базы) → П4 (ровно два файла против базы, `.gcloudignore`, секреты) → П7 (импорт модулей ветки)
> → П5 (объявление действия отдельным сообщением) → деплой. **Любой отказ — СТОП и доклад, не
> обход.**
>
> **Код не правишь ни одной строкой** — патч заморожен (`ADR-188 §1`). Нашёл дефект — стоп.
>
> **Ручную проверку `promote` не делай.** Ступень 3 закрывается прогоном `2026-08-23`, не этой
> сессией.
>
> Работаешь в своём дереве, коммитишь в свою ветку `s/SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2`.
> `07_STATE.md`, `06_DECISIONS_LOG.md`, `06_INDEX.md` не правишь; session-блок — одним новым файлом
> `reference/_inbox/session_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2_2026-08-18.md`.
>
> **Пишет** (`ADR-102 §1`): `reference/sales_refresh_window_guard_fix_deploy_2_2026-08-18.md`,
> `reference/_scratch_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2_2026-08-18/`,
> `reference/code/cf-facts/MANIFEST.md`, `reference/_inbox/session_SALES-REFRESH-WINDOW-GUARD-FIX-DEPLOY-2_2026-08-18.md`.
>
> Один скрипт на ШАГ; доставка файлом с логом в файл; `date -u` и `gcloud auth list` первой И
> последней командой каждого скрипта; диагностика и действие не в одном скрипте. При обрыве деплоя
> слепой retry запрещён. `Done` — только по подтверждённому логу. При неуспехе ветка код-репо в
> `master` НЕ сливается, откат — трафик на `cf-facts-00017-jon`, попытка пишется в `MANIFEST.md`.

**Параллель: НЕТ** — класс B.
