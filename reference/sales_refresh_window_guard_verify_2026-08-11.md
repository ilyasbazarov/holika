# FILE: sales_refresh_window_guard_verify_2026-08-11.md

# Независимая проверка предохранителя `SALES-REFRESH-WINDOW`: результат — ПРОЙДЕНА

**Задача:** `SALES-REFRESH-WINDOW-MANDATE-ADJ`, третий ход (архитектор, класс A).
**Дата:** 2026-08-11. **Предмет проверки:** коммиты `ac7afba` (три правки по адъюдикации) и
`8325cfc` (допуск на верхнем крае) ветки `s/SALES-REFRESH-WINDOW`.
**Основание проверки:** `reference/sales_refresh_window_mandate_adj_2026-08-11.md §3` —
правку вносил тот, кто её проектировал, поэтому проверяет другой (`ADR-146 §4`, тот же протокол).
**Провенанс:** `reference/_scratch_SALES-REFRESH-WINDOW-MANDATE-ADJ_2026-08-11/step2_guard_dryrun_check.sh`
и `step2_run.log`, якоря `2026-08-11T06:47:02Z`…`2026-08-11T06:47:11Z`, принципал один и тот же.

---

## §1 Семь пунктов протокола — по коду, не по описанию

| # | Что проверялось | Результат |
|---|---|---|
| 1 | Константа `GUARD_TOLERANCE_DAYS = 3` объявлена в `config.py`, импортирована (`bq_ops.py:34`) и участвует в условии отказа, а не лежит рядом | ✔ |
| 2 | Направление сравнения нижнего края: отказ при недостаточной ГЛУБИНЕ (`min_date > window_start + допуск`), допуск ослабляет границу, а не ужесточает | ✔ |
| 3 | Верхний край даёт отказ, а не запись в журнал (`raise ValueError`) | ✔ |
| 4 | Питоновских часов в предохранителе не осталось: `min`, `max` и обе границы окна приходят ОДНИМ запросом BigQuery | ✔ |
| 5 | Оба вызова безусловны и стоят ДО построения текста `MERGE` — `bq_ops.py:502` (продажи), `:709` (периметр) | ✔ |
| 6 | Тексты обоих отказов печатают конкретные даты; успешный журнал печатает оба края и величину допуска — пункт приёмки мандата про строку в журнале выполним | ✔ |
| 7 | Допуск на верхнем крае применён (правка `8325cfc`), направление верное: `max_date < freshness_floor − допуск` | ✔ |

**Замечание, не влияющее на вердикт.** `date.today()` остался в `get_coverage_stats`
(`bq_ops.py:737`) — это диагностическая функция, её собственный докстринг говорит «logged but NOT
used as a gate»; на окно `MERGE` она не влияет. Правки не требует, названо для полноты.

## §2 Холостая проверка нового текста запроса против живых таблиц

Текст запроса переписан целиком (четыре поля вместо одного), поэтому прежняя валидация к нему не
переносится. Проверены три сочетания, все `Query successfully validated`:

| Таблица | `window_days` | Результат |
|---|---|---|
| `stg_msklad.fact_sales_staging` | `7` | validated (`9 825` байт) |
| `stg_msklad.fact_sales_staging` | `90` | validated (`9 825` байт) |
| `stg_msklad.fact_sales_perimeter_staging` | `90` | validated (`139 450` байт) |

## §3 Поведение на живых данных — оба края сработали, каждый на своём сценарии

Реплика предиката (read-only) на фактическом состоянии обеих staging, `2026-08-11`:

| Таблица | Покрытие staging | Порог глубины (окно 90 + допуск) | Порог свежести (вчера − допуск) | Вердикт глубины | Вердикт свежести |
|---|---|---|---|---|---|
| продажи (часовой прогон) | `2026-08-04` … `2026-08-11` | `2026-05-16` | `2026-08-07` | **ОТКАЗ** | ok |
| периметр | `2026-05-04` … `2026-08-03` | `2026-05-16` | `2026-08-07` | ok | **ОТКАЗ** |

Это ровно два сценария, ради которых предохранитель писался, и каждый пойман своим краем:

- **Глубина.** Часовая staging при `window_days=90` — тот самый вызов, что снёс бы порядка 83 суток
  продаж. Отказ.
- **Свежесть.** Staging периметра отстаёт на восемь суток (`03.08` против `11.08`) — состояние
  живое, не выдуманное. Допуск в трое суток его не проглатывает: отказ. То есть допуск ослабил
  проверку ровно настолько, чтобы снять ложные отказы на пустых сутках, и не настолько, чтобы
  пропустить реальное отставание.

**Вердикт: проверка пройдена, предохранитель готов к деплою.** Последний невыполненный пункт
перед мандатом (`mandate_adj §5` п. 2) закрыт.

## §4 Что этой проверкой НЕ делалось

- Функция не исполнялась: `--dry_run` и реплика предиката отдельным запросом, ни `MERGE`, ни
  `promote`, ни деплоя.
- Поведение при пустой staging не проверялось отдельно — оно закрыто более ранней проверкой
  непустоты (`bq_ops.py:497`, `:704`), которая стоит ДО предохранителя и уже даёт отказ.
- Прочий код `cf-facts` не пересматривался: предмет проверки — два коммита предохранителя.
