# FILE: sales_refresh_window_deploy_2026-08-11.md

# Деплой `SALES-REFRESH-WINDOW` (узкая форма) — исполнен и ОТКАЧЕН тем же днём

**Задача:** `SALES-REFRESH-WINDOW-DEPLOY` (бриф `briefs/SALES-REFRESH-WINDOW-DEPLOY.md`).
**Мандат:** `ADR-159`/`ADR-158` — исполнен частично (шаги 1-6), затем ПРИОСТАНОВЛЕН
(`reference/sales_refresh_window_rollback_adj_2026-08-11.md`, ветка
`s/SALES-REFRESH-WINDOW-ROLLBACK-ADJ`, коммит `ed3f80c`) до исполнения шага 8.
**Дата:** 2026-08-11 (UTC-якоря в каждом скрипте).
**Провенанс:** `reference/_scratch_SALES-REFRESH-WINDOW-DEPLOY_2026-08-11/`.

---

## §1 Деплой (шаги 1-6 брифа) — исполнен полностью и штатно

1. **Сверка дрейфа** (`step1_run.log`, `08:10:20Z…08:10:46Z`): живая ревизия `cf-facts-00011-mab`
   побайтово совпала с `master` код-репо `holika-prod` (все 10 файлов, sha256 идентичны). Дрейфа нет.
2. **Ветка** `deploy/cf-facts-2026-08-11-refresh-window`, патч перенесён **по diff** (`bq_ops.diff`
   194 строки, `config.diff` 24 строки) через `patch -p0`, не копированием файлов. sha256 после
   патча совпал с целевым снапшотом `reference/code/cf-facts/{bq_ops.py,config.py}`.
3. **Гигиена:** `.gcloudignore` чист (исключает `*.bak`, `__pycache__/`, `patch_*.py`). Сплошной
   поиск секретов по обоим `.diff` — 0 совпадений (пустая выдача не есть факт отсутствия).
4. **Снимок ДО деплоя** (`step4_run.log`, `08:33:36Z…08:33:57Z`):
   `core.fact_sales_profit_snap_20260811` — `42 975` строк всего; май-2026: `3 411` строк /
   `96 844 445,61` KGS; июль-2026: `4 707` строк / `112 502 581,88` KGS. **Снимок НЕ удалён**
   (условие рабочего отката) и сохраняется.
5. **Коммит+push+деплой:** коммит `47b3a45` в ветку, push подтверждён владельцем в чате, деплой
   (`step5_run.log`, завершён `08:38:15Z`) дал новую ревизию **`cf-facts-00012-ber`**,
   `generation 1786437392633949`, `--source` указывал на каталог `cf-facts/` выкаченной ветки.
6. **Read-back** (`step6_run.log`, `08:38:39Z…08:38:52Z`): архив несёт 11 файлов, мусора нет
   (`.gcloudignore` в архив не попал). sha256 всех 11 файлов побайтово совпал с веткой деплоя.
   Секция записана в `reference/code/cf-facts/MANIFEST.md`.
7. **`window_days` первого прогона вычислен:** `105` (формула мандата, дата прогона `2026-08-11`
   UTC: `(102 суток от 2026-05-01) + 3`).

## §2 Остановка на шаге 8 — структурный гэп, найденный ДО исполнения

Перед не-идемпотентным прогоном перечитан `main.py`. Обнаружено: `mode=weekly` (`_run_weekly_load`,
`main.py:166-208`) НЕ принимает `window_days` — окно выборки жёстко `date.today() -
WEEKLY_WINDOW_DAYS` (`90`), параметр из тела запроса игнорируется; то же у `mode=hourly`. Реально
управляемы `window_days` только `promote`, `perimeter_promote`, `returns`, `perimeter`. Live-проверка
`stg_msklad.fact_sales_staging` (read-only) на момент остановки: покрытие `2026-08-04…2026-08-11`
(часовой срез). Вызов `mode=weekly` сегодня выбрал бы `2026-05-13…2026-08-11` — на `12` суток короче
требуемого покрытия `2026-05-01`. Задача (замкнуть май в паритет) этим прогоном не достигалась бы, а
предохранитель на шаге `promote(window_days=105)` корректно отказал бы по глубине (безопасно, но
бесполезно). Прогон НЕ запускался. Владельцу передан вопрос по форме `ADR-056 §4`.

## §3 Второй, более тяжёлый дефект — найден архитектором по итогам остановки

`reference/sales_refresh_window_rollback_adj_2026-08-11.md` (коммит `ed3f80c`, ветка
`s/SALES-REFRESH-WINDOW-ROLLBACK-ADJ`) подтвердил гэп `§2` и нашёл независимый, более опасный: ветка
удаления обоих `MERGE` (`bq_ops.py:379` продажи, `:647` периметр) фильтрует только по дате, а
`core.fact_sales_profit` пишут ДВА независимых загрузчика (обычные продажи и периметр), у каждого
своя staging. Слияние периметра посчитало бы «исчезнувшими» все оптовые строки (`6 922` строк,
`269 440 872,10` KGS), слияние продаж — все строки периметра (`4 024`+`1 441`+`6` строк,
`17 056 819,29` KGS суммарно). Срабатывание — недельный конвейер, воскресенье `2026-08-16 01:00 UTC`.
Предохранитель `ADR-145 §4` слеп к этому классу дефекта: он меряет ПОКРЫТИЕ дат, не ПРОИСХОЖДЕНИЕ
строки. Ущерба на момент находки НЕ было — доказано сопоставлением снимка `_snap_20260811` с живой
таблицей по каналам, разность `0` по всем четырём каналам (`snapshot_diff_run.log` архитектора).

## §4 Приостановка мандата и откат (эта сессия, `ADR-159` дополнение)

Мандат `ADR-159` приостановлен архитектором; шаги 7-12 брифа `SALES-REFRESH-WINDOW-DEPLOY` НЕ
исполняются ни в каком виде. Разрешено ровно одно действие: вернуть обслуживание `cf-facts` на
ревизию `cf-facts-00011-mab`.

**Исполнено** (объявлено отдельным сообщением, подтверждено владельцем в чате):
`gcloud run services update-traffic cf-facts --to-revisions=cf-facts-00011-mab=100
--region=asia-east1 --project=msklad-bi-prod` (`step_rollback_traffic_run.log`,
`09:16:13Z…09:16:35Z`). Способ — перевод трафика (сервис Cloud Run/gen2 CF), пересборка не
требовалась, ревизия `cf-facts-00011-mab` существовала (`READY`) с `2026-08-09T11:00:06Z`.

**Приёмка отката (три пункта, read-back):**

| # | Проверка | Результат |
|---|---|---|
| (i) | `gcloud run services describe cf-facts` → трафик | `cf-facts-00011-mab`, `100%` (`rollback_accept1_describe.json`) |
| (ii) | Сопоставление снимка `_snap_20260811` с живой таблицей, разрез по каналу, окно 90д | `0` строк / `0,00` KGS по всем четырём каналам («(NULL)», «Комиссия», «Оптовая торговля», «Розница») — `rollback_accept2_diff.log` |
| (iii) | То же сопоставление ПОСЛЕ первого часового `promote` на откаченной ревизии | `0` строк / `0,00` KGS по всем четырём каналам — `rollback_accept3_diff.log`. Прогон естественный (расписание `0 * * * *`, execution `0be2309e-a6ed-409c-b64e-37cb51f69fda`, `10:00:02Z…10:06:2xZ`, `SUCCEEDED`), НЕ вызывался вручную. Независимо подтверждено логами Cloud Run: запросы `10:00-10:03 UTC` обслужены ревизией `cf-facts-00011-mab`, не `00012-ber` (`gcloud logging read`, `resource.labels.revision_name`) |

**Вердикт отката: ПОДТВЕРЖДЁН.** Все три числа приёмки — нули; откат восстановил прежнее поведение
(положительный контроль п.iii). Обслуживание `cf-facts` работает на `cf-facts-00011-mab` штатно.

**Что НЕ делалось:** снимок `_snap_20260811` не удалён; ветка деплоя `deploy/cf-facts-2026-08-11-refresh-window` в код-репо не удалена (provenance); расписания не тронуты; код не правился; `promote` с расширенным окном не запускался.

## §5 Дальнейшая форма фикса — вне scope этой сессии

Три варианта фикса дефекта `§3` предложены архитектором (`rollback_adj §6`), рекомендация —
вариант 1 (ветки удаления режутся по каналу цели). Патч уже подготовлен (`s/SALES-REFRESH-WINDOW-ROLLBACK-ADJ`,
коммит `a336700`) — его деплой НЕ входит в эту сессию: требует независимой проверки другой сессией и
нового мандата.
