# FILE: sales_refresh_window_delete_stage3_2026-08-18.md

# Ступень 3 различителя: пара маркетплейса подтверждена — гипотеза (b)

**Сессия:** `SALES-REFRESH-WINDOW-DELETE-STAGE3` (класс B, мандат ВЫДАН поимённо владельцем,
чат 2026-08-18). **Дата:** 2026-08-18 (Бишкек) · **База:** `29ce5f2`.
**Мандат/форма:** `reference/dq_deploy_accept_adj_2026-08-18.md §10`.
**Постановка:** `reference/sales_refresh_window_delete_adj_2026-08-12.md §4/§4a`.
**Предыдущий шаг остатка:** `reference/sales_refresh_window_delete_discriminate_2026-08-18.md §5`.
**Провенанс:** `reference/_scratch_SALES-REFRESH-WINDOW-DELETE-STAGE3_2026-08-18/step1_probe.py`,
`run.log`, `step1_demands_raw.json`, `step2_positions_4ec09bec-4df8-11f1-0a80-1c8a0053cab5.json`.

---

## §1 Что проверялось

Пара из остатка ступени 2 (`13` групп дата+контрагент, `29` строк, `2 407 220,42` KGS):
дата `2026-05-12`, `agent_id = 31d135bc-4df8-11f1-0a80-1c8a0053c5b4`, `1 980 000,00` KGS —
три четверти денежной величины остатка. Единичный `GET` к `api.moysklad.ru`, только чтение.

Два живых вызова:
1. `entity/demand`, фильтр `moment>=2026-05-12 00:00:00;moment<=2026-05-12 23:59:59` — список
   документов отгрузки за день (без фильтра по контрагенту — синтаксис href-фильтра в репо не
   документирован, отбор по `agent_id` сделан локально после получения списка).
2. `entity/demand/{id}/positions` для найденного документа, совпавшего по `agent_id`.

## §2 Замер

| Величина | Значение |
|---|---|
| Документов `entity/demand` за 2026-05-12 (всего) | `2` |
| Документов с `agent_id=31d135bc…` | `1` (`id=4ec09bec-4df8-11f1-0a80-1c8a0053cab5`, `name=04235`, `moment=2026-05-12 14:44:00.000`) |
| Позиций документа | `2` |
| Сумма позиций (формула `price/100 × rate.value × quantity × (1 − discount/100)`, `revenue_kgs`) | `1 980 000,00` KGS |
| Ожидаемая сумма остатка (ступень 2) | `1 980 000,00` KGS |
| Совпадение (допуск `0,01` KGS) | **да** |

Формула `revenue_kgs` — снапшот `reference/code/cf-facts/fetch_demands.py:170-174`, не изобретена.

## §3 Вывод — бинарный, только по измеренной паре

Документ существует в источнике на стороне МойСклада и содержит ровно те же позиции и ту же
сумму, что были удалены боевым прогоном `SALES-REFRESH-WINDOW-DEPLOY`. **Гипотеза (b) подтверждена
для этой пары: удаление было дефектом выгрузки/загрузки, не поведением источника.**

**Вердикт НЕ распространяется на остальные `10` строк остатка** (`ADR-165`: критерий, не
проверенный на размеченном наборе обеих категорий, вердиктом не является). Эта пара была измерена
единственная — прямым чтением, не выводом по аналогии.

## §4 Что этой работой не делалось

Ни одной записи ни в МойСклад, ни в BigQuery. Ни одного вызова сверх заявленных двух. Секрет
`msklad-token` не печатался; проверка `grep -rF` по значению токена во всех файлах сессии перед
`git add` — чисто, совпадений нет.

## §5 Открытый вопрос для владельца

Пара маркетплейса подтверждена как дефект выгрузки (гипотеза b). Остаток строки — `10` строк
дешевле проверить классом A (сопоставление множеств `transaction_id` архива и текущего ядра,
`reference/sales_refresh_window_delete_discriminate_2026-08-18.md §5`, вариант 1) или ещё живыми
`GET` (вариант 2, класс B, мандат отдельно на каждую пару). Выбор формы закрытия остатка — решение
владельца/архитектора, не этой сессии (объём мандата настоящей сессии — ровно эта пара).
