# /reference/parity_registry.md — Реестр паритета (страница/отчёт МойСклад ↔ витрина/страница Looker Studio)

**Задача:** `PARITY-REGISTRY`, discovery-часть (класс B, мандат `ADR-076 §3a`).
**Основание:** `ADR-067 §5a/§5b`, `ADR-068 §3`, `ADR-069 §1/§4`.
**Что покрывает этот файл:** инвентарь пар из обоих сидов (`01_ARCHITECTURE.md §потребители` таблица PR-39; `reference/client_concerns_2026-07.md` треки A/Б), правило-мост там, где оно выводится из уже принятых ADR/SQL репо, endpoint report-API (Шаг 2, детали — `reference/parity_report_api_2026-07-28.md`), покрытие-с-даты, статус сходимости по существующим артефактам.
**Чего этот файл НЕ делает** (`ADR-076 §4`): не адъюдицирует НОВЫЕ правила моста. Где правило не выводится напрямую из принятого ADR/SQL — строка помечена «требуется», не заполнена предположением.
**Мост** — только уровень (b) паритета (`ADR-068 §1`): совпадение итогов/строк через опубликованное правило, не пиксельное совпадение страниц.
**DERIVED вне паритета** (`ADR-067 §4`): `total_sum_usd`/`revenue_usd`/`cogs_usd`/`margin_usd`/`gmroi`/ABC-XYZ и прочие производные от `dim_fx_rates`-конвертации в реестр НЕ включаются как объект сверки; сверка — только на KGS-величинах до производных.
**Якорь глубины паритета:** `2026-05-01` (`ADR-069 §2`) — используется во всех строках, где иное не обосновано фактом репо.

---

## Таблица пар (Шаг 3)

| Поверхность МойСклад (страница/отчёт) | Источник эталона (i/ii/iii, `ADR-067 §2`) + endpoint | Поверхность BI (витрина + страница LS) | Правило-мост (`ADR-068 §3`) | Покрытие с даты (`ADR-069`) | Статус сходимости + артефакт |
|---|---|---|---|---|---|
| «Прибыли и убытки» (Отчёты → П&Л), раздел «Расходы» | (ii) подтверждён: `entity/paymentout`, `entity/cashout`, `entity/loss` (конвертация по курсу документа, `ADR-010`), `entity/commissionreportin` (`reward÷100 × rate` + `commissionOverhead÷100 × rate`, `ADR-026`); (iii) контрольный снимок ручной выгрузки UI — `reference/pnl_2026-05.md`. Report-API прямого П&Л-эндпоинта не документирует (проверено `report/*`, нет пути) | `marts.expenses` → страница LS «Расходы» (`msklad_expenses`) | **Выведено (ADR-006 §Решение + ADR-013 + ADR-002-исключение):** сумма `paymentout+cashout+loss` (курс документа) минус статьи-исключения (`Перемещение исходящий`, `Перемещение`, `Закупка товаров`, `Возврат` — легитимно исключены из ETL, `ADR-002`/`ADR-005 п.1`), плюс `entity/commissionreportin` (слито с категорией «Расходы маркетплейсов», `ADR-006 §Решение(2)`), плюс «Налоги и сборы» — НАЧИСЛЕНИЕ из П&Л, отдельный источник, вне cash-сверки (`ADR-013`). «Вывод прибыли» — капитал, вне 26 категорий (`ADR-013`). Клиент воспроизводит вручную: открывает «Прибыли и убытки» за период, читает 26 строк расходов по списку `reference/expense_articles_client.md` (регистр статей на дашборде — canonical формы «Топливо»/«Неразнесенное списание», verbatim-список клиента содержит регистр-миссмэтч, исправленный `E1-T1-MECH`, `expense_articles_client.md` сноска) | `2026-05-01` (по умолчанию, `ADR-069 §2`) | **`0,00` (артефакт: `reference/fx_policy_expenses_2026-07-28.md` §4.1, эталон `reference/pnl_2026-05.md`; закрытие Q-8 — `ADR-006`; cutover-подтверждение — `ADR-033`)** |
| «Прибыльность по товарам» (`report/profit/byproduct`) / «Прибыльность по контрагентам» (`report/profit/bycounterparty`) | (i) подтверждён живым `GET`, `200`, май-2026 (`reference/parity_report_api_2026-07-28.md`) | `marts.sales_overview` → страница LS «Инвестор» (`msklad_sales_overview`) | **Выведено (`ADR-085 §1`).** Объект паритета = `core.fact_sales_profit.revenue_kgs`, ВАЛОВАЯ выручка (`Q-83`: тождественна `sell_sum_kgs`, возвраты в этой таблице хардкод `0.0`), сравнивается с `report/profit/byproduct`/`bycounterparty`. Нетто-выручка объектом паритета НЕ является: производная по формуле «валовая минус возвраты того же периода и того же зерна», сходится автоматически при сходимости этой пары и пары «Возвраты покупателей». `cogs_kgs`/`margin_kgs` в первую волну паритета не входят, вопрос конвертации `byvariant` снимается шагом `REPORT-FIELDS` (`ADR-085 §7`). **Правило суток (`ADR-088 §3`, задача `PARITY-CHECK-SALES-RETURNS`):** обе стороны сравниваются в зоне API (той же, что несёт `moment`, подтверждено `Q-93`); `report/profit/*` принимает `momentFrom`/`momentTo` включительно по обеим границам в этой же зоне; `core.fact_sales_profit.transaction_date` хранит `DATE(M+6ч)` (Бишкек) — ночная полоса `[18:00;21:00)` UTC, где эти два календаря расходятся, пуста (0 из 127 документов `entity/demand` мая-2026, `step3e_step5b_corrected.log`) **Объект паритета продаж — вариант (a) (`ADR-103 §8`, владелец, 2026-08-02):** объектом объявляется само число `report/profit/*`; любое расхождение нашей стороны с ним есть дефект нашей стороны, не повод сузить периметр. Приведение нашей величины после патча `SALES-INGEST-PATCH` (текст готов, не задеплоен, `reference/sales_ingest_patch_2026-08-03.md`): `core.fact_sales_profit.revenue_kgs` месяца обязан равняться Σ`entity/demand.revenue_kgs` (без строк-сирот) + Σ`entity/retaildemand.revenue_kgs` + Σ`entity/commissionreportin.revenue_kgs` (по `sum` документа, не `commitentSum`, `ADR-104 §4`) − отгрузки комиссионерам (исключены на приёме, `ADR-103 §7`, список контрагентов — `config.py::COMMISSION_CHANNEL_AGENT_IDS`). Строки-сироты (`ADR-100 §1`) этим патчем не устраняются — адресованы отдельно `SALES-REFRESH-WINDOW`; ожидаемый временный сдвиг расхождения — `reference/sales_ingest_patch_2026-08-03.md §5` (риск `R1`, `ADR-101`). | `2026-05-01` (по умолчанию) | **Расхождение 288 789,73 KGS (0,31%), причина НЕ установлена этой сессией — гэп, не закрыто.** `core.fact_sales_profit.revenue_kgs` (май-2026) = 93 522 995,53; `report/profit/byproduct.sellSum` (та же выборка, минорные единицы ÷100) = 93 234 205,80; `bycounterparty.sellSum` побайтово совпадает с `byproduct` (внутренний контроль согласованности report-API). Ночная полоса пуста → итог «без полос» идентичен общему. Проверена и НЕ подтвердилась гипотеза «забыта конвертация мультивалютных документов»: в мае-2026 у `entity/demand` три валюты (KGS/USD/RUB, `step5c_currency_check.log`), но пересчёт суммы по курсу документа (`rate.value`, минорные единицы × курс, `ADR-010`) даёт ТРЕТЬЕ число — 91 892 755,66 KGS (`step5d_currency_breakdown.log`), не совпадающее ни с `core`, ни с `report`. Артефакты: `reference/_scratch_PARITY-CHECK-SALES-RETURNS_2026-08-01/step5_sales_reconciliation.log`, `step5c_currency_check.log`, `step5d_currency_breakdown.log` **Патч подготовлен, не задеплоен (`SALES-INGEST-PATCH`, 2026-08-03):** после деплоя и досева мая ожидаемое расхождение — порядка `1 630 240` KGS (`≈1,75 %`), временно ХУЖЕ записанного — это ожидаемое поведение (риск `R1`, `ADR-101`), не регресс; расчёт — `reference/sales_ingest_patch_2026-08-03.md §5`. **ЗАКРЫТА ПРАВИЛОМ (`PARITY-SALES-CLOSE`, 2026-08-04; основание `ADR-116 §1` — решение владельца, вариант 1, поверх `ADR-067`/`ADR-068 §1`).** Разность `288 789,73` KGS разложена без свободного остатка на два механизма с установленной причиной в коде нашей стороны: **M1 — строки-сироты** `+1 630 240,60` KGS (7 позиций, `MERGE` без ветки удаления, `bq_ops.py:237-305`, `ADR-100 §1`), завышение; **M2 — периметр типов документов** `−1 341 450,08` KGS (розница `+1 188 422,00` / 506 док. и продажи по отчётам комиссионера `+2 133 028,08` / 7 док. — есть у эталона, нет у нас; отгрузка комиссионеру `−1 980 000,00` / документ `04235` — есть у нас, нет у эталона; `ADR-103`, `ADR-104`), занижение. Замыкание: `1 630 240,60 − 1 341 450,08 = 288 790,52`, свободный остаток `ε = −0,79` KGS (`−0,73` при сумме комиссионного канала по остатку вместо измерения; расхождение `0,06` — известные округления, `reference/sales_perimeter_confirm_2026-08-02.md:49`), то есть `0,0000008` месячной величины. **Критерий сходимости пары — инвариант разложения `Δ = M1 − |M2| + ε` при `|ε| < 1` KGS, а не порог на `Δ`:** пара остаётся закрытой, пока каждый сом отнесён к названному механизму, и переоткрывается только при появлении СВОБОДНОГО остатка. Следствие, обязательное к прочтению перед деплоем: после `SALES-INGEST-PATCH` (`M2 → 0`) `Δ` временно ВЫРАСТЕТ до `+1 630 239,81` KGS (`≈1,7485 %`) — инвариант при этом держится, пара закрытой быть не перестаёт, рост есть ожидаемое поведение (риск `R1`, `ADR-101`), не регресс и не основание для отката; после целевого варианта 1 `SALES-REFRESH-WINDOW` (`M1 → 0`) `Δ → −0,79`. Адреса устранения: M2 — `SALES-INGEST-PATCH` (текст готов, не задеплоен, деплой класса B); M1 — конвенция `C2` вперёд и вариант 1 `SALES-REFRESH-WINDOW` (не построен). Закрытие относится к ЗОНЕ ПАРИТЕТА (май-2026, датированный снимок, `ADR-115 §11`), не к текущим данным. Артефакт: `reference/parity_sales_close_2026-08-04.md`. **ПЕРЕСВЕРЕНО ПОСЛЕ ДЕПЛОЯ (`SALES-PERIMETER-PARITY-RECHECK`, 2026-08-10).** Измеренная (не предсказанная) `Δ = +3 610 239,81` KGS (`3,8722 %`): `core.revenue_kgs`(май, после досева окна `2026-05-01…2026-05-08`) `= 96 844 445,61` (`3411` строк) против эталона `report/profit/byproduct.sellSum`(май) `= 93 234 205,80` (не изменился с 2026-08-04). Разложение: **`M1`** (строки-сироты, пересчитано тем же методом) `= +1 630 239,81` KGS, 7 строк; **новый компонент** — документ `04235` (отгрузка комиссионеру «ООО «РВБ»», 2026-05-12) `= +1 980 000,00` KGS, НЕ удалён из `core` тем же дефектом `MERGE` без ветки `DELETE` (`ADR-100 §1`), что держит `M1` — код-исключение по `COMMISSION_CHANNEL_AGENT_IDS` в проде, но ретроактивно не применяется. `Δ = M1 + остаток + ε`: `1 630 239,81 + 1 980 000,00 = 3 610 239,81`, `ε = 0,00` KGS. **Расходится с прогнозом** `sales_ingest_patch_2026-08-03.md §5` (`≈+1 630 239,81`, `≈1,7485 %`) на ровно `+1 980 000,00` KGS — прогноз предполагал устранение ВСЕХ трёх подкомпонентов `M2`, факт — двух из трёх (розница и комиссионные отчёты добавлены полностью, подтверждено); третий не устранён. **Форма записанного инварианта (`Δ = M1 − |M2| + ε`) требует пересмотра архитектором** — измеренный остаток теперь ОДНОГО знака с `M1` (переоценка), а не противовес; статус пары (сошлась/переоткрыта) этой сессией не назначается (`ADR-133 §4`, `ADR-076 §4`). Полное разложение, провенанс живых вызовов — `reference/sales_perimeter_parity_recheck_2026-08-10.md`. |
| «Прибыльность по товарам» / «по контрагентам» (тот же отчёт, что выше) | (i) подтверждён (см. выше) | `marts.sales_overview` → страница LS «Операционка» (`msklad_sales_overview`, один из трёх источников) | **Выведено (`ADR-085 §1`).** Объект паритета = `core.fact_sales_profit.revenue_kgs`, ВАЛОВАЯ выручка (`Q-83`: тождественна `sell_sum_kgs`, возвраты в этой таблице хардкод `0.0`), сравнивается с `report/profit/byproduct`/`bycounterparty`. Нетто-выручка объектом паритета НЕ является: производная по формуле «валовая минус возвраты того же периода и того же зерна», сходится автоматически при сходимости этой пары и пары «Возвраты покупателей». `cogs_kgs`/`margin_kgs` в первую волну паритета не входят, вопрос конвертации `byvariant` снимается шагом `REPORT-FIELDS` (`ADR-085 §7`). Правило суток — идентично строке выше (тот же объект паритета, тот же замер, другая страница LS-потребителя) **Объект паритета — тот же вариант (a) (`ADR-103 §8`), приведение величины после патча `SALES-INGEST-PATCH` — идентично строке выше** (одна и та же величина `core.fact_sales_profit.revenue_kgs`, две страницы-потребителя). | `2026-05-01` (по умолчанию) | **Тот же результат, что строкой выше** (один и тот же объект паритета `revenue_kgs`, одна сверка на обе страницы LS): расхождение 288 789,73 KGS (0,31%), причина не установлена, гэп не закрыт. Артефакты — см. строку выше **Патч подготовлен, не задеплоен (`SALES-INGEST-PATCH`, 2026-08-03):** ожидаемый сдвиг после деплоя — см. строку выше. **ЗАКРЫТА ПРАВИЛОМ (`PARITY-SALES-CLOSE`, 2026-08-04) — то же правило и то же разложение, что строкой выше** (одна и та же величина `core.fact_sales_profit.revenue_kgs`, две страницы-потребителя; отдельного замера у этой строки нет и не было). Критерий сходимости — инвариант разложения `Δ = M1 − |M2| + ε` при `|ε| < 1` KGS; временный рост `Δ` до `≈1,7485 %` после деплоя `SALES-INGEST-PATCH` регрессом не является. Полный текст правила, величины механизмов и адреса устранения — строка выше и `reference/parity_sales_close_2026-08-04.md`. **ПЕРЕСВЕРЕНО ПОСЛЕ ДЕПЛОЯ (`SALES-PERIMETER-PARITY-RECHECK`, 2026-08-10) — тот же результат, что строкой выше** (одна и та же величина `core.fact_sales_profit.revenue_kgs`, одна сверка на обе страницы LS): измеренная `Δ = +3 610 239,81` KGS (`3,8722 %`), расходится с прогнозом `≈1 630 239,81` на `+1 980 000,00` KGS — документ `04235` не удалён из `core` тем же дефектом `MERGE` без ветки `DELETE` (`ADR-100 §1`), что держит `M1`; форма инварианта требует пересмотра архитектором. Полное разложение — строка выше и `reference/sales_perimeter_parity_recheck_2026-08-10.md`. |
| Возвраты покупателей (документы возврата, разрез по контрагенту) | (i) `report/profit/bycounterparty.returnSum` — колонка внутри отчёта о прибыльности, тот же движок, что рисует интерфейс; за май-2026 = 57 000 минорных единиц ÷100 = `570,00` KGS при `returnCount` = 8 (`reference/_scratch_PARITY-CHECK-SALES-RETURNS_2026-08-01/bycounterparty_may_returnsum.txt`, `bycounterparty_may_page_0.json`). Контрольный второй путь — (ii) агрегация ОБОИХ entity-типов того же API, `entity/salesreturn` + `entity/retailsalesreturn` (ровно то, что читает `fetch_returns.py:51`): за май первый пуст, второй даёт 8 документов на те же `570,00` (`step3b_returns_sanity.log`, `retail_returns_may.json`). `report/profit/bysalesreturn` = `404` есть факт о ЕДИНИЧНОМ эндпоинте, а не о недоступности уровня (i) для возвратов | `core.fact_returns` → `msklad_counterparty_returns` (страница LS «Операционка») и вычитаемое в `net_revenue_kgs` на странице LS «Инвестор» | **Выведено (`ADR-085 §1`):** объект паритета = сумма возвратов ЗА ПЕРИОД и В ЗЕРНЕ строки. Действующий Custom Query этому не соответствует: скользящее окно 365 дней от `CURRENT_DATE()` плюс агрегация только по `agent_id` при внешней группировке ещё по каналу и проекту, отсюда размножение суммы по строкам разреза. Дефект адресован `LS-COUNTERPARTY-RETURNS-FIX`, приёмка в `ADR-085 §10`. **Правило суток (`ADR-088 §3`):** `filter=moment>=X;moment<=Y` на entity-эндпоинтах интерпретирует границы в зоне API (той же, что несёт `moment`), верхняя граница включительна при `<=` (различитель `Q-93` снят узким окном на документе №00008, `step2b_q93_filter.log` — 5 контрольных окон, однозначный результат); `core.fact_returns.return_date` = `DATE(M)` без сдвига (`ADR-088 §3`, дефект `INGEST-MOMENT-ZONE-FIX` не чинится этой сессией). Ночная полоса интерфейс/возвраты `[21:00;24:00)` UTC пуста — 0 из 8 документов мая-2026 (см. правый столбец). **Объект паритета (владелец, 2026-08-01, вариант (c)):** число уровня (i) — `returnSum` в разрезе по контрагенту; агрегация двух entity-типов есть контрольный второй путь, и расхождение двух путей само есть сигнал. Перечень типов документов остаётся в этой колонке как ОПИСАНИЕ того, что читает ингест, и определением объекта паритета НЕ является: периметр типов задаёт источник, иначе следующий новый тип устареет молча **ПРАВИЛО-МОСТ ДОСТРОЕНО (`RETURNS-BRIDGE-RULE-ADJ`, 2026-08-17, архитектор).** Два сверяемых числа с адресом: `A` = `report/profit/bycounterparty.returnSum` (уровень (i), контрольный путь (ii) — см. выше); `B` = `SUM(core.fact_returns.sum_kgs)` за период, ОГРАНИЧЕННАЯ документами с привязкой к продаже («основание») — не всё множество строк таблицы. **Находка июля:** оракул структурно считает ТОЛЬКО возвраты с основанием (подтверждено точным совпадением суммы документа Эргешевой, `62 709,00 = 62 709,00`); три документа «Неман Фарм» без основания (`271 476,66` KGS) в оракуле не отражены — легитимное отступление, **закрывающее решение — сам этот ADR** (аналог периметровых исключений расходов `ADR-002`/`ADR-005 п.1`). Признак «основание» реализован дефектным полем `has_basis` (`ADR-098 §5`, `Q-101`, `DEFER`) — правило моста полагается на установленный факт семантики оракула, не на само поле; будущие месяцы верифицируют периметр сверкой суммы, не полем. **Критерий сходимости — инвариант разложения (форма `ADR-157 §1`), не порог на `Δ`:** `Δ = P − O + ε`, где `P` — сумма документов без основания (легитимное отступление выше), `O` — сирота эталона (документ есть у оракула, отсутствует в `core.fact_returns`), `ε` — остаток, допуск `< 1 KGS`. Пара сошедшаяся, пока `Δ` раскладывается без свободного остатка; переоткрывается только при появлении неотнесённой суммы. Полный текст — `reference/returns_bridge_rule_adj_2026-08-17.md` | `2026-05-01` (по умолчанию, `ADR-069 §2`) | **`0,00` на уровне core-таблицы за май-2026** — эталон `570,00` KGS против `core.fact_returns` `570,00` KGS, совпадение до копейки тремя независимыми путями. **Строка НЕ объявляется сошедшейся:** поверхность BI этой пары есть Custom Query `msklad_counterparty_returns`, который за период дашборда показывает не это число (скользящее окно 365 суток плюс размножение суммы по разрезу строки, `ADR-085 §10`); **СОШЛАСЬ (`LS-RETURNS-SURFACE-ADJ`, 2026-08-03). Счётчик реестра 1/7 → 2/7.** Поверхность пары ПЕРЕНАЗНАЧЕНА: `msklad_counterparty_returns` есть источник данных, а не поверхность — единственный сидящий на нём график «Топ контрагентов» (страница «Операционка») выводит Контрагент/Страна/Выручка/Маржа/% от общего дохода и НИ ОДНОЙ из трёх колонок возвратов (`return_sum_kgs`, `net_revenue_kgs`, `return_rate_pct`), то есть разрез возвратов по контрагенту не рисуется ничем. Поверхностью пары объявлен блок возвратов страницы Executive summary (источник `fact_returns`), уровень — итог за период дашборда. Read-back владельца 2026-08-03: период `2026-05-01…2026-05-31` даёт `570` KGS против эталона `570,00` KGS — совпадение точное. Периоды, заходящие за `2026-07-19`, к сверке непригодны: `core.fact_returns` не пополняется с этой даты (недельный прогон блокирован DQ Gate), повторная сверка по июлю — после восстановления потока. Разрез по контрагентам остаётся ВНЕ финишной черты до появления рисующего его графика; добавление двух колонок предложено владельцу и ОТКЛОНЕНО им (чат 2026-08-03) — решено, не отложено. Обоснование переназначения и сравнение двух формул запроса — `reference/ls_returns_surface_adj_2026-08-03.md` **Инвариант разложения проверен на июле-2026 (`RETURNS-BRIDGE-RULE-ADJ`, 2026-08-17): `Δ = 334 185,66 − 64 190,13 = 269 995,53`; `P = 271 476,66` (три документа «Неман Фарм», без основания); `O = 1 481,13` (документ UMAI WB, отсутствует в `core.fact_returns` — тип документа вне двух известных типов загрузчика, `RETURNS-INGEST-COMPLETENESS`, класс `K4` подтверждён методом исключения, точный тип не идентифицирован по решению владельца не расследовать дальше, `ADR-181 §2`; сумма при этом ИМЕНОВАННЫМ остатком остаётся, из объекта паритета не исключена). `P − O = 271 476,66 − 1 481,13 = 269 995,53 = Δ`, `ε = 0,00` — разложение точное, свободного остатка нет. Май-2026 — частный случай той же формулы (`P=0`, `O=0`, `Δ=0,00`).** **ВЕРДИКТ: ПАРА ОСТАЁТСЯ СОШЕДШЕЙСЯ** — рост `Δ` с `0,00` до `269 995,53` не есть регресс, пока инвариант держится (прецедент формы `ADR-157 §2`: «сошлась» значит «объяснена»); счётчик реестра не двигается. Разрез по документу и полное правило — `reference/returns_bridge_rule_adj_2026-08-17.md` |
| Остатки товаров (`report/stock/all`) / по складам (`report/stock/bystore`) | (i) подтверждён живым `GET`, `200` (`reference/parity_report_api_2026-07-28.md`) | `marts.inventory_health` → страница LS «Склад» (`msklad_inventory_health`) | **Частично выведено.** Сырое количество (`fact_inventory.stock` на дату последнего снапшота) сопоставимо по смыслу с `report/stock/all.rows[].stock`, но построчная сверка не выполнена в этой сессии. Производные метрики (`coverage_days_90d_calendar`, `coverage_days_true_adt`, `is_low_stock`/`is_oos`/`is_toxic`) — наш расчёт (ADT/окна продаж), у МойСклад аналога-отчёта нет ⇒ эти колонки не являются объектом паритета (не ORACLE-величина, расчётная метрика без стороны сравнения) **МОСТ АДЪЮДИЦИРОВАН ЦЕЛИКОМ (`PARITY-STOCK-ROWWISE-BRIDGE`, 2026-08-04; шаг 1 приёмки `ADR-084 §6`).** Пять элементов `ADR-079 §2`: документ-источник — отчёт `report/stock/all` (уровень эталона (i), `ADR-067 §2`); поле — `rows[].stock`; наша сторона — `core.fact_inventory.stock` за партицию `date_snapshot` (не `marts.inventory_health`: переупаковка мостом не является, `ADR-079 §2`); зерно — товар, ключ = UUID последнего сегмента `meta.href` (`cf-inventory/main.py:39-49`); периметр — `stockMode=all`, `quantityMode=all` (`cf-inventory/helpers.py:57-67`), `entity_type='product'`. **Правило конвертации в KGS НЕ ПРИМЕНЯЕТСЯ — величина не денежная:** `stock` есть КОЛИЧЕСТВО (штуки), денежная колонка таблицы — `cost_kgs` (`price/100`, `cf-inventory/main.py:52-53`), и она объектом паритета этой строки не является по уже действующему `ADR-120 §5`. Единица «KGS-штук», употреблённая в замерах выше и в двух артефактах, ошибочна как ярлык (числа верны); артефакты не переписываются (`ADR-043`). **Фильтра периода нет — величина «на момент»; эталонный момент задан правилом:** эталон снимается в окне, примыкающем к прогону `cf-inventory` (`cf-inventory-trigger`, `schedule: 0 21 * * *`, `timeZone: UTC` ⇒ `21:00Z` = `03:00` Бишкек, `11_INFRA_FACTS.md:98`), то есть в окне ПЕРЕСБОРКИ нашей стороны — буквальное прочтение `ADR-079 §3`, а не в момент чтения нашей (уже замороженной) таблицы. **Критерий сходимости — инвариант разложения, не порог на `Δ`** (форма `ADR-119 §2`, основание `05_CONVENTIONS` Часть I ★ / `ADR-075 §3`): `Δ = Σ(движение) + Σ(сироты)` без свободного остатка, где `Σ(движение)` — расхождения на совпавших ключах с ОДНОРОДНЫМ знаком (документы, проведённые после снимка), `Σ(сироты)` — строки, отсутствующие у одной стороны, с установленной природой КАЖДОЙ. Смесь знаков либо сирота без установленной природы есть свободный остаток. Порог на `Δ` невозможен по построению: `Δ` есть функция момента снятия эталона, а не свойство соответствия. Артефакт: `reference/parity_stock_rowwise_bridge_2026-08-04.md` | `2026-05-01` (по умолчанию, для `stock`-компонента; для расчётных флагов якорь неприменим) | **Повторный замер одним окном (`PARITY-STOCK-SAME-DAY`, `PARITY-STOCK-INTRANSIT`, окно `2026-08-04T05:29:51Z…05:30:00Z`, обе стороны за `date_snapshot='2026-08-04'`): источник `report/stock/all` 302 строки / `SUM(stock)=121 874,00`; наша сторона `core.fact_inventory` 303 строки / `SUM(stock)=121 878,00` (`marts.inventory_health` для этой партиции — `0` строк, не пересобрана на момент замера, сверка велась напрямую с `core`); разность `4,00 KGS-штук (0,0033%)` — на два порядка меньше прежнего невалидного замера (`0,12%`, дрейф суток снят). Причина остатка не устанавливалась. Артефакт: `reference/parity_stock_intransit_2026-08-04.md`** **Половина моста адъюдицирована (`PARITY-STOCK-INTRANSIT-CLOSE`, 2026-08-04):** объект паритета строки — ТОЛЬКО `stock`; расчётные метрики `coverage_days_90d_calendar`, `coverage_days_true_adt`, `is_low_stock`, `is_oos`, `is_toxic` объявлены ВНЕ паритета явным решением архитектора (не молчаливым исключением, как и требовал текст `Q-80`) — это наш расчёт поверх факта, стороны сравнения у МойСклада нет ни на одном из трёх уровней эталона (`ADR-067 §2`), тот же критерий, по которому вне реестра оставлены `marts.weight_flow` и величины в USD. Остаток строки — построчная сверка `stock`-компонента для установления причины разности `4,00` штуки: владелец выбрал закрытие установлением причины, а не правилом (чат 2026-08-04), работа заведена задачей `PARITY-STOCK-ROWWISE` (класс B, живой `GET report/stock/all`, мандат не выдан). В отличие от строки 22, эта пара измерима сегодня: `core.fact_inventory` пополняется отдельной функцией `cf-inventory` и заморозкой `cf-facts` не затронута. Артефакт: `reference/parity_intransit_close_2026-08-04.md §5`. **Причина остатка установлена построчным замером (`PARITY-STOCK-ROWWISE`, 2026-08-04, класс B, мандат выдан поимённо владельцем в чате):** полный внешний джойн по ключу `product_id` (298 строк источника / 303 строки нашей стороны) реконсилирует разность агрегата ПОЛНОСТЬЮ без остатка — `271` строка совпала точно, `25` строк разошлись на совпавшем ключе (`+5 291,00`, во всех 25 случаях наша сторона больше, ни одного случая наоборот), плюс `6` сирот нашей стороны (`108,00`) и `1` сирота источника (`44,00`); `5 291,00 + 108,00 − 44,00 = 5 355,00` — точное совпадение с прямой разностью агрегатов этого замера. Механизм: `core.fact_inventory` снимается ОДИН раз в сутки (`_loaded_at` партиции `2026-08-04` = `2026-08-03T21:00:04Z`), `report/stock/all` отдаёт непрерывно меняющийся текущий остаток — расхождение растёт с элапсом между моментом снимка и моментом живого запроса за счёт обычного оборота (продаж), однородный знак всех 25 расхождений подтверждает направление (запас уменьшился к моменту запроса). Величина НЕ является константой: замер прошлой сессии дал `4,00` KGS-штук при элапсе `≈8ч30м`, этот замер — `5 355,00` KGS-штук при элапсе `≈12ч06м` в тот же торговый день. Природа 7 строк-сирот (архивация/переход в ноль между снимком и запросом vs иной механизм) осталась неустановленной — `CONTEXT GAP`, не блокирует основной вывод (25 позиций дают `98,8%` величины расхождения). Артефакт: `reference/parity_stock_rowwise_2026-08-04.md`. Статус строки → причина явления установлена; адъюдикация правила-моста и закрытие статуса строки — архитектору. **ВЕРДИКТ: ПАРА НЕ СОШЛАСЬ (`PARITY-STOCK-ROWWISE-BRIDGE`, 2026-08-04, архитектор).** Два независимых основания, каждого достаточно. **(1) Ни один из двух замеров не является допустимым измерением пары:** эталон снят через `8ч 30м` (`05:29:54Z`) и `12ч 06м` (`09:06:08Z`) после пересборки нашей стороны (`_loaded_at = 2026-08-03T21:00:04Z`), тогда как мост требует окна прогона `cf-inventory`. Утверждение артефакта `parity_stock_intransit_2026-08-04.md:57-58` («условие одновременности `ADR-079 §3` выполнено буквально») опирается на разрыв между ЧТЕНИЯМИ обеих сторон в секунды — но наша сторона к моменту чтения была заморожена восемь с половиной часов; `ADR-079 §3` регулирует не этот интервал. Замер `PARITY-STOCK-ROWWISE` установил МЕХАНИЗМ и в этом качестве полностью состоятелен; допустимым измерением паритета он не является. **(2) В замере `09:06Z` есть свободный остаток:** семь строк-сирот (`6` наших на `108,00` штук, `1` источника на `44,00`), природа которых объявлена неустановленной самим артефактом (`CONTEXT GAP`); брутто `152,00` штуки, `0,12 %` величины. Малость от требования не освобождает — тем же признаком `ADR-100 §1` поймал семь строк-сирот продаж на `1 630 240,60` KGS, годами проходивших приёмку по итогу. **Кандидат «именованный допуск как функция элапса» ОТКЛОНЁН** по форме (порог на `Δ` отвергнут `ADR-119 §2`/`ADR-075 §3`; допуск сделал бы строку неопровержимой) и по факту: два замера одних суток несовместимы с элапс-пропорциональной моделью — элапс вырос в `1,42` раза, разность в `1 339` раз (`4,00` за `8ч30м`, включая бишкекское утро, против `5 355,00` за следующие `3ч36м`). Механизм остаётся названным (расходные документы после снимка), неверна параметризация по часам стенных часов: остаток уменьшается в момент ПРОВЕДЕНИЯ документа, а проведение у этого бизнеса приходит пачками (`8,9 %` суток без единого документа продажи, `reference/dq_gate_threshold_calibration_2026-08-04.md`). **Кандидат «переопределить объект паритета» ОТКЛОНЁН как решение** (величины МойСклада с историей остатков репо не знает; объект уже сужен `ADR-120 §5`), но его законная часть выделена в `CONTEXT GAP`: принимает ли `report/stock/all` ретроспективный момент — репо не устанавливает (сплошной поиск `moment=` даёт ноль совпадений со `stock`; `report/profit/*` принимают `momentFrom`/`momentTo` как ПЕРИОД, не срез). Положительный ответ снял бы требование синхронизации целиком. **Закрывает строку задача `PARITY-STOCK-SNAPSHOT-SYNC`** (класс B, единственное основание — секрет `msklad-token`, только чтение; **мандат ВЫДАН поимённо владельцем, чат 2026-08-04**): проба ретроспективного момента + пересверка в окне `21:00Z` + установление природы семи сирот. Порядок задан владельцем тем же решением (вариант А): шаг 1 (проба момента) идёт отдельным заходом первым в любой час, шаги 2-3 — только если проба отрицательна. `Q-80` остаётся `PARTIAL`; счётчик реестра не двигается (`5/7`). Артефакт: `reference/parity_stock_rowwise_bridge_2026-08-04.md` **Шаг 1 `PARITY-STOCK-SNAPSHOT-SYNC` исполнен, ОТРИЦАТЕЛЬНО (2026-08-04, класс B, окно `2026-08-04T15:17:44Z…15:17:51Z`).** Три `GET report/stock/all` в одну минуту (контроль / проба с `moment=2026-07-27 21:00:03`, кандидат — `_loaded_at` партиции недельной давности / повтор контроля), все `HTTP 200`, без `errors`. Контроль1 и контроль2 побайтово идентичны. Проба и контроль1 отличаются побайтово (эхо параметра в `meta.href`), но построчная сверка всех 326 позиций по `stock` даёт **0 расхождений** — параметр `moment` принят без ошибки, но данные не изменились при кандидате на 7 суток дальше суточной амплитуды оборота. **Граница вывода (дословно):** это означает «параметр `moment` в формате `YYYY-MM-DD HH:MM:SS` эндпоинтом `report/stock/all` не учитывается», а НЕ «МойСклад не умеет отдавать остатки задним числом» — другие имена/форматы параметра и endpoint `report/stock/bystore` не пробовались, публичная документация инструменту недоступна. Требование синхронизации с окном `21:00Z` (`ADR-123 §3`) НЕ снимается. Шаги 2-3 идут вторым заходом в окне `21:00Z` (вариант А, `ADR-123 §8`). `Q-80` остаётся `PARTIAL`, счётчик реестра не двигается (`5/7`). Артефакт: `reference/parity_stock_snapshot_sync_2026-08-04.md` **Шаг 2 `PARITY-STOCK-SNAPSHOT-SYNC` исполнен формой «принудительный прогон» (2026-08-06, класс B, мандат на запись выдан поимённо владельцем в тексте запуска, подтверждён ask-подтверждением).** Принудительный прогон `cf-inventory-trigger` (`RC=0`, подтверждено по данным — новая партиция `date_snapshot=2026-08-07`, `_loaded_at=2026-08-06T18:36:12Z`, `299` строк) плюс собственный живой `GET report/stock/all` в окне `2026-08-06T18:36:56Z…18:37:00Z` (элапс от `_loaded_at` ≈`44–48с`). Построчный полный внешний джойн по `product_id` за партицию того же прогона: `299` строк с обеих сторон, **все ключи совпали, 0 расхождений на совпавшем ключе, 0 сирот с любой стороны**, `SUM(stock)` совпадает точно (`107 392,00` = `107 392,00`), `Δ=0,00`. Разложение `Δ = Σ(движение) + Σ(сироты) = 0,00 + 0,00 = 0,00`, свободного остатка нет; предсказание правила (`Σ(движение)≈0` на секундном элапсе) подтвердилось точным нулём. Семь прежних сирот-UUID (`reference/_scratch_PARITY-STOCK-ROWWISE_2026-08-04/step6_join_result.json`) проверены против свежего замера: все семь отсутствуют на ОБЕИХ сторонах (не расходятся) — пустое множество сирот на этом замере, множество не воспроизвелось. Побочно: строк источника стало `299` против `326` на 2026-08-04 (`-27` за трое суток), природа дрейфа не исследована, вне периметра. **Статус сходимости пары этой сессией не проставляется** (адъюдикация архитектора). Артефакт: `reference/parity_stock_snapshot_sync_2026-08-06.md` **ВЕРДИКТ: ПАРА СОШЛАСЬ (`PARITY-STOCK-SNAPSHOT-SYNC` шаг 2, 2026-08-06, архитектор).** Разложение `ADR-123 §4` замкнуто точно: `Δ = Σ(движение) + Σ(сироты) = 0,00 + 0,00 = 0,00`, свободного остатка нет — приёмка `ADR-123 §8` исполнена дословно. Синхронизация момента — принудительный прогон с элапсом `≈44–48с` (мандат на форму выдан поимённо владельцем), что синхронизированнее штатного окна `21:00Z`, требуемого `ADR-123 §3` по существу (момент пересборки нашей стороны), а не по конкретному часу. **Оговорка, не блокирующая вердикт:** семь прежних сирот-UUID на этом замере отсутствуют на ОБЕИХ сторонах — это не расхождение присутствия и потому не входит в `Σ(сироты)` данного `Δ`, но это НЕ есть установление причины их исчезновения в широком смысле (ветка «переход в ноль» не подтверждена и не опровергнута прямым `GET entity/product/<uuid>`, как планировал `ADR-123 §8` шаг 3 буквально — сделана только сверка присутствия). Дрейф охватывает `27` строк источника за трое суток (`326`→`299`), на два порядка крупнее семи исходных сирот; причина не расследована, вне периметра этой задачи, свободного остатка не создаёт (обе стороны согласны). Счётчик реестра: `6/7`. |
| Заказы поставщикам, статус «В пути» / «Прибыл частично» (`entity/purchaseorder`, фильтр по `state`) | (ii) подтверждён живым `GET`, `200` (`reference/parity_report_api_2026-07-28.md`). Report-API специального эндпоинта «в пути» не документирует (`report/stock/*` — про текущие остатки, не про заказы) | `marts.in_transit` → страница LS «Склад» и страница LS «Закупки в пути» (`msklad_in_transit`, один источник, две страницы) | **Выведено из SQL (`reference/sql/sq_marts_in_transit.sql`), не адъюдикация:** строка = позиция заказа поставщику со статусом `status_name IN ('В пути', 'Прибыл частично')` И `in_transit_sum_kgs > 0` (`quantity_in_transit × price_kgs`). Клиент воспроизводит вручную: «Закупки» → «Заказы поставщикам», фильтр по статусу «В пути»/«Прибыл частично», сумма остатка к поставке по позициям. Легитимных отступлений `ADR-068 §2` для этой пары нет — фильтр статуса не входит в закрытый перечень, поэтому статусный фильтр — факт SQL, не отступление | `2026-05-01` (по умолчанию; для этой витрины зерно — «текущее состояние», не исторический ряд, якорь глубины формально неприменим к снимку на сейчас) | **Построчная сверка (`PARITY-INTRANSIT-ROWWISE`, `PARITY-STOCK-INTRANSIT`, окно `2026-08-04T05:31Z…05:33Z`): источник `entity/purchaseorder` (17 заказов в статусах «В пути»/«Прибыл частично» на сегодня — состав меняется день ко дню, не расхождение с прежним замером на 19; позиционная формула `fetch_purchases.py`) `341` позиций, `SUM(in_transit_sum_kgs)=76 514 274,30`; наша сторона `core.fact_purchases` (те же 17 заказов) `341` строка, та же сумма; полный внешний джойн по `(purchase_order_id, position_id)`: `0` сирот источника, `0` сирот нашей стороны, `0` расхождений сумм на совпавшем ключе — построчное совпадение подтверждено отдельно от совпадения агрегата (то же для `marts.in_transit`, 241 строка). Причина (её нет — три множества пусты) не устанавливалась за ненадобностью. Артефакт: `reference/parity_stock_intransit_2026-08-04.md`** **НЕ СОШЛАСЬ; правило-моста адъюдицировано, сверка проведена в обоих направлениях (`PARITY-STOCK-INTRANSIT-CLOSE`, 2026-08-04).** Прежний замер сверял ОДНО направление (наша сторона фильтровалась списком из 17 идентификаторов источника), поэтому одностороннее расхождение было невидимо по построению. Обратное направление измерено (`step1_run.log`, `step3_run.log`, read-only `bq query`): наша сторона по собственному признаку витрины даёт `17` заказов / `268` позиций / `78 748 543,05` KGS против `76 514 274,30` источника — **расхождение `+2 234 268,75` KGS (`+2,92 %`), наша сторона завышает**. Разложение: пересечение `15` заказов / `241` позиция — совпадение построчное и до копейки (`76 514 274,30` с обеих сторон); два заказа стоят в целевых статусах на обеих сторонах с нулевым остатком (`04a41f62-…` 12 позиций, `aab67ac6-…` 77 позиций) и исключаются фильтром моста симметрично — расхождением не являются (проверено прямым запросом, не вычитанием); **два заказа присутствуют только у нас** — `08bfe70e-…` (`PDMKGFRU26070711`, 5 позиций, `1 197 000,00`) и `e7d7a18b-…` (`HOLI260708 (TOR)`, 22 позиции, `1 037 268,75`). Причина: `core.fact_purchases` заморожена на `2026-08-01T17:02:16Z` (блок DQ Gate), тогда как состав заказов в целевых статусах у источника меняется день ко дню (`19` на `2026-08-02`, `17` на `2026-08-04`); текущее состояние этих двух заказов в источнике НЕ снималось (потребовало бы живого `GET`, класс B) — заморозки достаточно как объяснения, но она не есть измеренная причина. **Правило одновременности (`ADR-079 §3`) для этой пары имеет специальную форму: обе стороны суть снимок «на сейчас», поэтому при остановленном `cf-facts` пара не измерима в принципе, а не измерима с оговоркой** (частный случай `ADR-111 §3`). Объект паритета адъюдицирован: `in_transit_sum_kgs`, зерно — позиция заказа; `in_transit_sum_usd`/`fx_rate_used` — DERIVED, вне паритета (`ADR-067 §4`, считаются от курса на момент пересборки, `sq_marts_in_transit.sql:4-8`). Гейт закрытия строки — восстановление потока загрузки и пересверка. Артефакт: `reference/parity_intransit_close_2026-08-04.md`. **Пересверка после восстановления загрузки (`PARITY-STOCK-INTRANSIT-RECHECK`, окно `2026-08-05T07:20:25Z…07:21:13Z`, наша сторона `MAX(_loaded_at)=2026-08-05 07:02:15`, 18 минут до живого запроса).** Источник `entity/purchaseorder`: `17` заказов в целевых статусах (`В пути`×9, `Прибыл частично`×8), `341` позиция, из них `251` с остатком > 0, `SUM(in_transit_sum_kgs)=78 184 949,43`. Наша сторона по тому же фильтру моста (`status_name IN ('В пути','Прибыл частично') AND in_transit_sum_kgs > 0`): `16` заказов, `251` позиция, `78 184 949,43`. Множества заказов и позиций совпали полностью в обе стороны (симметрическая разность пуста на обоих уровнях); построчное сравнение `251=251` совпавших ключей даёт `Δ=0,00` без единого расхождения суммы. Разница счёта заказов (`16` против `17` в целевом статусе) объясняется одним заказом (`aab67ac6-e2d4-…`, «00135») с нулевым остатком на обеих сторонах — исключается фильтром `in_transit_sum_kgs > 0` симметрично, расхождением не является. **Предсказание `ADR-120 §2` подтверждено измерением: `Δ → 0,00`.** Судьба четырёх заказов прежнего расхождения: два «нулевых» (`04a41f62-…`, `aab67ac6-…`) в целевых статусах на обеих сторонах — один (`04a41f62`) вышел из нулевого остатка и теперь входит в пересечение (`1 670 675,12/,13`), второй (`aab67ac6`) остаётся нулевым симметрично; два прежних «только-у-нас» (`08bfe70e-…`, `e7d7a18b-…`) на ОБЕИХ сторонах теперь несут статус «Прибыл» (не целевой) — исключены фильтром статуса симметрично, причина расхождения (заморозка нашей стороны) подтверждена измерением, а не оставлена «достаточным объяснением». Полный ход, сырые данные и разложение — `reference/parity_intransit_recheck_2026-08-05.md`. Побочное наблюдение вне scope измерения: множество заказов `marts.in_transit` разошлось с `core.fact_purchases` на момент замера (`1` только в `core`, `2` только в марте) — витрина, видимо, не пересобиралась после самой свежей загрузки; причина не устанавливалась. Статус сходимости пары НЕ проставлен этой сессией — решение архитектора (`PARITY-STOCK-INTRANSIT-CLOSE`). **СОШЛАСЬ (`PARITY-STOCK-INTRANSIT-CLOSE`, второй ход, 2026-08-05, архитектор). Счётчик реестра 5/7 → 6/7.** Приёмка `ADR-084 §6` пройдена по всем трём шагам: мост адъюдицирован (`ADR-120 §3`, достроен до пяти элементов `ADR-079 §2` — см. ниже), сверка проведена в обе стороны по собственному признаку каждой стороны, расхождение УСТРАНЕНО (не закрыто правилом — устранять нечего). **`Δ = 0` ТОЧНО, а не в пределах допуска:** перепроверка адъюдикации без допуска (строгое десятичное равенство) даёт `251` совпавший ключ, `0` ключей с ненулевой разностью, пустую симметрическую разность на уровне заказов и позиций; обе стороны — `78 184 949,425` KGS (`,42`/`,43` в тексте выше есть направление округления половины тыйына на агрегате, не расхождение данных). Величина источника независимо пересчитана из СЫРЫХ тел ответов по формуле `fetch_purchases.py:141/147` — совпала со всеми 251 значением нашей стороны. **Мост достроен до пяти элементов `ADR-079 §2`** (`ADR-120 §3` назвал три): документ-источник — `entity/purchaseorder` + `.../positions` отдельным вызовом (`expand=positions` в списочном режиме не работает, `fetch_purchases.py:5`), отбор по `state.meta.href`; поля — `positions[].inTransit`/`price`/`discount`, заголовок `rate.value`; правило конвертации в KGS (`ADR-010`) — `price_kgs = price/100 × rate.value`, `in_transit_sum_kgs = round(price_kgs × inTransit × (1 − discount/100), 4)`; фильтр периода отсутствует по построению (величина «на момент»), заменяется правилом одновременности; зерно — позиция заказа, ключ `(purchase_order_id, position_id)`. **Наша сторона моста — `core.fact_purchases`, НЕ `marts.in_transit`** (`ADR-079 §2`: переупаковка мостом не является; прецедент `ADR-123 §1` для соседней снимочной пары). Оговорка `ADR-079 §4` о ненадёжности `is_in_transit` неприменима — периметр использует `status_name`, как и предписывает `02_ERP_CONTRACTS.md:408` п.5. Периметр не покрывает `13` позиций с `inTransit > 0` при нулевой цене — симметрично на обеих сторонах, границей объекта, не расхождением. **Критерий сходимости пары — инвариант разложения, не порог на `Δ`** (форма `ADR-119 §2`/`ADR-123 §4`, основание `05_CONVENTIONS` Часть I ★ / `ADR-075 §3`): пара сошедшаяся, пока (1) обе стороны сняты внутри ОДНОГО часового цикла `cf-facts` (элапс от `MAX(_loaded_at)` до живого `GET` назван числом, окно в UTC; при остановленной загрузке пара не измерима в принципе, `ADR-120 §4`) и (2) `Δ = Σ(движение) + Σ(сироты)` раскладывается без свободного остатка, где природа каждой сироты установлена. Переоткрытие — только при появлении свободного остатка. Часовое окно допустимо там, где суточное окно строки 21 не допустимо, потому что критерий самопроверяем: движение внутри элапса проявляется односторонним ключом либо расхождением на совпавшем ключе; в замере при элапсе `16-18` минут обе категории ПУСТЫ, то есть тишина интервала измерена, а не предположена. **Причина прежнего расхождения `+2 234 268,75` KGS подтверждена измерением, оговорка `ADR-120 §2` («достаточное объяснение, не измеренная причина») СНИМАЕТСЯ.** Подтверждение двойное: прямое — оба заказа хвоста (`08bfe70e-…`, `e7d7a18b-…`) несут статус «Прибыл» на ОБЕИХ сторонах, то есть наша сторона на свежих данных воспроизводит статус источника по тем самым заказам, из-за которых расхождение возникло (структурный дефект соответствия опровергнут); арифметическое — прежняя разность в точности равна сумме этих двух заказов (`1 197 000,00 + 1 037 268,75 = 2 234 268,75`), а прежнее пересечение 15 заказов воспроизводится сегодня ТОЧНО (сегодняшние `251` позиция минус вошедший заказ `04a41f62-…` дают `241` позицию и `76 514 274,300`, разность `0,000`), что исключает гипотезу двух случайно скомпенсировавшихся сдвигов. Судьба всех четырёх заказов прежних хвостов закрыта: каждый занимает одинаковое положение относительно периметра на обеих сторонах, положение измерено, а не выведено вычитанием. **Названные пределы вердикта:** сходимость измерена на одном окне (регресс возможен, обнаруживается повторным замером по критерию выше); fallback курса `(currency_rate or 1.0)` (`fetch_purchases.py:141`, нарушение `ADR-029 §1`) не сработал ни разу — `n_missing_rate_value = 0`, то есть `0` есть свойство данных замера, не доказательство корректности формулы (объект уже назван строкой `INGEST-CURRENCY-ASSERT`); момент смены статуса двух заказов хвоста не снимался. **Находка, вердикта не отменяющая, — клиентская витрина отстаёт от ядра на сутки:** `sq_marts_in_transit` имеет расписание `every 24 hours` (якорь ~`13:09Z`, `reference/sq_cadence_2026-07-27.md:83`), тогда как `core.fact_purchases` перезаписывается ежечасно; scorecard «Сумма KGS в пути» страниц «Склад»/«Закупки в пути» читает `marts.in_transit.in_transit_sum_kgs` (`reference/ls_surface_fields_sweep_2026-08-04.md §Пара 2`), поэтому клиент видит снимок возрастом до ~24 часов величины с семантикой «на сейчас». Прежняя запись `ADR-120 §Последствия` `[без правок] sq_marts_in_transit.sql` («множество заказов марта совпадает с ядром») была верна лишь потому, что ядро тогда стояло замороженным — заморозка МАСКИРОВАЛА отставание витрины; третий инстанс класса «утверждение о механизме прочитано как утверждение о состоянии» внутри истории одной пары. Заведена `INTRANSIT-SURFACE-LAG` (диагностика и подготовка — класс A; правка расписания живого `transferConfig` — класс B, мандат не выдан), второй её вопрос — обнуляется ли `in_transit_sum_kgs` у полностью прибывших заказов. На вердикт не влияет: объект паритета адъюдицирован слоем ядра, отставание витрины есть дефект переупаковки — отдельный слой, фикс-форвард туда. Артефакт: `reference/parity_intransit_close_2026-08-05.md`. |
| Взаиморасчёты с покупателями / счета покупателям (`entity/invoiceout`, разрез по `state`) | (ii) подтверждён живым `GET`, `200` (`reference/parity_report_api_2026-07-28.md`). Bulk-отчёта задолженности в report-API нет (`report/counterparty/debt`, `report/counterparty/debt/all` — оба `404`) | `marts.customer_invoices_ar` → страница LS «Операционка» (`msklad_customer_invoices_ar`) | **Выведено, построчная проверка полей исполнена (`INVOICES-PARITY-RECHECK`, 2026-08-04).** `entity/invoiceout` (`expand=state,agent,salesChannel,rate.currency`, `applicable IS NOT FALSE`) ↔ `marts.customer_invoices_ar`: `sum`→`sum_kgs` и `payedSum`→`payed_sum_kgs` через `× rate.value / 100` (`rate.value` документа; фоллбэк — текущий курс `entity/currency` для не-KGS без своего `rate.value`, запрет заглушки `1.0`, `ADR-029`); `unpaid_sum_kgs = sum_kgs − payed_sum_kgs`; группировка витрины по `agent_id+state_name` суммируется без задвоения. **Условие сходимости `0,00` требует ОДНОВРЕМЕННОГО снятия обеих сторон** — наша сторона обновляется суточным расписанием `invoices-daily-update` (`04:00` Бишкек), снимок эталона снятый позже неизбежно даёт разность, пропорциональную темпу выставления счетов за прошедший интервал | `2026-05-01` (по умолчанию) | **Сошлась с точностью до правила одновременности (`INVOICES-PARITY-RECHECK`, окно `2026-08-04T06:33…06:44Z`): источник `entity/invoiceout` (4527 документов, конвертация валют выполнена — 1966 не-KGS документов, ВСЕ несли собственный `rate.value`, фоллбэка не понадобилось) — выставлено `1 279 154 042,38`, оплачено `1 136 839 988,65`, не оплачено `142 314 053,73`; наша сторона `marts.customer_invoices_ar` (4526 счетов, загружена `2026-08-03T22:00:02Z`) — выставлено `1 279 111 083,57`, оплачено `1 136 839 988,65`, не оплачено `142 271 094,92`. Разность: количество `+1` (`0,022%`), выставлено `+42 958,81` (`0,0034%`), оплачено `0,00` (точное совпадение), не оплачено `+42 958,81` (`0,030%`). **Оплаченная сумма сходится до копейки на всех 4526 документах — исключает ошибку конвертации/периметра на загруженном множестве.** Вся разность арифметически замыкается ОДНИМ новым, полностью неоплаченным документом, появившимся в источнике за ~8,5 часов между суточной загрузкой нашей стороны и снятием эталона (правило одновременности `ADR-079 §3`, не дефект загрузчика). Условие 4 готовности программы (`reference/invoices_loader_programme_2026-08-02.md §4`) выполнено вариантом «расхождение закрыто правилом» (`ADR-068 §1`). Идентификация конкретного нового документа — вне scope (построчная сверка сверх количества/сумм). Артефакт: `reference/invoices_parity_recheck_2026-08-04.md`** |

---

## Раздел «вне реестра»

Поверхности, у которых нет пары (страницы LS без марта-аналога МойСклад, либо марта без страницы LS), с основанием по каждой (`ADR-069 §3`/`04_ROADMAP §Финиш Epic-1` — расширение реестра не самодеятельность):

| Поверхность | Основание отсутствия пары |
|---|---|
| `marts.weight_flow` (страница LS «Склад», `msklad_weight_flow`) | Оракула на стороне МойСклад нет ни на одном уровне (i/ii/iii): вес — расчётный KPI (`quantity × dim_products.weight`), в report-API и entity-API МойСклада весового отчёта/поля агрегата не существует. Пара не заводится за отсутствием стороны для сверки, не как самодеятельное расширение |
| `marts.abc_xyz` | Нет назначенной страницы LS в замороженном источнике PR-39 (`01 §потребители`, PR-30). Аналитический март без потребителя на момент источника |
| `marts.gmroi` | Нет назначенной страницы LS (PR-30), тот же источник исключения |
| `marts.gmroi_by_folder` | Нет назначенной страницы LS (PR-30); дополнительно наследует `rate_kgs_per_usd` из `marts.gmroi`, собственного запроса к `dim_fx_rates` не имеет (`ADR-050 §3`) |
| `marts.supplier_price_history` | Нет назначенной страницы LS (PR-30) |

Все пять поверхностей выше учтены явно (не пропущены молча), расширение реестра под них — решение владельца, не производится этой сессией.

---

## Плашка «вне паритета (до 2026-05)» (Шаг 4, `ADR-069 §4`)

**Назначенный текст плашки:** «Данные до 2026-05-01 — вне паритета с МойСклад: сверка с эталоном МойСклад не производилась, показатели за этот период не гарантированы к сходимости».

**Условие показа:** строка/точка на графике датирована ранее `2026-05-01`.

**Страницы LS — носители плашки** (поверхности с историческим рядом дат в отображении):

| Страница LS | Источник | Основание — почему плашка нужна |
|---|---|---|
| Инвестор | `msklad_sales_overview` | Динамика по датам — на графике возможны точки ранее `2026-05-01` |
| Операционка | `msklad_sales_overview`, `msklad_counterparty_returns` | То же — динамика контрагентов/стран, возвраты по датам |
| Расходы | `msklad_expenses` | Помесячная/понедельная динамика расходов — исторический ряд |
| Склад (график веса) | `msklad_weight_flow` | Динамика приёма/отгрузки по датам |

**Страницы/витрины — плашка НЕ нужна** (ранняя история не отображается вовсе — снимок «на сейчас», не временной ряд):

| Витрина | Основание |
|---|---|
| `marts.inventory_health` | `date_snapshot = MAX(...)` — единственный текущий снапшот, исторических точек на странице нет |
| `marts.in_transit` | Текущее состояние открытых заказов поставщику, не временной ряд |
| `marts.customer_invoices_ar` | «Custom Query, без date range — snapshot» (`03_PIPELINE_SPEC §marts.customer_invoices_ar`); `earliest_invoice_date`/`latest_invoice_date` — атрибуты строки, не ось графика |

**Применение плашки в Looker Studio — ручное действие владельца, в эту задачу не входит** (сама задача выдаёт только текст и адресный список).

---

## Датированный снимок и каденция пересъёма эталона

**Что зафиксировано.** Зона паритета есть **датированный снимок**, а не непрерывно обновляемая область
(решение владельца 2026-08-02, `ADR-101 §10`). Причина структурная: `cf-facts` промоутит
`core.fact_sales_profit` только внутри скользящего окна (`HOURLY_WINDOW_DAYS`/`WEEKLY_WINDOW_DAYS` =
7/90 суток от даты прогона, `config.py`). Данные за пределами окна — не поддерживаемое состояние: они
остаются такими, какими были записаны, когда диапазон ещё попадал в окно, и последующие правки
документов в МойСкладе (изменение суммы, переоформление, перенос даты) до них не долетают молча.

**Каденция пересъёма — решение владельца 2026-08-03, вариант 1** (`reference/deploy_order_adj_2026-08-03.md §10`):

> Эталон месяца снимается **один раз** — в последнее воскресенье, при котором весь месяц ещё целиком
> внутри окна 90 суток. Наша сторона снимается **в тот же день**, сразу после недельного прогона.
> Пересъём после этого не производится: месяц закрыт окончательно, потому что наша сторона физически
> перестаёт меняться.

Календарь правила (дата съёма = последнее воскресенье, при котором `дата − 90 суток ≤ первый день месяца`):

| Месяц зоны | Предел (первый день + 90 суток) | Воскресенье съёма |
|---|---|---|
| май-2026 | `2026-07-30` | `2026-07-26` — **упущено**, см. ниже |
| июнь-2026 | `2026-08-30` | `2026-08-30` |
| июль-2026 | `2026-09-29` | `2026-09-27` |
| август-2026 | `2026-10-30` | `2026-10-25` |

**Основание — замер, не оценка.** По 127 майским документам `entity/demand` кривая застывания
(`updated − moment`): `30,71 %` документов перестали меняться через 7 суток после конца месяца,
`81,89 %` — через 30, `95,28 %` — через 45, `99,21 %` — к `2026-07-26`; медиана `29,39` суток, максимум
`80,10`. Возвраты застывают в день создания (`lag = 0,00` у всех 8 майских). Расчёт и скрипты —
`reference/deploy_order_adj_2026-08-03.md §9`, `reference/_scratch_DEPLOY-ORDER-ADJ_2026-08-03/`.
Предел замера: поле `updated` отражает состояние на `2026-08-01`, поэтому кривая есть нижняя оценка
дрейфа; замерен один месяц и два типа документов из семи пар реестра.

**Почему именно эта точка.** Каденция зажата между двумя измеренными границами: снизу — скоростью
застывания документов в источнике, сверху — окном обновления, после которого выравнивать стороны уже
нечем. Между ними остаётся практически одна точка в месяц, и правило её называет. Только в ней правило
одновременности снимка (`ADR-079 §3`) выполняется буквально, а не декларируется.

**Запасной путь (обязателен).** Если целевой воскресный прогон сорван, окно уходит безвозвратно. Тогда
месяц закрывается одним из двух способов, и выбор называется явно в строке пары: либо датированным
снимком по последнему успешному прогону с поимённо названной потерей документов, либо досевом после
постройки постоянного режима (`SALES-REFRESH-WINDOW`, вариант 1, целевой по `ADR-101 §10`).

**Май-2026 — ЗАКРЫТ датированным снимком (решение владельца 2026-08-03, вариант 1).** Целевая точка
`2026-07-26` упущена: это ровно тот прогон, который первым провалился в аварии DQ Gate. Применён
запасной путь — снимок по последнему успешному недельному прогону `2026-07-19T01:11:56Z`.

- **Снимок:** `core.fact_sales_profit` за май — `1363` строки, `93 522 995,53` KGS; сторона источника
  на `2026-08-01` — `127` документов, `91 892 755,66` KGS; разность `1 630 239,87` KGS. Посуточная
  таблица всех 26 суток, три рассинхронизированных документа поимённо, доказательство и границы —
  `reference/may_zone_close_2026-08-03.md` (артефакт самодостаточен).
- **Потеря неденежная.** Три документа (`№04195`, `№04456x`, `№04227`) правились в источнике после
  `2026-07-19`, имеют дату раньше `2026-05-11` и штатным путём не догонятся никогда — но денежного
  эффекта эти правки не имели: двое соответствующих суток сходятся на уровне копеек, третьи
  расходятся ровно на позицию, атрибутированную строкам-сиротам ещё `ADR-100 §1`, и суммарная
  арифметика замыкается с остатком `−0,73` KGS. Рассинхронизированы неденежные поля; что именно в них
  изменено — не устанавливалось.
- **Разность `1 630 239,87` KGS этой записью НЕ закрывается** — она есть отдельный предметный дефект
  (`MERGE` без ветки удаления, `ADR-100 §1`, конвенция `C2`), патч подготовлен текстом, не выбран и не
  задеплоен.
- **Сходимость пар реестра эта запись не объявляет:** фиксируется состояние данных, статусы пар не
  менялись.

**Примечание к парам, опирающимся на `core.fact_sales_profit` (строки 18–19).** Число за май-2026
зафиксировано снимком на дату загрузки, а не живым состоянием источника. Пересверка этих пар после
досева мая, если он будет выбран, даст другое число **по построению** — это не регресс проверки.

**Обновление правила-моста строк 18–19 (`SALES-PARITY-VERDICT-ADJ`, 2026-08-10, архитектор).**
Форма инварианта переписана; прежняя запись `Δ = M1 − |M2| + ε` (`ADR-119 §2`) — superseded-in-part
в части ФОРМУЛЫ (критерий «инвариант разложения, а не порог на разность» остаётся в силе).
Действующая форма:

```
Δ = O + ε ,  |ε| < 1 KGS
O = Σ строк ядра, не имеющих соответствия в эталоне по причине ADR-100 §1
    (MERGE без ветки DELETE: однажды загруженная строка не удаляется никогда)
```

Причина правки: после деплоя периметра (`SALES-INGEST-PATCH-DEPLOY`, 2026-08-05) класс `M2`
(недостача нашей стороны) перестал существовать — две трети его загружаются штатно, а оставшийся
подкомпонент (документ `04235`, `ООО «РВБ»`, `2026-05-12`, `1 980 000,00` KGS) по механизму
тождественен `M1` и потому не вычитается, а складывается.

Измерение `2026-08-10` (`reference/sales_perimeter_parity_recheck_2026-08-10.md`, мандат `ADR-133`):
`core.revenue_kgs`(май) `= 96 844 445,61`; `report/profit.sellSum`(май) `= 93 234 205,80`;
`Δ = +3 610 239,81` (`3,8722 %`); `O = 1 630 239,81 + 1 980 000,00 = 3 610 239,81`; **`ε = 0,00`**.
**Пара «Продажи» сошлась** в смысле `ADR-068 §1` (каждый сом отнесён к названному механизму,
свободного остатка нет); счётчик реестра `7/7` не меняется. Прежний текст статус-ячейки строки 18
(«расхождение `288 789,73`, причина НЕ установлена») относится к состоянию `2026-07-28` и
сохраняется как датированная запись, а не как текущий статус (`ADR-043`).

**Названное следствие:** ни один недельный прогон внутри разрешённого окна выкладки
`SALES-REFRESH-WINDOW` (`2026-08-25`…`2026-09-29`) не покрывает строки `O` (даты `07.05`, `09.05`,
`12.05`, `26.05`) — окно конвейера `90` суток. Устранение майского перевеса требует, чтобы ПЕРВЫЙ
прогон после деплоя ветки `DELETE` был исполнен окном, покрывающим `2026-05-01`. Условие внесено в
приёмку строки `1` списка закрытия (`reference/scope_close_freeze_2026-08-10.md §3`), полный разбор —
`reference/sales_parity_verdict_adj_2026-08-10.md §3`.

---

## Гэпы (Шаг 5)

Всё, что реестр не смог заполнить по состоянию репо, отражено дважды: строкой «требуется» в таблице выше и `Q`-строкой в `STATE_PATCH` (см. session-блок коммита). Сводка:

1. **Мост `sales_overview` (Инвестор/Операционка) не выведен** — соответствие `revenue_kgs`/`cogs_kgs`/`margin_kgs` полям `report/profit/byproduct`/`bycounterparty` не проверено построчно → `Q-78`.
2. **`msklad_counterparty_returns` (Трек A) — источник в репо не найден** (ни SQL, ни правило), эталон `recon_2026-05.md` не посеян → `Q-79` (частично дублирует известный DEFER `ADR-005 §5`, формализуется отдельной строкой для реестра).
3. **Мост `inventory_health` частично не выведен** — построчная сверка `stock` с `report/stock/all` не выполнена; расчётные флаги (`is_low_stock`/`is_toxic`/`coverage_days_*`) не являются ORACLE-величиной по построению → `Q-80`.
4. **Мост `in_transit` (SQL выведен, численно не сверен)** — фильтр статуса зафиксирован из SQL, но 0,00-сверка с `entity/purchaseorder` не проведена → `Q-81`.
5. **Мост `customer_invoices_ar` не выведен** — соответствие полей `entity/invoiceout` (`sum`/`payedSum`/`state.name`) агрегатам SQL не проверено → `Q-82`.

---

## Предложение в `02_ERP_CONTRACTS §оракул` (proposed, применяется только после апрува владельца)

См. `NEW_DECISIONS` в session-блоке коммита `docs: реестр паритета PARITY-REGISTRY` — регистрация `reference/parity_registry.md` как артефакта в реестре эталонов `02 §оракул` последствиями ни одного accepted ADR не покрыта (`ADR-067 §Последствия`), правка не вносится этой сессией.
