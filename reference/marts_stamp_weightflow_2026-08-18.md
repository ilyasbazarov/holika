# FILE: marts_stamp_weightflow_2026-08-18.md

# Отметка времени сборки применена на `sq_marts_weight_flow` — приёмка закрыта тремя пунктами

**Сессия:** `MARTS-STAMP-WEIGHTFLOW` (класс B, мандат выдан поимённо владельцем, чат 2026-08-18,
`reference/dq_deploy_accept_adj_2026-08-18.md §7`; форма/предусловия/приёмка/откат — `§5` того же
файла). **Дата:** 2026-08-18 (Бишкек). **База:** `e6a6d28`.

**Объект.** `transferConfig` `sq_marts_weight_flow`, Config ID
`6a1f9418-0000-276f-a1e4-d4f547ee7418`, целевая таблица `msklad-bi-prod.marts.weight_flow`.

---

## Что сделано

Поле `params.query` живой конфигурации подменено на подготовленный текст
`reference/sql/sq_marts_weight_flow_stamped_2026-08-13.sql` **как есть**, без пересборки —
единственная правка. Расписание, адресат, права — не тронуты. Один вызов `bq update
--transfer_config`, логи — `reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/step3_run.log`.

Дополнительно, чтобы не ждать суточного расписания, выполнен один ручной прогон конфигурации
(`bq mk --transfer_run`), объявленный владельцу тем же сообщением, что и правка. Прогон завершился
`SUCCEEDED` (`reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/step6_run.log`,
`.../run_final_2026-08-18.json`).

## Предусловия — оба пройдены до правки

**(i) Свежий снимок живой конфигурации, сверка со снимком репо** (повторено заново, П0 сессии
`MARTS-BUILD-STAMP-P0` от `2026-08-17` не переиспользовался). Расхождение — ровно ожидаемое,
декоративное: буква в комментарии шапки («в МойСкладе» живой vs «в МойСклад» в снимке репо) плюс
отсутствие финального перевода строки в снимке; длины `2546` байт живой / `2545` байт снимок.
Сверх этих двух мест расхождений нет. Логи —
`reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/step1_run.log`,
`live_transfer_config_2026-08-18.json`, `live_query_extracted_2026-08-18.sql` (этот файл — эталон
для отката, снят ДО правки).

**(ii) `bq query --dry_run` подготовленного текста** — `rc=0`, непустой вывод:
`totalBytesProcessed=3245432` (число, не пустая строка). Логи —
`reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/step2_run.log`, `dryrun_2026-08-18.json`.

**(iii)** Действие объявлено владельцу отдельным сообщением до `ask` (объект, что меняется, чем
откатывается, ручной прогон) — дословное подтверждение «да» получено в чате 2026-08-18.

## Приёмка — три пункта, все закрыты фактами

**1. Read-back.** `bq show --transfer_config` после правки отдаёт текст запроса, побайтово равный
подготовленному файлу: `diff rc=0`, sha256 обеих сторон совпадает
(`ba18dc56cb20af7881480c503ecfecf89de760e4e2caae99c0fa262355cc3a45`). Логи —
`reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/step4_run.log`,
`postupdate_query_extracted_2026-08-18.sql`.

**2. Схема.** Обе колонки присутствуют в схеме `marts.weight_flow` после пересборки:
`_marts_built_at`, `_source_max_loaded_at` (полный состав — восемь прежних полей плюс эти две, без
сдвига и без потерь). Лог — `reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/step7_run.log`,
`table_schema_2026-08-18.json`.

**3. Состав живого результата (не факт правки конфигурации).** Замер по всей таблице после
ручного прогона (`559` строк):

| Колонка | Непустых строк | Мин | Макс |
|---|---|---|---|
| `_marts_built_at` | `559` из `559` | `2026-08-18 08:24:42` | `2026-08-18 08:24:42` |
| `_source_max_loaded_at` | `559` из `559` | `2026-08-18 08:01:45` | `2026-08-18 08:01:45` |

`_marts_built_at` несёт момент пересборки, `_source_max_loaded_at` несёт непустое значение
момента загрузки источников. Ноль пустых значений по обеим колонкам. Лог — тот же
`step7_run.log`, `data_check_2026-08-18.json`.

## Что НЕ делалось

- Не тронуты `sq_marts_customer_invoices_ar` и `sq_marts_expenses` — у них свои мандаты и свои
  сессии, пакетное исполнение запрещено (`ADR-115 §5`).
- Расписание, адресат, права `transferConfig` не менялись.
- Текст запроса не пересобирался и не правился сверх подстановки подготовленного файла целиком —
  финальная позиционная склейка `SELECT * FROM outbound UNION ALL SELECT * FROM inbound` применена
  как есть; корректность арности/типов подтверждена прохождением `bq query --dry_run`, не чтением.
- Никаких записей в `marts.*` руками, кроме результата штатного прогона `transferConfig`.

## Откат (не потребовался)

`bq update --transfer_config` прежним текстом из
`reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/live_query_extracted_2026-08-18.sql` (снят
до правки, содержит декоративные расхождения живого, не снимка репо). Пересборка витрины при
откате не требуется.

## Адреса логов

Все скрипты и логи — `reference/_scratch_MARTS-STAMP-WEIGHTFLOW_2026-08-18/`:
`step1_precheck_i.sh`/`step1_run.log` (предусловие i), `step2_precheck_ii.sh`/`step2_run.log`
(предусловие ii), `step3_apply_update.sh`/`step3_run.log` (правка), `step4_readback.sh`/
`step4_run.log` (приёмка 1), `step5_manual_run.sh`/`step5_run.log` (запуск ручного прогона),
`step6_wait_run.sh`/`step6_run.log` (ожидание завершения), `step7_accept_2_3.sh`/`step7_run.log`
(приёмка 2 и 3).
