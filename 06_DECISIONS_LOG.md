# FILE: 06_DECISIONS_LOG.md
# (полный файл — изменение: append ADR-017 и ADR-018 после ADR-016, дословно из архитекторского патча)

# 06 · DECISIONS_LOG — Журнал архитектурных решений (ADR)

**Статус:** LIVING (append-only). Никогда не переписывать прошлый ADR. Новое решение = новый ADR,
помечающий старый `superseded` при необходимости. Номер ADR = следующий по факту.

---

## ADR-000 · Принят docs-driven multi-agent метод
- **Контекст:** проект вёлся двумя документами — PROJECT_REFERENCE_v6 и RUNBOOK_v8; не масштабируется,
  растит контекст чата, смешивает стабильное и живое, нет append-only истории решений и разделения ролей.
- **Решение:** перейти на метод из `_METHOD.md` — плоский набор нумерованных `.md`, роли
  архитектор/разработчик/applier/человек, агенты читают репо по SHA, знание фиксируется session-блоками.
- **Последствия:** заведён репозиторий `ilyasbazarov/holika`; каркас ролей (`_ARCHITECT`, `_APPLIER`,
  `_GENERATOR`, `05_CONVENTIONS`, `08`) и `_METHOD` — в репо. Миграция ведётся по `TZ-MIGRATE-TO-DOCS-METHOD.md`.
- **Статус:** accepted

## ADR-001 · Отдельный репозиторий
- **Контекст:** проект не связан с другими (в т.ч. с эталонным `ilyasbazarov/docs`, откуда взят метод).
- **Решение:** вести в отдельном репо `ilyasbazarov/holika`, со своим потоком SHA и независимыми ролями.
- **Последствия:** все raw-URL в доках указывают на `ilyasbazarov/holika`.
- **Статус:** accepted

## ADR-002 · Граница контракта = эталонные цифры ERP (референс-оракул)
- **Контекст:** методу нужна жёсткая граница/красная линия. У проекта она есть — эталонные цифры в ERP.
- **Решение:** граница контракта — **референс-оракул приёмки** (`_METHOD.md` §4b): выход проекта обязан
  биться с эталонными цифрами ERP «по цифрам»; расхождение закрывается правилом/override/ADR, не тихим
  примирением. Доступ к ERP есть; недостающие эталоны выгружаются руками в `/reference`.
- **Последствия:** `00_CHARTER` §главный принцип и `02_ERP_CONTRACTS` фиксируют, что за цифры и как
  выгружаются. Точный состав эталона — открытый вопрос, закрывается discovery-брифом в P3.
- **Статус:** accepted

## ADR-003 · Applier, генератор брифов и discovery-петля — с самого старта
- **Контекст:** источник заведомо неполон; двигаться по задачам без обогащения репо = гадать
  вслепую, жечь ресурс.
- **Решение:** задействовать полный набор ролей сразу, включая `_APPLIER` и `_GENERATOR`; discovery-петля
  (`_METHOD.md` §11) — центральный механизм, не опция. Гэп → discovery-бриф (return-this = обогащение
  репо), не догадка.
- **Последствия:** `07_STATE` держит GAP-реестр; генератор умеет discovery-бриф; applier коммитит
  обогащение (`/reference`, ADR, патчи доков).
- **Статус:** accepted

## ADR-004 · Ввести 11_INFRA_FACTS (LIVING)
- **Контекст:** волатильные инфра-факты (URL/ревизии CF, Config ID SQ, расписания, IAM, секреты) не помещаются в STABLE 01/02 без нарушения дисциплины изменений.
- **Решение:** отдельный LIVING-документ `11_INFRA_FACTS` как канонический реестр инфра-фактов, по образцу эталонного репо. Факты обновляются часто, при каждом деплою/ротации секретов.
- **Последствия:** следующие единицы переносятся не в `01`/`02`/`03`, а в `11`: PR-07 (cf-табл.), PR-10 (standalone), PR-13 (cf-finance конфиг), PR-15 (config.py конст.), PR-18ч (cf-fx URL), PR-21 (SQ Config ID), RB-05 (аспект IAM), RB-42ч (мониторинг конфиг), RB-47 (deploy params). Карта доков `00_CHARTER` включает `11` в LIVING-слой.
- **Статус:** accepted

## ADR-005 · Границы референс-оракула приёмки (seed #1 — П&Л май-2026)
- **Контекст:** ADR-002 задал границу = эталонные цифры ERP, но состав/гранулярность/правило сверки/закрытие расхождений (Q-1) не определены. PR-28 сам документирует известный разрыв ~33% между fact_payments и П&Л МойСклада — эталон и выход НЕ сходятся by design; риск принять выход за эталон.
- **Решение:**
  - (1) Ground-truth = сторона МойСклада (П&Л), НЕ fact_payments. В seed все числа PR-28 дословно, с разметкой класса ORACLE / PIPELINE_SNAPSHOT / DERIVED.
  - (2) Гранулярность = статья П&Л (line-level), не только итог.
  - (3) Сверка = точное совпадение (до копейки) на ORACLE; PIPELINE_SNAPSHOT не сверяется.
  - (4) Целевая сверка расходов: fact_payments (+ будущий entity/loss) → 11 132 303,89, с известным разрывом как зарегистрированным исключением Q-8/Q-9/Q-10; допуск = документированный остаток, пока Q-8 не закрыт; закрытие = discovery→ADR, не подгонка.
  - (5) RECON-цифры (выручка 93 251 530,80; «В пути» 104 877 706,26) — DEFER в `/reference/recon_2026-05.md`; точка возврата: свежая выгрузка МойСклада ИЛИ ADR, принимающий PR-строки 21/25/47 как провенанс.
- **Последствия:** создан `/reference/pnl_2026-05.md`; `02_ERP_CONTRACTS` §оракул регистрирует артефакт+правило; Q-1 → partial; приёмка «по цифрам» включается на ORACLE-строках после апрува.
- **Статус:** accepted

## ADR-006 · Методология агрегации расходной части П&Л МойСклада и карта категорий дашборда
- **Контекст:** Q-8 требовал установить, как строится расходная строка П&Л МойСклада. Обнаружено, что 22 из 23 статей строятся из трёх источников (paymentout + cashout + loss) с конвертацией по курсу валюты документа; единственная исключение — статья «Комиссия», которая строится отдельно из `entity/commissionreportin` (сумма reward÷100 + commissionOverhead.sum÷100 по документам периода). Все 22 обычные статьи подтверждены точным совпадением (0.00 разницы) с ручной выгрузкой П&Л 2026-07-06; методология Комиссии проверена на 7 документах мая с точным совпадением.
- **Решение:**
  - (1) Зафиксировать двухисточниковую методологию агрегации П&Л как основную для построения марта расходов: источник #1 = paymentout+cashout+loss (22 статей + FX конверсия по курсу документа), источник #2 = entity/commissionreportin (1 статья, формула reward÷100 + commissionOverhead.sum÷100).
  - (2) На дашборде категория «Расходы маркетплейсов» объединяет обе статьи — источник #1 (статья «Расходы маркетплейцов» из paymentout/cashout/loss) И источник #2 (entity/commissionreportin reward+overhead) — решение владельца от 2026-07-06.
  - (3) Статьи вне списка 27 целевых категорий дашборда (переданного заказчиком) исключаются как движения капитала/финансирования, не операционные расходы: Перемещение исходящий, Благотворительность, Возврат займа собственнику, Выплата тела кредита, Проценты по кредиту, Возврат.
  - (4) Нулевые значения по статьям (IT, Аренда, Фулфилмент в мае-2026) — ожидаемы (подтверждено эталоном 2026-07-06), не пробел в данных.
  - (5) ORACLE seed #1 (ADR-005, 2026-06-25) заменяется на seed #2 (эталон 2026-07-06, свежий ручной снимок из МойСклада), поскольку seed #1 содержал ошибки ввода (гипотеза дрейфа данных отклонена владельцем).
- **Последствия:**
  - `02_ERP_CONTRACTS` §семантика П&Л — заменить комментарий GAP Q-8 на эту двухисточниковую методологию с примерами агрегации и ссылкой на entity/commissionreportin.
  - `03_PIPELINE_SPEC` — новая секция §marts.expenses с логикой построения марта и картой 27 категорий дашборда (от заказчика).
  - `/reference/pnl_2026-05.md` — заменить эталон на свежий снимок 2026-07-06 с той же разметкой ORACLE/PIPELINE_SNAPSHOT/DERIVED.
  - `07_STATE` — Q-8 → closed; Q-9/Q-10 оставляют блокирующие зависимости для UUID резолва и кода, но методология от них независима (работает по имени статьи).
- **Статус:** accepted

## ADR-007 · Три порога свежести — не примиряются, документируются по механизму+scope
- **Контекст:** источник (PR-19/PR-31/RB) содержит три разных порога «свежести», выглядящих как противоречие: DQ 3 дня, алерт 6 часов, ручная проверка 2 часа. Наполнение `03 §DQ` (M-P4-03d) требует решения: свести в одно число или развести. Установлено, что пороги принадлежат трём независимым механизмам и частично меряют разные колонки (`transaction_date` vs `_loaded_at`).
- **Решение:**
  - (1) Пороги НЕ примиряются в одно число. `03 §DQ` документирует все три в ОДНОЙ таблице с колонками: порог · механизм · метрика(колонка)+scope · назначение · действие при нарушении.
  - (2) Классификация по механизму: 3 дня = `cf-dq`/DQ Gate (блокирующий, автомат) на `MAX(DATE(transaction_date_raw))`; 6 часов = `cf-alert`/Telegram (уведомляющий, автомат) на `MAX(transaction_date)`; 2 часа = ручная/runbook liveness-проверка (человек) на `_loaded_at`.
  - (3) 2-часовой порог фиксируется как liveness-heartbeat на `_loaded_at` (пайплайн часовой → нет свежей загрузки за 2ч = пропущенный прогон). Формулировка еженедельного чеклиста RB (2ч на `transaction_date`) помечается как неточная, канон — `_loaded_at`.
  - (4) Ревизия `cf-dq` (Q-6 / TD-DQ-REVISION) — волатильный факт, дом `11_INFRA_FACTS`; на значения порогов не влияет и этот ADR не гейтит.
- **Последствия:**
  - `03_PIPELINE_SPEC` §DQ — таблица свежести (3д/6ч/2ч) по схеме (1)–(3); разблокирует M-P4-03d.
  - `07_STATE` — снять заметку-противоречие «пороги разбросаны»; ADR-007 → accepted.
  - `04_ROADMAP` — ветка примирения `ADR-007 → 03d` закрыта; `03d` стартует.
- **Статус:** accepted

### Пороги свежести (ADR-007 — три механизма, не примиряются)

| Порог | Механизм | Метрика (колонка) + scope | Назначение | При нарушении |
|---|---|---|---|---|
| ≤ 3 дня | cf-dq / DQ Gate (автомат, блокирующий) | `MAX(DATE(transaction_date_raw))`, бизнес-дата | Устаревание бизнес-данных в CORE; терпит пустые дни | DQ FAILED → блок promote; пустой день → manual promote (§16) |
| ≤ 6 часов | cf-alert / Telegram (автомат, уведомляющий) | `MAX(transaction_date)`, бизнес-дата | Оперативное уведомление о разрыве потока в течение дня | Telegram-алерт → §4 |
| ≤ 2 часа | ручная / runbook (человек, liveness) | `_loaded_at`, техническая дата загрузки | Heartbeat: запускался ли workflow (часовой пайплайн) | Диагностика §4.2 / §7 (silent skip) |

## ADR-008 · Audit-снапшоты — инструментация + латентный SCD-актив
- **Контекст:** `sq_audit_dim_{products,counterparties,employees}_snapshot` (ежедневный INSERT-append в датасет `audit`) обнаружены без §audit в 01–03/09, без потребителя (LS/марты не читают). Историчность dim'ов неравномерна: `dim_counterparties` = SCD2 в ядре; `dim_products` = без SCD2 (перезапись MERGE); `dim_employees` = схема не документирована (Q-4), вероятно без SCD2.
- **Решение:**
  - (1) Дом не в доменной спеке: Config ID/расписание/стратегия → `11_INFRA_FACTS` §SQ (PR-21); схема датасета `audit` → `/reference` (Q-4); SQL → `/reference/sql/` (Q-5); узел `audit` → `01 §топология` (1–2 строки).
  - (2) НЕ заводить `§04_AUDIT_SNAPSHOTS` в `03` и секцию в `09`: нулевая доменная логика, нет требования/оракула приёмки.
  - (3) Зафиксировать латентную роль: для `dim_products`/`dim_employees` снапшот = единственный носитель истории (паттерн SCD2-via-snapshot). Промоушен в consumer-facing спеку — только по появлении аналитического требования (тогда — новый ADR).
  - (4) Операционный флаг: INSERT-append без retention → линейный рост; retention/партиционирование `audit` — в `10_OPS_PLAYBOOK`/`11` (не блокер).
- **Последствия:** `01 §топология` +строка про `audit`; `11 §SQ` — три audit-SQ; `07_STATE` — Q-17 закрыт как «инструментация, промоушен по требованию».
- **Статус:** accepted

## ADR-009 · Легаси fact_payments-март расходов вытесняется raw-мартом ADR-006
- **Контекст:** живой `sq_marts_expenses` (CREATE OR REPLACE, PR-236) читает `core.fact_payments` — построен из 2 из 4 источников (только paymentout+cashout), задокументированный ~33% оракул-разрыв (ADR-005 §4, PR 433–436). ADR-006 §marts.expenses задаёт TO-BE из `raw.moysklad_*` напрямую. `EXCLUDE_EXPENSE_IDS` на Python ретайрнут (Ghost fix 2026-06-18); нынешняя чистка `fact_payments` = BQ DELETE по UUID после MERGE (Q-10). ADR-006 исключает 6 статей по ИМЕНИ, независимо от Q-9/Q-10.
- **Решение:**
  - (1) Живой `fact_payments`-based `sq_marts_expenses` = LEGACY. TO-BE = ADR-006 raw-март. Epic-1 строит TO-BE и ретайрит легаси.
  - (2) M-P4-03e2: SQL легаси-марта переносится в `03 §marts` с явной AS-IS/legacy пометкой (источник `fact_payments`, ~33% разрыв) + кросс-ссылка на `§marts.expenses` (TO-BE). Не примирять молча.
  - (3) Фильтрация raw-марта — по имени статьи (ADR-006), развязана от Q-9/Q-10, валидирована оракулом (0.00 diff 2026-07-06). ЗАРЕГИСТРИРОВАН риск: имя хрупко к переименованию/дублю. Future-item: по закрытии Q-9/Q-10 захардить исключения на UUID (сверив с именами) — defense-in-depth, не блокер Epic-1.
- **Последствия:** `03 §marts` — AS-IS/TO-BE разметка легаси-марта; `03 §marts.expenses` — примечание про имя-vs-UUID и future-хардинг; `07_STATE` — Q-15 закрыт (ADR-009), Q-16 закрыт (имя-based старт + UUID-хардинг future, связан с Q-9/Q-10).
- **Статус:** accepted (**§1 (build-framing) partially superseded by ADR-012** — см. ниже)

## ADR-010 · Конвертация мультивалютных сумм (minor-units × rate.value)
- **Контекст:** обнаружен корень TD-RECON-01 — конвертация `÷100 без ×rate` давала ~90× недооценку сумм в USD. Источник: PR-33 п.6, RB-38 §26.
- **Решение:** канон конвертации — `minor_units ÷ 100 × rate` документа (`rate.value` документа, **если задан; иначе — текущий курс валюты**) до суммирования в KGS; `÷100 без ×rate` запрещён. **Скоуп: все входящие мультивалютные суммы (`price`/`sum` позиций, `sum`/`payedSum` в `invoiceout`, `sum` в `paymentout`/`cashout`) — глобальный inbound-канон. Исходящая конвертация (KGS→USD через `cf-fx`/`dim_fx_rates`) — вне ADR-010 (Q-19).** Фикс применён 2026-06-25.
- **Последствия:** `02_ERP_CONTRACTS` §валюты:266 → ссылка на ADR-010; `09_GLOSSARY` §конвертация валют → кросс-ссылка.
- **Статус:** accepted (адъюдикация M-P5-AP, 2026-07-08 — правка fallback-приоритета и скоупа против исходной формулировки `proposed`)

## ADR-011 · Фильтрация статей расходов — только DELETE-after-MERGE (Ghost Records)
- **Контекст:** источник: PR-27, PR-34 п.26.
- **Решение:** выгружаются все платежи без исключений на этапе выгрузки; системные статьи удаляются `DELETE` после `MERGE`. Инвариант: никогда `if/continue` на этапе выгрузки (Ghost Records). **Статус утверждения: канон-по-источнику (документированное целевое поведение + TO-BE-инвариант); конформность живого кода `if/continue`-free НЕ верифицирована — гейт Q-3 (код не версионирован).** Полный `EXCLUDE_EXPENSE_IDS` = Q-10.
- **Последствия:** `03_PIPELINE_SPEC` §fact_payments:344 → ссылка на ADR-011.
- **Статус:** accepted (адъюдикация M-P5-AP, 2026-07-08 — добавлен caveat о неверифицированной конформности живого кода)

---

## ADR-012 · Прод-март расходов = `sq_marts_expenses` (fact_payments-based); TO-BE raw-март не строится
- **Контекст:** требовалось установить, что реально питает страницу «Расходы» и совпадает ли это с ADR-006 (Q-18). Репо противоречиво: ADR-009 говорит, что Epic-1 строит TO-BE raw-март и ретайрит легаси; владелец говорит, что проект уже построен и переделывать заново не нужно. Разрешено фактом из `/reference/sql/sq_marts_expenses.sql` @ снапшот 2026-07-07 (SHA `805450a`, M-P4-D5) и `bq ls`: страница «Расходы» ← кастом-запрос LS `msklad_expenses` (тонкая обёртка, фильтр по `DATE(moment)`, без логики) ← таблица `msklad-bi-prod.marts.expenses` ← scheduled query `sq_marts_expenses` (Config `6a22a243-0000-20fd-a458-883d24f4cad4`), который читает только `core.fact_payments` = `paymentout+cashout` (2 из 4 источников, ADR-009 стр.106 / ADR-005 §4). Нет `loss`, нет `commissionreportin`. Расписание: ежедневный автоматический прогон (BQ DTS default «every 24 hours», якорь = creationTime 2026-06-05 11:10:25 UTC → nextRunTime +24ч, фактические прогоны ~11:10 UTC, WRITE_TRUNCATE). ⚠ Исходная формулировка «ручной/on-demand» (снапшот D5 читал пустой timeBasedSchedule без nextRunTime) — исправлена по живой bq show-перепроверке 2026-07-08 (провенанс: /reference/bq_transferconfig_sq_marts_expenses_2026-07-08.txt). Группировка по `expense_item_name` (по имени, `COALESCE(…, 'Не указана')`); `total_sum_usd = SUM(sum_kgs) / последний курс` (единый текущий курс на всю историю). ADR-006 двухисточниковая методология (paymentout+cashout+loss+commissionreportin) в проде НЕ реализована — валидирована лишь ручной выгрузкой 2026-07-06.
- **Решение:**
  1. Факт (канон): прод-витрина расходов = `marts.expenses`, строится `sq_marts_expenses` (Config `6a22a243-0000-20fd-a458-883d24f4cad4`) из `core.fact_payments`. LS `msklad_expenses` — date-filtered pass-through, без агрегации/исключений/FX.
  2. TO-BE raw-март из ADR-006/009 НЕ строится (решение владельца). Правится существующий `sq_marts_expenses`, а не заменяется новым мартом.
  3. Переформулировка курса (supersedes ADR-009 §1): Epic-1 = верификация `marts.expenses` против эталона + точечные правки `sq_marts_expenses` там, где расхождение подтверждено (в первую очередь — добавить недостающие источники `loss` + `commissionreportin` по ADR-006). Не постройка с нуля.
  4. Гипотеза расхождения (grounded, не подтверждена): прод-март структурно занижает статьи, питаемые из `loss`, и «Расходы маркетплейсов» (источник #2 `commissionreportin` отсутствует целиком). Механизм задокументированного ~33% разрыва (ADR-005 §4). Подтверждается/опровергается в E1-T1.
  5. Провенанс + условие промоушена в `accepted`: `/reference/sql/sq_marts_expenses.sql` @ снапшот 2026-07-07 (SHA `805450a`, M-P4-D5). Живая перепроверка перед промоушеном:
```bash
     bq show --transfer_config --project_id=msklad-bi-prod \
       projects/msklad-bi-prod/locations/asia-east1/transferConfigs/6a22a243-0000-20fd-a458-883d24f4cad4
     # ПОДТВЕРЖДЕНО 2026-07-08: FROM = core.fact_payments (load-bearing гейт); расписание — ежедневный дефолт 24h (~11:10 UTC), НЕ manual (см. /reference/bq_transferconfig_sq_marts_expenses_2026-07-08.txt)
     bq show --format=prettyjson msklad-bi-prod:marts.expenses   # + свежесть последнего прогона
```
- **Последствия:** `ADR-009` §1 (build-framing) → помечен `partially superseded by ADR-012`. `04_ROADMAP` Epic-1 → «верификация+правки», не постройка. `07_STATE` Q-18 → CLOSED (факт); Q-15 → CLOSED (supersedes ADR-009): канон = `sq_marts_expenses`, не ретайрится, а правится точечно. `03 §marts.expenses` — при наполнении прозой отразить: прод = fact_payments-март, ADR-006 = целевая методология правки.
- **Статус:** accepted (промоушен M-P5-AP-2, 2026-07-08: bq show-перепроверка выполнена владельцем — FROM=core.fact_payments подтверждён; §Контекст-расписание исправлено «ручной»→«ежедневный дефолт 24h»; правка контекста не затрагивает решения §1–§4).

<!-- Новые ADR — append ниже. -->

## ADR-013 · Классификация дашборд-статей: «Вывод прибыли» (капитал), «Налоги и сборы» (начисление); фикс split исключений
- **Контекст:** репо был расколот по «Вывод прибыли» — исключали {`02 §семантика П&Л`, `pnl`-аннотация}, включали {`ADR-006 §3`, `03 §marts.expenses` #8, `pnl §п.5`, `client-27`}; 6-й слот исключений скакал «Возврат»(06/03)↔«Вывод прибыли»(02), «Возврат» — фантом (строки нет в оракуле). «Налоги и сборы» (2 168 917,60) — accrued-строка П&Л ВНЕ 32 opex, но листилась статьёй дашборда (Q-24). Счётчики гуляли: 30/31 (02), 31=факт32 (pnl), 26 (03). Reconciliation-identity: `client-27 = (oracle-26 − Комиссия) + Вывод прибыли + Налоги и сборы`. Решения владельца 2026-07-08; количественный evidence — E1-T2 (pending).
- **Решение:**
  1. **«Вывод прибыли»** (≈4 246 163,66/мес) = **движение капитала** (владелец). НЕ входит в operating-тотал «Расходы»; показывается отдельным блоком **«Капитал/распределение»**; входит в «общий минус от котла» (Итого отток), НЕ в «Операционные расходы». Двойного счёта нет. **accepted.**
  2. **Канон списка исключений дашборда (6):** Перемещение исходящий, Благотворительность, Возврат займа собственнику, Выплата тела кредита, Проценты по кредиту, **Вывод прибыли**. «Возврат» — фантом, удаляется из populated-доков. `ADR-006 §3` (append-only, текст не редактируется) корректируется настоящим ADR: «Возврат»→«Вывод прибыли». **accepted.**
  3. **«Налоги и сборы»** (2 168 917,60, май) = **начисление П&Л**, не касса. Отдельной П&Л-вьюхи в дашборде НЕТ (владелец) → показывается на «Расходах» строкой с меткой **«начислено, не платёж»** (сноска), входит в тотал. Источник = **2-й** (ручной/отдельный сид начисления из П&Л); cash-«Налоги» из `fact_payments` **подавляется**. Вынесена из cash exact-match → **постоянное зарегистрированное исключение** сверки (проверка против accrued-строки). Ops-хвост: рекуррентный ручной ввод — назначить владельца числа + каденс (помесячно). **Направление accepted**; ФОРМА механики (UNION в марте / override / suppress) — гейт **E1-T2 шаг-5** → задача **E1-T2-MECH-TAX**.
  4. **Канон-счётчик:** дашборд-«Расходы» = **26 операционных категорий** (`client-27` − «Вывод прибыли»), где «Налоги и сборы» — accrued-special, «Комиссия» слита в «Расходы маркетплейсов». Все доки ссылаются на этот перечень. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR; коррекция `ADR-006 §3` «Возврат»→«Вывод прибыли» живёт здесь (текст ADR-006 не трогается).
  - `03 §marts.expenses`: status-note ADR-013; §Исключения «Возврат»→«Вывод прибыли»; таблица категорий — убрать ВП (26 строк), «Налоги» → accrued/2-й источник; метка «26 шт» стала верной.
  - `02 §семантика П&Л`: уже содержит ВП в 6 исключениях — консистентно, правки нет; счётчик «30 из 31» — выверить при следующем касании (не блокер).
  - `pnl_2026-05.md`: §п.5 — добавить «Вывод прибыли»; счётчик «31 статья»→«32»; аннотация ВП «вне 26» — уже верна.
  - `expense_articles_client.md`: verbatim-список сохранить; аннотация — ВП реклассифицирована в «Капитал».
  - `07_STATE`: Q-22/Q-24/Q-25/Q-26 → CLOSED; Q-23 → subscope Q-9; gap E1-T2-MECH-TAX.
  - Задачи-правки (прод — отдельно, staging-first): E1-T1-MECH + E1-T2-MECH-TAX.
- **Статус:** §1 accepted · §2 accepted · §3 accepted (направление; форма — E1-T2) · §4 accepted.

## ADR-014 · Стандарт доставки исполняемых артефактов (single-paste, self-resolving secrets, redirect-to-file)
- **Контекст:** E1-T2 — первая версия скриптов требовала ручной вставки токена (`<ВСТАВИТЬ…>`), владелец отклонил формат, попросил зафиксировать правилом (AR-4); лог содержал наложение echo от heredoc в интерактивном Cloud Shell → доп. проверка достоверности по `_METHOD §6` (AR-5). `05` Часть II — заглушка, явно зарезервированная под «стандарт доставки артефактов», наполняется через ADR. Секреты: `11_INFRA_FACTS §секреты` (`msklad-token`, `bakai-fx-token`); проект `msklad-bi-prod` (`00_CHARTER §миссия`).
- **Решение:**
  1. Скрипты — ОДНИМ copy-paste-ready bash-блоком. Резолв секретов inline: `gcloud secrets versions access <secret> --project=msklad-bi-prod`, `<secret>` — имя из `11_INFRA_FACTS §секреты` (не изобретается). Плейсхолдеры `<ВСТАВИТЬ…>` запрещены по умолчанию.
  2. Источник имени секрета/проекта — `11_INFRA_FACTS` / `00_CHARTER`, не догадка. Секрет недоступен через `gcloud` → CONTEXT GAP (`_METHOD §6`), не повод оставить плейсхолдер.
  3. [рекомендуется] Запуск с редиректом: `bash script.sh > run.log 2>&1; cat run.log`. Мотив: избежать наложения echo/heredoc, держать лог в разделимой форме. Не-разделимый/наложенный лог = Untrusted (`_METHOD §6`) → чистый перезапуск.
- **Последствия:** `05` Часть II — наполняется слот «Стандарт доставки исполняемых артефактов».
- **Статус:** accepted (§1/§2 — явный запрос владельца; §3 — адъюдикация архитектора 2026-07-14: принят как дефолтная форма доставки, вето не заявлено; enforcement — через `_METHOD §6`, не отдельный гейт)

## ADR-015 · Формат архитекторской review-очереди (`/reference/architect_review_queue_<date>.md`)
- **Контекст:** владелец попросил впредь выносить открытые вопросы/гэпы отдельным файлом-«задачей на review/decision», а не только строками таблицы `07_STATE` (AR-7).
- **Решение:**
  1. Сессия с открытыми пунктами для архитектора завершается — в дополнение к session-блоку — файлом `/reference/architect_review_queue_<date>.md`: по пункту на решение (контекст · вопрос · варианты · рекомендация разработчика (не решение) · что блокирует).
  2. Инвариант: файл АУГМЕНТИРУЕТ, не ЗАМЕНЯЕТ реестр гэпов `07_STATE §Открытые вопросы` (`_METHOD §11`). Каждый пункт держит Q-id-строку в `07`; резолюции review-очереди уходят в `06` (ADR) и `07` (STATE_PATCH).
  3. Файл session-scoped, датирован, живёт в `/reference` (диагностический артефакт).
- **Последствия:** `05` Часть III — правило handoff'а. Первый экземпляр — `/reference/architect_review_queue_2026-07-14.md`.
- **Статус:** accepted (явный запрос владельца) · **клауза нейминга superseded-in-part по ADR-018 §4** (первый экземпляр физически материализован как `-2.md`, не под un-suffixed именем — см. ADR-018)

## ADR-016 · Root cause Q-27 = currency-conversion баг в core-load (cf-finance), СИСТЕМНЫЙ; форма и приоритет фикса
- **Контекст:** E1-T3 доказательно установил (recon_vyvod_pribyli_2026-05.md §2–4,§7–8): `cf-finance` при загрузке в `core.fact_payments` считает `sum_kgs = sum÷100` БЕЗ `×rate.value` для не-KGS документов — неисполнение уже принятого ADR-010 (чей scope глобален и включает `sum` в `paymentout`/`cashout`). Декомпозиция Δ(A↔B)=890 187,50 + Δ(B↔C)=0,00 = разрыв Q-27 без остатка. Системность: 38/38 (100%) подлежащих загрузке не-KGS документов мая занижены, ≥7 статей, Σ 1 108 075,75 KGS. Замеренный класс документов — `paymentout` (все 65 non-KGS мая); `cashout` в выборке не-KGS документов не дал.
- **Решение:**
  - §1 Root cause Q-27 = currency-conversion в Layer B (core-load, cf-finance), НЕ в `sq_marts_expenses`. По закону-следствию (`00_CHARTER` §главный принцип, ADR-002) — фикс-форвардом в Layer B. **accepted** (доказано фактом).
  - §2 Механизм СИСТЕМНЫЙ, не ВП-специфичный: ≥7 категорий `marts.expenses` занижены, Σ 1 108 075,75/май. Витрина для инвестора (`00_CHARTER` §миссия) в текущем виде занижает расходы по этим категориям — цифры НЕ предъявлять инвестору как финальные до `E1-T3-MECH-FX`. Системность закрыта ФАКТОМ (recon §8 + настоящий §2), отдельный Q-номер не заводится. **accepted**.
  - §3 Фикс-форвард = ENFORCEMENT ADR-010 в cf-finance: обязательное `minor_units ÷ 100 × rate.value` документа (если задан; иначе текущий курс) для всех не-KGS inbound-сумм на загрузке в core, по `paymentout` и `cashout` (охват cashout — из глобального scope ADR-010, forward-корректность, НЕ из замера E1-T3). Настоящий ADR НЕ переопределяет ADR-010, а фиксирует его неисполнение в проде и директиву на приведение в соответствие. **accepted** (директива).
  - §4 Форма: ОТДЕЛЬНАЯ задача `E1-T3-MECH-FX` (не пункт E1-T1-MECH). Основания: другой слой (код cf-finance vs SQL SQ), другой блокер-профиль (гейт Q-3 — код cf-finance не версионирован), другая oracle-валидация (re-diff core vs live API). Staging-first — ТРЕБОВАНИЕ. Гейт: discovery-бриф извлечения/версионирования cf-finance-кода (двигает Q-3) до написания фикса. **accepted**.
  - §5 Приоритет: `E1-T3-MECH-FX` ВЫШЕ E1-T1-MECH — по глубине слоя (core-load питает все марты) и радиусу (≥7 категорий; потенциально весь не-KGS во всех core.fact_*, если converter общий — не подтверждено, Q-3), не по величине суммы. НЕ stop-world: discovery cf-finance (Q-3) и E1-T3-MECH-FX идут своим треком; E1-T1-MECH диагностика/дизайн — параллельно. Порядок CUTOVER гейтится Q-31 (маршрутизация E1-T1-MECH через cf-finance → cutover после currency-fix; иначе независимо). **accepted**.
  - §6 Scope доказанного vs отложенного: доказано paymentout-занижение (май). Не измерено: cashout / другие месяцы / весь март / sales-сторона → Q-30 (DEFER, owner-gated). Находка поднимает приор Q-19 (÷100-без-×rate на sales-стороне — тот же класс) — рекомендован к приоритизации вместе с Q-30. **accepted**.
- **Последствия (blast radius):**
  - `06`: настоящий ADR-016 (append). Кросс-ссылка на ADR-010 (enforcement), ADR-002 (фикс-форвард), ADR-012 §4 (loss/commission — ортогональный дефект того же марта).
  - `07_STATE`: Q-27 → хвост «ADR-016 (proposed)» заменён на «ADR-016 (accepted)»; строка E1-T3 — аналогично + «review-очередь адъюдицирована»; заведена задача-строка E1-T3-MECH-FX; заведён Q-31; Q-19 — пометка «приор повышен ADR-016 §2»; Q-30 — «owner-gated, потенциально project-wide»; B-6 → снят по приоритету (остаётся Q-31 по порядку cutover).
  - `04_ROADMAP` Epic-1: последовательность требует пересмотра — E1-T3-MECH-FX (с discovery-префиксом Q-3) как приоритетный корректностный фикс; E1-T1-MECH параллельно на диагностике; порядок cutover — по Q-31. (Детализация будущих задач — по anti-improvisation, только по факту генерации.)
  - Клиентские swing-цифры (ADR-013) и любые расходные цифры дашборда — заморожены до `E1-T3-MECH-FX` (шире, чем ВП-swing ADR-013: занижение по ≥7 категориям).
- **Статус:** §1 accepted · §2 accepted · §3 accepted · §4 accepted · §5 accepted · §6 accepted.

## ADR-017 · Хранение исходников пайплайна: провенанс (снапшот) ≠ живой VC (отдельный репо); decision-половина Q-3
- **Контекст:** E1-T3-D-CFSRC извлёк `cf-finance` (rev `00006-piv`) снапшотом в `/reference/code/cf-finance/` (`MANIFEST.md`; disk==deployed для кода подтверждён `diff -rq`, расходится только `.gcloudignore` — build-файл). Review-очередь `-3` Пункт 1 (RQ-1) поставила выбор (a) отдельный код-репо / (b) внутри `holika` / (c) снапшот-only. Доп. факт: локальный `.git` в `/home/ilyasbazarov4/cf-finance/` (не исследован — `git log`/`remote` не читались). sha256: `main.py` ✅, `requirements.txt` ✅, `patch_main_finance.py` ❌ (2615 vs 2612 б, транскрипция; НЕ конвертер-файл). Исходная формулировка «RQ-1 гейтит `E1-T3-MECH-FX`» переоценена (§3).
- **Решение:**
  - §1 (a)/(b)/(c) смешивают два концерна — развести: **(A)** провенанс для доков/ADR vs **(B)** живой version control кода. **accepted.**
  - §2 **Провенанс (A)** = снапшот-per-revision `/reference/code/<cf>/` + `MANIFEST` (текущий механизм, `_METHOD §11`). Остаётся канон; для провенанса это НЕ «(c) недостаточно», а корректно и достаточно. Каждый значимый деплой → новый снапшот (append). Байт-точность снапшота — из `function-source.zip` / on-disk, **НЕ** из ручной транскрипции лога (закрывает риск класса `patch_main_finance.py`). **accepted.**
  - §3 **De-gate:** место-хранения НЕ гейтит `E1-T3-MECH-FX`. Реальный гейт фикса = байт-точный `main.py` (конвертер-локус `main.py::run_etl()`), верифицирован (sha256 `7a5d8dcd…181d`, disk==deployed). `patch_main_finance.py` (не верифицирован) — не конвертер (`try/except` на `trigger_marts()`), к фиксу нерелевантен. Остаётся staging-first (ADR-016 §4) + `expand=rate` (RQ-2) первым внутренним шагом. **accepted.**
  - §4 **Живой VC (B)** = **отдельный код-репо (вариант a)**; (b) внутри `holika` **отклонён**: `holika` — docs-репо с единой дисциплиной изменений (модель не пишет прямо, правки через session-блок + человек, append-only ADR, `_METHOD §10`); код меняется по деплой-каденсу иными механиками — `/pipeline-src/` внутри = инородное тело с чужой скоростью/моделью изменений. Отдельный репо держит docs-механику доступа (`_ARCHITECT §3`, `_APPLIER`, single-repo) чистой; код пиннится по SHA редко (только в код-правящих брифах) — доп. SHA там явная малая цена. **accepted.**
  - §5 **Приоритет/гейт:** (B) — standalone infra-задача **низкого приоритета, вне критического пути Epic-1, НЕ блокер** `E1-T3-MECH-FX` (задача `CODE-REPO-STANDUP` в `07`). 1-й шаг — discovery локального `.git` (`git log`/`git remote -v`) → seed-from-git vs seed-from-zip; байт-точный seed = `function-source.zip`, не транскрипция. Детализация — по факту генерации (anti-improvisation). **accepted.**
  - §6 **Прецедент:** §2+§4+§5 — конвенция для ВСЕХ CF; исполнение per-CF, сейчас только `cf-finance`. Residual Q-3 (`load_invoices.py`, `cf-facts`, `cf-dim`, `cf-fx`, `cf-dq`, `cf-inventory`, `cf-alert`) — снапшот + добавление в код-репо при их discovery, не пред-планируется. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-017 (append). Кросс: `_METHOD §10/§11`, ADR-016 §4 (staging-first/гейт), ADR-004 (URL/SHA код-репо → `11` при standup).
  - `07_STATE`: RQ-1 → CLOSED; Q-3 decision-половина (cf-finance) → CLOSED (residual Q-3 остаётся); строка `E1-T3-MECH-FX` — снять гейт «место хранения», оставить staging-first + `main.py` verified + `expand=rate` (RQ-2); заведена задача-строка `CODE-REPO-STANDUP`; Текущий фокус переписан; RQ-3 (`main.py.bak`) без изменений (DEFER).
  - `/reference/code/`: дисциплина «снапшот из zip/disk, не транскрипция» — применяется со следующего снапшота (не ретро).
  - `E1-T3-MECH-FX`: после деплоя фикса (новая ревизия) — снапшот новой ревизии в `/reference/code/cf-finance/` (§2).
- **Статус:** §1–§6 accepted.

## ADR-018 · Нейминг файлов review-очереди: обязательный ординал `-N` (≥1); grandfather существующих; правка клаузы ADR-015
- **Контекст:** ADR-015 задал `/reference/architect_review_queue_<date>.md` (один файл/дата, без суффикса на несколько сессий/день). Коллизия 2026-07-14: старые доки (`ADR-015 §Последствия` «Первый экземпляр — …_2026-07-14.md»; `07_STATE` строка E1-T3) ссылаются на un-suffixed `…_2026-07-14.md`, которого физически НЕТ; реальные файлы — `…-2.md` (адъюдикация ADR-016) и `…-3.md` (ADR-017/RQ-2/RQ-3). RQ-4. `05 Часть II` код-стандарт — заглушка.
- **Решение:**
  - §1 Конвенция (forward): `/reference/architect_review_queue_<date>-<N>.md`, ординал `N` **обязателен**, старт с `1`, инкремент на сессию в пределах даты. Bare-date форма (`…_<date>.md`) запрещена. `N` = порядок сессии дня (самодокументирует порядок). **accepted.**
  - §2 Datetime-штамп отклонён: над `-N` не даёт выигрыша в сортировке (оба сортируются), дороже на ручную диктовку в SESSION_LOG, ошибкоёмче. **accepted (выбор `-N`).**
  - §3 Существующие `…-2.md`/`…-3.md` — **grandfather, НЕ ретро-переименовывать**: append-only ADR уже цитируют эти имена (ADR-016 из `-2`, ADR-017 из `-3`); переименование создаст висячие ссылки в неизменяемом `06`. **accepted.**
  - §4 Висячая ссылка: un-suffixed `…_2026-07-14.md` — имя, никогда не материализованное. `07_STATE` (LIVING) строка E1-T3 → указать на физический `…-2.md`. `ADR-015 §Последствия` (append-only) не редактируется — настоящий ADR помечает его клаузу нейминга `superseded-in-part`. **accepted.**
- **Последствия:**
  - `06`: ADR-018 (append); клауза нейминга ADR-015 → `superseded-in-part` (пометка живёт здесь, текст ADR-015 не трогается).
  - `05 Часть III` §«Архитекторская review-очередь (ADR-015)» — добавить правило обязательного `-N` (SEMI-STABLE, через этот ADR).
  - `07_STATE`: RQ-4 → CLOSED (ADR-018); строка E1-T3 — исправить ссылку на `…-2.md`.
- **Статус:** §1–§4 accepted.

## ADR-019 · `sq_audit_dim_products_snapshot`: дрейф = `weight`; фикс — переупорядочение цели (`snapshot_at` первой колонкой)
- **Контекст:** человек обнаружил красный SQ в консоли BQ DTS 2026-07-17. Config `69fc93d1-0000-2d64-bdd1-30fd381336b4` = `sq_audit_dim_products_snapshot` (`11_INFRA_FACTS §SQ`). Ошибка неизменна на каждом прогоне: `Inserted row has wrong column count; Has 15, expected 14 at [2:1]`. SQL (`/reference/sql/sq_audit_dim_products_snapshot.sql`, снапшот 2026-07-07, D5 @ `805450a`): `INSERT INTO audit.dim_products_snapshots SELECT *, CURRENT_TIMESTAMP() AS snapshot_at FROM core.dim_products`. Полная фактура и провенанс → `/reference/sq_audit_dim_products_drift_2026-07-17.md`. Статус FAILED был известен репо (`11 §SQ`: «as-is, не чинить — вне scope ADR-008/M-P4-11a») — это было scope-ограничение сессии M-P4-11a, не решение смириться.
  - §1 **Факт дрейфа (Q-32 CLOSED).** Дельта = единственная колонка `weight` FLOAT64 (`core.dim_products` поз.14, в цели отсутствует). Колонки 1–13 идентичны по имени, типу и позиции; `snapshot_at` TIMESTAMP занимает поз.14 цели. `02 §схемы dim` (14 колонок) согласован с живой `core.dim_products` — расходится цель, не док.
  - §2 **Хронология (Q-33а, Q-36 CLOSED).** 2026-05-07 — заводка конфига + 3 ручных прогона вне DTS (13 389 = 3 × 4 463; = `updateTime` из `/reference/sql/README` §Наблюдения п.2, который склеивал три события в одно). 2026-05-08 04:00 — первый прогон по расписанию. **2026-06-03 — первое падение.** Дыра = 2026-06-03…2026-07-17 = **45 суток**, подтверждена трижды независимо. Срезы за окно **безвозвратны**: `dim_products` в ядре без SCD2 (`ADR-008 §Контекст`), снапшот — единственный носитель истории (`ADR-008 §3`); задним числом не доливаются. Ущерб — потеря опциона, не операционный отказ (`ADR-008 §2`: потребителя нет), но накапливается монотонно.
  - §3 **Форма фикса = E: пересоздать `audit.dim_products_snapshots` с `snapshot_at` ПЕРВОЙ колонкой; SQ переписать на `SELECT CURRENT_TIMESTAMP() AS snapshot_at, * FROM core.dim_products`.** Новые колонки источника растут в хвост обеих таблиц ⇒ добавления переживаются автоматически, без участия человека. Отклонены:
    - **A** (`ALTER … ADD COLUMN weight`, сохранить `SELECT *`) — **не чинит**: `ADD COLUMN` кладёт `weight` на поз.15, `snapshot_at` остаётся на поз.14, `INSERT … SELECT *` матчит по позиции ⇒ FLOAT64 → TIMESTAMP, падение сохраняется.
    - **B** (явный именованный список колонок) — требует **вспомнить** дописать новую колонку в список. Наблюдённый отказ состоит ровно в том, что зависимость `core.dim_products` → `audit.dim_products_snapshots` нигде не объявлена и её не удержали в голове. B перекладывает на человека шаг, на котором человек уже споткнулся.
    - **C** (B + drift-assert по `INFORMATION_SCHEMA`) — требует **читать журнал падений**. Установлено фактом: журнал не читали 45 суток (Q-34 → DEFER, ADR-020 §3). Assert без наблюдателя воспроизводит ровно тот отказ, который чинится.
    - **D** (ретайр SQ) — противоречит `ADR-008 §3` (латентный SCD2-актив), не обоснован.
    **Принятый размен E, осознанно:** перестановка колонок источника с совместимыми типами пройдёт тихо и запишет значения не в те колонки. Экспозиция узкая: `core.dim_products` наполняется MERGE (`ADR-008 §Контекст`), MERGE колонки не переставляет — перестановка требует осознанного пересоздания таблицы. Редкое намеренное действие против частого рефлекторного `ADD COLUMN`. Переименование под E безвредно (те же данные в ту же позицию); удаление колонки и смена типа падают громко.
    **Триггер пересмотра в сторону C:** выход Q-34 из DEFER.
  - §4 **Приоритет.** НЕ блокер Epic-1 и НЕ гейт Step 6 `E1-T3-MECH-FX`: `audit.*` не на пути `cf-finance`/мартов, потребителя нет. Порядок: **cutover Step 6 → `AUDIT-SNAPSHOT-FIX`**.
  - §5 **Retention (`ADR-008 §4`) закрыт количественно:** 129 905 строк за 27 суток, из них 1 сутки ad-hoc ⇒ ~4 492 строк/сут, ~1,64 млн/год, порядок сотен МБ. `DEFER` подтверждён цифрой.
  - §6 **Q-37 (латентный дефект актива):** по `DATE(snapshot_at) = 2026-05-07` лежат три идентичных среза. Сегодня безвреден; срабатывает при промоушене таблицы в consumer-facing роль. **Не чинить превентивно**, `AUDIT-SNAPSHOT-FIX` этого не касается.
- **Последствия:**
  - `11 §SQ` — ячейки строки `69fc93d1-…` переписаны; пометка «as-is, не чинить» снята.
  - `07_STATE` — Q-32/Q-33/Q-36 CLOSED; Q-37/Q-39 DEFER; задача `AUDIT-SNAPSHOT-FIX` (TODO).
  - `ADR-008` не редактируется (append-only): §2 и §3 остаются в силе и служат основанием §3/§5 настоящего ADR; снимается только пометка о состоянии SQ в `11`.
  - Прод-SQL и миграционный DDL не пишутся этой сессией (роль архитектора, `_METHOD §2`) — предмет task-brief.
- **Статус:** §1–§6 accepted.

## ADR-020 · Состояние SQ-флота: 12/13 зелёные; наблюдаемость прогонов → DEFER; выявлена март-на-март зависимость без заданного порядка
- **Контекст:** read-only снятие состояния всех 13 transferConfig, 2026-07-17 (`bq` 2.1.33). Провенанс → `/reference/sq_fleet_health_2026-07-17.md`. Поводом послужил дефект ADR-019, проживший 45 суток невидимым и найденный глазами в консоли, а не алертом.
  - §1 **Состояние флота (Q-33б CLOSED).** Сняты 13/13. Красный ровно один — `sq_audit_dim_products_snapshot` (45 не-OK / 50). Остальные 12 — ноль не-OK. **Ограничение — часть решения, а не сноска:** `0/50` означает «нет падений в последних 50 прогонах», не «никогда». Глубина ~50 суток у восьми configs; вся удерживаемая история у `sq_marts_weight_flow` (0/45), `sq_marts_expenses` (0/43), `sq_marts_customer_invoices_ar` (0/43); **у `sq_marts_sales_overview` — только ~4 суток** (интервал `every 2 hours` × 50 прогонов). Доснятие `sales_overview` с бо́льшим `--max_results` — опция, не гейт.
  - §2 **`sq_marts_expenses` — ноль падений за ВСЮ историю конфига** (43 прогона = ровно 2026-06-05…2026-07-17 при creationTime 2026-06-05, `ADR-012 §Контекст`); прогон 11:10 UTC подтверждает задокументированное `~11:10 UTC` (`11 §SQ`) третьим независимым источником. Витрина «Расходы» не замерзала ни разу.
  - §3 **Q-34 (наблюдаемость прогонов SQ-флота) → DEFER.** Гэп реален: дефект ADR-019 прожил 45 суток невидимым; задокументированный DQ-гейт (`03 §DQ`, 6 чеков) и пороги свежести (ADR-007) наблюдают только `staging`/`core` и состояние прогонов `transferConfig` не покрывают; прецедент того же класса в репо — `11 §CF`, `finance-daily-update`, `maxRetryDuration=0s` (RB-42). Полный конфиг `cf-alert` не задокументирован (**Q-12**) ⇒ «алерта нет вообще» **не утверждается**. Основания DEFER: (а) эмпирика §1–§2 — молча падала ровно одна таблица без потребителей, риск для инвесторской поверхности (`00_CHARTER §миссия`, `ADR-002`) не подтвердился; (б) механизм Q-34 наблюдает `state=FAILED` и **не поймал бы** §4 настоящего ADR, где отказ выглядит как `SUCCEEDED` ⇒ покрывает не тот класс молчания; (в) Epic-1 не закрыт, страховка не вытесняет критический путь. **Триггер промоушена из DEFER (обязателен):** любой новый красный в SQ-флоте **ИЛИ** закрытие Epic-1 **ИЛИ** появление первого потребителя у `audit.*`.
  - §4 **Q-38 — единственная март-на-март зависимость.** Проверены все 13 SQL `/reference/sql/*.sql` @ `4fce6a8` на `FROM`/`JOIN` по `marts.*`: ровно одно попадание — `sq_marts_gmroi_by_folder` (Config `6a004e88-…`) читает `msklad-bi-prod.marts.gmroi` (Config `6a006664-…`); остальные 11 март-SQ читают только `core.*`, три audit-SQ — `core` → `audit`. Порядок на 2026-07-17: `gmroi` 08:03 UTC → `gmroi_by_folder` 12:02 UTC — верный, запас ~4ч, но **не задан ничем**: оба `every 24 hours` (DTS-дефолт от собственного `creationTime`), зависимость в конфиге не объявлена, оркестрации нет; Config ID зависимого меньше Config ID источника ⇒ заведён раньше. При дрейфе якоря/паузе/пересоздании/замедлении `gmroi` за 12:02 `gmroi_by_folder` **успешно** агрегирует вчерашний `marts.gmroi`; `03 §DQ` этого не видит; `_mart_refreshed_at = CURRENT_TIMESTAMP()` проштампует свежесть агрегации поверх устаревших данных. Форма фикса (оркестрация · assert на `_mart_refreshed_at` источника · слияние в один SQ) **не назначается** — anti-improvisation. **DEFER**: не блокер Epic-1, `gmroi_by_folder` вне Трека A/B.
  - §5 **Q-39 — оптовое распространение фикса ADR-019 на `sq_audit_dim_counterparties_snapshot` / `sq_audit_dim_employees_snapshot` ОТКЛОНЕНО.** Паттерн у всех трёх идентичен (`INSERT INTO audit.X_snapshots SELECT *, CURRENT_TIMESTAMP() … FROM core.X`) и зелены они лишь потому, что их источники ещё не получали новых колонок — то есть это две взведённые мины того же класса. Но **ценность активов различна**, и это меняет вопрос с «как чинить» на «зачем он»: `core.dim_counterparties` несёт SCD2 нативно (`02 §схемы dim`: `scd2_valid_from` / `scd2_valid_to` / `scd2_is_current`, «JOIN всегда с `AND scd2_is_current = TRUE`») ⇒ история уже в ядре, снапшот её дублирует, и довод `ADR-008 §3` («снапшот = единственный носитель») к нему **не применим**; `core.dim_employees` в `02` не документирован вовсе (входит в **Q-4**) ⇒ решать нечего. Чинить оптом по совпадению формы SQL = улучшать по инерции. **DEFER**, решать отдельно и по каждому.
  - §6 **Q-21 partial-close.** `sq_marts_customer_invoices_ar` (Config `6a23f3ea-…`): фактическая каденция — **ежедневная**, якорь ~10:00 UTC (43 прогона подряд, 0 не-OK). Гипотеза Q-21 («вероятно тоже ежедневный автозапуск») подтверждена фактом; свой якорь, отличный от `sq_marts_expenses` (~11:10 UTC) — тот же паттерн DTS-дефолта от `creationTime`, что установлен ADR-012. **Остаток Q-21:** поле `schedule` в transferConfig не снято (`bq show` не выполнялся) — DEFER, не блокер.
- **Последствия:**
  - `11 §SQ` — добавлена строка `sq_marts_customer_invoices_ar` с фактической каденцией (дом ответа Q-21 по её же формулировке).
  - `07_STATE` — Q-33 CLOSED; Q-34/Q-35/Q-38/Q-39 DEFER; Q-21 partial-close; задача `SQ-FLEET-HEALTH` DONE.
  - **Q-35:** `10_OPS_PLAYBOOK.md` объявлен в `00_CHARTER §карта документов` стр.52 и в `MIGRATION_MAP` §Целевой набор, на него ссылаются `11 §секреты`, `03 §DQ`, `ADR-008 §4`, `02:241` — файла в репо @ `4fce6a8` **нет (404)**, M-P4 закрыт DONE 22/22 без него. Дом ответа Q-34 не существует. DEFER, не создавать этой сессией.
  - `ADR-008` не редактируется; §2 (нет потребителя) — основание §3(а).
- **Статус:** §1–§6 accepted.

## ADR-021 · Процессные правила, вынесенные из инцидента SQ-AUDIT-DRIFT: многоходовая сессия и «успех ≠ факт»
- **Контекст:** сессия SQ-AUDIT-DRIFT шла в четыре хода discovery (выдача диагностики → лог → следующая диагностика) и породила **четыре** session-блока, каждый — дельтой к результату предыдущего, ни один не закоммичен между ними. При попытке применения applier корректно встал на «СТРУКТУРНОЕ НЕСООТВЕТСТВИЕ» (`_METHOD §124`) и отказался достраивать. Прошёл ровно один патч из четырёх — создание нового файла, т.е. единственный, не требовавший предыдущего состояния. Побочно: диагностика дважды вернула ложную картину (`NO_RUN` по всем 13 configs; затем `JSON-PARSE-FAIL` по 10) при `rc=0` и пустом `stderr` — прод был в порядке оба раза.
  - §1 **Многоходовая сессия (конвенция, → `05` Часть III).** `05` Часть III предписывает «в конце каждой сессии — ОДИН блок», но не покрывает чат, живущий через несколько циклов «дал скрипт → получил лог»; для discovery-петли (`_METHOD §11`) это норма, и здесь метод имел дыру. Правило: session-блок адресуется **последнему закоммиченному SHA**, а не результату предыдущего блока; патч-цепочка на собственные неприменённые патчи запрещена; якорь патча — только текст, существующий в репо по SHA. **Следствие:** ADR/Q, выданные, но не закоммиченные, — черновики; append-only (`_METHOD §6`) защищает журнал, а не черновики, поэтому консолидация невыпущенных ADR разрешена, а ретро-правка выпущенных — нет (прецедент неприкосновенности: `ADR-009 §1`, помечен `partially superseded`, но не переписан — потому что был закоммичен и цитируется).
  - §2 **«Успех инструмента + пустой результат ≠ факт» (конвенция, → `05` Часть I).** Существующее правило `★ Untrusted-вывод терминала` ловит лог **видимо** повреждённый («задвоенные команды, обрывки прошлых строк, рассинхрон тела и статуса») — и оба раза сработало. Но `NO_RUN ×13` был **чист на вид** и ложен; пойман лишь потому, что во втором блоке того же лога случайно оказалась противоречащая команда. Без неё сессия доложила бы владельцу, что весь SQL-флот никогда не запускался — уверенно и неверно. Правило: `rc=0` при нулевой/пустой/непарсимой выдаче — **гэп наблюдения**, пока обратное не доказано независимым способом; отсутствие данных в выводе не есть доказательство отсутствия данных в системе.
  - §3 **Правило разбора `bq ls --format=prettyjson` в стандарт НЕ вносится.** Механизм установлен фактом (при усечении по `--max_results` печатаются два JSON-документа: `[{"nextPageToken": …}]` длиной 51 байт, затем страница; `docs=2` ровно у тех 10 configs, где `runs=50`), но это поведение конкретной версии утилиты (2.1.33): протухнет, а протухшее правило в источнике истины хуже отсутствия правила. Факт уже закаптурен в `/reference/sq_fleet_health_2026-07-17.md` §Ф-4 — этого достаточно. `05` Часть II остаётся заглушкой.
  - §4 **Blast radius §2 — Q-40.** `05` Часть I — доменно-нейтральное зеркало `_METHOD §6`; добавление правила только в `05` создаёт расхождение с каноном, а правка `_METHOD` задевает проекты за пределами holika (методология переносимая). Настоящим ADR правило вводится как **holika-локальное уточнение** в `05` Часть I; промоушен в канон `_METHOD §6` — отдельный вопрос **Q-40**, DEFER до того, как правило себя докажет.
- **Последствия:** `05_CONVENTIONS.md` — две вставки (Часть I, Часть III); Часть II не трогается. `07_STATE` — Q-40 (DEFER). Апрув владельца на обе конвенции получен явно в сессии 2026-07-17 (`_METHOD §124` соблюдён).
- **Статус:** §1–§4 accepted.

## ADR-022 · IAM-lockdown публичных write-эндпоинтов CF в прод-таблицы (cf-finance); TD-SEC-01 подтверждён и устранён точечно
- **Контекст:** в Шаге D cutover-сессии `E1-T3-MECH-FX-CUTOVER` (2026-07-20) вскрыт активный инцидент — `cf-finance` задеплоена с `--trigger-http --allow-unauthenticated` (TD-SEC-01, `11 §CF` стр.16/26). Анонимные вызовы с Google-инфраструктурных IP каждые 2–5 мин полностью перезаписывали `core.fact_payments` активным кодом функции, независимо от паузы Scheduler. Прямое нарушение границы контракта (`00_CHARTER §главный принцип`, ADR-002): бесконтрольная перезапись поверхности реконсиляции с оракулом. Ремедиация применена в проде в ходе сессии (через IAM-binding, НЕ редеплоем): снят `roles/run.invoker` для `allUsers`; создана привязка `etl-sa@msklad-bi-prod.iam.gserviceaccount.com` → `roles/run.invoker`; Scheduler-джоб `finance-daily-update` переведён на OIDC (`--oidc-service-account-email=etl-sa@…`). Верифицировано логами: анонимный запрос → `403`; Scheduler → успешный авторизованный вызов.
- **Решение:**
  - §1 Публичный неаутентифицированный доступ к CF, являющимся write-эндпоинтами в прод-таблицы, закрывается ПО УМОЛЧАНИЮ (политика, не point-fix). Read/utility-эндпоинты вне scope политики — их классификация = Q-42.
  - §2 Ратификация live-ремедиации `cf-finance` (снятие `allUsers`, `etl-sa` `run.invoker`, Scheduler OIDC) как канонической модели вызова `cf-finance`.
  - §3 TD-SEC-01: статус «потенциальный риск (замороженный источник)» → «подтверждённый инцидент; для `cf-finance` устранён». Дом статуса — `11 §CF` (не `00_CHARTER`: TD-SEC-01 там отсутствует).
  - §4 Ops-следствие (флаг, не гейт): ручной/ad-hoc вызов `cf-finance` теперь требует identity-token, не анонимного `curl`. Нота — в `11 §CF` (`10_OPS_PLAYBOOK` не существует, Q-35 DEFER).
- **Последствия:**
  - `06`: настоящий ADR-022 (append). Кросс: ADR-002 / `00_CHARTER §главный принцип` (граница контракта), ADR-004 (`11` — LIVING, per-deploy), Q-42 (флот), Q-43 (тот же джоб).
  - `11 §CF` (cf-finance deploy-блок): из канонической deploy-команды удалить `--allow-unauthenticated` — иначе следующий редеплой заново откроет дыру (live-lockdown был IAM-binding, не редеплой). Пометка стр.26 переписана; строка Scheduler дополнена OIDC-фактом; добавлена ops-нота §4.
  - `11 §IAM` (было пусто, стр.53): внесён факт — `etl-sa@msklad-bi-prod.iam.gserviceaccount.com` → `roles/run.invoker` на `cf-finance`; `allUsers`-привязка снята; верификация 2026-07-20 (анон→403, Scheduler→200).
  - `07_STATE`: TD-SEC-01/Q-42/Q-43/Q-31/«Текущий фокус» — см. STATE_PATCH сессии `E1-T3-CUTOVER-REVIEW-ADJ`.
- **Статус:** §1 accepted · §2 accepted · §3 accepted · §4 accepted.

## ADR-023 · attemptDeadline=180s — клиентский таймаут Scheduler, НЕ обрывает серверную обработку; raise = гигиена, не safety-гейт
- **Контекст:** Q-43 спрашивал, обрывает ли истечение клиентского `attemptDeadline` серверную обработку `cf-finance` на середине MERGE (риск частичной записи в `core.fact_payments`). Единственный чистый прогон после IAM-lockdown ADR-022 (finance-daily-update, 2026-07-20T21:00Z / 03:00 Asia/Bishkek) триангулирован тремя независимыми источниками (провенанс `/reference/fin_sched_deadline_probe_2026-07-21.md`): Source A (Scheduler) — DEADLINE_EXCEEDED/504 @ T+180s, ретраев нет (maxRetryDuration=0s); Source B (Cloud Run, rev 00012-cik) — 200, latency 807s, все 5 маркеров run_etl(), SIGTERM отсутствует; Source C (BQ, решающий) — MERGE-job DONE, error_result=null, inserted=1449/updated=4937 @ T+~13м25с (~10 мин ПОСЛЕ клиентского 504).
- **Решение:**
  - §1 `attemptDeadline=180s` — чисто клиентский таймаут HTTP-вызова Scheduler; серверную обработку Cloud Run НЕ обрывает и не может в этой конфигурации (клиентская HTTP-отмена не убивает продолжающийся серверный запрос; инстанс не терминирован). Риск частичной записи из-за истечения этого дедлайна — фактом НЕ подтверждён. **accepted (доказано триангуляцией A/B/C).**
  - §2 Область вердикта УЗКАЯ: §1 снимает статус safety-гейта с raise `attemptDeadline`; НЕ утверждает невозможность частичной записи вообще (n=1, это механистическая констатация одного контрпримера, не статистика). **accepted.**
  - §3 Несущая гарантия целостности `core.fact_payments` не зависит от дедлайна и стоит независимо: (а) Cloud Run не обрывает in-flight запрос при отключении клиента; (б) запись = атомарный BQ MERGE + идемпотентный ghost-DELETE (ADR-011) — обрыв между MERGE и DELETE оставляет гхосты, снимаемые DELETE следующего прогона (восстановимо, не коррупция). **accepted.**
  - §4 Raise `attemptDeadline` переквалифицирован из «обязательный пре-cutover safety-гейт» в **гигиену/шумоподавление ложных 504 в истории Scheduler**. Ложный 504 функциональных последствий не имеет: maxRetryDuration=0s ⇒ ретрая нет; монитор на 5xx Cloud Run, а Cloud Run вернул 200 ⇒ алерт не поднимается; 504 живёт только в истории джоба. **accepted.**
  - §5 Форма/значение: владелец адаптировал рекомендацию архитектора (2026-07-21) — значение = **1800s** (= серверный `--timeout` и одновременно потолок `attemptDeadline` для HTTP-джоба Scheduler ⇒ ложный 504 исключён by construction, кроме реального серверного таймаута). Исполняет ЧЕЛОВЕК (архитектор не запускает); применяется Scheduler-side `gcloud scheduler jobs update http … --attempt-deadline=1800s` (§6), с read-back верификацией `attemptDeadline`+OIDC. **accepted (владелец).**
  - §6 Guardrail применения: raise — правка Scheduler-джоба (`jobs update`, partial-update ⇒ OIDC сохраняется), НЕ редеплой cf-finance (ADR-022: lockdown allUsers был IAM-binding; редеплой рискует переоткрыть дыру / потерять OIDC). Тайминг развязан от Step 6 cutover — НЕ гейт, применимо в любой момент. После применения — верификация read-back'ом (ADR-021 §2), затем патч значения в `11 §CF` (LIVING, ADR-004). **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-023 (append). Кросс: ADR-011 (ghost-DELETE идемпотентность), ADR-021 §2 (верификация read-back; здесь — положительно подтверждённый случай, не «пусто=гэп»), ADR-022 (не редеплоить / OIDC), Q-43/Q-44/Q-45 (07_STATE).
  - `07_STATE`: Q-43 → CLOSED (fact + decision); значение raise 1800s зафиксировано, исполнение — человеком.
  - `11 §CF`: после подтверждённого `jobs update` — обновить `attemptDeadline=180s` → `1800s` в строке Scheduler (Scheduler-side; deploy-блок cf-finance не трогать).
- **Статус:** §1–§6 accepted.

## ADR-024 · Маршрут Q-31 — источники `loss`/`commissionreportin` заводятся новым НЕЗАВИСИМЫМ ингестом (не расширение cf-finance); cutover развязан от E1-T3-MECH-FX
- **Контекст:** E1-T1 Шаг 1 (grounding Q-31) установил фактом (`design_sq_marts_expenses_E1-T1-MECH.md §гейт-факт`; провенанс `bq ls`/`bq show` по всем 4 датасетам audit/core/marts/stg_msklad): `raw.moysklad_loss` и `raw.moysklad_commissionreportin` НЕ существуют нигде. Исходная развилка Q-31 (α — через cf-finance; β — прямое чтение готовых raw) негодна целиком — ни α, ни β не реализованы. Развилка сместилась с «маршрутизации» на «дизайн нового ингеста».
- **Решение:**
  - §1 Источники заводятся новым независимым ингестом — отдельная CF/механизм, НЕ расширение codebase `cf-finance`. **accepted.**
  - §2 Основания (все factual): (1) код cf-finance — в отдельном НЕ поднятом репо (ADR-017 §4/§5, CODE-REPO-STANDUP не исполнен) ⇒ зависимость сейчас нецелесообразна; (2) независимый ингест не наследует баг ADR-016 (÷100 без ×rate.value) — реализует ADR-010 (`sum_kgs = minor_units ÷ 100 × rate.value`, rate.value документа если задан, иначе текущий курс) с первого дня, не fix-forward'ом; (3) cutover развязан от E1-T3-MECH-FX — соответствует ADR-016 §5 (NOT stop-world). **accepted.**
  - §3 Провенанс кода нового ингеста — снапшот-per-revision по ADR-017 §2/§6: байт-точно из function-source.zip/on-disk в `/reference/code/<cf>/`+MANIFEST, не транскрипция. **accepted.**
  - §4 НЕ противоречит ADR-012 §2/§3 («TO-BE raw-март не строится / правится существующий sq_marts_expenses»): новый ингест НЕ заменяет март — питает НОВЫЕ таблицы-источники, которые ЧИТАЕТ существующий sq_marts_expenses через SQL-дельту E1-T1-MECH. Март остаётся sq_marts_expenses. **accepted.**
  - §5 Обязательное раскрытие смешанного состояния на cutover: после cutover E1-T1-MECH-INGEST `marts.expenses` смешана по качеству — loss/commissionreportin корректны с первого дня (§2), но paymentout/cashout несут баг ADR-016 до E1-T3-MECH-FX. Заморозка ADR-016 §2 (не предъявлять инвестору как финальные) на buggy-долю СОХРАНЯЕТСЯ; смешанное состояние раскрывается ЯВНО в момент реального cutover, не implicit. Корректность paymentout/cashout снимается закрытием E1-T3-MECH-FX (гейт — Step 4 dry-compare, Q-41/Q-45, артефакт не подтверждён). **accepted.**
  - §6 Имя новой CF (`cf-finance-ext` в дизайне — условное) НЕ финализируется здесь: нейминг из фактов проекта через ADR (05 Часть II — заглушка), окончательное имя — в task-brief E1-T1-MECH-INGEST; имя, намекающее на родство с cf-finance, под вопросом (§2 — намеренно независим). **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-024 (append). Кросс: ADR-010, ADR-016 §2/§5, ADR-017 §2/§4/§5/§6, ADR-012 §2/§3/§4.
  - `07_STATE`: Q-31 хвост ADR-0XX(proposed)→ADR-024(accepted); B-8 СНЯТ; заведена строка E1-T1; Текущий фокус → генерация E1-T1-MECH-INGEST.
  - `04_ROADMAP` Epic-1: при генерации завести под-задачу `E1-T1-MECH-INGEST` (независимый ингест loss/commissionreportin) под E1-T1; cutover = гейтированная `E1-T1-MECH-CUTOVER`. Детализация — по факту генерации (anti-improvisation), здесь только место в порядке.
  - `/reference/code/`: со стендапа нового ингеста — снапшот его ревизии по §3.
- **Статус:** §1–§6 accepted (RQ-6-E1T1-ADJ, 2026-07-22).

## ADR-025 · SESSION-блок/STATE_PATCH фиксирует роль (`обновил`); «Статусы задач» — Epic-M-scoped, задачи Epic-1+ живут задача-строками в GAP-реестре
- **Контекст:** Applier-сессия E1-T1 (`architect_review_queue_2026-07-21-2.md`, Пункты 1 и 4) уперлась в два расхождения шаблона: (1) header `07_STATE` требует `обновил: <роль> (сессия: <T-id>)`, но шаблон `STATE_PATCH` (05 Часть III / `_METHOD §8`) роль НЕ несёт — Applier не смог подставить роль, оставил header нетронутым (корректно); (2) STATE_PATCH E1-T1 адресовал строку E1-T1 в «Статусы задач», но та scoped на Epic-M (`07_STATE:15`), а parent-задачи Epic-1 живут задача-строками в GAP-реестре (прецеденты E1-T3, E1-T3-D-CFSRC, E1-T3-MECH-FX; согласовано с ADR-015). Оба — гэп шаблона, не выдумка Applier'а.
- **Решение:**
  - §1 `STATE_PATCH` получает обязательное поле `- обновил: <роль> (сессия: <T-id>)` рядом с `updated_at`. Роль — из брифа сессии (E1-T1 → разработчик, 08-бриф §Роль), не угадывается; нет роли в SESSION-блоке → CONTEXT GAP. Applier переносит поле в header `07_STATE`. Правка `05` Часть III (SEMI-STABLE, через настоящий ADR). **accepted.**
  - §2 Размещение (документирует существующую реальность): «Статусы задач» scoped на Epic-M. Задачи Epic-1+ (E1-Tn и дочерние …-MECH/…-INGEST/…-CUTOVER, discovery-задачи) заводятся задача-строками (тип=задача) в «Открытые вопросы / GAP-реестр», НЕ в «Статусы задач». Колонку «Примечание» в «Статусы задач» НЕ добавлять (вариант c Пункта 1 отклонён). **accepted.**
  - §3 Ретро: header, оставленный Applier'ом на `RQ-5-ADR023-ADJ` при уже применённом STATE_PATCH E1-T1, — не ошибка Applier'а (роль была недоступна); заводимая строка E1-T1 фиксирует роль сессии-источника (разработчик). Дальше — по §1. **accepted.**
- **Последствия:**
  - `06`: настоящий ADR-025 (append). Кросс: `_METHOD §8` (шаблон доменно-нейтральный; промоушен правки в `_METHOD` — owner-gated, класс Q-40 — здесь НЕ делается), ADR-015, ADR-018.
  - `05` Часть III: (а) в шаблон STATE_PATCH добавить строку `- обновил: <роль> (сессия: <T-id>)`; (б) добавить примечание о scoping «Статусы задач»/GAP-реестр.
  - `07_STATE`: заведена строка E1-T1 (GAP-реестр); header — на `RQ-6-E1T1-ADJ`.
- **Статус:** §1–§3 accepted (owner может ветировать правку `05`; по существу — приведение доков к уже действующему требованию, не новый выбор).

## ADR-026 · `core.fact_commissionreportin.sum_kgs`: `reward` — с уровня `positions[]`; конвертация обязана нести `× rate.value`; клауза формулы `ADR-006` → superseded-in-part
- **Контекст:** сессия `E1-T1-MECH-INGEST` (2026-07-23) при бэкфилле мая-2026 установила фактом: на документе `entity/commissionreportin` поля `reward` НЕТ — величина живёт на уровне позиций (`positions[].reward`) и агрегируется; `commissionOverhead` — document-level. Сходимость с оракулом точная: «Комиссия» = 438 729,42 (reward 63 311,00 + overhead 375 418,42) против `/reference/pnl_2026-05.md` (seed #2, `ADR-006`). Одновременно вскрыт дефект источника истины: `ADR-006 §Контекст`/`§Решение(1)` и `02 §семантика П&Л` (Источник #2) задают формулу как `reward÷100 + commissionOverhead.sum÷100` — **без `× rate.value`**, что противоречит `ADR-010` (скоуп: все входящие мультивалютные суммы, глобальный inbound-канон) и воспроизводит в доках класс бага `ADR-016`. Исходный пропозал сессии (P1) нёс ту же формулировку («минорные единицы (÷100)») и в той редакции НЕ принят.
- **Решение:**
  - §1 **Locus:** для `entity/commissionreportin` `reward` агрегируется с уровня `positions[]`; `commissionOverhead` — document-level. **accepted** (grounded сходимостью с оракулом до копейки).
  - §2 **Формула-канон:** `sum_kgs = Σ(positions[].reward ÷ 100 × rate.value) + (commissionOverhead ÷ 100 × rate.value)`, где `rate.value` — курс документа, если задан, иначе текущий курс валюты (`ADR-010`). Форма `÷100` без `× rate.value` для этого источника **запрещена**. **accepted.**
  - §3 **Superseded-in-part:** клауза формулы источника #2 в `ADR-006 §Контекст` и `§Решение(1)` помечается `superseded-in-part` настоящим ADR — в части (а) отсутствия `× rate.value` и (б) locus `reward`. Остальной `ADR-006` (двухисточниковая методология, карта категорий дашборда, seed #2) — в силе. Текст `ADR-006` НЕ редактируется (append-only, `_METHOD §6`; прецедент маркировки — `ADR-018 §4`, `ADR-009 §1`). **accepted.**
  - §4 **Открытый факт (не гейт):** литеральная форма document-level поля — `commissionOverhead` (скаляр) или `commissionOverhead.sum` (объект). `02` утверждает второе, пропозал сессии — первое; артефактом не подтверждено ни одно (ссылка «МойСклад API docs» — не артефакт возврата, `ADR-021 §2`; `ADR-017 §2` — байт-точно, не транскрипция). Заведён `Q-49`; закрывается сырым JSON-фрагментом одного документа мая в `/reference`. На числа НЕ влияет: overhead 375 418,42 ≠ 0 доказывает, что задеплоенный код читает верный путь; открыт только литерал в доке. **accepted (классификация гэпа).**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-026 (append); клауза формулы источника #2 в `ADR-006` → `superseded-in-part` (пометка живёт здесь, текст `ADR-006` не трогается).
  - `02_ERP_CONTRACTS §семантика П&Л`, блок «Источник #2» — замена строк формулы и примечания на редакцию §1+§2 (STABLE, через настоящий ADR).
  - `03_PIPELINE_SPEC`: адресат — новая секция `§cf-loss-commission` (по образцу `§cf-finance`/`§cf-fx`); наполнение — по возврату артефакта Шага 6, не сейчас (anti-improvisation; `03` — док исполнения, патчится session-блоком, `05 §Дисциплина изменений`).
  - `07_STATE`: заведён `Q-49`; контрольные цифры `E1-T1-MECH-INGEST` — без изменений (сходимость подтверждена).
- **Статус:** §1–§4 accepted.

## ADR-027 · `currency_code` из МойСклад API = `rate.currency.isoCode` (буквенный ISO 4217); числовой `.code` запрещён
- **Контекст:** сессия `E1-T1-MECH-INGEST` (2026-07-23) сняла фактом два места (`fact_loss`, `fact_commissionreportin`), где в `currency_code` писался `rate.currency.code` (числовой код ISO 4217) вместо `rate.currency.isoCode` (буквенный, «KGS»). `02 §валюты` нормирует суммовую конвертацию (`ADR-010`), но поле кода валюты в контрактах не задокументировано (схемы `fact_payments` в `02` отсутствуют — вне scope M-P4-A-02, полная схема core — `Q-4`). Пропозал сессии (P2) принят в редакции архитектора: (а) исправлен стандарт — ISO **4217**, не «417»; (б) снята ложная каузальная связка «ошибка была следствием curl/415» — это два независимых инцидента (415 — `02 §поведение API`/RB-18, контент-тип на GET; выбор `.code` — ошибка чтения контракта), в append-only журнал такая причинность не вносится; (в) дом правила — `02 §валюты` (контракт API, глобально), а не проза «во всех новых ингестах».
- **Решение:**
  - §1 **Канон:** `currency_code` = `rate.currency.isoCode` (буквенный ISO 4217, STRING). `rate.currency.code` (числовой) в целевые таблицы НЕ пишется. **accepted.**
  - §2 **Скоуп глобальный** (как `ADR-010`): все входящие ингесты из МойСклад — существующие и будущие. Ретро-аудит уже загруженных `core.fact_*` на числовой код настоящим ADR НЕ назначается (не измерен; схема `core.fact_payments` не задокументирована — `Q-4`); проверяется как под-пункт при закрытии `Q-4`. **accepted.**
  - §3 **Верификационное следствие:** `02 §поведение МойСклад API` — при `expand=` `limit` не должен превышать 100, иначе API молча игнорирует `expand` и возвращает `NULL` вместо вложенного объекта. Значит `rate`/`rate.currency.isoCode` деградируют ТИХО. Профиль запроса ингеста (`expand`, `limit`) подлежит подтверждению по закоммиченному `main.py` (`Q-49`), не по памяти. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-027 (append). Кросс: `ADR-010`, `ADR-024 §2`, `Q-4`, `Q-49`.
  - `02_ERP_CONTRACTS §валюты` — вставка правила §1 после абзаца «Формализовано как **ADR-010**…» (STABLE, через настоящий ADR).
  - `07_STATE`: `Q-4` — под-пункт «числовой vs буквенный код валюты в уже загруженных `core.fact_*`» (отдельный Q не заводится); `Q-49` — профиль запроса.
- **Статус:** §1–§3 accepted.

## ADR-028 · Курс `rate.value` МойСклада — часть оракула; внешняя верификация курсов вне границы контракта (inbound)
- **Контекст:** `Q-48` (возврат `E1-T1-MECH-INGEST`, 2026-07-23): есть ли ревью-процесс корректности курсов конвертации МойСклада (сверка с ЦБ/НБКР/реальным курсом), или брать `rate.value` как есть. Наблюдение сессии: RUB-документ мая нёс `rate=1.1013`, происхождение цифры не установлено.
- **Решение:**
  - §1 `rate.value` документа МойСклада — **часть референс-оракула** (`00_CHARTER §главный принцип`, `ADR-002`: «Источник истины реконсиляции — ТОЛЬКО прямой запрос к API МойСклада»). Выход пайплайна обязан воспроизводить цифры МойСклада, включая применённые в них курсы. **accepted.**
  - §2 Сверка `rate.value` с внешним источником (НБКР/ЦБ/биржа) в inbound-конвертации НЕ вводится: это завело бы второй конкурирующий оракул и нарушило закон-следствие «слой не лезет через границу чинить чужой слой» (`_METHOD §4`, `05 §Граница контракта`). Некорректный курс в документе — дефект слоя ERP, правится в ERP владельцем (фикс-форвард в нужный слой), не в пайплайне. **accepted.**
  - §3 Приоритет источника курса не меняется: `rate.value` документа, если задан; иначе текущий курс валюты (`ADR-010`). Иного источника в inbound-каноне нет — намеренно. **accepted.**
  - §4 **Разграничение:** §1–§3 касаются ТОЛЬКО входящей конвертации (валюта документа → KGS). **Исходящая** (KGS → USD для инвестора, `cf-fx`/`dim_fx_rates`) вне scope `ADR-010` и настоящего ADR; вопрос происхождения/корректности курса там открыт и живёт своей поверхностью (класс `Q-19`). Не смешивать. **accepted.**
  - §5 **Ops-следствие (флаг владельцу, не гейт):** если владелец хочет контроль курсов — его место процедура в МойСкладе (ERP), не чек в пайплайне. DQ-чек «курс vs внешний источник» настоящим ADR НЕ назначается: порог заранее не калибруется (anti-improvisation; ср. `Q-20`). **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-028 (append). Кросс: `ADR-002`, `ADR-010`, `ADR-016`, `Q-19`, `Q-20`.
  - `07_STATE`: `Q-48` → CLOSED (ADR-028).
  - `02`/`09`: правки прозы НЕ назначается — `02 §валюты` уже несёт приоритет источника курса (`ADR-010`); дублирование не создаётся.
- **Статус:** §1–§5 accepted.

## ADR-029 · Fallback-курс в `_rate_and_currency` — фикс-форвард, scope подтверждён (не гейтится наличием не-KGS данных)
- **Контекст:** `Q-50` (`E1-T1-МECH-INGEST`, Шаг 6, 2026-07-24) — хардкод `1.0` при `rate.value is None` расходится с `ADR-010`/`ADR-028` («иначе текущий курс валюты»). `02_ERP_CONTRACTS §валюты`/`§семантика П&Л` (Источник #1 loss, формула Источника #2 commissionreportin, `ADR-006`/`ADR-026 §2`) подтверждают: `loss`/`commissionreportin` УЖЕ внутри глобального inbound-канона `ADR-010`, несмотря на то что буквальный список полей в тексте `ADR-010` (`06`) их не перечисляет — это дыра формулировки `ADR-010`, не дыра scope.
- **Решение:**
  - §1 Fallback-ветка `_rate_and_currency` обязана фетчить фактический текущий курс валюты документа, не константу `1.0`. **accepted.**
  - §2 Обязательство НЕ гейтится замером не-KGS документов в выборке — прямая аналогия `ADR-016 §3` (`cashout`) и `Q-47` (`loss`, DEFER: «ингест обязан нести ×rate.value независимо от наличия не-KGS документов в выборке»). **accepted.**
  - §3 Не блокирует Шаг 6/cutover `E1-T1-МECH-INGEST` — контрольные цифры мая 100% KGS. **accepted.**
- **Последствия:** `/reference/code/cf-loss-commission/main.py::_rate_and_currency` — точечная правка (задача-строка `E1-T1-МECH-RATE-FALLBACK`). `02_ERP_CONTRACTS` — правка прозы НЕ назначается (канон уже корректен). `07_STATE`: `Q-50` → CLOSED, новая задача-строка `E1-T1-МECH-RATE-FALLBACK`.
- **Статус:** accepted (директива, по образцу `ADR-016`).

## ADR-030 · SQL-конвенция C1: запрет `MERGE … WHEN NOT MATCHED THEN INSERT ROW` в новом коде проекта
- **Контекст:** живой инцидент Шага 6 `E1-T1-MECH-INGEST` (ревизия `cf-loss-commission-00004-div`, сессия 2026-07-24, провенанс — `architect_review_queue_2026-07-24-1.md` п.4). Добавление поля `applicable` в staging-схему (фикс Ghost Records, `ADR-011`) через `ALTER TABLE ADD COLUMN` (колонка легла в конец) без синхронной правки порядка колонок в `core` дало молчаливую порчу `BOOL → FLOAT64` в `sum_kgs` при `MERGE … WHEN NOT MATCHED THEN INSERT ROW` (позиционное сопоставление). Пойман только потому, что типы разошлись (`BOOL` vs `FLOAT64`); с двумя `FLOAT64`-колонками подряд та же ошибка прошла бы тихо. `05_CONVENTIONS.md` Часть II на момент решения — заглушка (`C1`/`C2` в репо не существовали; исходное предложение сессии нумеровало пункт `C3` по ошибочной аналогии — поправлено архитектором).
- **Решение:** `MERGE … WHEN NOT MATCHED THEN INSERT ROW` запрещён в НОВОМ коде проекта; разрешён только явный `INSERT (колонки) VALUES (…)`, устойчивый к рассинхрону порядка колонок. Scope — только новый `MERGE`-код; существующие `MERGE`-конструкции не ретрофитятся автоматически (прецедент `ADR-020 §5`/Q-39 — решать по каждому случаю отдельно, не оптом по совпадению формы SQL). Апрув владельца получен явно в сессии 2026-07-24 (`_METHOD` строка 124 соблюдена).
- **Последствия:** `05_CONVENTIONS.md` Часть II — первая содержательная запись, конвенция `C1`. `07_STATE` — без изменений (не гейтит текущие задачи; сам баг уже точечно пофикшен в рамках Шага 6, ревизия `cf-loss-commission-00004-div`).
- **Статус:** accepted.


## ADR-031 · Провенанс `cf-loss-commission` закоммичен (закрывает Q-46/B-9); C1-конвенция распространена на `core.fact_commissionreportin`; ADR-029 реализован; автозапуск заведён; Q-49/Q-51 закрыты
- **Контекст:** провенанс `cf-loss-commission` извлечён дважды за сессию (ревизии `00006-dip`, `00007-kuh`) напрямую из задеплоенного архива, sha256-верифицирован disk(скачано)==deployed, закоммичен человеком в `holika`. Чтение закоммиченного `main.py` вскрыло, что `MERGE core.fact_commissionreportin` всё ещё нёс позиционную вставку (`WHEN NOT MATCHED THEN INSERT ROW`) — тот же класс риска, что `ADR-030` уже закрыл для `core.fact_loss`, но не для этой таблицы. `ADR-029` (fallback-курс) на момент начала сессии был директивой без реализации — реализовано и задеплоено в рамках сессии.
- **Решение:**
  - §1 Провенанс `cf-loss-commission` — `/reference/code/cf-loss-commission/{main.py, requirements.txt, MANIFEST.md}`, актуальная ревизия `cf-loss-commission-00007-kuh`, sha256-верифицирован. `B-9` СНЯТ, `Q-46` закрыт артефактом (не только применением). **accepted.**
  - §2 C1/`ADR-030` применён к `MERGE core.fact_commissionreportin` (явные колонки вместо `INSERT ROW`) — закрывает остававшийся незакрытым риск молчаливой порчи данных при будущем изменении схемы. **accepted.**
  - §3 `ADR-029` реализован и задеплоен (`entity/currency` вместо хардкода `1.0` при отсутствии `rate.value` документа). Задача-строка `E1-T1-МECH-RATE-FALLBACK` → DONE. **accepted.**
  - §4 `Q-49` CLOSED фактом из закоммиченного `main.py`: (а) `commissionOverhead` читается как объект (`.get("sum")`), подтверждает канон `02 §семантика П&Л`, не скалярную форму; (б) expand-профиль (`loss: expenseItem,agent,rate.currency`; `commission: agent,rate.currency,positions`) с `limit=100` — в безопасных границах `02 §поведение API`, `expand` не деградирует. **accepted.**
  - §5 `Q-51` CLOSED решением владельца (не DEFER-ожиданием): код-ревью признан достаточным закрытием приёмочного критерия «конвертация проверена на ≥1 не-KGS документе» — живой тест не требуется как условие. Основание — 128+191 документов за всю историю аккаунта, ни одного не-KGS. Прецедент того же класса, что `Q-47`. Триггер пересмотра прежний: первый реальный не-KGS документ. **accepted (владелец).**
  - §6 Cloud Scheduler `loss-commission-daily-update` заведён (`0 3 * * * Asia/Bishkek`, OIDC `etl-sa`, `attemptDeadline=1800s` = серверный timeout, `maxRetryDuration=0s`) — по прецеденту `finance-daily-update`/`ADR-023`. **accepted.**
- **Последствия (blast radius):**
  - `07_STATE` — `E1-T1-MECH-INGEST`/`Q-46`/`Q-49`/`Q-50`/`Q-51`/`B-9` обновлены по `STATE_PATCH` (см. выше).
  - `11_INFRA_FACTS §CF` — адресат для нового блока `cf-loss-commission` (сейчас записи там нет вообще, хотя функция уже вторая по счёту в проекте); наполнение — вне scope этого блока.
  - `03_PIPELINE_SPEC §cf-loss-commission` — теперь может наполняться (артефакт есть); само наполнение прозой — вне scope этого блока.
- **Статус:** §1–§6 accepted.

## ADR-032 · Форма `marts.expenses` после дельты — 17 колонок сохраняются, отсутствующие измерения заполняются `'Не указан'`
- **Контекст:** живой SQL отдаёт 17 колонок с грануляром до `moment` × `payment_type` × статья × контрагент × проект × канал, включая `total_sum_usd`. Черновой набросок дизайна отдавал 4 колонки (статья, месяц, сумма, количество) и не отмечал, что это усечение живой инвесторской витрины на 13 колонок при ежедневном WRITE_TRUNCATE. `core.fact_loss` не несёт `project_name`/`sales_channel_name`/`payment_type`; `core.fact_commissionreportin` не несёт также статью, проект и канал.
- **Решение:** форма выхода сохраняется байт-в-байт (17 колонок, те же имена, тот же порядок). Ветки union заполняют недостающие измерения литералом `'Не указан'`, идентификаторы — `NULL`. `payment_type` получает два новых значения: `'loss'` и `'commission'`. Статья для комиссий — литерал `'Расходы маркетплейсов'` (ADR-006 §2). Сводная форма (статья × месяц) отвергнута как отдельный проект по дашборду, а не правка запроса; путь к ней не закрыт.
- **Последствия:** дашборд продолжает работать без переделки. Корзина `'Не указан'` по проекту и каналу вырастет — это честное отражение источника, не деградация качества. Фильтр по `payment_type` на дашборде может потребовать добавления двух значений — проверяется на Шаге 2 брифа.
- **Статус:** accepted (владелец, 2026-07-26)

## ADR-033 · Приёмка E1-T1-MECH — сходимость в 0,00; смешанное состояние ADR-024 §5 снято фактом
- **Контекст:** ADR-024 §5 предписывал раскрывать смешанное качество марта на cutover, поскольку платежная часть несла валютный баг ADR-016 (Σ 1 108 075,75 KGS за май, ≥7 статей). Приёмочного числа для сверки не задавал. Обсуждались две формы: раздельная приёмка с фиксацией известного остатка, либо ре-гейт на закрытие FX.
- **Решение:** ни та, ни другая не нужны. Валютная починка подтверждена в проде двумя независимыми доказательствами (Q-45) и в данных — все пять проверенных статей за май совпали с эталоном, итог трёх источников = 10 232 903,20 = целевая величина, разрыв 0,00. Приёмка = полная сходимость с эталоном, без оговорок и без раздельных схем.
- **Последствия:** B-9 снят. ADR-024 §5 исполнен по существу (раскрывать нечего). Критерии приёмки брифа сформулированы как пять проверяемых числовых условий.
- **Статус:** accepted

## ADR-034 · Черновики и системные статьи в новых таблицах фильтруются на стороне запроса; код загрузчиков не меняется. Зафиксирована инверсия дисциплины ADR-011
- **Контекст:** `cf-loss-commission` не отбрасывает непроведённые документы, а сохраняет флаг `applicable` в таблице, и не имеет DELETE-шага для системных статей-исключений. Первоначально это было квалифицировано как дефект. Проверка данными: черновиков в `core.fact_loss` нет (все строки `applicable = TRUE`), системных статей нет (три статьи, все содержательные). Проверка кода: `cf-finance/main.py` строки 47–48 — `if row.get("applicable") is False: continue`, то есть **платежный** загрузчик отбрасывает черновики на выгрузке.
- **Решение:** поведение `cf-loss-commission` признано соответствующим ADR-011 (тянуть всё без исключений, фильтровать в базе; никогда не `if/continue` на выгрузке — иначе смена статуса документа не приедет и в таблице зависнет фантом). Правило нарушает `cf-finance`, не новый загрузчик. Код не правится ни там, ни там. Фильтр `applicable = TRUE` ставится в ветке списаний SQL-дельты как страховка, а не как обход дефекта. Приведение `cf-finance` к ADR-011 — отдельная задача, не гейт.
- **Последствия:** одна строка в SQL вместо правки кода и редеплоя. Нарушение ADR-011 в `cf-finance` зафиксировано как технический долг. Для комиссий флаг `applicable` вообще не выгружается — исключить черновики на этой стороне нельзя; за май вопрос не возникает (сходимость 0,00), в общем случае остаётся ограничением.
- **Статус:** accepted

## ADR-035 · Таблица соответствий названий статей не создаётся — вариантов написания в данных нет
- **Контекст:** набросок дизайна предписывал обязательную канонизацию названий через `LOWER(TRIM())` + таблица соответствий, перечисляя пары `Топливо`/`ТОпливо`, `Мой Склад`/`Мой склад`, `Банк (комиссия)`/`Банк-комиссия`, `Неразнесенное списание`/`неразнесенные списания`. Отдельно было отмечено, что `LOWER(TRIM())` не разводит две последние пары — там отличаются пунктуация и число, а не регистр.
- **Решение:** артефакт не создаётся. Факт 2026-07-26: по обеим таблицам 29 различных названий, ни одного дубля по регистру или написанию. Ни `Банк-комиссия`, ни `ТОпливо`, ни `Мой склад` со строчной, ни формы во множественном числе в данных не существуют. Перечисленные варианты были наблюдением из более старой сверки и в текущих таблицах не встречаются. Названия проходят в март как есть, `COALESCE(..., 'Не указана')` сохраняется как в живом SQL.
- **Последствия:** вопрос канонизации снят с этой задачи. Владелец успел ответить, что `Банк (комиссия)` и `Банк-комиссия` считать двумя статьями — ответ применения не имеет, второй формы в данных нет. Если варианты появятся позже, решение переоткрывается.
- **Статус:** accepted

## ADR-036 · Пустые `project_id` / `sales_channel_id` / `agent_name` в `core.fact_loss` заполняются `'Не указан'`; причина пустоты не проверена и догоняется отдельной задачей
- **Контекст:** во всех строках `core.fact_loss` эти поля NULL. Ранее было ошибочно сказано, что «есть идентификаторы, но нет названий» — идентификаторов тоже нет. Гипотеза причины (Q-55): `expand` запрашивает только `expenseItem,agent,rate.currency`, поэтому проект и канал приходят неразвёрнутыми ссылками и код читает из них NULL.
- **Решение:** в дельте заполняется `'Не указан'`, догон не делается. Три основания: (1) для списания эти измерения слабы по смыслу — у списания товара нет контрагента, пустота честная; (2) цена догона — правка кода, редеплой, новый снимок провенанса, плюс подтягивание названий из справочников, при нулевом вкладе в цель задачи (правильные суммы по статьям); (3) порядок обратим без боли — колонки те же, пересборка ночная, миграции нет; обратный порядок дороже.
- **Последствия:** Q-55 остаётся открытым как непроверенная гипотеза, а не как установленный факт отсутствия данных. Разбивка списаний по проектам и каналам, если понадобится, — отдельная мелкая задача, не переделка марта.
- **Статус:** accepted (владелец, 2026-07-26)

## ADR-037 · Четыре системные статьи-исключения применяются и к ветке списаний
- **Контекст:** Q-10 закрыт фактом 2026-07-26 — `cf-finance` физически удаляет из `core.fact_payments` сразу после MERGE четыре `expense_item_id`: `24c0e914-2d8c-11f1-0a80-11b0000c7043`, `4e1c05f2-0673-11e6-a655-0cc47a342ca4`, `8dbf9374-0a01-11e4-b9bf-002590a32f46`, `8dbf99a0-0a01-11e4-a743-002590a32f46` (строки 122–133 задеплоенного `main.py`, комментарий «Cleaning up excluded system expenses»). Названия в коде отсутствуют, только идентификаторы. У `cf-loss-commission` аналогичного шага нет. Проверка `core.fact_loss` на эти идентификаторы вернула пусто — но в таблице на тот момент было 8 строк, только май.
- **Решение:** тот же список применяется фильтром в ветке списаний SQL-дельты. Основание: исключение квалифицирует статью как не-операционный расход, и это свойство статьи, а не типа документа, из которого она пришла. Код `cf-loss-commission` не правится (согласуется с ADR-034). Форма фильтра — `COALESCE(expense_item_id, '') NOT IN (…)`: без `COALESCE` строка с NULL-идентификатором даст `NULL` в `NOT IN` и будет молча выброшена.
- **Последствия:** за май операция холостая (сходимость 0,00 получена без фильтра). За остальные месяцы — страховка, эффект неизвестен до заливки истории; перепроверка вынесена в Шаг 0 брифа. Названия четырёх статей остаются неизвестными: `stg_msklad.fact_payments_stg` не существует, механизм загрузки `cf-finance` иной. На задачу не влияет — фильтр работает по идентификаторам.
- **Статус:** accepted

## ADR-038 · Процедура переключения прода должна закрывать оба пути пересборки марта; записанная ранее форма «pause → правка → возврат расписания» неверна
- **Контекст:** факт 2026-07-26 из задеплоенного кода — `cf-finance.trigger_marts()` (строки 16–30) вызывает `StartManualTransferRunsRequest` на `transferConfigs/6a22a243-0000-20fd-a458-883d24f4cad4`, то есть на тот же scheduled query `sq_marts_expenses`, в конце каждого ночного прогона. Март пересобирается дважды в сутки: 03:00 Asia/Bishkek (пинок от загрузчика) и ~11:10 UTC (собственное расписание). Ручной запуск через API не блокируется постановкой расписания на паузу. Требование `staging-first` из GAP-реестра опиралось на неполную картину: закрывался один путь из двух.
- **Решение:** `staging-first` остаётся требованием. Процедура переключения переписывается: (1) сохранить текст действующего SQL для отката **до первого касания**; (2) подменять сразу после подтверждённого ночного прогона, чтобы до следующего запуска было максимум времени; (3) сразу после подмены самому запустить одну пересборку и сверить результат; (4) паузу расписания как защитную меру **не считать достаточной** и не полагаться на неё. Отдельно зафиксировано смягчающее: `WRITE_TRUNCATE` пересобирает март целиком из источников, поэтому прогон на промежуточном состоянии не накапливает порчу и самоисправляется следующим корректным прогоном, а падение запроса оставляет предыдущую таблицу нетронутой. Класс риска — временно неверный дашборд, не потеря данных.
- **Последствия:** `11 §SQ` дополняется вторым путём запуска и полным ID конфига (был обрезан до `6a22a243-…`). Бриф `E1-T1-MECH-CUTOVER`, когда будет генериться, наследует эту процедуру. Побочно: любой ручной прогон `cf-finance` вне расписания также триггерит пересборку марта — это надо учитывать при отладке.
- **Статус:** accepted

## ADR-039 · Исходящая FX-конвертация марта — вне scope E1-T1-MECH; выбор политики курса owner-gated; blast radius шире расходов
- **Контекст:** `Q-56`. Живой `sq_marts_expenses` (`/reference/sql/sq_marts_expenses.sql` стр.19–26) считает `total_sum_usd = ROUND(SUM(p.sum_kgs) / fx.rate_kgs_per_usd, 2)`, где `fx` — CROSS JOIN одной строки `SELECT rate_kgs_per_usd FROM core.dim_fx_rates ORDER BY date DESC LIMIT 1`. Класс — исходящая конвертация (`09 §конвертация валют`, `03` стр.372–374), вне scope `ADR-010`/`ADR-028 §4`, остаток `Q-19`. Сессия `E1-T1-MECH-PREP` сохранила поведение в дельте как есть, сознательно.
- **Решение:**
  - §1 В scope `E1-T1-MECH` формула `total_sum_usd` НЕ трогается. Задача — про источники расходов (`loss`/`commissionreportin`); правка FX меняла бы вторую независимую вещь тем же cutover'ом на живой ежедневной WRITE_TRUNCATE-витрине (`ADR-038` — процедура переключения и без того закрывает два пути пересборки). **accepted (архитектор).**
  - §2 Развилка «(a) штатно / (b) дефект» НЕ адъюдицируется архитектором: это политика инвесторской отчётности, не архитектурный инвариант. Референс-оракул (`ADR-002`, `ADR-005`) — line-level МойСклада в KGS; `total_sum_usd` относится к классу DERIVED, ни одна из форм границу контракта не нарушает. **Owner-gated.** **accepted.**
  - §3 Фактура для §2 (не решение): паттерн `latest_fx` содержательно КОРРЕКТЕН для метрик состояния на текущий момент и СПОРЕН для исторических потоков, где сумма меняется изо дня в день без изменения фактов. Соответственно «единая политика на все марты» и «политика по типу метрики» — два разных ответа; §2 выбирает какой. **accepted (как фактура).**
  - §4 Предусловие варианта (b), НЕ проверенное: `core.dim_fx_rates` наполняется из Bakai OpenBanking с 2026-06-03 (`02 §core.dim_fx_rates`). Покрытие таблицы по датам ранее этой даты не замерено ни одной сессией. Без майских курсов вариант (b) для мая-2026 технически неисполним. Замер — ПРЕДУСЛОВИЕ решения, не его следствие. **accepted.**
  - §5 Blast radius при (b) шире расходов: тот же паттерн `latest_fx` задокументирован как канонический SQL `marts.in_transit` (`03 §marts`, стр.126–134); прочие SQ флота на этот паттерн не сканировались. Скан — часть discovery §4. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-039 (append). Кросс: `ADR-002`, `ADR-005`, `ADR-010`, `ADR-028 §4`, `ADR-038`, `Q-19`, `Q-56`.
  - `07_STATE`: `Q-56` сужен; заведена discovery-строка `FX-OUTBOUND-COVERAGE`.
  - `03`/`09`: правки прозой НЕ назначается — решения политики нет, документировать нечего.
  - `briefs/E1-T1-MECH.md`: подтверждается, что дельта сохраняет формулу как есть.
- **Статус:** §1 accepted · §2 accepted (передано владельцу) · §3–§5 accepted.

## ADR-040 · Гигиена задеплоенного архива CF: очищенный `.gcloudignore` — условие следующего деплоя (закрывает ПАТТЕРН RQ-3, не инстанс)
- **Контекст:** `RQ-3` (2026-07-17) зафиксировал `main.py.bak` в ревизии `cf-finance-00006-piv` со статусом «DEFER, убрать при следующем деплое». Факт 2026-07-26 (`Q-57`): ревизия `00012-cik` несёт `main.py.bak`, `main.py.pre-e1t3-mech-fx.bak`, `patch_main_finance.py`, `__pycache__/`. Между ревизиями минимум шесть деплоев; формулировка не сработала ни разу.
- **Решение:**
  - §1 Внеплановый деплой ради чистки НЕ назначается: риск гигиенический (не безопасность — IAM закрыт `ADR-022`; не функциональность), а деплой `cf-finance` — не-идемпотентная операция (`05 Часть I ★ Не-идемпотентные операции`) с живым прецедентом переоткрытия дыры при редеплое (`ADR-022 §Последствия`). **accepted.**
  - §2 Следующий деплой любой CF проекта по любой причине обязан идти с `.gcloudignore`, исключающим `*.bak`, `__pycache__/` и разовые patch-скрипты. Это УСЛОВИЕ деплоя, не пожелание. **accepted.**
  - §3 Проверка — read-back содержимого задеплоенного архива ПОСЛЕ деплоя (`ADR-021 §2`: успех команды деплоя ≠ факт чистоты архива); результат фиксируется в `/reference/code/<cf>/MANIFEST.md` соответствующей ревизии (`ADR-017 §2`). **accepted.**
  - §4 Диагноз провала `RQ-3`: правило жило только строкой в `07_STATE` и не было ничьим условием — разработчик читает `00`+`05`+`07`+бриф, а обязывающая формулировка в `05` отсутствовала. Поэтому условие §2 вносится в `05 Часть II`, а не только в настоящий ADR. Прецедент формы — `ADR-030`/C1. **accepted.**
  - §5 Осознанная издержка: после §2 в архиве не останется до-фиксовых `.bak`, а именно такой файл дал второе независимое доказательство закрытия `Q-45`. Замена штатная и уже существующая — снапшот-per-revision `/reference/code/` (`ADR-017 §2`). Провенанс переносится в назначенное для него место, а не теряется. **accepted.**
  - §6 Scope — все CF проекта (`cf-finance`, `cf-loss-commission`, прочие), per-CF по прецеденту `ADR-017 §6`/`ADR-024 §3`. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-040 (append). Кросс: `ADR-017 §2/§6`, `ADR-021 §2`, `ADR-022`, `ADR-024 §3`, `RQ-3`, `Q-57`.
  - `07_STATE`: `Q-57` → CLOSED; `RQ-3` → CLOSED тем же (паттерн).
  - `05 Часть II`: новый пункт «Гигиена деплоя Cloud Functions».
- **Статус:** §1–§6 accepted.

## ADR-041 · Гомоглифы в task-ID: `07_STATE` нормализуется под латиницу; `06` grandfather; scope нормализации замерен
- **Контекст:** `Q-58`. Собственный замер архитектора @ SHA `d063a8c` (цифры очереди не приняты на веру — `ADR-021 §2`): `07_STATE` — 42 вхождения с кириллической `М` (U+041C) против 29 латинских; `06_DECISIONS_LOG` — 5 против 43. Разбивка `07`: `E1-T1-МECH-INGEST` 15/4, `E1-T1-МECH` 10/4, `E1-T3-МECH-FX` 9/5, `E1-T3-МECH-FX-CUTOVER` 3/0, `E1-T1-МECH-RATE-FALLBACK` 3/0, `E1-T2-МECH-TAX` 2/1, `E1-T1-MECH-PREP` 15 (целиком латиница). `_APPLIER.md` адресует строки `07_STATE` буквальным текстовым поиском по T-id → расхождение написания даёт тихий промах.
- **Решение:**
  - §1 `07_STATE.md` нормализуется под латиницу одним механическим проходом. `07` — LIVING, grandfather-оговорок не требует. **accepted.**
  - §2 `06_DECISIONS_LOG.md` НЕ трогается (append-only, `_METHOD §6`; прямой прецедент `ADR-018 §3`). Пять кириллических вхождений (стр.373, 377, 378, 393) остаются; настоящий ADR фиксирует, что они читаются как латинские идентификаторы. **accepted.**
  - §3 Правило нормализации механическое и проверяемое, не «на усмотрение applier'а»: замена подстроки `-МECH` (U+041C) на `-MECH` (U+004D) в пределах `07_STATE.md`. Приёмка — повторный счёт: 0 кириллических вхождений. **accepted.**
  - §4 Порядок: нормализация выполняется ПОСЛЕДНЕЙ операцией коммита. Прочие патчи сессии якорятся на текст, содержащий кириллические ID; нормализация первой сместила бы якоря. **accepted.**
  - §5 Blast radius замерен и ограничен: полный скан смешанных латинско-кириллических токенов по `07` и `06` показал, что единственный класс гомоглифных ИДЕНТИФИКАТОРОВ — семейство `*-МECH*`; все прочие смешанные токены (`GAP-реестр`, `session-блок`, `не-KGS`, `снапшот-per-revision` и т.д.) — легитимные композиты, идентификаторами не являются. `04_ROADMAP` и `05_CONVENTIONS` вхождений `*MECH*` не содержат вовсе (согласуется с `ADR-025 §2`). **accepted.**
  - §6 Конвенция на будущее (Proposal 1 сессии `E1-T1-MECH-PREP`) **принята владельцем явно** (`_METHOD §8`, сессия `E1-T1-MECH-PREP-ADJ`, 2026-07-26) и вносится в `05 Часть III` с уточнением scope: проверка гомоглифов применяется к ИДЕНТИФИКАТОРАМ (`E1-*`, `M-P*`, `Q-*`, `B-*`, `ADR-*`, `RQ-*`), не к любому смешанному токену — иначе правило срабатывает на легитимных композитах §5 и обесценивается за одну сессию. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-041 (append). Кросс: `ADR-018 §3`, `ADR-021 §2`, `ADR-025 §2`, `Q-58`.
  - `07_STATE`: механическая нормализация + `Q-58` → CLOSED в части нормализации.
  - `05 Часть III`: новый пункт «Идентификаторы — только латиница».
- **Статус:** §1–§6 accepted.

## ADR-042 · Коллизия ID `B-9`: содержимое `ADR-033` читается как `B-11`; исторический `B-9` сохраняется
- **Контекст:** `ADR-033 §Последствия` содержит «B-9 снят», имея в виду смешанное состояние качества марта (`ADR-024 §5`). `07_STATE §Блокеры` держит `B-9` за другим, ранее закрытым блокером — провенанс-снапшот `cf-loss-commission` (закрыт 2026-07-25, `ADR-031 §1`; ссылаются `Q-46`, `Q-3`). `B-10` занят. Applier не переписал существующий `B-9` и не угадал новый номер, оставив `07` нетронутым по этой строке — корректное поведение по `_METHOD §8` («СТРУКТУРНОЕ НЕСООТВЕТСТВИЕ», стоп, не угадывать).
- **Решение:**
  - §1 Квалификация: непреднамеренное переиспользование ID, не сознательное переоткрытие. Основания — два содержательно не связанных смысла; отсутствие в `ADR-033` какой-либо ссылки на провенанс-снапшот; наличие свободного номера на момент написания. **accepted.**
  - §2 Содержимое `ADR-033` заводится в `07 §Блокеры` под `B-11`. Исторический `B-9` сохраняется в прежнем смысле — перекрёстные ссылки `ADR-031 §1`, `Q-46`, `Q-3` не ломаются. Вариант «переименовать/superseded старый `B-9`» отклонён: цена — висячие ссылки в append-only `06` ради опечатки. **accepted.**
  - §3 Текст `ADR-033` в `06` НЕ редактируется (append-only). Настоящий ADR фиксирует: упоминание «B-9» в `ADR-033 §Последствия` по факту нумерации есть `B-11`. Прямой прецедент формы — `ADR-018 §4` (LIVING-док указывает на физический объект, append-only получает пометку, текст не трогается). **accepted.**
  - §4 `B-11` заводится СРАЗУ СНЯТЫМ: он снят тем же фактом, которым его снимает `ADR-033` (сходимость трёх источников 10 232 903,20, разрыв 0,00; валютная починка подтверждена `Q-45`). Заводить открытым и закрывать той же операцией — фикция истории. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-042 (append). Кросс: `ADR-018 §4`, `ADR-024 §5`, `ADR-031 §1`, `ADR-033`, `Q-45`, `Q-46`, `Q-3`.
  - `07_STATE §Блокеры`: новая строка `B-11` (СНЯТ); строка `B-9` не трогается.
- **Статус:** §1–§4 accepted.

## ADR-043 · Диагностические скрипты не удаляют временные артефакты; путь печатается (расширение ADR-014)
- **Контекст:** Proposal 2 сессии `E1-T1-MECH-PREP`. Первый диагностический прогон распаковал `function-source.zip` и удалил временную папку по завершении; тот же исходник понадобился следующему шагу той же сессии — распаковывали заново. Второй случай того же класса за проект. **Замечание по форме:** предложение звучало как «`ADR-014` дополнить пунктом» — неисполнимо, `06` append-only (`_METHOD §6`, `_ARCHITECT §7`); корректная форма — новый ADR, расширяющий `ADR-014`, плюс вставка в `05 Часть II`.
- **Решение:**
  - §1 Диагностический скрипт НЕ удаляет созданные им временные директории. **accepted.**
  - §2 Путь к временной директории печатается последней строкой вывода. **accepted.**
  - §3 Scope — только диагностика (read-only разведка). Прод-скрипты и деплой-пайплайны не затрагиваются; уборка временных артефактов — решение человека, не скрипта. **accepted.**
  - §4 `ADR-014` не редактируется, а расширяется настоящим ADR. **accepted.**
- **Последствия:** `05 Часть II` — вставка в блок «Стандарт доставки исполняемых артефактов». `07_STATE` — правки не требует. Апрув владельца на конвенцию получен явно в сессии `E1-T1-MECH-PREP-ADJ`, 2026-07-26 (`_METHOD §8`; прецедент формы — `ADR-021 §Последствия`).
- **Статус:** §1–§4 accepted.

## ADR-044 · Автовердикт скрипта, построенный на текстовом поиске, — не факт (расширение ADR-021 §2)
- **Контекст:** Proposal 3 сессии `E1-T1-MECH-PREP`. Диагностический скрипт грепал `sum_kgs` и `rate` как совпадение в ОДНОЙ строке и выдал вердикт «валютной починки НЕТ»; в реальном `main.py` токены разнесены на строки 66–68. Вердикт был ложным и обратным факту — `Q-45` закрыт «ДА» двумя независимыми доказательствами. `ADR-021 §2` уже устанавливает «успех инструмента ≠ факт», но применительно к статусным меткам в `07_STATE`, не к выводу самодельного скрипта. **Замечание по форме:** «дополнить `ADR-021 §2`» неисполнимо (append-only) — новый ADR + вставка в `05 Часть I`.
- **Решение:**
  - §1 Автоматический вердикт (`PASS`/`FAIL`, «найдено»/«не найдено»), выведенный скриптом из текстового поиска, — **гипотеза наблюдения, не факт**. **accepted.**
  - §2 Скрипт, выносящий вердикт, обязан печатать **доказательную выдержку** — совпавшие строки с номерами, а не только метку. Без выдержки вердикт непроверяем, и правило §1 требовало бы не доверять выводу, ничем его не заменяя. **accepted.**
  - §3 Многострочные конструкции — известная ловушка греп-вердикта: отрицательный результат поиска по одной строке НЕ есть отсутствие конструкции в коде. **accepted.**
  - §4 Вставка — `05 Часть I §★ Успех инструмента ≠ факт`, тем же holika-локальным статусом, что и исходное правило (`ADR-021 §4`). `ADR-021` не редактируется. **accepted.**
  - §5 Отмечено: инцидент удовлетворяет триггеру промоушена `Q-40` («правило сработало ≥1 раз вне этой сессии»). Промоушен в канон `_METHOD §6` настоящим ADR НЕ назначается — blast radius за пределами holika, owner-gated; вынесен в review-очередь. **accepted.**
- **Последствия:** `05 Часть I` — вставка. `07_STATE` — `Q-40` получает пометку «триггер промоушена наступил». Апрув владельца на конвенцию получен явно в сессии `E1-T1-MECH-PREP-ADJ`, 2026-07-26 (`_METHOD §8`).
- **Статус:** §1–§5 accepted.

## ADR-045 · `Q-64`: фактическая каденция мартов — DEFER до после Шага 0; замер = предусловие генерации брифа cutover; сворачивается с остатком `Q-21`
- **Контекст:** `Q-64`. `bq ls -j` (без `--all_users`) показал пересборки `marts.sales_overview` в 15:34, 17:34, 19:34 UTC (интервал два часа) и `marts.expenses` в 14:41 UTC. `11 §SQ` стр.49 документирует `sq_marts_expenses` как «every 24 hours (BQ DTS default, ~11:10 UTC; schedule-поле пустое, nextRunTime активен)»; `ADR-038` добавляет второй путь — `cf-finance.trigger_marts()` в конце ночного прогона (~21:00 UTC / 03:00 Asia/Bishkek). Ни 14:41, ни двухчасовая каденция ни одному из двух известных путей не соответствуют.
- **Решение:**
  - §1 Наблюдение НЕ интерпретируется. Вариант «списать на ручные прогоны диагностической сессии» ОТКЛОНЁН как закрытие гэпа догадкой: отсутствие `--all_users` гипотезы не различает — BQ DTS исполняет scheduled query под учётной записью создателя конфига, поэтому прогоны DTS МОГУТ попасть в листинг вызывающего наравне с его ручными джобами. Наблюдение совместимо с обеими гипотезами. **accepted.**
  - §2 Приоритет — DEFER до закрытия Шага 0 `E1-T1-MECH`. Шаг 0 read-only и от каденции не зависит; отвлекать критический путь нечем. **accepted.**
  - §3 Замер становится **жёстким предусловием генерации брифа `E1-T1-MECH-CUTOVER`**, а не плавающим DEFER. Обоснование: `ADR-038 §Решение(2)` рассчитывает окно cutover от каденции («подменять сразу после подтверждённого ночного прогона, чтобы до следующего запуска было максимум времени»); неверная каденция даёт неверный тайминг. Форма «DEFER-строка в `07`» отклонена по прецеденту провала `ADR-040 §Диагноз`: правило, не ставшее ничьим условием, не исполняется (`RQ-3` прожил шесть ревизий). **accepted.**
  - §4 Величина риска ограничена самим `ADR-038 §Решение`: `WRITE_TRUNCATE` пересобирает март целиком из источников, прогон на промежуточном состоянии не накапливает порчу и самоисправляется следующим корректным, падение запроса оставляет предыдущую таблицу нетронутой. Класс риска — временно неверный дашборд, НЕ потеря данных. Следовательно §3 — предусловие тайминга, а не safety-гейт; эскалация в критический путь Epic-1 не назначается. **accepted.**
  - §5 Форма замера (read-only, один прогон, сворачивается с остатком `Q-21`): `bq ls -j --all_users` за сутки + `bq show --transfer_config` по `6a22a243-0000-20fd-a458-883d24f4cad4` (`sq_marts_expenses`) и `6a23f3ea-0000-2952-853d-582429be7ecc` (`sq_marts_customer_invoices_ar`) — снимает поле `schedule` у обоих и историю прогонов, закрывая остаток `Q-21` тем же прогоном. Отдельной кампании не заводится. **accepted.**
  - §6 `11 §SQ` стр.49–50 патчится ТОЛЬКО по факту замера (`ADR-021 §2`: задокументированная каденция — не факт о поведении). Сейчас не трогается. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-045 (append). Кросс: `ADR-021 §2`, `ADR-038`, `ADR-040 §Диагноз`, `Q-21`, `Q-33`, `Q-64`.
  - `07_STATE`: `Q-64` → DEFER с предусловием §3; `Q-21` остаток → сворачивается в замер §5.
  - `11_INFRA_FACTS §SQ`: правки НЕТ до замера (§6).
- **Статус:** §1–§6 accepted.

## ADR-046 · Датировка сессий и артефактов — локальная дата Бишкека; временны́е ФАКТЫ в тексте — UTC с суффиксом `Z`
- **Контекст:** п.6 очереди `-2`, обнаружено при разборе `Q-63`. Сессия `E1-T1-MECH-PREP` датирована `2026-07-26`, при этом её наблюдения (`lastAttemptTime: None` у нового джоба; «последний прогон finance — 2026-07-24T21:00:05Z») сходятся только если она шла в ночь на 26 июля по Бишкеку, то есть 2026-07-25 по UTC. Репо датирует сессии по локальному времени, конвенция нигде не записана. Затрагивает: имена `/reference/architect_review_queue_<date>-<N>.md` (`ADR-018 §1`), поле `updated_at` в `07_STATE` (`ADR-025 §1`), даты в заголовках ADR, датированные провенанс-артефакты.
- **Решение:**
  - §1 Канон датировки сессий и артефактов = **локальная дата Бишкека** (UTC+6). Документирует действующую практику; ретро-правок не требует. **accepted.**
  - §2 Вариант «перейти на UTC» ОТКЛОНЁН по механике, не по предпочтению: `ADR-018 §3` запрещает ретро-переименование (append-only `06` уже цитирует существующие имена). Переход немедленно создал бы смешанный набор дат без права его выровнять — гарантированный долг вместо снятого. **accepted.**
  - §3 Любой временно́й ФАКТ в прозе доков, ADR, `/reference`-артефактов и session-блоков записывается **в UTC с явным суффиксом `Z`** (форма: `2026-07-25T19:44:52Z`). Основание: `ADR-021 §2` — факт без однозначного якоря непроверяем. Смешение «как называется сессия» и «что зафиксировано как факт» — прямая причина `Q-63`. **accepted.**
  - §4 Разделение ролей §1/§3 не переопределяет `ADR-018 §1` (нейминг) и `ADR-025 §1` (`updated_at`) — оба продолжают работать на локальной дате. **accepted.**
  - §5 Правило UTC-**якоря расчётов** (снимать `date -u` в этой же сессии перед расчётами по расписаниям) настоящим ADR НЕ вводится: это поведенческая конвенция агента → `05 Часть I`, требует явного апрува владельца (`_METHOD §8`). Вынесено в `NEW_CONVENTIONS` сессии, свёрнуто с п.3 очереди. **accepted (как разграничение scope).**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-046 (append). Кросс: `ADR-018 §1/§3`, `ADR-021 §2`, `ADR-025 §1`, `Q-63`.
  - `05 Часть III`: новый пункт «Датировка — локальная; временны́е факты — UTC».
  - `07_STATE`: `Q-63` получает пометку «смежная конвенция закрыта (ADR-046)».
- **Статус:** §1–§5 accepted (по существу — приведение доков к уже действующей практике, не новый выбор; правка `05` может быть ветирована владельцем, прецедент формы — `ADR-025 §Статус`).

## ADR-047 · Перепроверки Шага 0 `E1-T1-MECH`: жёсткий порядок; переоткрытие поимённо, не оптом; референс-оракул существует только за май-2026
- **Контекст:** пп.7–8 очереди `-2`. `ADR-034`, `ADR-035`, `ADR-036`, `ADR-037` приняты на майской выборке (`core.fact_loss` — 8 строк) и все несут оговорку о перепроверке после заливки истории. История залита: `B-10` СНЯТ фактом — `core.fact_loss` = 128, `core.fact_commissionreportin` = 191. Приёмочные майские числа — `briefs/E1-T1-MECH.md` стр.44–53: `core.fact_payments` 8 600 919,01 + `core.fact_loss (applicable = TRUE)` 1 193 254,77 + `core.fact_commissionreportin` 438 729,42 = **10 232 903,20** = целевая величина эталона, разрыв 0,00 (`ADR-033`).
- **Решение:**
  - §1 **Порядок проверок Шага 0 жёсткий:** (1) регрессия майских сумм; и ТОЛЬКО при разрыве 0,00 — (2) наличие `applicable = FALSE` в истории (`ADR-034`); (3) попадание четырёх системных `expense_item_id` в историю (`ADR-037`); (4) варианты написания названий статей (`ADR-035`); (5) непустые `project_id`/`sales_channel_id`/`agent_name` (`ADR-036`/`Q-55`). Отклонение в (1) делает (2)–(5) бессмысленными. **accepted.**
  - §2 Регрессия по маю (любая из трёх сумм ≠ приёмочной ИЛИ итог ≠ 10 232 903,20) = **СТОП всей задачи**, не уточнение решения: это расхождение с референс-оракулом, закрывается правилом / override / ADR по `ADR-002` и `00_CHARTER §главный принцип`, никогда подгонкой. **accepted.**
  - §3 Любая из проверок (2)–(5), давшая результат против премиссы своего ADR, переоткрывает **этот** ADR **отдельным новым ADR ДО** правки SQL, не после. Оптом не решать — прецедент `ADR-020 §5` («паттерн один, ценность активов разная»). Текст исходного ADR в `06` не редактируется (append-only, `_METHOD §6`); новый ADR помечает его `superseded-in-part`. **accepted.**
  - §4 **Ограничение силы перепроверок.** Референс-оракул посеян ТОЛЬКО за май-2026 (`ADR-005`; `/reference/pnl_2026-05.md`; иные периоды → P5, `Q-1`). Для месяцев кроме мая изменение сумм от фильтров `applicable` / системных статей **не может быть провалидировано оракулом — сверять не с чем**. Результат проверок (2)–(4) на не-майской истории есть факт о наличии/отсутствии строк, НЕ приёмка корректности сумм. Приёмка `E1-T1-MECH` остаётся майской (`ADR-033`). **accepted.**
  - §5 Переоткрытие `ADR-036` при непустых полях — **узкое**: меняется правило заполнения ветки списаний, НЕ форма выхода. `ADR-032` (17 колонок, те же имена, тот же порядок) стоит независимо и не переоткрывается. **accepted.**
  - §6 Провенанс замера `ADR-035` не воспроизводим: «по обеим таблицам 29 различных названий» невозможно получить из `core.fact_loss` (8 строк на момент замера) и `core.fact_commissionreportin` (статья — литерал `'Расходы маркетплейсов'`, `ADR-032`) ⇒ в замер входил третий источник, в тексте `ADR-035` не названный. Проверка (4) обязана **назвать выборку явно** и **напечатать сами значения**, а не только их число (`ADR-044 §2`). **accepted.**
  - §7 Уточнение ссылки: число 1 193 254,77 в `07 §Блокеры B-10` приписано `ADR-033`; в тексте `ADR-033` его нет, фактический источник — `briefs/E1-T1-MECH.md` стр.49 (сходится: 411 838,94 + 751 438,54 + 29 977,29). `06` не редактируется; ссылка исправляется в `07` (LIVING). **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-047 (append). Кросс: `ADR-002`, `ADR-005`, `ADR-020 §5`, `ADR-032`, `ADR-033`, `ADR-034`, `ADR-035`, `ADR-036`, `ADR-037`, `ADR-044 §2`, `Q-1`, `Q-55`.
  - `07_STATE`: `Q-55` → пометка «условие переоткрытия `ADR-036` назначено»; `B-10` → ссылка на источник числа исправлена.
  - `briefs/E1-T1-MECH.md`: Шаг 0 дополняется порядком §1, оговоркой §4 и требованием §6.
  - `04_ROADMAP`: правки нет (детализация Epic-1 живёт задача-строками в `07`, `ADR-025 §2`).
- **Статус:** §1–§7 accepted.

## ADR-048 · `Q-53` остаточное: гигиенический эффект `ADR-023 §4` подтверждается прогоном; форма — строка верификации, не открытый вопрос
- **Контекст:** п.9 очереди `-2`. `Q-53` закрыт применением: `attemptDeadline` `finance-daily-update` поднят 180s → 1800s, read-back подтверждён, OIDC `etl-sa@msklad-bi-prod.iam.gserviceaccount.com` уцелел (`ADR-023 §6` исполнен). Остаток: `status.code = 4` — статус прогона 2026-07-24, ДО правки; конфигом не очищается.
- **Решение:**
  - §1 `ADR-023 §4` (raise = гигиена/шумоподавление) считается подтверждённым **по эффекту** только после наблюдения прогона с новым значением. Обоснование: `ADR-021 §2` — конфиг не есть факт о поведении; read-back подтверждает применение, не результат. **accepted.**
  - §2 Форма — **ОДНА строка верификации** в следующем session-блоке, НЕ открытый вопрос в GAP-реестре: `gcloud scheduler jobs describe finance-daily-update --location=asia-east1 --format="yaml(lastAttemptTime,status)"`. Ожидание: `lastAttemptTime` со слотом `2026-07-25T21:00Z` либо позже и пустой `status` либо `code: 0`. Регион `asia-east1` — из `11 §CF` (зафиксирован сессией `E1-T1-MECH-PREP-ADJ`), не догадка. **accepted.**
  - §3 Ничего не гейтит: `ADR-023 §4` уже переквалифицировал raise из safety-гейта в гигиену, ложный 504 функциональных последствий не имеет (`maxRetryDuration=0s` ⇒ ретрая нет; монитор на 5xx Cloud Run, а Cloud Run вернул 200). **accepted.**
  - §4 `Q-53` остаётся **CLOSED** — хвост верификации не переоткрывает закрытое решение (прецедент применения: `ADR-021 §2` адресует метку, не решение). **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-048 (append). Кросс: `ADR-021 §2`, `ADR-023 §4/§6`, `Q-53`.
  - `07_STATE`: `Q-53` → добавлена строка «хвост верификации назначен»; новых `Q` не заводится.
  - `11 §CF`: правки нет.
- **Статус:** §1–§4 accepted.

## ADR-049 · `min_date` в `core.dim_fx_rates` — не аномалия, а незадокументированный предшествующий источник; часть покрытия — forward-fill, не наблюдённый курс
- **Контекст:** п.1 очереди `-3`. Замер `/reference/fx_outbound_coverage_2026-07-26.md §1` (2026-07-26T13:44:00Z): `min_date = 2025-04-29`, `max_date = 2026-07-26`, 454 строки = 454 различных даты = 454 календарных дня, дыр нет. `02 §core.dim_fx_rates` стр.132 утверждает «Источник с 2026-06-03: Bakai Bank OpenBanking API». Расхождение ~13 месяцев. Очередь предложила (a) discovery либо (b) DEFER новым `Q`.
- **Решение:**
  - §1 Ни (a), ни (b) — факт уже в репо, в замороженных артефактах-источниках (`07 §Артефакты-источники`): `RUNBOOK_v8` журнал инцидентов `М-25` (2026-06-03) — «CF-FX мигрирован **с НБКР XLS → Bakai Bank API**»; `RUNBOOK_v8 §13` — «cf-fx теперь использует Bakai **вместо НБКР XLS**»; `PROJECT_REFERENCE_v6` стр.180 — «cf-fx (**после миграции** 2026-06-03)»; `TD-CF-FX` закрыт той же датой с формулировкой «MERGE по дате — дублей нет», то есть до 2026-06-03 таблица наполнялась и имела дубли. Discovery ставить не на что. **accepted.**
  - §2 Квалификация: `02` не ложна — «Источник **с** 2026-06-03: Bakai» буквально верно. `02` **неполна как контракт таблицы**: умалчивает предшествующий источник и потому позволяет прочесть дату миграции источника как дату начала покрытия. Правится патчем `02` (STABLE ⇒ через настоящий ADR), не новым `Q`. **accepted.**
  - §3 **Находка сверх п.1, существеннее самой аномалии.** Покрытие 454/454 без дыр означает курс на каждом календарном дне, включая выходные. Репо-факты: источник (официальный курс НБКР) в выходные не публикуется — `RUNBOOK_v8` журнал 2026-05-13 («FX lag > 3 дня (**выходные**)» → forward-fill 4 строки), `М-19` 2026-05-18 (lag 7 дней → 7 строк), `М-24` 2026-05-21, `М-28` 2026-06-03 (7 строк); механизм — `cf-fx` делает forward-fill автоматически при 401 (`PROJECT_REFERENCE_v6` стр.186/757), ручной forward-fill `INSERT` — штатная процедура `RUNBOOK_v8 §13.4`. ⇒ **структурно установлено: часть строк — копии предыдущего курса.** Доля не замерена — `Q-66`. Форма квалификации — прецедент `Q-30`. **accepted.**
  - §4 Следствие §3 для `ADR-039 §2` — **фактура, не решение** (продолжение `ADR-039 §3`): вариант «курс на дату периода» на текущем наполнении даёт курс последнего дня, когда источник ответил, а не курс дня. Май-2026 — один из месяцев, где forward-fill применялся минимум трижды. Решение без этого факта было бы принято на неверной картине качества таблицы. **accepted (как фактура).**
  - §5 Остаток: почему `min_date` именно `2025-04-29`, не документировано. Отдельный `Q` НЕ заводится — величина не гейтит ни одно решение, триггера промоушена нет (ср. `Q-35`/`Q-65`, у которых он есть). Факт фиксируется прозой в `02`. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-049 (append). Кросс: `ADR-002`, `ADR-021 §2`, `ADR-039 §2/§3/§4`, `Q-30`, `Q-56`, `Q-66`.
  - `02_ERP_CONTRACTS.md` стр.132: замена строки на блок «два периода источника + покрытие + предупреждение о forward-fill».
  - `07_STATE`: `FX-OUTBOUND-COVERAGE` → DONE; `Q-56` — предусловие `ADR-039 §4` снято; новый `Q-66`.
  - `03 §cf-fx`: правки НЕ назначается — поведенческий контракт уже перенесён (`04_ROADMAP` M-P4-03c); следствие адресовано **потребителям** таблицы, их дом — `02`.
- **Статус:** §1–§5 accepted.

## ADR-050 · Развилка `ADR-039 §2` — трёхчленная, не двухчленная; `gmroi_by_folder` не пункт политики; `sq_marts_sales_overview` уже реализует вариант «по дате»
- **Контекст:** п.2 очереди `-3`. Замер `/reference/fx_outbound_coverage_2026-07-26.md §2`: основной путь `latest_fx` — `abc_xyz`, `expenses`, `gmroi`, `in_transit`, `inventory_health` (5); fallback — `supplier_price_history`; косвенно — `gmroi_by_folder` (`MAX(rate_kgs_per_usd)` из `marts.gmroi`); `sales_overview` — `LEFT JOIN dim_fx_rates fx ON f.transaction_date = fx.date` (стр.120–121). Очередь предложила развилку «единая политика на все 5+1 / политика per-метрика».
- **Решение:**
  - §1 Owner-gated статус `ADR-039 §2` **подтверждается**: класс DERIVED, границу `ADR-002`/`ADR-005` не нарушает ни одна форма. **accepted.**
  - §2 Дихотомия очереди **неполна**. `sq_marts_sales_overview` уже конвертирует по дате ⇒ обе формы существуют в репо одновременно, неоднородность есть действующее состояние, а не будущее следствие. Развилка переформулируется: **(b′)** привести 5 к форме `sales_overview` — устранить неоднородность (цена: 5 правок SQL); **(a′)** оставить `latest_fx` в 5 — узаконить решением (цена: 0 правок кода, правка `03 §marts`); **(c′)** применять форму по типу метрики (цена: правки в подмножестве + критерий отнесения). **accepted.**
  - §3 `gmroi_by_folder` — **не пункт политики**: своего запроса к `core.dim_fx_rates` нет, наследует решение по `gmroi` автоматически. Счёт «5+1» завышает объём: пунктов решения — **5**, плюс один частичный, плюс один производный. Цена (b′) = 5 правок, не 7. **accepted.**
  - §4 `supplier_price_history` — контрпример бинарности изнутри кода проекта: основной join по дате заказа, `latest_fx` только как fallback при NULL. **Третья форма** — «по дате с деградацией на последний курс» — уже реализована и применима к потоку; снимает противопоставление «состояние vs поток» из `ADR-039 §3`. **accepted (как фактура).**
  - §5 Разметка по типу метрики (фактура для (c′)): состояние-на-сейчас — `in_transit`, `inventory_health`; исторический поток — `expenses`; смешанные/оконные — `gmroi`, `abc_xyz` (окно потока, применённое к текущему стоку). Куда отнести последние два, из репо не выводится — это часть политики, не вход к ней. **accepted.**
  - §6 Новое расхождение док/код: `03 §marts` стр.126–134 документирует `latest_fx` как **канонический**, живой `sq_marts_sales_overview` канону не следует. Класс — `Q-60`. Заводится `Q-67`; какая форма канон — это ровно `ADR-039 §2`, здесь не решается. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-050 (append). Кросс: `ADR-002`, `ADR-005`, `ADR-039 §1/§2/§3/§5`, `Q-56`, `Q-60`, `Q-67`.
  - `07_STATE`: `Q-56` расширен фактурой; новый `Q-67`.
  - `03 §marts`: правки НЕ назначается до решения владельца. SQL флота НЕ правится (`ADR-039 §1`).
- **Статус:** §1–§6 accepted.

## ADR-051 · Интерактивный Cloud Shell как ненадёжный транспорт: один `Q`, не под-класс; `ADR-014 §3` — триггер перевода из «рекомендуется» в условие
- **Контекст:** п.3 очереди `-3`. Прямая вставка copy-paste bash-блока в интерактивный Cloud Shell дважды подряд обрывала терминал целиком — потеря сессии без следа, не наложение строк как в `Q-62`. Обход (heredoc-файл + `nohup`) сработал. Причина не установлена: падение наблюдал человек, у модели доступа к консоли не было.
- **Решение:**
  - §1 Причина **не выводится** (`_METHOD §6`): наблюдения изнутри консоли нет, гипотезы (объём паста, глубина блока, лимит буфера терминала, обрыв WebSocket) не различены. **accepted.**
  - §2 Заводится **один** `Q-68` с кроссом на `Q-62`, не под-класс и не второй параллельный `Q`: у вопроса «под-класс или отдельный факт» нет содержательных последствий — общий наблюдаемый признак (объём/глубина блока в интерактивном шелле) и общий сработавший обход (доставка через файл). Две реестровые строки дали бы два DEFER об одном. **accepted.**
  - §3 Приоритет **ниже** `Q-62`: там последствие подтверждено фактом (из искажённого паста ушёл `gcloud scheduler jobs run`, не-идемпотентное действие исполнилось раньше, чем человек увидел состояние джоба); здесь последствий нет — сессия умерла целиком, ничего не исполнив. Тихий частично-исполняющийся отказ дороже громкого. **accepted.**
  - §4 **Содержательный выход пункта — не новый `Q`, а перевод `ADR-014 §3` из «[рекомендуется]» в условие доставки.** `ADR-040 §Диагноз` уже поставил диагноз этому классу формулировок: правило, не ставшее ничьим условием, не исполняется (`RQ-3` прожил шесть ревизий). Два инцидента класса в двух сессиях подряд — триггер, по прецеденту `ADR-040 §2/§4`. Формулировка вынесена в `ADR-055`. **accepted.**
  - §5 Консолидация черновиков: `proposed`-правило по `Q-62` (очередь `-2` п.3) и UTC-якорь (очередь `-2` п.4, `ADR-046 §5`) в `05` не приземлились ⇒ черновики, консолидация разрешена (`ADR-021 §1`). Свёрнуты в `ADR-055`. Дословный текст черновика жил только в переписке ретайрнутой сессии — инстанс `Q-65`, не отдельная проблема. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-051 (append). Кросс: `ADR-014 §3`, `ADR-021 §1`, `ADR-040 §2/§4`, `ADR-043`, `ADR-046 §5`, `ADR-055`, `Q-62`, `Q-65`, `Q-68`.
  - `07_STATE`: новый `Q-68`; `Q-62` получает кросс.
- **Статус:** §1–§5 accepted.

## ADR-052 · `11_INFRA_FACTS.md` @ `98097a5` содержал копию `05_CONVENTIONS.md`; причина — ошибка коммита владельца; файл восстановлен, `B-12` заведён и снят
- **Контекст:** обнаружено сессией `FX-OUTBOUND-COVERAGE-ADJ`. `11_INFRA_FACTS.md` @ SHA `98097a5d06803504d5e15d9362c4772b4bd837c8` — 20 520 байт, 224 строки; строка 1 `# FILE: 05_CONVENTIONS.md`, последняя `--- END 05_CONVENTIONS.md ---`; побайтовое совпадение с `05_CONVENTIONS.md` того же SHA до строки 209, где `05` продолжается разделом `ADR-046`, а `11` заканчивается Частью IV ⇒ `11` был копией `05` в редакции ДО вставки `ADR-046`. Инфра-фактов — ноль. Проверено против независимого источника (тарбол репо того же SHA — байт-в-байт то же) ⇒ содержимое репо, не артефакт чтения (`ADR-021 §2`).
- **Решение:**
  - §1 Класс — **СТРУКТУРНОЕ НЕСООТВЕТСТВИЕ** (`_METHOD §8`), не `CONTEXT GAP`: файл читался, но не соответствовал роли по `00_CHARTER §карта документов` стр.53. **Хуже `Q-35`** (404, отказ громкий): здесь `HTTP 200` и валидный на вид markdown ⇒ отказ **тихий**. **accepted.**
  - §2 Заведён **`B-12`**. Что держал: (i) адресат патча `ADR-045 §6` — `11 §SQ` стр.49–50, при том что замер каденции есть жёсткое предусловие генерации брифа `E1-T1-MECH-CUTOVER` (`ADR-045 §3`) ⇒ критический путь Epic-1; (ii) строку верификации `ADR-048 §2` (регион `asia-east1` «из `11 §CF`»); (iii) приёмку `E1-T1-MECH` п.5 — полный `transferConfig` ID (`briefs/E1-T1-MECH.md` стр.230); (iv) `ADR-014 §1` — имена секретов. **accepted.**
  - §3 Что НЕ держал: Шаг 0/0б `E1-T1-MECH` (read-only по `core.fact_*`) и `FX-OUTBOUND-COVERAGE` (закрыта артефактом). Эскалация в критический путь Шага 0 не назначалась. **accepted.**
  - §4 **Причина УСТАНОВЛЕНА владельцем (2026-07-26): некорректный коммит обновления `11` — не промах applier'а.** Смежный прецедент по тому же файлу — `Q-60` (патч `11` не применён, тогда как патч `07` применён): два тихих промаха по одному файлу, механизмы разные (пропуск против подмены). Защита от повтора вводится `ADR-054` по прямому запросу владельца — в форме правила остановки для любой роли, не в форме hook'а. **accepted.**
  - §5 **Восстановление ВЫПОЛНЕНО владельцем и верифицировано артефактом** (`ADR-021 §2`): `11_INFRA_FACTS.md` @ SHA `206244dfab3035d4e86e30a893feb983868bb0eb` (по этому файлу идентичен с `f8652632990702f84e209367fa0cb610c50d983b`) — 12 158 байт, 75 строк, версия 0.2 (M-P4-A-03). Полный дифф дерева `98097a5 → 206244d` — изменён ровно один файл ⇒ якоря прочих патчей сессии действительны без правки. Четыре зависимости §2 проверены поимённо: `§SQ` стр.49 = `sq_marts_expenses`, стр.50 = `sq_marts_customer_invoices_ar` (**номера строк совпадают с цитатой `ADR-045 §6`** — адресат воспроизведён точно); `§CF` стр.25 — `finance-daily-update`, `asia-east1`; полные Config ID `6a22a243-0000-20fd-a458-883d24f4cad4` и `6a23f3ea-0000-2952-853d-582429be7ecc`; `§секреты` — `msklad-token`, `bakai-fx-token`. Исправление `Q-60` присутствует (стр.22). **accepted.**
  - §6 Путь «восстановить `11` из замороженных источников» остаётся **ОТКЛОНЁННЫМ** и после факта: `11` — LIVING-док волатильных фактов, замороженный источник дал бы состояние на дату заморозки ⇒ документ, который врёт по-другому и врёт тихо. Восстановление из истории коммитов — единственный корректный путь для этого класса дока; фиксируется как прецедент. **accepted.**
  - §7 Побочный факт, правило из него НЕ выводится (одна точка, `_METHOD §6`): `briefs/E1-T1-MECH.md` стр.22 и `briefs/FX-OUTBOUND-COVERAGE.md` стр.23 включают `11` в context-to-load, пиннясь на свои SHA (`bee5a10e` / `1d572461`) ⇒ ни один разработчик не был обязан увидеть порчу @ `98097a5`. **accepted.**
  - §8 `B-12` заводится в `07 §Блокеры` СРАЗУ СНЯТЫМ — прецедент формы `ADR-042 §4`. Отличие: там блокер снят тем же фактом, которым заводился; здесь он реально существовал внутри сессии и снят отдельным действием владельца, поэтому запись сохраняет обе даты. Мотив сохранить строку: `§2` есть единственный в репо реестр «что зависит от `11`», полезный независимо от инцидента. **accepted.**
  - §9 Висячий указатель в восстановленном файле, стр.25: ссылка на `/reference/sched_attempt_deadline_readback_2026-07-26.md`, файла в репо нет, при этом на этом read-back'е стоит `ADR-048`. Отдельный `Q` НЕ заводится — провенанс восстанавливается **фикс-форвардом** тем же прогоном, который `ADR-048 §2` уже назначил: команда `describe` расширена полем `attemptDeadline`, вывод сохраняется под тем именем, на которое ссылка указывает. Это подтверждение значения **на сегодня**, не ретроактивная валидация прошлого read-back'а. Второй указатель — `10_OPS_PLAYBOOK §17` в стр.67 — уже зарегистрирован `Q-35` (он прямо перечисляет `11 §секреты` среди ссылающихся); фактическая процедура в `RUNBOOK_v8 §17`, указатель правится как LIVING-док без ADR. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-052 (append). Кросс: `ADR-014 §1`, `ADR-021 §2`, `ADR-023 §5/§6`, `ADR-025 §2`, `ADR-042 §4`, `ADR-045 §3/§5/§6`, `ADR-048 §2`, `ADR-054`, `Q-35`, `Q-41`, `Q-60`, `_METHOD §8`.
  - `07_STATE`: `B-12` заведён и СНЯТ; `INFRA-FACTS-RESTORE` → DONE; «Текущий фокус» дополнен.
  - `11_INFRA_FACTS.md`: правится только указатель стр.67. Патчи `ADR-045 §6` / `ADR-048 §2` теперь имеют действующий адресат.
  - `00_CHARTER §карта документов`: правки нет.
- **Статус:** §1–§9 accepted.

## ADR-053 · Решение по `ADR-039 §2` не принимается до замера доли forward-fill; порог интерпретации зафиксирован ДО замера
- **Контекст:** `ADR-049 §3/§4` установил, что часть строк `core.dim_fx_rates` — копии предыдущего курса, доля не замерена (`Q-66`). Владелец согласился с рекомендацией архитектора: сначала цифра, потом политика (2026-07-26).
- **Решение:**
  - §1 Решение по политике исходящей конвертации (`ADR-039 §2`) **не принимается до замера**. Основание: ценность варианта «по дате» прямо зависит от того, сколько дней месяца несут настоящий курс. Выбор до замера — выбор без цены варианта. **accepted (владелец, 2026-07-26).**
  - §2 Форма — **один read-only запрос**, не discovery-бриф и не кампания. Прецедент формы — `ADR-048 §2` (одна строка верификации вместо открытого вопроса). Свёрнуто с read-back'ом `ADR-052 §9` в один прогон. **accepted.**
  - §3 Замер — **жёсткое предусловие решения владельца**, не плавающий DEFER (прецедент `ADR-045 §3`; диагноз формы — `ADR-040 §4`). **accepted.**
  - §4 Ничего не гейтит: формула в `E1-T1-MECH` не трогается (`ADR-039 §1`), замер read-only, идёт параллельно Шагу 0. **accepted.**
  - §5 **Порог интерпретации фиксируется ДО получения числа**, чтобы результат не подгонялся под предпочтение. Метрика — число дней-копий, выпавших на РАБОЧИЕ дни мая-2026 (копия в субботу/воскресенье ожидаема — источник в выходные не публикует, `ADR-049 §3`; копия в рабочий день есть след сбоя). Май-2026 содержит 10 суббот и воскресений (5+5, 1 мая — пятница); праздничный календарь КР в репо не зафиксирован и в порог не вводится. **0–2 рабочих дня-копии** ⇒ рекомендация архитектора будет `(b′)` (привести 5 витрин к курсу по дате). **≥ 5** ⇒ рекомендация будет `(c′)` (по типу метрики + явная пометка качества курса в витрине), поскольку точность `(b′)` в этом случае фиктивна. **3–4** ⇒ граница, решение владельца, архитектор раскладывает цену обоих на числе. Порог — рекомендация, не решение; `ADR-039 §2` остаётся owner-gated. **accepted.**
  - §6 Ограничение метода замера, фиксируется явно: равенство курса двум соседним дням есть **верхняя граница** forward-fill, а не доказательство — источник мог опубликовать то же значение дважды (`ADR-044 §1`: автовердикт не факт). Поэтому запрос обязан печатать май построчно с днём недели, а трактовка идёт по выдержке, не по проценту (`ADR-044 §2`). **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-053 (append). Кросс: `ADR-039 §2`, `ADR-040 §4`, `ADR-044 §1/§2`, `ADR-045 §3`, `ADR-048 §2`, `ADR-049 §3/§4`, `Q-56`, `Q-66`.
  - `07_STATE`: `Q-66` → форма замера и порог назначены; `Q-56` → отмечено, что решение владельца ждёт замера.
  - SQL флота, `03 §marts`: правок нет.
- **Статус:** §1–§6 accepted.

## ADR-054 · Содержимое файла не соответствует заявленному — немедленная остановка любой роли (прямой запрос владельца)
- **Контекст:** `ADR-052`. Владелец сформулировал требование прямо (2026-07-26): если модель видит, что вместо ожидаемого дока внутри лежит другое содержимое — стоп и эскалация, а не попытка отработать сессию. Мотив владельца дословно по смыслу: сессия, прошедшая на подменённом доке, тратится дважды — сначала на неверный проход, потом на повторный после исправления. Предложенные архитектором альтернативы (git pre-commit hook, полная проверка целостности набора) владельцем не выбраны — выбрана форма правила остановки.
- **Решение:**
  - §1 Любая роль после загрузки контекста, **до переформулировки задачи и до любой содержательной работы**, сверяет по каждому загруженному файлу репо, что это тот файл: первая строка совпадает с именем/номером файла (все доки проекта начинаются с `# FILE: <имя>` либо `# NN · <ИМЯ>`). **accepted (владелец).**
  - §2 Не совпало → **немедленная остановка** с явной меткой и запросом человеку:
    `СОДЕРЖИМОЕ НЕ ТО: <файл> — ожидалось <что>, внутри <что>`
    Продолжать сессию на подменённом файле **запрещено**, даже если кажется, что остального контекста достаточно. Токены, потраченные на проход по неверному доку, тратятся вторично после исправления — это и есть цена нарушения. **accepted (владелец).**
  - §3 Метка отличается от `CONTEXT GAP` **намеренно**: `CONTEXT GAP` = файла/значения нет; здесь файл есть, читается и отвечает `HTTP 200` — отказ тихий, и ему нужно собственное громкое имя, иначе он проскакивает мимо правила anti-improvisation. **accepted.**
  - §4 Scope — все роли, все файлы репо. Вносится в `05` Часть I (архитектор, разработчик) **и** в `_APPLIER.md` (applier читает `_APPLIER` + затрагиваемые файлы, `05` он не грузит — по диагнозу `ADR-040 §4` правило, не попавшее в читаемый ролью файл, не исполняется). **accepted.**
  - §5 Форма «git pre-commit hook» настоящим ADR **не вводится** — владелец выбрал правило остановки, не машинную проверку на коммите. Путь не закрыт: если класс воспроизведётся при действующем §1–§2, hook возвращается к обсуждению. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-054 (append). Кросс: `ADR-021 §2`, `ADR-040 §4`, `ADR-052 §1/§4`, `Q-35`, `Q-60`, `_METHOD §6/§8`.
  - `05` Часть I: новый пункт «★ Содержимое файла ≠ заявленному → стоп».
  - `_APPLIER.md`: вставка того же правила в блок правил доступа.
  - `07_STATE`: правки не требует.
- **Статус:** §1–§5 accepted (прямой запрос владельца, форма как `ADR-014 §1/§2`).

## ADR-055 · Доставка исполняемых артефактов — условие, а не рекомендация; три правила (апрув владельца)
- **Контекст:** `ADR-051 §4/§5`. `ADR-014 §3` имел статус «[рекомендуется]» и не исполнялся: два инцидента одного класса в двух сессиях подряд (`Q-62` — наложение строк с тихим исполнением не-идемпотентной команды; `Q-68` — полная потеря сессии терминала). Архитектор предложил пять пунктов, владелец выбрал сжатую форму — три (2026-07-26).
- **Решение:**
  - §1 **Доставка только через файл.** Скрипт передаётся как heredoc-запись в файл и запускается `bash script.sh > run.log 2>&1; cat run.log`. Прямая вставка исполняемого блока в интерактивный шелл формой доставки **не является**. Сюда свёрнуты запреты многострочного `bash -c '…'` с вложенным квотингом и блокирующего `sleep` внутри блока — они перестают быть отдельными правилами, потому что при доставке через файл не возникают. Это перевод `ADR-014 §3` из «[рекомендуется]» в **условие**; `ADR-014` не редактируется (append-only), расширяется настоящим ADR — прецедент формы `ADR-043 §4`. **accepted (владелец).**
  - §2 **Не-идемпотентное действие — отдельным пастом, одной командой, после подтверждения состояния.** Запрещено объединять диагностику и действие (`gcloud … run`, `deploy`, `create`) в одном блоке. Основание — `Q-62`: команда ушла раньше, чем человек увидел состояние джоба; ущерба не было случайно (ингест идемпотентен). **accepted (владелец).**
  - §3 **UTC-якорь.** Сессия, делающая любой расчёт по расписаниям, времени прогонов или слотам, снимает `date -u` **первой командой этой же сессии** и считает от него; локальная дата контекста якорем расчёта не является. Основание — `Q-63`. Разграничение с `ADR-046`: датировка сессий и артефактов остаётся локальной (`§1`), якорь расчёта — UTC. **accepted (владелец).**
  - §4 `ADR-043` (диагностика не убирает за собой, путь печатается последней строкой) остаётся в силе и не переоткрывается. **accepted.**
  - §5 Scope — диагностика и разовые операционные скрипты. Прод-пайплайны и деплой-скрипты не затрагиваются. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-055 (append). Кросс: `ADR-014 §3`, `ADR-021 §1`, `ADR-040 §2/§4`, `ADR-043`, `ADR-046 §1/§3`, `ADR-051`, `Q-62`, `Q-63`, `Q-68`.
  - `05` Часть II: блок «Стандарт доставки исполняемых артефактов (ADR-014)» дополняется пунктом-условием; исходный текст не переписывается.
  - `07_STATE`: `Q-62` и `Q-68` получают путь закрытия; ни один не закрывается автоматически — оба ждут первой сессии, прошедшей по новому правилу без инцидента.
- **Статус:** §1–§5 accepted (апрув владельца явный, `_METHOD §8`).

## ADR-056 · Язык коммуникации с владельцем — правило для всех роль-файлов (прямой запрос владельца)
- **Контекст:** владелец заявил прямо (2026-07-26): ответы моделей перегружены внутренними идентификаторами и незакрытыми рассуждениями, из-за чего модерация сессий требует лишней когнитивной работы. Требование повторялось неоднократно и не было зафиксировано ни в одном файле проекта — то есть жило только в переписке, что по диагнозу `ADR-040 §4` гарантирует неисполнение. Прямой прецедент класса: `ADR-014 §1/§2` (владелец отклонил формат доставки и попросил зафиксировать правилом).
- **Решение:**
  - §1 **Проза, адресованная владельцу, читается без открывания репо.** Внутренние идентификаторы (`ADR-…`, `Q-…`, `B-…`, `RQ-…`, task-ID) допускаются в session-блоках, патчах, ADR и доках — там они и есть адресация. В прозе к владельцу идентификатор допустим **только** вместе с расшифровкой смысла в той же фразе, и только если без него нельзя. **accepted (владелец).**
  - §2 **Каждый ответ владельцу разделён на два слоя:** «для владельца» — обычными словами, что решено и что делать; «для репо» — патчи и session-блок, где терминология обязательна. Смешивать слои запрещено. **accepted (владелец).**
  - §3 **Рассуждение без вывода не выдаётся.** Любой блок, адресованный владельцу, кончается тем, что делать: действие, развилка или явное «действий не требуется». **accepted (владелец).**
  - §4 **Вопрос владельцу подаётся только полным блоком:** что решаем обычными словами → варианты → плюсы → минусы и риски → рекомендация архитектора с обоснованием. Вопрос без вариантов и без рекомендации не задаётся. Это распространяется и на любые подсветки и предупреждения. **accepted (владелец).**
  - §5 **Вопрос, который роль может снять своей работой, владельцу не задаётся** — сначала снимается: замером, чтением репо, командой, которую владелец и так запускает. Живой прецедент нарушения: в очереди `-3` архитектор спросил владельца про наличие прошлого read-back'а `attemptDeadline`, тогда как значение снимается тем же `describe`, который уже был назначен (`ADR-052 §9`) — вопрос был лишним по построению. **accepted (владелец).**
  - §6 **Порядок изложения: сначала ответ, потом обоснование.** Не наоборот. **accepted (владелец).**
  - §7 Проверяемость: §1 и §4 машинно проверяемы (идентификатор без расшифровки в прозе к владельцу; вопрос без блока «варианты / минусы / рекомендация»). §3, §5, §6 проверяются ревью ответа. Правило вносится в `05` Часть I (общее для ролей) и продублировано ссылкой в `_ARCHITECT §6` не будет — `05` Часть I читают архитектор и разработчик, `_APPLIER` владельцу прозы не адресует. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-056 (append). Кросс: `ADR-014 §1/§2`, `ADR-040 §4`, `ADR-052 §9`, `_METHOD §8`.
  - `05` Часть I: новый пункт «★ Язык коммуникации с владельцем».
  - `07_STATE`: правки не требует.
- **Статус:** §1–§7 accepted (прямой запрос владельца).

## ADR-057 · Замер forward-fill закрыт фактом; порог `ADR-053 §5` объявлен вне модели и механически не применяется; выбор политики `ADR-039 §2` уходит в DEFER
- **Контекст:** `ADR-053 §2` назначил замер одним read-only запросом. Артефакт возвращён и закоммичен — `/reference/fx_forward_fill_2026-07-26.md` (@ SHA `5c10dc0b63aa5e0d25e11655b0d6a4af7d2f72c8`), прогон `2026-07-26T14:46:56Z`. Результат: **май-2026 несёт одно значение `87.4205` на все 31 день** (вошло 2026-05-01, внутримесячных изменений ноль); доля дней, в которые значение не менялось — 63,3 % (апрель), 96,8 % (май), 86,7 % (июнь), 73,1 % (июль 1–26); копий в рабочие дни 13–20 в месяц. Миграция источника 2026-06-03 (`ADR-049 §1`) режим качественно не изменила.
- **Решение:**
  - §1 `Q-66` **CLOSED замером.** Артефакт удовлетворяет `ADR-021 §2` (числа плюс построчная выдержка мая, а не метка) и `ADR-044 §2` (напечатаны строки). **accepted.**
  - §2 **Порог `ADR-053 §5` формально пройден в сторону `(c′)` (20 ≥ 5), но механически НЕ применяется.** Основание: порог строился под модель «forward-fill есть меньшинство дней, измеряем его размер»; измеренная величина (96,8 % за май, ни одного изменения за месяц) лежит **за пределами** этой модели. Это НЕ переинтерпретация порога под удобный ответ — что было бы прямым нарушением цели, для которой `ADR-053 §5` вводился: порог **не сдвигается и не переписывается**, он объявляется неприменимым с указанием причины, и вместо ответа по нему поднимается новый вопрос. Прецедент формы — `ADR-045 §1` (наблюдение не интерпретируется, пока гипотезы не различены). **accepted.**
  - §3 **Выбор политики `ADR-039 §2` уходит в DEFER** с новым предусловием: различить причину малоподвижности курса (`Q-69`). Основание — `_METHOD §4` закон-следствие и `00_CHARTER §закон-следствие`: ни один вариант конвертации не исправляет вход, который не меняется; выбор формулы поверх непроверенного входа есть костыль на стороне потребителя вместо фикс-форварда в слой-источник. Владелец подтвердил приоритет явно (2026-07-26, вариант 1 из развилки сессии). **accepted (владелец).**
  - §4 Фактическое следствие, **снижающее ставку** решения `ADR-039 §2`: для мая-2026 варианты «по курсу на дату операции» и «по последнему курсу на конец мая» дают **одно и то же число** — в мае одно значение. Различается только «по сегодняшнему курсу». Значит действующая неоднородность флота (`ADR-050 §2`) на майских числах практически не проявляется, включая `sq_marts_sales_overview`, который джойнит по дате: он умножает майскую выручку на ту же константу, что и `latest_fx`-файлы. **accepted.**
  - §5 Границы контракта не задеты: референс-оракул мая выражен в KGS (`ADR-005`, `/reference/pnl_2026-05.md`, целевая величина 10 232 903,20), `total_sum_usd` — класс DERIVED (`ADR-039 §2`). Приёмка `E1-T1-MECH` не затронута; эскалация в её критический путь не назначается. **accepted.**
  - §6 Новый факт сверх задания замера, фиксируется **без интерпретации**: в апреле и июле часть выходных дней несёт НОВОЕ значение (апрель — 8 выходных при 5 копиях в выходные; июль — 8 при 6). Это не согласуется с формулировкой «источник в выходные не публикует» (`ADR-049 §3`, опирающейся на `RUNBOOK_v8` журнал 2026-05-13). Формулировка `02` под наблюдение **не переписывается** — она трассируется к замороженному источнику; расхождение уходит в `Q-69` как часть того же вопроса о точности таблицы. **accepted.**
  - §7 `03 §marts` и SQL флота не правятся (`ADR-039 §1`). `Q-67` закрывается тем же решением, что `Q-56`, следовательно сдвигается вместе с ним. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-057 (append). Кросс: `ADR-005`, `ADR-021 §2`, `ADR-039 §1/§2`, `ADR-044 §2`, `ADR-045 §1`, `ADR-049 §1/§3`, `ADR-050 §2`, `ADR-053 §1–§6`, `Q-30`, `Q-56`, `Q-66`, `Q-67`, `Q-69`.
  - `02 §core.dim_fx_rates`: строка «Доля не замерена — `Q-66`…» заменяется замеренным фактом (патч 2).
  - `07_STATE`: `Q-66` → CLOSED; `Q-56` и `Q-67` → DEFER с новым предусловием; новый `Q-69`; задача-строка `FX-RATE-FIDELITY`.
  - `03_PIPELINE_SPEC`, `04_ROADMAP`, `/reference/sql/*`: правок нет.
- **Статус:** §1–§7 accepted.

## ADR-058 · Точность `core.dim_fx_rates`: две неразличённые гипотезы; различитель — GCS-архив ответа источника плюс логи `cf-fx`; форма — один discovery-бриф
- **Контекст:** `ADR-057 §3`. Гипотеза **(A)** — `cf-fx` или его источник теряет изменения курса (совместимо с автоматическим forward-fill при `401`, `03 §cf-fx`, и с задокументированными сбоями мая: `RUNBOOK_v8` журнал 2026-05-13 forward-fill 4 строки, `М-19` 2026-05-18 lag 7 дней → 7 строк, `М-24` 2026-05-21). Гипотеза **(B)** — официальный курс НБКР действительно малоподвижен, таблица честна. Замер `/reference/fx_forward_fill_2026-07-26.md §7` между ними не выбирает: он смотрит только внутрь таблицы.
- **Решение:**
  - §1 Гипотеза догадкой не выбирается (`_METHOD §6`; прямой прецедент `ADR-045 §1` — вариант «списать на очевидное объяснение» отклонён как закрытие гэпа догадкой). **accepted.**
  - §2 Различитель — **внутренний, read-only, без второго оракула:** `cf-fx` сохраняет снапшот ответа источника в GCS по шаблону `fx-rates/bakai_{YYYY-MM-DD}.json` (`03 §cf-fx` стр.59); бакет `msklad-raw-msklad-bi-prod` (`01_ARCHITECTURE` стр.29). Сверка «что источник отдал» против «что легло в таблицу» за одни и те же даты различает (A) и (B) внутренними средствами. **accepted.**
  - §3 Второй, независимый различитель **того же прогона** — логи `cf-fx` за те же даты, фильтр `resource.type="cloud_run_revision" AND resource.labels.service_name="cf-fx"` (`RUNBOOK_v8 §13.2`, не изобретается): наличие `bakai_token_expired` / `degraded` / признаков forward-fill в дни-копии подтверждает (A) без разбора JSON. Два различителя в одном прогоне — требование `ADR-021 §2` (один инструмент не есть факт). **accepted.**
  - §4 **До-миграционный период (в том числе май-2026, источник НБКР XLS) архивом такого вида не покрыт.** Задача обязана явно доложить, есть ли по нему внутренние свидетельства, и **не добывать** курсы извне. Внешняя сверка исторических курсов настоящим ADR НЕ вводится: `ADR-028` отклонил внешнюю сверку курсов как второй конкурирующий оракул, но сделал это для **входящей** конвертации (`rate.value` документа); применимость того же довода к **исходящей** конвертации (класс `Q-19`) не адъюдицирована и является отдельным owner-gated решением. Два класса не смешивать. **accepted.**
  - §5 Форма — **один discovery-бриф** `briefs/FX-RATE-FIDELITY.md`, read-only; return-this = обогащение репо (`/reference`-артефакт), не прод-код и не правка `cf-fx`. Отдельной кампании не заводится. Замер каденции мартов (`ADR-045 §5`) с этим прогоном **НЕ сворачивается** — другой объект и другой гейт (там предусловие брифа cutover, здесь предусловие политики конвертации). **accepted.**
  - §6 Приоритет: параллельно `E1-T1-MECH`, не гейтит ничего, кроме решения владельца по `ADR-039 §2` и, через него, `Q-56`/`Q-67`. Read-only, живых витрин не касается, риска нет. **accepted.**
  - §7 Ни один из двух исходов настоящим ADR не предрешается. Подтвердится (A) — правка идёт **фикс-форвардом в `cf-fx`** отдельной задачей, и политика конвертации выбирается уже на исправленных данных. Подтвердится (B) — вопрос точности снимается, развилка `ADR-050 §2` возвращается владельцу в прежнем виде, но с известной ценой каждого варианта. Разработчику ADR предлагать не поручается: квалификация результата за архитектором. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-058 (append). Кросс: `ADR-021 §2`, `ADR-028`, `ADR-039 §1/§2`, `ADR-043`, `ADR-044 §2/§3`, `ADR-045 §1/§5`, `ADR-050 §2`, `ADR-054`, `ADR-055`, `ADR-057 §3`, `Q-19`, `Q-30`, `Q-69`.
  - `07_STATE`: новый `Q-69`; задача-строка `FX-RATE-FIDELITY` (READY).
  - Новый файл: `briefs/FX-RATE-FIDELITY.md`.
  - `03 §cf-fx`: правки НЕТ до результата (`ADR-021 §2`: задокументированное поведение не есть факт о поведении).
- **Статус:** §1–§7 accepted.

## ADR-059 · Квалификация выхода FX-RATE-FIDELITY: (B) подтверждена для эпохи Bakai; forward-fill опровергнут как механизм окна мая; вывод «различить нечем» не принят
- **Контекст:** артефакт `/reference/fx_rate_fidelity_2026-07-26.md` (три захода, `ADR-058 §5`).
- **Решение:**
  - §1 Гипотеза (B) accepted для 2026-06-03…2026-07-26. Основание — прямая построчная сверка архива ответа источника с таблицей, 26/26 дат июля-2026, включая все пять дней изменения курса (артефакт §2). Требование `ADR-021 §2` («один инструмент не есть факт») удовлетворено по существу: сравниваются два независимых ИСТОЧНИКА (JSON-снапшот ответа и таблица), а не два инструмента над одним. **accepted.**
  - §2 Побочный факт `ADR-057 §6` CLOSED для эпохи Bakai: источник отвечает в выходные и меняет значение в выходные — 2026-07-04 (Сб) и 2026-07-11 (Сб) несут новые значения при наличии и совпадении файлов архива (артефакт §2). Формулировка «источник в выходные не публикуется» (`02 §core.dim_fx_rates`, трасса `ADR-049 §3` → `RUNBOOK_v8` журнал 2026-05-13) не ложна, но ЭПОХА-ЗАВИСИМА: относится к периоду НБКР XLS и на эпоху Bakai не распространяется. Замороженный источник не переписывается (`ADR-057 §6`); квалификация вносится в `02`. **accepted.**
  - §3 Механизм forward-fill ОПРОВЕРГНУТ как объяснение начала окна 2026-05-01…2026-06-02. forward-fill определён как копия последнего известного курса (`03 §cf-fx` стр.58); 2026-04-30 = 87.4293, 2026-05-01 = 87.4205, `same_as_prev_day = false` — вошло НОВОЕ значение (артефакт §4a; независимо подтверждено ранее закоммиченным `/reference/fx_forward_fill_2026-07-26.md` стр.79/стр.130). Следствие: формулировка гипотезы (A) в `ADR-058 §Контекст` («совместимо с автоматическим forward-fill при 401») к этому окну НЕ применима и подлежит переформулировке до любой проверки. Квалификация артефакта §5 («структурный признак в пользу (A) — потеря обновлений / forward-fill») принимается в первой половине и отклоняется во второй. **accepted.**
  - §4 Вывод артефакта §5/§7 «строгая дифференциация окна недоступна в принципе» НЕ принят — три внутренних, read-only, в scope различителя не исчерпаны:
    - (D1) `nbkr_dailyrus_raw.xls` (698 КБ, создан 2026-06-03T10:01:24Z, Update = Creation) — содержимое НЕ прочитано (артефакт §7). Кандидат на массовую выгрузку ежедневных курсов НБКР, то есть на пропавший архив источника за окно. Чтение файла, уже лежащего в бакете проекта, НЕ есть «добыча курсов извне» и `ADR-058 §4` не нарушает — это тот же класс внутреннего чтения, что и `bakai_*.json`.
    - (D2) История заданий BigQuery по записям в `core.dim_fx_rates` за окно — различает «загрузчик писал одно значение» и «разовая вставка литеральной константы», плюс даёт личность писавшего. Горизонт хранения метаданных заданий — свойство платформы, в репо не зафиксирован; запрос сам покажет, попадает ли окно в него, и пустой результат обязан читаться как гэп наблюдения (`05` Часть I ★, `ADR-021 §2`), не как «заданий не было».
    - (D3) До-миграционный загрузчик курсов не опознан (артефакт §7) — его логи суть прямой аналог различителя `ADR-058 §3` для окна, но искать логи сервиса без имени нечем. Первый шаг — установить имя, второй — фильтр по нему.
    Пока D1–D3 не исполнены, «нечем различить» есть гэп наблюдения, а не факт. **accepted.**
  - §5 Различитель `ADR-058 §3` (логи `cf-fx`) объявляется ИСЧЕРПАННЫМ, а не открытым шагом: для эпохи Bakai избыточен (§1 закрывает вопрос методом сильнее), для окна невозможен — `cf-fx` в связке с Bakai до 2026-06-03 не существовал (`ADR-049 §1`), логов искомого сервиса за окно нет by construction. Технический недобор артефакта §3 (лимит 200 строк выбран шумом AUTOSCALING / STARTUP TCP probe) переисполнению НЕ подлежит. **accepted.**
  - §6 `nbkr_cache.json` — ЛИД, не вывод. Установлено: 365 записей 2025-04-29…2026-04-28, все 87.4293; создан 2026-04-29T15:04:57Z, Update = Creation; начало окна файла буквально совпадает с `min_date` таблицы; источником данных таблицы НЕ является (артефакт §4a запрос 1). Временная смежность (файл записан 04-29; последние гранулярные дни таблицы 04-29/04-30; окно застывания начинается 05-01) — основание для проверки, не для вывода (_METHOD §6). Отдельный `Q` не заводится, лид сворачивается в `Q-70` — прецедент формы `ADR-051 §2` (две реестровые строки об одном дали бы два DEFER). Заодно остаток `ADR-049 §5` («почему `min_date` именно 2025-04-29, не документировано») получает триггер и адресуется тем же `Q-70`; `ADR-049 §5` не переписывается. **accepted.**
  - §7 ЦЕНА РАЗВИЛКИ `ADR-039 §2` — фактура, не решение (продолжение `ADR-039 §3`, `ADR-050 §2/§4/§5`). Все значения `rate_kgs_per_usd`, документированные в четырёх FX-артефактах проекта (`fx_outbound_coverage §1`, `fx_forward_fill §2/§3`, `fx_rate_fidelity §2/§4a`), лежат в [87.4205; 87.45]: разброс 0,0295 KGS = 0,0337 %. На референс-оракуле мая-2026 (10 232 903,20 KGS, `ADR-005` / `ADR-057 §5`) три варианта политики дают 117 053,82 USD против 117 014,33 USD — расхождение 39,49 USD, то есть 0,034 %. Следствие: развилка `ADR-039 §2` по существу есть вопрос ВОСПРОИЗВОДИМОСТИ величины для инвестора (`00_CHARTER §миссия`), а не вопрос точности. Оговорка обязательна: MIN/MAX по всем 454 строкам не замерен — граница построена по наблюдённым значениям и является предположением о ненаблюдённых; твердеет одним запросом, свёрнутым в прогон `FX-MAY-WINDOW`. Это НЕ магнитудный замер `Q-30` (входящая конвертация, класс ÷100 без ×rate) — `Q-30` настоящим ADR не трогается и остаётся DEFER, owner-gated. **accepted (как фактура).**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-059 (append). Кросс: `ADR-005`, `ADR-021 §2`, `ADR-039 §2/§3`, `ADR-044 §1/§2`, `ADR-045 §1`, `ADR-049 §1/§3/§5`, `ADR-050 §1/§2`, `ADR-051 §2`, `ADR-053 §6`, `ADR-057 §1/§3/§4/§5/§6`, `ADR-058 §2/§3/§4/§5/§7`, `Q-30`, `Q-56`, `Q-67`, `Q-69`, `Q-70`.
  - `02_ERP_CONTRACTS.md §core.dim_fx_rates`: четыре правки — append в стр.138, замена стр.140, замена стр.142, замена стр.144 (патчи 1–4).
  - `07_STATE`: `Q-69` сужен (не CLOSED); новый `Q-70`; новая задача-строка `FX-MAY-WINDOW`.
  - `03_PIPELINE_SPEC §cf-fx`: правки НЕТ — поведенческий контракт эпохи Bakai подтверждён фактом.
  - SQL флота: правок нет (`ADR-039 §1`).
  - `/reference`: артефакт `fx_rate_fidelity_2026-07-26.md` подлежит коммиту — @ SHA `9646a19` отдаёт 404 при 200 на соседних файлах каталога, тогда как `07_STATE` того же SHA на него ссылается.
- **Статус:** §1–§7 accepted.

## ADR-060 · 02 §core.dim_fx_rates — предупреждение потребителям сужается до окна 2026-05-01…2026-06-02; для эпохи Bakai снимается фактом
- **Контекст:** `ADR-059 §1/§2/§3`. Действующий текст `02` (стр.138–144) утверждает про таблицу как про единое целое, что `rate_kgs_per_usd` на дату D нельзя трактовать как курс дня D, и что часть строк — forward-fill. Для эпохи Bakai это опровергнуто сверкой с архивом ответа источника; оставление текста как есть создало бы расхождение внутри одного дока — класс `Q-60`, в проекте уже реализовавшийся дважды (`ADR-052`, `Q-67`).
- **Решение:** `02` (STABLE) патчится настоящим ADR. Оговорка становится трёхчастной по периодам: эпоха Bakai — снята фактом; 2025-04-29…2026-04-30 — не подтверждена источником, оговорка сохраняется в ослабленной форме; 2026-05-01…2026-06-02 — механизм не установлен, `Q-70`. Квалификация «часть строк — forward-fill» (`ADR-049 §3`) сохраняет силу для периода НБКР XLS и не распространяется на текущий режим. Дубль-абзац стр.144 устраняется как противоречащий сужению. **accepted.**
- **Последствия:** `02_ERP_CONTRACTS.md §core.dim_fx_rates` — стр.138/140/142/144 (патчи 1–4). `ADR-049` и `ADR-057` не редактируются (append-only, `_METHOD §6`). `03`/`09`/`04` — правок нет.
- **Статус:** accepted.

## ADR-061 · Предусловие DEFER ADR-057 §3 снято; выбор политики ADR-039 §2 возвращён владельцу; окно мая перестаёт быть его гейтом
- **Контекст:** `ADR-057 §3` отложил выбор политики исходящей конвертации до различения причины малоподвижности курса (`Q-69`), и владелец подтвердил этот приоритет явно (2026-07-26). `ADR-059 §1` различил причину для действующего режима: вход, которым политика управляет, проверен. Не различено окно 2026-05-01…2026-06-02 (33 дня в прошлом), причём `ADR-059 §7` ограничивает его максимальный вес величиной 0,034 % (≈ 39,49 USD на майском эталоне), а `ADR-058 §4` оставляет единственный путь его окончательного закрытия — внешние курсы — отдельным неадъюдицированным owner-gated решением.
- **Решение:**
  - §1 Предусловие `ADR-057 §3` считается исполненным в части, управляющей живыми витринами: закон-следствие `_METHOD §4` / `00_CHARTER §закон-следствие` запрещал выбирать формулу поверх непроверенного входа — вход эпохи Bakai проверен (`ADR-059 §1`). **accepted (владелец, 2026-07-26).**
  - §2 Окно 2026-05-01…2026-06-02 (`Q-70`) НЕ гейтит `ADR-039 §2`. Основания: (а) ни один вариант политики не восстанавливает вход за прошедшее окно — фикс-форвард в слой-источник для закрытого периода требует данных, которых внутри проекта нет; (б) удержание предусловия, исполнимость которого не установлена, превращает жёсткое предусловие в бессрочную заморозку — ровно тот отказ, против которого введены `ADR-045 §3` и `ADR-053 §3` и который диагностирован `ADR-040 §Диагноз`. **accepted.**
  - §3 `Q-70` ведётся как независимый вопрос, приоритет ниже `E1-T1-MECH`, форма — один discovery-бриф (прецедент `ADR-058 §5`), read-only, параллельно, гейтит только собственный вывод. Бриф МЕХАНИЧЕСКИЙ: шаги D1–D3 назначены `ADR-059 §4` поимённо, суждения не требуют ⇒ генерируется свежим дешёвым чатом через `_GENERATOR.md` (`_METHOD §9`). **accepted.**
  - §4 Выбор между (a′)/(b′)/(c′) (`ADR-050 §2`) исполнен владельцем — см. `ADR-062`. Настоящий ADR фиксирует только снятие предусловия; owner-gated статус `ADR-050 §1` соблюдён. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-061 (append). Кросс: `ADR-039 §2`, `ADR-040 §Диагноз`, `ADR-045 §3`, `ADR-050 §1/§2`, `ADR-053 §3`, `ADR-057 §3`, `ADR-058 §4`, `ADR-059 §1/§7`, `ADR-062`, `Q-56`, `Q-67`, `Q-69`, `Q-70`.
  - `07_STATE`: `Q-56` и `Q-67` — DEFER снят; `Q-70` — помечается «не гейтит `ADR-039 §2`»; задача-строка `FX-MAY-WINDOW` (READY).
  - `02 §core.dim_fx_rates` стр.144: см. патч 4.
  - SQL флота: правок нет настоящим ADR (см. `ADR-062 §9`).
- **Статус:** §1–§4 accepted.

## ADR-062 · Политика исходящей FX-конвертации — вариант (c′); критерий отнесения = наличие дневного зерна в строке витрины; ADR-050 §2 superseded-in-part по цене (b′); ADR-050 §5/§6 superseded-in-part
- **Контекст:** `ADR-039 §2` (owner-gated), DEFER снят `ADR-061`. Архитектор в той же сессии выдал рекомендацию (b′) и ОТОЗВАЛ её после подъёма живого SQL из `/reference/sql/` — посылка `ADR-050 §2` («цена (b′) = 5 правок SQL») фактически неверна. Владелец принял (c′) 2026-07-26.
- **Решение:**
  - §1 Политика — (c′): форма курса применяется ПО ТИПУ МЕТРИКИ. Единой формы на все марты нет сознательно. **accepted (владелец, 2026-07-26).**
  - §2 Критерий отнесения — НАЛИЧИЕ ДНЕВНОГО ЗЕРНА В СТРОКЕ ВИТРИНЫ. Критерий выводится из структуры витрины (проверяем по SQL), а не назначается вкусом ⇒ `ADR-050 §5` в части «куда отнести `gmroi`/`abc_xyz` из репо не выводится, это часть политики» — SUPERSEDED-IN-PART: выводится. Текст `ADR-050` не редактируется (append-only), прецедент формы `ADR-009 §1`. **accepted.**
  - §3 Разметка флота — ФАКТ из живого SQL (`/reference/sql/`, снапшот D5 2026-07-07 и позднейшие коммиты), не оценка:
    - `sq_marts_expenses` — дневное зерно есть (`p.moment`, присутствует в `GROUP BY`), величина `total_sum_usd` = исторический поток ⇒ КУРС ПО ДАТЕ. Единственная правка.
    - `sq_marts_in_transit` — состояние на текущий момент (`status_name IN ("В пути", "Прибыл частично")`, `days_until_delivery` от `CURRENT_DATE()`) ⇒ последний курс, УЗАКОНЕН.
    - `sq_marts_inventory_health` — конвертирует `stock × cost_kgs` из последнего снапшота (`date_snapshot = MAX(date_snapshot)`) ⇒ последний курс, УЗАКОНЕН.
    - `sq_marts_gmroi` — ключ строки (`period_days`, `product_id`), окна 30/90 дней от `CURRENT_DATE()`, дневного зерна НЕТ; в одной строке смешаны поток (`gross_profit`) и состояние (`avg_inventory`) ⇒ «по дате» НЕИСПОЛНИМО без пересборки агрегата и переопределения метрики ⇒ последний курс + пометка качества.
    - `sq_marts_abc_xyz` — ключ строки `product_id`, окно 90 дней, дневного зерна НЕТ ⇒ то же.
    - `sq_marts_gmroi_by_folder` — `MAX(rate_kgs_per_usd)` из `marts.gmroi`, своего запроса к `core.dim_fx_rates` нет ⇒ наследует, не пункт политики (`ADR-050 §3` подтверждён фактом кода).
    - `sq_marts_supplier_price_history` — уже реализует целевую форму (по дате с деградацией) ⇒ правки нет, служит эталоном формы.
    **accepted.**
  - §4 `ADR-050 §2` SUPERSEDED-IN-PART в части цены (b′): «5 правок SQL» опровергнуто кодом — одна правка исполнима и корректна, две витрины состояния получили бы содержательное ухудшение, две не имеют дневного зерна. Следствие метода: цена варианта, назначенная без чтения кода, не является фактурой (`ADR-021 §2` по существу — задокументированная оценка ≠ факт о коде). **accepted.**
  - §5 Форма правки `expenses` — КОПИЯ существующего в репо паттерна `supplier_price_history`, не изобретение (`_METHOD §6`): `LEFT JOIN core.dim_fx_rates fx ON fx.date = DATE(p.moment)` плюс `COALESCE` на последний курс при отсутствии строки. **accepted.**
  - §6 ДОКУМЕНТИРОВАННАЯ ГРАНИЦА ДЕГРАДАЦИИ: `core.dim_fx_rates.min_date = 2025-04-29`. Платежи ранее этой даты (если есть) получат курс через `COALESCE` ⇒ для самого раннего периода воспроизводимость НЕ достигается. Замер `MIN(moment)` по `core.fact_payments` против 2025-04-29 — обязательный шаг задачи `FX-POLICY-EXPENSES`; результат фиксируется в `03 §marts` как граница, не как сюрприз. **accepted.**
  - §7 Окно 2026-05-01…2026-06-02 (`Q-70`) политику не изменяет: `expenses` по дате за май умножит на ту же константу 87.4205 (`ADR-057 §4`). Если `Q-70` выявит дефект, правка идёт фикс-форвардом в слой курсов, НЕ в марты (`_METHOD §4`, `00_CHARTER §закон-следствие`). **accepted.**
  - §8 Пометка качества курса обязательна для витрин без дневного зерна (`gmroi`, `abc_xyz` и наследующий `gmroi_by_folder`): USD-величина посчитана по курсу на момент пересборки марта, а не по курсу периода. Колонка `rate_kgs_per_usd` в этих витринах уже присутствует; форма явной пометки назначается задачей реализации, здесь не изобретается. **accepted.**
  - §9 Правка НЕ входит в `E1-T1-MECH` (`ADR-039 §1` подтверждается): отдельная задача `FX-POLICY-EXPENSES`, staging-first, cutover по процедуре `ADR-038`. НЕ сворачивается с cutover `E1-T1-MECH` — два независимых изменения одной живой ежедневной `WRITE_TRUNCATE`-витрины одним переключением запрещены тем же `ADR-039 §1`. ПОРЯДОК: после cutover `E1-T1-MECH`, чтобы дельта расходов и правка курса не смешались в одном diff. **accepted.**
  - §10 `Q-56` → CLOSED решением. `Q-67` → CLOSED, но НЕ как расхождение: `03 §marts` стр.126–136 содержит `latest_fx` ВНУТРИ канонического SQL витрины состояния `in_transit`; репо-широкого утверждения «`latest_fx` — канон для всего» в `03` нет (стр.374 говорит о классе конвертации, не о форме курса). При (c′) этот фрагмент остаётся верным и правке не подлежит. Формулировка `ADR-050 §6` («док и код расходятся») была НАД-ПРОЧТЕНИЕМ — SUPERSEDED-IN-PART. `03 §marts` патчится ДОБАВЛЕНИЕМ явной политики, а не исправлением `in_transit`. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-062 (append). Кросс: `ADR-002`, `ADR-005`, `ADR-009 §1`, `ADR-010`, `ADR-021 §2`, `ADR-028 §4`, `ADR-038`, `ADR-039 §1/§2/§3/§5`, `ADR-050 §1/§2/§3/§4/§5/§6`, `ADR-053`, `ADR-057 §3/§4`, `ADR-059 §7`, `ADR-061`, `Q-19`, `Q-56`, `Q-67`, `Q-70`.
  - `03_PIPELINE_SPEC.md §marts`: НОВЫЙ подраздел «§политика исходящей FX-конвертации» — вставка в начало §marts (патч 5). Канонический SQL `in_transit` стр.126–136 НЕ трогается.
  - `02_ERP_CONTRACTS.md §core.dim_fx_rates` стр.144: см. патч 4 (несёт ссылку на настоящий ADR).
  - `07_STATE`: `Q-56` → CLOSED; `Q-67` → CLOSED; новая задача-строка `FX-POLICY-EXPENSES` (READY).
  - `/reference/sql/sq_marts_expenses.sql`: правится задачей `FX-POLICY-EXPENSES`; снапшот обновляется ПОСЛЕ cutover (`ADR-021 §2` — снапшот фиксирует факт, не намерение).
  - SQL четырёх витрин (`in_transit`, `inventory_health`, `gmroi`, `abc_xyz`) и `gmroi_by_folder`, `supplier_price_history`: правок НЕТ.
  - `09_GLOSSARY`: правок нет — термины входящей/исходящей конвертации уже определены, политика не является термином.
  - `04_ROADMAP`: правок нет — задача заводится задача-строкой в `07` (`ADR-025 §2`).
- **Статус:** §1–§10 accepted.

## ADR-063 · Q-68 — второй инстанс, отказ тихий; класс опасности пересмотрен; UTC-якорь и подтверждение личности вызывающего — условие в 05 Часть II
- **Контекст:** артефакт `/reference/fx_rate_fidelity_2026-07-26.md §1` (оговорка по методу прогона) и §7 (последний пункт, вынесен на архитектора). Первый заход деградировал до анонимного вызова посреди прогона: все запросы после даты 2026-07-13, весь Step 4 и Step 5 вернули 401 / no active account, что читается как «данных нет», а не «прав нет». Ложное «архива за эти даты нет» поймано только случайным противоречием Step 1 ↔ Step 3 внутри того же лога.
- **Решение:**
  - §1 Второй инстанс `Q-68`, отдельный `Q` НЕ заводится — `ADR-051 §2` (общий признак: интерактивный/облачный шелл как ненадёжный транспорт; две реестровые строки об одном дали бы два DEFER). **accepted.**
  - §2 КЛАСС ОПАСНОСТИ `Q-68` ПЕРЕСМОТРЕН. `ADR-051 §3` ранжировал `Q-68` ниже `Q-62` на основании «последствий нет — сессия умерла целиком, ничего не исполнив». Настоящий инстанс основание опровергает: отказ был ТИХИМ, и последствие класса подтверждено фактом — ложный отрицательный результат почти стал фактом репо. `ADR-051 §3` SUPERSEDED-IN-PART (текст не редактируется, append-only); приоритет `Q-68` выравнивается с `Q-62`. **accepted.**
  - §3 Условие закрытия `Q-68` УТОЧНЯЕТСЯ: «первая сессия, прошедшая по новому правилу без инцидента» (`ADR-055 §1`) читается как «без инцидента ЭТОГО КЛАССА». Настоящая сессия условию не удовлетворяет: доставка прошла по правилу (артефакт §6 — heredoc-файл, `bash script.sh > run.log`, `date -u` первой командой во всех трёх заходах), но инцидент класса был. `Q-68` остаётся ОТКРЫТ. **accepted.**
  - §4 Защита — УСЛОВИЕ, а не рекомендация (расширение `ADR-055 §3`): `date -u` требуется первой И последней командой скрипта, вместе с переподтверждением личности вызывающего в начале и в конце. Основание выбора формы — `ADR-040 §Диагноз`: правило, не ставшее ничьим условием, не исполняется (`RQ-3` прожил шесть ревизий). **accepted (апрув владельца явно, 2026-07-26).**
  - §5 ДОМ ПРАВИЛА — `05` Часть II (стандарт доставки, где живёт `ADR-055`), НЕ Часть I. Часть I — место закона («успех инструмента ≠ факт»), Часть II — место условий исполнения. Дублирование в двух частях ОТКЛОНЕНО: у проекта задокументированная болезнь противоречащих доков (`Q-60`, `ADR-052`). Формулировка предыдущего черновика архитектора (Часть I) исправлена до коммита — черновик, `ADR-021 §1`. **accepted.**
  - §6 Продвижение в канон `_METHOD` НЕ назначается — класс `Q-40`, owner-gated, уже на решении; настоящее правило его цену не меняет. **accepted.**
- **Последствия (blast radius):**
  - `06`: настоящий ADR-063 (append). Кросс: `ADR-014 §3`, `ADR-021 §1/§2`, `ADR-040 §Диагноз`, `ADR-046 §1`, `ADR-051 §1/§2/§3`, `ADR-055 §1/§3`, `Q-40`, `Q-62`, `Q-63`, `Q-68`.
  - `05_CONVENTIONS.md` Часть II: замена п.3 блока «Доставка — условие, не рекомендация» (патч 6).
  - `07_STATE`: `Q-68` — второй инстанс, приоритет выровнен с `Q-62`, условие закрытия уточнено (патч 11).
  - `_METHOD.md`: НЕ трогается (§6).
- **Статус:** §1–§6 accepted.

## ADR-064 · Компактизация горячего контекста: 07_STATE = только открытое; 07_ARCHIVE; 06_INDEX вместо полного 06 в обязательном контексте

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия PIPELINE-AUDIT)
**Контекст:** обязательный контекст архитектора (`_METHOD`+`00`+`04`+`06`+`07`) достиг ~447 КБ (06=245 КБ, 07=126 КБ) ≈ 130–160K токенов кириллицы — бо́льшая часть окна модели уходит на бутстрап; оба файла растут без предела (append-only/накопительный). Наблюдаемые следствия: дробление сессий (E1-T1-MECH-PREP → -ADJ → -ADJ-2 за день), деградация внимания к концу сессий, полная цена перечитывания каждой сессией.
**Решение:**
1. `07_STATE.md` держит ТОЛЬКО открытое (open / DEFER с триггером промоушена / IN PROGRESS / READY / ожидает решения). Полностью закрытые Q/задачи/блокеры при закрытии переезжают в `07_ARCHIVE.md` (полный текст, дословно) — тем же коммитом, что закрывающий STATE_PATCH.
2. Вводится `06_INDEX.md` (LIVING): строка на ADR — номер · заголовок · статус · пометка superseded. Каждый append ADR в `06` сопровождается строкой в индексе тем же коммитом.
3. Обязательный контекст ролей: `06_DECISIONS_LOG.md` заменяется на `06_INDEX.md`; полный текст конкретного ADR поднимается точечно из `06` по номеру, когда вопрос сессии на него опирается. `07_ARCHIVE.md` в обязательный контекст НЕ входит, поднимается точечно по ID.
4. Канон не меняется: `06` остаётся append-only первоисточником решений; архив — не первоисточник, а разгрузка `07`.
**Последствия (blast radius, поимённо):** `07_STATE.md` — пересобран (открытое) + правило компактности в шапке; НОВЫЕ файлы `07_ARCHIVE.md`, `06_INDEX.md`; `_ARCHITECT.md` — §3 (строка «Обязательный контекст архитектора», карта repo, §9 быстрый старт); `_APPLIER.md` — §обязанность синхронизации индекса при append `NEW_DECISIONS`; `briefs/_GENERATOR.md` — карта repo (06 → 06_INDEX для контекста); `_START_HERE.md` — перечень живых доков; `05_CONVENTIONS.md` Часть I §Дисциплина изменений документации — два правила синхронизации (закрытие→архив; append ADR→строка индекса); `00_CHARTER §карта документов` — две строки (06_INDEX, 07_ARCHIVE). Точные drop-in — `PATCHES_ADR-064.md` сессии.

## ADR-065 · CODE-REPO-STANDUP промоутится в критический путь; деплой CF — только из код-репо

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия PIPELINE-AUDIT)
**Контекст:** прод-код живёт на диске Cloud Shell; репо хранит снапшоты-провенанс (`ADR-017`). Дрейф ревизий уже случался и стоил сессий доказательств (Q-44: доки `00006-piv` vs прод `00012-cik`; Q-45 — два независимых подтверждения фикса). Residual Q-3: 5 CF не версионированы. Текущий приоритет задачи («низкий, вне критпути») не соответствует наблюдаемой цене инцидентов.
**Решение:** задача `CODE-REPO-STANDUP` — в критический путь Epic-1 (после cutover `E1-T1-MECH`, до/параллельно `FX-POLICY-EXPENSES`). Целевой инвариант: деплой любой CF выполняется ТОЛЬКО из код-репо; ревизия ↔ коммит. Развилки исполнения (где живёт репо: внутри holika / отдельный; seed-from-git vs seed-from-disk; форма деплой-процедуры) — owner-gated, отдельная сессия по 1-му шагу ADR-017 §4.
**Последствия:** `07_STATE` строка `CODE-REPO-STANDUP` — пометка промоушена (внесена этой сессией); `04_ROADMAP` Epic-1 — задача входит в scope-перечень при следующем патче роадмапа; провенанс-дисциплина `/reference/code/` (ADR-017) не отменяется до standup.

## ADR-066 · Пилот Claude Code на брифе FX-POLICY-EXPENSES; applier и SHA-curl ретайрятся по итогам пилота

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия PIPELINE-AUDIT)
**Контекст:** класс инцидентов последних недель — транспортный (человек-как-канал): Q-62/Q-68 (copy-paste bash ронял/накладывал Cloud Shell), Q-60/B-12 (ручные коммиты подменяли файлы), Q-46 (негде коммитить). Защиты ADR-014/043/044/051/054/055 компенсируют канал, который устраним конструктивно: локальный клон репо + исполнение команд агентом + ревью diff владельцем.
**Решение:** пилот Claude Code — ОДНА задача, бриф `FX-POLICY-EXPENSES` (staging-first, численная приёмка 0,00 — идеальный тест), ПОСЛЕ cutover `E1-T1-MECH` (порядок ADR-062 §9 не меняется). Предусловие пилота: ADR-064 исполнен (иначе агент наследует контекстный налог). Состав: `CLAUDE.md` (законы: anti-improvisation, оракул, session-блок, owner-гейты на не-идемпотентном), субагенты `.claude/agents/` (архитектор / исполнитель с разными правами), pre-commit hook (ADR-054 первая строка=имя, ADR-041 гомоглифы, схема session-блока, синхронизация 06_INDEX). Owner-гейты на не-идемпотентных операциях СОХРАНЯЮТСЯ (подтверждение команд — нативный механизм).
**Последствия:** до итогов пилота ничего не ретайрится — `_APPLIER`, SHA-curl-доступ и стандарты доставки действуют как есть. По success-критерию пилота (задача прошла цикл бриф→исполнение→приёмка→session-блок→коммит без ручного транспорта, целостность доков не нарушена) — отдельный ADR ретайрит applier-роль и SHA-curl для сессий Claude Code; чат-сессии (консультации архитектора вне Claude Code) сохраняют текущий протокол. Success-критерий и состав `CLAUDE.md` — следующая архитектор-сессия.

## ADR-067 · Финишная черта проекта = числовой паритет с интерфейсом МойСклад; иерархия источников эталона; ADR-002-формулировка «только entity-API» — superseded-in-part

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия PIPELINE-AUDIT; §6 — owner-gated развилки)
**Контекст:** финишная черта проекта отсутствовала (Q-71, гэп `00_CHARTER §критерии успеха`) — причина неконтролируемого расширения scope. Владелец зафиксировал критерий: клиент заходит в МойСклад, видит цифры в интерфейсе; расхождение BI с ними неприемлемо. Транспорт эталона: API МойСклада; если API не раскрывает картину — выгрузка отчётов либо ручная копия того, что рисует страница интерфейса.
**Решение:**
1. **Финишная черта:** числовой паритет между сверяемыми поверхностями BI и тем, что рисует интерфейс МойСклад для клиента. Расхождение = дефект либо явное задокументированное правило — третьего нет (расширение ADR-002).
2. **Иерархия источников эталона (по убыванию предпочтения):**
   (i) **report-API МойСклада** — движок отчётов, то же, что рисует UI; точные endpoint'ы и их соответствие страницам UI — discovery (задача PARITY-REGISTRY);
   (ii) **агрегация entity-API** — там, где report-API недоступен или доказанно совпадает;
   (iii) **ручная копия страницы UI** → `/reference` с датой, скоупом и значениями построчно.
   Формулировка ADR-002/PR-34 «источник истины — ТОЛЬКО прямой запрос к entity-API» → **superseded-in-part**: API остаётся каноническим транспортом, но верхний эталон — то, что видит клиент. Расхождение агрегации entity-API против UI/report-API — факт о семантике МойСклада, закрывается правилом соответствия, а НЕ «починкой» пайплайна под entity-агрегат.
3. **Эталон — датированный снимок.** МойСклад пересчитывает историю при правках документов задним числом; расхождение, возникшее из-за правок ПОСЛЕ снятия эталона, — не дефект пайплайна, закрывается пересъёмом эталона за период (прецедент формы — реконфирмация Q-27, 2026-07-26).
4. **DERIVED-слой вне паритета по определению.** Величины, которых в МойСклад нет (USD-конвертация `total_sum_usd`, производные метрики GMROI/ABC-XYZ и т.п.), — DERIVED по разметке ADR-005; паритет проверяется на KGS-величинах ДО производных преобразований. Политика DERIVED-слоя — ADR-062, вне этого ADR.
5. **Операционализация:** (a) **реестр паритета** (задача PARITY-REGISTRY) — пары «страница/отчёт МойСклад ↔ витрина/страница LS», сид: `/reference/client_concerns_2026-07.md` + `01 §потребители` (карта PR-39); каждая пара несёт статус сходимости; (b) **Epic-1 «готов»**, когда каждая пара реестра = 0,00 либо расхождение закрыто правилом/ADR; (c) автоматизация посева эталона (задача ORACLE-AUTOMATION) промоутится из рекомендации аудита в задачу: паритет — не разовый акт, а поддерживаемое состояние, ручной посев помесячно не масштабируется.
6. **Owner-gated развилки (НЕ решены этим ADR):** **Q-73** — уровень паритета: (a) «пиксель» (наши страницы рисуют ровно то же) / (b) «сверяемые величины + опубликованный мост соответствий» / (c) гибрид — режим-зеркало на чувствительных страницах; выбор (a) влечёт ревизию ADR-006/ADR-013 (карта 27 категорий, «Вывод прибыли» вне opex — сознательные отступления от вида МойСклада, одобренные клиентом). **Q-74** — глубина истории паритета (какие периоды обязаны сходиться).
**Последствия (blast radius):** `00_CHARTER §критерии успеха` — CONTEXT GAP-строка заменяется критерием (патч 7a); `00_CHARTER §главный принцип` — абзац иерархии источников (патч 7b); `04_ROADMAP` Epic-1 — строка финиша (патч 7c); `07_STATE` — Q-71 закрыт → архив; заведены Q-73, Q-74, PARITY-REGISTRY, ORACLE-AUTOMATION; `06_INDEX.md` — строка ADR-067.

## ADR-068 · Q-73 закрыт: уровень паритета = (b) «сверяемые величины + опубликованный мост соответствий»

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия PIPELINE-AUDIT)
**Контекст:** развилка ADR-067 §6. Владелец: цифры обязаны сходиться с МойСклад; клиентские пожелания ПОВЕРХ (переклассификации, уже принятые фиксами проекта) — легитимны; новых пожеланий нет.
**Решение:**
1. Уровень паритета — **(b)**: итоги и строки сверяемых поверхностей совпадают с МойСклад через **опубликованный мост соответствий** — карту правил «статьи/строки МойСклада → категории BI». Сумма при перестановке инвариантна; исключения перечислены поимённо.
2. **Легитимные отступления моста** = только уже принятые клиент-одобренные решения: ADR-006 (карта 27 категорий), ADR-013 («Вывод прибыли» — капитал вне opex; «Налоги» — начислением), исключения ADR-002-периметра («Перемещение исходящий», системные статьи). Ревизия ADR-006/ADR-013 НЕ требуется. Любое НОВОЕ пожелание клиента поверх = новое правило моста через новый ADR — не тихая правка.
3. **Форма моста:** колонка в реестре паритета (`/reference/parity_registry.md`, задача PARITY-REGISTRY): для каждой пары «поверхность МойСклад ↔ витрина BI» — правило соответствия, достаточное, чтобы клиент воспроизвёл нашу цифру из своей страницы МойСклада вручную.
4. Вариант (c) (режим-зеркало) не отвергается навсегда: точечный триггер — повторная претензия клиента к конкретной странице после объяснения моста; включение — отдельным ADR.
**Последствия:** `00_CHARTER §критерии успеха` — финальная формулировка критерия (патч 7a-v2); PARITY-REGISTRY — разгейчен, форма артефакта дополнена колонкой «мост»; Q-73 → закрыт, в архив.

## ADR-069 · Q-74 закрыт: глубина паритета = (c) с якорем 2026-05-01; расширение назад — только явным ADR по замеру

**Дата:** 2026-07-27 · **Статус:** accepted (владелец делегировал выбор даты архитектору: «работать только с теми данными, которые реально есть»)
**Контекст:** развилка ADR-067 §6. Принцип владельца: не имитировать гарантию там, где данных/проверки нет; проект стартовал не со старта бизнеса клиента — ранняя стартовая точка вычислений легитимна.
**Решение:**
1. Глубина паритета — **(c)**: явная граница по дате; ранняя история помечается в Looker Studio «вне паритета (до 2026-05)».
2. **Якорь = 2026-05-01.** Обоснование фактами репо, не мнением: (i) май-2026 — единственный период с эталоном (`/reference/pnl_2026-05.md`, ADR-005) и доказанной сходимостью 0,00 тремя источниками (ADR-033); (ii) масштаб валютного бага в истории `core.fact_payments` за месяцы до фикса НЕ замерялся (Q-30 DEFER) — паритет ранних месяцев недоказуем без замера; (iii) покрытие `core.dim_fx_rates` до эпохи Bakai источником не подтверждено (ADR-049/ADR-059).
3. **Расширение назад — разрешено, но только явно:** посев эталона за период (ORACLE-AUTOMATION) + сверка + ADR «период <YYYY-MM> включён в паритет». Тихое расширение запрещено. Q-30 при таком расширении промоутится из DEFER автоматически (замер месяца = часть включения).
4. Плашка «вне паритета» — обязательный элемент затронутых страниц LS (форма — задача PARITY-REGISTRY, поверхность-специфично).
**Последствия:** `00_CHARTER §критерии успеха` — дата в критерии (патч 7a-v2); Q-74 → закрыт, в архив; PARITY-REGISTRY — колонка «покрытие с даты»; триггер промоушена Q-30 дополнен (расширение паритета назад).

## ADR-070 · Скаффолд пилота Claude Code — состав файлов, права, pre-commit hook, success-критерий; среда пилота = локальная машина (исполнение делегата ADR-066)

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия CC-PILOT-SCAFFOLD)
**Контекст:** ADR-066 accepted; success-критерий и состав CLAUDE.md делегированы следующей архитектор-сессии. Предусловие (ADR-064 исполнен) подтверждено фактом @ eecded58. Развилка среды исполнения поднята полным блоком (варианты/векторы) и решена владельцем в той же сессии.
**Решение:**
§1 Состав скаффолда (5 новых файлов): CLAUDE.md (законы _METHOD §6 + 05 Части I–II, свод компактный — контекстный налог ADR-064 не воспроизводить); .claude/settings.json (права: read-only — allow; не-идемпотентное и push — ask; разрушительный git — deny); .claude/agents/architect.md (без Bash/Edit/Write); .claude/agents/executor.md (полные инструменты, мандат = один бриф); tools/hooks/pre-commit (установка симлинком — шаг старта сессии в CLAUDE.md). accepted (владелец, 2026-07-27).
§2 Success-критерий пилота (все пункты одновременно): (a) бриф FX-POLICY-EXPENSES прошёл полный цикл контекст→staging→приёмка→session-блок→коммит внутри Claude Code; (b) численная приёмка задачи выполнена (регрессия мая = 0,00 против /reference/pnl_2026-05.md; total_sum_usd мая не изменился; инвариантность к дате пересборки двумя прогонами — по строке FX-POLICY-EXPENSES в 07); (c) ноль инцидентов транспортного класса Q-62/Q-68 (ни одного copy-paste исполняемого блока в интерактивный шелл); (d) каждое не-идемпотентное действие прошло явное ask-подтверждение владельца, ни одного авто-исполнения; (e) целостность доков пост-фактум: append-only 06 не нарушен (git diff старых ADR пуст), 06_INDEX синхронизирован, 07_STATE компактен (ADR-064), ADR-054 по всем правленым докам; (f) роль владельца свелась к ревью diff и подтверждениям — ручного переноса файлов не было. Любой провал (a)–(e) = пилот FAIL → откат к текущему протоколу, разбор отдельной сессией; (f) — качественный, оценивается владельцем. accepted.
§3 Ретайр applier/SHA-curl настоящим ADR НЕ производится (подтверждение ADR-066 §Последствия): отдельный ADR по итогам пилота. Чат-сессии сохраняют текущий протокол. accepted.
§4 Ограничение хука зафиксировано честно: полная валидация схемы session-блока хуком неисполнима (блок рождается в чате); хук держит косвенные инварианты (индекс/архив/updated_at/ADR-054/ADR-041), полнота схемы — ревью владельца. Пункт «схема session-блока» из ADR-066 исполняется в этой редуцированной форме сознательно, не молча. accepted.
§5 Среда исполнения пилота — ЛОКАЛЬНАЯ МАШИНА ВЛАДЕЛЬЦА (вариант А; выбор владельца, 2026-07-27). Мотив: пилот затевался ради вывода исполнения из ненадёжного интерактивного канала Cloud Shell (класс Q-62/Q-68); вариант Cloud Shell оставлял бы исполнение в этом канале. Разовая настройка (gcloud SDK + auth + клон + Claude Code + проверка /permissions) — предусловие старта пилота, фиксируется в задача-строке CC-PILOT-SCAFFOLD; Cloud Shell остаётся fallback-средой только отдельным решением. accepted (владелец, 2026-07-27).
**Последствия (blast radius):** НОВЫЕ файлы CLAUDE.md, .claude/settings.json, .claude/agents/architect.md, .claude/agents/executor.md, tools/hooks/pre-commit; 06_INDEX.md — строка ADR-070 (accepted); 07_STATE — задача-строка CC-PILOT-SCAFFOLD; существующие доки (00–09, _ARCHITECT, _APPLIER, _GENERATOR, _START_HERE) — БЕЗ правок до итогов пилота.

## ADR-071 · Расширение мандата сессий Claude Code на FX-MAY-WINDOW как тренировочный прогон; Q-75 отложен до замера

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия CC-PILOT-BOOTSTRAP)
**Контекст:** success-критерий `ADR-070 §2` пунктами (c) и (f) меряет не инструмент, а связку «владелец + инструмент»; владелец с Claude Code ранее не работал. Первый заход сразу на `FX-POLICY-EXPENSES` (живая ежедневная `WRITE_TRUNCATE`-витрина, приёмка 0,00) смешал бы кривую обучения с результатом пилота. Старт пилота при этом гейтится дважды: решение владельца по cutover `E1-T1-MECH` и отсутствие `briefs/FX-POLICY-EXPENSES.md` в репо (факт @ `2ce6ea4`).
**Решение:**
§1 Мандат сессий Claude Code расширяется на ОДНУ дополнительную задачу — `FX-MAY-WINDOW` — со статусом **тренировочный прогон**. accepted (владелец, 2026-07-27).
§2 Тренировочный прогон **не является пилотом** и в success-критерий `ADR-070 §2` не входит ни одним пунктом: провал по неопытности владельца не засчитывается инструменту, успех не засчитывается пилоту. **Асимметрия сознательна:** инцидент транспортного класса (`Q-62`/`Q-68`), случившийся на тренировочном прогоне, ФИКСИРУЕТСЯ в `07_STATE` — воспроизведение этого класса уже внутри Claude Code есть факт против основания `ADR-066` и теряться из-за формального статуса «тренировка» не должно.
§3 Основание допустимости — свойства самой задачи, а не удобство: `ADR-061 §2/§3` — read-only, живых витрин не касается, ничего не гейтит, go владельца получен 2026-07-26. Расширение мандата на задачу с иными свойствами настоящим ADR НЕ разрешается.
§4 Порядок конвейера НЕ меняется: cutover `E1-T1-MECH` → `FX-POLICY-EXPENSES` (пилот) → `PARITY-REGISTRY` (`ADR-062 §9`, `ADR-066`, `ADR-067 §5`). Тренировочный прогон идёт параллельно и ни одну из строк очереди не сдвигает.
§5 **Предусловие прогона — брифа нет** (`briefs/FX-MAY-WINDOW.md` отсутствует @ `2ce6ea4`). Генерация — по `_GENERATOR.md` в форме, назначенной строкой `FX-MAY-WINDOW` в `07_STATE` (механический бриф, дешёвая модель, пять обязательных оговорок). **Санкционированное отклонение:** при запуске генератора внутри Claude Code правило `_GENERATOR §ПРАВИЛО ДОСТУПА` (curl по SHA) заменяется чтением локального клона с фиксацией `git rev-parse HEAD` в брифе. Основание: правило написано против устаревшего кэша web-fetch; локальный клон этому риску не подвержен по построению, а curl-транспорт вернул бы ручной перенос файла — класс `Q-60`/`B-12`, против которого затеян `ADR-066`. Отклонение локально для сессий Claude Code; чат-сессии сохраняют curl-протокол (`ADR-066 §Последствия`, `ADR-070 §3`).
§6 **`Q-75` — решение отложено до замера** (выбор владельца). Форма фикса НЕ назначается заранее (`_METHOD §6`). Основание: утверждение о порядке проверки правил взято из документации инструмента, а не из наблюдения на этой установке ⇒ фактом не является (`ADR-021 §2`, `05` Часть I ★). Правка состава прав меняет `ADR-070 §1` и требует отдельного ADR — тихо не правится.
§7 Исполняемость `tools/hooks/pre-commit` — исполнение `ADR-070 §1`, нового решения не требует (прецедент `Q-53`/`Q-46`, «CLOSED применением»). Зафиксировано здесь, чтобы факт «хук лежал неисполняемым» не потерялся: неисполняемый хук git пропускает МОЛЧА ⇒ отказ тихий, класс `Q-68`/`ADR-063 §2`. Проверка исполняемости обязана быть по факту срабатывания (умышленно нарушающий коммит обязан упасть), не по режиму файла.
**Последствия (blast radius):** `CLAUDE.md §Scope пилота` — абзац о тренировочном прогоне (патч 2); `07_STATE` — строка `CC-PILOT-SCAFFOLD` (патч 3), строка `FX-MAY-WINDOW` (патч 4), новая строка `Q-75` (патч 5), `updated_at` (патч 6); `06_INDEX.md` — строка `ADR-071` (патч 7). Доки `00`–`09`, `_ARCHITECT`, `_APPLIER`, `_GENERATOR`, `_START_HERE` — БЕЗ правок (отклонение §5 живёт в ADR, эталонные файлы ролей не трогаются — прецедент `Q-40`).

## ADR-072 · Фикс проверки №2 pre-commit; фикстурный самотест хука как обязательный артефакт; верификация пер-проверка, не пер-хук

**Дата:** 2026-07-27 · **Статус:** accepted (владелец, сессия CC-HOOK-REVIEW)
**Контекст:** владелец заявил, что проверка №2 хука (`ADR-041`, кириллица в ID) не срабатывает на macOS из-за отсутствия `-P` в BSD grep. Измерение **опровергло масштаб заявки, подтвердив симптом**: `grep -PqE` возвращает `rc=2` и `conflicting matchers specified` на GNU grep 3.11 тоже — `-P` и `-E` суть два конфликтующих движка. Проверка нефункциональна с момента написания (`ADR-070`) на любой платформе. Отказ **не молчаливый**: stderr печатал ошибку при каждом прогоне, но не читался (класс `ADR-063 §2`).
**Решение:**
§1 `grep -PqE` → `grep -qE`, паттерн и локаль без изменений. Основание: паттерн не содержит PCRE-специфичных конструкций; измерено (GNU grep 3.11, вход проверен дампом байтов) — подделка ловится, легитимная строка чиста в четырёх режимах: `C.utf8`, `C`, несуществующая локаль, локаль не задана. **Закрепление локали ОТКЛОНЕНО как импровизация** (внесено предыдущей редакцией этого ADR и снято до коммита): замера, что оно что-то решает, нет, а `C.UTF-8` на macOS отсутствует ⇒ это вписывало в хук то самое допущение об окружении, за которое отклонён вариант «требовать GNU grep через brew». Вариант «требовать GNU grep» ОТКЛОНЁН по тому же основанию. accepted.
§2 **НОВЫЙ артефакт скаффолда: `tools/hooks/selftest.sh`** — фикстурный самотест. Состав скаффолда `ADR-070 §1` расширяется с 5 файлов до 6. **Провенанс прогонов (сессия CC-HOOK-REVIEW):** до-фиксовый хук → `ПРОВАЛ 2-bad`, `провалено 1`; после фикса → `провалено 0`; хук-заглушка `exit 0` → `провалено 4`; недоступный `git` → `провалено 8` с диагностикой «упал НЕ на хуке». **Честная оговорка:** заявка «артефакт прогнан» в первой редакции §2 была сделана ДО прогона (нарушение `ADR-021 §2` в ADR, вводящем это же правило); прогоны выполнены после указания владельца, результат совпал с заявкой, что заявку не оправдывает. accepted.
§3 **Правило класса:** верификация хука — **пер-проверка**, не пер-хук. Каждая проверка обязана упасть на заведомо плохом входе и пропустить заведомо хороший. Основание — признанный дефект `ADR-071 §7`: спроектированный им живой тест ронял коммит на правиле №5 и был бы зелёным при полностью сломанных правилах №1–4, то есть доказывал «хук зовётся», а выдавалось это за «хук работает». Класс — `Q-41` (метка `PASS` без артефакта) и `ADR-044` (греп-вердикт без напечатанных строк). `ADR-071 §7` — **superseded-in-part** в части формы проверки; требование «проверять по факту срабатывания, не по режиму файла» остаётся в силе. accepted.
§4 **Предел самотеста зафиксирован честно** (форма `ADR-070 §4`): проверка №4 (удаление строки из `07_STATE` ⇒ `07_ARCHIVE` тем же коммитом) фикстурой не покрывается — требует истории коммитов, а не одного снимка; остаётся на ревью владельца, как и полнота схемы session-блока. Непокрытие названо, а не умолчано. accepted.
§5 **Отступление от `ADR-071 §Последствия`, названное вслух:** первая строка `_GENERATOR.md` приводится к `ADR-054` (`# FILE: _GENERATOR.md`). `ADR-071` писал «`_GENERATOR` — без правок», имея в виду правила доступа; заголовок — иной предмет. Основание правки, а не ослабления проверки: из 18 подпадающих документов репо 17 держат форму, отступает один ⇒ дефект в файле. Вариант «ослабить проверку №1» ОТКЛОНЁН как подгонка правила под данные. accepted (владелец).
§6 **Бит исполняемости остаётся неисполненным:** `tools/hooks/pre-commit` @ `1680524` — режим `644`, файл байт-в-байт как @ `2ce6ea4` ⇒ `git update-index --chmod=+x` из `ADR-071 §7` не закоммичен. Входит в этот коммит. Пока бит не в репо, любой новый клон получает тихо отключённый хук — включая будущую среду пилота. accepted.
§7 **Вне scope настоящего ADR:** охват набора `[МСЕАРОТНКВХ]` (какие именно гомоглифы ловятся) и фильтр проверки №1, не покрывающий `CLAUDE.md`, `MIGRATION_MAP.md`, `PROJECT_REFERENCE_v6.md`, `RUNBOOK_v8.md`, `TZ-MIGRATE-TO-DOCS-METHOD.md`. Оба — предмет `ADR-054`/`ADR-041`, расширение без замера есть импровизация (`_METHOD §6`). Дефекта на текущем содержимом нет: первая строка всех пяти файлов фактически корректна. Заведено как `Q-76`. Дополнительно вне scope: поведение паттерна под BSD grep не замерено — все измерения сессии выполнены на GNU grep 3.11. Замер владельца на macOS входит в `Q-76`.
§8 **Самотест верифицирован контр-прогонами, не только зелёным результатом.** Зелёный тест недостоверен, пока не показано, что он умеет краснеть. Первая редакция §2 содержала дефект: критерий плохого кейса «коммит не прошёл» удовлетворялся ЛЮБЫМ сбоем (замерено на недоступном `git` — все плохие кейсы отчитались `ok`, хук не участвовал). Критерий усилен: плохой кейс требует падения ИМЕННО хука — наличия строки `PRE-COMMIT FAIL` в выводе, иначе печатается реальный вывод сбоя. Полностью зелёный прогон подделать посторонним сбоем нельзя и в первой редакции (хорошие кейсы краснеют), но построчная диагностика была недостоверна — класс `ADR-044` (вердикт без напечатанного доказательства). accepted.
§9 **Побочный инцидент измерения, зафиксирован как урок, а не как находка.** В ходе §1 получен результат «подделка не ловится ни в одной локали», едва не оформленный как находка о локалях. Причина — сломанный стенд: `printf` в `dash`/`sh` не понимает `\xHH` и записывает экранирование буквально, то есть кириллицы во входе не было вовсе, а `grep` честно отвечал «не найдено». Отрицательный результат сломанного замера почти стал фактом (`05` Часть I ★, `ADR-021 §2`). **Два следствия в артефакте:** (i) фикстура `2-bad` задаёт кириллицу литеральным символом, не экранированием, и проверяет саму себя сразу после записи; (ii) вход любого замера на байтовом уровне подтверждается дампом (`od -An -tx1`) ДО чтения вердикта. accepted.
**Последствия (blast radius):** `tools/hooks/pre-commit` — строки 17–18 (патч 1); НОВЫЙ файл `tools/hooks/selftest.sh` (патч 2); `_GENERATOR.md` — строка 1 (патч 3); `06_INDEX.md` — строка `ADR-072`; `CLAUDE.md §Старт каждой сессии` — п.5 дополняется прогоном самотеста; `07_STATE` — строка `CC-PILOT-SCAFFOLD`, новая строка `Q-76`, `updated_at`; `ADR-071 §7` — помечен `superseded-in-part` ссылкой отсюда (текст `ADR-071` НЕ редактируется, `06` append-only).

## ADR-073 · Мандат Claude Code на `SQ-CADENCE-MEASURE` не расширяется (исполнено вне инструмента); cutover вне брифа — процессный факт; `Q-64` закрыт фактом; форма замера `ADR-045 §5` superseded-in-part; тренировочный прогон остаётся предусловием пилота

**Дата:** 2026-07-28 · **Статус:** accepted (владелец)
**Контекст:** между постановкой сессии CC-MANDATE-SQ-CADENCE и её завершением cutover `E1-T1-MECH` был исполнен, а замер каденции выполнен владельцем вручную в терминале — в форме, которую подготовленная редакция ADR отклоняла как основную. Расширение мандата стало беспредметным. Параллельная сессия внесла факты каденции в `11 §SQ`; артефакт и снимок SQL закоммичены владельцем к `9465294`. Полный листинг заданий за 13–28 июля (`INFORMATION_SCHEMA.JOBS_BY_PROJECT`, окно `2026-07-13T01:34:01Z … 2026-07-28T03:34:03Z`) закоммичен как `/reference/bq_jobs_2026-07-28.md`.
**Решение:**
§1 **Расширение мандата НЕ производится.** `SQ-CADENCE-MEASURE` исполнена вне Claude Code 2026-07-27; границы мандата `ADR-070`/`ADR-071` не меняются, `CLAUDE.md §Scope пилота` не правится. `briefs/SQ-CADENCE-MEASURE.md` остаётся историческим артефактом и исполнению не подлежит. accepted.
§2 **Процессный факт, задним числом не переписывается:** cutover исполнен 2026-07-27T15:52Z ВНЕ брифа `E1-T1-MECH-CUTOVER` (бриф не генерировался), при незакрытом на тот момент предусловии `ADR-045 §3`. Оценка постфактум: ущерба не выявлено — замер того же дня подтвердил каденцию, на которую опирается `ADR-038`, а подмена в 15:52Z при суточном слоте 11:10 UTC дала ~19,3 ч до планового прогона, что соответствует требованию `ADR-038` максимизировать зазор. Оценка НЕ санкция на повторение: порядок «бриф → предусловие → cutover» остаётся обязательным. accepted.
§3 **Предусловие `ADR-045 §3` исполнено и обеспечено провенансом.** Артефакт `reference/sq_cadence_2026-07-27.md` в репо @ `9465294` (13 370 байт): якоря `date -u` начала и конца обоих прогонов, совпадение личности вызывающего, версия инструмента `bq` 2.1.35, сырые `scheduleOptionsV2`/`nextRunTime`, обе целевые конфигурации, поимённые задания. Статус «заявлено, не приложено» снят. accepted.
§4 **`Q-64` ЗАКРЫТ фактом; строка уходит в `07_ARCHIVE` (`ADR-064`).** Часть `sales_overview`: двухчасовая каденция есть КОНФИГ (`sq_marts_sales_overview` `69ff34b4-…`, `every 2 hours`), не ручные прогоны. Часть `marts.expenses` 14:41: **наблюдение НЕ ВОСПРОИЗВОДИТСЯ.** Провенанс закрытия — закоммиченный `/reference/bq_jobs_2026-07-28.md §3/§4/§5`: замер `INFORMATION_SCHEMA.JOBS_BY_PROJECT`, 319 заданий с целью в `marts.*` за окно `2026-07-13T01:34:01Z … 2026-07-28T03:34:03Z`, из них по `marts.expenses` — ровно 16: пятнадцать суточных прогонов в слоте `11:10:00–11:10:09 UTC` без пропусков (13–27.07) плюс ручной прогон cutover `2026-07-27T15:53:48Z`. Окно `2026-07-26T14:30–15:00Z` заданий в 14:41 не содержит; ближайшие — анонимные `SELECT` в `14:46:58`/`14:47:01`, чтение, не запись (`§5` артефакта). **Встречная проверка:** наблюдение той же сессии «`sales_overview` 15:34» подтверждено записью в листинге ⇒ часы сессии шли по UTC и читались верно, дефект локален и относится только к строке про 14:41. **Квалификация:** ошибка чтения листинга сессией `E1-T1-MECH-PREP-ADJ` (`bq ls -j` без флага всех пользователей), не событие в проде. Отклонение `ADR-045 §1` («списать на ручные прогоны») снимается не интерпретацией, а фактурой. **Граница достоверности снята наблюдением, не выводом (`/reference/bq_jobs_2026-07-28.md §6`):** запрос без фильтра по датасету показал `etl-sa@msklad-bi-prod.iam.gserviceaccount.com` (760 заданий) наравне с аккаунтом владельца — видимость чужих принципалов подтверждена прямо, довод «прогон исполняется под владельцем конфигурации» (`ADR-045 §1`) более не требуется как единственное основание. accepted.
§5 **Второй путь пересборки `ADR-038` за пятнадцать суток не проявился ни разу.** `cf-finance.trigger_marts()` (~21:00 UTC) в листинге отсутствует: при живом пути ожидалось бы ≈30 заданий по `marts.expenses` за окно вместо факта — 16 (`/reference/bq_jobs_2026-07-28.md §4`). Положительный контроль инструмента подтверждён тем же артефактом: ручной прогон cutover (`StartManualTransferRunsRequest`) виден в той же выдаче и неотличим по форме от суточного — значит инструмент способен показать пересборку не по расписанию, и её отсутствие не объясняется слепотой замера. Каденция витрины фиксируется как **одноканальная**. `ADR-038` не supersede — сам путь может существовать в коде, — но опора процедуры переключения на него снимается: расчёт окна ведётся только от расписания `sq_marts_expenses`. accepted.
§6 **Форма замера `ADR-045 §5` — superseded-in-part.** Флаг `--all_users` на установленной версии `bq` 2.1.35 не существует: CLI отвечает `FATAL Flags parsing error: Unknown command line flag 'all_users'`, а `bq help ls` печатает `--[no]all_jobs: DEPRECATED. Use --all instead`. Итоговый замер `§4` исполнен инструментом `INFORMATION_SCHEMA.JOBS_BY_PROJECT`, а не флагом `bq ls -j` — основание смены инструмента и фиксация версий `bq` (2.1.33/2.1.34/2.1.35, расходятся по проекту) задокументированы в `/reference/bq_jobs_2026-07-28.md §2`. Канонической целью замера назначается видимость прогонов DTS и заданий иных принципалов, а не конкретный флаг конкретной версии `bq`; флаг/инструмент устанавливается на месте с фиксацией в артефакте. accepted.
§7 **Тренировочный прогон Claude Code остаётся ОБЯЗАТЕЛЬНЫМ перед пилотом.** `SQ-CADENCE-MEASURE` ушла вне инструмента ⇒ ни одной сессии исполнения в Claude Code не состоялось. Если пилотом станет первая же сессия, `ADR-070 §2(c)/(f)` будут мерить кривую обучения владельца, а не инструмент — смешение, признанное недопустимым в `ADR-071 §2`. Тренировочная задача — `FX-MAY-WINDOW` (бриф в репо, read-only, живых витрин не касается, ничего не гейтит: `ADR-071 §1/§3`). Порядок: `FX-MAY-WINDOW` → генерация брифа `FX-POLICY-EXPENSES` → пилот. accepted.
§8 **Хвост верификации cutover сокращён до ОДНОГО пункта** (строка `E1-T1-CUTOVER-VERIFY`, форма `ADR-048 §2`). Снято фактом: (i) подмена подтверждена ручным прогоном `2026-07-27T15:53:48Z` — прод пересобран новым SQL через минуту после правки конфигурации, то есть заявленная приёмка «прод==staging по 28 месяцам» имела под собой пересобранный прод; (ii) снимок в репо приведён в соответствие — `reference/sql/sq_marts_expenses.sql` @ `9465294` = 3114 байт (текст запроса, без лишнего завершающего перевода строки), оба новых источника присутствуют, доcutover-версия сохранена как `reference/sql/sq_marts_expenses_pre_e1t1_2026-07-27.sql` (1359 байт). **Остаётся один пункт:** первый АВТОМАТИЧЕСКИЙ прогон по расписанию `2026-07-28 11:10 UTC` (`code: 0`, витрина пересобрана) + регрессия мая-2026 на проде = 10 232 903,20 против `reference/pnl_2026-05.md`. Смысл остатка: ручной прогон доказал корректность запроса, но не доказал, что расписание отрабатывает само после правки конфигурации. accepted.
§9 **Дополнительный транспортный инцидент 2026-07-28, зафиксирован как урок при замере `Q-64`.** При ручном повторении двух запросов россыпью в Cloud Shell (вне скрипта) — `ERROR: (bq) You do not currently have an active account selected` дважды подряд; скриптовый прогон той же сессии с якорями `date -u` по краям отработал целиком и остался достоверным. Ущерба нет: данные уже были получены скриптом. Отрицательный и положительный пример правила `ADR-055 §1` получены в одной сессии — фактура в пользу того, что правило работает. Провенанс: `/reference/bq_jobs_2026-07-28.md §7`. Отдельный `Q` не заводится (тот же класс, что `Q-68`); строка `Q-68` в `07_STATE` дополняется этим фактом тем же коммитом. accepted.
§10 **Вне scope:** политика `FX-POLICY-EXPENSES` (`ADR-062`) не пересматривается; права `.claude/settings.json` (`ADR-070 §1`) не меняются; `Q-62`/`Q-68` не закрываются (см. §9 — Q-68 получает дополнение фактом, не закрытие); `Q-33` (глубина истории прогонов) не переоткрывается. accepted.
**Последствия (blast radius):** `06_INDEX.md` — строка `ADR-073` + исправление строки `ADR-072`; `11_INFRA_FACTS.md §SQ` — три правки (провенанс, снимок, одноканальность); `07_STATE.md` — удаление строки `Q-64`, новая строка `E1-T1-CUTOVER-VERIFY`, дополнение `FX-MAY-WINDOW`, дополнение `Q-68`, «Текущий фокус», header; `07_ARCHIVE.md` — две строки (`Q-64`, `SQ-CADENCE-MEASURE`). `CLAUDE.md`, `05_CONVENTIONS`, `_METHOD`, `04_ROADMAP`, брифы, `reference/*` — БЕЗ правок. `ADR-045 §5` — superseded-in-part (§6), `§3`/`§4`/`§6` в силе. `ADR-071` — в силе целиком. `ADR-038` — не supersede, опора на второй путь снята (§5).

## ADR-074 · Четыре дефекта pre-commit: вердикт из статуса пайплайна под pipefail, класс гомоглифов зависит от локали, тихая смерть на файле больше буфера пайпа, ложный позитив проверки №4 на переформулировке

**Дата:** 2026-07-28 · **Статус:** §1-§9 accepted (§1-§5, §7, §8, §9 архитектор, сессия CC-HOOK-DEFECT-2; §6 апрув владельца в той же сессии, вариант (a))
**Контекст:** исполнитель сессии FX-MAY-WINDOW доложил, что один и тот же staged-дифф (`07_STATE.md` плюс `07_ARCHIVE.md`) даёт разные исходы хука: тихий незакоммит, `exit 1` без вывода, `FAIL` про кириллицу, `exit 141` трижды. Обход — коммит `b242c8a` через `--no-verify` по прямому апруву владельца. Гипотеза исполнителя (SIGPIPE от хвостового `grep -q` под `pipefail`) подтверждена частично и с обратным следствием; фактических дефектов четыре, не один. Все четыре существуют с момента `ADR-070`.
**Решение:**
§1 **Вердикт механической проверки не берётся из статуса пайплайна под `pipefail`.** Замерено: проверка №1 (`git show ":$f" | head -1`, присваивание верхнего уровня) на файле 90 909 байт даёт `rc=141` и пустой вывод 8 прогонов из 8, на файле 4 091 байт проходит 4 из 4; порог — буфер пайпа, `07_STATE.md` (68 027 б) и `07_ARCHIVE.md` (80 599 б) оба выше. Проверка №2 на диффе 114 107 байт с настоящей кириллической С (U+0421) внутри идентификатора вида `ADR-041` печатает «инварианты пройдены» и `rc=0` 5 прогонов из 5, тот же гомоглиф в диффе 1 627 байт ловится 3 из 3: при совпадении `grep -qE` закрывает пайп, средняя стадия получает SIGPIPE, `pipefail` поднимает 141, `if` читает это как ложь. Проверки №4 и №5 несут тот же дефект: 40 идентичных прогонов на одном индексе дали 37 FAIL №5, 2 FAIL №4 и 1 `rc=0` при реальном нарушении №4. Фикс: пайп с ранним выходом убран из проверки №1, хвостовой `grep -q` заменён на подсчёт в проверках №2/№4/№5. accepted.
§2 **Класс гомоглифов переводится с bracket-выражения на альтернацию целых символов.** Под байтовой семантикой (локаль `C`, несуществующая локаль, локаль не задана) `[МСЕАРОТНКВХ]` распадается в множество байтов, и ведущий байт `0xD0` совпадает с любой кириллицей диапазона. Замерено по реальному содержимому репо @ `40518db`: в `LC_ALL=C` ложные совпадения в `07_STATE.md` (1: `DQ-порог`, `DQ-конфигурации`), `07_ARCHIVE.md` (1: `BQ-выгрузка`), `06_DECISIONS_LOG.md` (5), `04_ROADMAP.md` (4), `00_CHARTER.md` (1), `_ARCHITECT.md` (1); в `C.UTF-8` ноль. Это прямо нарушает `05` Часть III «Идентификаторы, только латиница» (`ADR-041 §6`), где scoped-охват объявлен намеренным именно потому, что композиты вида `GAP-реестр`/`не-KGS` легитимны. Форма `(М|С|Е|А|Р|О|Т|Н|К|В|Х)` замерена: 3 подделки из 3 ловятся, ложных 0 во всех трёх локальных режимах. Дополнительно проверка №2 печатает совпавшие строки (`ADR-044`), а не только вердикт. accepted.
§3 **`ADR-072 §1` — `superseded-in-part`, только по факту, не по решению.** Утверждение «легитимная строка чиста в четырёх режимах: `C.utf8`, `C`, несуществующая локаль, локаль не задана» неверно для трёх байтовых режимов на реальном содержимом репо; замер `ADR-072` шёл по синтетической строке без композита «латинский префикс, дефис, русское слово». **Решение** `ADR-072 §1` отклонить закрепление локали остаётся в силе и подтверждается: `C.UTF-8` на macOS отсутствует, и §2 снимает зависимость от локали в самом паттерне, не в окружении. Текст `ADR-072` не редактируется (`06` append-only). accepted.
§4 **`selftest.sh` расширяется по трём осям, не по одной.** «Фикстура достаточного объёма» поймала бы §1 и промахнулась по §2. Оси: размер (файл больше буфера пайпа, good и bad), локаль (весь набор кейсов прогоняется в `C` и в первой доступной UTF-8-локали системы, определяется через `locale -a`), легитимный композит (good-кейс из строк, дословно встречающихся в репо). Замерено: прежний `selftest.sh` на сломанном хуке даёт «провалено 0» и в `C`, и в `C.UTF-8`, то есть слеп ко всем дефектам. Новый набор кейсов: на Linux (GNU coreutils, доступна `C.UTF-8`) 28 кейсов, на исправленном хуке «провалено 0», контр-прогон против прежнего хука «провалено 7» с попаданием в каждый из дефектов (`4-good-reform`, `6-good`, `7-bad` в обеих локалях, `8-good` в `C`). На macOS владельца многобайтная локаль функциональной пробой не найдена, набор вырождается в 14 кейсов (только `C`), контр-прогон против прежнего хука «провалено 4». Число кейсов платформозависимо ПО КОНСТРУКЦИИ и это не деградация: ось локали проверяет независимость паттерна от локали, а байтовый режим `C` — самый строгий из двух. accepted.
§5 **Отдельная `Q` не заводится** (прецедент `Q-53`: CLOSED применением): дефект и фикс закрыты в одной сессии с верификацией пер-проверка (`ADR-072 §3`). Остаток, не покрытый замером, — поведение на macOS/BSD; он живёт как `Q-76 (в)` и дополняется там строкой про новую форму класса. Условие снятия было: `bash tools/hooks/selftest.sh` на машине владельца, ожидание «провалено 0». **ИСПОЛНЕНО в этой же сессии (2026-07-28, macOS, BSD grep): «пройдено 28, провалено 0», обе локали, многобайтная найдена функциональной пробой как `en_US.UTF-8`.** `Q-76 (в)` закрывается фактом, вместе с ним остаток `ADR-072 §7` о поведении паттерна под BSD grep. Пункты `Q-76 (а)`/`(б)` (охват набора гомоглифов и фильтр проверки №1) остаются открытыми без изменений, строка `Q-76` из `07_STATE` не уходит, архивации нет. accepted.
§6 **Класс процессного дефекта промоутится в `05`.** Третий инстанс подряд одного класса: `grep -PqE` (`ADR-072 §1`), сломанный `printf` в стенде (`ADR-072 §9`), bracket-выражение над многобайтными символами (§2). Во всех трёх заявка «замерено» опиралась на вход, не встречающийся в репо. Владелец выбрал вариант (a): правило заносится в `05` Часть I ★ «Успех инструмента ≠ факт» третьим абзацем, тем же коммитом. Вариант (b) «оставить в append-журнале» отклонён владельцем; цена отклонённого варианта — прецедент `Q-40` (правило есть, промоушен не сделан, класс повторился). accepted (владелец).
§7 **Проверка №4 переводится с «удалена строка» на «ID исчез».** Прежняя форма срабатывала на любой правке существующей строки таблицы, потому что в diff это удаление плюс добавление; ложный позитив был документирован в самом тексте ошибки хука («Ложное срабатывание при переформулировке строки, обход: git commit --no-verify»). Цена — `--no-verify` на каждом `STATE_PATCH`, правящем существующую строку, то есть на большинстве сессий, что и есть механизм привыкания из `Q-75`. Новая форма: сравниваются множества ID удалённых и добавленных строк, требование `07_ARCHIVE.md` в коммите возникает только для ID, исчезнувших полностью; исчезнувшие ID печатаются в сообщении. Предел `ADR-072 §4` («проверка №4 фикстурой не покрывается, требует истории коммитов») СНЯТ: `run_case_hist` делает сид-коммит через `--no-verify` и судит только второй коммит, покрыты три кейса (`4-bad-gone`, `4-good-reform`, `4-good-archive`). accepted.
§8 **Литеральный гомоглиф в коммитимом файле сам есть нарушение `ADR-041`.** Прежняя фикстура `2-bad` задавала кириллицу литеральным символом (`ADR-072 §9(i)`), и этот литерал лежит в репо в `tools/hooks/selftest.sh` строки 49-50; попал он туда только потому, что проверка №2 была сломана дефектом §1 (дифф того коммита превышал буфер пайпа). Исправленный хук ловит его сразу, что и обнаружилось при контрольном коммите настоящего ADR: коммит отбит в обеих локалях с печатью совпавших строк. `ADR-072 §9(i)` **superseded-in-part**: подделка собирается восьмеричным escape POSIX `printf` (проверено идентичным результатом в `bash`, `sh`, `dash`), урок §9 сохранён и усилен — фикстура проверяется сразу после записи И подтверждается дампом байтов (`od -An -tx1`, ожидание `d0 9e`). Проза ADR, `05` и `07` описывает гомоглифы кодовой точкой, не символом. Требование `ADR-072 §9(ii)` остаётся в силе без изменений. accepted.
§9 **Форма верификации сама обязана быть платформо-нейтральной.** Первая редакция §8 проверяла фикстуру поиском `d0 9e` в выводе `od -An -tx1`, а первая редакция §4 подбирала UTF-8-локаль совпадением имени в `locale -a`. Обе формы отказали на macOS владельца при полностью корректной фикстуре и корректном хуке: формат `od` различается между GNU и BSD и байт может оказаться последним в строке дампа, а имя локали ничего не гарантирует, поскольку несуществующая локаль молча откатывается в `C`. Это тот же класс, что §6 объявляет правилом, воспроизведённый внутри самого ADR-074 и поймавшийся на первом прогоне у владельца, а не в репо. Формы исправлены: дамп нормализуется удалением всех пробелов и переносов (`tr -d`), поиск идёт по `d09e`; локаль подбирается ФУНКЦИОНАЛЬНОЙ пробой (bracket-выражение над кириллицей против «`Q-`» плюс кириллическая буква: совпало значит байтовая семантика), а не по имени. Правило-следствие: проверка окружения формулируется через наблюдаемое поведение, не через имя или версию. accepted.
**Последствия (blast radius):** `tools/hooks/pre-commit` — замена файла целиком (проверки №1, №2, №4, №5, новые переменные `CYR` и `ID_ROW`); `tools/hooks/selftest.sh` — замена файла целиком (три оси, `run_case_hist`, выбор UTF-8-локали); `06_INDEX.md` — новая строка `ADR-074`; `06_DECISIONS_LOG.md` — `ADR-072 §1` помечен `superseded-in-part` ссылкой отсюда, `ADR-072 §4` помечен исчерпанным (§7), текст `ADR-072` не редактируется; `05_CONVENTIONS.md` Часть I ★ — вставка третьего абзаца (§6); `07_STATE.md` — строки `CC-PILOT-SCAFFOLD`, `Q-76`, `FX-POLICY-EXPENSES`, «Текущий фокус», `updated_at`; `CLAUDE.md` §Старт п.5 — не меняется, прогон самотеста там уже есть.

## ADR-075 · Итоги пилота Claude Code: вердикт PASS по (a)-(e), (f) с оговоркой; `ADR-070 §2(b)` superseded-in-part (критерий выведен из свойства данных, а не из формулы); два дефекта провенанса закрыты; applier ретайрится для сессий внутри инструмента

**Дата:** 2026-07-28 · **Статус:** accepted (владелец, сессия CC-PILOT-EXIT)
**Контекст:** `ADR-066` назначил пилот, `ADR-070 §2` зафиксировал success-критерий из шести пунктов «все одновременно» и правило «любой провал (a)-(e) = FAIL». Тренировочный прогон `FX-MAY-WINDOW` исполнен 2026-07-28 (`ADR-073 §7`), пилотная сессия `FX-POLICY-EXPENSES` исполнена 2026-07-28, cutover `2026-07-28T11:38:21Z`. Настоящий ADR квалифицирует итог, потому что автоматического вердикта `ADR-070 §2` не предусматривает.

**Решение:**

§1 **Вердикт: пилот PASS.** Пункты (a)-(e) подтверждены не заявкой сессии, а содержимым репо (сверка архитектор-сессии по SHA `e77ac203` против SHA `c229bb7b`, то есть состояния до пилотного прогона):
(a) три коммита формата проекта в интервале `11:43:36…11:43:57Z` (`63b3890` патч `03`, `9276a1a` state patch, `e77ac20` cutover и артефакты); все коммиты того же дня ДО пилота имеют формы `Add files via upload` / `Create …` / `Rename …`, то есть веб-интерфейс GitHub, ручной транспорт класса `Q-60`/`B-12`. Прямого различителя «коммитил агент или человек» репо не даёт (author и committer в обоих случаях подпись владельца); квалификация опирается на форму сообщений и интервал, это названо, а не выдано за доказательство.
(b) май-2026 в KGS `10 232 903,20`, разрыв `0,00`; схема 17 колонок побайтово идентична; воспроизводимость подтверждена двумя прогонами staging. Отдельно по клаузе про USD, см. §2.
(c) транспортных инцидентов класса `Q-62`/`Q-68` не зафиксировано.
(d) оба не-идемпотентных действия прошли явное ask-подтверждение; порядок соблюдён: гейт `E1-T1-CUTOVER-VERIFY` закрыт фактом в `11:10:01Z`, cutover в `11:38:21Z`, то есть после гейта (в отличие от прецедента `ADR-073 §2`).
(e) `06_DECISIONS_LOG.md` до и после пилота 203 875 байт, ноль изменённых строк (append-only не нарушен); `06_INDEX.md` без изменений и это корректно, новых ADR сессия не заводила; `07_STATE.md` 48 181 → 44 825 байт, две строки ушли (`ADR-064` соблюдён); `07_ARCHIVE.md` принял две строки тем же коммитом; `03_PIPELINE_SPEC.md` одна строка заменена, это исполнение `ADR-062 §6`, не новое решение; снимок `reference/sql/sq_marts_expenses.sql` 3179 байт, sha256 `f3c0ecaf108324628c2d8025c1872d1054eef9ba48a31cff42e67fe7d3def731`, совпадает с заявкой архивной строки. accepted.

§2 **`ADR-070 §2(b)` — superseded-in-part.** Клауза «`total_sum_usd` мая не изменился» опровергнута фактом: `117 014,30` → `117 053,73`, сдвиг `+39,43`, отношение курсов `87.45/87.4205 = 1,000337`. Дефект в критерии, а не в исполнении: клауза выведена из свойства данных («в мае одно значение курса»), тогда как единственное значение курса гарантирует инвариантность НОВОЙ величины и ничего не говорит о совпадении со СТАРОЙ, которая считалась по курсу даты пересборки (`87.45` от 2026-07-27). Сдвиг был арифметически неизбежен.
**Существенно для квалификации:** переформулировка датирована ДО прогона, а не после результата. Она записана решением владельца №1 в шапке `briefs/FX-POLICY-EXPENSES.md` при генерации 2026-07-28: приёмка по USD читается как воспроизводимость величины закрытого периода, однократный сдвиг принимается (`ADR-057 §4`). Это не подгонка правила под данные (класс, отклонённый в `ADR-072 §5`).
**Дефект, который остаётся:** адрес переформулировки. Правка клаузы accepted-ADR легла в шапку брифа вместо нового ADR, против `_METHOD §6` и `CLAUDE.md §Дисциплина документации`. Настоящий параграф исправляет адрес задним числом; текст `ADR-070` не редактируется (`06` append-only). Действующая редакция пункта (b): регрессия мая в KGS = `0,00` против `/reference/pnl_2026-05.md`; величина USD закрытого периода воспроизводима между прогонами; сдвиг USD относительно прежней политики посчитан и объяснён. accepted.

§3 **Правило-следствие: критерий приёмки выводится из формулы, а не из свойства данных.** Свойство входа («один курс за месяц», «ноль не-KGS документов», «одна строка в справочнике») говорит о поведении новой формулы и не говорит о разнице со старой. Разница считается сравнением двух формул, а не наблюдением над данными. Промоутится в `05` Часть I отдельным блоком тем же коммитом (прецедент формы `ADR-074 §6`; цена непромоушена, `Q-40`: правило есть, класс повторяется). accepted.

§4 **Дефект провенанса 1 закрыт.** `reference/fx_policy_expenses_2026-07-28.md` §«Шаг 6 — cutover» говорил `НЕ ИСПОЛНЕН` и обещал заполнить время подмены и sha256 после исполнения, хотя файл закоммичен в `11:43:57Z`, через пять минут после cutover. Назначенный провенанс-артефакт противоречил факту, класс `ADR-054`. Секция заменена; факты перенесены дословно из строки `FX-POLICY-EXPENSES` в `07_ARCHIVE.md`, где они единственно и жили. **Названная неполнота:** полный `job_id` ручной пересборки в архивной строке усечён (`scheduled_query_6a68ac72-…`), в артефакт перенесён в том же усечённом виде; восстанавливается из локального лога шага 6, если он сохранился, и в этом случае дописывается отдельным коммитом. accepted.

§5 **Дефект провенанса 2 закрыт, причина названа.** В `06_DECISIONS_LOG.md` текст `ADR-074` лежал ПОСЛЕ маркера `--- END 06_DECISIONS_LOG.md ---`. К пилоту отношения не имеет: файл побайтово одинаков до и после пилотной сессии, значит дефект внесён скриптом правки доков сессии `CC-HOOK-DEFECT-2` (тем самым, который отработал дважды при исправной защите от повторного применения, причина расхождения не установлена). Маркер перенесён в конец файла, тексты ADR не редактировались. Факт засчитывается как фактура в пользу `ADR-066`: ручной скриптовый транспорт дал структурный дефект там, где сессия внутри инструмента его не дала. accepted.

§6 **Ретайр по `ADR-066 §Последствия` — исполняется частично и поимённо.**
(i) **Applier ретайрится для сессий внутри Claude Code.** Внутри инструмента роль уже не существует фактически: патчи применяет и коммитит исполнитель. Держать её ради формы значит держать ручной транспорт.
(ii) **SHA-curl для чат-сессий СОХРАНЯЕТСЯ.** Это единственный независимый способ проверить результат работы инструмента; снятие превратило бы ревью в самопроверку. Отклонение `ADR-071 §5` (чтение локального клона внутри инструмента) остаётся локальным для Claude Code.
(iii) **Сохраняются без изменений:** ask-гейты на не-идемпотентном, роль архитектора как отдельный слой суждения, `_APPLIER.md` как файл роли для чат-сессий. Инструмент снял транспортную проблему, не проблему решений. accepted.

§7 **(f) — PASS с оговоркой, оговорка зафиксирована фактом.** Владелец подтвердил: ручного переноса файлов не было, роль свелась к ревью и подтверждениям. Оговорка владельца: подтверждать приходилось КАЖДЫЙ запуск скрипта, включая read-only. Это не дефект исполнения и не снимает PASS, но это ровно тот механизм привыкания, о котором предупреждает `Q-75`, и на read-only шагах подтверждение не несёт защитной функции. Состав прав настоящим ADR НЕ меняется (`ADR-070 §1`, `ADR-071 §6`: правка прав требует отдельного ADR); вопрос вынесен отдельным решением. Строка `Q-75` дополняется этим фактом тем же коммитом. accepted.

§8 **Вне scope:** состав `.claude/settings.json`; формальное закрытие `Q-70`; очередь задач и форма мандата (`ADR-076`); политика `ADR-062` не пересматривается.

**Последствия (blast radius):** `06_INDEX.md` — новая строка `ADR-075`; `06_DECISIONS_LOG.md` — перенос маркера конца файла в конец (§5), тексты ADR не редактируются; `reference/fx_policy_expenses_2026-07-28.md` — замена секции «Шаг 6 — cutover» (§4); `05_CONVENTIONS.md` Часть I — новый блок «★ Критерий приёмки выводится из формулы» перед «★ Не-идемпотентные операции» (§3); `07_STATE.md` — «Текущий фокус», дополнение строки `Q-75` (§7), удаление строки `CC-PILOT-SCAFFOLD` (закрыта §1), header; `07_ARCHIVE.md` — принимает строку `CC-PILOT-SCAFFOLD`; `_APPLIER.md`, `_GENERATOR.md`, `_ARCHITECT.md`, `_METHOD.md` — БЕЗ правок (§6 живёт в ADR, эталонные файлы ролей не трогаются, прецедент `Q-40`).

## ADR-076 · Выход из пилот-режима: мандат Claude Code по КЛАССУ задачи (A постоянно, B по задача-специфичному ADR); очередь после пилота; `PARITY-REGISTRY` разрезана на discovery и адъюдикацию моста

**Дата:** 2026-07-28 · **Статус:** accepted (владелец, сессия CC-PILOT-EXIT; выбор варианта (b) из трёх)
**Контекст:** мандат выдавался по одной задаче отдельным ADR (`ADR-070`, `ADR-071 §3`, `CLAUDE.md §Scope пилота`). Это узкое место, подтверждённое фактом: бриф `briefs/PARITY-REGISTRY.md` сгенерирован внутри инструмента 2026-07-28 и лежит неисполнимым, потому что мандата на задачу нет. Владельцу предъявлены три варианта (мандат по задаче, по классу, полный) с векторами и ценой; выбран мандат по классу.

**Решение:**

§1 **Класс A (постоянный мандат, нового ADR не требует):** чтение репо и облака; запись ТОЛЬКО в staging-таблицы (`*_staging`) и в `/reference`; коммит в локальный репозиторий. `git push` остаётся `ask`. Не-идемпотентных облачных операций нет по определению класса.
**Класс B (требует задача-специфичного ADR, как раньше):** подмена живых конфигураций витрин и любые правки `transferConfig`; деплой и правка Cloud Functions, Scheduler, Workflows; чтение и запись секретов; любые операции над инвесторскими поверхностями и денежными величинами.
**Правило разрешения сомнения: при сомнении задача идёт в класс B.** Ошибка классификации в сторону B стоит одной архитектор-сессии, в сторону A стоит инцидента на живом проде. accepted (владелец).

§2 **Где живёт класс.** (i) Обязательное поле «Класс задачи» в шапке брифа (`08_TASK_BRIEF_TEMPLATE`, правка тем же коммитом); (ii) таблица `07_STATE §Мандат Claude Code: класс задач`, откуда его берёт генератор; (iii) `CLAUDE.md §Мандат сессий` как закон сессии. **Расхождение между классом в брифе и классом в `07_STATE` есть стоп-условие сессии**, не повод выбрать удобный: форма остановки, как у `ADR-054`. accepted.

§3 **Очередь после пилота** (выбор владельца, обоснование см. §4):
(a) **`PARITY-REGISTRY`, discovery-часть — первой.** Критический путь: финиш Epic-1 считается от этого реестра (`ADR-067 §5b`). **Класс B**, единственное основание: задаче нужен `msklad-token` из Secret Manager (`11 §секреты`), а работа с секретами есть класс B по §1. **Задача-специфичный мандат выдаётся настоящим параграфом поимённо**, отдельной сессии не требует, при выполнении всех оговорок §5.
(b) **`CODE-REPO-STANDUP`, шаг 1** (discovery локального `.git` в каталоге `cf-finance`) — параллельно, **класс A**, идёт по постоянному мандату. Промоутнут в критический путь `ADR-065`; без него заблокирован любой деплой CF, то есть и cutover `E1-T3-MECH-FX`.
(c) **`Q-4`** (дамп схем через `INFORMATION_SCHEMA.COLUMNS` в `/reference`) — **класс A**, отдельной сессии не стоит, приклеивается хвостом к (a) или (b). Разблокирует решение по `Q-39`.
(d) **`E1-T3-MECH-FX`, Шаг 4** (staging dry-compare, артефакта которого в репо нет, `Q-41`) — **класс A**. Сам cutover этой задачи — **класс B** и дополнительно гейтится `ADR-065`.
(e) **`ORACLE-AUTOMATION`** — после реестра, форма эталона определяется реестром (`ADR-067 §5c`).
(f) **`E1-T2-MECH-TAX` в инструмент НЕ отдаётся:** задача требует входа владельца (число начисления и каденс), автономной сессией не является. accepted.

§4 **`PARITY-REGISTRY` разрезается надвое.** Задача смешивает два типа работы. Discovery (соответствие endpoint'ов report-API страницам UI, инвентарь поверхностей Looker Studio, замеры сходимости) есть добыча фактов и идёт в инструмент. **Мост соответствий есть правила уровня `ADR-068 §3` и адъюдицируется архитектором** по возвращённой фактуре: исполнитель, назначающий правило соответствия, назначает легитимное отступление от оракула, а это решение владельца и архитектора, не исполнителя. Артефакт discovery-части: `/reference/parity_discovery_<date>.md`; `/reference/parity_registry.md` заполняется после адъюдикации. accepted.

§5 **Обязательные оговорки брифа `PARITY-REGISTRY` (нарушение любой = провал сессии):**
1. **Токен не печатается.** Читается одной командой в переменную окружения; `set -x` запрещён; `echo` значения запрещён; значение не попадает в session-блок и в сообщение коммита.
2. **Форма заголовка авторизации берётся из снапшота кода `reference/code/cf-finance/main.py`, а не изобретается** (`_METHOD §6`).
3. **Проверка перед `git add`:** поиск значения токена по всем файлам сессии (`grep -rF -- "$MSKLAD_TOKEN" <каталог сессии>`); любое совпадение есть стоп, не предупреждение.
4. **Сетевые вызовы (`curl`) в `.claude/settings.json` не перечислены ни в одном списке.** Поведение инструмента на неперечисленной команде на этой установке НЕ замерено, из документации не берётся (класс `Q-75`, `ADR-021 §2`). Различитель назначен: вывод `/permissions` на Шаге 0 плюс первый фактический вызов; результат фиксируется в session-блоке.
5. **Эталон датируется снимком** (`ADR-067 §3`): МойСклад пересчитывает историю при правках задним числом, недатированный эталон даст ложные расхождения.
6. **`Q-77` вносится в discovery-артефакт как кандидат в правило моста.** Приведение `moment` из TIMESTAMP в DATE по UTC при зоне UTC+6 теперь влияет и на выбор курса дня (`FX-POLICY-EXPENSES`, побочный факт); до якоря паритета это ничего не гейтило, с `2026-05-01` становится живым источником расхождений на границе месяца. accepted.

§6 **Рабочие файлы сессии.** Скрипты, логи и промежуточные дампы пишутся в `reference/_scratch_<TASK>_<YYYY-MM-DD>/` С САМОГО НАЧАЛА, не в корень репо, и коммитятся вместе с артефактом как провенанс (прецедент формы: `reference/_scratch_fx_may_window_2026-07-28/`). Основание: `CLAUDE.md §Старт` пункт 1 требует чистого дерева, а корень, засыпанный неотслеживаемыми файлами прогона, останавливает каждую следующую сессию. accepted.

§7 **Вне scope:** состав прав `.claude/settings.json` (`ADR-070 §1`, отдельное решение); ретайр `_APPLIER` для чат-сессий (`ADR-075 §6`); расширение реестра паритета за пределы сида `client_concerns` (`04_ROADMAP §финиш Epic-1`: расширение есть новое решение владельца, не тихое расползание).

**Последствия (blast radius):** `06_INDEX.md` — новая строка `ADR-076`; `CLAUDE.md` — секция «Scope пилота» заменяется секцией «Мандат сессий (ADR-076)»; `08_TASK_BRIEF_TEMPLATE.md` — обязательное поле «Класс задачи» в шаблоне и в правилах заполнения; `_GENERATOR.md` — пункт 4 «Что сделать» дополняется определением класса; `07_STATE.md` — новая секция «Мандат Claude Code: класс задач», «Текущий фокус»; `04_ROADMAP.md` — БЕЗ правок (очередь живёт в `07_STATE`, роудмап не расписывает будущие задачи, `_METHOD §5`); `05_CONVENTIONS.md` — БЕЗ правок по этому ADR.

## ADR-077 · Права под класс A: ограничение переносится на слой личности, не на список команд; вариант «разрешить запуск скриптов» отклонён навсегда; переход гейтится замером трёх неизвестных

**Дата:** 2026-07-28 · **Статус:** §1, §2, §6, §7 accepted (владелец, сессия CC-PILOT-EXIT); §3, §4, §5 accepted как процедура, форма фикса НЕ назначается до замера
**Контекст:** `ADR-075 §7` зафиксировал фактом со слов владельца: в пилотной сессии подтверждение запрашивалось на КАЖДЫЙ запуск скрипта, включая read-only шаги. `Q-75` предупреждал ровно об этом риске (привыкание подтверждать механически) до того, как он проявился. Владельцу предъявлены три варианта с ценой; выбор: (b) как цель, (c) немедленно, (a) не брать ни в каком виде.

**Решение:**

§1 **Принцип: ограничение класса A переносится на слой ЛИЧНОСТИ, а не на список разрешённых команд.** Список прав фильтрует строку команды, а не содержимое скрипта. Правило доставки `ADR-055 §1` обязывает исполнять именно файлом-скриптом, поэтому список команд по построению не может отличить read-only скрипт от записывающего: он видит одну и ту же строку `bash script.sh`. Единственное место, где различие выразимо механически, есть права принципала, под которым идёт сессия. accepted (владелец).

§2 **Вариант «разрешить запуск скриптов» (a) ОТКЛОНЁН навсегда, с ценой, чтобы не всплыл повторно.** Разрешение на запуск любого скрипта есть разрешение на исполнение скрипта, внутри которого лежит `bq update --transfer_config` или `gcloud functions deploy`. Все прочие ask-гейты после этого декоративны, а свойство `ADR-070 §2(d)`, единственное подтверждённое пилотом свойство безопасности, исчезает механически, не по ошибке исполнителя. Отклонение не пересматривается уменьшением области действия («разрешить только `bash step*.sh`»): содержимое файла от имени файла не зависит. accepted (владелец).

§3 **Переход к (b) гейтится замером ТРЁХ неизвестных.** Ни одна из них не выводится из документации инструмента или облака (`ADR-021 §2`, `05` Часть I ★):
**(D1) Механизм трения.** Гипотеза: команда `bash script.sh` не покрыта ни одним правилом `.claude/settings.json`. Различитель: вывод `/permissions` плюс один наблюдённый запуск в живой сессии. Пока не замерено, правка списка прав шла бы вслепую.
**(D2) Понимает ли `bq` impersonation.** `gcloud` умеет работать от имени сервисного аккаунта без ключа, поведение `bq` на этой установке НЕ замерено. Если `bq` impersonation не уважает, единственная альтернатива есть отдельная конфигурация с файлом-ключом, а это управляемый секрет на машине владельца, то есть цена варианта (b) вырастает и владелец пересматривает объём, а не узнаёт об этом постфактум.
**(D3) Куда фактически пишет класс A и отделима ли эта запись от живых витрин.** Факт, обнаруженный при разборе: `marts.expenses_staging` лежит в датасете `marts` вместе с живой `marts.expenses`. Право записи, выданное на уровне датасета, покрывает обе, и защита обнуляется. Замер отвечает: покрывает ли табличный уровень прав пересоздание staging-таблицы (`--replace`), и если нет, то цена выноса staging в отдельный датасет (правка формы staging-шага во всех последующих брифах). **Исход D3 не предрешён и форма фикса здесь НЕ назначается** (`_METHOD §6`).
Артефакт замера: `/reference/cc_readonly_principal_<date>.md`. accepted.

§4 **Состав ролей принципала НЕ назначается заранее, а выводится из провенанса.** Скрипты и логи исполненных класс-A прогонов коммитятся как провенанс (`ADR-076 §6`), в частности `reference/_scratch_fx_policy_expenses_2026-07-28/` и `reference/_scratch_fx_may_window_2026-07-28/`. Минимальный набор прав собирается перечислением фактически исполненных вызовов `gcloud` и `bq` по этим каталогам, а не гаданием по названиям ролей. Право, не встретившееся в провенансе, не выдаётся; понадобилось в новой задаче, значит расширяется явно. accepted.

§5 **Верификация переноса — пер-проверка с обязательным контр-прогоном** (`ADR-072 §3`/`§8`). Зелёный результат недостоверен, пока не показано, что защита умеет краснеть. После перевода класс-A сессий на новый принципал обязателен негативный контроль: заведомо записывающая команда по живой витрине обязана упасть отказом прав, и текст отказа печатается в артефакт. Прогон без контр-прогона доказывает лишь, что чтение работает. accepted.

§6 **Промежуточная мера (c) вступает в силу немедленно, до исполнения §3.** Два правила:
(i) **Один скрипт на ШАГ брифа, а не на команду.** Диагностика и действие в одном скрипте по-прежнему запрещены (`ADR-055 §2`), но дробить read-only шаг на пять скриптов ради удобства нельзя: каждое дробление есть лишнее подтверждение и вклад в привыкание.
(ii) **Перед любым ask на действие класса B исполнитель обязан отдельным сообщением в чат назвать: что именно будет исполнено, на каком объекте, чем откатывается.** Владелец отличает настоящий гейт от рутинного запуска скрипта по наличию этого сообщения, а не по виду диалога инструмента. Это и есть работающая защита от привыкания на период, пока трение остаётся. accepted (владелец).

§7 **`Q-75` НЕ закрывается настоящим ADR.** Решение о направлении принято, состав прав не изменён, замер не исполнен. Строка остаётся открытой, триггер переформулирован: закрывается исполнением замера `§3` и последующей правкой прав, либо отрицательным результатом D2/D3, при котором владелец пересматривает объём варианта (b). accepted.

§8 **Вне scope:** состав `.claude/settings.json` этим ADR не правится (`ADR-070 §1` в силе до отдельного решения по итогам замера); классы задач и очередь (`ADR-076`) не пересматриваются; `Q-28` (Regional Access Boundary на личном аккаунте) не переоткрывается, но входит в контекст замера как известная особенность IAM этого проекта.

**Последствия (blast radius):** `06_INDEX.md` — новая строка `ADR-077`; `07_STATE.md` — новая задача-строка `CC-READONLY-PRINCIPAL`, строка в таблице `§Мандат Claude Code: класс задач`, дополнение `Q-75`, «Текущий фокус»; `CLAUDE.md §Мандат сессий` — абзац промежуточной меры (§6); `.claude/settings.json`, `ADR-070 §1`, `ADR-055`, `04_ROADMAP` — БЕЗ правок.

## ADR-078 · Регистрация `reference/parity_registry.md` в `02_ERP_CONTRACTS §оракул`

**Дата:** 2026-07-28 · **Статус:** accepted (владелец, сессия PARITY-REGISTRY)
**Контекст:** discovery-часть задачи `PARITY-REGISTRY` (сессия 2026-07-28, класс B, `ADR-076 §3a`) собрала `reference/parity_registry.md` — реестр пар «поверхность МойСклад ↔ витрина/страница LS» с правилом-мостом (`ADR-068 §3`), покрытием-с-даты (`ADR-069`) и статусом сходимости. `ADR-067 §Последствия` не называл правку `02 §оракул` в числе последствий этой сессии, поэтому регистрация оставалась `proposed` в session-блоке коммита `79aac22` до отдельного апрува (STABLE-док, дисциплина изменений `05` Часть I).
**Решение:** добавить строку в `02_ERP_CONTRACTS.md §оракул → Реестр эталонов`:

| Артефакт | Класс / период | Источник | Правило сверки | Статус |
|---|---|---|---|---|
| `reference/parity_registry.md` | реестр пар «поверхность МойСклад ↔ витрина/страница LS», все периоды/треки Epic-1 | сводный: ADR-067/068/069 + `reference/sql/*` + report-API/entity-API МойСклада (провенанс `reference/parity_report_api_2026-07-28.md`) | построчно по каждой паре реестра; статус — только по ссылке на артефакт-сверку (`ADR-021 §2`), отсутствие артефакта не читается как «сходится» | заведён 2026-07-28; 1 из 7 пар численно сходится (Расходы, 0,00); остальные 6 — «требуется» (`Q-78`…`Q-82`) |

Регистрация фиксирует существование реестра как обязательного контекста `02`; адъюдикация самих мостов (`Q-78`…`Q-82`) этим ADR не производится — вне мандата сессии (`ADR-076 §4`), решает архитектор.
**Последствия (blast radius):** `02_ERP_CONTRACTS.md §оракул` — новая строка реестра эталонов; `06_INDEX.md` — строка `ADR-078`.

## ADR-079 · Адъюдикация моста PARITY-REGISTRY: критерий «вне паритета»; мост описывает core-слой, а не SQL марта; правило одновременности снимка; состав, порядок и форма добычи карты источника

**Дата:** 2026-07-29 · **Статус:** §1-§5 accepted (архитектор), §6 proposed, §7-§9 accepted (владелец)
**Контекст:** discovery-часть `PARITY-REGISTRY` исполнена (сессия 2026-07-28, класс B, `ADR-076 §3a`), артефакты `reference/parity_registry.md` и `reference/parity_report_api_2026-07-28.md` в репо. Из семи пар численно сходится одна (Расходы, `0,00`). Пять пунктов вынесены архитектору файлом `reference/architect_review_queue_2026-07-28-1.md`, реестр гэпов несёт `Q-78`…`Q-82`. Адъюдикация моста в мандат исполнителя не входила (`ADR-076 §4`).

**Решение:**

§1 **Критерий «величина вне паритета» (закрывает `Q-80` в части расчётных флагов).** Величина витрины не является объектом паритета, если на стороне МойСклада нет публикуемого аналога ни на одном уровне иерархии `ADR-067 §2` (i report-API, ii агрегация entity-API, iii страница UI). Такая величина не выбрасывается молча: вносится в `reference/parity_registry.md` строкой с основанием. Под критерий на текущей фактуре подпадают `coverage_days_90d_calendar`, `coverage_days_true_adt`, `is_low_stock`, `is_oos`, `is_toxic` (`marts.inventory_health`) и весь `marts.weight_flow` (уже в разделе «вне реестра»). Критерий назначен вместо перечня сознательно: перечень требует правки при каждой новой колонке, критерий проверяем по SQL. Отношения к `ADR-067 §4` (DERIVED через USD-конвертацию) не имеет и его не расширяет: это другое основание. accepted.

§2 **Мост считается выведенным только при описанном core-слое.** Для каждой сверяемой величины пары мост обязан называть: документ-источник МойСклада, поле этого документа, правило конвертации в KGS (`ADR-010`), фильтр периода и зерно строки. Снимок март-SQL из `reference/sql/` моста не образует: он документирует переупаковку `core` в витрину, а не происхождение `core`. Основание: `ADR-068 §3` требует правила, по которому клиент воспроизводит нашу цифру руками из своей страницы; переупаковка `core` этого не даёт. Прецедент положительный: пара «Расходы» единственная сошлась в `0,00` именно потому, что её core-слой адъюдицирован (`ADR-006`, `ADR-010`, `ADR-013`, `ADR-026`), а не потому, что её SQL проще. accepted.

§3 **Правило одновременности для снимочных витрин.** Для витрин с зерном «состояние на текущий момент» (`marts.in_transit`, `marts.inventory_health`) эталон обязан сниматься в том же окне, что и пересборка витрины; окно фиксируется в артефакте сверки в UTC. Иначе сверка невоспроизводима и расхождение неатрибутируемо. Расширение `ADR-067 §3` (датированный снимок) на класс снимочных поверхностей. accepted.

§4 **Переклассификация `Q-78`, `Q-79`, `Q-81`, `Q-82`.** Это не «мост выведен, но не сверен численно», а «нет карты источник → core». В частности `Q-81` (`marts.in_transit`) закрывается не одним замером: фильтр `status_name IN ('В пути','Прибыл частично')` взят из SQL, а величины `quantity_in_transit` и `in_transit_sum_kgs` считаются в `core.fact_purchases`, формула из API в репо не документирована, и `02 §семантика данных` п.5 предупреждает о ненадёжности `is_in_transit` в той же таблице. Построчная сверка идёт ПОСЛЕ карты, не до неё: сверка до карты даёт расхождение, которое некуда отнести, и это уже наблюдалось на расходах до `ADR-006` (разрыв ~33 %, `ADR-005 §Контекст`). accepted.

§5 **`ADR-076 §4` superseded-in-part по имени артефакта.** Фактические имена приняты: discovery-часть вернула `reference/parity_registry.md` (неадъюдицированные ячейки помечены «требуется») и `reference/parity_report_api_2026-07-28.md`; предписанное имя `/reference/parity_discovery_<date>.md` не используется. Причина: бриф `briefs/PARITY-REGISTRY.md` (Шаг 3, критерии приёмки) предписал собирать реестр напрямую, исполнитель шёл по брифу; `ADR-078` уже зарегистрировал имя `reference/parity_registry.md` в `02 §оракул`, переименование сейчас ломает ссылку в STABLE-доке ради формы. Разрез задачи на discovery и адъюдикацию (`ADR-076 §4` по существу) в силе. Процессный вывод: генератор брифа не сверил имя артефакта с назначенным в ADR; проверка соответствия брифа основанию остаётся за архитектором на приёмке. accepted.

§6 **Форма логирования discovery по HTTP-эндпоинтам.** В лог пишутся имена полей строки данных (ключи первого элемента массива строк ответа), а не первые N символов тела. Основание: в `reference/_scratch_PARITY-REGISTRY_2026-07-28/run_step2.log` окно снипета исчерпано блоками `context` и `meta`, ни одно измеримое поле отчётов `report/*` не зафиксировано (единственное исключение `report/stock/all`, где строка мелкая), из-за чего вердикт «endpoint подтверждён живым `200`» оказался неприменим для вывода правила моста. proposed (правка `05` Часть II, применяется после апрува владельца).

§7 **Состав и порядок задач добычи карты (выбор владельца 2026-07-29, вариант (b) из трёх).** Карта снимается двумя заходами, инвесторская поверхность первой:
(a) **`SOURCE-MAP-SALES`, класс A.** `core.fact_sales_profit` → `marts.sales_overview` → страницы LS «Инвестор» и «Операционка». Включает различитель `Q-83`. Основание приоритета: две страницы LS из пяти, инвесторская поверхность, наибольшая цена ошибки.
(b) **`SOURCE-MAP-REST`, класс A.** `core.fact_purchases` (`marts.in_transit`), `core.fact_customer_invoices` (`marts.customer_invoices_ar`), SQL custom query `msklad_counterparty_returns` (снимка в `reference/sql/` нет), `stock`-компонент `marts.inventory_health`.
(c) **`REPORT-FIELDS`, класс B** (см. §9). Съём имён полей строк на стороне МойСклада, один заход на все эндпоинты. Не гейтит (a): наша сторона карты от него не зависит. Гейтит финальную формулировку правил моста в реестре.
Построчные сверки пар и правка `reference/parity_registry.md` идут после (a)-(c), отдельными задачами Epic-1. accepted (владелец).

§8 **Форма добычи карты (выбор владельца 2026-07-29, вариант (a) из трёх): с живого кода, не из данных.** `SOURCE-MAP-SALES` снимает снапшот задеплоенного `cf-facts` в `reference/code/cf-facts/` и ведёт `MANIFEST.md` по форме уже существующего `reference/code/cf-loss-commission/MANIFEST.md` (ревизия, `entryPoint`, `source.storageSource` с `generation`, `updateTime`, `state`, `serviceAccountEmail`, `uri`, sha256 по каждому файлу, метод извлечения). Путь к архиву берётся из поля `source.storageSource` в выводе `describe`, а НЕ конструируется по образцу пути `cf-loss-commission`: имя бакета в образце содержит номер проекта и регион, подстановка имени функции в чужой путь есть догадка (`_METHOD §6`). Вывод соответствия из данных (сравнение `core` с ответами API без чтения кода) отклонён: он даёт гипотезы, а не карту, и воспроизводит ровно тот класс ошибки, против которого стоит `ADR-076 §5.2`.
**Побочный результат, входит в задачу:** заполняется пустой слот `11_INFRA_FACTS §CF` по `cf-facts` («URL/ревизия не зафиксированы в источнике, ожидает discovery»). `11` имеет статус LIVING (`ADR-004`), правится session-блоком без отдельного ADR.
**Что этот снапшот НЕ решает:** развилку `CODE-REPO-STANDUP` (seed-from-git против seed-from-zip, место живого VC) и условие `ADR-065` (деплой CF только из код-репо). Снапшот есть провенанс, а не код-репо. accepted (владелец).

§9 **Мандат класса B на `REPORT-FIELDS` выдаётся поимённо настоящим параграфом.** Единственное основание класса B: доступ к секрету `msklad-token` (`ADR-076 §1`). Действуют ВСЕ шесть оговорок `ADR-076 §5` без изменений (токен не печатается; форма заголовка авторизации из `reference/code/cf-finance/main.py`; `grep -rF` по значению токена перед `git add`; поведение инструмента на неперечисленной команде фиксируется в session-блоке; эталон датируется снимком; `Q-77` как кандидат в правило моста), плюс форма логирования `§6` настоящего ADR. Задача только читает: ни живых конфигураций, ни деплоя, ни записи в МойСклад. accepted (владелец).

**Последствия (blast radius):** `06_INDEX.md` строка `ADR-079`; `07_STATE.md` шапка (`updated_at`, `обновил`), «Текущий фокус», таблица «Мандат Claude Code: класс задач» (строки `SOURCE-MAP-SALES`, `SOURCE-MAP-REST`, `REPORT-FIELDS`), «Открытые вопросы» (три новые задача-строки, новая строка `Q-83`, дописки в `Q-78`…`Q-82`); `11_INFRA_FACTS.md §CF` слот `cf-facts` заполняется задачей `SOURCE-MAP-SALES`, не этим ADR; `reference/parity_registry.md` правится после сверок, не этим ADR; `05_CONVENTIONS.md` Часть II только по §6 и только после апрува; `02_ERP_CONTRACTS.md` без правок (`ADR-078` уже применён).

## ADR-080 · Изоляция параллельных сессий Claude Code: буфер session-блоков плюс отдельное рабочее дерево на сессию; дисциплина поимённого коммита отклонена фактом

**Дата:** 2026-07-29 · **Статус:** §1-§4 accepted (владелец, сессия CC-PARALLEL-SESSIONS, выбор варианта (5) из пяти); §5-§7 accepted как процедура
**Контекст:** 2026-07-28 три сессии Claude Code исполнялись одновременно на ОДНОМ рабочем дереве. Владелец описал режим запуска: выбор папки репо, новый чат, повтор. Замер `reference/_scratch_CC-PARALLEL-SESSIONS_2026-07-29/probe.log` (прогон `2026-07-28T18:56:27Z`) подтвердил: `git worktree list` вернул одну запись, изоляции не было. Проявились три РАЗНЫЕ поломки, ранее не различённые и в репо не зарегистрированные.

**Решение:**

§1 **Дисциплина поимённого коммита как решение ОТКЛОНЕНА, и отклонение опирается на факт, а не на аналогию с `ADR-077 §2`.** Транскрипты сессий (`~/.claude/projects/*/*.jsonl`, выборка в `probe.log §G3`) показывают: `17:48:27Z` сессия `PARITY-REGISTRY` исполнила `git add 06_DECISIONS_LOG.md 06_INDEX.md reference/architect_review_queue_2026-07-28-1.md` и не коммитила; `17:50:43Z` сессия `Q-4` исполнила `git add` пяти своих путей; `17:50:59Z` сессия `Q-4` исполнила `git commit -m` БЕЗ путей, что дало `bfd067e` на 16 файлов двух сессий. **Обе стороны добавляли файлы поимённо, `git add -A` и `git commit -a` не использовал никто.** Правило, предложенное исполнителем в логе инцидента, фактически уже действовало и инцидент не предотвратило: индекс аддитивен, перечисление своих путей на стадии `add` не влияет на то, что заберёт `commit` по всему индексу. Отклонение не пересматривается сужением («коммитить с указанием путей»): такая правка чинит одну поломку из трёх и не касается ни §2, ни §3. accepted (владелец).

§2 **Вторая поломка: гонка за ФАЙЛ на диске, отдельная от гонки за индекс.** Сессия `Q-4` застейджила `07_STATE.md` в `17:50:43Z`, но в коммите `bfd067e` (`17:50:59Z`) файла нет: на момент `git add` содержимое на диске равнялось HEAD. Различить две причины (правка не легла на диск либо была затёрта параллельной сессией между редактированием и добавлением) этот лог не позволяет, и причина здесь НЕ назначается (`_METHOD §6`). Существенно другое: обе причины есть следствие одного рабочего дерева, и ни одна git-команда против них не помогает. Победитель определяется порядком записи, отказ тихий. accepted.

§3 **Третья поломка: `tools/hooks/pre-commit` под общим индексом даёт ложный вердикт.** Хук берёт `staged() = git diff --cached --name-only`, то есть ВЕСЬ индекс. При двух сессиях в одном индексе проверки №3 (ADR в `06` требует строки в `06_INDEX`), №4 (строка ушла из `07_STATE` требует `07_ARCHIVE`) и №5 (`updated_at`) проверяют объединение и дают ЛОЖНЫЙ ЗЕЛЁНЫЙ; проверки №1 и №2 дают ложный красный на чужом файле. Инстанс подтверждён: `ADR-078` из сессии `PARITY-REGISTRY` прошёл проверку №3 в паре с чужим `06_INDEX.md` под сообщением коммита про `Q-4`. Дефекта кода нет, снят инвариант «один коммит равен одной сессии», под который хук спроектирован. Класс `ADR-072 §3` (сломанная проверка хуже отсутствующей, отказ тихий). accepted.

§4 **ФАЗА 1, вступает в силу немедленно: буфер session-блоков.**
(i) Сессия НЕ правит `07_STATE.md`, `06_DECISIONS_LOG.md` и `06_INDEX.md`. Совсем, независимо от содержания задачи.
(ii) Сессия кладёт свой session-блок целиком одним НОВЫМ файлом `reference/_inbox/session_<TASK>_<YYYY-MM-DD>.md`. Имя уникально по построению, две сессии столкнуться не могут.
(iii) Свои артефакты и `reference/_scratch_<TASK>_<YYYY-MM-DD>/` (`ADR-076 §6`) сессия коммитит как раньше, поимённо.
(iv) Отдельный проход СБОРКА применяет накопленные блоки к `07_STATE`, `06_DECISIONS_LOG`, `06_INDEX` и `07_ARCHIVE` ОДНИМ коммитом, там же раздаёт номера ADR по порядку и удаляет разобранные файлы из `reference/_inbox/`. Сборка идёт в одиночной сессии, роль по образцу `_APPLIER.md` (роль ретайрена для сессий внутри инструмента `ADR-075 §6(i)`, здесь возвращается не как ручной транспорт, а как отдельный проход по файлам буфера).
(v) **Непустой `reference/_inbox/` есть стоп-условие старта новой сессии** (форма остановки как у `ADR-054`), не повод «разобрать потом». Обоснование: пропущенная сборка даёт протухший `07_STATE`, а протухший `07_STATE` есть худший из отказов этого метода.
(vi) Побочно закрывается `Q-65`: у раздела `SESSION_LOG` появляется физический адресат, которого в репо не существовало (шаблон `05` Часть III адресует его в `/docs` и `CHANGELOG`, ни того ни другого в репо нет). Вариант (c) реестровой строки принят в форме `reference/_inbox/`, не `/reference/session_log_<date>.md`. accepted (владелец).

§5 **ФАЗА 2: отдельное рабочее дерево на сессию. Замер ИСПОЛНЕН, форма назначается (в отличие от `ADR-077 §3`).**
(i) **Достижимость хука из связанного дерева подтверждена наблюдением** (`probe.log §G2`, `2026-07-28T18:56Z`): `git-dir` дерева собственный (`.git/worktrees/_holika_probe_hook`), `git-common-dir` общий, симлинк `pre-commit → ../../tools/hooks/pre-commit` разрешается, заведомо провальный коммит вернул `PRE-COMMIT FAIL: 07_STATE.md правится без обновления updated_at` и RC 1. Хук един на все деревья и работает. `ADR-074` в фазе 2 сохраняется без правок.
(ii) Версия инструмента на машине владельца `2.1.220`, штатный механизм worktree доступен. Одна сессия равна одному дереву равно одной ветке; git не даёт выложить одну ветку в два дерева, то есть инвариант обеспечивается инструментом, а не памятью исполнителя.
(iii) **`worktree.baseRef` обязан быть `"head"`, не значение по умолчанию `"fresh"`.** Значение по умолчанию ветвит дерево от ветки по умолчанию НА REMOTE; при `git push` на ask (`ADR-076 §1`) локальный `main` регулярно опережает `origin/main`, и сессия получила бы устаревший `07_STATE`. Замер `probe.log §G1b` вернул 0 коммитов расхождения и этого требования НЕ подтверждает: ноль получен потому, что владелец запушил непосредственно перед прогоном. Требование выведено из политики `ADR-076 §1`, а не из числа, и это названо, а не выдано за доказательство (`ADR-021 §2`).
(iv) `.claude/worktrees/` исключён у владельца через `.git/info/exclude` строка 11. Файл локальный, в репозиторий не коммитится, `.gitignore` в репо отсутствует (404 на SHA `0251c36`). Исключение промоутится в отслеживаемый `.gitignore` тем же коммитом, иначе оно не переживает переклонирование и частью метода не является. accepted.

§6 **`.DS_Store` вносится в `.gitignore` тем же коммитом.** Замер `probe.log §G1b` показал его постоянным неотслеживаемым файлом. `CLAUDE.md §Старт` пункт 1 требует чистого дерева, значит правило проваливается на каждом старте по причине, к работе отношения не имеющей, и перестаёт читаться. Это тот же механизм привыкания, что в `Q-75` и `ADR-077 §6`. accepted.

§7 **Вне scope.** Состав `.claude/settings.json` этим ADR не правится (`ADR-070 §1` в силе; правила `Bash(git add:*)`/`Bash(git commit:*)` в `allow` не трогаются, потому что §1 показал, что список команд здесь не работает по построению). `ADR-077` (перенос класса A на отдельный принципал) идёт своим треком и этим ADR не гейтится и не гейтит его. `ADR-076` (классы задач и очередь) не пересматривается. Форма адъюдикации мостов `Q-78`…`Q-82` не затрагивается.

**Последствия (blast radius):** `06_INDEX.md` — новая строка `ADR-080`; `07_STATE.md` — новая задача-строка `CC-SESSION-ISOLATION` в `§Открытые вопросы / GAP-реестр`, новая строка в таблице `§Мандат Claude Code: класс задач`, дополнение `§Текущий фокус`, header; `CLAUDE.md` — пункт 1 секции `§Старт каждой сессии`, секция `§Конец сессии`, новая секция `§Параллельные сессии (ADR-080)`; `05_CONVENTIONS.md` Часть III — новый блок `§Буфер session-блоков` после «Правил применения»; `.gitignore` — НОВЫЙ файл; `tools/hooks/pre-commit`, `.claude/settings.json`, `_METHOD.md`, `_APPLIER.md`, `04_ROADMAP.md`, `08_TASK_BRIEF_TEMPLATE.md` — БЕЗ правок. `Q-65` закрывается §4(vi) и переносится в `07_ARCHIVE.md` тем же коммитом.

## ADR-081 · Форма фазы 2 изоляции сессий: дерево заводится явной git-командой с точкой ветвления HEAD; требование настройки `worktree.baseRef` снимается; приёмка на уровне git исполнена контр-прогоном

**Дата:** 2026-07-29 · **Статус:** §1-§8 accepted (владелец, сессия CC-SESSION-ISOLATION-P2, выбор варианта (Б) из трёх)
**Контекст:** `ADR-080 §5` назначил фазой 2 отдельное рабочее дерево на сессию и потребовал установки `worktree.baseRef: "head"`. Разведка среды показала: настройка не задана нигде (проектный `.claude/settings.json` несёт только раздел прав, пользовательский `~/.claude/settings.json` — только тему), то есть действовало значение по умолчанию, то самое, которое `ADR-080 §5(iii)` называет ловушкой. Подкоманды `config` у инструмента нет (раздел `Commands` справки прочитан целиком), задать ключ можно только файлом настроек или через интерфейс приложения. Владелец запускает сессии из десктопного приложения.

**Решение:**

§1 **Форма фазы 2 назначается: дерево заводится явной командой git, не флагом инструмента.**
```
git worktree add .claude/worktrees/<TASK> -b s/<TASK> HEAD
```
Точка ветвления задана аргументом `HEAD` и умолчаний не оставляет. **`ADR-080 §5(iii)` superseded-in-part:** установка `worktree.baseRef: "head"` перестаёт быть обязательной, потому что вопрос, который она решала, при этой форме не возникает. Основания два. Первое: настройка, живущая только в файле настроек машины владельца, не переживает переклонирование и частью метода не является — тот же дефект, что был у исключения через `.git/info/exclude` (`ADR-080 §5(iv)`). Второе: документация инструмента прямо указывает git как путь для полного контроля над расположением дерева и выбором ветки; документация фактом не является (`ADR-021 §2`) и приводится как основание выбора, не как доказательство. accepted (владелец).

§2 **Размещение: `.claude/worktrees/<TASK>`.** Путь внесён в `.gitignore` коммитом `aa8fae6` (`ADR-080 §5(iv)`), совпадает с местом по умолчанию у инструмента, и замер подтвердил: при двух вложенных деревьях `git status` основного дерева пуст. Размещение вне репо отклонено: засоряет родительский каталог и требует помнить пути. accepted.

§3 **Приёмка на уровне git ИСПОЛНЕНА контр-прогоном** (`2026-07-28T20:34:54Z`, лог `/tmp/wt_accept.log`, в репо не вносится как разовый диагностический). Три факта: (i) два дерева заведены от `3baa491`, каждое на своей ветке, `git worktree list` показывает три записи; (ii) **изоляция индексов подтверждена** — `git add reference/_inbox/_probe_A.md` в дереве A дал строку в статусе A и ПУСТОЙ статус в дереве B и в основном дереве, то есть условие приёмки `ADR-080 §5` выполнено дословно; (iii) хук из связанного дерева срабатывает — заведомо провальный коммит вернул `PRE-COMMIT FAIL: 07_STATE.md правится без обновления updated_at` и RC 1, что независимо переподтверждает `ADR-080 §5(i)`. Уборка `git worktree remove --force` прошла без следов. accepted.

§4 **Что замером НЕ подтверждено, названо явно (`ADR-021 §2`).** Расхождение локального `main` и `origin/main` в прогоне снова равнялось нулю, как и в замере `ADR-080 §5(iii)`: ловушка ветвления не воспроизведена ни разу. Под формой §1 она невоспроизводима по построению, потому что точка ветвления есть аргумент команды, а не значение настройки. Это названо, а не выдано за доказательство корректности. accepted.

§5 **Остаток приёмки, скриптом не проверяемый.** Контр-прогон проверял git, не приложение. Открыто: откроется ли `.claude/worktrees/<TASK>` в десктопном приложении как обычный проект и останется ли сессия работать в ней. Закрывается первым реальным прогоном задачи в дереве. **До этого прогона параллельный запуск остаётся закрытым.** Отдельный `Q` не заводится: остаток принадлежит задача-строке `CC-SESSION-ISOLATION`. accepted.

§6 **Порядок сессии в дереве.** Старт: `git worktree add .claude/worktrees/<TASK> -b s/<TASK> HEAD`, открыть эту папку в приложении как проект, дальше обычный старт по `CLAUDE.md`. Конец: коммит своих артефактов поимённо в свою ветку, session-блок одним новым файлом в `reference/_inbox/` (`ADR-080 §4`), дерево остаётся до сборки. Проверка на старте `git worktree list` показывает забытые деревья прошлых сессий. accepted.

§7 **Слияние веток и уборка деревьев отдаются проходу сборки** (`_ASSEMBLER.md`). Сборка идёт в основном дереве, сливает ветки `s/<TASK>` в `main`, затем разбирает буфер и удаляет отработавшие деревья и ветки. Конфликтов при слиянии не ожидается по построению фазы 1: ветки не трогают `07_STATE`, `06_DECISIONS_LOG` и `06_INDEX`. Конфликт, если он всё же возникнет, есть громкий отказ и обрабатывается как конфликт блоков (`_ASSEMBLER.md §6`, остановка и вопрос). accepted.

§8 **Правки правил вне scope буфера, названо во избежание тихого расхождения.** Буфер и сборка покрывают только `07_STATE`, `06_DECISIONS_LOG`, `06_INDEX`, `07_ARCHIVE` (`ADR-080 §4(i)/(iv)`). Правки `CLAUDE.md`, `05_CONVENTIONS.md` и файлов ролей под сборку не попадают и коммитятся сессией самостоятельно, поимённо, как её артефакты (`ADR-080 §4(iii)`). При параллели в разных деревьях две правки одного файла правил дадут мердж-конфликт при сборке, то есть громкий отказ, а не тихое смешение. Отдельного механизма под это не вводится: случай редкий, а лекарство было бы дороже болезни. accepted.

**Последствия (blast radius):** `06_INDEX.md` — новая строка `ADR-081`; `07_STATE.md` — три замены по `STATE_PATCH` выше; `CLAUDE.md` — пункт про дерево в `§Старт каждой сессии`, дополнение `§Конец сессии`, полная замена секции `§Параллельные сессии`; `_ASSEMBLER.md` — новый раздел про слияние веток и уборку деревьев, дополнение `§0` и `§4`; `.gitignore`, `tools/hooks/pre-commit`, `.claude/settings.json`, `_METHOD.md`, `05_CONVENTIONS.md`, `04_ROADMAP.md` — БЕЗ правок. Правки `CLAUDE.md` и `_ASSEMBLER.md` идут ОТДЕЛЬНЫМ коммитом (§8), не коммитом сборки. **Эти правки в репозиторий на момент сборки не внесены** (проверено `git log`/поиском сборщиком) — остаются задолженностью следующей сессии/владельца, не молчаливым допущением.

## ADR-082 · Пусковые сценарии сессий: RUNBOOK как отдельный документ, признак параллелимости по классу И пересечению файлов, роли как агенты; из шаблона брифа убраны рудименты чат-запуска

**Дата:** 2026-07-29 · **Статус:** §1-§5 accepted (владелец, сессия LAUNCH-RUNBOOK, четыре развилки решены явно)
**Контекст:** владелец сформулировал конечную цель: проект должен запускаться в трёх режимах — чат-сессия для суждения, одна задача по брифу в Claude Code, несколько задач параллельно — и делать это без ручного прикладывания ссылок на роли при каждом старте. Аудит репо на SHA `551f016` показал: механика изоляции готова (`ADR-080`, `ADR-081`), классы мандата A/B описаны и сходятся в трёх местах, но порядок запуска не записан нигде, признака параллелимости не существует, а `08_TASK_BRIEF_TEMPLATE.md` несёт два рудимента запуска в обычном чате.

**Решение:**

§1 **Параллелимость определяется двумя признаками сразу, не одним.** Задача параллелится, если она класса A **и** её набор файлов на запись не пересекается ни с одной задачей, идущей одновременно. Класс A сам по себе недостаточен: две задачи класса A могут писать в один файл. **Класс B не параллелится никогда** — он и так требует задача-специфичного ADR на каждый запуск, то есть отдельного разбора. Признак ставится один раз при генерации брифа, записывается в поле «Параллель» брифа и в колонку «Параллель» таблицы `07_STATE §Мандат Claude Code`; расхождение между ними есть стоп-условие сессии, форма как у класса (`ADR-076`). **Незаполненное поле читается как «нет»** — умолчание в сторону запрета, по образцу «при сомнении класс B»: забытая ячейка не должна молча открывать параллель. Для проверки пересечения бриф несёт отдельное поле «Файлы на запись». accepted (владелец, вариант (в) из трёх).

§2 **Из `08_TASK_BRIEF_TEMPLATE.md` убираются два рудимента запуска в обычном чате.** Первый: требование «context-to-load всегда содержит литеральный raw-URL для каждого пункта». Бриф исполняется внутри Claude Code, файлы лежат на диске, `CLAUDE.md` подхватывается инструментом сам; raw-URL нужны только чату без доступа к диску, а брифы там не исполняются. Именно отсюда бралась ручная рутина владельца. Второй: «ты ПИШЕШЬ код/артефакты, человек ЗАПУСКАЕТ и возвращает логи, ты не исполняешь сам». Это прямо противоречит `.claude/agents/executor.md`, где выданы `Bash`, `Edit`, `Write`; сессия, прочитавшая бриф, не знала, кому верить, и это определяет, ждёт она логов от человека или работает сама. Секция «Роль» переписывается под самостоятельное исполнение с сохранением всех ограничений на не-идемпотентное (`ADR-076 §5`). Тем же основанием в `_GENERATOR.md` разводятся два случая: внутри Claude Code читать с диска, правило «только curl, только по SHA» относится к запуску в чате. accepted.

§3 **Роли получают агентов, но текстовый запуск остаётся основным.** Заводятся `.claude/agents/assembler.md` и `.claude/agents/generator.md` по образцу существующих `architect.md` и `executor.md`. **Выбор роли через механизм агентов на десктопном приложении наблюдением НЕ проверен** и здесь за факт не выдаётся (`ADR-021 §2`): агенты вводятся как ускорение, а основным способом остаются готовые тексты первого сообщения из `09_RUNBOOK.md`, которые работают независимо от механизма агентов. Проверка закрывается первым прогоном. accepted (владелец, вариант (а)).

§4 **Пусковые инструкции живут отдельным документом `09_RUNBOOK.md`, статус LIVING.** Он адресован ВЛАДЕЛЬЦУ, а не сессии: отвечает на вопрос «что нажать и что написать». Размещение в `CLAUDE.md` отклонено — тот читается сессией после старта, а инструкция нужна до него. Размещение внутри `08_TASK_BRIEF_TEMPLATE.md` отклонено — шаблон описывает одну задачу, а режимов пять. Документ несёт: таблицу выбора режима, по каждому режиму команды до запуска и готовый текст первого сообщения без плейсхолдеров в shell-командах, шпаргалку и раздел разбора отказов. accepted (владелец, вариант (а)).

§5 **Выход чат-сессии архитектора фиксируется правилом, а не остаётся разовым решением.** Архитектор выдаёт session-блок, владелец кладёт его одним новым файлом в `reference/_inbox/` и коммитит, применяет проход сборки (`ADR-080 §4`). Правки правил (`CLAUDE.md`, `05_CONVENTIONS.md`, файлы ролей, `09_RUNBOOK.md`) под буфер не подпадают и идут отдельным коммитом (`ADR-081 §8`). Обоснование фиксации: маршрут выхода чат-сессии был открытым вопросом с начала работ и трижды поднимался заново; незаписанное решение возвращается вопросом. accepted (владелец).

**Последствия (blast radius):** `06_INDEX.md` — новая строка `ADR-082`; `07_STATE.md` — четыре замены по `STATE_PATCH` (шапка, колонка «Параллель» в таблице мандата с заполнением всех строк, врезка над таблицей, новая задача-строка `LAUNCH-RUNBOOK`); `09_RUNBOOK.md` — НОВЫЙ файл; `.claude/agents/assembler.md` и `.claude/agents/generator.md` — НОВЫЕ файлы; `08_TASK_BRIEF_TEMPLATE.md` — четыре правки; `_GENERATOR.md` — одна правка. БЕЗ правок: `CLAUDE.md`, `05_CONVENTIONS.md`, `_METHOD.md`, `_ASSEMBLER.md`, `_ARCHITECT.md`, `.claude/settings.json`, `tools/hooks/pre-commit`, `.gitignore`, `04_ROADMAP.md`. Новые файлы и правки `08`/`_GENERATOR` идут ОТДЕЛЬНЫМ коммитом (`ADR-081 §8`), не коммитом сборки. **Эти правки в репозиторий на момент сборки не внесены** (подтверждено владельцем — подготовлены локально, коммит следует отдельно тем же днём), остаются задолженностью следующего коммита владельца, не молчаливым допущением.

## ADR-083 · Семь процессных дефектов конвейера сессий: параллелимость есть свойство ПАРЫ и проверяется механически; сплошной поиск понятия при правке по якорям; маркер времени прихода в блоке последствий; проверка буфера положительным счётчиком; деревья переносятся в видимый каталог; своё дерево для любой пишущей сессии; имя `<TASK>-GEN` для режима генерации

**Дата:** 2026-07-29 · **Статус:** §1–§8 accepted. §1–§4, §6, §7 — архитектор; §5 (перенос деревьев)
и §8 (архитектор внутри инструмента) — accepted ВЛАДЕЛЬЦЕМ 2026-07-29 в ответ на развилки, выданные
этой же сессией.

**Контекст:** первый реальный прогон конвейера по `09_RUNBOOK.md` (2026-07-29, генерация брифа
`SOURCE-MAP-SALES`) вскрыл семь дефектов правил, ни один из которых не является дефектом кода или
ошибкой исполнителя. Пять найдены сессиями (генератор и сборка) независимо друг от друга, два —
владельцем на запуске. Общий знаменатель шести из семи: правило, которое держится на памяти
исполнителя в момент запуска, а не на механике — ровно тот класс, который `ADR-077 §1` и
`ADR-080 §1` уже отклоняли дважды, каждый раз по факту, а не по аналогии.

**Решение:**

§1 **Параллелимость: булев признак СОХРАНЯЕТСЯ как ДОПУСК, пару решает механическая проверка.**
Возражение принято по существу: «параллелится» есть свойство ПАРЫ задач, и признак на одну задачу
его не выражает. Но из этого не следует, что признак на задачу бесполезен: он выражает НЕОБХОДИМОЕ
условие («класс A и полный набор файлов на запись объявлен»), которое известно на момент генерации
брифа, тогда как ДОСТАТОЧНОЕ условие (непересечение наборов) известно только на момент запуска,
когда пара выбрана. Поэтому условие разрезается по времени, а не сводится к одному месту:
(i) колонка «Параллель» в `07_STATE §Мандат Claude Code` и поле «Параллель» брифа остаются, но их
смысл назначается явно — это ДОПУСК, а не разрешение; незаполненное читается как `нет` (`ADR-082 §1`
в этой части без изменений);
(ii) поле «Файлы на запись» получает ОБЯЗАТЕЛЬНУЮ машиночитаемую форму: заголовок ровно
`**Файлы на запись**`, дальше маркированный список, один путь на строку, путь первым токеном в
обратных кавычках, каталог со слэшем на конце. Основание — замер: форма реального брифа
`briefs/SOURCE-MAP-SALES.md` извлекается, а инлайновая форма, предписанная сейчас `08` строкой 28,
не извлекается и даёт ОТКАЗ (RC 3), не «пересечения нет»;
(iii) вводится `tools/parallel_check.sh` (новый файл). Он читает поля «Файлы на запись» ВЫБРАННЫХ
брифов, печатает извлечённые наборы целиком (`ADR-044`: совпавшие строки, не метка) и пересечения;
пересечение = RC 1 = запускать вместе нельзя. Пересечением считается и вложенность (каталог-предок
против файла внутри него) — проверено замером;
(iv) прогон проверки — ОБЯЗАТЕЛЬНЫЙ шаг режима П в `09_RUNBOOK`, ДО первой команды
`git worktree add`, а не рекомендация;
(v) **отсутствие поля, непарсимая форма или пустой набор есть ОТКАЗ проверки, а не разрешение**
(`ADR-021 §2`, `05` Часть I ★): единственный способ, которым такая проверка может убить сама себя, —
тихо вернуть пустое множество и быть прочитанной как зелёный.
**Отклонено (a): оставить булев признак плюс ручную сверку списков.** Это и есть текущее состояние;
оно уже дало ровно один правильный результат ровно потому, что генератор-сессия случайно держала оба
брифа в контексте. Правило, срабатывающее при удачном стечении контекста, механикой не является.
**Отклонено (b): заменить колонку списком файлов на запись.** Список известен только после генерации
брифа, а строка таблицы заводится раньше; и один список в двух местах (таблица и бриф) даёт дрейф,
который пришлось бы ловить ещё одним стоп-условием — цена выше пользы.
**Оговорка в шапке `briefs/SOURCE-MAP-SALES.md` строки 10-17 не переписывается:** она есть первая,
ручная инстанция этой проверки и остаётся как провенанс. Следующие брифы несут только поле.

§2 **Строка 86 `08_TASK_BRIEF_TEMPLATE.md` исправляется; правка по якорям обязана сопровождаться
сплошным поиском понятия.** Дефект признан дефектом автора патча, не решения: `ADR-082 §2` отменил
требование литеральных raw-URL, был применён по четырём перечисленным якорям, и файл целиком на то
же понятие не проверялся. **Сессия нашла ВТОРОЙ инстанс того же дефекта, ранее не названный:**
`_GENERATOR.md` строка 27 (заголовок «Прочитай из репо (raw-URL, корень репо, с подставленным SHA)»
стоит на уровне `##` и потому относится к обоим случаям запуска, а не только к чату) и строка 57
(«выдай самодостаточный бриф: роль, raw-URL контекста»). Тем самым дефект перестаёт быть единичной
опечаткой и получает правило:
**ADR, отменяющий или переопределяющий ПОНЯТИЕ (а не двигающий значение), применяется не только по
перечисленным якорям: исполнитель обязан прогнать сплошной поиск понятия по репо, напечатать
совпавшие строки с номерами (`ADR-044`) и внести в блок последствий решение по КАЖДОМУ совпадению —
«правится» либо «остаётся, потому что …». Пустая выдача поиска фактом не является.**
Правило вносится в `05` Часть I, раздел «Дисциплина изменений документации».
**Отклонено: починить только строку 86.** Второй инстанс уже существовал и был найден не правилом,
а попутно, при чтении на другую задачу; починка одного вхождения оставила бы механизм нетронутым.
Правило проверено на самом себе: сплошной поиск в этой сессии дал по понятию «raw-URL» девятнадцать
совпадений, из них правятся два файла, остальные остаются сознательно — `_ARCHITECT.md`,
`_APPLIER.md`, `_START_HERE.md`, `_METHOD §10` описывают запуск в обычном чате, где правило доступа
по SHA в силе (кроме `_ARCHITECT.md`, см. §8), а `briefs/*` и `06_DECISIONS_LOG.md` историчны и
ретро-правке не подлежат.

§3 **Каждый пункт блока «Последствия» несёт МАРКЕР ВРЕМЕНИ ПРИХОДА.** Четыре маркера, из них
сборщик обязан проверять ровно один:
- `[сейчас]` — приходит ТЕМ ЖЕ коммитом сборки, применяет сборщик. **Единственный вид, наличие
  которого сборщик обязан подтвердить в репо;** расхождение здесь остаётся стоп-условием.
- `[отдельным коммитом]` — правки правил вне буфера (`ADR-081 §8`): `CLAUDE.md`,
  `05_CONVENTIONS.md`, файлы ролей, `09_RUNBOOK.md`, `08_TASK_BRIEF_TEMPLATE.md`, `.claude/`,
  `tools/`, `.gitignore`. Сборщик их НЕ ищет в репо и на их отсутствии НЕ останавливается; он
  перечисляет их в теле своего коммита как названную задолженность.
- `[задачей <ID>]` — приходит отдельной задачей позже. Сборщик не проверяет наличие правки, но
  обязан убедиться, что задача существует строкой в `07_STATE`; её отсутствие есть гэп.
- `[без правок]` — файл назван как заведомо незатронутый (форма уже употребляется, теперь
  легализована наравне с остальными).
Основание — две остановки сборки на ИСПРАВНЫХ решениях: `ADR-081` и `ADR-082` написали в
последствиях «идут отдельным коммитом», сборщик прочитал это как утверждение о свершившемся факте,
файлов в репо не нашёл и встал. Остановки были правильными по правилу и ложными по существу;
неправильна формулировка, а не поведение сборщика. Прямое подтверждение того, что дефект был именно
во времени, а не в содержании: обе правки пришли в репо через один коммит после своей сборки
(`551f016` после `904226e`, `79905d2` после `04650aa`), то есть решения были верны, а сборка о
них не могла знать. Устаревшие примечания в `06_INDEX.md` («на момент сборки НЕ внесены») снимаются
тем же коммитом.
**Правка ограничена `05` Часть III; `_METHOD §8` НЕ правится** — доменно-нейтральный канон,
blast radius за пределами holika, owner-gated по прецеденту `ADR-021 §4`. Расхождение названо и
зарегистрировано `Q-84`, а не оставлено незамеченным.
**Отклонено: разбить блок последствий на три именованные подсекции.** Пункт часто один, а
подсекций всегда три — форма тяжелее содержания; маркер в начале строки читается и человеком, и
поиском.

§4 **Проверка буфера заменяется на `tools/session_status.sh`** (новый файл). Он печатает четыре
факта старта ПОЛОЖИТЕЛЬНЫМИ счётчиками плюс совпавшие строки и возвращает RC 1 при «не чисто».
Критерий однозначен и утвердителен: `файлов кроме .gitkeep: 0`.
Основание — факт, замеренный этой сессией: вывод `ls` при каталоге, где лежит только `.gitkeep`, и
при каталоге ПУСТОМ (`.gitkeep` потерян) побайтово одинаков. То есть голый `ls` не различает
«буфер разобран» и «каталог сломан», а именно второе состояние ведёт к исчезновению каталога из
git (`_ASSEMBLER §2`). Скрипт различает: отсутствие каталога есть громкий отказ (RC 2), отсутствие
`.gitkeep` при существующем каталоге печатается предупреждением.
Скрипт знает, где он запущен: в СВЯЗАННОМ дереве чужие деревья и ветки `s/*` нормальны и
стоп-условием не являются (`ADR-081 §6`), проверяется только буфер. Без этого правило
«непустой буфер = стоп» ломало бы старт каждой сессии в своём дереве. Проверено замером в трёх
режимах.
**Отклонено (a): `ls -A`.** Показывает `.gitkeep`, но критерий «чисто» остаётся ОТРИЦАТЕЛЬНЫМ
(отсутствие вывода), а отсутствие вывода в этом проекте фактом не является (`05` Часть I ★).
**Отклонено (b): инлайновый `find` в четырёх местах** (`CLAUDE.md`, `09_RUNBOOK` дважды,
`_ASSEMBLER` дважды). Один и тот же текст в пяти местах даёт ровно дефект §2 при первой же правке.
**Цена названа, а не умолчана:** `bash tools/…` правилами `.claude/settings.json` не покрыт
(`ADR-070 §1`), поэтому запуск, вероятно, потребует подтверждения там, где четыре git-команды его
не требовали. Это НЕ замерено и здесь за факт не выдаётся; различитель тот же, что у
`ADR-077 §3 (D1)`, отдельного замера не заводится. Одно подтверждение на старт вместо четырёх
команд — приемлемый обмен, и он же снимается переходом на отдельный принципал.

§5 **Деревья переносятся в ВИДИМЫЙ каталог: `worktrees/<TASK>` вместо `.claude/worktrees/<TASK>`;
`ADR-081 §2` superseded-in-part. Развилка вынесена владельцу и решена им 2026-07-29 в пользу
переноса.** `.gitignore` получает строку `/worktrees/`; прежняя строка `**/.claude/worktrees/`
остаётся (безвредна, покрывает старые деревья при переходе).
Основание: каталог `.claude` скрыт в Finder, в диалоге выбора папки десктопного приложения не
виден, и на первом же прогоне владелец пошёл обходным путём — открыл основную папку и переключил
ветку селектором, получив `fatal: 's/SOURCE-MAP-SALES' is already checked out at …`. Отказ
правильный, но цена — потерянное время НА КАЖДОМ запуске, а не однократно.
Довод, на котором `ADR-081 §2` выбирал `.claude/worktrees/` («совпадает с местом по умолчанию у
инструмента»), отпал вместе с самим механизмом: `ADR-081 §1` постановил заводить дерево явной
git-командой, и место по умолчанию перестало иметь значение. Остальные доводы §2 (путь в
`.gitignore`, статус основного дерева чист, родительский каталог не засоряется) выполняются
видимым каталогом внутри репо ровно так же.
**Отклонено (a): записать в `09_RUNBOOK` способ открытия (`Cmd+Shift+G` с полным путём либо
`Cmd+Shift+.`).** Это лечение памятью, повторяемое на каждом запуске, при том что альтернатива
стоит однократной правки семи мест. Подсказка про `Cmd+Shift+G` при этом В РЮНБУК ВНОСИТСЯ — как
запасной путь и для старых деревьев, но не как решение.
**Отклонено (b): вынести деревья за пределы репо.** Отклонено по тем же основаниям, что в
`ADR-081 §2`: засоряет родительский каталог и требует помнить пути.

§6 **Единое правило: ЛЮБАЯ сессия, пишущая в репо, идёт в своём дереве и своей ветке. Единственное
исключение — режим С (сборка), которому дерево запрещено структурно** (`ADR-081 §7`: она сливает
ветки в `main`, из связанного дерева этого не сделать). Режим Г переводится в дерево; запись
`09_RUNBOOK` строка 73 «основное дерево, дерево под это заводить не нужно, сессия короткая и пишет
один файл» отменяется.
Основание: фактический прогон 2026-07-29 шёл в отдельном дереве и прошёл штатно, то есть цена
правила нулевая. Обоснование прежней записи не выдерживает проверки по двум пунктам: сессия пишет
не один файл, а два (`briefs/<TASK>.md` и файл буфера), и «короткая» свойством безопасности не
является — инцидент 2026-07-28 занял меньше двух минут по меткам транскриптов (`ADR-080 §1`).
Побочно: правило снимает из головы владельца таблицу исключений — исключение остаётся одно и оно
структурное.
**Названный факт, не выдаваемый за проблему:** агент `generator` (`.claude/agents/generator.md`,
строка 4) несёт `tools: Read, Grep, Glob, Write` — без `Bash`, то есть сам ни дерево не заведёт, ни
коммит не сделает. Дерево заводит владелец командой до запуска (как в режиме И), коммит делает
владелец либо текстовый вариант запуска. Расширение прав агента настоящим ADR НЕ производится:
`Bash` у роли, которая по определению только читает и пишет один файл, — отдельное решение.
**Отклонено: оставить режим Г в основном дереве.** Отклонено фактом: пока режим Г идёт в основном
дереве, любая одновременная сессия воспроизводит инцидент 2026-07-28 полностью, а «мы же не
запускаем их одновременно» есть дисциплина, отклонённая `ADR-080 §1`.

§7 **Имя дерева и ветки режима Г: `<TASK>-GEN`.** Столкновение снимается, новых префиксов не
заводится, маска `s/*` в `_ASSEMBLER §4 шаг 0` покрывает `s/<TASK>-GEN` без единой правки сборщика.
Основание: конвенция уже существует В РЕПО де-факто — файл буфера генератор-сессии называется
`reference/_inbox/session_SOURCE-MAP-SALES-GEN_2026-07-29.md` (коммиты `2006bca`, `0531e15`,
`c8c4689`), то есть task-ID генерации уже несёт суффикс `-GEN`, и расходились с ним только имя
дерева и имя ветки. Решение приводит три имени к одному, а не вводит четвёртое.
**Отклонено (a): назвать столкновение ожидаемым поведением в рюнбуке.** Отказ действительно
громкий и ущерба не несёт, но случается ПОСЛЕ того, как владелец открыл приложение и выбрал папку,
то есть платится временем на каждой такой паре — при том что переименование бесплатно.
**Отклонено (b): отдельный префикс ветки (`g/<TASK>`).** Ломает маску `s/*`: сборщик перестал бы
находить ветки генерации и убрал бы буфер, не слив их. Отказ был бы ТИХИМ — худший класс в этом
проекте (`ADR-072 §3`, `ADR-080 §2`).

§8 **Архитектор внутри Claude Code нигде не описан** (пункт найден сверх задания, accepted владельцем).
`_ARCHITECT.md §3` предписывает «только curl, только по SHA» без оговорок, а `09_RUNBOOK §0`/`§1`
знают архитектора только как обычный чат. Настоящая сессия шла архитектором ВНУТРИ Claude Code с
чтением с диска по прямому указанию владельца (`ADR-082 §2`), то есть практика уже разошлась с
документом — тот же дефект, что `ADR-082 §2` устранил для генератора, но для роли архитектора он
не устранялась, потому что тогда такого запуска не было. Развод двух случаев в `_ARCHITECT.md §3` по
образцу `_GENERATOR.md` и строка режима в таблице `09_RUNBOOK §0`. accepted (владелец, 2026-07-29).

**Последствия (blast radius; каждый пункт с маркером времени прихода по §3):**

- `[сейчас]` `06_DECISIONS_LOG.md` — append `ADR-083` (этот текст).
- `[сейчас]` `06_INDEX.md` — новая строка `ADR-083`; замена примечаний в строках `ADR-081` и
  `ADR-082` (устаревшая пометка «на момент сборки НЕ внесены», см. §3).
- `[сейчас]` `07_STATE.md` — шапка (`updated_at`, `обновил`); врезка над таблицей мандата; новая
  строка таблицы мандата `PROCESS-DEBT (применение патчей)`; дописки к задача-строкам
  `CC-SESSION-ISOLATION` и `LAUNCH-RUNBOOK`; новая строка `Q-84`.
- `[отдельным коммитом]` `05_CONVENTIONS.md`, `08_TASK_BRIEF_TEMPLATE.md`, `_GENERATOR.md`,
  `CLAUDE.md`, `09_RUNBOOK.md`, `_ASSEMBLER.md`, `.claude/agents/assembler.md`, `.gitignore`,
  `tools/session_status.sh` (новый), `tools/parallel_check.sh` (новый), `_ARCHITECT.md`.
- `[задачей SOURCE-MAP-REST]` первая проверка `tools/parallel_check.sh` на ДВУХ реальных брифах.
- `[без правок]` `_METHOD.md` (см. §3 и `Q-84`); `tools/hooks/pre-commit`, `tools/hooks/selftest.sh`;
  `.claude/settings.json`, `00_CHARTER.md`, `01`–`04`, `07_ARCHIVE.md`, `11_INFRA_FACTS.md`,
  `.claude/agents/{architect,executor,generator}.md`, `briefs/SOURCE-MAP-SALES.md`.

## ADR-084 · Форма блока «Текущий фокус» в 07_STATE: стенд-ап с заменой против дописывания

**Контекст.** Раздел «Текущий фокус» в `07_STATE.md` @ `ef1c1877` вырос в один абзац на 5462 знака,
внутри которого ТРИ разных объявления «Текущий фокус:» подряд, причём второе (`SOURCE-MAP-REST`)
противоречит статусу той же задачи в реестре ниже в том же файле, а третье описывает шаг, который
сессией не исполнялся и задачей не назначен. Механическая причина одна: формат `STATE_PATCH`
(`05_CONVENTIONS` Часть III стр.210, зеркально `_METHOD §8`) задаёт одну строку
«Текущий фокус: <что дальше>», но нигде не сказано, что она ЗАМЕНЯЕТ прежнюю, а `_ASSEMBLER.md`
Шаг 4 стр.125 говорит только «`STATE_PATCH` → правки в `07_STATE.md`». Сборка буфера 2026-07-30
разбирала два блока за проход (`SOURCE-MAP-SALES` плюс `SOURCE-MAP-REST`), каждый со своей строкой
фокуса, обе дописались к уже лежавшему тексту. Дополнительно: пересказ закрытого `Q-4` занимал первую
четверть блока при том, что полный текст закрытия уже лежит в `07_ARCHIVE.md`, то есть правило
компактности `ADR-064` нарушено дважды.

**Решение.**

§1 Раздел «Текущий фокус» разделяется на два подраздела с разной семантикой записи. `### Стенд-ап`
ЗАМЕНЯЕТСЯ целиком каждой сборкой и несёт ровно пять строк: прошлый шаг, где мы, следующий шаг,
развилки на владельце, счётчик. `### Подробности для модели` дописывается, длина не ограничена.
Дописывание в стенд-ап есть дефект применения, а не стилистика.

§2 Формат `STATE_PATCH` в `05_CONVENTIONS` Часть III получает два раздельных поля вместо одной строки
фокуса: «Стенд-ап» (замена, пять подстрок) и «Подробности для модели» (append). Причина прежнего
дефекта именно в том, что одно поле выполняло обе работы.

§3 При нескольких блоках в одной сборке стенд-ап берётся из последнего по порядку шага 2
`_ASSEMBLER.md`; стенд-апы остальных блоков отбрасываются, содержательный остаток уходит в детали.
Объявление «Текущий фокус:» внутри деталей запрещено.

§4 Строка «Счётчик» обязательна и несёт только измеримое с названным знаменателем (пары реестра
паритета, снятые карты, фазы Epic M). Единый процент по проекту не назначается: `04_ROADMAP` детально
расписывает только текущий эпик, поэтому взвешенный процент был бы импровизацией (`_METHOD §6`).

§5 Любая названная работа получает идентификатор в момент постановки, а не в момент запуска: без
имени невозможны ветка `s/<TASK>`, дерево `worktrees/<TASK>`, бриф `briefs/<TASK>.md` и папка логов.
Формулировка «следующий шаг» без имени задачи есть незакрытая постановка.

§6 Финишная формулировка Epic-1 уточняется: пара реестра считается сошедшейся после трёх шагов
(правило моста адъюдицировано, численная сверка проведена, расхождение устранено либо закрыто ADR-ом),
а не после одной сверки. `ADR-067` в этой части расширяется, не отменяется.

**Последствия.**

- [сейчас] `07_STATE.md`: раздел «Текущий фокус» заменён на `### Стенд-ап` плюс
  `### Подробности для модели`; пересказ `Q-4` снят как дублирующий `07_ARCHIVE.md` строку `Q-4`
  (проверено чтением архива); пересказ `CODE-REPO-STANDUP` снят как дублирующий живую строку того же
  ID в реестре ниже; шапка обновлена.
- [сейчас] `05_CONVENTIONS.md` Часть III стр.210: одна строка фокуса заменена двумя полями (§2).
- [сейчас] `_ASSEMBLER.md` Шаг 4 стр.125: внесено правило замены и правило нескольких блоков (§1, §3).
- [сейчас] `07_STATE.md` реестр открытых вопросов: заведена строка `Q-85` (расхождение с каноном
  `_METHOD §8`).
- [сейчас] `07_STATE.md` реестр открытых вопросов: заведена строка `STATE-COMPACT`, без неё маркер
  `[задачей STATE-COMPACT]` ниже непроверяем (`_ASSEMBLER` Шаг 3a).
- [сейчас] `06_INDEX.md`: строка ADR-084.
- [задачей STATE-COMPACT] Уборка реестра: полностью закрытые строки уходят в `07_ARCHIVE.md` дословно
  по `ADR-064`.
- [без правок] `_METHOD.md §8`: промоушен правила замены в доменно-нейтральный канон НЕ производится.
  Основание то же, что у `Q-40` и `Q-84`: blast radius за пределами holika, owner-gated. Расхождение
  с каноном названо сознательно и держится строкой `Q-85`.
- [без правок] `04_ROADMAP.md` Epic 1: §6 уточняет приёмку пары, но раздел держит только заголовок и
  scope, строчной правки не требует.

**Провенанс посадки.** Текст введён сессией `STATE-STANDUP` 2026-07-30, последствия `[сейчас]`
исполнены коммитом `e2e5eeb3` (загрузка через веб-интерфейс GitHub), сам текст решения в
`06_DECISIONS_LOG.md` не попал и посажен сборкой `ADR-LANDING-0730` того же дня. Расхождение
названо, а не заглажено: между 12:38 и посадкой репо ссылался на несуществующее решение.

**Статус:** §1-§5 accepted (владелец, вариант A, 2026-07-30), §6 accepted (архитектор).

## ADR-085 · Адъюдикация review-очереди 2026-07-29-1 и техдолг Custom Query msklad_counterparty_returns

**Контекст.** Очередь `reference/architect_review_queue_2026-07-29-1.md` (положила сессия
`SOURCE-MAP-SALES`) несла три пункта без исполнения: объект паритета для пары «Инвестор»/«Операционка»,
трактовка зоны `moment` в `cf-facts`, `byvariant` без `× rate.value`. Одновременно владелец
предоставил текст Custom Query `msklad_counterparty_returns`, снимка которого в репо не существовало
(`Q-79`), и запросил разбор его как техдолга с декомпозицией на задачи.

**Решение.**

§1 Объект паритета для продаж: объектов два, оба валовые и оба с оракулом — `revenue_kgs` против
`report/profit/*` и возвраты против документов возврата МойСклада. Нетто объявляется ПРОИЗВОДНОЙ
величиной, правило моста фиксирует формулу «нетто = валовая минус возвраты того же периода и того же
зерна», сходимость нетто наступает автоматически при сходимости двух объектов. Вариант «нетто из марта
как объект паритета» отклонён: нетто определено в трёх местах по-разному (`sq_marts_sales_overview.sql`,
Custom Query `msklad_counterparty_returns`, `02_ERP_CONTRACTS §схемы core`), и паритет привязывался бы
к неоднозначной величине. Вариант «считать возвраты в `core.fact_sales_profit` правкой `cf-facts`»
отклонён: правка живого CF плюс двойное вычитание у двух потребителей, которые уже вычитают сами.

§2 Паре возвратов выдаётся оракул-кандидат: документы возврата МойСклада. Эндпоинт
`report/profit/bysalesreturn` возвращает `404` (факт, зафиксирован строкой 20 реестра паритета),
поэтому уровень эталона выбирается по результату `REPORT-FIELDS` на `entity/salesreturn`.

§3 Восьмой пары не заводится: пара «Возвраты покупателей» уже стоит строкой 20 реестра и уже входит в
семь пар. Настоящий ADR даёт существующей паре оракула-кандидата (§2) и правило-мост (§1), знаменатель
счётчика остаётся 7. Прежняя формулировка «реестр получает восьмую пару, знаменатель 7 → 8» была
ошибкой архитектора, выправлена до применения (роль-исполнитель поймала дублирование строк на приёмке
патча).

§4 Колонка `core.fact_sales_profit.return_sum_kgs` всегда `0.0` (хардкод `cf-facts`, `bq_ops.py`),
фактические возвраты живут в `core.fact_returns`; `revenue_kgs` тождественна `sell_sum_kgs`, то есть
описание «Нетто выручка KGS» в `02_ERP_CONTRACTS §схемы core` было неверным (`Q-83`).

§5 Scope задачи `REPORT-FIELDS` расширяется тремя шагами: съём имён полей по списку эндпоинтов
`ADR-079 §9`, различитель зоны `moment` (§6), ключи и валюта первого элемента `report/profit/byvariant`
(§7). Мандат класса B, выданный поимённо `ADR-079 §9`, распространяется на расширенный scope;
оговорки `ADR-076 §5`, форма логирования `ADR-079 §6` и промежуточная мера `ADR-077 §6` сохраняются.

§6 Зона `moment` в `cf-facts`: DEFER отклонён. Довод DEFER имел силу, пока построчных сверок не было
ни одной; следующая фаза это шесть сверок подряд, и неразличённая зона отравит каждую. Различитель
дешёвый (сверить `moment` одного известного документа с его локальным временем в интерфейсе
МойСклада) и идёт шагом внутри `REPORT-FIELDS`, который всё равно обращается к тому же API с тем же
токеном.

§7 `byvariant` без `× rate.value`: принятие по семантике отклонено. Довод «базовая валюта отчётных
эндпоинтов» правдоподобен, но это ровно тот способ рассуждения, который в `ADR-016` уже стоил проекту
45 суток невидимого дефекта, а `margin_kgs`/`margin_pct` подтверждённо стоят на клиентской странице.
Факт снимается шагом внутри `REPORT-FIELDS` (ключи и валюта первого элемента), отдельная задача не
заводится.

§8 Декомпозиция техдолга Custom Query на задачи с именами: `LS-QUERY-SNAPSHOT` (снимок текста в
`reference/sql/` плюс инвентарь запросов, класс A), `SOURCE-MAP-INVOICES` (четвёртая ветка карты
происхождения, кто грузит `core.fact_customer_invoices`, через `INFORMATION_SCHEMA.JOBS_BY_PROJECT`,
класс A; это и есть задача под `Q-82`, у которой не было имени), `LS-COUNTERPARTY-RETURNS-FIX`
(правка живого запроса, класс B), `PARITY-SCD2-ATTRIBUTION` (атрибуция контрагента и страны на дату
сделки против текущего среза `scd2_is_current`, сначала решение, затем класс B). Порядок:
`LS-QUERY-SNAPSHOT` и `SOURCE-MAP-INVOICES` первыми и допускают параллель, затем `REPORT-FIELDS`,
затем `LS-COUNTERPARTY-RETURNS-FIX`, затем `PARITY-SCD2-ATTRIBUTION`. Довод порядка: снимок нужен
раньше правки, иначе нечем доказать, что правка применилась; `REPORT-FIELDS` раньше правки запроса,
иначе приёмка правки проверяет запрос сам против себя.

§9 Инвентарь Custom Query в интерфейсе Looker Studio инструменту недоступен и снимается владельцем.
`LS-COUNTERPARTY-RETURNS-FIX` инструменту не отдаётся: живая клиентская поверхность и денежные
величины, исполняет владелец в интерфейсе, read-back обязателен.

§10 Приёмка `LS-COUNTERPARTY-RETURNS-FIX`: возвраты берутся в границах периода дашборда и в том же
зерне, что строка результата; сумма возвратов по странице совпадает с прямым запросом к
`core.fact_returns` за тот же период; поле `transaction_date` переименовано по смыслу (счётчик дней);
`counterparty_count` перестаёт быть константой.

**Последствия.**

- [сейчас] `02_ERP_CONTRACTS.md` строки 47-49: описания `sell_sum_kgs`/`return_sum_kgs`/`revenue_kgs`
  заменены (§1, §4). ИСПОЛНЕНО коммитом `e2e5eeb3`.
- [сейчас] `reference/parity_registry.md`: колонка «Правило-мост» строк 18-19 заменена, строка 20
  заменена ЦЕЛИКОМ по месту, вставки новой строки не производится, знаменатель остаётся 7 (§1, §3).
  ИСПОЛНЕНО коммитом `af354ca5`.
- [сейчас] `07_STATE.md`: четыре строки задач (§8) плюс строки таблицы мандата. ИСПОЛНЕНО коммитом
  `e2e5eeb3`.
- [сейчас] `06_INDEX.md`: строка ADR-085. НЕ ИСПОЛНЕНО на 12:38, вносится сборкой
  `ADR-LANDING-0730`.
- [задачей REPORT-FIELDS] Три шага расширенного scope (§5, §6, §7).
- [задачей LS-COUNTERPARTY-RETURNS-FIX] Правка живого Custom Query по приёмке §10.
- [без правок] `04_ROADMAP.md`: задачи §8 живут строками в `07_STATE`, раздел Epic 1 держит только
  заголовок и scope.

**Провенанс посадки.** Решение введено сессией `STATE-STANDUP` 2026-07-30. Текст пересобран сборкой
`ADR-LANDING-0730` из выдачи той же сессии: содержание всех параграфов присутствует в выдаче
дословно, номера §1-§5 и §8-§10 сохранены, потому что на них уже ссылаются `07_STATE.md`,
`02_ERP_CONTRACTS.md` и `reference/parity_registry.md`; номера §6 и §7 в исходной выдаче не
проставлены и восстановлены при посадке, раскладка подтверждена владельцем 2026-07-30.

**Статус:** §1-§5 и §8-§10 accepted (владелец 2026-07-30); §6/§7 accepted, нумерация восстановлена
при посадке.

## ADR-086 · Определение задачи есть свойство репо, а не разговора: исполнение без брифа, корректирующая запись по 48e48e4c, синхронизация базы сессии

**Контекст.** 2026-07-30 запущены две параллельные сессии генерации брифов, `LS-QUERY-SNAPSHOT-GEN` и
`SOURCE-MAP-INVOICES-GEN`, обе от базы `ef1c1877`. Обе выдали бриф на одну и ту же работу под двумя
именами. Установлено фактом:

(i) Имени `LS-QUERY-SNAPSHOT` в репо не существовало ни в одном файле до коммита `e2e5eeb3` (12:38
UTC), которым загружено `07_STATE.md` с определением задачи по `ADR-085 §8` (снимок Custom Query
Looker Studio). Локальный клон этого коммита не имел, `grep -rn "LS-QUERY-SNAPSHOT"` по локальному
репо дал 0 совпадений (зафиксировано в session-блоке сессии `LS-QUERY-SNAPSHOT-GEN`, коммит
`1d2d06f8`).

(ii) Генератор поступил по правилам: объявил `CONTEXT GAP` и задал два вопроса владельцу. Определение
задачи он получил из разговора, потому что в репо его не было. Ответ определил задачу как discovery
по `Q-82`, то есть как работу задачи `SOURCE-MAP-INVOICES`.

(iii) Файл `briefs/LS-QUERY-SNAPSHOT.md` из коммита `1d2d06f8` нёс 0 упоминаний `reference/sql`,
`Looker`, `Custom Query`, «снимок» и «инвентар» против 11 упоминаний `JOBS_BY_PROJECT`, 6 упоминаний
`fact_customer_invoices` и 7 упоминаний `Q-82`; артефактом объявлен
`reference/customer_invoices_load_jobs_2026-07-30.md`.

(iv) Правки сборки `130f92bb` в `07_STATE.md` (строка таблицы мандата и строка задачи) отброшены при
слиянии `443376c9` с `origin/main` без записи. Тихое примирение расхождения `07_STATE` относится к
классу, запрещённому `ADR-072 §3` и `ADR-080 §2`.

**Решение.**

§1 Задача может исполняться БЕЗ брифа при одновременном выполнении четырёх условий: класс A; приёмка
записана дословно строкой задачи в `07_STATE.md`; полный набор файлов на запись перечислен в тексте
запуска; определение задачи в тексте запуска процитировано из строки `07_STATE.md` дословно, а не
пересказано. Любое невыполненное условие возвращает требование брифа. Мотив: на работу из двух файлов,
половина которой ручная, бриф стоит дороже задачи, но пересказ определения своими словами есть ровно
тот механизм, который дал инцидент (iii).

§2 Корректирующая запись по коммиту `48e48e4c`. Действие (удаление `briefs/LS-QUERY-SNAPSHOT.md`)
оставлено в силе: файл с именем одной задачи и телом другой есть ловушка для следующей сессии, а
живой `briefs/SOURCE-MAP-INVOICES.md` покрывает ту же работу полнее (395 строк против 183, несёт шаг
границы по `_loaded_at` и перекрёстную сверку с инвентарём Cloud Functions). Обоснование в теле
коммита признаётся НЕВЕРНЫМ в двух утверждениях: «Origin переопределил имя задачи» (переопределения
не было, имя введено один раз и сразу в нынешнем смысле, см. (i)) и «устаревший» (файл не устаревал,
он не соответствовал своему имени с момента создания). Из ложной формулировки следовало, что процесс
работал и обогнал файл; фактическая картина обратная, процесс выдал сессии неверное определение
задачи. Историю коммита не переписываем, обоснование исправляется настоящим параграфом.

§3 Определение задачи не берётся из разговора. Отсутствие строки задачи в `07_STATE.md`
есть жёсткая остановка сессии, а не гэп, закрываемый ответом владельца в чате. Подтверждение
владельца закрывает КЛАСС задачи и допуск параллели, но не её определение: определение вносится в
`07_STATE.md` отдельным проходом до запуска.

§4 Пятая проверка старта: отставание локальной базы от `origin/main`.
`tools/session_status.sh` получает `git fetch` и счётчик `git rev-list --count HEAD..origin/main`,
ненулевое значение означает «не чисто» наравне с непустым буфером. `CLAUDE.md §Старт` п.1 и
`_ASSEMBLER.md §3` получают то же требование. Сборщику запрещено самостоятельно разрешать расхождение
с `origin`, если оно задевает `07_STATE.md`, `06_DECISIONS_LOG.md` или `06_INDEX.md`: это остановка и
вопрос владельцу того же класса, что конфликт блоков (`_ASSEMBLER §6`). Назначается задачей
`PIPE-BASE-SYNC`.

§5 Выход архитектор-сессии не доставляется в репо через веб-интерфейс GitHub. Веб-загрузка
обходит хук, не касается локального клона и позволяет донести последствия решения без самого решения;
`ADR-084` и `ADR-085` есть доказательство: последствия легли коммитами `e2e5eeb3`/`af354ca5`, тексты
не легли, и четыре документа семь часов ссылались на несуществующие решения.

**Последствия.**

- [сейчас] `06_DECISIONS_LOG.md`: тексты `ADR-084` и `ADR-085` посажены (П-1, П-2).
- [сейчас] `06_INDEX.md`: три строки, `ADR-084`, `ADR-085`, `ADR-086` (П-4).
- [сейчас] `07_STATE.md` строка 22: «в реестре 8 пар» заменено на «7 пар», согласовано владельцем
  2026-07-30 и не попало в загрузку `e2e5eeb3` (П-6).
- [сейчас] `07_STATE.md` строка 156: строка задачи `LS-QUERY-SNAPSHOT` приведена к факту, брифа нет,
  задача исполняется по §1 (П-7).
- [сейчас] `07_STATE.md` реестр: заведены `Q-86` (списание сессии `LS-QUERY-SNAPSHOT-GEN`), `Q-87`
  (`SELECT *` в Custom Query), `PIPE-BASE-SYNC`, `LS-RETURNS-FX-HARDCODE`, `LS-PERIOD-CONTRACT` (П-8).
- [сейчас] `briefs/SOURCE-MAP-INVOICES.md` блок «Основание»: снят приказ вносить строку мандата,
  которая уже стоит в таблице с коммита `e2e5eeb3`; SHA контекста перепиннен (П-9).
- [задачей PIPE-BASE-SYNC] Пятая проверка старта и правила §4 в `tools/session_status.sh`,
  `CLAUDE.md`, `_ASSEMBLER.md`.
- [без правок] Коммит `48e48e4c` и его тело: история не переписывается (§2).

**Статус:** §1-§5 accepted (владелец 2026-07-30; §1 вариант b, §2 вариант a, §3-§5 апрув того же
дня).

## ADR-087 · Контракт Custom Query Looker Studio: период из параметров дашборда, состав колонок объявляется явно

**Контекст.** Инвентарь `reference/ls_custom_queries_2026-07-30.md` (задача `LS-QUERY-SNAPSHOT`,
2026-07-30) показал фактом: параметры периода дашборда `@DS_START_DATE`/`@DS_END_DATE` используют два
Custom Query из пяти. `msklad_expenses` полностью; `msklad_counterparty_returns` только в части
продаж, возвраты идут скользящим окном 365 дней; `fact_returns` идёт скользящим окном 90 дней;
`msklad_customer_invoices_ar` без периода вовсе; `msklad_inventory_latest` по последнему
`date_snapshot`. Дефект был разобран как частный случай одного запроса (`ADR-085 §8`, приёмка `§10`),
фактически он общий: дата-контрол дашборда управляет не всей страницей, и зритель не может отличить,
какая цифра ему подчиняется, а какая нет. Владелец выбрал форму закрытия: одно правило на все Custom
Query с явным списком законных исключений, вместо пяти отдельных правок.

**Решение.**

§1 По умолчанию период Custom Query берётся из параметров дашборда `@DS_START_DATE`/`@DS_END_DATE`.
Это относится ко ВСЕМ временным фильтрам запроса, включая фильтры внутри подзапросов: смешение
(продажи по параметрам, возвраты своим окном) есть дефект, а не компромисс, потому что производная
величина считается по двум разным периодам.

§2 Единственное законное исключение это класс «снимок на момент»: величина по своей природе периода
не имеет (остатки на складе, задолженность на сейчас). Исключение объявляется в ДВУХ местах: строкой
в инвентаре `reference/ls_custom_queries_*.md` и подписью на самой странице дашборда. Второе
обязательно: без подписи исключение неотличимо от дефекта для того, кто смотрит цифру.

§3 Скользящее окно, заданное литералом внутри запроса (`INTERVAL 365 DAY`, `INTERVAL 90 DAY`),
запрещено. Это третье поведение периода помимо §1 и §2, и оно худшее из трёх: цифра меняется каждый
день при неизменном периоде дашборда, то есть невоспроизводима и не сверяема с МойСкладом
(`ADR-067`). Существующие вхождения перечислены в §5 и §6.

§4 Каждый Custom Query несёт в инвентаре ОБЪЯВЛЕННОЕ поведение периода (по параметрам / снимок на
момент). Расхождение объявленного с текстом запроса есть стоп-условие того же класса, что расхождение
класса задачи между брифом и `07_STATE` (`ADR-076`): не повод выбрать удобное, а повод остановиться.

§5 Правка `fact_returns` сворачивается в задачу `LS-RETURNS-FX-HARDCODE`: это один и тот же запрос,
и курс с периодом чинятся одной правкой в одном интерфейсе. Отдельной задачи под период этого
запроса не заводится, дробление дало бы два прохода и два read-back по одному объекту.

§6 Классификация `fact_returns`: переводится под параметры дашборда (§1), исключение §2 к нему не
применяется. Основание фактом (владелец, 2026-07-30): запрос стоит на странице «Executive summary»,
на этой странице есть дата-контрол в режиме автоматического диапазона. Значит зритель может менять
период, а блок возвратов на это не отвечает, потому что окно вписано литералом. Сумма возвратов не
относится к классу «величина без периода» (§2), в отличие от остатков и задолженности.

§7 Custom Query не использует `SELECT *`: состав колонок объявляется списком. Мотив тот же, что у
периода: состав клиентской поверхности задаётся решением, а не схемой таблицы, и новая колонка марта
не должна появляться на дашборде молча. Форма закрытия существующего вхождения
(`msklad_inventory_latest`): в список вносятся ВСЕ колонки таблицы на момент правки, не только
используемые графиками. Довод: аудит «какие колонки используются» требует обхода каждого графика,
даёт хрупкий результат (добавили график, сломался запрос) и на замере 2026-07-30 уже дал два
неразрешимых соответствия («Бренд» и «Запас в днях (сглаженный)» имеют в схеме по нескольку
кандидатов). Полный список закрывает дрейф, ничего не ломает и снимает вопрос соответствия целиком.
Список берётся с живой схемы в момент правки, а не из снимка `reference/schema_dump_*`: снимок может
отстать.

**Последствия.**

- [сейчас] `06_DECISIONS_LOG.md`: текст `ADR-087` (этот блок).
- [сейчас] `06_INDEX.md`: строка `ADR-087` (П-12).
- [сейчас] `07_STATE.md`: строки `LS-PERIOD-CONTRACT` и `LS-RETURNS-FX-HARDCODE` переписаны
  (П-13, П-14).
- [задачей LS-RETURNS-FX-HARDCODE] Правка `fact_returns`: литерал курса и литерал окна одной правкой
  (§3, §5).
- [задачей LS-PERIOD-CONTRACT] Объявление исключений §2 для `msklad_customer_invoices_ar`
  (страница «Операционка») и `msklad_inventory_latest` (страница «Склад»): строки в инвентаре плюс
  подписи на страницах.
- [задачей LS-INVENTORY-EXPLICIT-COLUMNS] Замена `SELECT *` явным списком колонок в
  `msklad_inventory_latest` (§7).
- [задачей LS-COUNTERPARTY-RETURNS-FIX] Период возвратов приводится к §1; приёмка `ADR-085 §10`
  остаётся в силе целиком, настоящий ADR её не заменяет.
- [без правок] `Q-87` закрывается настоящим ADR (§7), отдельного решения не требует.
- [без правок] `05_CONVENTIONS.md`, `tools/*`: правило доменное, а не процессное, механической
  проверке в репо не поддаётся (текст запросов живёт в Looker Studio, репо держит снимок).

**Статус:** §1-§7 accepted (владелец 2026-07-30, форма «одно правило с явными исключениями»).

| ADR-088 | Время документа МойСклад: API отдаёт UTC, интерфейс рендерит в зоне аккаунта; правило моста называет правило суток по каждой сверяемой таблице; выбор эталонных суток гейтится замером, а не задаётся вопросом

Контекст: `Q-77` заведён `E1-T1-MECH-CUTOVER` (2026-07-27) как две неразличённые гипотезы — «настоящий UTC» против «местного времени без пересчёта». Карта происхождения продаж (`reference/source_map_sales_2026-07-29.md §5`) вскрыла, что внутри одного архива `cf-facts` ветки трактуют зону противоположно: `bq_ops.py:158` конвертирует, полагая `moment` временем в UTC (фактический сдвиг +6ч), а `fetch_returns.py:115-122`/`fetch_purchases.py:41-50` не конвертируют, полагая `moment` уже местным временем; карта отказалась выбирать правдоподобную версию и назначила различителем сверку с интерфейсом. Сессия `REPORT-FIELDS` (2026-07-31) сверку исполнила через владельца: документ `entity/salesreturn` №00008 (`id = 0d9650b7-7549-11f1-0a80-19c400103fb5`), API `2026-07-01 15:33:00.000`, интерфейс `01.07.2026 18:33`, сдвиг +3 часа — не подтвердилась ни одна из двух гипотез. Второй факт владельца (2026-07-31, чат): часовой пояс аккаунта МойСклад = Москва. Два факта вместе гипотезы различают.

Решение: §1 Причина установлена, `Q-77` закрывается фактом. Интерфейс рендерит время в зоне аккаунта (Москва, UTC+3), показанное `18:33` есть московское, следовательно возвращённое API `15:33` есть UTC. Гипотеза «API отдаёт московское время» отвергнута. `Q-77` закрывается: его вопрос (какая зона) имеет ответ; его последствие (состояние слоя загрузки) переходит в задачу `INGEST-MOMENT-ZONE-FIX` и отдельной `Q`-строкой не дублируется (`ADR-051 §2`). Строка `Q-77` переносится в `07_ARCHIVE.md` дословно тем же коммитом (`ADR-064`). accepted (архитектор). §2 Эталонные часы паритета есть часы интерфейса МойСклад. Основание: `ADR-068 §3` определяет мост как правило, по которому клиент воспроизводит нашу цифру руками со своей страницы; процедура воспроизведения и есть контракт, а клиент видит интерфейс, не ответ API. Существенное уточнение, вскрытое настоящим ADR: эти часы — настройка аккаунта, а не константа. Их изменение меняет эталон паритета ретроспективно и есть решение владельца, а не техническая деталь (`§7`). accepted (архитектор). §3 Мост обязан называть правило суток каждой сверяемой таблицы явно, как функцию от UTC-времени документа. Окно запроса к API выводится из этого правила, а не назначается по виду периода. На текущей фактуре: интерфейс и ручные выгрузки из него = `DATE(M+3ч)`; `core.fact_sales_profit` = `DATE(M+6ч)`; `core.fact_returns`, `core.fact_purchases`, `core.fact_loss`, `core.fact_commissionreportin` = `DATE(M)`. Расширение `ADR-079 §2` (мост описывает core-слой): правило суток входит в обязательный состав описания моста наравне с полем-источником, правилом конвертации в KGS, фильтром периода и зерном строки. accepted (архитектор). §4 Дефект локализован и сужен. Ветка продаж НЕ дефектна: `DATE(PARSE_TIMESTAMP(...), 'Asia/Bishkek')` при UTC на входе даёт истинные бишкекские сутки, `02_ERP_CONTRACTS.md:41` подтверждён фактом и правки не требует. Дефектны четыре ветки, берущие дату UTC и объявляющие её местной: возвраты и закупки в `cf-facts` (`moment_str[:10]` при комментарии «МойСклад отдаёт в UTC+6»), списания и комиссии в `cf-finance` (приведение к дате по UTC, исходная формулировка `Q-77`). Дефект не зависит от `§7`: по UTC неверно при любом выборе эталонных суток. Заводится `INGEST-MOMENT-ZONE-FIX` (класс B: правка и деплой Cloud Functions, дополнительно гейтится `ADR-065`); мандат настоящим ADR НЕ выдаётся, целевое правило суток гейтится `§7`. Про `core.fact_payments.moment` (приходит уже DATE) правило суток не установлено и не предполагается — при постановке задачи фикса устанавливается отдельно. accepted (архитектор). §5 Экспозиция названа количественно и входит в приёмку первой сверки. Полосы, на которых календари расходятся: интерфейс против продаж — `M ∈ [18:00; 21:00)`, по Бишкеку ночь 00:00–03:00; интерфейс против возвратов и закупок — `M ∈ [21:00; 24:00)`; продажи против возвратов — `M ∈ [18:00; 24:00)`, по Бишкеку 00:00–06:00. Задача `PARITY-CHECK-SALES-RETURNS` обязана посчитать документы этих полос ОТДЕЛЬНО и ДО общего вердикта: иначе ненулевая разница неатрибутируема и повторяется класс ошибки, наблюдавшийся на расходах до `ADR-006` (разрыв ~33 %, `ADR-005 §Контекст`). accepted (архитектор). §6 Замер ночных полос — первый шаг задачи `PARITY-CHECK-SALES-RETURNS`, до содержательных замеров. Считаются документы мая-2026 в полосах `§5` по каждому типу (продажи, возвраты). Основание приоритета: от результата зависит, имеет ли развилка `§7` цену вообще; вопрос, снимаемый замером, владельцу не задаётся (`ADR-056`). accepted (архитектор). §7 Выбор эталонных суток (`Q-92`) — owner-gated, решается ПОСЛЕ `§6`, не раньше. Варианты и их цена, зафиксированы заранее, чтобы решение принималось не в спешке: (a) московские сутки интерфейса как есть — паритет в строгом смысле достижим, клиент воспроизводит цифру руками, но бишкекский бизнес отчитывается по московским суткам, а `core.fact_sales_profit` придётся пересчитать и перезалить, поскольку он хранит бишкекские; (b) переключить часовой пояс аккаунта на Бишкек — один календарь во всём контуре, ветка продаж уже корректна, кода на большой таблице не меняется; цена: отображение истории в интерфейсе меняется, ручная выгрузка `reference/pnl_2026-05.md` сделана в московском отображении и её период смещается, единственная сошедшаяся пара «Расходы» (`0,00`) требует перепроверки; (c) объявленное отступление `ADR-068 §2` — ничего не меняется ни в МойСкладе, ни в коде продаж, сверка идёт по окну, выведенному из наших суток; цена: уровень паритета (b) по `ADR-068 §1` ослабляется — клиент не воспроизведёт наш месячный итог, просто выбрав «май» у себя, и отступление живёт постоянно на дашборде. Гэп внутри варианта (b), из репо не снимается: меняет ли переключение пояса отображение УЖЕ существующих документов или только новых. Не предполагается ни в какую сторону (`_METHOD §6`). Различитель назначен и обратим: переключить пояс, открыть тот же документ №00008 (при ретроактивности покажет `21:33`), при нежелательном результате вернуть настройку. Исполняет владелец в интерфейсе МойСклад, инструменту недоступно ни в каком классе. Исполнять различитель ДО `§6` не рекомендуется: при пустой ночной полосе вариант (b) может не понадобиться. proposed (решение владельца). §8 `Q-93` — зона границ запроса. В какой зоне МойСклад интерпретирует `momentFrom`/`momentTo` и включает ли верхнюю границу — не проверено. Предположение «в той же зоне, в которой отдаётся `moment`» правдоподобно и фактом не является (`ADR-021 §2`). Различитель: запрос узким окном вокруг документа с известным `M`, исполняется первым шагом задачи `PARITY-CHECK-SALES-RETURNS` вместе с `§6`. accepted (архитектор). §9 `ADR-085 §1` уточняется, не отменяется. Нетто-выручка определена там как валовая минус возвраты «того же периода и того же зерна»; сутки продаж и сутки возвратов в ядре сейчас расходятся на 6 часов (`§3`), поэтому автоматическая сходимость нетто наступает только после `INGEST-MOMENT-ZONE-FIX` либо при сверке обеих пар по одним и тем же часам (`§3`). Объект паритета (две валовые величины) остаётся прежним. `06` append-only, текст `ADR-085` не редактируется. accepted (архитектор). §10 Обобщение измерения ограничено явно. Замерен один документ одного типа, от 2026-07-01. Постоянство сдвига вне июля отдельно не проверялось; отсутствие перехода на летнее время в обеих участвующих зонах есть внешнее знание, а не факт репо, поэтому второй замер на документе мая-2026 входит в `§6` и отдельной работы не требует. accepted (архитектор).

Последствия:
   * [сейчас] `06_INDEX.md` — строка настоящего ADR
   * [сейчас] `07_STATE.md` шапка (`updated_at`, `обновил`), блок `### Стенд-ап` (замена целиком), `§Подробности для модели` (append)
   * [сейчас] `07_STATE.md` `§Открытые вопросы / GAP-реестр` — строка `Q-77` CLOSED и переносится в `07_ARCHIVE.md` дословно ЭТИМ ЖЕ коммитом (`ADR-064`); новые строки `Q-92`, `Q-93`, `INGEST-MOMENT-ZONE-FIX`, `PARITY-CHECK-SALES-RETURNS`
   * [сейчас] `07_STATE.md` таблица `§Мандат Claude Code: класс задач` — новые строки `INGEST-MOMENT-ZONE-FIX` и `PARITY-CHECK-SALES-RETURNS` (обе класс B, параллель нет, мандат НЕ выдан). Вносятся этой же сборкой во избежание дефекта, зафиксированного `Q-89`
   * [сейчас] `07_ARCHIVE.md` — дословный перенос строки `Q-77` с пометкой «закрыт фактом: часовой пояс аккаунта Москва ⇒ API отдаёт UTC; последствие адресовано задачей `INGEST-MOMENT-ZONE-FIX`»
   * [задачей PARITY-CHECK-SALES-RETURNS] `reference/parity_registry.md` — правило суток по `§3` вносится в ячейки пар «Продажи» и «Возвраты покупателей» вместе с результатом сверки; настоящим ADR реестр не правится (`ADR-079 §7`)
   * [задачей INGEST-MOMENT-ZONE-FIX] `02_ERP_CONTRACTS.md:69` (`order_date`) и `02_ERP_CONTRACTS.md:94` (`return_date`) — зона не названа ни в одной строке (проверено `grep`); правило суток вносится вместе с фиксом, не раньше
   * [без правок] `02_ERP_CONTRACTS.md:41` — «Дата транзакции в Asia/Bishkek» подтверждено фактом (`§4`)
   * [без правок] `ADR-085` — уточнение внесено `§9` настоящего ADR, текст решения не редактируется (`06` append-only)
   * [без правок] `03_PIPELINE_SPEC.md`, `09_GLOSSARY.md`, `11_INFRA_FACTS.md` — упоминаний зоны времени документа не содержат (проверено `grep`)

Статус: §1-§6, §8-§10 accepted (архитектор). §7 proposed (решение владельца, гейтится замером §6). Мандат класса B на `INGEST-MOMENT-ZONE-FIX` настоящим ADR НЕ выдаётся.

---

## ADR-089: Атрибуция контрагента в SCD2-справочнике матчит оракул: текущий вид, не снимок на дату сделки

Контекст: `PARITY-SCD2-ATTRIBUTION` (`ADR-085 §8`, 2026-07-30) — второе из двух решений-предусловий
построчных сверок реестра паритета. Custom Query `msklad_counterparty_returns` (и любая денормализация
`core.dim_counterparties` — universal-правило `02_ERP_CONTRACTS.md:409`, `AND scd2_is_current = TRUE`)
присваивает историческим продажам сегодняшнее имя и страну контрагента, тогда как SCD2-версионирование
справочника (`scd2_valid_from/to`) существует ровно для обратного — чтобы уметь восстановить состояние
на дату. Бьёт по разрезу «страны», названному Треком A (`reference/client_concerns_2026-07.md`).

Решение:
§1 Факт о модели данных МойСклада: `country` — «кастомное поле» (`02_ERP_CONTRACTS.md:154`) сущности
контрагента, не поле документа продажи/возврата — схемы `core.fact_sales`/`core.fact_returns`
(`02_ERP_CONTRACTS.md:18-22`, `94-98`) несут только `agent_id` (FK), колонки `country` там нет.
МойСклад структурно не хранит атрибуты контрагента ПО ДОКУМЕНТУ — только по сущности контрагента,
одним текущим значением. accepted (архитектор).

§2 Наше собственное SCD2 на `dim_counterparties` — не отражение версионирования внутри МойСклада, а
артефакт нашего пайплайна: `cf-dim` детектирует изменение при ПОСЛЕДУЮЩЕМ суточном прогоне и создаёт
вторую запись (`01_ARCHITECTURE.md:392`, RB-22). МойСклад сам никогда не предъявляет клиенту «страну
контрагента на дату сделки» — этой величины не существует нигде в его собственной модели данных,
включая интерфейс и report-API. accepted (архитектор).

§3 Отсюда: разрез «страна»/«имя контрагента» оракула (`ADR-067` — то, что рисует интерфейс МойСклада)
для ЛЮБОГО периода, включая исторический, есть текущее состояние сущности контрагента — оракулу
нечем показать иное. Правило-мост «снимок на дату сделки» дало бы число, НЕ воспроизводимое клиентом
никаким запросом к МойСкладу — то есть нарушило бы главный принцип (`00_CHARTER §главный принцип`)
сильнее, чем решает. Правило-мост фиксируется как (b): «текущий вид контрагента» — осознанное
свойство поверхности, обязательное условие паритета «по цифрам» с оракулом, не примирение половин.
accepted (архитектор).

§4 Действующий JOIN `AND scd2_is_current = TRUE` (`02_ERP_CONTRACTS.md:157/409`) признаётся
КОРРЕКТНЫМ по существу, правки кода/запросов не требуется. `PARITY-SCD2-ATTRIBUTION` закрывается этим
решением, класс B не открывается. accepted (архитектор).

§5 Остаточное ограничение решения, названное явно: аргумент §1-§3 структурный (custom-поле живёт на
сущности, документ несёт только FK), не прямое наблюдение отображения МойСклада. Отдельный, более
узкий случай — замороженные реквизиты в печатных формах документов (если МойСклад такое практикует
для каких-то полей) — этим решением не покрывается и не отменяется; различитель под него не заводится
проактивно, потому что цена ошибки для АНАЛИТИЧЕСКОГО разреза «страна»/«имя» уже определена §3, и
никакое наблюдение печатной формы эту цену не меняет для этого разреза. accepted (архитектор).

Последствия:
[сейчас] `02_ERP_CONTRACTS.md` строка 157 — замена (текст выше), STABLE-док, правка идёт через
настоящий ADR.
[сейчас] `07_STATE.md` — GAP-строка `PARITY-SCD2-ATTRIBUTION` переносится в `07_ARCHIVE.md` дословно
с закрывающей припиской, удаляется строка таблицы «Мандат Claude Code: класс задач» для той же
задачи, обновляется стенд-ап (тексты — блок STATE_PATCH выше).
[сейчас] `06_INDEX.md` — строка ADR-089, вносится сборкой.
[без правок] `reference/parity_registry.md` — строка 20 («Возвраты покупателей») адресует период/
зерно (`LS-COUNTERPARTY-RETURNS-FIX`), не атрибуцию контрагента; настоящий ADR её не трогает.

Статус: accepted (архитектор).

## ADR-090 · Парность GAP-реестра и таблицы мандата; ревизия таблицы 2026-08-01; `Q-89` CLOSED

**Дата:** 2026-08-01 · **Статус:** §1–§7 accepted (владелец, чат 2026-08-01; сессия MANDATE-TABLE-AUDIT)

**Контекст:** `Q-89` (`07_STATE.md:179`) зафиксировал, что таблица `§Мандат` не обновляется при заводке задача-строки в GAP-реестре. Построчная ревизия 2026-08-01 показала, что дефект двусторонний и уже сработал шесть раз: пять задач без строки мандата (`LS-RETURNS-FX-HARDCODE`, `LS-PERIOD-CONTRACT`, `LS-INVENTORY-EXPLICIT-COLUMNS`, остаток `LAUNCH-RUNBOOK`, шаги 2+ `CODE-REPO-STANDUP`), одна строка мандата без задачи (`PROCESS-DEBT`, заведена пунктом последствий `ADR-083`), плюс две строки закрытых задач (`Q-4`, `REPORT-FIELDS`), которые разовая уборка `STATE-COMPACT` не поймала по построению: `Q-4` архивирован до той сессии, `REPORT-FIELDS` после. Провенанс построчно — `reference/mandate_table_audit_2026-08-01.md §1-§2`.

**Решение:**

§1 **Парность.** Задача-строка в GAP-реестре `07_STATE` и строка в таблице `§Мандат Claude Code: класс задач` — парные. Заводится задача-строка ⇒ ТЕМ ЖЕ коммитом заводится строка мандата. Задача-строка уезжает в `07_ARCHIVE.md` ⇒ ТЕМ ЖЕ коммитом убирается строка мандата. Ответственность на проходе сборки (`_ASSEMBLER.md`), потому что именно он применяет обе правки. Форма прецедентна: та же парность механизирована для `06_DECISIONS_LOG` ↔ `06_INDEX` проверкой №3 хука. accepted (владелец).

§2 **Исключение одно.** Строки, где вместо класса стоит «не отдаётся» или «—», описывают не мандат, а его отсутствие; они живут, пока существует задача-строка. accepted.

§3 **Уточнение колонки «Параллель» для задач без брифа.** Врезка над таблицей читает `да` как «класс A И полный набор файлов на запись объявлен в брифе». Для задач, исполняемых без брифа по `ADR-086 §1`, набор объявляется в тексте запуска — это то же самое требование, поданное другим носителем, а не его ослабление. Прогон `tools/parallel_check.sh` перед парным запуском обязателен без изменений. accepted.

§4 **Таблица мандата заменяется целиком** на 27 строк (текст применён этой сборкой). Убраны `Q-4` и `REPORT-FIELDS` (задачи закрыты, строки в архиве). accepted.

§5 **`PROCESS-DEBT`** заводится задача-строкой задним числом со статусом DONE и ТЕМ ЖЕ коммитом переносится в `07_ARCHIVE.md` по `ADR-064`, с явно названным остатком: пункт `[задачей SOURCE-MAP-REST]` из `ADR-083` (первая проверка `tools/parallel_check.sh` на двух реальных брифах) провенанса не имеет — `grep -rl parallel_check reference/` дал 0 совпадений, что фактом «не запускалось» не является (`ADR-021 §2`). Остаток адресуется в текст архивной строки, отдельный `Q` не заводится (`ADR-051 §2`). accepted.

§6 **`Q-89` CLOSED** настоящим решением: правило синхронизации назначено, документ назван. accepted.

§7 **Вне scope:** механизация парности отдельной проверкой в `tools/` (отдельное решение, цена и форма не оценивались); распространение таблицы мандата на `07_STATE §Статусы задач (Epic M)` и `(Epic 1)` — их определения живут в `04_ROADMAP`, это отдельное решение; промоушен `Q-39` из DEFER (триггер наступил, но форма фикса owner-gated).

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — append настоящего ADR; `06_INDEX.md` — строка ADR-090.
[сейчас] `07_STATE.md` — шапка (`updated_at`, `обновил`); блок `### Стенд-ап` заменяется целиком; врезка над таблицей мандата получает предложение §3; таблица мандата заменяется целиком (§4); строка `Q-89` CLOSED и уезжает в архив; строка `PROCESS-DEBT` заводится и уезжает в архив (§5); новая строка `INFRA-FACTS-SWEEP`; новая строка `Q-94`.
[сейчас] `07_ARCHIVE.md` — дословный перенос строк `Q-89` и `PROCESS-DEBT`.
[задачей INFRA-FACTS-SWEEP] закрытие `Q-6`, `Q-11`, `Q-12`, `Q-13`, `Q-14`, `Q-47`, `Q-55`.
[без правок] `_METHOD.md`, `00_CHARTER.md`, `01`–`04`, `08`, `09`, `11_INFRA_FACTS.md`, `tools/`, `.claude/settings.json`.

## ADR-091 · Разрез задач по линии исполнимости действия; текст SQL-запроса Looker Studio не есть интерфейс; новая задача `INFRA-FACTS-SWEEP`

**Дата:** 2026-08-01 · **Статус:** §1–§10 accepted (владелец, чат 2026-08-01; сессия MANDATE-TABLE-AUDIT)

**Контекст:** часть строк таблицы мандата помечена «инструменту не отдаётся» целиком, хотя недоступен инструменту только последний шаг задачи. Прямой случай — четыре задачи Looker Studio: `ADR-085 §9` закрывает инструменту ИНТЕРФЕЙС, и это верно, но текст запроса пишется в `reference/sql/`, то есть является записью в `/reference` — классом A по букве `ADR-076 §1`. При действующей формулировке владелец пишет SQL руками и без провенанса, а инструмент простаивает.

**Решение:**

§1 **Принцип.** Задача классифицируется по линии «какое действие исполнитель физически может совершить», а не по теме. Основания класса B в `ADR-076 §1` перечислены как разные пункты и неравноценны: живой read-only `GET` с секретом инструменту доступен (прецедент `REPORT-FIELDS` отработан целиком с провенансом); деплой CF доступен технически, но есть настоящая B; правка в интерфейсе Looker Studio или МойСклад недоступна физически ни в каком классе. accepted (владелец).

§2 **Текст SQL-запроса Custom Query интерфейсом НЕ является.** Его подготовка — класс A. `ADR-085 §9` этим НЕ отменяется и не сужается: инвентарь и правка в интерфейсе по-прежнему снимаются и исполняются владельцем. accepted.

§3 **Четыре задачи Looker Studio разрезаются** (`LS-COUNTERPARTY-RETURNS-FIX`, `LS-RETURNS-FX-HARDCODE`, `LS-PERIOD-CONTRACT`, `LS-INVENTORY-EXPLICIT-COLUMNS`): подготовка текста либо строки инвентаря — класс A; вставка в интерфейс и read-back — владелец. **Read-back остаётся частью приёмки каждой:** подготовленный текст без read-back закрытием задачи не является. Приёмки задач (`ADR-085 §10`, `ADR-087 §5/§6/§7`) не переписываются. accepted.

§4 **`CODE-REPO-STANDUP` шаг 2 разрезается:** 2a (сбор снимка, `.gitignore`, локальный коммит на диске Cloud Shell) — класс A; 2b (создание удалённого репозитория и `push`) — владелец, исходящее действие вовне, `git push` остаётся ask. Запуск 2a гейтится выбором формы seed владельцем (снимок с диска/zip либо `git init` заново); история коммитов отсутствует физически — шаг 1 установил 0 коммитов, — поэтому «взять историю» вариантом не является. accepted.

§5 **`CC-READONLY-PRINCIPAL` шаги 2-3 разрезаются:** подготовка патча `.claude/settings.json` и скрипта выдачи ролей — класс A; исполнение обоих — владелец. Основание держать исполнение за владельцем НЕ есть класс операции, а совпадение ролей: это единственная задача проекта, где исполнитель и бенефициар правки совпадают — инструмент правил бы собственный список прав. accepted.

§6 **`ORACLE-AUTOMATION` разрезается:** написание скрипта посева — класс A, доступно сейчас; первый живой прогон с секретом — класс B, мандат НЕ выдан, выдаётся поимённо по прецеденту `ADR-079 §9`. Форма эталона по-прежнему определяется реестром (`ADR-067 §5c`). accepted.

§7 **`INGEST-MOMENT-ZONE-FIX`:** подготовка патча в снапшоте `reference/code/` — класс A, но ТОЛЬКО после закрытия `Q-92` (целевое правило суток до него неизвестно, готовить нечего). Деплой — класс B, гейт `ADR-065`, без изменений. accepted.

§8 **`PARITY-REGISTRY (мост соответствий)` переформулируется:** «не отдаётся сессии-исполнителю; адъюдицируется архитекторской сессией, в том числе внутри Claude Code (`ADR-083 §8`)». Прежняя формулировка написана до того, как архитектор стал запускаться внутри инструмента, и читалась как «инструменту нельзя вообще» — фактически неверно. `ADR-076 §4` по существу не меняется: адъюдикация как была решением архитектора, так и остаётся. accepted.

§9 **`E1-T2-MECH-TAX` переформулируется:** блокирует не класс, а отсутствующий вход владельца (число начисления и каденс); после его получения staging-часть становится классом A. accepted.

§10 **Новая задача `INFRA-FACTS-SWEEP`, класс A:** хвост мелких read-only фактов одной сессией — `Q-6`, `Q-12`, `Q-13` (три `gcloud … describe`), `Q-14`, `Q-47` (укладка SQL/лога замера в `/reference`), `Q-55` (три `COUNTIF` по `core.fact_loss`), `Q-11` (свежая выгрузка бизнес-метрик). Форма прецедентна — `ADR-076 §3c`. `Q-39` в хвост НЕ включена: триггер её промоушена наступил, но решение по форме фикса owner-gated, и протаскивать решение под видом замера запрещено. accepted.

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — append настоящего ADR; `06_INDEX.md` — строка ADR-091.
[сейчас] `07_STATE.md` — строки задач `LS-COUNTERPARTY-RETURNS-FIX` (снятие устаревшего BLOCKED), `LS-RETURNS-FX-HARDCODE`, `LS-PERIOD-CONTRACT`, `LS-INVENTORY-EXPLICIT-COLUMNS`, `CODE-REPO-STANDUP`, `CC-READONLY-PRINCIPAL`, `ORACLE-AUTOMATION`, `INGEST-MOMENT-ZONE-FIX`, `PARITY-REGISTRY`, `E1-T2-MECH-TAX` получают дописку о разрезе; новая строка `INFRA-FACTS-SWEEP`; таблица мандата (через `ADR-090 §4`).
[задачей CODE-REPO-STANDUP] выбор формы seed владельцем — предусловие запуска 2a.
[задачей INGEST-MOMENT-ZONE-FIX] подготовка патча — после закрытия `Q-92`.
[без правок] `ADR-085 §9` и `ADR-087` по существу не меняются; `02_ERP_CONTRACTS.md`, `03_PIPELINE_SPEC.md`, `reference/sql/*` (правятся задачами, не этим решением).

## ADR-092 · Мандат класса B на `PARITY-CHECK-SALES-RETURNS` выдаётся поимённо

**Дата:** 2026-08-01 · **Статус:** §1–§3 accepted (владелец, чат 2026-08-01; сессия MANDATE-TABLE-AUDIT)

**Контекст:** бриф `briefs/PARITY-CHECK-SALES-RETURNS.md` собран 2026-08-01 и лежит неисполнимым, потому что мандата класса B на задачу нет. Задача есть первая волна построчных сверок реестра паритета, от которого считается финиш Epic-1 (`ADR-067 §5b`). Прецедент формы — `ADR-079 §9`.

**Решение:**

§1 **Мандат класса B на `PARITY-CHECK-SALES-RETURNS` выдаётся поимённо настоящим параграфом.** Единственное основание класса B: доступ к секрету `msklad-token` (`ADR-076 §1`). Действуют ВСЕ шесть оговорок `ADR-076 §5` без изменений (токен не печатается; форма заголовка авторизации берётся из снапшота `reference/code/cf-finance/main.py`, а не изобретается; `grep -rF` по значению токена перед `git add`, любое совпадение есть стоп; поведение инструмента на неперечисленной команде фиксируется в session-блоке; эталон датируется снимком; правило-кандидат моста вносится в артефакт), плюс форма логирования `ADR-079 §6` (`date -u` и `gcloud auth list` первой И последней командой скрипта). Задача только читает: ни живых конфигураций, ни деплоя, ни записи в МойСклад. accepted (владелец).

§2 **Приёмка не переписывается** — пять шагов дословно по строке задачи `07_STATE` и по брифу. Вопрос `Q-92` (эталонные сутки паритета) сессией НЕ задаётся ни при каком результате замера. accepted.

§3 **Параллель:** класс B не параллелится никогда (врезка `07_STATE §Мандат`, `ADR-082 §1`). accepted.

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — append настоящего ADR; `06_INDEX.md` — строка ADR-092.
[сейчас] `07_STATE.md` — строка задачи `PARITY-CHECK-SALES-RETURNS`: «мандат НЕ выдан» → «мандат выдан поимённо»; строка таблицы мандата; стенд-ап.
[без правок] `briefs/PARITY-CHECK-SALES-RETURNS.md` — бриф собран верно, правки не требует; `ADR-088`, `ADR-089` не пересматриваются.

## ADR-093 · Язык ответов владельцу — три меры; `ADR-056 §7` superseded-in-part в части недублирования правила в файлы ролей

**Дата:** 2026-08-01 · **Статус:** §1–§6 accepted (владелец, чат 2026-08-01; сессия MANDATE-TABLE-AUDIT)

**Контекст:** владелец заявил прямо (чат, 2026-08-01), что правило `ADR-056` нарушается систематически, и потребовал разбора причины, а не извинения. Нарушение воспроизведено этой же сессией: первый ответ владельцу был подан таблицами с колонками внутренних кодов и ссылками на строки файлов, то есть слоем «для репо» вместо слоя «для владельца» (`ADR-056 §1/§2/§6`). Причины установлены чтением репо, не гипотезой:

(i) **Два документа расходятся в составе обязательного чтения архитектора.** `CLAUDE.md §Старт` п.2 требует шесть документов, включая `05_CONVENTIONS.md` — единственное место, где правило живёт операционно. `_ARCHITECT.md §3` («Обязательный контекст архитектора») и `§9` (текст быстрого старта) перечисляют пять и `05_CONVENTIONS` не называют. Сессия пошла по короткому списку и файл с правилом на старте не открыла.

(ii) **`ADR-056 §7` прямо отказался дублировать правило в файл роли архитектора** («продублировано ссылкой в `_ARCHITECT §6` не будет»), опираясь на то, что `05` Часть I и так читают. Пункт (i) показывает, что не читают.

(iii) **`ADR-056 §7` объявил §1 и §4 машинно проверяемыми — проверка не построена.** `grep` по `tools/` даёт ноль совпадений по правилу языка. Всё, что в проекте имеет механическую проверку, исполняется (чистота дерева, парность `06`/`06_INDEX`, кириллица в кодах, отставание базы); правило языка — единственный закон проекта без проверки вообще.

(iv) **Правило языка конкурирует с проверяемым правилом.** «Работа на фактах» (`_METHOD §6`, `CLAUDE.md ★`) требует поднять документ и показать источник; дешевейший способ показать источник — назвать его код. Где проверяемое правило и непроверяемое тянут в разные стороны, побеждает проверяемое. Это объясняет не единичный срыв, а повторяемость.

(v) **Ритуал конца сессии про язык не спрашивает:** в session-блоке четыре поля, ни одно не про слой изложения.

**Решение** (владелец выбрал варианты А, Б, В из четырёх предъявленных; вариант Г отложен в `Q-94`):

§1 **(А) Списки обязательного чтения выравниваются.** `_ARCHITECT.md §3` и `§9` получают `05_CONVENTIONS.md` в состав обязательного контекста архитектора, приводясь в соответствие с `CLAUDE.md §Старт` п.2. Расхождение двух документов о составе обязательного чтения есть дефект того же класса, что `ADR-054` ловит для содержимого файла, и лечится так же — приведением к одному источнику. accepted (владелец).

§2 **(Б) Разрез по артефакту.** Сообщение владельцу не несёт внутренних идентификаторов (`ADR-…`, `Q-…`, `B-…`, `RQ-…`, task-ID) и ссылок на строки файлов. Всё, что их требует, уходит в файл репозитория — session-блок либо артефакт `/reference` — а в сообщении стоит одна строка со ссылкой на этот файл. Мотив: так два правила перестают конкурировать (причина (iv)) — ссылаться кодами можно сколько угодно, но в файле, не в сообщении. Это физическая форма разреза, который `ADR-056 §2` уже требует по договорённости. accepted (владелец).

§3 **(В) Открывающий блок.** Каждый ответ владельцу открывается коротким блоком обычными словами, без единого идентификатора, и кончается тем, что делать: действие, развилка или явное «действий не требуется». Отсутствие блока есть непринятый ответ независимо от содержания. Прецедент формы — `ADR-077 §6`: владелец отличает настоящий гейт от рутины по НАЛИЧИЮ отдельного сообщения, а не по виду диалога инструмента. accepted (владелец).

§4 **`ADR-056 §7` superseded-in-part** в части «в `_ARCHITECT §6` дублировано не будет». Правило вносится в `_ARCHITECT.md` и в файлы всех четырёх ролей `.claude/agents/` (`architect.md`, `assembler.md`, `executor.md`, `generator.md`). Остальные параграфы `ADR-056` в силе без изменений. accepted (владелец).

§5 **Причина (iv) называется явно и не лечится дисциплиной.** Настоящее решение не утверждает, что §1–§3 закрывают её полностью: они снимают (i), (ii), (v) и делают нарушение видимым с одного взгляда, но конкуренцию правил снимает только измерение. Отсюда `Q-94`. accepted.

§6 **Вне scope:** машинная проверка ответа (`Q-94`, не замерена); правка `_METHOD.md` (доменно нейтрален, blast radius за пределами holika, owner-gated по прецеденту `ADR-021 §4`); состав `.claude/settings.json`.

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — append настоящего ADR; `06_INDEX.md` — строка ADR-093.
[сейчас] `07_STATE.md` — новая строка `Q-94`; стенд-ап (развилка владельца).
[отдельным коммитом] `_ARCHITECT.md` (§3 и §9 — состав обязательного контекста; новый пункт о языке), `.claude/agents/architect.md`, `.claude/agents/assembler.md`, `.claude/agents/executor.md`, `.claude/agents/generator.md` (правило языка), `05_CONVENTIONS.md` Часть I ★ Язык (новые пункты 7 и 8 — разрез по артефакту и открывающий блок), `09_RUNBOOK.md` (тексты первого сообщения — состав обязательного чтения архитектора).
[без правок] `_METHOD.md` (§6); `CLAUDE.md` §Язык — формулировка уже верна, менять нечего; `_GENERATOR.md`, `_ASSEMBLER.md` — правило приходит через `.claude/agents/`.

## ADR-094 · Форма seed код-репо: архив исходников задеплоенной ревизии; заготовка `.git` на диске не переиспользуется; порядок относительно замера

**Дата:** 2026-08-01 · **Статус:** §1–§7 accepted (владелец через делегирование выбора, чат 2026-08-01; сессия DEPLOY-TRUTH-ADJ)

**Контекст:** `ADR-017 §4` выбрал отдельный код-репо и оставил открытой форму первичного наполнения, `ADR-017 §5` назвал первым шагом discovery локального `.git` с развилкой seed-from-git против seed-from-zip и оговоркой «байт-точный seed = `function-source.zip`, не транскрипция». Шаг 1 исполнен (`reference/code_repo_standup_d1_2026-07-28.md`) и установил: `.git` в `/home/ilyasbazarov4/cf-finance/` существует, но несёт НОЛЬ коммитов; `main.py` в индексе, при этом рабочая копия уже разошлась с проиндексированной версией; рядом лежат `main.py.bak`, `main.py.pre-e1t3-mech-fx.bak`, `patch_main_finance.py`, `__pycache__/`; remote не задан. То есть ветка «seed-from-git» пуста по построению: истории нет. Владелец делегировал выбор архитектору (чат 2026-08-01), рекомендация была предъявлена ему до делегирования вместе с двумя альтернативами и их ценой.

**Решение:**

§1 **Seed берётся из архива исходников задеплоенной ревизии** (`function-source.zip` ревизии, получаемой read-only командой), НЕ с рабочей копии диска и НЕ транскрипцией. Основание — целевой инвариант `ADR-065`: «деплой любой CF выполняется ТОЛЬКО из код-репо; ревизия ↔ коммит». Инвариант либо верен с первого коммита, либо не верен никогда: задним числом соответствие пришлось бы доказывать для каждой ревизии отдельно. Seed из архива даёт это соответствие побайтово и воспроизводимо — любой может заново скачать тот же архив и получить то же дерево. Продолжает линию `ADR-017 §2` («байт-точность из `function-source.zip` / on-disk, НЕ из ручной транскрипции»). accepted.

§2 **Ревизия для seed определяется замером `DEPLOY-REVISION-PROBE`, а не назначается заранее.** Пока `Q-95` открыт, неизвестно, какая ревизия обслуживает трафик и несёт ли она валютную правку; сеять от неверной ревизии значит зафиксировать инвариант §1 на ложном основании. Поэтому шаг 2a `CODE-REPO-STANDUP` идёт ПОСЛЕ замера. accepted.

§3 **Существующая заготовка `.git` на диске Cloud Shell НЕ переиспользуется.** Она не несёт истории (ноль коммитов), но несёт устаревший индекс, разошедшийся с рабочей копией, и соседство мусорных файлов. Переиспользование дало бы историю, начинающуюся с состояния, про которое нельзя сказать, чему оно соответствует на проде — ровно то, ради ухода от чего задача промоутнута в критический путь (`ADR-065`, дрейф ревизий уже стоил сессий доказательств). Заготовка не удаляется и вообще не трогается: удаление чужого состояния на диске владельца в scope не входит. accepted.

§4 **Расхождения рабочей копии с задеплоенным разбираются ОТДЕЛЬНЫМ вторым коммитом** с явным объяснением, что это за отличия, а не подмешиваются в первый. Мусорные файлы (`*.bak`, `__pycache__/`, разовый `patch_main_finance.py`) в историю не вносятся; вместо этого в первом же коммите заводится `.gitignore`. accepted.

§5 **Разрез исполнения без изменений** (`ADR-091 §4`): шаг 2a (получение архива, сборка дерева, локальный коммит) — класс A, идёт инструментом; шаг 2b (создание удалённого репозитория и первый `push`) — исходящее действие, исполняет владелец. accepted.

§6 **Scope — только `cf-finance`.** Прочие функции по-прежнему per-CF и не пред-планируются (`ADR-017 §6`); настоящий ADR задаёт форму, применимую к ним, но задач под них не заводит. accepted.

§7 **Вне scope:** место хостинга удалённого репозитория (решение владельца в момент шага 2b); форма деплой-процедуры «только из репо» (`ADR-065`, отдельное решение после standup); статус `E1-T3-MECH-FX` (меняется замером, не этим ADR).

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — append настоящего ADR; `06_INDEX.md` — строка ADR-094.
[сейчас] `07_STATE.md` — строка `CODE-REPO-STANDUP` (развилка формы seed закрыта, порядок относительно замера назван); строка таблицы мандата `CODE-REPO-STANDUP, шаг 2a`; новые строки `DEPLOY-REVISION-PROBE` и `Q-95`; новая строка таблицы мандата `DEPLOY-REVISION-PROBE`; стенд-ап.
[задачей DEPLOY-REVISION-PROBE] определение ревизии для seed (§2).
[задачей CODE-REPO-STANDUP] первый коммит, `.gitignore` и второй коммит расхождений (§4); создание удалённого репозитория и первый `push` (§5, владелец).
[без правок] `ADR-017 §4` (место хранения не пересматривается), `ADR-065` (инвариант не меняется), `reference/code/` дисциплина снапшотов-провенанса продолжает действовать до standup.

## ADR-095 · `E1-T3-MECH-FX` закрывается DONE по ЗАПИСАННОМУ эталону; форма приёмки заменена явно; охват закрытия ограничен измеренным

**Дата:** 2026-08-01 · **Статус:** §1–§7 accepted (владелец, чат 2026-08-01; сессия E1-T3-CLOSE)

**Контекст:** приёмка задачи была записана как «staging dry-compare не-KGS май = 0,00 → прод re-diff `core` против live API = 0,00 без остатка». Ни один из двух шагов в этой форме не исполнялся и уже не будет: staging-стадия пройдена (правка задеплоена 2026-07-20, `Q-41` про отсутствующий артефакт закрыт ранее), а свежий live re-diff есть класс B и мандата не имеет. При этом содержательный результат достигнут и подтверждён тремя независимыми замерами: (i) `DEPLOY-REVISION-PROBE` — задеплоенная ревизия `cf-finance-00012-cik`, держащая 100 % трафика, несёт `× rate.value` (строка конвертера напечатана целиком); (ii) `Q-96` — оба документа, составлявшие ВСЁ расхождение мая (`Δ = 890 187,50`, локализовано до двух документов из 25 в `reference/recon_vyvod_pribyli_2026-05.md §2`), сошлись с записанным эталоном до копейки: 900 000,00 и 937,50; (iii) `marts.expenses` за май = 10 232 903,20 — точное совпадение с целевой величиной эталона (`ADR-057 §5`, `ADR-005`, `/reference/pnl_2026-05.md`). Дополнительно: пара «Расходы» реестра паритета была независимо зафиксирована сходящейся в 0,00 ещё 2026-07-28 другой сессией и против другого артефакта эталона (`reference/parity_registry.md`, `reference/fx_policy_expenses_2026-07-28.md`) — подтверждений не одно.

**Решение:**

§1 **Приёмка задачи закрывается записанным эталоном вместо свежего live re-diff.** Замена формы приёмки названа явно и не выдаётся за исполнение исходной: сравнение шло с оракулом, снятым 2026-07-14, а не с сегодняшним состоянием МойСклада. Основание допустимости — `00_CHARTER §главный принцип` требует числового паритета с тем, что рисует интерфейс; оракул есть зафиксированный снимок этого интерфейса (`ADR-067 §3` прямо предписывает датировать эталон снимком), и сверка с ним есть штатная форма приёмки, а не суррогат. accepted (владелец).

§2 **Охват закрытия ограничен измеренным и перечислен явно:** тип документа `paymentout`, месяц май-2026, поверхности `core.fact_payments` и `marts.expenses`. НЕ покрыто и закрытием НЕ утверждается: `cashout` (766 документов в таблице, в мае их нет вовсе); месяцы вне мая; прочие `core.fact_*`. Эти линии остаются за `Q-30` (магнитудный замер, DEFER) без изменений. accepted.

§3 **Связь «текущий код → верные числа» установлена прямо, а не выведена.** Строки зоны паритета переписаны 2026-07-22 и позже, то есть уже после деплоя ревизии `cf-finance-00012-cik`; значит проверенные числа произведены ровно тем кодом, который обслуживает трафик сейчас. accepted.

§4 **Остаток назван и вынесен, а не растворён в закрытии:** ровно один документ зоны паритета не переписан после деплоя (`Q-97`), максимум порядка 7 500 KGS, менее 0,08 % майского эталона. Закрытие задачи от него не зависит; `Q-97` живёт своей строкой. accepted.

§5 **Гипотеза «ежесуточный прогон переписывает всю историю» НЕ подтверждена и снимается.** Она была названа согласующейся в подробностях `Q-96` и помечена как гипотеза, не факт; инвентарь по всей таблице (`loaded_min = 2026-07-07` по `paymentout`) её опровергает. Формулировка `Q-96` не переписывается — она относится к маю и там верна (`06` append-only, прошлое не редактируется). accepted.

§6 **Что закрытие НЕ означает.** Оно не закрывает Epic-1 и не касается реестра паритета: сходится одна пара из семи, как и раньше. Оно не отменяет `ADR-065` — правило «деплой любой CF только из код-репо» действует вперёд, и тот факт, что деплой 2026-07-20 состоялся до принятия правила (2026-07-27), задним числом правило не нарушает и его не ослабляет. accepted.

§7 **Вне scope:** `Q-30`; статус прочих `core.fact_*`; форма деплой-процедуры.

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — append настоящего ADR; `06_INDEX.md` — строка ADR-095.
[сейчас] `07_STATE.md` — обе строки `E1-T3-MECH-FX` (таблица Epic 1 и GAP-реестр) уезжают в архив; обе её строки таблицы мандата убираются (парность `ADR-090 §1`); новые строки `Q-97` в реестре и в таблице мандата; стенд-ап; шапка.
[сейчас] `07_ARCHIVE.md` — дословный перенос обеих строк `E1-T3-MECH-FX`.
[задачей Q-97] определение валюты документа №05662 либо признание его строкой класса Ghost Records.
[без правок] `Q-30` (DEFER без изменений); `ADR-010`, `ADR-016`, `ADR-065` не пересматриваются; `reference/recon_vyvod_pribyli_2026-05.md` и `reference/parity_registry.md` не правятся.

---

## ADR-096: Модель закрепляется за ролью в файле роли; таблица стоимости `_METHOD §2` получает механизм

**Дата:** 2026-08-01 · **Статус:** §1–§5 accepted (владелец, чат 2026-08-01; сессия ROLE-MODELS)

**Контекст:** `_METHOD §2` задаёт роли как модели разной стоимости (архитектор — дорогая, разработчик —
средняя, applier — дешёвая) и это одно из оснований метода: дорогое суждение отделено от дешёвого
прогона. Файлы ролей `.claude/agents/` поля модели не несли, поэтому фактическая модель бралась из
интерфейса на момент запуска. Следствие наблюдалось 2026-08-01: архитекторская сессия исполнила
четыре задачи класса A и пять проходов сборки на дорогой модели. Тот же класс дефекта, что
`ADR-093` (правило есть, механизма нет, побеждает то, что механизировано).

**Решение:**

§1 **Поле `model` вносится в каждый файл роли:** `architect` — `opus`; `executor`, `generator`,
`assembler` — `sonnet`. accepted (владелец, чат 2026-08-01).

§2 **Сборка отнесена к средней, а не к дешёвой модели**, вопреки буквальному «applier — дешёвая»
(`_METHOD §2`). Основание: роль сборки в этом проекте несёт стоп-условия, требующие суждения
(конфликт блоков, маркер последствий без адресата, расхождение с `origin`), и уже дважды
останавливалась по существу. Дешёвая модель на такой роли экономит на том месте, где тихий отказ
дороже прогона. Расхождение с каноном названо сознательно, а не оставлено незамеченным. accepted.

§3 **Закрепление не запрещает архитектору исполнять.** Оно делает дешёвый запуск умолчанием, а не
ограничением: владелец по-прежнему может попросить любую роль о чём угодно в пределах её класса
задач. Дисциплина «исполнение идёт отдельной сессией» остаётся организационной и живёт в
`09_RUNBOOK`, а не в этом поле. accepted.

§4 **`_METHOD.md` НЕ правится.** Он доменно-нейтрален и дистиллирован из внешнего эталона; правка
задевает проекты за пределами holika — owner-gated по прецеденту `ADR-021 §4`, тот же довод, что у
`Q-40`, `Q-84`, `Q-85`. Настоящее решение реализует существующее правило канона, а не меняет его,
поэтому нового расхождения не создаёт и отдельного `Q` не заводит. accepted.

§5 **Вне scope:** состав `.claude/settings.json`; выбор модели для чат-сессий вне инструмента;
reasoning effort и прочие параметры ролей — не назначаются, замера под них нет.

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — append настоящего ADR; `06_INDEX.md` — новая строка.
[сейчас] `07_STATE.md` — стенд-ап; `### Подробности для модели`; шапка.
[отдельным коммитом] `.claude/agents/architect.md`, `.claude/agents/executor.md`,
`.claude/agents/generator.md`, `.claude/agents/assembler.md` — поле `model` (правки внесены сессией
и закоммичены ею же по `ADR-081 §8`).
[без правок] `_METHOD.md` (§4); `09_RUNBOOK.md` — тексты запуска остаются верны, роль выбирается там
же, где и раньше; `05_CONVENTIONS.md`; `CLAUDE.md`.


## ADR-097 · `Q-98` переклассифицирован — сравниваемых чисел три, а не два; `0,31 %` есть остаток двух встречных смещений; причина закрывается замером, не адъюдикацией; развилка моста не задаётся до замера

**Дата:** 2026-08-01 · **Статус:** §1–§5, §7 accepted; §6 порядок accepted, варианты (a)/(b)/(c) proposed, требуют апрува (владелец, чат 2026-08-01; сессия `PARITY-GAPS-ADJ`)

**Контекст:** сборка 2026-08-01 завела `Q-98` с формулировкой «причина не установлена» по итогам
`PARITY-CHECK-SALES-RETURNS`. Сессия сверки сопоставила два числа — `core.fact_sales_profit.revenue_kgs`
за май-2026 (`93 522 995,53`) и `report/profit/byproduct`/`bycounterparty.sellSum` за тот же период
(`93 234 205,80`) — и записала расхождение `288 789,73` KGS (0,31 %) как единичное, отклонив по пути
гипотезу мультивалютной конвертации (пересчёт по `rate.value` дал третье число `91 892 755,66`).
Настоящее решение вынесено БЕЗ новых обращений к источнику: вся фактура снята чтением артефактов
`reference/_scratch_PARITY-CHECK-SALES-RETURNS_2026-08-01/` и снапшота кода
`reference/code/cf-facts/`, уже лежащих в репо.

**Решение:**

§1 **Картина заменяется с двухчисловой на трёхчисловую.** Число `91 892 755,66` — не «третье
число, опровергающее гипотезу конвертации», как оно записано в реестре, а самостоятельная
величина: сумма СОБСТВЕННЫХ итогов документов МойСклада (`entity/demand.sum`, 127 документов
мая), сконвертированная курсом того же документа. Три величины и три попарные разности:
`core` `93 522 995,53`; `report` `93 234 205,80`; `Σ demand.sum` `91 892 755,66`;
`core − report` = `288 789,73` (0,31 %); `core − Σ demand.sum` = `1 630 239,87` (1,7741 %);
`report − Σ demand.sum` = `1 341 450,14` (1,4598 %). Отсюда вывод, меняющий приоритет вопроса:
записанные `0,31 %` есть РАЗНОСТЬ ДВУХ ВСТРЕЧНЫХ СМЕЩЕНИЙ порядка полутора процентов каждое, а
не малая единичная ошибка. Почти скомпенсированная пара опаснее видимого разрыва: поверхность
выглядит почти сошедшейся, и оба смещения переживают приёмку. accepted.

§2 **Механическая причина того, что `core` не обязан совпадать с итогом документа, названа
кодом, а не предположением.** `revenue_kgs` есть наша арифметика по позициям —
`price/100 × rate.value × quantity × (1 − discount/100)` (`reference/code/cf-facts/fetch_demands.py:137-141`),
проходящая через `MERGE` без изменения (`bq_ops.py`, `s.revenue_kgs AS revenue_kgs`); собственный
итог `demand.sum` в пайплайн не попадает ни на одном шаге. Периметр документов у двух сторон тоже
не один: ингест читает только `entity/demand` (`grep -rn "retaildemand"` по всем `*.py` снапшота —
0 совпадений), тогда как `report/profit/*` есть отчёт аккаунта. Оба факта — из репо. accepted.

§3 **Четыре гипотезы причины, с различителем и ценой каждая; иные гипотезы этим ADR не
запрещены.**
(H1) **Разный периметр типов документов.** Отчёт считает весь аккаунт, ингест — только оптовые
отгрузки. Косвенное указание: `report/profit/bycounterparty` за май печатает `salesCount` = 639
при 127 документах `entity/demand`, а `returnCount` = 8 в той же выдаче в точности равен числу
розничных возвратов, что делает прочтение «`salesCount` считает документы» вероятным. Прочтение
НЕ доказано: семантика поля в репо не документирована, и на этих данных `8` одновременно есть
число документов, число позиций и число единиц. Различитель: один `GET` `entity/retaildemand` за
май (счётчик и сумма). Цена: один запрос, класс B. Предсказание, которое различитель обязан
подтвердить или опровергнуть: розничные продажи ≈ `1 341 450,14`.
(H2) **Наша арифметика не воспроизводит итог документа.** Различитель, класс A целиком: сумма
`revenue_kgs` в разрезе `transaction_date` одним read-only запросом, сопоставленная офлайн с уже
сохранёнными заголовками — локализует `1 630 239,87` до суток, затем до документов. Цена: один
запрос к BigQuery. Продолжение (один `GET` позиций локализованного документа, класс B) ставится
только если шаг A локализацию дал.
(H3) **Протухший снимок.** Майские строки `core` записаны до последующих правок документов и
более не обновляются (см. §5). Различитель, класс A целиком: `_loaded_at` майских строк против
уже сохранённого `updated` документов (102 из 127 документов, 89 % суммы, несут `updated` позже
мая). Цена: один запрос к BigQuery.
(H4) **Асимметрия границ самого замера.** Отчёт запрошен с `momentTo=2026-06-01 00:00:00` при
обеих границах включительно (`Q-93`), сущности — с `moment<2026-06-01 00:00:00`. Верхний край у
сторон разный. Априорная вероятность мала (нужен документ ровно в полночь), цена проверки — один
повторный запрос отчёта с `momentTo=2026-05-31 23:59:59`. accepted.

§4 **Порядок: класс A первым.** Гипотезы H2 и H3 различаются целиком в классе A и нового мандата
не требуют; H1 и H4 требуют класса B. Дешёвое и не гейтящееся идёт раньше дорогого и гейтящегося;
результат класса A способен сузить или снять часть класса B. Задача — `PARITY-SALES-DISCRIMINATE`,
два шага, приёмка каждого выведена формулой, а не свойством данных (`ADR-075 §3`), и обязана
печатать строки, а не метку (`ADR-021 §2`, `ADR-044`). accepted.

§5 **Отдельная структурная находка, не зависящая от причины `Q-98`.** У `cf-facts` есть только
скользящие окна 7 и 90 суток (`config.py`, `HOURLY_WINDOW_DAYS`/`WEEKLY_WINDOW_DAYS`) и нет режима
досева для продаж; `MERGE` дополнительно ограничивает целевую таблицу тем же окном
(`T.transaction_date >= DATE_SUB(CURRENT_DATE(), INTERVAL window_days DAY)`, `bq_ops.py`). Якорь
паритета `2026-05-01` (`ADR-069 §2`) перестал попадать в самое широкое окно `2026-07-30`; весь май
выходит из него к `2026-08-29`. Пока досева нет, паритет мая не может быть «поддерживаемым
состоянием» (`ADR-067 §5c`) — только датированным снимком. Ловушка досева называется здесь, чтобы
не быть открытой заново: окно выборки и окно `MERGE` совпадают по построению, поэтому строка
старше окна, поданная в staging, не найдёт пары по `ON` и уйдёт в `WHEN NOT MATCHED THEN INSERT`,
то есть задвоится. Досев повторным вызовом `mode=promote` с прежними параметрами запрещён.
Заводится задачей `SALES-REFRESH-WINDOW`. accepted.

§6 **Развилка уровня моста НЕ задаётся до замера** — прецедент формы `ADR-088 §6/§7`, где вопрос
`Q-92` сознательно не задавался до замера ночных полос и это себя оправдало (полосы оказались
пусты). Варианты и цена каждого фиксируются здесь заранее, чтобы после замера развилка решалась
без нового разбора: **(a) один оракул, один объект** — объектом паритета продаж объявляется число
`report/profit/*`, любое расхождение есть дефект нашей стороны; цена — ингест обязан покрыть тот
же периметр документов, что и отчёт, и воспроизводить итог документа, то есть правка `cf-facts`
плюс досев мая, класс B, гейтится `ADR-065`; плюс — единственный вариант, при котором клиент
воспроизводит наше число со своей страницы. **(b) сузить периметр обеих сторон** — эталон берётся
агрегацией `entity/demand`, то есть уровень (ii) вместо (i); цена — понижение уровня оракула при
доступном (i), что `ADR-067 §2` разрешает только с явным обоснованием; минус — клиент, открыв
«Прибыльность по товарам», увидит не наше число, то есть ровно то, что финишная черта запрещает.
**(c) объявленное отступление `ADR-068 §2`** — правило «выручка BI есть оптовый канал, розница вне
периметра»; цена — паритет ослаблен, но легитимно и без правки прод-кода; требует подтверждения
владельцем, что розничный канал вне scope BI. accepted (порядок), варианты (a)/(b)/(c) — `proposed`,
решаются после `PARITY-SALES-DISCRIMINATE`.

§7 **Что отклонено чтением, без замера.** Множитель курса (`rate.multiplier`): поля нет ни в
одном из 127 сохранённых заголовков, конвертация `× rate.value` формально полна. Потеря страниц
отчёта: 279 уникальных товаров на трёх страницах при `meta.size` = 279, суммы `byproduct` и
`bycounterparty` побайтово равны (`9 323 420 580,10` минорных единиц обе) — сторона отчёта
собрана верно и артефактом сбора не объясняется. accepted.

**Последствия (blast radius):**

[сейчас] `07_STATE.md` §Открытые вопросы, строка `Q-98`: формулировка «причина не установлена»
  заменяется на трёхчисловую картину §1 плюс четыре гипотезы §3 плюс адресат `PARITY-SALES-DISCRIMINATE`.
[сейчас] `07_STATE.md` §Открытые вопросы: заводятся две задача-строки —
  `PARITY-SALES-DISCRIMINATE` и `SALES-REFRESH-WINDOW` (тексты — в `STATE_PATCH` §Новые открытые
  вопросы, переносятся дословно).
[сейчас] `07_STATE.md` §Мандат Claude Code: класс задач — четыре строки тем же коммитом
  (`ADR-090 §1`): `PARITY-SALES-DISCRIMINATE, шаг 1` (A, параллель нет, постоянный, read-only
  BigQuery плюс офлайн-сопоставление; пишет `reference/parity_sales_discriminate_<date>.md`,
  `reference/_scratch_PARITY-SALES-DISCRIMINATE_<date>/`); `PARITY-SALES-DISCRIMINATE, шаг 2`
  (B, параллель нет, мандат НЕ выдан, основание — `msklad-token`, `ADR-076 §1`);
  `SALES-REFRESH-WINDOW, подготовка` (A, параллель нет, постоянный; пишет
  `reference/sales_refresh_window_<date>.md`); `SALES-REFRESH-WINDOW, деплой` (B, параллель нет,
  НЕ выдан, дополнительно гейтится `ADR-065`).
[сейчас] `07_STATE.md` §Подробности для модели — абзац «Адъюдикация `Q-98`».
[сейчас] `07_STATE.md` §Стенд-ап — заменяется целиком (`ADR-084 §1`).
[сейчас] `06_INDEX.md` — строка настоящего ADR.
[задачей PARITY-SALES-DISCRIMINATE] Различители H1–H4 (§3), выбор варианта (a)/(b)/(c) (§6).
[задачей SALES-REFRESH-WINDOW] Досев зоны паритета и ловушка задвоения (§5).
[без правок] `reference/parity_registry.md` строки 18–19 настоящим ADR НЕ правятся: правка
  правила-моста осмысленна только после выбора (a)/(b)/(c), а правка колонки статуса до замера
  заменила бы один незакрытый факт другим. `ADR-085 §1` (объект паритета продаж) не
  пересматривается — пересмотр есть содержание развилки §6.

## ADR-098 · `Q-99` — у пары «Возвраты покупателей» есть эталон уровня (i), он найден внутри уже скачанного отчёта; `ADR-085 §2` superseded-in-part в части основания выбора уровня (ii)

**Дата:** 2026-08-01 · **Статус:** §1, §2, §4-§7 accepted; §3 accepted вариантом (c) (владелец, чат 2026-08-01; сессия `PARITY-GAPS-ADJ`)

**Контекст:** `ADR-085 §2` выдал паре возвратов оракула-кандидата уровня (ii) — `entity/salesreturn` —
на основании того, что `report/profit/bysalesreturn` возвращает `404`. Сессия
`PARITY-CHECK-SALES-RETURNS` установила фактом, что за май-2026 `entity/salesreturn` пуст
(0 документов из 11 за всю историю аккаунта), тогда как `core.fact_returns` за май несёт 8 строк
на `570,00` KGS, все типа `retailsalesreturn`, и что ингест (`fetch_returns.py:51`) читает ОБА
типа, то есть назначенный оракул не покрывал реальный источник таблицы ни одного дня.

**Решение:**

§1 **Факт, добытый этой сессией чтением уже скачанного, а не новым запросом.**
`report/profit/byproduct` и `report/profit/bycounterparty` за май-2026 несут `returnSum` = 57 000
минорных единиц = `570,00` KGS и `returnCount` = 8. Это ровно те восемь розничных возвратов и
ровно та сумма, что лежат в `core.fact_returns` (`step3c_bq_returns_may.log`: 8 строк, `570,0`),
и та же сумма, что даёт живой `GET` к `entity/retailsalesreturn` (57 000 ÷ 100). Совпадение до
копейки, тремя независимыми путями. accepted.

§2 **`ADR-085 §2` superseded-in-part.** Основание выбора уровня (ii) — «отдельного отчёта по
возвратам нет, потому что `report/profit/bysalesreturn` = `404`» — было верным о ФАКТЕ `404` и
неверным о ВЫВОДЕ: эталон уровня (i) для возвратов существовал всё это время колонкой внутри того
же отчёта, который сессия сверки и так тянула. `404` по отдельному эндпоинту не есть отсутствие
величины в report-API — это тот же класс ошибки, что `ADR-021 §2` разбирает для пустой выдачи.
Сам `ADR-085 §1` (нетто как производная, объект паритета продаж) настоящим параграфом НЕ
затрагивается. accepted.

§3 **Три варианта объекта паритета пары, с ценой каждого.**
**(a) Поднять оракул пары до уровня (i)** — `report/profit/bycounterparty.returnSum` как объект,
`byproduct.returnSum` как контроль. Плюсы: (i) выше (ii) по `ADR-067 §2`, то есть не требуется
доказывать, что наша агрегация сущностей совпадает с интерфейсом, — отчёт и ЕСТЬ движок
интерфейса; периметр типов документов определяет источник, а не наш список, поэтому третий тип
возврата приедет в эталон сам; зерно `bycounterparty` совпадает с зерном BI-поверхности
(`msklad_counterparty_returns` — разрез по контрагенту); май уже сходится `0,00`. Минусы:
`returnSum` живёт внутри отчёта о прибыльности, и его периметр не обязан совпадать с «все
возвраты» — возврат, относящийся к продаже прошлого периода, может учитываться иначе, это в репо
не документировано и не проверено.
**(b) Расширить entity-мост на оба типа** (`salesreturn` + `retailsalesreturn`), оставив уровень (ii).
Плюсы: мост описывает ровно то, что делает ингест, и проверяется чтением одного файла. Минусы:
перечень типов документов становится НАШИМ перечнем, а не перечнем источника, — при появлении
третьего типа мост устареет молча; это буквально тот же дефект, который сейчас и обнаружен, только
сдвинутый на один шаг. Плюс `ADR-067 §2` разрешает уровень (ii) при доступном (i) лишь с явным
обоснованием, которого после §1 больше нет.
**(c) Оба пути сразу:** объект паритета — число отчёта (уровень (i)); агрегация обоих entity-типов
— контрольная сверка, расхождение двух путей само есть сигнал. Цена: два запроса вместо одного в
`ORACLE-AUTOMATION`. Плюс: нынешняя находка получена именно расхождением двух путей, то есть
механизм уже доказал свою полезность на этом самом вопросе.
**Выбран вариант (c)** — владелец, чат 2026-08-01, при рекомендации архитектора (c). Оговорка
принята вместе с вариантом: перечень типов остаётся в мосте как ОПИСАНИЕ ингеста, а не как
определение объекта паритета. accepted (владелец).

§4 **Предел вывода назван явно.** Совпадение проверено на одном месяце, восьми документах и сумме
`570,00` KGS против выручки `93,5` млн — как тест на полноту это почти ничего не доказывает.
Поэтому вариант (c) рекомендуется не из осторожности вообще, а потому что второй путь остаётся
единственной проверкой первого до тех пор, пока месяцев с непустыми возвратами обоих типов не
наберётся больше. accepted.

§5 **Побочный дефект ингеста, найденный тем же чтением.** Все восемь розничных возвратов мая
несут ключ `demand` (сырые тела в `retail_returns_may.json`), что противоречит собственному
докстрингу `fetch_returns.py:13` («retailsalesreturn никогда не имеет demand»); проверка
`has_basis = "demand" in doc` (`fetch_returns.py:70`) смотрит на наличие ключа, а не на значение,
из-за чего `core.fact_returns.has_basis` = `true` у всех восьми. Денежных величин не трогает,
документированную колонку делает неверной. Заводится `Q-101` со статусом DEFER; отдельной задачи
и отдельной строки мандата не заводится, потому что отдельного деплоя не требует — едет вместе с
`INGEST-MOMENT-ZONE-FIX`. accepted.

§6 **Ответ на уточняющий вопрос владельца (чат 2026-08-01): «система» в вариантах §3 есть
МойСклад, а не наши Cloud Functions.** Оба пути утверждённого варианта (c) идут в API МойСклада:
отчётная точка `report/profit/bycounterparty` и сущностные точки
`entity/salesreturn` + `entity/retailsalesreturn`. Сравнение нашей таблицы с нашей же таблицей
эталоном не является ни в одном из трёх вариантов и вариантом не рассматривалось:
`00_CHARTER §главный принцип` (правило владельца PR-34 №34) прямо называет такое сравнение
проверкой внутренней согласованности пайплайна, а не проверкой полноты данных. Суждение
владельца «данные для сверки обязаны тянуться из API МойСклада» подтверждается репо и
возражений не имеет. accepted.

§7 **Три границы к формулировке «обязательно тянуть из API МойСклада» — не возражения, а
условия, без которых правило даёт ложную уверенность.**
(i) **Внутри API МойСклада есть иерархия, и точки не взаимозаменяемы** (`ADR-067 §2`): отчётные
точки суть движок интерфейса, сущностные — сырые документы. Нынешняя находка получена расхождением
двух точек ОДНОГО И ТОГО ЖЕ API: `entity/salesreturn` за май пуст, `report/profit/*.returnSum`
за тот же май даёт `570,00`. «Тянуть из API» необходимо, но недостаточно — надо ещё называть,
какой уровень API есть эталон, иначе спор двух ответов источника нечем разрешить.
(ii) **Тянуть надо в ДАТИРОВАННЫЙ снимок, а не «каждый раз заново»** (`ADR-067 §3`). МойСклад
пересчитывает историю при правках задним числом; замер этой сессии: 102 из 127 майских документов
(89 % суммы) несут `updated` позже мая. Если движутся обе стороны сравнения, расхождение
невозможно закрыть в принципе — каждая новая сверка даёт новое число. Поэтому «обязательно из
API» читается как «эталон снимается из API и датируется», а не «сравниваем с живым API при
каждом прогоне».
(iii) **Требование относится к стороне ЭТАЛОНА, не к нашей.** Нашу величину нельзя перевычислять
из API в момент сравнения — проверка выродится в сравнение API с самим собой. Найденные по
продажам `1 630 239,87` ровно про это: наше число есть наша арифметика по позициям, и лечится
это тем, чтобы ингест воспроизводил число источника, а не тем, чтобы в момент сверки брать число
источника напрямую. accepted.

**Последствия (blast radius):**

[сейчас] `07_STATE.md` §Открытые вопросы, строка `Q-99`: CLOSED вариантом (c) (владелец
  2026-08-01); строка переносится дословно в `07_ARCHIVE.md` тем же коммитом (`ADR-064`) с
  однострочной выжимкой «закрыт чем».
[сейчас] `reference/parity_registry.md` строка 20 — три точечные правки, дословный текст ниже
  (`_ARCHITECT §6`, правило явного патча). **Колонка «Источник эталона» заменяется ЦЕЛИКОМ на:**
  «(i) `report/profit/bycounterparty.returnSum` — колонка внутри отчёта о прибыльности, тот же
  движок, что рисует интерфейс; за май-2026 = 57 000 минорных единиц ÷100 = `570,00` KGS при
  `returnCount` = 8 (`reference/_scratch_PARITY-CHECK-SALES-RETURNS_2026-08-01/bycounterparty_may_returnsum.txt`,
  `bycounterparty_may_page_0.json`). Контрольный второй путь — (ii) агрегация ОБОИХ entity-типов
  того же API, `entity/salesreturn` + `entity/retailsalesreturn` (ровно то, что читает
  `fetch_returns.py:51`): за май первый пуст, второй даёт 8 документов на те же `570,00`
  (`step3b_returns_sanity.log`, `retail_returns_may.json`). `report/profit/bysalesreturn` = `404`
  есть факт о ЕДИНИЧНОМ эндпоинте, а не о недоступности уровня (i) для возвратов». **В колонку
  «Правило-мост» ДОПИСЫВАЕТСЯ в конец:** «**Объект паритета (владелец, 2026-08-01, вариант (c)):**
  число уровня (i) — `returnSum` в разрезе по контрагенту; агрегация двух entity-типов есть
  контрольный второй путь, и расхождение двух путей само есть сигнал. Перечень типов документов
  остаётся в этой колонке как ОПИСАНИЕ того, что читает ингест, и определением объекта паритета
  НЕ является: периметр типов задаёт источник, иначе следующий новый тип устареет молча».
  **Колонка «Статус сходимости» заменяется ЦЕЛИКОМ на:** «**`0,00` на уровне core-таблицы за
  май-2026** — эталон `570,00` KGS против `core.fact_returns` `570,00` KGS, совпадение до копейки
  тремя независимыми путями. **Строка НЕ объявляется сошедшейся:** поверхность BI этой пары есть
  Custom Query `msklad_counterparty_returns`, который за период дашборда показывает не это число
  (скользящее окно 365 суток плюс размножение суммы по разрезу строки, `ADR-085 §10`);
  сходимость пары объявляется после `LS-COUNTERPARTY-RETURNS-FIX` и read-back. Счётчик реестра
  остаётся 1/7».
[сейчас] `07_STATE.md` §Открытые вопросы: заводится строка `Q-101` (§5), тип «факт», DEFER,
  строки мандата не требует.
[сейчас] `07_STATE.md` §Подробности для модели — абзац «Адъюдикация `Q-99`».
[сейчас] `06_INDEX.md` — строка настоящего ADR; в строке `ADR-085` пометка
  «§2 superseded-in-part настоящим ADR».
[отдельным коммитом] правок вне буфера настоящий ADR не требует.
[задачей ORACLE-AUTOMATION] второй путь сверки возвратов (§3c) закладывается в скрипт посева
  эталона: две точки API вместо одной, расхождение путей есть сигнал, а не повод выбрать одну.
[задачей LS-COUNTERPARTY-RETURNS-FIX] объявление сходимости пары после правки Custom Query и
  read-back; до этого счётчик реестра остаётся 1/7.
[без правок] `ADR-085 §1`, `§3`–`§10` не пересматриваются; `02_ERP_CONTRACTS.md` правки не
  требует — схема `core.fact_returns` верна, менялся эталон, а не наша сторона.

## ADR-099 · `Q-100` — причина в незакрытом канале «текст запуска сборки»; `ADR-086 §1` к сборке не приложился, потому что у неё нет строки задачи; фикс — снять канал и печатать инвентарь буфера

**Дата:** 2026-08-01 · **Статус:** §1-§5 (включая §2a/§2b) accepted (§2 — вариант «в», владелец, чат 2026-08-01; сессия `PARITY-GAPS-ADJ`)

**Контекст:** сборка 2026-08-01 остановилась, потому что текст её задачи утверждал, что шапка
session-блока несёт три указания по нумерации ADR, перекрывающие `_ASSEMBLER.md §5`, а
единственный файл буфера такой шапки не нёс. Владелец подтвердил, что указаний не отправлял, и
назвал случай третьим по счёту. Настоящий ADR разбирает причину и предлагает форму фикса.

**Решение:**

§1 **Причина установлена структурно, сопоставлением двух файлов.** `ADR-086 §1/§3` закрыл канал
«определение задачи из разговора» требованием: определение цитируется из строки `07_STATE.md`
дословно, а не пересказывается. У прохода сборки строки задачи нет по построению — его определение
есть файл роли `_ASSEMBLER.md`, — поэтому требование дословной цитаты к нему не приложилось, и
текст запуска остался свободной прозой о содержимом буфера. `_ASSEMBLER.md §3` перечисляет вход
(статус дерева, обязательный контекст, ВСЕ файлы буфера, хук) и текста запуска в перечне не
называет, но и не запрещает; `§10` не содержит стоп-условия под случай «текст запуска утверждает
о буфере то, чего в буфере нет». Прямое следствие, отвечающее на вопрос владельца «почему не
ловится раньше»: расхождение может проявиться только у сборщика, потому что ни одна предыдущая
стадия текста запуска сборки не видит — «раньше» здесь структурно невозможно. Оптимизируется не
момент обнаружения, а существование канала. accepted.

§2 **Форма фикса — две меры вместе; порознь каждая неполна.**
**(A) Снять канал.** `_ASSEMBLER.md §3` получает явную строку: текст запуска входом НЕ является,
определение прохода есть этот файл, содержимое буфера читается из буфера. `_ASSEMBLER.md §10`
получает стоп-условие: «текст запуска утверждает о содержимом буфера то, чего в буфере нет».
Цена: правка одного файла роли, ноль на прогон. Слабость названа честно: это правило без
механизма — ровно тот класс, который `ADR-093 §5` разобрал как систематически проигрывающий
проверяемому правилу.
**(B) Напечатать факт первым шагом.** `tools/inbox_manifest.sh` печатает по каждому файлу
`reference/_inbox/`: имя файла, задачу и дату из имени, первую строку файла, наличие каждого из
четырёх разделов (`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`), число
пунктов последствий без маркера времени. Шаг 1 `_ASSEMBLER.md §4` («Инвентаризация», сейчас голый
`ls`) заменяется на прогон скрипта с выводом в отчёт. Цена: один скрипт того же класса, что уже
работают (`tools/session_status.sh`, `tools/parallel_check.sh`), секунды на прогон. Механизм:
утверждение превращается в напечатанный факт ДО первой правки, по правилу «вердикт печатает
строки, а не метку» (`ADR-021 §2`, `ADR-044`). Побочная польза: голый `ls` в шаге 1 остаётся
прямым нарушением `ADR-083 §4` (счётчик вместо отсутствия вывода) — мера (B) закрывает и его.
**(C) Протоколировать инстанс.** `_ASSEMBLER.md §11` (отчёт в теле коммита) получает пункт:
расхождение текста запуска с буфером, если было, называется в отчёте; отсутствие расхождения
тоже называется одной строкой. Причина — см. §4.
**Владелец утвердил вариант «в» — все три меры вместе (чат 2026-08-01), при рекомендации
архитектора того же варианта. ИСПОЛНЕНО этой же сессией коммитом `d97f51b`** по `ADR-081 §8`
(правки файлов ролей и `tools/` сессия коммитит сама, поимённо, отдельным коммитом): новый файл
`tools/inbox_manifest.sh` (исполняемый, `chmod +x`) плюс четыре правки `_ASSEMBLER.md` —
`§3` п.0 (текст запуска не есть вход), `§4` шаг 1 (прогон скрипта вместо голого `ls`, вывод
целиком в отчёт), `§10` (два новых стоп-условия), `§11` (пункт отчёта). accepted.

§2a **Приёмка меры (B), исполненная в этой же сессии, а не отложенная.** На реальном буфере
скрипт дал `RC 0` и напечатал по единственному файлу задачу, дату, первую строку, наличие всех
четырёх разделов и «пунктов последствий без маркера времени: 0». На подготовленном негативном
наборе (каталог в `scratchpad`, в репо не коммитится) обнаружены и напечатаны все четыре класса
находок: посторонний скрытый файл, имя не по шаблону `session_<TASK>_<YYYY-MM-DD>.md`,
отсутствующий раздел, пункт последствий без маркера времени — последний с номером строки и самой
строкой, а не меткой (`ADR-044`); `RC 1`. `bash tools/hooks/selftest.sh` после правок —
«пройдено 28, провалено 0». accepted.

§2b **Оговорка о моменте вступления в силу, чтобы её не открыли заново.** Проход сборки, который
сольёт ветку этой сессии, получит `tools/inbox_manifest.sh` на диск шагом 0, то есть РАНЬШЕ своего
шага 1, — но сам `_ASSEMBLER.md` он прочитал по редакции до слияния. Поэтому первым проходом,
обязанным идти по новым правилам, является СЛЕДУЮЩИЙ, а не тот, что вносит правку. Это не дефект
и не повод исполнить правку в основном дереве в обход `ADR-081 §8`. accepted.

§3 **DEFER отклонён, с обоснованием.** Довод за DEFER силён на вид: гейт сработал все три раза,
ущерба нет, три остановки есть три пойманных расхождения. Он отклоняется по одной причине:
все три раза утверждение было ПОЛНОСТЬЮ ложным, и потому опровергалось первым же взглядом в
буфер. Утверждение, ЧАСТИЧНО совпадающее с содержимым буфера, сборщик подтвердит и применит — и
это уже не остановка, а тихий дрейф. Цена такого дрейфа в проекте измерена: `ADR-086 §2` —
файл с именем одной задачи и телом другой прожил в репо и был удалён с обоснованием, которое
тем же ADR признано неверным в двух утверждениях. accepted.

§4 **Частота инстансов есть утверждение, а не факт репо, и это часть дефекта.** Проверено
`grep` по `07_STATE.md`, `07_ARCHIVE.md`, `06_DECISIONS_LOG.md`, `_ASSEMBLER.md`,
`05_CONVENTIONS.md`: записи о двух предыдущих случаях нет (0 совпадений по «текст запуска»,
«задача сборки», «запуск сборки»). Две остановки сборки, которые в репо ЗАПИСАНЫ — маркер
последствий без task-ID (`07_STATE §Подробности`, сборка 2026-07-31) и дублирование строк реестра
(`ADR-085 §3`), — относятся к другому классу: дефект был в самом блоке, а не в тексте запуска.
Канал, который никто не протоколирует, нельзя ни измерить, ни закрыть по факту; отсюда мера (C).
accepted.

§5 **Названа, но не утверждена, вероятная кормушка канала.** Утверждения о содержимом блока могут
попадать в текст запуска сборки из чат-выдачи исполнявшей сессии — то есть знание идёт по
чат-каналу вместо репо-канала, что `_METHOD §1` прямо называет потерей знания, а `_METHOD §8`
закрывает требованием одного session-блока. Этой сессией НЕ проверено (чаты предыдущих сессий ей
недоступны), поэтому мерой не закрывается и в §2 не заложено; мера (B) делает фактическое
содержимое блока видимым независимо от того, верна ли эта догадка. accepted как названное
предположение, не как факт.

**Последствия (blast radius):**

[сейчас] `07_STATE.md` §Открытые вопросы, строка `Q-100`: CLOSED — причина установлена (§1),
  форма фикса утверждена владельцем и исполнена коммитом `d97f51b` (§2, §2a); строка переносится
  дословно в `07_ARCHIVE.md` тем же коммитом (`ADR-064`).
[сейчас] `07_STATE.md` §Подробности для модели — абзац «Адъюдикация `Q-100`».
[сейчас] `06_INDEX.md` — строка настоящего ADR.
[отдельным коммитом] `_ASSEMBLER.md` §3 п.0, §4 шаг 1, §10, §11 и новый
  `tools/inbox_manifest.sh` — **ИСПОЛНЕНО коммитом `d97f51b`** в ветке `s/PARITY-GAPS-ADJ` той же
  сессии (`ADR-081 §8`). Сборщик их в репо НЕ ищет и на отсутствии НЕ останавливается
  (`ADR-083 §3`); фактически они приезжают тем же слиянием ветки, что и настоящий блок.
[без правок] `ADR-086` не пересматривается: его §1/§3 верны для задача-сессий, настоящий ADR
  лишь распространяет тот же принцип на проход, у которого строки задачи нет. `CLAUDE.md` и
  `05_CONVENTIONS.md` правки не требуют — правило живёт в файле роли, который сборщик читает
  первым. `tools/hooks/selftest.sh` расширения не требует: новая проверка живёт отдельным
  скриптом шага 1, а не хуком, потому что срабатывает на буфере, а не на коммите.

---

## ADR-100: Расхождение продаж закрыто механизмом вне H1–H4: склад не удаляет исчезнувшие строки и не переставляет дату документа; H2 и H3 опровергнуты, H1 и H4 замером не затронуты по построению; мандат класса B на шаг 2 выдан поимённо

Контекст: шаг 1 `PARITY-SALES-DISCRIMINATE` (2026-08-01, класс A) локализовал разность `core` против `Σ demand.sum` по суткам мая-2026 и оставил ранжирование гипотез архитектору (`ADR-097 §3`). Настоящее решение вынесено БЕЗ новых обращений к источнику и без запросов к BigQuery: вся фактура снята чтением уже лежащих в репо артефактов шага 1, заголовков документов сессии сверки и снапшота кода `reference/code/cf-facts/`.

Решение:

§1 **Сироты.** `MERGE` (`bq_ops.py:237-305`) не имеет ветки удаления; позиция, удалённая в источнике задним числом, остаётся в `core` навсегда. За май-2026 таких строк ровно семь, что подтверждается двумя независимыми признаками: разностью числа строк по суткам (`2 + 2 + 3` на `2026-05-07` / `2026-05-09` / `2026-05-26`) и распределением `_loaded_at` (ровно семь майских строк не обновлены прогоном `2026-07-19`). Их сумма `1 630 240,60` покрывает всю целевую разность `1 630 239,87`, остаток `−0,73` есть округления по прочим суткам. accepted.

§2 **Замороженная дата.** `WHEN MATCHED THEN UPDATE SET` (`bq_ops.py:240-256`) не содержит `transaction_date`; дата ставится только при вставке (`bq_ops.py:258-305`). Документ `e98a7030-…` (`04292`, 24 позиции, `1 335 896,51`) числится в `core` двумя частями: `1 151 630,51` за `2026-05-26` и `184 266,00` за `2026-05-31`. На месячный итог влияния нет, суточный разрез искажён. accepted.

§3 **H2 опровергнута данными.** На 22 сутках мая из 26 совпадают и суммы, и число строк, во всех трёх валютах аккаунта. Ошибка формулы дала бы равномерное расхождение и не двигала бы число строк. `ADR-097 §3 (H2)` закрыт. accepted.

§4 **H3 опровергнута в заявленной форме, механизм заменён.** Данные не устарели: 1356 строк из 1363 переписаны `2026-07-19`. Дефект противоположный по смыслу — склад накапливает и не забывает. `ADR-097 §3 (H3)` закрыт с заменой формулировки, а не подтверждён. accepted.

§5 **H1 и H4 замером шага 1 не затронуты по построению.** Шаг 1 сравнивал две величины, обе построенные на `entity/demand`; H1 и H4 живут в паре «отчёт против документов» (`report − Σ demand.sum` = `1 341 450,14`). Обе остаются открытыми, обе остаются классом B. После §1-§2 предсказание H1 стало односторонним и точным: розница за май-2026 должна дать величину порядка `1 341 450,14`. accepted.

§6 **Порядок: подтверждающий замер перед правкой кода.** Совпадение до копейки и до строки есть сильный признак и одновременно ровно тот случай, когда подгонка выглядит так же, поэтому механизм подтверждается отдельным дешёвым замером `SALES-ORPHAN-CONFIRM` (класс A, два read-only запроса) ДО того, как под него правится боевой код. Вариант принят владельцем (чат 2026-08-01). accepted (владелец).

§7 **Правка загрузчика идёт одним патчем с задачей об окне обновления.** Три дефекта (окно без досева, отсутствие удаления, замороженная дата) живут в одной и той же операции `MERGE`; разнесение их по разным правкам дороже и рискованнее. Объединение принято владельцем (чат 2026-08-01). Три ловушки названы заранее и обязаны сниматься dry-run, а не рассуждением: задвоение при досеве (`ADR-097 §5`); ветка удаления, которая из-за условия окна внутри `ON` (`bq_ops.py:238`) формально считает «исчезнувшей» любую строку за пределами окна; неустановленная допустимость обновления колонки партиционирования внутри `MERGE`. accepted (владелец).

§8 **Поправка к артефакту шага 1, на числа не влияющая.** Заявление «`rate.value` присутствует у 5 из 127 документов, для остальных взят текущий курс» неверно: поле несут 63 документа, не несут ровно 64 сомовых, и заявленная подстановка ни разу не срабатывает. Итог `91 892 755,66` верен при обеих формулах. Артефакт датирован и не переписывается (`ADR-043`). accepted.

§9 **Blast radius ограничен одной таблицей.** `MERGE` применяется только к `core.fact_sales_profit`; закупки (`bq_ops.py:442-472`) и возвраты (`bq_ops.py:493+`) грузятся `WRITE_TRUNCATE` и описанных дефектов по построению не имеют. Их собственные свойства настоящим решением не оцениваются. accepted.

§10 **Мандат класса B на шаг 2 `PARITY-SALES-DISCRIMINATE` выдаётся поимённо настоящим параграфом** (прецедент формы `ADR-092 §1`, `ADR-079 §9`). Единственное основание класса B — доступ к секрету `msklad-token` (`ADR-076 §1`). Действуют ВСЕ шесть оговорок `ADR-076 §5` без изменений (токен не печатается; форма заголовка авторизации берётся из снапшота, а не изобретается; `grep -rF` по значению токена перед `git add`, любое совпадение есть стоп; поведение инструмента на неперечисленной команде фиксируется в session-блоке; эталон датируется снимком; правило-кандидат моста вносится в артефакт), плюс форма логирования `ADR-079 §6` (`date -u` и `gcloud auth list` первой И последней командой скрипта). Шаг только читает: ни живых конфигураций, ни деплоя, ни записи в МойСклад. Объём ровно два `GET`: `entity/retaildemand` за май-2026 (`meta.size` и сумма) и повтор `report/profit/bycounterparty` с `momentTo=2026-05-31 23:59:59`. Приёмка не переписывается — она уже стоит дословно в строке задачи (`ADR-097 §3`). Класс B не параллелится никогда (`ADR-082 §1`), поэтому исполнение идёт ПОСЛЕ `SALES-ORPHAN-CONFIRM`, а не одновременно с ним. Развилка объекта паритета (a/b/c, `ADR-097 §6`) сессией шага 2 НЕ задаётся ни при каком результате замера: она решается архитектором после, прецедент `ADR-088 §6/§7`. accepted (владелец, чат 2026-08-01).

Последствия:

[сейчас] `07_STATE.md` §Открытые вопросы — новая задача-строка `SALES-ORPHAN-CONFIRM` (текст дословно в `STATE_PATCH`).
[сейчас] `07_STATE.md` §Открытые вопросы, строка `SALES-REFRESH-WINDOW` — колонка «Как закрывается» заменяется целиком.
[сейчас] `07_STATE.md` §Открытые вопросы, строка `PARITY-SALES-DISCRIMINATE` — append об уточнении объёма шага 2 и о выданном мандате.
[сейчас] `07_STATE.md` §Открытые вопросы, строка `Q-98` — append о сужении.
[сейчас] `07_STATE.md` §Мандат Claude Code: класс задач — новая строка `SALES-ORPHAN-CONFIRM`; замена колонки «Основание» в строке `SALES-REFRESH-WINDOW, подготовка`; замена строки `PARITY-SALES-DISCRIMINATE, шаг 2` целиком («НЕ выдан» → «ВЫДАН поимённо `ADR-100 §10`») (парность `ADR-090 §1`).
[сейчас] `07_STATE.md` §Подробности для модели — абзац «Адъюдикация H1–H4».
[сейчас] `07_STATE.md` §Стенд-ап — заменяется целиком (`ADR-084 §1`).
[сейчас] `06_INDEX.md` — строка настоящего ADR.
[задачей SALES-ORPHAN-CONFIRM] Подтверждение §1 и §2 на живых данных `core`.
[задачей SALES-REFRESH-WINDOW] Патч трёх дефектов `MERGE` (§7) и снятие трёх ловушек.
[задачей PARITY-SALES-DISCRIMINATE] Сторона отчёта: H1 и H4 (§5), шаг 2 под мандатом §10, очередь после `SALES-ORPHAN-CONFIRM`.
[без правок] `reference/parity_sales_discriminate_2026-08-01.md` — датированный артефакт, поправка §8 живёт в журнале решений, не в нём. `briefs/` — отдельного брифа под шаг 2 настоящим решением не требуется, приёмка стоит в строке задачи; генерация брифа остаётся правом исполняющей стороны. `reference/parity_registry.md` строки 18-19 — правка правила-моста осмысленна только после выбора (a)/(b)/(c) (`ADR-097 §6`), настоящим решением не трогается. `ADR-097 §5` не пересматривается, §7 его расширяет составом патча, а не отменяет. `ADR-092` не пересматривается: он выдан другой задаче.

Статус: §1-§5, §8, §9 accepted; §6, §7, §10 accepted (владелец, чат 2026-08-01)


## ADR-101 · Адъюдикация неотгейченной очереди архитектора: `Q-103` закрыт мис-специфицированной приёмкой, а не дефектом механизма; `Q-91` закрывается правкой живого документа при неприкосновенном архиве; `Q-39` разделён по объектам; `Q-70` НЕ закрывается — найден непроверенный отрезок 23 суток; `Q-20` переформулирован — порог не нужен, проверка переносится в слой загрузки; `Q-92` сужен до одного бита


Контекст: владелец поручил (чат 2026-08-02) взять на ревью все строки реестра, которые требуют архитектора и при этом ничем не отгейчены — блоки (1)-(3) триажа `07_STATE §Подробности для модели` плюс решение уровня 0 без идентификатора. Разбор вынесен БЕЗ единого облачного вызова: вся фактура снята чтением файлов репо, каждый факт несёт адрес `файл:строка`. Полный разбор — `reference/ungated_queue_adj_2026-08-02.md`.

Решение:

§1 **`Q-103` CLOSED: механизм `ADR-100 §1` подтверждён, дефектной была приёмка.** `ADR-100 §1` назвал суммами строк-сирот числа `97 592,35` и `1 462 797,00`, которые в действительности есть разности по суткам из таблицы шага 1 (`reference/parity_sales_discriminate_2026-08-01.md:80/100`), то есть величины другой природы: разность суток включает копеечные остатки округления по ВСЕМ строкам этих суток, а не только по сиротам. Живая сумма семи сирот `1 630 239,8085` против целевой разности `1 630 239,87` даёт остаток `0,0615` KGS — механизм объясняет разность точнее, чем записано (`−0,73`). Расхождение `0,7915` лежит на стороне ожидания и симптомом не является. Класс дефекта уже назван проектом: критерий приёмки выведен из наблюдённого свойства данных вместо формулы (`ADR-075 §3`); отдельный `Q` не заводится (`ADR-051 §2`). Вариант 2 ревью-очереди (дополнительный замер) отклонён — он перепечатает те же семь строк, уже напечатанные построчно И агрегатом в одном логе; вариант 3 (правка текста `ADR-100`) отклонён по append-only (`05_CONVENTIONS.md:128-129`). Следствие: `SALES-REFRESH-WINDOW` разблокирована безусловно. **accepted (архитектор).**

§2 **`Q-91` CLOSED по форме: правится живой документ, архивный не трогается.** Прецедент прямой — `ADR-059 §2`: эпоха-зависимая формулировка поправлена в живом `02`, замороженный источник не переписан. `07_ARCHIVE.md` по `ADR-064` несёт то, что было известно на момент закрытия строки. Поправка идёт в `11_INFRA_FACTS.md §Ручные/разовые загрузки` и обязана разделять факт и вывод: отсутствие `sourceUris` ⇒ источник не GCS есть факт; «локальный NDJSON-файл на диске» есть вывод из этого признака, а механика соседнего загрузчика (`load_payments.py`, `load_table_from_json`, файла не возникает вовсе) показывает второе одинаково согласующееся прочтение. Какой приём применён к счетам — не установлено (`ADR-021 §2`). Отдельного ADR под сам факт не требуется: решением является только форма. Текст патча — в артефакте §2. **accepted (архитектор).**

§3 **`Q-39` разделён по объектам, как и требовал `ADR-020 §5`.** Живые схемы (`reference/schema_dump_2026-07-28.md`): `core.dim_employees` — 7 колонок без версионирования; `audit.dim_employees_snapshots` — 8 колонок, `snapshot_at` последней, то есть буквально конструкция `ADR-019 §1`. Довод `ADR-008 §3` («снапшот — единственный носитель истории») применим, основание `ADR-020 §5` («в `02` не документирован ⇒ решать нечего») снято закрытием `Q-4` 2026-07-28. Форма фикса не изобретается — вариант E `ADR-019 §3`; это не оптовое распространение по совпадению формы SQL, а тот же довод, проверенный на конкретном активе. `core.dim_counterparties` несёт SCD2 нативно, история уже в ядре, довод `ADR-008 §3` неприменим — чинить нечего, открытым остаётся ретайр против формы E, решение владельца (правка живого `transferConfig` есть класс B). Перед ретайром переподтвердить отсутствие потребителя у `audit.*` (факт 2026-07-17, не наследовать). Заводится `AUDIT-SNAPSHOT-FIX-EMPLOYEES`. `Q-37` не трогается (`ADR-019 §6`). **accepted (архитектор) в части квалификации; ретайр контрагентов — proposed (решение владельца).**

§4 **`Q-70` НЕ закрывается и остаётся суженным.** Различитель D1, исполненный владельцем 2026-07-28, покрывает `2026-04-25…2026-05-10`; окно застывания — `2026-05-01…2026-06-02`. Отрезок `2026-05-11…2026-06-02` (23 суток) источником не сверялся, и именно в нём D2 нашёл шесть ручных forward-fill `INSERT`, копирующих последний известный курс, — то есть значения нашей таблицы там появились копированием, что предсказывает гипотеза (A), а не опровергает её. Закрытие сейчас было бы распространением совпадения, установленного на входе в окно, на его середину, то есть выбором правдоподобной половины (`_METHOD §6`, прецедент `ADR-045 §1`). Остаток дёшев и полностью назван: файл источника уже в репо, читатель известен; класс A, гейтов нет. Вес вопроса — `292,56` USD (0,25 %) на майском эталоне по пересчитанной границе `[87.2319; 87.45]`. Заводится `FX-MAY-WINDOW-D1-TAIL`. Квалификация вынесена здесь потому, что `ADR-058 §7` закрепляет её за архитектором. **accepted (архитектор).**

§5 **`Q-20` переформулирован: нижний порог не нужен и не помог бы.** Ни `core.fact_sales_profit`, ни `core.fact_payments` не несут валюты документа, суммы в минорных единицах и применённого курса — конвертация выполняется в загрузчике (`fetch_demands.py:137-141`), в таблицу ложится только результат в KGS. Следовательно, внутри хранилища сравнивать не с чем, любая проверка там есть агрегатная эвристика, а ошибка «забыли `× rate`» построчная и затрагивает только валютные строки: гейт «эмпирическая калибровка» не имел исполнителя потому, что калибровка невозможна на этой схеме в принципе. Проверка переносится в слой, где валюта и курс есть на руках, — прямое применение закона `05_CONVENTIONS.md:146-148` / `_METHOD §4` (мерить там, где видно; чинить там, где причина). Форма: для каждой позиции либо валюта документа есть валюта аккаунта, либо применён `rate.value` (при отсутствии — курс по `ADR-010`); ответ бинарный, порога нет. Сохранение провенанса конвертации колонками таблиц фактов названо как отдельное, более позднее решение и здесь не принимается. Действующий агрегатный порог не снимается, но перестаёт считаться покрытием класса. Условная арифметика (при прочтении «средняя выручка на строку»): майские `93 522 995,53` на 1363 строках ⇒ ≈ `68 616` KGS/строка, регрессия `÷100` даёт ≈ `6,86` млн, что НИЖЕ порога `10` млн — проверка не сработала бы и на своей цели; утверждение условное, потому что текста проверки в репо нет (`CONTEXT GAP`, снимается шагом 1 новой задачи). Заводится `INGEST-CURRENCY-ASSERT`. **accepted (архитектор).**

§6 **`Q-92` сужен до одного бита; гейт `ADR-088 §6` снят фактом.** Ночные полосы за май-2026 пусты по обеим парам, значит численно три варианта сегодня неразличимы. Структурно же при вариантах (b) и (c) целевое правило суток для четырёх дефектных веток одно и то же — `DATE(M+6ч)`, бишкекские сутки, ровно то, что ветка продаж уже делает (`ADR-088 §3/§4`); только (a) требует `DATE(M+3ч)` и пересчёта `core.fact_sales_profit`. Поэтому подготовку `INGEST-MOMENT-ZONE-FIX` гейтит не выбор из трёх, а исключение (a). Рекомендация архитектора — исключить (a): единственный вариант, требующий переписать самую большую таблицу фактов при том, что механизма пересева ещё нет и он сам гейтится `ADR-065`; бишкекский бизнес на московских сутках ломает воспроизводимость для клиента в другую сторону; цена (a) с момента `ADR-088 §7` выросла — таблица получила два подтверждённых дефекта `MERGE` (`ADR-100 §1/§2`). Текст строки `Q-92` переписывается (устарел). Открытость сохраняется: пустота полос — свойство мая-2026, не правило. Само решение остаётся за владельцем, `ADR-088 §7` сохраняет статус `proposed`. **§6 accepted (архитектор) в части квалификации и сужения; выбор варианта — proposed (решение владельца).**

§7 **`Q-102` (правило `C2`) принимается по существу с одной поправкой.** Предложение зеркалит охват `C1` правильно, но молчит о второй половине дефекта: условие окна внутри `ON` (`bq_ops.py:238`) делает «исчезнувшей» любую строку за пределами окна, поэтому ветка удаления без собственного явного ограничения по дате опаснее исходного дефекта (`ADR-100 §7`, ловушка ii). Правило с поправкой — в `NEW_CONVENTIONS`. Приёмка идёт отдельным именованным коммитом, не через буфер (`ADR-081 §8`). **accepted (владелец, чат 2026-08-02).**

§8 **`Q-88` принимается по сути, но правило предлагается меньше и общее.** После `ADR-084 §1` стенд-ап заменяется целиком, поэтому точечный патч по его строкам бессмыслен по построению; запрета на такой блок при этом нет, а `_ASSEMBLER.md §6` определяет конфликт только МЕЖДУ блоками — внутриблоковое противоречие ловится лишь общим законом против импровизации, то есть структурно это тот же незакрытый канал, что разобран `ADR-099`. Предлагается не пункт в `05` Часть III, а пятый признак конфликта в `_ASSEMBLER.md §6`: правило становится общим (любая пара несовместимых указаний внутри одного блока), а машинерия остановки не меняется. Текст — в `NEW_CONVENTIONS`. **accepted (владелец, чат 2026-08-02).**

§9 **`Q-40`/`Q-84`/`Q-85` — одно решение о политике, а не три по существу.** Возражение, на котором стоят все три (`ADR-021 §4`: правка `_METHOD` задевает проекты за пределами holika), снято фактом, найденным ещё сессией `E1-T1-MECH-PREP-ADJ-2` и записанным в `reference/architect_review_queue_2026-07-26-2.md:75-79`: сам `_METHOD.md:146-148` несёт прецедент помеченного локального расширения (`§11 [РАСШИРЕНИЕ]` с провенанс-строкой). К помеченному расширению довод о чужих проектах не применяется: эталон `cb33fb4` не правится, чужие пины не ломаются. Рекомендация: принять политику один раз — локальное правило, сработавшее вне породившей его сессии хотя бы однажды, продвигается в `_METHOD` помеченным расширением. Это закрывает три строки одним проходом и заранее снимает четвёртую. Цена названа: `_METHOD` формально STABLE, это его первая правка после дистилляции, затрагиваются §6 и §8. **proposed (решение владельца).**

§10 **Природа зоны паритета во времени заводится строкой реестра.** Решение названо в плане последовательности (`PARITY-GAPS-ADJ`, «решение уровня 0»), идентификатора не имеет и в реестре не видно; сегодня оно принято умолчанием в пользу датированного снимка — окно `cf-facts` скользящее 90 суток, первые майские сутки вышли из него `2026-07-30`, весь май выйдет к `2026-08-29`, при этом 89 % суммы майских документов правились задним числом. Отсутствие строки есть часть дефекта: решение, принятое умолчанием, никто не пересматривает — тот же класс, что `Q-20`. Заводится `PARITY-ZONE-SNAPSHOT-DECISION`; решать его следует вместе с выбором варианта `SALES-REFRESH-WINDOW`, это одна развилка с двух сторон. **proposed (решение владельца).**

Последствия:
* [сейчас] `06_INDEX.md` — строка настоящего ADR
* [сейчас] `07_STATE.md` шапка (`updated_at`, `обновил`), блок `### Стенд-ап` (замена целиком), `§Подробности для модели` (append)
* [сейчас] `07_STATE.md §Открытые вопросы / GAP-реестр` — `Q-103` и `Q-91` CLOSED и переносятся в `07_ARCHIVE.md` дословно ЭТИМ ЖЕ коммитом (`ADR-064`); дописки к `Q-70`, `Q-39`, `Q-20`, `Q-6`; замена колонки «Как закрывается» у `Q-92`; четыре новые задача-строки (`FX-MAY-WINDOW-D1-TAIL`, `AUDIT-SNAPSHOT-FIX-EMPLOYEES`, `INGEST-CURRENCY-ASSERT`, `PARITY-ZONE-SNAPSHOT-DECISION`)
* [сейчас] `07_STATE.md` таблица `§Мандат Claude Code: класс задач` — шесть новых строк под четыре новые задачи (`ADR-090 §1`, парность). У `Q-103` и `Q-91` строк мандата нет и не было, при их архивации убирать нечего
* [сейчас] `07_ARCHIVE.md` — дословный перенос строк `Q-103` («закрыт `ADR-101 §1`: механизм подтверждён, расхождение `0,7915` лежало в мис-специфицированной приёмке») и `Q-91` («закрыт `ADR-101 §2`: поправка внесена в `11_INFRA_FACTS`, архивная строка неприкосновенна»)
* [сейчас] `11_INFRA_FACTS.md:114-115` — замена фрагмента о `bq load` по тексту `reference/ungated_queue_adj_2026-08-02.md §2` (факт и вывод разведены)
* [сейчас] `reference/ungated_queue_adj_2026-08-02.md` — артефакт сессии, закоммичен ею же
* [задачей FX-MAY-WINDOW-D1-TAIL] Сверка источника с `core.dim_fx_rates` за `2026-05-11…2026-06-02`; квалификация результата — архитектору (`ADR-058 §7`)
* [задачей AUDIT-SNAPSHOT-FIX-EMPLOYEES] Пересоздание `audit.dim_employees_snapshots` формой E и правка запроса; исполнение класс B, мандат не выдан
* [задачей INGEST-CURRENCY-ASSERT] Съём исходника `cf-dq` (закрывает `CONTEXT GAP` и `Q-6`), подготовка проверки конвертации в слое загрузки, деплой класс B
* [отдельным коммитом] `05_CONVENTIONS.md` Часть II (правило `C2`, §7) и `_ASSEMBLER.md §6` (пятый признак конфликта, §8) — **апрув владельца получен (чат 2026-08-02)**; правки этих файлов идут вне буфера (`ADR-081 §8`) и вносятся ПОСЛЕ присвоения номера настоящему ADR, потому что номер входит в заголовок `C2` и в мотив пятого признака (прецедент сиквенса: `ADR-081`/`ADR-082`, чьи правки пришли одним коммитом после своей сборки)
* [без правок] `06_DECISIONS_LOG.md` — `ADR-100`, `ADR-088`, `ADR-020`, `ADR-019`, `ADR-021`, `ADR-058`, `ADR-059` не редактируются (append-only); их уточнения внесены §§ настоящего ADR
* [без правок] `03_PIPELINE_SPEC.md:76` — строка порога `currency_normalization` не трогается до результата шага 1 `INGEST-CURRENCY-ASSERT`: текста проверки в репо нет, правка была бы догадкой
* [без правок] `reference/parity_sales_discriminate_2026-08-01.md`, `reference/sales_orphan_confirm_2026-08-01.md` — датированные артефакты, не переписываются (`ADR-043`); поправка живёт в §1

Статус: **§1–§10 accepted.** §1–§5 — архитектор, ратифицированы владельцем в чате 2026-08-02 («принято» по шести пунктам: §1, §2, §3-квалификация, §4, §7, §8). §7, §8 — владелец, чат 2026-08-02. §3 в части ретайра снимка контрагентов, §9 (политика продвижения локальных правил), §10 (зона паритета — датированный снимок сейчас, режим досева как цель) — владелец, чат 2026-08-02, вторым ходом сессии. §6 — архитектор в части квалификации и сужения; выбор варианта переквалифицирован владельцем в предложение клиенту (`MSKLAD-TZ-PROPOSAL`), fallback при отказе — вариант (c) `ADR-088 §7`; подготовка `INGEST-MOMENT-ZONE-FIX` разгейчена независимо от ответа клиента. §10 несёт **`superseded-in-part` к `ADR-067`** в части формулировки финишной черты. Мандат класса B настоящим ADR не выдаётся ни одной задаче.

---

## ADR-102: Проверка параллельности получает второй источник набора файлов — поле `Пишет:` таблицы мандата; класс B запрещается механически; `ADR-090 §3` superseded-in-part в части носителя объявления

Контекст: `ADR-082 §1` требует прогона `tools/parallel_check.sh` перед одновременным запуском двух задач, а `ADR-086 §1` разрешает исполнять задачу без брифа. Скрипт читал набор файлов только из `briefs/<TASK>.md`, поэтому для задач без брифа возвращал `RC 3` и правило параллельности оставалось без исполняющего механизма для целого класса сессий (`SALES-ORPHAN-CONFIRM`, `STATE-COMPACT`, `UNGATED-QUEUE-ADJ`). Дефект найден проверками после сборки 2026-08-02 и подтверждён прогоном на живой задаче. Полный разбор — `reference/parallel_check_no_brief_2026-08-02.md`.

Решение:

§1 **Носитель объявления для задач без брифа — таблица `07_STATE §Мандат Claude Code`, поле `Пишет:`.** `ADR-090 §3` называл носителем «текст запуска», и это `superseded-in-part`: текст запуска не является файлом репо, машинно непроверяем и до старта сессии нигде не сохраняется. Рекомендация сессии `UNGATED-QUEUE-ADJ` («читать набор из session-блока») отклонена по механизму: блок пишется в конце сессии, а проверка нужна до её запуска. Таблица мандата этим требованиям удовлетворяет: она лежит в репо, заполняется по действующему правилу и её парность со строкой реестра уже обязательна (`ADR-090 §1`). accepted (архитектор).

§2 **Приоритет источников: бриф, затем таблица.** Бриф задача-специфичен и точнее; таблица — запасной носитель. Источник печатается в выводе по каждой задаче, иначе вердикт непроверяем (`ADR-044`). Читаются ВСЕ строки задачи, включая шаговые (`<TASK>, шаг 2a`): набор задачи есть объединение наборов её шагов. accepted (архитектор).

§3 **Класс задачи запрещает параллель механически.** Задача, все строки которой класса B, даёт `RC 1` — прежний вердикт скрипта отсылал к ручной проверке «отдельно по `07_STATE`», то есть правило `ADR-082 §1` держалось на памяти исполнителя. Смешанный класс (`A/B`) печатает предупреждение и разбирается по файлам: запуск может касаться шага класса A, и отказ по всей задаче был бы неверен. Класс, который установить не удалось, печатается как неустановленный и на вердикт НЕ влияет — отсутствие данных фактом не является (`ADR-021 §2`). accepted (архитектор).

§4 **Подавлены два источника ложных пересечений.** Плейсхолдер `<TASK>` раскрывается в имя задачи, иначе любые две задачи ложно пересекались бы на общем шаблоне `reference/_scratch_<TASK>_<date>/`. Из хвоста `Пишет:` отбрасываются токены с пробелом или знаком параграфа: рядом с путями там стоят ссылки вида `` `ADR-081 §8` ``, и две задачи, цитирующие один ADR, дали бы пересечение, то есть заблокировали бы законный запуск. Ложное срабатывание здесь не безобидно: оно учит игнорировать проверку. accepted (архитектор).

§5 **Отказ остаётся отказом.** Нет ни брифа, ни строк мандата — `RC 3`; источник найден, но набор пуст — `RC 3`. Тихого пропуска нет ни в одной ветке (`ADR-021 §2`). accepted (архитектор).

§6 **Приёмка — шесть кейсов по РЕАЛЬНОМУ содержимому репо, синтетической фикстуры нет намеренно** (`05_CONVENTIONS` Часть I, расширение `ADR-074`: замер, обосновывающий механическую проверку, обязан идти по реальному содержимому, иначе доказано лишь, что проверка умеет краснеть). Ожидание и факт совпали во всех шести: `RC` 0/1/3/0/3/1. Лог — `reference/_scratch_PARALLEL-CHECK-NO-BRIEF_2026-08-02/run_tests.log`. Отдельный самотест по образцу `tools/hooks/selftest.sh` НЕ заводится: у хука он оправдан тем, что хук молча пропускает коммиты, а здесь отказ громкий и виден в выводе. accepted (архитектор).

§7 **Побочная находка: пять строк мандата класса A из 18 не несут поля `Пишет:`.** Две дозаполняются этим же блоком из уже существующего текста репо; две (`ORACLE-AUTOMATION, написание скрипта`; `CC-READONLY-PRINCIPAL, шаги 2-3, подготовка патча`) НЕ дозаполняются — путей в репо нет, подстановка правдоподобного была бы импровизацией (`_METHOD §6`); одна относится к DONE-задаче. Отныне строка мандата без `Пишет:` есть отсутствие входа у механической проверки, а не мелочь оформления. accepted (архитектор).

Последствия:
* [отдельным коммитом] `tools/parallel_check.sh` — правка внесена сессией поимённо (`ADR-081 §8`, `tools/` под буфер не подпадает), коммит предшествует настоящему блоку в той же ветке
* [сейчас] `06_INDEX.md` — строка настоящего ADR
* [сейчас] `07_STATE.md` шапка, блок `### Стенд-ап` (замена целиком), `§Подробности для модели` (append)
* [сейчас] `07_STATE.md` таблица `§Мандат Claude Code` — дозаполнение поля `Пишет:` в двух строках (`INGEST-MOMENT-ZONE-FIX, подготовка патча`; `CC-READONLY-PRINCIPAL, шаг 1`) плюс замена предложения о носителе в примечании над таблицей (`07_STATE.md:372`, текст в `STATE_PATCH`)
* [без правок] `08_TASK_BRIEF_TEMPLATE.md:26-32` и `09_RUNBOOK.md:161/221` — единственные места вне решений, называющие проверку; проверено чтением: они описывают поле «Файлы на запись» и форму вызова, утверждения «читается только из брифа» не несут, поэтому правки не требуют
* [без правок] `05_CONVENTIONS.md` — `grep parallel_check` даёт 0 совпадений, текста правила там нет
* [сейчас] `07_ARCHIVE.md` — строка `PARALLEL-CHECK-NO-BRIEF` заводится сразу закрытой, минуя `07_STATE.md` (`ADR-064`); парная строка мандата не заводится, потому что создавалась и убиралась бы одним коммитом (`ADR-090 §1`)
* [сейчас] `reference/parallel_check_no_brief_2026-08-02.md` и `reference/_scratch_PARALLEL-CHECK-NO-BRIEF_2026-08-02/` — артефакт и провенанс, закоммичены сессией
* [задачей ORACLE-AUTOMATION] Объявление набора файлов на запись при взятии задачи (§7)
* [задачей CC-READONLY-PRINCIPAL] То же для шагов 2-3 (§7)
* [без правок] `ADR-090` — текст не редактируется (append-only), уточнение носителя внесено `§1` настоящего решения

Статус: §1–§7 accepted (архитектор). Мандат класса B настоящим ADR не выдаётся.

## ADR-103: Расхождение продаж разложено полностью — H4 опровергнута, H1 подтверждена с содержанием шире записанного (три составляющих периметра, одна встречного знака); найден дефект признания выручки по отгрузке комиссионеру; варианты объекта паритета приведены к фактам

**Контекст:** `07_STATE.md:23` поручал архитекторскую адъюдикацию H1/H4 и `Q-98` по итогам обоих шагов `PARITY-SALES-DISCRIMINATE`; развилка объекта паритета (`ADR-097 §6`) явно оставлена архитектору (`ADR-100 §10`). Шаг 2 оставил два неквалифицированных наблюдения: расхождение счётчика на `6` документов и остаток суммы `153 028,14` KGS. Настоящее решение вынесено БЕЗ единого облачного вызова и без обращений к BigQuery: вся фактура снята чтением репо плюс офлайн-счётом по уже лежащим в репо телам ответов. Полный разбор — `reference/sales_h1h4_adj_2026-08-02.md`.

**Решение:**

§1 **H4 опровергнута фактом, не отложена.** Повтор `report/profit/bycounterparty` с симметричной верхней границей `momentTo=2026-05-31 23:59:59` дал ту же сумму `93 234 205,80`, разность `0,00` (`reference/parity_sales_discriminate_step2_2026-08-02.md:60-67`). Документа ровно в полночь в мае нет. `ADR-097 §3 (H4)` закрыт. **accepted (архитектор).**

§2 **H1 подтверждена, её содержание шире записанного.** `ADR-097 §3 (H1)` предсказывал одну составляющую («розница ≈ `1 341 450,14`») и одно направление (отчёт шире ингеста). Разложение обеих сторон по контрагенту (замер этой сессии, офлайн) даёт три составляющих, из них одна встречного знака: розница `+1 188 422,00` (506 док.); продажи по отчётам комиссионера `+2 133 028,14` (7 док.); отгрузка комиссионеру `−1 980 000,00` (1 док., `entity/demand` `04235`). Числовое предсказание не подтвердилось (розница даёт `88,6 %` предсказанного), сама гипотеза верна. Реконструкция сходится до копейки и до документа: `93 234 205,80 = (91 892 755,66 − 1 980 000,00) + 1 188 422,00 + 2 133 028,14`; `639 = (127 − 1) + 506 + 7`. **accepted (архитектор).**

§3 **Остаток счётчика и остаток суммы — одно наблюдение, а не два.** Из 64 контрагентов отчёта расхождение сверх копеечных округлений дают ровно три, и их `salesCount` в сумме равен `7` — ровно остатку счётчика. Контрагентов, присутствующих на стороне документов и отсутствующих в отчёте, нет. Прочие 61 контрагент сходятся с максимальным отклонением `0,81` KGS и суммарным `0,06`. **accepted (архитектор).**

§4 **Приписка семи документов отчётам комиссионера — по четырём признакам, не по одному.** (i) `Q-51` (`07_ARCHIVE.md:82`) фиксирует фактом `7` документов `entity/commissionreportin` за май-2026 — точное совпадение с остатком счётчика; (ii) все три контрагента — маркетплейсы, канал в проекте уже заведён (`ADR-024`), вознаграждение за май верифицировано против оракула — `438 729,42` KGS (`07_STATE.md:520`), то есть `20,6 %` приписываемых продаж; (iii) отгрузка ООО «РВБ» на `1 980 000,00` в отчёте отсутствует при `sellSum` `2 783,00` и `salesCount` `1` — вычитание отгрузки есть единственное прочтение, совместимое одновременно и с суммой, и со счётчиком; (iv) `grep -rn "retaildemand\|commissionreportin"` по снапшоту `reference/code/cf-facts/` даёт ноль совпадений — ингест продаж читает только `entity/demand` (расширение факта `ADR-097 §2` на второй тип). **accepted (архитектор).**

§5 **`Q-98` разложен полностью, но НЕ закрывается настоящим решением.** Обе половины `0,31 %` объяснены механизмами: сторона `core` — строки-сироты (`ADR-100 §1`, `ADR-101 §1`), сторона отчёта — периметр (§2). Ни одна из четырёх гипотез `ADR-097 §3` не осталась открытой. Закрытие гейтится подтверждающим замером: сумма `2 133 028,14` получена как ОСТАТОК, а не измерена, и это ровно тот случай, где подгонка выглядит так же (`ADR-100 §6`). Строка `Q-98` переформулируется, статус остаётся ОТКРЫТ. **accepted (архитектор).**

§6 **Форма подтверждающего замера разрезана по классу, и бо́льшая половина мандата не требует.** `core.fact_commissionreportin` суммы продаж не несёт: схема `reference/code/cf-loss-commission/main.py:170-180` хранит `reward_sum_kgs`, `commission_overhead_sum_kgs` и их сумму; загрузчик (`main.py:128-151`) агрегирует `reward` с уровня позиций (`ADR-026 §1`), `price × quantity` не сохраняет. Отсюда часть A (класс A, один read-only запрос к BigQuery) закрывает три бита из четырёх — число документов, множество `agent_id`, разбивку `3/3/1`; часть B (класс B, один `GET` с `expand=positions`) закрывает сумму. Приёмка обеих частей выведена формулой, не свойством данных (`ADR-075 §3`), и печатает строки (`ADR-044`). Заводится задачей `SALES-PERIMETER-CONFIRM`. Мандат класса B настоящим решением НЕ выдаётся. **accepted (архитектор).**

§7 **Найден дефект вне всех четырёх гипотез: отгрузка комиссионеру признаётся выручкой BI.** `ADR-097 §3 (H1)` формулировал периметр односторонне (наш ЛИБО равен, ЛИБО у́же); замер нашёл встречное — документ, который есть у нас и которого нет у отчёта. Товар, переданный на реализацию, попадает в `core.fact_sales_profit.revenue_kgs` как выручка месяца отгрузки; МойСклад продажей его не считает, а эталон определён как то, что рисует интерфейс (`ADR-067 §1`). Величина за май-2026 — `1 980 000,00` KGS (`2,1 %` месячной выручки, больше всего розничного канала), направление — завышение, поверхность — инвесторская. Это дефект по существу, а не выбор периметра: он переживает варианты (b) и (c), а при (b) ещё и прикрывается сходимостью. Заводится задачей `SALES-CONSIGNMENT-REVENUE`; форма фикса настоящим решением не назначается. **accepted (архитектор).**

§8 **Варианты `ADR-097 §6` приведены к фактам; выбор остаётся владельцу.** (a) Один оракул — цена теперь известна точно: три правки периметра (`entity/retaildemand`; продажи `entity/commissionreportin`; исключение отгрузок комиссионерам) плюс досев мая; класс B, гейтится `ADR-065`; форма — отдельный ингест по прецеденту `ADR-024`, не внутри патча `MERGE` из `ADR-100 §7`. (b) Сузить периметр обеих сторон — оснований не осталось: уровень (i) не просто доступен, он разложен до копейки, довод о непрозрачности снят фактом, а сам вариант прикрывает дефект §7 сходимостью; `ADR-067 §2` требует явного обоснования понижения уровня, обоснования нет. **Не рекомендуется.** (c) Объявленное отступление — записанная форма («розница вне периметра») фактам больше не соответствует, она покрывает `1 188 422,00` из `1 341 450,14`; приведённая к фактам форма обязана называть все три составляющие, и её третья клауза есть дефект §7 в виде правила, то есть решение об учётной политике на инвесторской поверхности, а не о периметре. **Рекомендация архитектора — (a)**, с разрезом на три задачи в порядке: подтверждающий замер §6 часть A → `SALES-CONSIGNMENT-REVENUE` (не ждёт выбора) → `SALES-PERIMETER-EXTEND` (ждёт выбора). Обоснование: финишная черта есть числовой паритет с тем, что рисует интерфейс клиенту (`00_CHARTER §критерии успеха`, `ADR-067`); из трёх вариантов только (a) её исполняет. **§8 accepted (архитектор) в части приведения вариантов к фактам и снятия (b); ВЫБРАН ВАРИАНТ (a) — accepted (владелец, чат 2026-08-02).** Следствия выбора: объектом паритета продаж объявляется число `report/profit/*`, любое расхождение есть дефект нашей стороны; развилка `ADR-097 §6` закрыта и из «Развилок на владельце» снимается; `SALES-PERIMETER-EXTEND` разгейчена по линии решения и остаётся гейченной только подтверждающим замером; правило-мост `reference/parity_registry.md` строки 18-19 подлежит правке (пункт последствий ниже), §9 в этой части исполнен выбором.

§9 **Что настоящим решением не делается.** `reference/parity_registry.md` строки 18-19 этой СЕССИЕЙ не правятся, но после выбора владельцем варианта (a) правка стала осмысленной и адресована задачей (`ADR-097 §6`, блок последствий `ADR-100` — их оговорка «только после выбора» исполнена). `ADR-100 §9` (blast radius одной таблицы) не пересматривается. `ADR-100 §7` (три дефекта `MERGE` одним патчем) не расширяется периметром — см. §8. Исторический масштаб дефекта §7 вне мая-2026 не замерялся и не предполагается. **accepted (архитектор).**

**Последствия (blast radius):**

[сейчас] `07_STATE.md` §Открытые вопросы, строка `Q-98` — колонка «Как закрывается» получает абзац переформулировки (текст дословно в `STATE_PATCH`); строка остаётся ОТКРЫТОЙ.
[сейчас] `07_STATE.md` §Открытые вопросы, строка `PARITY-SALES-DISCRIMINATE` — DONE целиком, перенос в `07_ARCHIVE.md` тем же коммитом (`ADR-064`).
[сейчас] `07_STATE.md` §Открытые вопросы — три новые задача-строки: `SALES-PERIMETER-CONFIRM`, `SALES-CONSIGNMENT-REVENUE`, `SALES-PERIMETER-EXTEND` (тексты дословно в `STATE_PATCH`).
[сейчас] `07_STATE.md` §Мандат Claude Code: класс задач — четыре правки парностью `ADR-090 §1`: новые строки `SALES-PERIMETER-CONFIRM, часть A` (A, параллель нет, постоянный, один read-only запрос к BigQuery; пишет `reference/sales_perimeter_confirm_<date>.md`, `reference/_scratch_SALES-PERIMETER-CONFIRM_<date>/`), `SALES-PERIMETER-CONFIRM, часть B` (B, параллель нет, мандат НЕ выдан, основание — `msklad-token`, `ADR-076 §1`), `SALES-CONSIGNMENT-REVENUE, подготовка` (A, параллель нет, гейтится частью A замера; правит снапшот `reference/code/cf-facts/`, ничего не деплоит), `SALES-PERIMETER-EXTEND, подготовка` (A, параллель нет, гейтится выбором владельца §8); убирается строка `PARITY-SALES-DISCRIMINATE, шаг 1` и `шаг 2` вместе с уходом задачи в архив.
[сейчас] `07_STATE.md` §Подробности для модели — абзац «Адъюдикация H1/H4 и `Q-98`».
[сейчас] `07_STATE.md` §Стенд-ап — заменяется целиком (`ADR-084 §1`).
[сейчас] `06_INDEX.md` — строка настоящего ADR.
[задачей SALES-PERIMETER-CONFIRM] Подтверждение приписки §4 и исторический масштаб дефекта §7.
[задачей SALES-CONSIGNMENT-REVENUE] Фикс признания выручки по отгрузке комиссионеру.
[задачей SALES-PERIMETER-EXTEND] Расширение периметра ингеста (выбор владельца — вариант (a), сделан) плюс правка правила-моста `reference/parity_registry.md` строки 18-19 под объявленный объект паритета.
[без правок] `reference/parity_sales_discriminate_2026-08-01.md` и `reference/parity_sales_discriminate_step2_2026-08-02.md` — датированные артефакты, не переписываются (`ADR-043`); квалификация их наблюдений живёт в журнале решений. `ADR-097 §5`, `ADR-100 §7`, `ADR-100 §9`, `ADR-101` не пересматриваются. `reference/code/cf-facts/` и `reference/code/cf-loss-commission/` — снапшоты, настоящим решением не правятся.

**Статус:** §1-§7, §9 accepted (архитектор); §8 accepted (архитектор — приведение вариантов к фактам и снятие (b); владелец, чат 2026-08-02 — выбор варианта (a)).

## ADR-104: `Q-98` закрыт — приписка остатка стороны отчёта отчётам комиссионера подтверждена измерением, а не вычитанием; мандат класса B на часть B зафиксирован; правило-кандидат моста по сумме документа комиссионера внесено

**Контекст:** решение сессии `PARITY-SALES-H1H4-ADJ` (тот же буфер) разложило сторону отчёта на три составляющих и оставило один незакрытый бит: сумма `2 133 028,14` KGS была получена как остаток, а не измерена, что есть ровно тот случай, где подгонка выглядит так же (`ADR-100 §6`). Настоящее решение фиксирует итог подтверждающего замера. Полный расчёт — `reference/sales_perimeter_confirm_2026-08-02.md`.

**Решение:**

§1 **Мандат класса B на часть `SALES-PERIMETER-CONFIRM` фиксируется постфактум.** Он выдан владельцем в чате 2026-08-02, а не ADR-параграфом заранее, потому что задача была заведена и исполнена внутри одного дня; форма прецедента (`ADR-092 §1`, `ADR-079 §9`, `ADR-100 §10`) сохранена по существу: единственное основание класса B — доступ к секрету `msklad-token` (`ADR-076 §1`); объём ровно один `GET` на чтение; все шесть оговорок `ADR-076 §5` соблюдены и их статус перечислен в артефакте; форма логирования `ADR-079 §6` исполнена; объявление действия отдельным сообщением до гейта (`ADR-077 §6`) исполнено. **accepted (владелец, чат 2026-08-02).**

§2 **Приписка подтверждена всеми четырьмя битами.** Часть A: `7` документов, множество `agent_id` равно трём ожидаемым, разбивка `3 / 3 / 1`. Часть B: сумма `2 133 028,08` KGS против ожидания `2 133 028,14` (Δ `−0,06`, копеечные округления), разбивка по контрагентам `2 111 374,17 / 18 870,91 / 2 783,00` совпала до копейки по каждому из трёх. Совпадение не есть результат выбора удобной формулы: печатались все три кандидата денежной величины документа (`sum`, `commitentSum`, `Σ price × quantity`), совпали два тождественных между собой, третий разошёлся на величину вознаграждения. **accepted (архитектор).**

§3 **`Q-98` CLOSED.** Записанные `288 789,73` KGS (`0,31 %`) объяснены полностью как разность двух встречных смещений, каждое из которых измерено: `core − Σ demand.sum` = `1 630 239,87` — строки-сироты `MERGE` без ветки удаления (`ADR-100 §1`, `ADR-101 §1`, подтверждено `SALES-ORPHAN-CONFIRM`); `report − Σ demand.sum` = `1 341 450,14` — периметр типов документов из трёх составляющих (розница `+1 188 422,00`, продажи комиссионера `+2 133 028,08`, отгрузка комиссионеру `−1 980 000,00`), подтверждён настоящим замером. Остаток обеих реконструкций — копейки округления. Строка переносится в `07_ARCHIVE.md` тем же коммитом (`ADR-064`). Открытыми остаются не вопросы причины, а задачи фикса: `SALES-CONSIGNMENT-REVENUE` и `SALES-PERIMETER-EXTEND`. **accepted (архитектор).**

§4 **Правило-кандидат моста для расширения периметра.** Отчёт `report/profit/*` считает продажи по отчёту комиссионера полной суммой документа (`sum`, тождественно `Σ price × quantity` позиций), а НЕ суммой комитента (`commitentSum`); разница есть вознаграждение комиссионера (`63 311,00` KGS за май-2026, `3,0 %` оборота канала). Ингест, взявший `commitentSum`, занизит выручку ровно на вознаграждение и получит расхождение, читающееся как мелкая ошибка канала. Правило вносится в артефакт как КАНДИДАТ (`ADR-076 §5`, оговорка 6); его место в `reference/parity_registry.md` определяет `SALES-PERIMETER-EXTEND`, не настоящее решение. **accepted (архитектор).**

§5 **Масштаб дефекта признания выручки замерен и оказался единичным.** За всю зону паритета (`2026-05-01` … `2026-08-01`, `7 120` строк, `293 247 739,41` KGS) отгрузок контрагентам-маркетплейсам ровно одна — `2026-05-12`, `1 980 000,00` KGS, две строки позиций. Доля — `0,675 %` зоны паритета и `2,1 %` мая-2026. `SALES-CONSIGNMENT-REVENUE` разгейчена этим фактом; форма фикса настоящим решением не назначается. **accepted (архитектор).**

§6 **Процессное следствие: схему целевой таблицы брать из контрактов, не из снапшота кода.** Три запроса первого прогона упали на `Unrecognized name: demand_id`, потому что схема бралась из `reference/code/cf-facts/bq_ops.py:46-63`, а это `STAGING_SCHEMA` таблицы `stg_msklad.fact_sales_staging` (`bq_ops.py:82`/`113`), не `core.fact_sales_profit`. Живая форма core-таблицы документирована в `02_ERP_CONTRACTS.md:36-49`, включая `transaction_id | STRING | MD5(demand_id + position_id)`. Отказ был громким, вердикта на неполных данных не выносилось. Класс тот же, что `★ Работа на фактах`: факт лежал в репо, поднят был не тот файл. Отдельного `Q` не заводится (`ADR-051 §2`), правило фиксируется этим параграфом и абзацем «Подробности для модели». **accepted (архитектор).**

**Последствия (blast radius):**

[сейчас] `07_STATE.md` §Открытые вопросы, строка `Q-98` — CLOSED, дословный перенос в `07_ARCHIVE.md` тем же коммитом с выжимкой «закрыт чем» по §3 (`ADR-064`).
[сейчас] `07_STATE.md` §Открытые вопросы, строка `SALES-PERIMETER-CONFIRM` (заводится буфером `PARITY-SALES-H1H4-ADJ`) — DONE целиком, в `07_STATE` не оседает, уходит в `07_ARCHIVE.md` тем же коммитом.
[сейчас] `07_STATE.md` §Открытые вопросы, строки `SALES-CONSIGNMENT-REVENUE` и `SALES-PERIMETER-EXTEND` — колонка «Как закрывается» получает пометку «гейт замера снят, разгейчена» (§5 и §2 соответственно).
[сейчас] `07_STATE.md` §Мандат Claude Code: класс задач — строки `SALES-PERIMETER-CONFIRM, часть A` и `часть B` убираются вместе с уходом задачи в архив (парность `ADR-090 §1`).
[сейчас] `07_STATE.md` §Подробности для модели — абзац «`SALES-PERIMETER-CONFIRM`, части A и B».
[сейчас] `07_STATE.md` §Стенд-ап — заменяется целиком (`ADR-084 §1`).
[сейчас] `06_INDEX.md` — строка настоящего ADR.
[задачей SALES-PERIMETER-EXTEND] Внесение правила §4 в `reference/parity_registry.md` вместе с правкой правила-моста строк 18-19 под объявленный объект паритета.
[задачей SALES-CONSIGNMENT-REVENUE] Фикс признания выручки по отгрузке комиссионеру.
[без правок] `02_ERP_CONTRACTS.md` — §6 не меняет контракт, а называет, что читать его надо оттуда; схема уже верна. `reference/code/cf-facts/` и `reference/code/cf-loss-commission/` — снапшоты, не правятся. `reference/parity_registry.md` — см. пункт задачи выше. `ADR-100`, `ADR-101`, `ADR-097 §5` не пересматриваются.

**Статус:** §1 accepted (владелец, чат 2026-08-02); §2-§6 accepted (архитектор).

## ADR-105: `§Подробности для модели` получает назначение, пункт назначения и (вторым шагом) бюджет; «длина не ограничена» отменяется

**Контекст.** `ADR-084 §1` учредил раздел словами «`### Подробности для модели` дописывается,
длина не ограничена», зеркально `05_CONVENTIONS.md:244` и `_ASSEMBLER.md §Шаг 4`. Раздел получил
при рождении три свойства сразу: append-only, снятый лимит, отсутствие пункта назначения при
устаревании. Замер 2026-08-02 на базе `7dc7d99`: раздел — 89 050 симв., 50,8 % файла; GAP-реестр
— ещё 39,6 %, вместе 90,4 %. Файл вырос с 83 285 до 273 901 байта за пять дней (×3,3).
Единственное снижение за всю историю (`STATE-COMPACT`, −15 559 байт) отыграно обратно за
тридцать часов, то есть разовая уборка без правила проверена и не работает. Раздел раздут
не старым осадком, а темпом записи: 55 % объёма написано за два последних дня, поэтому правило
вида «храним последние N дней» задачу не решает. Структурное подтверждение причины: в
`07_ARCHIVE.md` есть разделы под Epic M, под закрытые вопросы и под снятые блокеры — и нет
раздела под устаревшие «Подробности», то есть сборщику некуда положить абзац, даже если он
захочет. `07_STATE` читается целиком на старте каждой сессии (`CLAUDE.md §Старт` п.2,
`_ARCHITECT.md §3`, `_METHOD §5`), сессий за пять дней — 41. Полный разбор с пятью вариантами —
`reference/state_details_bloat_adj_2026-08-02.md`; выбор варианта — владелец, чат 2026-08-02.

**Решение.**

§1 **Назначение раздела переопределяется.** `### Подробности для модели` есть оперативная память
для СЛЕДУЮЩЕЙ сессии, а не журнал сделанного. Критерий записи — один вопрос: «сломается ли
следующая сессия, если этого не прочтёт?». «Как мы это установили» уходит в артефакт
`/reference`; в `07_STATE` остаётся следствие плюс ссылка. Форма уже канон де-факто — 34 из 90
абзацев так и написаны.

§2 **Побочный продукт в раздел не пишется.** Правило `ADR-084 §3` / `_ASSEMBLER.md §Шаг 4`
«остаток отброшенного стенд-апа идёт в Подробности» отменяется: стенд-апы неглавных блоков
отбрасываются полностью, а их остаток либо проходит критерий §1 и пишется обычным абзацем от
своего имени, либо не пишется вовсе. Записи о генерации брифов не пишутся — `briefs/<TASK>.md`
самодостаточен и жив.

§3 **Раздел получает пункт назначения.** В `07_ARCHIVE.md` заводится раздел
`## Подробности для модели — снятые абзацы (полный текст)`. Абзац, чей предмет полностью закрыт
(строка уехала в `07_ARCHIVE`, либо ADR принят и все его последствия `[сейчас]` применены),
переезжает **дословно** тем же коммитом сборки. Симметрия `ADR-064` для реестров, распространённая
на прозу. **Оговорка, выведенная из первого прохода:** абзац, несущий живое предупреждение
открытой строке реестра, не переносится, даже если его собственный предмет закрыт — иначе вынос
забирает предупреждение у живой задачи.

§4 **`ADR-084 §1` в части «длина не ограничена» — superseded-in-part.** Немедленного числового
лимита настоящий ADR НЕ назначает: число обязано быть замером, а не назначением
(`★ Anti-improvisation`). Лимит назначается задачей `STATE-DETAILS-BUDGET` после 3–5 сборок под
§1–§3, и тем же решением распространяется на максимальную длину строки GAP-реестра — иначе
создаётся канал перетекания текста в реестр, у которого пункт назначения есть (`ADR-064`), а
лимита длины строки нет (максимум сейчас 4 358 симв. при медиане 908).

§5 **Первое применение §3 обязательно и исполняется тем же коммитом.** Правило вытеснения без
первого прохода бессмысленно. Проход подготовлен `STATE-DETAILS-ADJ`: 44 абзаца к выносу,
46 остаются, каждое решение с основанием — `reference/state_details_patch_2026-08-02.md`,
список `reference/_scratch_STATE-DETAILS-ADJ_2026-08-02/evict_list.tsv`, применение
`apply_evict.py --apply`. Замер режима `--check`: `07_STATE.md` 175 453 → 126 582 симв.
(−27,9 %), раздел 89 050 → 40 179 симв. (−54,9 %), 90 → 46 абзацев.

§6 **Правило прохода вытеснения — бинарное.** Абзац либо остаётся как есть, либо уезжает
дословно; переписывание и сокращение чужих абзацев при вытеснении запрещено. Мотив: потерять
знание дословным переносом нельзя, переписыванием можно, а «сборщик вынес не то» есть главный
названный риск этого варианта.

**Последствия (blast radius):**

[сейчас] `06_DECISIONS_LOG.md` — настоящий ADR; `06_INDEX.md` — строка.
[сейчас] `07_STATE.md` строка 29 — врезка раздела заменяется литералом
`reference/state_details_patch_2026-08-02.md §2`.
[сейчас] `05_CONVENTIONS.md` строка 244 — «здесь длина не ограничена» заменяется литералом
того же патча `§3`.
[сейчас] `_ASSEMBLER.md` строки 145–150 — литерал `§4` патча (отмена сливания остатка
стенд-апа по `§2`; правило вытеснения по `§3`).
[сейчас] `07_ARCHIVE.md` — новый раздел (`§5` патча), 44 абзаца дословно.
[сейчас] `07_STATE.md §Подробности для модели` — вынос 44 абзацев (`§6` патча, скриптом).
[сейчас] `07_STATE.md §Открытые вопросы` + `§Мандат` — строка `STATE-DETAILS-BUDGET`
(парность `ADR-090 §1`).
[задачей STATE-DETAILS-BUDGET] Числовой бюджет по §4 плюс проверка в
`tools/hooks/pre-commit` и кейсы в `tools/hooks/selftest.sh`; распространение на длину строки
GAP-реестра.
[без правок] `ADR-064` — не меняется; §3 не переопределяет его, а достраивает симметричным
правилом для прозы.
[без правок] `ADR-084 §1` в части стенд-апа (пять строк, замена целиком) — в силе;
superseded-in-part только слова «длина не ограничена» и правило `§3` о сливании остатка.
[без правок] Сокращение строк GAP-реестра как таковое настоящим ADR не поручается — вопрос
владельцу не задавался и в scope сессии не входил; §4 трогает только будущую длину строки.

**Статус: accepted** (апрув владельца, чат 2026-08-02: «согласен, делай шаг первый»).

## ADR-106: GAP-реестр разрезается на индекс и полный текст; колонка «Как закрывается» перестаёт быть лентой дописок

**Контекст.** После `ADR-105` реестр стал крупнейшим разделом `07_STATE`: 70 023 симв.,
54,2 % файла, 62 строки. Объём сидит в одной колонке — `Как закрывается` даёт 74,7 %, сам
вопрос 22,5 %. Причина: колонка делает две работы — метод закрытия (короткое, стабильное) и
ленту дописок (растёт, не убывает); 23 строки из 62 несут маркеры дописки и дают 59 % объёма
колонки, 20 строк несут внутри две и более даты. `ADR-064` этого не лечит: пункт назначения
срабатывает только при полном закрытии, до тех пор строка растёт неограниченно — канал уже
назван `ADR-105 §4`. Побочно: 39 строк из 62 не называют статус в начале, то есть реестр
перестал быть приборной панелью. Полный разбор с тремя вариантами —
`reference/registry_compact_adj_2026-08-02.md`; выбор варианта C — владелец, чат 2026-08-02.

**Решение.**

§1 **Шаг 1 — разрез на индекс и полный текст (вариант C).** Заводится `07_GAPS.md`, куда
**дословно** переезжают все 62 строки. `07_STATE §Открытые вопросы` оставляет индекс:
`ID · статус · вопрос одной фразой · гейт`. Приём не новый — `ADR-064` уже сделал это с
журналом решений (`06_INDEX` вместо полного `06_DECISIONS_LOG` в обязательном контексте);
настоящий параграф расширяет `ADR-064`, не отменяет его. Замер: индекс 8 528 симв. против
70 023; `07_STATE.md` 129 106 → 68 166 симв. (−47,2 %), доля реестра 54,2 % → 12,5 %.
**Индекс есть НОВЫЙ производный текст, а не замена содержания:** оригинал сохраняется целиком,
поэтому неточная формулировка индекса материала не теряет и правится без археологии.

§2 **Статус называется первым словом, из закрытого словаря.** `OPEN` (открыт, работа не
назначена либо идёт) · `READY` (исполнимо сейчас, форма задана) · `DEFER` (отложен, триггер
назван) · `PARTIAL` (частично закрыт, остаток назван). У 39 строк, где статуса в начале не
было, он выведен из уже написанного текста строки; содержание строки при этом не меняется —
строка целиком уезжает в `07_GAPS.md` дословно.

§3 **Шаг 2 — разрез колонки (вариант A), задачей `REGISTRY-COLUMN-SPLIT`, после шага 1.**
`Как закрывается` оставляет метод, статус и гейт; лента дописок уезжает в артефакт вопроса
либо в архив, в строке — ссылка. Порядок именно такой: шаг 1 снимает стоимость чтения
немедленно и дословно, после чего разрез делается по одной строке, не блокируя Epic-1.

§4 **Жёсткая форма строки с проверкой хуком (вариант B) НЕ принимается сейчас.** Единственный
вариант, требующий переписать все 62 строки, то есть единственный с реальным риском потери
смысла. Его главная выгода — механический предел длины — уже входит в объём задачи
`STATE-DETAILS-BUDGET` (`ADR-105 §4`). Возврат к B — только если после шага 2 строки продолжат
расти, и уже с фактами.

§5 **Защитные механизмы, обязательные для обоих шагов.** (i) Ничего не удаляется — только
перемещается. (ii) Перенос бинарный: строка либо остаётся как есть, либо уезжает дословно;
переписывание при переносе запрещено. (iii) Там, где переформулировка неизбежна, старая
редакция уезжает в `07_ARCHIVE.md` дословно тем же коммитом. (iv) Применение — скриптом с
предохранителями по образцу `ADR-105 §5`: привязка к `git hash-object` целевого файла (не к
`HEAD`), отказ при изменении файла, при расхождении множеств или порядка ID и при повторном
применении; режим `--check` печатает дельту до применения. (v) Проверка 4 хука расширяется с
`07_STATE.md` на `07_STATE.md` + `07_GAPS.md`, иначе закрытие строки теряет защиту ровно в
момент переезда. (vi) Счётчик строк реестра в стенд-апе печатается каждой сборкой.
(vii) Новая проверка 9 хука: множества ID индекса и полного текста обязаны совпадать.

§6a **Инвентарь буфера учится ловить этот класс до старта прохода.** `tools/inbox_manifest.sh`
получает проверку «пункт `[сейчас]`, адресованный файлу вне четвёрки сборки». Попутно найдены и
исправлены два его собственных дефекта: он был слеп к пунктам через `*` (56 из 110 в истории) и
понимал лишь одну форму заголовка последствий из четырёх (9 из 27 блоков). Замер ложных
срабатываний по всем 43 блокам истории: 4 блока, 11 пунктов, ложных ноль — то есть у дефекта
четыре инстанса, а не два. Заведён `tools/inbox_manifest_selftest.sh` (самотеста у скрипта не
было вовсе): `пройдено 8, провалено 0`. Предел назван: пункт, не называющий файла, не ловится.

§6 **Проверки хука сдаются прогоном, а не текстом.** Правки `tools/hooks/*` закоммичены этой
сессией в свою ветку и инертны до появления `07_GAPS.md` (проверка 9 сторожится
`git cat-file -e :07_GAPS.md`), поэтому слияние ветки без применения §1 репо не ломает.
Самотест обязан оставаться `провалено 0`: на момент сдачи `пройдено 36` против прежних `28`.

§7 **Разделение труда названо явно, потому что первая редакция этого блока его перепутала.**
Сборка вправе править ровно четыре файла (`_ASSEMBLER.md §1`) и есть механический переносчик
без права трактовки; пункт `[сейчас]`, адресованный файлу вне этой четвёрки, она применить не
может, и его невыполнимость есть стоп-условие (`ADR-083 §3`, `_ASSEMBLER.md §3a`). Первая
редакция пометила `[сейчас]` пять таких пунктов, и сборка встала — правильно. Всё, что вне
четвёрки, переносится в `[отдельным коммитом]` и приходит ИМЕНОВАННЫМ коммитом сразу следом
за коммитом сборки, того же прохода. Форма не изобретается: прецедент `519e71d` («`ADR-105`
§2/§3 в `05_CONVENTIONS.md`/`_ASSEMBLER.md`») пришёл ровно так, следом за `9461bf5`.

§8 **Порядок двух коммитов задан и проверяется скриптом.** Сначала коммит сборки (в том числе
строка `REGISTRY-COLUMN-SPLIT` в реестре), затем именованный коммит с разрезом. Обратный
порядок скрипт отвергает сам: множества ID индекса и реестра разойдутся на одну строку.
Предохранитель скрипта привязан к дословному тексту КАЖДОЙ снятой строки (`row_hashes.tsv`),
а не к хэшу всего `07_STATE.md`: правки сборки в других разделах файла законны и не должны
читаться как порча, а подмена текста снятой строки обязана ловиться. Строка, добавленная после
снимка, допускается, печатается отдельно и обязана присутствовать в индексе.

**Последствия.**

[сейчас] `06_DECISIONS_LOG.md` — настоящий ADR; `06_INDEX.md` — строка.
[сейчас] `07_STATE.md` — абзац в `§Подробности для модели`; шапка (`updated_at`, `обновил`).
[сейчас] `07_STATE.md §Открытые вопросы` + `§Мандат` — строка `REGISTRY-COLUMN-SPLIT`
(парность `ADR-090 §1`). Заводится В СТАРОМ формате таблицы: разрез приходит следующим
коммитом и подхватит её как 63-ю.
[отдельным коммитом] `07_GAPS.md` — новый файл, 63 строки дословно, И
`07_STATE.md §Открытые вопросы` — таблица заменена индексом плюс врезка. Оба действия ОДНИМ
коммитом и только вместе (§1/§2): индекс без файла-приёмника означал бы удаление полного
текста 63 строк. Применяется `apply_split.py --apply`; сухой прогон `--check` печатает дельту.
[отдельным коммитом] `_METHOD.md` строка 159 — литерал
`reference/registry_split_patch_2026-08-02.md §3`.
[отдельным коммитом] `00_CHARTER.md §карта документов` — строка `07_GAPS` (литерал `§4` патча).
[отдельным коммитом] `_ASSEMBLER.md` строки 250–251 — литерал `§5` патча.
[отдельным коммитом] `tools/hooks/*` и `tools/inbox_manifest*.sh` — файлы УЖЕ в ветке и
проверены прогоном; остаётся подставить номер ADR вместо `ADR-106` (§6).
[отдельным коммитом] `CLAUDE.md §Старт` п.2 и `_ARCHITECT.md §3/§9` — точечное чтение
`07_GAPS.md` по ID (литерал `§7` патча).
[задачей `REGISTRY-COLUMN-SPLIT`] Шаг 2 по §3. Строка заводится коммитом сборки выше, то
есть на момент проверки `_ASSEMBLER.md §3a` она в `07_STATE` есть.
[без правок] `ADR-064` — не отменяется; §1 расширяет его приём на реестр.
[без правок] `STATE-DETAILS-BUDGET` — остаётся как есть; максимальная длина строки реестра
по-прежнему её объём (`ADR-105 §4`), настоящий ADR числа не назначает.
[без правок] Семь строк (`Q-6`, `Q-11`…`Q-14`, `Q-47`, `Q-55`), числящихся открытыми при
DONE-задаче `INFRA-FACTS-SWEEP` — расхождение названо в `§8` патча и НЕ адъюдицируется
настоящим ADR: это вопрос факта, а не составления индекса.

**Статус: accepted** (апрув владельца, чат 2026-08-02: «согласен, делай C»).

## ADR-107: Заморозка процессного слоя до сходимости реестра паритета

**Дата:** 2026-08-02 · **Статус:** accepted (владелец, чат 2026-08-02: «берем заморозку процесса
и грубый замер трех пар»)

**Контекст.** Замер сессии `PROCESS-FREEZE` (`reference/process_freeze_2026-08-02.md §2`): за
13 суток (`09cb6cb` → `bee4289`) `06_DECISIONS_LOG.md` вырос ×9,0 (71 644 → 646 200 симв.),
`07_STATE.md` ×4,8 (42 315 → 203 733 симв.). Из 46 решений `ADR-060`…`ADR-105` предметом
примерно 26 является метод и процесс, а не предметная область. Закрытых `Q`-строк в
`07_ARCHIVE.md` за всю историю 63 при 62 открытых на 2026-08-02 — темп закрытия равен темпу
заведения, то есть система в стационарном состоянии, а не сходится. Причина названа: у
процессного слоя нет ограничителя — дефект даёт решение, решение даёт правило, правило даёт
проверку и парные строки двух таблиц (`ADR-090 §1`), а закрытие идёт медленнее заведения.
Метод обязан ловить свои дефекты (`ADR-021 §2` и далее), но не обязан обслуживать их немедленно.
Одновременно: из 62 строк реестра к финишу Epic-1 ведут 23, из семи пар паритета три не
измерялись ни разу (`§1`, `§3` артефакта).

**Решение.**

§1 **До сходимости всех пар реестра паритета (`ADR-067`) новые решения о методе не принимаются.**
Исключение ровно одно: правило, без которого встаёт уже начатая работа по паритету.

§2 **ADR такого исключения обязан назвать поимённо задачу паритета, которая без него встаёт.**
Не может назвать — исключением не является. Критерий проверяемый, а не тематический; приём тот
же, которым `ADR-091 §1` резал задачи по линии исполнимости действия, а не по теме.

§3 **Четырнадцать строк корзины «метод» получают статус `DEFER`** с одним общим триггером
промоушена — «сходимость реестра паритета». Строки: `Q-35`, `Q-40`, `Q-62`, `Q-68`, `Q-75`,
`Q-76`, `Q-84`, `Q-85`, `Q-94`, `LAUNCH-RUNBOOK`, `CC-READONLY-PRINCIPAL`,
`METHOD-PROMOTE-LOCAL-RULES`, `STATE-DETAILS-BUDGET`, `REGISTRY-COLUMN-SPLIT`. Ничего не
удаляется (`ADR-106 §5(i)`); в `07_ARCHIVE.md` они НЕ переезжают — они открыты, а не закрыты
(`ADR-064`). Строки мандата тех же задач остаются на месте: `DEFER` не есть закрытие, парность
`ADR-090 §1` уборки не требует.

§4 **Процессный дефект, замеченный по ходу, фиксируется одной строкой наблюдения в артефакте
сессии и решением не становится.** Строка реестра под него до финиша не заводится. Расширение
приёма `ADR-051 §2` (не заводить `Q` на повторный инстанс того же объекта) на процессный класс
целиком и на время заморозки.

§5 **Заморозка не распространяется на три вещи.** (i) Механические проверки, чинящие уже
сломанное (`tools/hooks/*`): сломанная проверка хуже отсутствующей, отказ тихий (`ADR-072 §3`).
(ii) Шаг 1 `ADR-106 §1` — решение принято, перенос дословный, применитель написан и снабжён
предохранителями. (iii) Настоящий ADR.

§6 **Шаг 2 `ADR-106 §3` (`REGISTRY-COLUMN-SPLIT`) замораживается §3** по прямому подтверждению
владельца (чат 2026-08-02). Выгода шага 2 — торможение роста строк — не имеет цены, пока рост
строк заморожен §4.

§7 **Снятие заморозки автоматическое** при сходимости реестра паритета, отдельного решения не
требует. Досрочное снятие — решение владельца, как всякое решение уровня финишной черты
(`ADR-067`).

**Последствия (blast radius).**

[сейчас] `06_DECISIONS_LOG.md` — настоящий ADR; `06_INDEX.md` — строка `ADR-107`.
[сейчас] `07_STATE.md` — статус `DEFER` и общий гейт у четырнадцати строк индекса; абзац в
`§Подробности для модели`; замена стенд-апа; шапка.
[сейчас] `07_GAPS.md` — дописка о заморозке в те же четырнадцать строк полного текста.
[без правок] `ADR-106` — не отменяется; §1 прямо выведен из-под заморозки, §3 отложен §6.
[без правок] `ADR-105 §4` — `STATE-DETAILS-BUDGET` не отменяется, а откладывается.
[без правок] `_METHOD.md`, `05_CONVENTIONS.md`, `CLAUDE.md` — заморозка есть решение о темпе
принятия решений, а не правка правил работы; правки этих файлов она по построению не требует.
[задачей] снятие `DEFER` с четырнадцати строк — проходом сборки при сходимости реестра паритета.

---

## ADR-108: Мандат класса B на `PARITY-COARSE-TOTALS` выдаётся поимённо

**Дата:** 2026-08-02 · **Статус:** accepted (владелец, чат 2026-08-02: «мандат на замер трех пар
выдаю»)

**Контекст.** Три пары реестра паритета — остатки (строка 21), заказы «в пути» (строка 22),
счета покупателям (строка 23) — числятся «не сверялось» и не измерялись ни разу, при том что
каждая из четырёх измеренных пар дала одну-две структурные находки. Пока грубые итоги по ним
неизвестны, размер остатка проекта не оценивается в принципе, а форма фикса по уже найденным
расхождениям выбирается вслепую (`reference/process_freeze_2026-08-02.md §1`). Довод «ширина
раньше глубины» записан `07_STATE §Текущий фокус` (волна 1, 2026-08-01) и не исполнен. Работа
требует живого `GET` к МойСкладу с секретом `msklad-token`, то есть класса B по `ADR-076 §1`.

**Решение.**

§1 **Мандат класса B на `PARITY-COARSE-TOTALS` выдаётся поимённо настоящим параграфом.**
Единственное основание класса B — доступ к секрету `msklad-token`. Прецедент формы — `ADR-092 §1`.

§2 **Объём — ровно грубые итоги трёх пар:** по каждой паре число со стороны источника, число со
стороны витрины и разность. Ни построчности, ни установления причин, ни правок. Объединение трёх
пар в ОДИН заход обосновано `ADR-077 §6`: класс B выдаётся поимённо, три отдельные выдачи есть
три подтверждения и вклад в привыкание, против которого этот параграф прямо предостерегает.

§3 **Обязательны без изменений:** шесть оговорок `ADR-076 §5` (обращение с токеном), форма
логирования `ADR-079 §6` (имена полей строки данных, не первые N символов тела), датирование
эталона снимком (`ADR-067 §3`), правило одновременности для снимочных витрин (`ADR-079 §3`).

§4 **Класс B не параллелится** (`ADR-082 §1`) — заход одиночный.

§5 **Вне scope:** установление причин любого расхождения; вывод правил-мостов `Q-80`/`Q-81`/`Q-82`;
любые правки кода, витрин, Custom Query и конфигураций; расширение реестра паритета.

§6 **Две ловушки названы брифом заранее и не оставлены на усмотрение сессии.** (i) Валюта этим
замером НЕ конвертируется: `entity/*` отдаёт суммы в минорных единицах, политика исходящей
конвертации есть предмет отдельных решений (`ADR-062`) и источник уже случившегося дефекта —
не-KGS документы сообщаются числом и суммой отдельной строкой. Молча применённое правило
превратило бы замер величины в проверку правила. (ii) Витрина ради замера не пересобирается;
её устаревание относительно источника есть часть результата.

**Последствия (blast radius).**

[сейчас] `06_DECISIONS_LOG.md` — настоящий ADR; `06_INDEX.md` — строка `ADR-108`.
[сейчас] `07_STATE.md` — строка индекса `PARITY-COARSE-TOTALS` (`READY`) и парная строка в
`§Мандат Claude Code: класс задач` (`ADR-090 §1`); `07_GAPS.md` — полный текст той же строки.
[без правок] `briefs/PARITY-COARSE-TOTALS.md` — бриф собран сессией `PROCESS-FREEZE` под
настоящий мандат, правки не требует.
[без правок] `reference/parity_registry.md` — строки 21-23 правит сама задача, не сборка.
[задачей `PARITY-COARSE-TOTALS`] замер и запись трёх разностей.

---

## ADR-109: Адъюдикация урожая грубого замера; пара «в пути» сошлась; суммы по счетам расхождением не объявляются; шаг 2b код-репо — единственный гейт деплойного потока

**Дата:** 2026-08-02 · **Статус:** accepted (архитектор — §1–§4, §6; §5 — приоритет, подтверждён требованием владельца в чате 2026-08-02 «эти задачи возьмём с наивысшим приоритетом»)

**Контекст.** `PARITY-COARSE-TOTALS` измерила три пары, ни разу не измерявшиеся
(`reference/parity_coarse_totals_2026-08-02.md`); владелец потребовал превращения находок в
задачи на результат и объяснения, какой готовый код не деплоится.

**Решение.**

§1 **Пара «Заказы поставщикам в пути» признаётся сошедшейся** (`0,00` при `78 748 543,05`),
уровень эталона (ii): сторона источника получена применением опубликованного правила к СЫРЫМ
документам источника, что `ADR-098 §7` не запрещает (запрещено сравнение нашей таблицы с нашей
же таблицей). Оговорка записывается в строку 22 реестра: ноль доказывает верность
воспроизведения источника ингестом и НЕ доказывает совпадения с числом, отображаемым интерфейсом.
Счётчик пар `1/7` → `2/7`.

§2 **Пара «Остатки товаров» остаётся «не сверялось».** Разность `0,12 %` не интерпретируется:
наша сторона — снимок за предыдущие сутки, источник живой, правило одновременности
(`ADR-079 §3`) нарушено, слагаемые «дрейф склада» и «расхождение» не разделены. Закрывается
задачей `PARITY-STOCK-SAME-DAY`.

§3 **Пара «Счета покупателям» разделяется на два факта.** Сравнима и принимается недостача
`468` документов из `4526` (`10,3 %`). Суммы **несравнимы и расхождением не объявляются**:
`43,4 %` документов источника не в KGS, сложены по номиналу, правило пересчёта для этой ветки
не выведено (`Q-82`, обслуживающая Cloud Function не установлена —
`reference/source_map_rest_2026-07-30.md §4`). Проценты `7,61`/`12,55`/`12,81` в реестр не
вносятся; вносится недостача документов и пометка о несравнимости сумм.

§4 **Заводятся пять задач на результат** с проверяемой приёмкой и порядком:
`INVOICES-STALENESS-PROBE`, `INVOICES-FIELD-MAP`, `PARITY-STOCK-SAME-DAY`, `SALES-MERGE-DRYRUN`,
`INVOICES-PERIMETER-DIFF` (составы — `reference/parity_coarse_adj_2026-08-02.md §4`).

§5 **Шаг 2b `CODE-REPO-STANDUP` объявляется единственным гейтом всего деплойного потока** и
получает высший приоритет. Класс и мандат не меняются (`B`, владелец). Пока он открыт, подготовка
патчей увеличивает запас недоставленной работы, а не приближает финиш `ADR-067`.

§7 **Место хостинга код-репо выбрано владельцем** (чат 2026-08-02): приватный GitHub
`https://github.com/ilyasbazarov/holika-prod`. Довод выбора зафиксирован: практика ведения
репозитория этого проекта уже есть и переносится без освоения нового инструмента, тогда как
вариант внутри облачного периметра потребовал бы отдельного разбирательства с правами, то есть
добавил бы гейт вместо снятия. Остаток шага 2b — создание удалённого репозитория и первый `push`
из Cloud Shell (каталог `/home/ilyasbazarov4/cf-finance-coderepo`, первый коммит `4317047`);
исполняет владелец, инструменту не отдаётся (`ADR-076 §1`, исходящее действие вовне).

§6 **Поправка факта к `reference/process_freeze_2026-08-02.md §1`:** подготовленных патчей в
проекте нет ни одного; «разгейчено» было прочитано как «готово». Признак подготовки назначается
механический — артефакт в `reference/` с именем задачи (`ADR-021 §2`).

**Последствия (blast radius).**

[сейчас] `06_DECISIONS_LOG.md` + `06_INDEX.md` — ADR и строка.
[сейчас] `07_STATE.md` + `07_GAPS.md` — новые задача-строки под брифы, дописки к
`CODE-REPO-STANDUP` и `SALES-REFRESH-WINDOW`, дописка в `§Контрольные цифры`, абзац в
`§Подробности для модели`, шапка.
[сейчас] `07_STATE.md §Мандат` — парные строки (`ADR-090 §1`).
[задачей] строки 21, 22, 23 `reference/parity_registry.md` правят сами задачи, не сборка.
[без правок] `ADR-107` — заморозка не нарушается, все решения настоящего ADR предметные, а не о
методе.
[без правок] `ADR-065`, `ADR-079 §3`, `ADR-098 §7` — применяются, не пересматриваются.

**Дополнение сборки (2026-08-02).** По прямому решению владельца задача `INVOICES-STALENESS-PROBE`,
заведённая §4, открыта и закрыта в одном проходе сборки буфера (её собственная сессия завершила
работу раньше применения этого ADR) — строка `07_STATE.md` под неё не заводилась, полный текст
закрытия ушёл сразу в `07_ARCHIVE.md`. Гейт `INVOICES-FIELD-MAP` («зависит от результата
`INVOICES-STALENESS-PROBE`») снят более поздним по цепочке зависимостей блоком `INVOICES-PROGRAMME`
(`ADR-110 §1`) в пользу «гейтов нет» — решение владельца в чате 2026-08-02.

---

## ADR-110: Программа постройки загрузчика счетов покупателям — контракт, декомпозиция из шести задач, определение готовности из пяти условий; разбиение работ на параллельные проходы

**Дата:** 2026-08-02 · **Статус:** accepted (архитектор — §1–§4, §6, §7; §5 — владелец, чат 2026-08-02, вариант A)

**Контекст.** `INVOICES-STALENESS-PROBE` (2026-08-02) подтвердила, что `core.fact_customer_invoices`
не пополнялась с `2026-06-05` (58 суток, единственное значение времени загрузки на всех 4058
строках), при том что витрина поверх неё пересобирается ежедневно и выглядит живой. Загрузчик
как элемент конвейера никогда не строился: таблица заполнена ручным прогоном от личного аккаунта
(`11_INFRA_FACTS §Ручные/разовые загрузки`). Владелец потребовал проект в согласии с
архитектурой, декомпозицию на задачи и определение готовности, исключающее бесконечную разработку.

**Решение.**

§1 **Контракт загрузчика** зафиксирован девятью свойствами с провенансом каждого
(`reference/invoices_loader_programme_2026-08-02.md §1`); ключ `invoice_id`, приём
staging→`MERGE`, форма `MERGE` — явный `INSERT (колонки)` (`C1`, `ADR-030`), принципал —
сервисный аккаунт конвейера, не личный.

§2 **Три унаследованных правила обязательны и названы явно**, потому что лежат в других
документах и легко пропускаются: сутки документа как функция UTC-времени (`ADR-088`) — новый
загрузчик НЕ повторяет дефект четырёх существующих веток; правило пересчёта валюты есть ВХОД
задачи, а не деталь реализации; поведение при удалении счёта в источнике обязано быть названо
явно, иначе воспроизводится механизм строк-сирот (`ADR-100`).

§3 **Декомпозиция из шести задач** с единственной жёсткой последовательностью
`T1 → T2 → T3 → T4 → T5 → T6`; внутри программы параллелить нечего, вся программа параллелится
с работами вне её. Деплойная часть дополнительно гейтится первым выгрузом код-репозитория
(`ADR-065`).

§4 **Определение готовности — пять проверяемых условий, все обязательны:** автоматичность (два
суточных прогона дают два разных времени загрузки без ручного запуска); полнота (число счетов
равно источнику либо разность объяснена правилом); идемпотентность (два прогона подряд не
меняют числа); паритет (строка 23 реестра — `0,00` либо закрыта правилом); наблюдаемость
(проверка свежести покрывает таблицу). Пятое условие обязательно потому, что без него программа
воспроизводит исходный дефект: молчаливая заморозка возможна ровно там, где её некому заметить.
Перечень того, чем работа расползаться НЕ должна, зафиксирован тем же параграфом.

§5 **Место жительства загрузчика: вариант A — режим внутри `cf-finance`.** Решение владельца
(чат 2026-08-02), рекомендация архитектора принята. Довод: наименьшее число действий класса B на
пути к работающей загрузке, а именно они сейчас дефицитный ресурс проекта; прецедент прямой — та
же функция уже поглотила standalone-загрузчик платежей (`01_ARCHITECTURE.md §топология`); секрет,
механика `MERGE` и суточное расписание там уже есть, новых единиц инфраструктуры не появляется.
**Две оговорки, принимаемые вместе с вариантом и обязательные для `INVOICES-LOADER-DESIGN`:**
(i) публичный доступ `cf-finance` (`allow-unauthenticated`, помечен предупреждением в
`01_ARCHITECTURE.md §топология`) есть дефект, существовавший ДО программы и от неё не зависящий;
расширение объёма функции его не создаёт и не усугубляет логически, но увеличивает ценность
поверхности — чинится СВОЕЙ задачей, связывать одно с другим и блокировать восстановление данных
ради несвязанного улучшения запрещено; (ii) суточный прогон `cf-finance` удлиняется — проект
обязан назвать текущую длительность прогона ДО правки и после, иначе удлинение обнаружится
таймаутом на проде, а не замером.

§6 **Разбиение работ на проходы.** Сплошной прогон свежести таблиц (`CORE-FRESHNESS-SWEEP`) и
снятие исходников функции проверок (`DQ-SOURCE-CAPTURE`) исполняются ОТДЕЛЬНЫМИ сессиями:
оба класса A, оба параллелимы, наборы файлов на запись не пересекаются, брифы собраны.
Довод против исполнения их в архитекторской сессии — не чистота ради чистоты, а два измеримых
свойства: контекст архитекторской сессии несёт весь ход дня и вытесняет предметные факты, а
параллельный запуск сокращает время до ответа вдвое.

§7 **Решение владельца чинить тихо** (предупреждение на странице дашборда не ставится) принято
и зафиксировано; следствие для порядка работ — досев истории раньше оформительских мер.

**Последствия (blast radius).**

[сейчас] `06_DECISIONS_LOG.md` + `06_INDEX.md` — ADR и строка.
[сейчас] `07_STATE.md` + `07_GAPS.md` — две задача-строки под брифы, шесть строк программы,
уточнение гейта `INVOICES-FIELD-MAP`, абзац в `§Подробности для модели`, шапка.
[сейчас] `07_STATE.md §Мандат` — парные строки (`ADR-090 §1`).
[без правок] `briefs/CORE-FRESHNESS-SWEEP.md`, `briefs/DQ-SOURCE-CAPTURE.md` — собраны этой
сессией.
[без правок] `01_ARCHITECTURE.md`, `02_ERP_CONTRACTS.md`, `03_PIPELINE_SPEC.md` — программа их
не правит, новый узел вносится в топологию задачей `INVOICES-LOADER-DEPLOY` по факту деплоя, а не
по намерению.
[без правок] `ADR-107` — заморозка не нарушается, все решения предметные, а не о методе.
[задачей] шесть задач программы.

**Дополнение сборки (2026-08-02).** Единственный вопрос, который сессия собиралась завести
(`INVOICES-LOADER-HOME` — где живёт загрузчик), снят решением владельца в чате 2026-08-02 (§5) —
строка реестра под него не заведена. Гейт `INVOICES-FIELD-MAP`, унаследованный от `ADR-109 §4`
(«зависит от результата `INVOICES-STALENESS-PROBE`»), снят настоящим ADR в пользу «гейтов нет,
первый вход программы» — решение владельца в чате 2026-08-02 (сборка буфера 2026-08-02): задача
`INVOICES-FIELD-MAP` (соответствие полей) признана независимой от того, как архитектор в итоге
переклассифицирует находку по замороженной таблице.

---

## ADR-111: Восстановление потока данных получает первенство над сверкой паритета; четыре задачи по урожаю прогона свежести; правило «измерение против замершего склада не производится»

**Дата:** 2026-08-02 · **Статус:** accepted (архитектор; §1 подтверждён владельцем в чате
2026-08-02 запросом «диагностику вперёд всего остального»)

**Контекст.** `CORE-FRESHNESS-SWEEP` (2026-08-02) покрыл все 26 таблиц датасетов `core` и
`marts`. Найдено: заморожены три таблицы (счета — 58 суток, справочник сопоставления полей — 94
суток, резервная копия продаж — 91 сутки), отстают три (продажи и закупки — 18ч 50м при часовой
частоте, возвраты — 14 суток при недельной), пять не несут отметки времени и методом не
проверяемы. Остановка продаж и закупок ЖИВАЯ: соседние функции работают, то есть отказ
локализован, а не общий.

**Решение.**

§1 **Первенство отдаётся восстановлению потока.** Диагностика (`FACTS-WORKFLOW-STOP-DIAG`, класс
A, бриф собран) идёт вперёд всех прочих задач, включая `DQ-SOURCE-CAPTURE`, которой ранее в этой
же сессии была обещана очередь. Довод: живая остановка увеличивает объём будущего досева
пропорционально времени, тогда как все прочие находки статичны.

§2 **Измерение паритета «на сейчас» против остановленной загрузки не производится.** Сравнение
живого источника с замершим складом даёт расхождение, растущее само по себе, то есть измеряет
простой, а не дефект. Гейт добавляется `PARITY-STOCK-SAME-DAY` и `INVOICES-PERIMETER-DIFF`.
Исторические измерения по маю остаются действительными и пересмотра не требуют: майские данные
загружены задолго до остановки.

§3 **Единый механизм отказа трёх режимов есть гипотеза, а не факт.** Против неё говорит разное
время остановки. Диагностике запрещено принимать гипотезу на веру: она обязана либо подтвердить,
либо назвать два независимых отказа.

§4 **Четыре задача-строки заводятся** (`FACTS-WORKFLOW-STOP-DIAG`, `FACTS-BACKFILL`,
`DIM-METADATA-MAPPINGS-FROZEN`, `MARTS-BUILD-STAMP`); состав, класс и приёмка — в `07_GAPS.md`.

§5 **Инстанс молчания сторожа свежести зафиксирован**, вопрос `DQ-SOURCE-CAPTURE`
переформулирован с «какие таблицы покрыты» на «срабатывает ли проверка вообще и доходит ли
сигнал до человека». Строка не переписывается, дописка идёт в колонку гейта. *(Блок-источник несёт
внутреннее расхождение по номеру инстанса — «третий» в дописке гейта против «четвёртого» в тексте
этого параграфа и в разделе «Подробности для модели»; сборка переносит обе формулировки дословно,
не выбирая между ними — см. «Дополнение сборки» ниже.)*

§6 **Витрина без отметки времени сборки и без возраста источника есть маскировка, а не
нейтральное свойство.** Две таблицы пересобираются ежедневно поверх источников, которые могут
стоять, и выглядят свежими независимо от возраста данных — тот же механизм два месяца прятал
замороженные счета. Форма фикса не назначается настоящим ADR (это работа `MARTS-BUILD-STAMP`), но
признаётся дефектом, а не особенностью.

**Последствия (blast radius).**

[сейчас] `06_DECISIONS_LOG.md` + `06_INDEX.md` — ADR и строка.
[сейчас] `07_STATE.md` + `07_GAPS.md` — четыре задача-строки, дописки в гейты
`DQ-SOURCE-CAPTURE`, `PARITY-STOCK-SAME-DAY`, `INVOICES-PERIMETER-DIFF`, замена стенд-апа, абзац
деталей, шапка.
[сейчас] `07_STATE.md §Мандат` — парные строки.
[без правок] `briefs/FACTS-WORKFLOW-STOP-DIAG.md` — собран этой сессией.
[без правок] `ADR-107` — заморозка не нарушается, все решения предметные.
[без правок] `03_PIPELINE_SPEC.md`, `11_INFRA_FACTS.md` — правятся по факту диагноза, не по
намерению.
[задачей] четыре задачи выше.

**Дополнение сборки (2026-08-02).** Пятая находка `CORE-FRESHNESS-SWEEP` — резервная копия
`core.fact_sales_profit_byvariant_backup`, заморожена с `2026-05-03`, владелец не назван — не
попала ни в одну из четырёх задач настоящего ADR (справочник сопоставления полей закрывается
`DIM-METADATA-MAPPINGS-FROZEN`, но резервная копия продаж — другая таблица и другой признак
находки: у неё есть отметка времени, просто она не двигается). При этом `STATE_PATCH`
`FACTS-STOP-ADJ` утверждал «все находки заведены задачами выше» — утверждение неточное. По
решению владельца (чат 2026-08-02, вне текста самого блока) находка заводится отдельной строкой
`FACT-SALES-BYVARIANT-BACKUP-OWNER`, текстом, предложенным `CORE-FRESHNESS-SWEEP`; мандат класса
A/B ей не назначается — блок-источник его не называл, только описание находки.

## ADR-112 · Адъюдикация диагностики остановки загрузки фактов — переквалификация гипотезы, порядок работ, периметр гейта

**Статус: accepted** (архитектор §1–§4/§6; §5 — владелец, решение в чате 2026-08-02 «по всем
рекомендациям зелёный свет»).

**Контекст:** сессия `FACTS-WORKFLOW-STOP-DIAG` (2026-08-02, класс A) назвала механизм остановки
верно, но объявила `CONTEXT GAP` там, где ответ лежал в `03_PIPELINE_SPEC.md`, и не заметила в
собственных логах контр-факта, этому ответу противоречащего. Владелец выбрал вариант «сначала три
безопасных замера, потом решать» вместо немедленного вмешательства класса B.

**Решение:**

§1 Тезис «`drift_check` читает вчерашнюю выручку из `core.fact_sales_profit`» переквалифицируется
из «правдоподобная неподтверждённая гипотеза» в **«документированное поведение
(`03_PIPELINE_SPEC.md:72,78`), не сверенное с задеплоенной ревизией»**. Остаток `CONTEXT GAP`
сужается до сверки кода с докой и закрывается `DQ-SOURCE-CAPTURE`.

§2 Противоречие между §1 и самовосстановлением недельного гейта после провалов `2026-06-14` и
`2026-06-28` признаётся **главной незакрытой частью диагностики**. Три объяснения названы
поимённо, ни одно не выбирается до замера.

§3 Момент начала отказа переопределяется: спусковой крючок — переход `target_date` на субботу при
выходном пороге в полночь по Бишкеку (`18:00Z`), а не событие в 17-18 часов. Прежняя
формулировка «точнее не установлено, окно опроса — час» superseded-in-part.

§4 `Q-6` закрывается фактом, снятым самой диагностикой; факт переносится в
`11_INFRA_FACTS §cf-dq`. Вскрытое расхождение датировки «T-1-фикса» отдельной строки не получает
(`ADR-051 §2`) и дописывается в `DQ-SOURCE-CAPTURE`.

§5 **Порядок работ (владелец):** `DQ-GATE-BEHAVIOR-PROBE` → `DQ-SOURCE-CAPTURE` → решение о форме
восстановления (класс B) → `FACTS-BACKFILL`. Прежний порядок стенд-апа
(`FACTS-WORKFLOW-STOP-DIAG`, применён сборкой `6480019`), ставивший `DQ-SOURCE-CAPTURE` последним,
superseded: он противоречил пункту (v) собственного блока.

§6 Периметр гейта («проверка по продажам останавливает закупки и возвраты») признаётся ПРЕДМЕТНЫМ
дефектом, под заморозку `ADR-107` не подпадающим, и получает строку реестра
`DQ-GATE-SCOPE-SPLIT`. Форма фикса не назначается до чтения кода.

**Последствия:** заводятся `DQ-GATE-BEHAVIOR-PROBE` (класс A, бриф собран) и `DQ-GATE-SCOPE-SPLIT`
(подготовка A / деплой B); закрывается `Q-6`; правится `11_INFRA_FACTS §cf-dq`; строка
`DQ-SOURCE-CAPTURE` получает дописку и приоритет; решение о форме восстановления (класс B)
остаётся за владельцем и отложено до результата замера. `03_PIPELINE_SPEC.md` НЕ правится этим
решением — расхождение датировки фиксируется, форма правки назначается после чтения кода.

## ADR-113 · Тезис о самоподдерживающемся отказе DQ Gate снимается как опровергнутый кодом; источники трёх величин гейта зафиксированы поимённо; вмешательство класса B в живой конвейер лишается основания до замера

**Статус: accepted** (архитектор; фактическая часть проверяема чтением кода, решение сводится к снятию опровергнутого утверждения).

**Контекст:** `07_STATE` нёс вывод «`drift_check` читает `core.fact_sales_profit`, отказ самоподдерживающийся, сам не снимется», на основании которого владельцу предлагались три варианта восстановления, два из которых суть вмешательство класса B в живой конвейер (ручной промоут либо правка порога с передеплоем). Чтение задеплоенного кода (`reference/code/cf-dq/main.py`, sha256 сверен с ревизией `cf-dq-00007-hot`) показывает, что `target_rev` читается из `stg_msklad.fact_sales_staging`, перезаливаемой каждый час шагом, стоящим в DAG ДО проверки, — то есть от блокировки промоута эта величина не зависит и цикл самоподдержания невозможен.

**Решение:**

§1 Источники трёх величин гейта фиксируются поимённо — `yesterday_rev` из staging, `ma7` из ядра (окно `T-8…T-2`), `freshness` из staging.

§2 Тезис о самоподдерживающемся отказе снимается во всех местах `07_STATE`, где он живёт.

§3 Арифметика «`freshness` начнёт падать `2026-08-05`» снимается как построенная на неверном источнике.

§4 `CONTEXT GAP` о механизме восстановления `2026-07-26` объявляется закрытым: прохождение чека не требовало промоута за тот день.

§5 До исполнения замера `DQ-GATE-RECOVERY-CONFIRM` вмешательство класса B в живой конвейер по этому поводу основания не имеет — два независимых пути самоснятия блокировки следуют из кода и стоят ноль.

§6 Датированные артефакты `dq_gate_behavior_probe_2026-08-02.md` и `facts_stop_diag_adj_2026-08-02.md` НЕ переписываются (`ADR-043`), поправка живёт в новом артефакте и в `07_STATE` со взаимными ссылками.

**Последствия:**
- [сейчас] `07_STATE §Подробности для модели` — три поправки (источник `ma7`/`yesterday_rev`, снятие тезиса о самоподдерживающемся отказе, снятие арифметики `freshness`) в затронутых абзацах.
- [сейчас] `07_GAPS.md` строка `Q-102` — дописан второй инстанс класса (`tools/inbox_manifest.sh`).
- [сейчас] `07_GAPS.md` строка `FACTS-BACKFILL` — дописано, что объём досева вероятно нулевой, вердикт за замером.
- [задачей DQ-GATE-RECOVERY-CONFIRM] проверка обоих предсказанных путей самоснятия и вердикт по необходимости досева.
- [задачей DQ-GATE-SCOPE-SPLIT] форма фикса периметра гейта; настоящим решением не назначается.
- [без правок] `03_PIPELINE_SPEC.md §DQ` — расхождение доки с кодом (асимметрия источников не проговорена) названо, но не правится: SEMI-STABLE, отдельное решение.
- [без правок] `reference/dq_gate_behavior_probe_2026-08-02.md`, `reference/facts_stop_diag_adj_2026-08-02.md` — датированы, не переписываются (`ADR-043`).

Введено сессией `DQ-DRIFT-SOURCE-CORRECTION` (2026-08-03, архитектор, без брифа, `ADR-086 §1`), применено сборкой буфера 2026-08-03.

## ADR-114 · Поверхностью BI пары реестра паритета является ГРАФИК, а не источник данных; пара «Возвраты покупателей» сошлась и переназначена на Executive summary

**Статус: accepted** (архитектор — §1/§2/§4; §3 — владелец, чат 2026-08-03).

**Контекст:** реестр паритета называл поверхностью пары возвратов Custom Query `msklad_counterparty_returns`. Единственный график на этом источнике («Топ контрагентов», «Операционка») выводит Контрагента, Страну, Выручку, Маржу и % от общего дохода — ни одной из трёх колонок возвратов. Состав полей графиков инвентарь `reference/ls_custom_queries_2026-07-30.md` снимал только для одного источника из пяти. Пять суток пара числилась несошедшейся по признаку, который никто не проверял, при том что численное совпадение `570,00` = `570,00` было установлено ещё 2026-08-01 тремя независимыми путями.

**Решение:**

§1 Поверхностью BI пары реестра является выведенный на дашборд ГРАФИК с конкретным составом полей, а не Custom Query и не таблица-основа; величина, которую ничто не рисует, сверяемой поверхностью не является.

§2 Пара «Возвраты покупателей» переназначается на блок возвратов Executive summary (источник `fact_returns`), уровень — итог за период дашборда, и объявляется СОШЕДШЕЙСЯ по read-back владельца `570` за май-2026; счётчик реестра 1/7 → 2/7.

§3 Разрез возвратов по контрагенту остаётся ВНЕ финишной черты до появления рисующего его графика; расширение реестра — решение владельца, предложение сделано и ОТКЛОНЕНО (чат 2026-08-03), вопрос закрыт.

§4 При работе с любой парой реестра состав полей её графика снимается явно и фиксируется в артефакте — «источник подключён» не есть «величина видна».

**Последствия:**
- [сейчас] `reference/parity_registry.md` строка пары возвратов — статус сходимости `0,00`, поверхность и обоснование переназначения внесены (правка исполнена сессией, уже в репо).
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md`: `LS-RETURNS-FX-HARDCODE` и `LS-COUNTERPARTY-RETURNS-FIX` → `07_ARCHIVE.md`; `CODE-REPO-VISIBILITY` → `DEFER`.
- [сейчас] `07_STATE.md §Мандат Claude Code`: снимаются ЧЕТЫРЕ строки двух закрытых задач (парность `ADR-090 §1`).
- [сейчас] `07_GAPS.md` строка `GATE-STATE-VERIFIABILITY`: дописан второй инстанс класса.
- [задачей ORACLE-AUTOMATION] вторая половина `Q-79` (посев эталона возвратов) — от настоящего решения не зависит, статус `Q-79` не меняется.
- [без правок] `00_CHARTER §критерии успеха` — формулировка «сверяемых поверхностей BI» настоящим решением уточняется, но не переписывается: `ADR-067`/`ADR-068` остаются в силе, здесь дано прочтение термина «поверхность», а не новое определение финиша.

Введено сессией `LS-RETURNS-SURFACE-ADJ` (2026-08-03, архитектор, без брифа, `ADR-086 §1`), применено сборкой буфера 2026-08-03.

---

## ADR-115 · Мандат класса B не является следующим шагом деплойной очереди; первым идёт форма деплой-процедуры, а восстановление потока деплоем не является

**Статус: accepted** (архитектор — §1-§3, §5-§8, §10, §11-фактура; §4 — владелец, вариант Б, чат 2026-08-03; §9 — владелец, вариант 1, чат 2026-08-03, фактура и запасной путь — архитектор; §11 — владелец, вариант 1, чат 2026-08-03; §12 — мандат ВЫДАН владельцем, чат 2026-08-03; §13 — архитектор).

**Контекст:** владелец запросил порядок и форму мандатов класса B на накопившиеся деплои, назвав четыре кандидата и предположив, что три из них имеют готовые патчи, а восстановление живого потока требует вмешательства класса B. Обе посылки проверены и обе не подтвердились: тезис о самоподдерживающемся отказе снят `ADR-113` двумя днями ранее, а механическая проверка готовности (`ADR-109 §1`) находит артефакт подготовки ровно у одной задачи из семи. Одновременно найдена развилка, названная открытой трижды и не имеющая строки ни в одном реестре, — форма деплой-процедуры «только из код-репо» (`ADR-065`, owner-gated).

**Решение:**

§1 Гейт `ADR-065` снят для всех трёх функций — подтверждено read-only вызовом к удалённому репозиторию (`refs/heads/master = 81812f4…`), а не сообщением в чате. Строка `CODE-REPO-SEED-REST` закрывается `DONE`. accepted (архитектор).

§2 Готовность к деплою определяется механическим признаком `ADR-109 §1` и на сегодня есть у ОДНОЙ задачи (`INVOICES-LOADER-DEPLOY`). Статус `READY` в реестре означает «исполнимо сейчас» (`ADR-106 §2`) и готовностью патча не является; прочтение `READY` как готовности зафиксировано как воспроизводимая ошибка чтения реестра. accepted (архитектор).

§3 **Восстановление живого потока идёт первым и деплоем не является.** До результата замера `DQ-GATE-RECOVERY-CONFIRM` (класс A, после `2026-08-03T18:00Z`) вмешательство класса B основания не имеет (`ADR-113 §5` подтверждается и не пересматривается). При опровергающем исходе восстановление становится задачей класса B, идёт ОДИНОЧНЫМ заходом впереди всей очереди, и форма его выводится из наблюдённой причины, а не назначается заранее. accepted (архитектор).

§4 **Форма деплой-процедуры выбрана владельцем — вариант Б** (чат 2026-08-03), закрывая развилку, оставленную открытой `ADR-065` и трижды переподтверждённую как неназначенную (`ADR-094 §7`, `reference/code_repo_standup_d2_2026-08-01.md:100`, `reference/deploy_revision_probe_2026-08-01.md:121`). Правило: **ещё не задеплоенный патч ложится в ОТДЕЛЬНУЮ ветку код-репо; слияние в основную ветку происходит только после успешной проверки развёрнутого архива.** Основная ветка тем самым в любой момент равна проду, что и есть содержание инварианта «ревизия ↔ коммит». Соответствие фиксируется записью в `MANIFEST.md` соответствующей функции по результату read-back архива (`ADR-040`, `ADR-017 §2`), а не сообщением команды деплоя (`ADR-021 §2`). В процедуру входят без изменений: чистый `.gcloudignore` как условие деплоя (`ADR-040`), раздельность диагностики и действия (`ADR-055 §2`), объявление действия отдельным сообщением до подтверждения (`ADR-077 §6`), и явный запрет секретов в репозитории (он публичный, `CODE-REPO-VISIBILITY`) — сегодня это верно по факту, но правилом не записано. Запись процедуры текстом — задача `DEPLOY-PROCEDURE`, класс A. accepted (владелец — форма; архитектор — состав встраиваемых условий).

§5 **Пакетный мандат класса B на несколько деплоев отклоняется.** Прецедент `ADR-101` волны 2 применим к готовым однородным единицам работы; здесь предметы разнородны, готовность разная, и откат у каждого свой, поэтому пакет связал бы откаты между собой. Действующая форма — один поимённый ADR на один деплой (`ADR-092`, `ADR-100 §6`, `ADR-104 §1`, `ADR-108`) — сохраняется без изменений; обязательный минимум его содержания (объект и sha256 патча, чем откатывается, read-back по `ADR-040`, раздельность диагностики и действия по `ADR-055 §2`, объявление действия отдельным сообщением по `ADR-077 §6`) собирается из уже принятых правил и не изобретается. accepted (архитектор).

§6 **Первым деплоем новой эры назначается `INVOICES-LOADER-DEPLOY`** — он же приёмка процедуры. Основание: единственный построенный патч; не трогает работающий поток (новый режим против правки формулы в живом ингесте); откат тривиален (предыдущая ревизия `cf-finance` без режима `invoices`). accepted (архитектор); выдача самого мандата — владелец, отдельным ADR после `§4`.

§7 **Выбор варианта `SALES-REFRESH-WINDOW` не открыт** — он сделан владельцем 2026-08-02 (`ADR-101 §10`): вариант 3 к исполнению, вариант 1 как цель, вариант 2 отклонён. Противоречие двух строк реестра снимается в пользу более позднего решения. Вопрос допустимости `UPDATE` партиционирующей колонки внутри `MERGE` закрыт прошедшим dry-run (`SALES-MERGE-DRYRUN`); открытым остаётся только источник COGS для диапазона шире 90 суток, и он бьёт исключительно по варианту 1 в момент его постройки. accepted (архитектор).

§8 Заморозка `ADR-107` на `DEPLOY-PROCEDURE` не распространяется по двум независимым основаниям: процедура деплоя Cloud Function есть предметный контур пайплайна (тот же разбор, что у `DQ-GATE-SCOPE-SPLIT`), а при строгом прочтении исключение обосновано поимённо — без процедуры встают `INVOICES-PARITY-RECHECK` (строка 23 реестра паритета) и ветка досева зоны паритета. accepted (архитектор).

§9 **Каденция пересъёма эталона выведена из двух измеренных границ и выбрана владельцем — вариант 1** (чат 2026-08-03). Снизу каденцию держит скорость застывания документов в источнике, сверху — окно обновления 90 суток, после которого выравнивать стороны нечем; между ними остаётся практически одна точка в месяц, и правило её называет: **эталон месяца снимается ОДНОКРАТНО, в последнее воскресенье, при котором весь месяц ещё целиком внутри окна 90 суток; наша сторона снимается в тот же день, сразу после недельного прогона; пересъём после этого не производится.** Календарь: май → `2026-07-26`, июнь → `2026-08-30`, июль → `2026-09-27`, август → `2026-10-25`. **Запасной путь обязателен и есть часть правила:** сорванный целевой прогон уносит окно безвозвратно, тогда месяц закрывается либо датированным снимком по последнему успешному прогону с поимённо названной потерей документов, либо досевом после постройки постоянного режима. Замер-основание и его предел — `reference/deploy_order_adj_2026-08-03.md §9`. Варианты 2 и 3 отклонены: второй удваивает стоимость ради заведомо расходящегося раннего снимка (`30,71 %` застывания на +7 суток), третий небезопасен по построению — после выхода месяца из окна наша сторона перестаёт меняться, и триггер молчит навсегда, а источник продолжает двигаться. accepted (владелец — выбор; архитектор — фактура, границы и запасной путь).

§10 **Размещение обоих решений.** Процедура деплоя: полный текст — `reference/deploy_procedure_2026-08-03.md`, короткая форма правила — `05_CONVENTIONS.md` Часть II рядом с гигиеной деплоя (`ADR-040`), внесена сессией отдельным именованным коммитом (`ADR-081 §8`). Каденция: `reference/parity_registry.md`, раздел «Датированный снимок и каденция пересъёма» — внесён сессией (класс A, `/reference`). Плашка в `02_ERP_CONTRACTS.md` (STABLE, вне списка `ADR-081 §8`) приходит СБОРКОЙ по текстам-кандидатам `reference/deploy_order_adj_2026-08-03.md §12`. Тем самым закрываются оба условия строки `PARITY-ZONE-SNAPSHOT-DECISION`. accepted (архитектор).

§11 **Гейт закрытия мая снят офлайн; правки денег не меняли.** Прежняя квалификация («требует живого запроса к источнику, класс B») была ошибкой: различающие данные уже лежали в репо, и посуточное сравнение замороженной нашей стороны с более поздним снимком источника отвечает на вопрос напрямую. Расхождение по маю целиком объясняется строками-сиротами `ADR-100 §1` (арифметика замыкается с остатком `−0,73` KGS), свободного места под денежный эффект июльских правок нет. Утрата по маю — неденежная. Границы вердикта названы в `reference/deploy_order_adj_2026-08-03.md §13.3`: сравнение посуточное, подокументное невозможно по схеме таблицы; что именно поменялось в трёх документах, не устанавливалось. Три варианта закрытия мая, их цена и рекомендация (вариант 1 — датированный снимок) — `§14` того же артефакта. **Владелец выбрал вариант 1 (чат 2026-08-03); май закрыт** записью `reference/may_zone_close_2026-08-03.md` и абзацем в `reference/parity_registry.md`. Разность `1 630 239,87` KGS этим НЕ закрыта — она есть отдельный дефект `MERGE` без ветки удаления (`ADR-100 §1`, `C2`). Вывод `§9`-хода о выросшей срочности постоянного режима досева снимается: режим возвращается в роль страховки. accepted (архитектор — фактура и квалификация; владелец — выбор варианта 1, чат 2026-08-03).

§12 **Мандат класса B на `INVOICES-LOADER-DEPLOY` — текст готов к выдаче, выдаёт владелец.** Архитектор обязан предъявить анализ до выдачи, не после; анализ ниже, бриф собран (`briefs/INVOICES-LOADER-DEPLOY.md`).

*Техническая целесообразность.* Таблица `core.fact_customer_invoices` заморожена с `2026-06-05T09:07:07Z`; витрина `marts.customer_invoices_ar` ежедневно пересчитывается ПОВЕРХ статических данных, то есть инвесторская поверхность показывает устаревшее, не сообщая об этом. Загрузчика ядра никогда не существовало (`ADR-110 §1`). Это починка тихо сломанного, а не улучшение. Пара реестра «Счета покупателям» — одна из пяти незакрытых.

*Риски, названные поимённо, с митигацией.*
(R1) **Главный — диспетчер режимов вносится ВПЕРВЫЕ**: в живой ревизии точка входа есть `def main(request): run_etl(); return "OK", 200`, и ошибка в диспетчере ломает ЕЖЕДНЕВНУЮ загрузку платежей — боевую функцию, к счетам отношения не имеющую. Митигация: шаг 8(а) брифа требует прогона БЕЗ параметров с подтверждением неизменности прежнего поведения; откат — ревизия `cf-finance-00012-cik`.
(R2) **Дрейф рабочей копии** между постройкой (`2026-08-02`) и деплоем. Митигация: шаг 2 — побайтовая сверка свежего снапшота с `master`, расхождение есть стоп.
(R3) **Запись в живую таблицу**: первый прогон тянет всю историю без фильтра по дате (`ADR-034`). Митигация: шаг 5 — полный прогон в staging-таблицу ДО прода, с проверкой трёх величин арифметикой.
(R4) **Тихая деградация `expand`** при `limit=100` не пронаблюдена (`design §11.3`). Митигация: шаг 1, один read-only `GET` до всего остального.
(R5) **Мусор в архиве** (`RQ-3`, прожил шесть ревизий). Митигация: шаг 3 плюс read-back шага 7; этот деплой закрывает `RQ-3`.
(R6) **Таймаут** — не материален: суточный прогон платежей `824с` при потолке `1800с`, режим счетов идёт отдельным вызовом отдельного расписания (шаг 9), время существующего прогона не увеличивает.
(R7) **Секрет в публичном репозитории** — митигация: сплошной поиск по патчу с печатью совпадений до коммита в ветку.

*Чего анализ не утверждает.* «Отсутствие рисков» предъявить нельзя: R1 есть риск боевой функции, и он снимается не рассуждением, а шагом 8(а). Утверждается другое — каждый названный риск имеет проверяемую митигацию внутри брифа и явный откат на предыдущую ревизию.

*Форма мандата.* Один поимённый ADR на один деплой (`§5`); пакетом с другими деплоями не объединяется. Единственные основания класса B: деплой Cloud Function, создание расписания, доступ к секрету `msklad-token`, запись в живую `core.fact_customer_invoices`. Шесть оговорок `ADR-076 §5` и форма логирования `ADR-079 §6` обязательны. **accepted — мандат ВЫДАН владельцем (чат 2026-08-03) после предъявленного анализа.** Бриф `briefs/INVOICES-LOADER-DEPLOY.md` готов.

§13 **Именование задач очереди финиша.** Зонтичные задачи получают собственные ID, иначе ветку и бриф не на что завести (замечание владельца, чат 2026-08-03): `SALES-INGEST-PATCH` — один патч на две строки `SALES-PERIMETER-EXTEND` и `SALES-CONSIGNMENT-REVENUE` (объект один, прецедент объединения `ADR-100 §7`); бриф собран. `PARITY-INTRANSIT-ROWWISE` — построчная сверка пары «Заказы в пути» (`Q-81`), строки не имела. `PARITY-STOCK-INTRANSIT` — один заход класса B на два read-only замера (`PARITY-STOCK-SAME-DAY` плюс `PARITY-INTRANSIT-ROWWISE`); объединение законно, потому что работа однородная и только читающая — в отличие от деплоев (`§5`). accepted (архитектор).

§14 **Три строки заводятся по прямому поручению владельца (чат 2026-08-03).**

(а) `LS-SURFACE-FIELDS-SWEEP` — съём состава полей ГРАФИКОВ по трём оставшимся парам реестра (остатки, заказы в пути, счета покупателям). Основание: `ADR-114 §1` установил, что поверхностью пары является график, а не источник данных, и что инвентарь `reference/ls_custom_queries_2026-07-30.md` снимал состав полей только для одного источника из пяти. Цена невыполнения измерена: пара возвратов пять суток числилась несошедшейся по непроверенному признаку. **Порядок обязателен: заход идёт ДО `PARITY-STOCK-INTRANSIT`**, иначе слот класса B тратится на сравнение величины, которой на экране может не быть. Класс A, параллелится. READY, гейтов нет.

(б) `DQ-FRESHNESS-COVERAGE` — покрытие проверкой свежести шести фактовых таблиц ядра, у которых наблюдателя нет вовсе (`fact_returns`, `fact_purchases`, `fact_inventory`, `fact_payments`, `fact_customer_invoices`, `fact_commissionreportin`). Основание: `DQ-SOURCE-CAPTURE` установил, что гейт проверяет ТОЛЬКО домен продаж; цена измерена — таблица счетов простояла замороженной 58 суток и найдена случайно. Порог у каждой таблицы выводится из её собственной каденции, а не назначается общим числом; текст проверки для счетов уже готов (`invoices_loader_design §11`, техническая свежесть по `_loaded_at`, порог 48 часов) и служит образцом формы. **Гейт: `DQ-GATE-SCOPE-SPLIT`** — добавление проверок в текущий гейт до разделения периметра заставит провал по любой из шести гасить промоут продаж, то есть расширит уже известный радиус поражения. Подготовка — класс A, деплой — класс B. OPEN.

(в) `DQ-ALERT-FILTER-FIX` — починка лог-метрики специфичного алерта `msklad-dq-gate-failed`. Основание: `DQ-SOURCE-CAPTURE §2` — метрика ждёт `service_name="cf-dq"` и `severity>=ERROR`, а провал логируется `CRITICAL` на ресурсе Workflow; за 90 суток точным фильтром ноль совпадений, проверено запросом. Алерт выглядит полностью настроенным и не может сработать по построению — отказ тихий. Приёмка: исправленный фильтр даёт ненулевую выдачу на УЖЕ СЛУЧИВШЕМСЯ провале (`2026-08-01T18:02:01Z`), то есть проверяется на реальном событии, а не на синтетике (`05_CONVENTIONS` Часть I, расширение `ADR-074`). Класс B — правка живой конфигурации алертинга; мандат НЕ выдан. От `DQ-GATE-SCOPE-SPLIT` НЕ зависит, идёт независимо. READY.

accepted (архитектор — состав и гейты; заведение — поручение владельца).

**Последствия:**
- [сейчас] `07_STATE §Открытые вопросы` и `07_GAPS.md` — заводится строка `DEPLOY-PROCEDURE` (PARTIAL: текст записан, остаток — первое применение).
- [сейчас] `07_STATE`/`07_GAPS.md`/`07_ARCHIVE.md` — строки `MAY-ZONE-CLOSE` и `DEPLOY-ORDER-ADJ` заводятся и архивируются тем же проходом (открыты и закрыты одним блоком), одним коммитом в оба файла (`ADR-106 §2`).
- [сейчас] `07_STATE §Мандат Claude Code: класс задач` — две постоянные строки: `DEPLOY-PROCEDURE, подготовка` (A, параллель нет, постоянный, Пишет: `reference/deploy_procedure_<date>.md`, `05_CONVENTIONS.md` отдельным коммитом); `DEPLOY-PROCEDURE, первое применение` (B, НЕ выдан — исполняется вместе с первым деплоем). Строка `MAY-ZONE-CLOSE` заводится и убирается тем же коммитом вместе с задача-строкой (`ADR-090 §1`).
- [сейчас] `07_GAPS.md` строка `PARITY-ZONE-SNAPSHOT-DECISION` → `DONE`, перенос в `07_ARCHIVE.md` дословно; парная строка таблицы мандата убирается (`ADR-090 §1`).
- [отдельным коммитом] `02_ERP_CONTRACTS.md §core.fact_sales_profit` — плашка датированного снимка по тексту-кандидату 1 (`reference/deploy_order_adj_2026-08-03.md §12`); STABLE-док, вне пяти файлов сборки.
- [отдельным коммитом] `02_ERP_CONTRACTS.md §оракул`, строка `reference/parity_registry.md` — дописка про однократный съём по каденции, текст-кандидат 2 там же; существующий текст ячейки не переписывается; STABLE-док, вне пяти файлов сборки.
- [отдельным коммитом] `05_CONVENTIONS.md` Часть II — подстановка присвоенного номера `ADR-115` вместо формулировки «номер подставляется сборкой» (прецедент `ADR-106 §7`); правка внесена сессией коммитом `1f8cf3c`, подстановка номера — отдельной именованной правкой, не сборкой.
- [сейчас] `07_GAPS.md` строка `CODE-REPO-SEED-REST` → `DONE`, перенос в `07_ARCHIVE.md` дословно; парная строка таблицы мандата убирается (`ADR-090 §1`).
- [сейчас] `07_GAPS.md` строка `SALES-REFRESH-WINDOW` — снимается хвост «выбор варианта — владелец», заменяется дословной ссылкой на решение `§7` (вариант 3 сейчас, вариант 1 цель); указывается, что вопрос `UPDATE transaction_date` закрыт dry-run.
- [сейчас] `07_GAPS.md` строка `INGEST-MOMENT-ZONE-FIX` — снимается дописка «подготовка ТОЛЬКО после закрытия `Q-92`» (`ADR-091 §7`, superseded `ADR-101 §6`), приводится в соответствие со строкой таблицы мандата.
- [сейчас] `07_GAPS.md` строка `INVOICES-LOADER-DEPLOY` — дописывается предусловие: патч обязан быть в код-репо, процедура переноса назначается `DEPLOY-PROCEDURE`; мандат ВЫДАН (чат 2026-08-03), бриф `briefs/INVOICES-LOADER-DEPLOY.md`.
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + таблица мандата — заводятся строки `SALES-INGEST-PATCH` (READY, класс A, параллель да, Пишет: `reference/code/cf-facts/`, `reference/parity_registry.md`, `reference/sales_ingest_patch_<date>.md`), `PARITY-INTRANSIT-ROWWISE` (READY, класс B, НЕ выдан) и `PARITY-STOCK-INTRANSIT` (READY, класс B, НЕ выдан — зонтичный заход на два замера).
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + таблица мандата — заводятся строки `LS-SURFACE-FIELDS-SWEEP` (READY, A, параллель да, Пишет: `reference/ls_surface_fields_sweep_<date>.md`), `DQ-FRESHNESS-COVERAGE` (OPEN, подготовка A / деплой B НЕ выдан, гейт `DQ-GATE-SCOPE-SPLIT`, Пишет: `reference/code/cf-dq/`, `reference/dq_freshness_coverage_<date>.md`), `DQ-ALERT-FILTER-FIX` (READY, B, НЕ выдан, Пишет: `reference/dq_alert_filter_fix_<date>.md`).
- [сейчас] `07_GAPS.md` строка `PARITY-STOCK-SAME-DAY` — снимается гейт «после восстановления потока»: `core.fact_inventory` (`cf-inventory`, 21:00 UTC) и `marts.inventory_health` живы и от аварии DQ Gate не зависят (`reference/core_freshness_sweep_2026-08-02.md`, строки 224/239); гейт был выписан шире, чем следует из фактов. Остаётся мандат класса B.
- [задачей INVOICES-LOADER-DEPLOY] первое применение процедуры, оно же её приёмка.
- [задачей DQ-GATE-RECOVERY-CONFIRM] вердикт о восстановлении потока и о необходимости `FACTS-BACKFILL`; настоящим решением не предвосхищается.
- [без правок] `06_DECISIONS_LOG.md` прошлые ADR — `ADR-065` не пересматривается, его развилка исполняется, а не отменяется; `ADR-113` подтверждается и не меняется.
- [без правок] `reference/dq_gate_behavior_probe_2026-08-02.md`, `reference/sales_refresh_window_2026-08-01.md` — датированы, не переписываются (`ADR-043`).

Введено сессией `DEPLOY-ORDER-ADJ` (2026-08-03, архитектор, без брифа, `ADR-086 §1/§3`), применено сборкой буфера 2026-08-03. Три пункта последствий ([отдельным коммитом], `02_ERP_CONTRACTS.md` ×2 и `05_CONVENTIONS.md` ×1) в тексте блока несли маркер `[сейчас]`; переквалифицированы сборкой в `[отдельным коммитом]` по прямому решению владельца (чат 2026-08-03, «вариант 1»), поскольку `02_ERP_CONTRACTS.md`/`05_CONVENTIONS.md` не входят в пять файлов, которые сборка коммитит (`_ASSEMBLER.md §4 Шаг 7`).

## ADR-116 · Очередь до финиша Epic-1 зафиксирована; пара «Продажи» закрывается правилом, а не досевом; мандат класса B на `PARITY-STOCK-INTRANSIT` выдан поимённо

**Статус: accepted** (§1 владелец — выбор варианта 1, чат 2026-08-03; архитектор — фактура · §2 архитектор; заведение — поручение владельца · §3 владелец — мандат выдан, чат 2026-08-03 · §4 владелец — форма, чат 2026-08-03 · §5 владелец — порядок, чат 2026-08-03 · §6 архитектор).

**Контекст:** владелец запросил разбор строки «Следующий шаг» стенд-апа обычными словами и очередь
задач до закрытия проекта. Разбор нашёл в стенд-апе две вещи: развилку, у которой нет предмета
(второй «готовый патч» существует как решение без кода, `ADR-115 §7`), и порядок, при котором
деплой предшествует восстановлению остановленного конвейера, то есть не может быть принят
подтверждённым логом (`_METHOD §3`). Одновременно проверено, чем именно отгейчен мандат на
единственный заход, способный закрыть две пары реестра сразу.

**Решение:**

§1 **Пара «Продажи» (строки 18/19 реестра) закрывается явным правилом, не числовым нулём.**
Основание — `ADR-067`: финишная черта допускает закрытие расхождения правилом/ADR наравне с
`0,00`. Расхождение разложено до копейки (`ADR-100 §1`, `ADR-103`, `ADR-104`, остаток `−0,73` KGS),
оба механизма названы и адресованы: периметр — патчем `SALES-INGEST-PATCH`, строки-сироты —
конвенцией `C2` вперёд и целевым вариантом 1 `SALES-REFRESH-WINDOW`. Ветка досева (вариант 2)
отклонена: она не устраняет причину, а переписывает историю, при этом несёт незакрытый источник
COGS шире 90 суток (`SALES-MERGE-DRYRUN`, вопрос (б)), риск задвоения (`ADR-097 §5` ловушка i) и
ограничена по времени выходом мая из окна обновления `2026-08-29`. accepted (архитектор —
фактура и рекомендация; владелец — выбор варианта 1, чат 2026-08-03).

§2 **Исполнение `§1` — задачей `PARITY-SALES-CLOSE`, класс A, роль архитектор.** Состав работы,
пять обязательных пунктов и границы — `reference/finish_queue_adj_2026-08-03.md §4`. Задача не
деплоит, в `core.*` не пишет, выбор объекта паритета продаж (вариант (a), `ADR-103 §8`) не
пересматривает. Гейта нет; место в очереди — после трёх остальных пар, по доводу `ADR-085 §2`:
правило моста, выведенное по одной паре без взгляда на остальные, уже однажды выдало паре
возвратов оракул, не покрывавший её данные. accepted (архитектор; заведение — поручение
владельца, чат 2026-08-03).

§3 **Мандат класса B на `PARITY-STOCK-INTRANSIT` — ВЫДАН поимённо владельцем** (чат 2026-08-03).
Единственное основание класса B — доступ к секрету `msklad-token` (`ADR-076 §1`); задача только
читает, записи не производит нигде, отката не требует по построению. Анализ (целесообразность,
пять рисков с митигацией, чего анализ не утверждает) предъявлен ДО выдачи по прецеденту
`ADR-115 §12` — `reference/finish_queue_adj_2026-08-03.md §3.2`. Шесть оговорок `ADR-076 §5` и
форма логирования `ADR-079 §6` обязательны без изменений. **Ограничение момента старта, не
мандата:** заход не начинается до закрытия `LS-SURFACE-FIELDS-SWEEP` (`ADR-115 §14а`). accepted
(владелец — выдача; архитектор — анализ и оговорка порядка).

§4 **Досев счетов и пересверка идут под ОДНИМ мандатом класса B.** `INVOICES-BACKFILL` и
`INVOICES-PARITY-RECHECK` объединяются: предмет один, второй шаг есть приёмка первого, откат один
и тот же — состояние `core.fact_customer_invoices` до досева. Запрет пакетных мандатов
`ADR-115 §5` не нарушается: он касается деплоев, у которых откаты различны и связывание откатов
есть самостоятельный риск. Сам мандат этим ADR НЕ выдаётся — выдаётся поимённо перед заходом,
после предъявленного анализа. accepted (владелец — форма, чат 2026-08-03; архитектор —
обоснование границы с `ADR-115 §5`).

§5 **Порядок очереди до финиша.** (1) `DQ-GATE-RECOVERY-CONFIRM` параллельно с
`LS-SURFACE-FIELDS-SWEEP` → (2) `PARITY-STOCK-INTRANSIT` → (3) `INVOICES-BACKFILL` +
`INVOICES-PARITY-RECHECK` → (4) `PARITY-SALES-CLOSE` → (5) деплой `SALES-INGEST-PATCH` →
(6) `FACTS-BACKFILL`. Восстановление впереди деплоя — подтверждение `ADR-115 §3` фактом: приёмка
деплоя требует подтверждённого лога реального прогона (`_METHOD §3`), а прогонов нет с
`2026-08-01T18:00Z`; деплой счётчик не двигает и временно ухудшает видимое расхождение до
`≈1,75 %` (риск `R1`, `ADR-101`); у деплоя есть незакрытое предусловие класса B (живой `GET`
позиции `entity/retaildemand`). accepted (владелец — порядок, чат 2026-08-03; архитектор —
доводы).

§6 **Развилка стенд-апа об объединении двух деплоев объявляется пустой и владельцу повторно не
задаётся** (`ADR-056 §5`: вопрос, снимаемый чтением репо, не задаётся). У `SALES-REFRESH-WINDOW`
выбран вариант 3 — плашка «датированный снимок» без правки кода (`ADR-115 §7`); целевой вариант 1
не построен (`ADR-109 §1`: артефакта подготовки нет). accepted (архитектор).

**Последствия:**
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
  задач` — заводится строка `PARITY-SALES-CLOSE` (READY, класс A, параллель да, Пишет:
  `reference/parity_registry.md` только строки 18/19, `reference/parity_sales_close_<date>.md`),
  парность `ADR-090 §1` соблюдена.
- [сейчас] `07_STATE §Мандат Claude Code: класс задач`, строка `PARITY-STOCK-INTRANSIT` — «НЕ
  выдан» → «ВЫДАН поимённо `ADR-116 §3` (владелец, чат 2026-08-03)»; дописывается ограничение
  порядка «старт после `LS-SURFACE-FIELDS-SWEEP`».
- [сейчас] `07_GAPS.md` строка `PARITY-STOCK-INTRANSIT` — тот же перевод мандата; статус `READY`
  не меняется.
- [сейчас] `07_GAPS.md` строки `INVOICES-BACKFILL` и `INVOICES-PARITY-RECHECK` — дописка «идут
  одним заходом под одним мандатом класса B (`ADR-116 §4`)»; гейт `INVOICES-LOADER-DEPLOY` у
  первой снимается (задача закрыта DONE 2026-08-03).
- [сейчас] `07_GAPS.md` строки `SALES-PERIMETER-EXTEND`, `SALES-CONSIGNMENT-REVENUE`,
  `SALES-INGEST-PATCH` — дописка: деплой стоит в очереди ПОСЛЕ финишного счётчика (`ADR-116 §5`),
  строки 18/19 реестра закрываются правилом (`§1`), а не результатом этого деплоя.
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_ARCHIVE.md` — строка
  `FINISH-QUEUE-ADJ` заводится и архивируется тем же проходом (прецедент
  `INVOICES-STALENESS-PROBE`); парная строка мандата не заводится (класс A, задача закрыта).
- [задачей PARITY-SALES-CLOSE] строки 18 и 19 `reference/parity_registry.md`, колонка «Статус
  сходимости» — форма «закрыто правилом» с датой, остающейся разностью и ссылкой на ADR.
- [без правок] `00_CHARTER.md`, `_METHOD.md` — финишная черта и метод не меняются: `§1`
  пользуется уже существующей клаузой `ADR-067`, нового способа закрытия пары не вводится.
- [без правок] `05_CONVENTIONS.md` — заморозка процессного слоя (`ADR-107 §1`) соблюдена, решений
  о методе этот ADR не содержит.

Введено сессией `FINISH-QUEUE-ADJ` (2026-08-03, архитектор, без брифа, `ADR-086 §1`, прецедент
`PARALLEL-CHECK-NO-BRIEF`), применено сборкой буфера 2026-08-03.

## ADR-117 · Мандат класса B на `INVOICES-BACKFILL` выдаётся постфактум

**Статус: accepted** (владелец — мандат выдан в чате 2026-08-04; форма решения без поля «Статус»
предъявлена владельцу при сборке буфера 2026-08-04, принята явным выбором варианта А в том же чате).

**Контекст:** сессия `INVOICES-BACKFILL` заведена и исполнена в один день (бриф собран 2026-08-03,
исполнен 2026-08-04) без предварительно выданного мандата класса B (`07_STATE.md §Мандат Claude
Code: класс задач`, строка `INVOICES-BACKFILL`, стояла «НЕ выдан»). Сессия остановилась на этом
гейте (`CONTEXT GAP`), владелец выдал мандат постфактум в чате, до действия был отдельным сообщением
объявлен объект (`msklad-bi-prod.core.fact_customer_invoices`, read-only), объём (шаги 1-5 брифа) и
откат (не требуется) — форма `ADR-077 §6`.

**Решение:** форма прецедента (`ADR-092 §1`) сохранена по существу. Единственное основание класса
B — операция затрагивает живую инвесторскую/денежную поверхность `core.fact_customer_invoices`
(чтение, не запись, `ADR-076 §1`); объём ровно read-only: `bq show` схемы + два `SELECT` (сплошной
календарь суток `2026-06-05`…`2026-08-03`, агрегат `COUNT(*)/MIN/MAX` таблицы) + один справочный
`SELECT` свежести (`_loaded_at`); откатывать нечего — состояние живой таблицы не менялось ни одной
командой сессии. Вердикт read-only проверки: пробелов в покрытии нет, досев закрыт побочным
продуктом деплоя `INVOICES-LOADER-DEPLOY` (2026-08-03) — `total_rows=4526` живой таблицы =
`fetched=meta_size=4526` боевого прогона; полный провенанс — `reference/invoices_backfill_2026-08-03.md`.

**Последствия:**
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач`, строка `INVOICES-BACKFILL` — «НЕ выдан»
  → «выдан постфактум (чат, 2026-08-04, `ADR-117`)».
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md`, строка `INVOICES-BACKFILL` — статус →
  `DONE`; полный текст переносится в `07_ARCHIVE.md` тем же коммитом (`ADR-064`), парная строка
  мандата убирается тем же коммитом (`ADR-090 §1`).
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md`, строка `INVOICES-PARITY-RECHECK` — гейт
  «`INVOICES-BACKFILL` не завершена» снимается (первая половина условия закрыта); мандат класса B
  по-прежнему НЕ выдан (`ADR-116 §4`) — вторая половина условия блокировки остаётся до отдельного
  решения по мандату на `INVOICES-PARITY-RECHECK`.

Введено сессией `INVOICES-BACKFILL` (2026-08-04, класс B), применено сборкой буфера 2026-08-04
после явного апрува владельца формы решения (блок не нёс поля «Статус» и размеченных последствий с
маркером времени прихода — структурное расхождение с шаблоном `05_CONVENTIONS` Часть III, снятое
владельцем выбором варианта А в чате 2026-08-04, а не догадкой сборщика).

## ADR-118 · Мандат класса B на `INVOICES-PARITY-RECHECK` выдаётся постфактум

**Статус: accepted** (владелец — мандат выдан в чате 2026-08-04, форма прецедента `ADR-092 §1`/`ADR-117`).

**Контекст:** сессия `INVOICES-PARITY-RECHECK` остановилась на `CONTEXT GAP` (мандат класса B не
выдан, `07_STATE.md:658`/`07_GAPS.md:80` на момент старта). Владелец выдал мандат постфактум в чате
той же сессии; до действия был отдельным сообщением объявлен объект (read-only `GET
entity/invoiceout` + read-only `SELECT marts.customer_invoices_ar`), объём (один постраничный обход
без фильтра по дате, форма — дословно из `reference/code/cf-finance/invoices.py`) и откат (не
требуется, ничего не пишется) — форма `ADR-077 §6`.

**Решение:** форма прецедента (`ADR-092 §1`/`ADR-117`) сохранена по существу. Шесть оговорок
`ADR-076 §5` и форма логирования `ADR-079 §6` соблюдены (токен не печатался, `grep -rF` по значению
токена дал 0 совпадений дважды — сразу после прогона и перед коммитом по всему `reference/`, якоря
`date -u`/`gcloud auth list` в начале (`06:33:23Z`) и в конце (`06:44:20Z`) прогона, форма запроса
не изобретена — взята из снапшота боевого кода). Read-only проверка: живой построчный `GET
entity/invoiceout` (пагинация `limit=100`, `expand=state,agent,salesChannel,rate.currency`) плюс
`SELECT marts.customer_invoices_ar`; разность сведена без остатка к одной причине — асинхронность
суточного снимка (~8,5ч между суточной загрузкой и снятием эталона): `0,022%`/`0,0034%`/`0,00%`/
`0,030%`. Полный провенанс — `reference/invoices_parity_recheck_2026-08-04.md`.

**Последствия:**
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач`, строка `INVOICES-PARITY-RECHECK` —
  «НЕ выдан» → строка убирается вместе с закрытием задачи (`ADR-090 §1`).
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md`, строка `INVOICES-PARITY-RECHECK` —
  статус → `DONE`; полный текст переносится в `07_ARCHIVE.md` тем же коммитом (`ADR-064`).
- [сейчас] `reference/parity_registry.md`, строка 23 — заполнена мостом и статусом (сессия
  `INVOICES-PARITY-RECHECK`, до сборки).

Введено сессией `INVOICES-PARITY-RECHECK` (2026-08-04, класс B), применено сборкой буфера
2026-08-04.

## ADR-119 · Пара «Продажи» закрыта правилом; приёмка пары — инвариант разложения, а не порог на разность; счётчик финиша `5/7`, две строки реестра ждут вердикта

**Статус: accepted** (архитектор — вывод правила и фактура; выбор формы закрытия принят владельцем ранее, `ADR-116 §1`; §3 выбор по строке 21 отдельно за владельцем).

**Контекст.** `ADR-116 §1` (владелец, чат 2026-08-03, вариант 1) постановил закрывать пару «Продажи» явным правилом, а не числовым нулём, и поручил исполнение задаче `PARITY-SALES-CLOSE` (`ADR-116 §2`). Требовалось вывести и опубликовать само правило. Дополнительно при сверке счётчика перед стартом обнаружено, что план очереди `ADR-116 §5` и фактическое состояние реестра расходятся на две строки.

**Решение:**

§1 **Разность пары «Продажи» закрыта разложением без свободного остатка.** `Δ = +288 789,73` KGS
(`0,3097 %`) = `M1` строки-сироты `+1 630 240,60` (`ADR-100 §1`, `MERGE` без ветки удаления,
`bq_ops.py:237-305`) минус `|M2|` периметр типов документов `1 341 450,08` (`ADR-103`, `ADR-104`:
розница `1 188 422,00` / 506 док., продажи по отчётам комиссионера `2 133 028,08` / 7 док.,
отгрузка комиссионеру `1 980 000,00` / 1 док. встречным знаком) плюс `ε = −0,79` KGS. Замыкание
подтверждено вторым независимым путём — реконструкцией стороны эталона до копейки и до документа.
У каждого механизма установлена причина в коде нашей стороны и назван адрес устранения: `M2` —
`SALES-INGEST-PATCH` (текст готов, не задеплоен), `M1` — конвенция `C2` вперёд и целевой
вариант 1 `SALES-REFRESH-WINDOW` (не построен). **accepted** (архитектор — вывод правила; выбор
формы закрытия принят владельцем ранее, `ADR-116 §1`).

§2 **Критерий сходимости пары «Продажи» — инвариант разложения, не порог на разность.** Пара
считается сошедшейся в смысле `ADR-068 §1`, пока `Δ = M1 − |M2| + ε` при `|ε| < 1` KGS, то есть
пока каждый сом разности отнесён к одному из двух названных механизмов; переоткрытие — только при
появлении свободного остатка. Порог на `Δ` отвергнут как невыполнимый по построению: обе
назначенные правки меняют `Δ` — сначала вверх (после `SALES-INGEST-PATCH` до `+1 630 239,81`,
`≈1,7485 %`, риск `R1` `ADR-101`), затем вниз (после варианта 1 `SALES-REFRESH-WINDOW` до
`−0,79`), и всякий порог объявил бы верную правку регрессом. Основание формы —
`05_CONVENTIONS` Часть I `★ Критерий приёмки выводится из формулы, не из свойства данных`
(`ADR-075 §3`). Закрытие относится к зоне паритета (май-2026, датированный снимок,
`ADR-115 §11`), не к текущим данным. **accepted** (архитектор).

§3 **Счётчик финиша после этой сессии — `5/7`, а не `7/7`; строки 21 и 22 требуют отдельной
задачи-вердикта `PARITY-STOCK-INTRANSIT-CLOSE`.** Сошлись строки 17, 18, 19, 20, 23. Строки 21
(«Остатки товаров», `Q-80`) и 22 («Заказы поставщикам в пути», `Q-81`) измерены
`PARITY-STOCK-INTRANSIT` (2026-08-04), но статуса сходимости не получили, и это не упущение той
сессии: объявление пары сошедшейся есть адъюдикация правила-моста, не отдаваемая
сессии-исполнителю (`ADR-076 §4`, уточнено `ADR-091 §8`). По трёхшаговой приёмке `ADR-084 §6`
строке 22 недостаёт только шага 1 при точном построчном совпадении (341 позиция, полный внешний
джойн, `0`/`0`/`0`), строке 21 — шагов 1 и 3 (остаток `4,00` штуки на `121 878,00`, `0,0033 %`,
одна лишняя строка, причина не устанавливалась). Заводится задача `PARITY-STOCK-INTRANSIT-CLOSE`:
класс A, роль архитектор, гейта нет, новых измерений не требует для строки 22; для строки 21
вопрос «закрывать остаток правилом либо устанавливать причину» — развилка владельца, предъявлена
ему отдельно. **accepted** (архитектор — фактура и форма; выбор по строке 21 — владелец).

§4 **Процессное следствие, названное без правки правил (`ADR-107 §4`).** Расхождение счётчика с
планом `ADR-116 §5` возникло потому, что план приписал шагу-замеру счётчик, который его класс
работ дать не может: задача-замер пары и задача-вердикт по паре — разные работы. Наблюдение
фиксируется строкой в артефакте и в `§Подробности для модели`; строки реестра под себя не
получает, правила очереди этим решением не правятся (процессный слой заморожен, `ADR-107 §1`,
и ни одна задача паритета без такой правки не встаёт). **accepted** (архитектор).

**Последствия:**
- [сейчас] `reference/parity_registry.md` строки 18 и 19, колонка «Статус сходимости» — дописка
  «закрыта правилом» с величинами обоих механизмов, инвариантом приёмки, предупреждением о
  росте `Δ` после деплоя и ссылкой на артефакт. Внесено сессией `PARITY-SALES-CLOSE` в ветке,
  слито в `main` до сборки.
- [сейчас] `reference/parity_sales_close_2026-08-04.md` — артефакт адъюдикации. Внесён сессией
  в ветке, слит в `main` до сборки.
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
  задач` — строка `PARITY-SALES-CLOSE` закрыта DONE и уезжает в `07_ARCHIVE.md` дословно,
  строка мандата убирается (парность `ADR-090 §1`).
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
  задач` — заводится строка `PARITY-STOCK-INTRANSIT-CLOSE` (READY, класс A, параллель да,
  Пишет: `reference/parity_registry.md` только строки 21/22,
  `reference/parity_stock_intransit_close_<date>.md`), парность `ADR-090 §1` соблюдена.
- [сейчас] `07_STATE §Стенд-ап` — заменяется целиком (`ADR-084 §1`); окончательный текст,
  применённый этой сборкой, взят из более позднего по порядку `§5` блока `FACTS-FLOW-RESTORE-ADJ`
  (`ADR-121`), не из этого решения — см. §11 отчёта сборки.
- [без правок] `06_DECISIONS_LOG.md` тексты `ADR-100`, `ADR-103`, `ADR-104`, `ADR-116` — не
  редактируются (append-only), настоящее решение их не отменяет и не двигает их значений.
- [без правок] `02_ERP_CONTRACTS.md §оракул` — форма регистрации реестра (`ADR-078`) не меняется:
  правится статус строки внутри уже зарегистрированного файла, не сам файл как артефакт.
- [без правок] `00_CHARTER §критерии успеха` — формулировка финишной черты не меняется: закрытие
  правилом уже предусмотрено ею дословно («расхождение закрыто явным правилом/ADR»).
- [задачей `PARITY-STOCK-INTRANSIT-CLOSE`] вердикты по строкам 21 и 22 реестра, закрытие `Q-80`
  и `Q-81`.
- [задачей `SALES-INGEST-PATCH` (деплой)] фактическое устранение `M2`; после него `Δ` вырастает
  до `≈1,7485 %` — ожидаемо по `§2`, переоткрытием пары не является.
- [задачей `SALES-REFRESH-WINDOW` (вариант 1)] фактическое устранение `M1`.

Введено сессией `PARITY-SALES-CLOSE` (2026-08-04, архитектор, без брифа, `ADR-086 §1`), применено
сборкой буфера 2026-08-04.

## ADR-120 · Пара «Заказы в пути» не сошлась — прежняя сверка была односторонней; правила-мосты строк 21 и 22 адъюдицированы; пара «в пути» не измерима при остановленной загрузке

**Статус: accepted** (архитектор — §1-§3, §5, §7; владелец — §6 выбор формы закрытия строки 21, чат 2026-08-04).

**Контекст.** Владелец поручил вынести вердикт по строке 22 реестра, ожидая формальности:
предыдущая сессия показала построчное совпадение `341` позиции при нулях во всех трёх множествах
полного внешнего джойна. Перед вердиктом проверено направление сверки, и оно оказалось одним из
двух.

**Решение:**

§1 **Прежняя сверка строки 22 была односторонней и вердикта не обосновывает.** Наша сторона
фильтровалась списком из 17 идентификаторов, снятых у источника, поэтому множество «есть у нас,
нет у источника» было исключено из рассмотрения построением запроса, а не измерено. Обоснование,
которым это было оправдано (полная перезапись таблицы каждым прогоном), не выполняется при
остановленных прогонах. **accepted** (архитектор; факт — замер `step1_run.log`/`step3_run.log`).

§2 **Строка 22 сошедшейся не объявляется; расхождение `+2 234 268,75` KGS (`+2,92 %`).** Наша
стороны `17` заказов / `268` позиций / `78 748 543,05` против `76 514 274,30` источника.
Разложение полное: пересечение `15` заказов / `241` позиция совпадает построчно и до копейки; два
заказа с нулевым остатком стоят в целевых статусах на обеих сторонах и исключаются фильтром моста
симметрично (проверено прямым запросом); два заказа присутствуют только у нас. Причина —
заморозка `core.fact_purchases` на `2026-08-01T17:02:16Z` при меняющемся день ко дню составе
статусов у источника; текущее состояние этих двух заказов в источнике не снималось (класс B), и
«достаточное объяснение» не выдаётся за «измеренную причину». **accepted** (архитектор).

§3 **Правило-мост строки 22 адъюдицировано** (шаг 1 приёмки `ADR-084 §6`, закрывает половину
`Q-81`): объект паритета — `in_transit_sum_kgs`, зерно — позиция заказа поставщику; периметр —
фильтр `status_name IN ('В пути','Прибыл частично') AND in_transit_sum_kgs > 0`, применяемый
симметрично к обеим сторонам (симметричность подтверждена замером, не постулирована);
`in_transit_sum_usd` и `fx_rate_used` — DERIVED, вне паритета (`ADR-067 §4`), считаются от курса
на момент пересборки. **accepted** (архитектор).

§4 **Правило одновременности `ADR-079 §3` для пар-снимков «на сейчас» имеет специальную форму:
при остановленной загрузке нашей стороны пара не измерима в принципе, а не измерима с оговоркой.**
Расхождение таких пар растёт пропорционально простою и о качестве соответствия не говорит ничего.
Частный случай уже принятого `ADR-111 §3`, распространения на другие правила не требует.
Следствие: закрытие строки 22 гейтится восстановлением потока и пересверкой, `Q-81` → `PARTIAL`.
**accepted** (архитектор).

§5 **Расчётные метрики `marts.inventory_health` — вне паритета явным решением** (закрывает
половину `Q-80` ровно в форме, предписанной его собственным текстом): `coverage_days_90d_calendar`,
`coverage_days_true_adt`, `is_low_stock`, `is_oos`, `is_toxic` — наш расчёт поверх факта, стороны
сравнения у МойСклада нет ни на одном из трёх уровней эталона (`ADR-067 §2`). Объект паритета
строки 21 — только `stock`. Критерий тот же, по которому вне реестра оставлены `marts.weight_flow`
и величины в USD. **accepted** (архитектор).

§6 **Остаток строки 21 закрывается установлением причины, а не правилом; заводится
`PARITY-STOCK-ROWWISE`** (класс B, живой `GET report/stock/all` плюс read-only `bq query` одним
окном, с сохранением сырых строк обеих сторон — прежняя сессия их не сохранила, отчего причина и
не восстанавливается офлайн). Мандат класса B этим решением НЕ выдаётся — выдаётся поимённо
владельцем перед заходом, после предъявленного анализа (форма `ADR-115 §12`, прецедент
`ADR-116 §3`). **accepted** (владелец — выбор формы закрытия, чат 2026-08-04; архитектор —
постановка).

§7 **Процессное наблюдение, названное без правки правил (`ADR-107 §4`).** Класс дефекта —
«сверка по списку одной стороны выдаётся за построчную сверку пары» — воспроизводим и уже дал
один ложноположительный результат. Правило метода под него не пишется: процессный слой заморожен,
и ни одна задача паритета без такой правки не встаёт. Наблюдение живёт в артефакте и в
`§Подробности для модели`; строки реестра под себя не получает. **accepted** (архитектор).

**Последствия:**
- [сейчас] `reference/parity_registry.md` строки 21 и 22, колонка «Статус сходимости» — внесено
  сессией `PARITY-STOCK-INTRANSIT-CLOSE` в ветке, слито в `main` до сборки.
- [сейчас] `reference/parity_intransit_close_2026-08-04.md` плюс
  `reference/_scratch_PARITY-STOCK-INTRANSIT-CLOSE_2026-08-04/` — внесены сессией в ветке, слиты
  в `main` до сборки.
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` — `Q-81` `OPEN` → `PARTIAL`, `Q-80`
  `OPEN` → `PARTIAL`, оба с названным остатком и гейтом.
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
  задач` — строка `PARITY-STOCK-INTRANSIT-CLOSE`, заведённая `ADR-119 §3`, обновляется до
  `PARTIAL`, класс A, параллель да, Пишет: `reference/parity_registry.md` только строки 21/22,
  `reference/parity_intransit_close_<date>.md`, парность `ADR-090 §1` соблюдена.
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
  задач` — заводится строка `PARITY-STOCK-ROWWISE` (OPEN, класс B, параллель нет, мандат НЕ
  выдан, Пишет: `reference/parity_registry.md` только строка 21,
  `reference/parity_stock_rowwise_<date>.md`), парность `ADR-090 §1` соблюдена.
- [сейчас] `07_STATE §Стенд-ап` — заменяется целиком (`ADR-084 §1`); окончательный текст,
  применённый этой сборкой, взят из более позднего по порядку `§5` блока `FACTS-FLOW-RESTORE-ADJ`
  (`ADR-121`), не из этого решения — см. §11 отчёта сборки.
- [без правок] `reference/sql/sq_marts_in_transit.sql` — дефекта в тексте витрины нет, множество
  заказов марта совпадает с ядром в обе стороны (проверено).
- [без правок] `06_DECISIONS_LOG.md` тексты `ADR-079`, `ADR-084`, `ADR-111` — не редактируются
  (append-only); настоящее решение их не отменяет, `§4` есть частный случай `ADR-111 §3`.
- [задачей `PARITY-STOCK-ROWWISE`] построчная сверка `stock`, закрытие остатка `Q-80`.
- [задачей восстановления потока] пересверка строки 22 и закрытие остатка `Q-81`.

Введено сессией `PARITY-STOCK-INTRANSIT-CLOSE` (2026-08-04, архитектор, без брифа, `ADR-086 §1`),
применено сборкой буфера 2026-08-04.

## ADR-121 · Мандат класса B на восстановление потока не открывается — блокировка снимается сама, дата вычислена; вариант (в) исполнен классом A; правка гейта принята как плановая работа

**Статус: accepted** (архитектор).

**Контекст.** `ADR-115 §3` поставил условие: класс B на восстановление открывается только при
опровергающем исходе замера `DQ-GATE-RECOVERY-CONFIRM`. Замер дал опровергающий исход (блок
пережил переход выходные→будни), и очередь `architect_review_queue_2026-08-03-2.md` вынесла
владельцу выбор из трёх вариантов. Разбор начат с воспроизведения формулы чека, которого до сих
пор не делалось.

**Решение:**

§1 **Мандат класса B на восстановление потока НЕ открывается.** Опровергнута была гипотеза
«снимется при переходе на будни», а не «снимется вообще»; второе из первого не следует, и
различает их расчёт, а не наблюдение. Окно `ma7` скользит по ядру и при замороженном ядре
пустеет; при `ma7 == 0` чек проходит безусловно. Гарантированная верхняя граница самоснятия —
`2026-08-10`; ожидаемая фактическая — прогон `2026-08-04T18:00:02Z`, где `ma7 = 5 558 128,17`
требует `555 812,82`, а сутки `2026-08-04` уже несут `1 136 030,01`. Формула проверена против
боевого лога (совпадение `ma7` до единицы). **accepted** (архитектор).

§2 **Вариант (а), ручной промоут, отклонён.** Не-идемпотентная операция на живых данных ради
выигрыша порядка десяти часов; цена ошибки несимметрична (правка боевого ядра против часа
наблюдения); вдобавок проводит в ядро ровно те данные, на которые указал чек, не ответив, прав
ли чек. **accepted** (архитектор).

§3 **Вариант (б), правка порога или логики с передеплоем, отклонён как СРОЧНАЯ мера и принят как
ПЛАНОВАЯ.** Отклонён сейчас: чек в данный момент работает правильно; калибровка не сделана, а
прецедент неверной калибровки в проекте есть; деплой идёт через процедуру с веткой и read-back.
Принят как плановый: выходной порог `0,03` не описывает субботу этого бизнеса — `2026-07-18`
`1 752 665`, `2026-07-25` `65 385`, `2026-08-01` `25 921`, две из трёх проваливают чек на
порядки, что сходится с реконструкцией по девяти субботам (`DQ-SOURCE-CAPTURE`). Правка обязана
идти ОДНИМ деплоем с `DQ-GATE-SCOPE-SPLIT`: из двух дефектов периметр опаснее порога, потому что
именно он превращает тихую субботу в остановку трёх таблиц и в искажение двух клиентских
страниц. Нового числа порога это решение НЕ назначает — калибровка требует замера распределения.
**accepted** (архитектор).

§4 **Вариант (в) исполнен этой сессией классом A; его цена была переоценена.** `msklad-token` и
живой `GET` не нужны: staging есть свежая копия источника (перезаливается ежечасно шагом,
стоящим в DAG до гейта, `ADR-113`), что подтверждено прямо — единственное значение `_loaded_at`
на всех `674` строках, `2026-08-04 07:01:08`. Падение выручки реально в источнике и видно по
числу документов. Остаток вопроса — ПРИЧИНА паузы — инструментом неустановим в принципе и
адресуется владельцу; живой `GET` к тому же API ответа не добавил бы. **accepted** (архитектор).

§5 **Рекомендация — вариант (г), дождаться прогона и проверить**, с именованной контрольной
точкой `FACTS-FLOW-RESUME-CONFIRM` утром `2026-08-05` (`ADR-084 §5`: работа без идентификатора
есть незакрытая постановка). **Крайний срок вмешательства — `2026-08-07` включительно**: окно
`MERGE` семисуточное, `2026-08-01` выпадает из него `2026-08-08`, после чего пропущенные сутки
сами не заедут и появится дыра. При отрицательном исходе контрольной точки правильным
становится (а) как разовая мера с немедленной постановкой (б). **accepted** (архитектор).

§6 **Вердикт `FACTS-BACKFILL` «не требуется» подтверждён расчётом, статус не меняется.** При
ожидаемом исходе промоут пройдёт с окном 7 суток от `2026-07-28`, а staging несёт ровно
`07-28…08-04`, то есть пропущенные `08-01` и `08-03` заедут тем же прогоном. Условность вердикта
сохраняется и теперь имеет точную дату истечения (§5). **accepted** (архитектор).

§7 **Механизм строк-сирот подтверждён на текущих данных, вне мая-2026** (`2026-07-30`: `3`
строки, `100 602,00` KGS в ядре при их отсутствии в источнике). Новой строки реестра не заводит
(`ADR-051 §2`: предмет назван `SALES-REFRESH-WINDOW`), закрытия пары «Продажи» не пересматривает
— правило закрытия построено на инварианте разложения и относит такие строки к названному
механизму. **accepted** (архитектор).

**Последствия:**
- [сейчас] `reference/facts_flow_restore_adj_2026-08-04.md` плюс
  `reference/_scratch_FACTS-FLOW-RESTORE-ADJ_2026-08-04/` — внесены сессией в ветке, слиты в
  `main` до сборки.
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_ARCHIVE.md` — строка
  `FACTS-FLOW-RESTORE-ADJ` заводится и архивируется тем же проходом; строка мандата не заводится
  (задача закрыта в момент заведения, `ADR-090 §2` по смыслу).
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
  задач` — заводится строка `FACTS-FLOW-RESUME-CONFIRM` (OPEN, класс A, параллель да, мандат
  постоянный, Пишет: `reference/facts_flow_resume_confirm_<date>.md`), парность `ADR-090 §1`.
- [сейчас] `07_GAPS.md` строка `FACTS-BACKFILL` — дописка о подтверждённом вердикте и точном
  крайнем сроке `2026-08-07`; статус `OPEN` не меняется.
- [сейчас] `07_GAPS.md` строка `DQ-GATE-SCOPE-SPLIT` — дописка: правка периметра идёт ОДНИМ
  деплоем с калибровкой выходного порога (`§3`).
- [сейчас] `07_STATE §Стенд-ап` — заменяется целиком (`ADR-084 §1`), берётся из этого блока
  (последний по порядку `§5` среди трёх блоков буфера 2026-08-04).
- [сейчас] `06_INDEX.md` — строки этого ADR и двух предыдущих (`ADR-119`, `ADR-120`).
- [без правок] `reference/code/cf-dq/` и любой прод-код — не трогались; решение ничего не
  деплоит и патча не готовит.
- [без правок] `03_PIPELINE_SPEC.md §DQ` — пороги не меняются этим решением; изменение придёт
  вместе с калибровкой, отдельным ADR.
- [задачей `FACTS-FLOW-RESUME-CONFIRM`] проверка исхода прогона `2026-08-04T18:00Z`.
- [задачей `DQ-GATE-SCOPE-SPLIT`] плановая правка периметра и порога одним деплоем.

Введено сессией `FACTS-FLOW-RESTORE-ADJ` (2026-08-04, архитектор, третий ход, без брифа,
`ADR-086 §1`), применено сборкой буфера 2026-08-04.

---

## ADR-122 · Калибровка выходного порога `drift_check`; мандат класса B на `DQ-GATE-SCOPE-SPLIT-DEPLOY`; процедура деплоя распространена на Cloud Workflows; адъюдикация fail-open от 2026-08-03 найдена неприземлённой и восстановлена тремя адресами

**Контекст.** `ADR-121 §3` принял правку порога как ПЛАНОВУЮ, нового числа не назначил и потребовал
замера распределения; `ADR-121 §4-бис` одновременно запретил назначать калибровку в любую сторону до
подтверждения гипотезы владельца о причине паузы. Пара требований в производной записи реестра
превратилась в гейт без исполнителя. Владелец запросил три вещи: решение по калибровке, мандат на
деплой периметра, бриф деплоя.

**Решение:**

§1 **Замер распределения исполнен настоящей сессией** (класс A, три read-only скрипта; прецедент
исполнения замера архитекторским заходом — `ADR-121 §4`). Разделение «калибровка отдельным шагом,
деплой после» не потребовалось. Артефакт `reference/dq_gate_threshold_calibration_2026-08-04.md`.
**accepted** (архитектор).

§2 **Выходной порог `0,03` остаётся без изменения — потому что задача калибровки не имеет решения
на этой метрике.** Три основания, проверяемые по таблицам артефакта: (i) распределение `ratio` по
выходным непрерывно через `0,03`, разделяющего числа нет; (ii) `16` из `31` провала выходных за
пятнадцать месяцев — сутки без единого документа, они проваливают чек при любом положительном
пороге, и снижение в тридцать раз их не касается; (iii) при верности гипотезы владельца чек сработал
ПРАВИЛЬНО, и ослабление провело бы следующий пропуск ввода молча в инвесторскую витрину — тихое
примирение цифр, запрещённое `00_CHARTER §главный принцип`. **accepted** (архитектор).

§3 **Требование `ADR-121 §3` исполнено; направление связки восстановлено по канону.** Канон `06`
налагает ограничение на правку ПОРОГА («не выезжает отдельным деплоем»); производная дописка
`07_GAPS.md:78` наложила его на правку ПЕРИМЕТРА, чем сделала деплой заложником калибровки, которую
`ADR-121 §4-бис` запрещал назначать. Действует формулировка `06`; дописка приводится к ней. По
существу вопрос снят: правки порога нет, бандлить нечего. Текст `ADR-121` не редактируется
(`06` append-only). **accepted** (архитектор).

§4 **Вердикт §2 не означает, что гейт исправен.** `drift_check` отождествляет «почти не продавали»
с «данные испорчены», тогда как `8,9 %` суток этого бизнеса не имеют документов вовсе, на всех днях
недели. Метрика, у которой нормальный и аварийный вход дают одно значение, не калибруется — она
переделывается. Заводится `DQ-GATE-METRIC-REDESIGN`; форма НЕ назначается. **accepted** (архитектор).

§5 **Мандат класса B на `DQ-GATE-SCOPE-SPLIT-DEPLOY` выдан поимённо.** Форма — один поимённый ADR на
один деплой (`ADR-115 §5`), прецеденты `ADR-092`/`ADR-108`/`ADR-116 §3`/`ADR-117`. Объект: два Cloud
Workflows `asia-east1` — `msklad-pipeline-hourly` (`000003-f02`) и `msklad-pipeline-weekly`
(`000003-fa9`). sha256 патча: hourly `821edbeef502d786b2345219a01f0f7c2cee36d8d0b184a7ac63b85395eac47f`,
weekly `9b7fcee08a2f2b46704c3b7aa8d83a26317260c6e79bb675e712ae76884d1647`. Откат — повторный деплой
исходного текста названных ревизий (сохранён снимком `2026-08-02` и seed-коммитом `master`).
Единственное основание класса B — правка живой конфигурации Workflows; секретов задача не касается,
поэтому шесть оговорок `ADR-076 §5` (адресующие работу с токеном) буквально не применяются, а
`ADR-076 §6` в силе. Риск ограничен фактом: патч есть чистая перестановка строк, проверено
механически (`diff <(sort живой) <(sort патч)` пуст для обоих файлов). **accepted — мандат ВЫДАН
владельцем (чат 2026-08-04, «вариант 1, подтверждаю мандат») после предъявленного анализа**;
архитектор — фактура и форма, выдача — владелец, по прецеденту `ADR-115 §12`/`ADR-116 §3`.

§6 **Процедура вариант Б распространяется на класс объектов Cloud Workflows** с двумя
подстановками: снимок живого и read-back идут через `describe --format="value(sourceContents)"`
вместо архива; `.gcloudignore` (`ADR-040`) неприменим, его эквивалент — побайтовое равенство
развёрнутого текста файлу ветки. Инвариант «основная ветка равна проду» переносится без изменений.
**accepted** (архитектор).

§7 **Два предусловия деплоя, без которых процедура неисполнима.** (i) Исходников workflow в код-репо
нет — деплою предшествует seed обоих YAML в `master` из ЗАДЕПЛОЕННОГО объекта (форма `ADR-094 §1`),
иначе инвариант «ревизия ↔ коммит» неверен с первого коммита. (ii) Размещение — каталог `workflows/`
верхнего уровня, не внутри `cf-facts/`: иначе следующий `--source=cf-facts/` затянет YAML в архив
функции и нарушит `ADR-040` (прецедент цены — `RQ-3`). **accepted** (архитектор).

§8 **Адъюдикация `reference/dq_gate_fail_open_adj_2026-08-03.md` найдена неприземлённой.** Её
ADR-кандидат номера не получил, в `06_INDEX.md` отсутствует, три требования §7 к
`DQ-GATE-SCOPE-SPLIT` не дошли ни до реестра, ни до брифа подготовки. Решение по каждому:
(1) ограниченность блокирующего действия → `DQ-GATE-METRIC-REDESIGN`, деплой не гейтит;
(2) ветка `ma7 == 0 → passed=True` → `DQ-GATE-FAIL-OPEN-FIX`, деплой не гейтит — формулировка
кандидата «разделение сохранит дефект в каждой из частей» проверена и не подтверждается: после
разделения закупки и возвраты через гейт не проходят вовсе, fail-open остаётся ровно там, где был;
(3) свежесть на промоутнутой таблице → дописка в `DQ-FRESHNESS-COVERAGE`. **accepted** (архитектор).

§9 **Класс дефекта назван:** механический признак готовности (`ADR-109 §1`) ловит отсутствие
ПОДГОТОВКИ и не ловит отсутствие ПРИЗЕМЛЕНИЯ — артефакт в `reference/` есть, решения в `06` нет,
признак показывает зелёное. Третий инстанс `GATE-STATE-VERIFIABILITY`; отдельной строки не
заводится (`ADR-051 §2`), дописывается в неё. **accepted** (архитектор).

**Последствия:**
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code` —
  заводится `DQ-GATE-SCOPE-SPLIT-DEPLOY` (READY, класс B, параллель нет, мандат ВЫДАН поимённо,
  Пишет: `reference/code/cf-facts/MANIFEST.md`, `reference/dq_gate_scope_split_deploy_<date>.md`,
  `reference/_scratch_DQ-GATE-SCOPE-SPLIT-DEPLOY_<date>/`), парность `ADR-090 §1`.
- [сейчас] то же — заводится `DQ-GATE-FAIL-OPEN-FIX` (OPEN; подготовка A, Пишет:
  `reference/code/cf-dq/`, `reference/dq_gate_fail_open_fix_<date>.md`; деплой B, НЕ выдан,
  гейтится `ADR-065`).
- [сейчас] то же — заводится `DQ-GATE-METRIC-REDESIGN` (OPEN, гейт `DQ-GATE-SCOPE-SPLIT-DEPLOY`;
  проектирование — класс A, Пишет: `reference/dq_gate_metric_redesign_<date>.md`).
- [сейчас] то же — заводится `DQ-GATE-SCOPE-CONFIRM` (OPEN, гейт `DQ-GATE-SCOPE-SPLIT-DEPLOY`,
  класс A, параллель да, Пишет: `reference/dq_gate_scope_confirm_<date>.md`).
- [сейчас] `07_STATE §Мандат Claude Code` — строка `DQ-GATE-SCOPE-SPLIT, деплой` УБИРАЕТСЯ
  (заменена строкой `DQ-GATE-SCOPE-SPLIT-DEPLOY`); строка `DQ-GATE-SCOPE-SPLIT, подготовка`
  ДЕРЖИТСЯ с пометкой «исполнена 2026-08-04» до архивации задача-строки (прецедент формы —
  `PARITY-REGISTRY (discovery-часть)`).
- [сейчас] `07_GAPS.md` строка `DQ-GATE-SCOPE-SPLIT` — статус OPEN не меняется; дописка: деплойная
  часть выделена в `DQ-GATE-SCOPE-SPLIT-DEPLOY`; калибровочный гейт снят вердиктом §2; дописка
  `ADR-121 §3` приводится к направлению канона (§3).
- [сейчас] `07_GAPS.md` строка `DQ-FRESHNESS-COVERAGE` — гейт перенаправлен с `DQ-GATE-SCOPE-SPLIT`
  на `DQ-GATE-SCOPE-SPLIT-DEPLOY`; дописано требование измерять свежесть на промоутнутой таблице по
  `_loaded_at` (§8 пункт 3). То же в её строке мандата.
- [сейчас] `07_GAPS.md` строка `GATE-STATE-VERIFIABILITY` — дописан третий инстанс (§9).
- [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_ARCHIVE.md` — строка
  `DQ-GATE-DEPLOY-ADJ` заводится и архивируется тем же проходом; строка мандата не заводится.
- [сейчас] `06_INDEX.md` — строка этого ADR.
- [сейчас] `briefs/DQ-GATE-SCOPE-SPLIT-DEPLOY.md`, `reference/dq_gate_deploy_adj_2026-08-04.md`,
  `reference/dq_gate_threshold_calibration_2026-08-04.md`,
  `reference/_scratch_DQ-GATE-DEPLOY-ADJ_2026-08-04/` — внесены сессией в ветке
  `s/DQ-GATE-DEPLOY-ADJ`, сливаются сборкой.
- [задачей `DQ-GATE-SCOPE-SPLIT-DEPLOY`] применение патча периметра к обоим живым Workflows, seed
  `workflows/` в код-репо, запись «ревизия ↔ коммит» в `MANIFEST.md`.
- [задачей `DQ-GATE-SCOPE-CONFIRM`] подтверждение на первом реальном провале `drift_check`, что
  закупки и возвраты исполнились, а промоут — нет; там же закрывается гэп наблюдения по логам гейта
  (`calibration §5`), форма запроса берётся из скриптов `DQ-SOURCE-CAPTURE`.
- [без правок] `03_PIPELINE_SPEC.md §DQ` — порог не меняется, править нечего; расхождение доки с
  кодом об источниках трёх величин остаётся `[без правок]` по `ADR-113`.
- [без правок] `06_DECISIONS_LOG.md` текст `ADR-121` — append-only, §3 правит только производную
  запись реестра.
- [без правок] `reference/dq_gate_fail_open_adj_2026-08-03.md`, `reference/dq_source_capture_2026-08-02.md` —
  датированы, не переписываются (`ADR-043`); поправки живут здесь и в артефактах этой сессии.

Статус: accepted (архитектор — §1-§4, §6-§9; §5 — фактура и форма архитектор, мандат класса B
ВЫДАН владельцем, чат 2026-08-04).

Введено сессией `DQ-GATE-DEPLOY-ADJ` (2026-08-04, архитектор, без брифа, `ADR-086 §1`), применено
сборкой буфера 2026-08-04.

- ADR-123: Правило-мост строки 21 реестра паритета адъюдицировано целиком; эталонный момент есть
  момент пересборки нашей стороны; критерий — инвариант разложения, не допуск; пара не сошлась

  **Статус: accepted** (архитектор — §1-§7, §9; §8 — постановка и форма архитектор, мандат класса B
  **ВЫДАН владельцем**, чат 2026-08-04, «идём вариантом А, мандат даю»).

  **Контекст.** `ADR-120 §5` сузил объект паритета строки 21 до одного `stock`; `ADR-120 §6`
  (выбор владельца) поручил закрывать остаток установлением причины, а не правилом. Причина
  установлена замером `PARITY-STOCK-ROWWISE` (2026-08-04, класс B): расхождение порождается
  расходными документами, проведёнными между разовым суточным снимком `core.fact_inventory` и
  моментом живого `GET report/stock/all`. Остаток работы — переформулировать причину в правило-мост
  и вынести вердикт по паре; это адъюдикация, не отдаваемая сессии-исполнителю (`ADR-076 §4`,
  уточнено `ADR-091 §8`). Строка реестра назвала три кандидата: (a) синхронизация живого запроса с
  моментом `cf-inventory`, (b) именованный допуск как функция элапса, (c) переопределение объекта
  паритета.

  **Решение:**

  §1 **Мост строки 21 выведен целиком по пяти элементам `ADR-079 §2`** (шаг 1 приёмки
  `ADR-084 §6`): документ-источник — отчёт `report/stock/all` (уровень эталона (i), `ADR-067 §2`);
  поле — `rows[].stock`; наша сторона — `core.fact_inventory.stock` за партицию `date_snapshot`
  (`marts.inventory_health` есть переупаковка и мостом не является, `ADR-079 §2`); зерно — товар,
  ключ равен UUID последнего сегмента `meta.href` (`cf-inventory/main.py:39-49`); периметр —
  `stockMode=all`, `quantityMode=all` (`cf-inventory/helpers.py:57-67`), `entity_type='product'`.
  **accepted** (архитектор).

  §2 **Правило конвертации в KGS к этой строке не применяется — величина не денежная.** `stock`
  есть количество (штуки), денежная колонка таблицы — `cost_kgs` (`price/100`,
  `cf-inventory/main.py:52-53`), и она объектом паритета строки не является по уже действующему
  `ADR-120 §5`; настоящее решение его не расширяет. Единица «KGS-штук» в
  `parity_stock_intransit_2026-08-04.md`, `parity_stock_rowwise_2026-08-04.md` и в строке 21
  реестра ошибочна как ярлык при верных числах; артефакты не переписываются (`ADR-043`), единица
  фиксируется здесь. **accepted** (архитектор).

  §3 **Эталонный момент — момент ПЕРЕСБОРКИ нашей стороны, а не момент чтения нашей таблицы.**
  Это буквальное прочтение `ADR-079 §3` («эталон снимается в том же окне, что и пересборка
  витрины»), а не новое правило; новым является лишь то, что момент назван и потому правило впервые
  исполнимо: `cf-inventory-trigger`, `schedule: 0 21 * * *`, `timeZone: UTC` (`11_INFRA_FACTS.md:98`),
  то есть `21:00Z` = `03:00` Бишкек; наблюдение согласуется (партиция `2026-08-04` несёт единственный
  `_loaded_at = 2026-08-03T21:00:04Z`). Живой контрольный `GET` для сверки строки 21 снимается в
  окне, примыкающем к этому прогону, окно фиксируется в UTC в артефакте; замер в произвольный час
  сверкой пары не является — он измеряет оборот. Кандидат (a) принят в этой форме. **accepted**
  (архитектор).

  §4 **Критерий сходимости пары — инвариант разложения, не порог на разность** (форма скопирована с
  `ADR-119 §2`, основание — `05_CONVENTIONS` Часть I ★ «Критерий приёмки выводится из формулы, не из
  свойства данных», `ADR-075 §3`). Пара сошлась в смысле `ADR-068 §1`, когда
  `Δ = Σ(движение) + Σ(сироты)` раскладывается без свободного остатка, где `Σ(движение)` —
  расхождения на совпавших ключах с ОДНОРОДНЫМ знаком (проверяемый признак отнесения к документам,
  проведённым после снимка), а `Σ(сироты)` — строки, отсутствующие у одной стороны, с установленной
  природой КАЖДОЙ. Смесь знаков либо сирота без установленной природы есть свободный остаток.
  Переоткрытие — только при появлении свободного остатка. **accepted** (архитектор).

  §5 **Вердикт: пара НЕ сошлась.** Два независимых основания, каждого достаточно. (1) Ни один из
  двух исполненных замеров не является допустимым измерением пары по §3: эталон снят через `8ч 30м`
  (`2026-08-04T05:29:54Z`) и `12ч 06м` (`09:06:08Z`) после пересборки нашей стороны. Утверждение
  `parity_stock_intransit_2026-08-04.md:57-58` о выполненной одновременности опирается на разрыв
  между ЧТЕНИЯМИ сторон в секунды — не тот интервал, который регулирует `ADR-079 §3`. Замер
  `PARITY-STOCK-ROWWISE` установил механизм и в этом качестве состоятелен полностью. (2) В замере
  `09:06Z` есть свободный остаток: семь строк-сирот (`6` наших на `108,00` штук, `1` источника на
  `44,00`), природа которых объявлена неустановленной самим артефактом; брутто `152,00` штуки,
  `0,12 %` величины. Малость от требования §4 не освобождает — тем же признаком `ADR-100 §1` поймал
  семь строк-сирот продаж на `1 630 240,60` KGS, годами проходивших приёмку по итогу. `Q-80`
  остаётся `PARTIAL`, счётчик реестра остаётся `5/7`; по `ADR-084 §6` строке 21 недостаёт шагов 2 и
  3. **accepted** (архитектор).

  §6 **Кандидат (b) — именованный допуск как функция элапса — отклонён по двум независимым
  основаниям.** По форме: допуск на `Δ` есть порог на разность, уже отвергнутый для пары реестра
  (`ADR-119 §2`, `ADR-075 §3`), а калиброванный «темп оборота» есть ровно свойство данных;
  дополнительно такой допуск делает строку неопровержимой навсегда. По факту: два замера одних суток
  несовместимы с элапс-пропорциональной моделью — элапс вырос в `1,42` раза, разность в `1 339` раз
  (`4,00` штуки за `8ч30м`, включая бишкекское утро, против `5 355,00` за следующие `3ч36м`).
  Механизм остаётся названным `PARITY-STOCK-ROWWISE`; неверна параметризация по стенным часам:
  остаток уменьшается в момент ПРОВЕДЕНИЯ документа, а проведение у этого бизнеса приходит пачками
  — свойство установлено независимо (`reference/dq_gate_threshold_calibration_2026-08-04.md`:
  `40` суток из `451`, `8,9 %`, без единого документа продажи; ответ владельца по паузе
  `2026-08-01…08-03`). Пришлись ли `5 351,00` штуки на реальный оборот окна `05:30…09:06Z` или на
  ввод накопленных документов — НЕ устанавливалось; различитель назван в §8. **accepted**
  (архитектор).

  §7 **Кандидат (c) — переопределить объект паритета — отклонён как решение и принят как разведка.**
  Величины МойСклада, несущей историю остатков, репо не знает ни на одном из трёх уровней
  `ADR-067 §2`, а объект строки уже сужен `ADR-120 §5`; переопределять его второй раз, не назвав
  замену, есть импровизация (`_METHOD §6`). Законная часть (c) выделена в **`CONTEXT GAP`:
  принимает ли `report/stock/all` ретроспективный момент.** Репо этого не устанавливает — сплошной
  поиск `moment=` по всем `*.md`/`*.py` даёт ноль совпадений в связке со `stock` (совпадения только
  у `report/profit/*`, где `momentFrom`/`momentTo` задают ПЕРИОД, а не срез); `cf-inventory` передаёт
  четыре параметра без временных (`helpers.py:60-66`); `REPORT-FIELDS` вызывал эндпоинт как
  `?limit=1`. Отсутствие параметра в нашем коде не есть отсутствие его в API
  (`★ Успех инструмента ≠ факт`). Положительный ответ снимает требование §3 целиком и делает пару
  измеримой в любой час. Разведка назначена §8, отдельной строки реестра не получает (`ADR-051 §2`).
  **accepted** (архитектор).

  §8 **Заводится `PARITY-STOCK-SNAPSHOT-SYNC` — единственная работа, закрывающая строку 21.**
  Класс B, единственное основание класса — доступ к секрету `msklad-token` (`ADR-076 §1`); задача
  только читает, в `core.*`/`marts.*` и в живые конфигурации не пишет; оговорки `ADR-076 §5` и форма
  логирования `ADR-079 §6` обязательны. Три шага одним заходом: (1) проба ретроспективного момента
  (§7) — два `GET` в одну минуту, с параметром и без, причём момент выбирается заведомо дальше
  наблюдённой суточной амплитуды, иначе идентичность ответов неразличима с «параметр
  проигнорирован»; (2) пересверка в окне `21:00Z` против партиции того же прогона (§3), окно в UTC —
  в артефакт; проверяемое предсказание правила: в `03:00` по Бишкеку документы не проводятся,
  поэтому `Σ(движение)` обязана быть близка к нулю, и значимое `Σ(движение)` в ночном окне
  опровергло бы механизм §6 и потребовало бы новой адъюдикации; (3) природа семи сирот — прямой
  `GET entity/product/<uuid>` по каждому UUID из
  `reference/_scratch_PARITY-STOCK-ROWWISE_2026-08-04/step6_join_result.json`. Приёмка дословно
  (`ADR-086 §1`): «Разность `Δ` замера в окне `21:00Z` разложена без свободного остатка на
  `Σ(движение)` с однородным знаком и `Σ(сироты)` с установленной природой каждой из семи строк;
  либо назван и предъявлен свободный остаток». Объявление пары сошедшейся производит архитектор по
  этому артефакту, не исполнитель (`ADR-076 §4`, `ADR-091 §8`). Объявление действия по `ADR-077 §6`
  заготовлено в `reference/parity_stock_rowwise_bridge_2026-08-04.md §8`.

  **Мандат класса B ВЫДАН владельцем поимённо** (чат 2026-08-04, «идём вариантом А, мандат даю»),
  после предъявленного анализа и объявления действия — форма `ADR-115 §12`, прецеденты `ADR-116 §3`,
  `ADR-120 §6`, `ADR-122 §5`. Мандат покрывает задачу целиком (все три шага), основание одно —
  секрет `msklad-token`; задача только читает, откат не требуется, потому что состояние не меняется.

  **Порядок исполнения задан владельцем — вариант А (тем же решением, чат 2026-08-04): шаг 1
  исполняется ОТДЕЛЬНЫМ заходом первым, в любой час, до шагов 2-3.** Основание выбора: шаг 1 ко
  времени суток не привязан и его исход определяет, нужны ли шаги 2-3 в ночной форме вообще —
  положительный ответ по ретроспективному моменту снимает требование §3 и делает пару измеримой в
  любой час. Отрицательный ответ оставляет §3 единственной формой, и тогда шаги 2-3 идут вторым
  заходом в окне `21:00Z`. Разрезом задачи на две строки реестра это НЕ является (`ADR-091 §1`: оба
  шага одного класса, один набор файлов, один мандат) — это порядок внутри одной задачи.
  **accepted** (архитектор — постановка и форма; мандат и порядок — владелец).

  §9 **Побочные находки, названные без правки правил (`ADR-107 §4`).** (i) GCS-архив `cf-inventory`
  содержит разобранные записи, не ответ источника (`main.py:172`/`179-182` против комментария
  `main.py:177`), поэтому датированным снимком эталона служить не может — сверка с ним есть
  сравнение нашей таблицы с нашим же артефактом (`00_CHARTER §главный принцип`); правка кода не
  назначается, цена хранения сырых строк не оценивалась. (ii) Прочтение `ADR-079 §3` как «одно окно
  ЧТЕНИЯ» — воспроизводимый класс, унаследованный вторым замером от первого; правило метода под него
  не пишется (процессный слой заморожен, `ADR-107 §1`), уточнение живёт в §3 настоящего решения,
  которое есть правило-мост предметной пары, а не правило метода. (iii) Парность `ADR-090 §1` была
  нарушена на самой задаче `PARITY-STOCK-ROWWISE-BRIDGE`: строка реестра существовала в обоих
  файлах, строки мандата не было; задача закрывается DONE тем же проходом, поэтому строка мандата
  задним числом не заводится, дефект назван. **accepted** (архитектор).

  **Последствия:**
  - [сейчас] `reference/parity_registry.md` строка 21, колонки «мост» и «статус сходимости» —
    внесено сессией в ветке, слито в `main` до сборки.
  - [сейчас] `reference/parity_stock_rowwise_bridge_2026-08-04.md` — артефакт адъюдикации, внесён
    сессией в ветке.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
    задач` — строка `PARITY-STOCK-ROWWISE-BRIDGE` закрыта DONE и уезжает в `07_ARCHIVE.md` дословно;
    строки мандата у неё не было (§9 iii), убирать нечего, и это не расхождение.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
    задач` — заводится строка `PARITY-STOCK-SNAPSHOT-SYNC` (READY, класс B, параллель нет, мандат
    ВЫДАН поимённо владельцем, чат 2026-08-04), парность `ADR-090 §1` соблюдена; тексты обеих строк —
    в `STATE_PATCH` выше.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` — `Q-80` остаётся `PARTIAL`, гейт
    переводится на `PARITY-STOCK-SNAPSHOT-SYNC`, в текст дописывается вердикт §5.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` — строка `PARITY-STOCK-INTRANSIT-CLOSE`
    остаётся `PARTIAL`, в её гейте «строка 21 — `PARITY-STOCK-ROWWISE`» заменяется на «строка 21 —
    `PARITY-STOCK-SNAPSHOT-SYNC`».
  - [сейчас] `07_STATE §Стенд-ап` — заменяется целиком (`ADR-084 §1`) текстом из `STATE_PATCH`, если
    настоящий блок последний по порядку `§5`; иначе применяется стенд-ап последнего блока.
  - [сейчас] `06_INDEX.md` — строка этого ADR.
  - [задачей `PARITY-STOCK-SNAPSHOT-SYNC`] допустимое измерение пары, природа семи сирот, ответ по
    ретроспективному моменту, закрытие остатка `Q-80` и вердикт о сходимости строки 21.
  - [без правок] `06_DECISIONS_LOG.md` тексты `ADR-079`, `ADR-084`, `ADR-100`, `ADR-119`, `ADR-120` —
    не редактируются (append-only); настоящее решение их не отменяет и значений не двигает: §3 есть
    прочтение `ADR-079 §3`, §4 есть перенос формы `ADR-119 §2` на другую пару, §2 опирается на
    `ADR-120 §5` и его не расширяет.
  - [без правок] `00_CHARTER §критерии успеха` — формулировка финишной черты не меняется.
  - [без правок] `03_PIPELINE_SPEC.md`, `02_ERP_CONTRACTS.md`, `11_INFRA_FACTS.md` — фактов не
    двигает; `11_INFRA_FACTS.md:98` цитируется как уже существующий факт.
  - [без правок] `reference/code/cf-inventory/**` — код читался, не правился; правка формы
    GCS-архива (§9 i) не назначается.
  - [без правок] `reference/parity_registry.md` строки 17-20, 22, 23 — не трогались.

Введено сессией `PARITY-STOCK-ROWWISE-BRIDGE` (2026-08-04, архитектор, без брифа, `ADR-086 §1`),
применено сборкой буфера 2026-08-04.

- ADR-124: Пара «Заказы поставщикам в пути» (строка 22 реестра) СОШЛАСЬ — `Δ = 0` точно; причина
  прежнего расхождения подтверждена измерением, оговорка `ADR-120 §2` снята; мост достроен до пяти
  элементов и получил критерий-инвариант; клиентская витрина отстаёт от ядра на сутки

  **Статус: accepted** (архитектор).

  **Контекст.** `ADR-120 §2` объявил строку 22 несошедшейся с расхождением `+2 234 268,75` KGS
  (`+2,92 %`), назвал причиной заморозку `core.fact_purchases` и честно пометил вывод «достаточным
  объяснением», а не измеренной причиной; `ADR-120 §4` признал пару неизмеримой при остановленной
  загрузке. Загрузка восстановилась (`FACTS-FLOW-RESUME-CONFIRM`, 2026-08-05), гейт снят замером,
  и `PARITY-STOCK-INTRANSIT-RECHECK` (2026-08-05, класс B) провела первое допустимое измерение.
  Объявление пары сошедшейся есть адъюдикация правила-моста, не отдаваемая сессии-исполнителю
  (`ADR-076 §4`, `ADR-091 §8`), поэтому вердикт выносится здесь.

  **Решение:**

  §1 **Строка 22 реестра паритета объявляется СОШЕДШЕЙСЯ; счётчик реестра `5/7` → `6/7`.** Приёмка
  `ADR-084 §6` пройдена по всем трём шагам: правило-мост адъюдицировано (`ADR-120 §3`, достроено §3
  настоящего решения), численная сверка проведена в обе стороны по собственному признаку каждой
  стороны, расхождение УСТРАНЕНО — не закрыто правилом, устранять нечего. `Δ = 0` **точно**, а не в
  пределах допуска: строгое десятичное сравнение всех `251` ключа `(purchase_order_id, position_id)`
  даёт `0` расхождений, симметрическая разность пуста и на уровне заказов (`16`/`16`), и на уровне
  позиций (`251`/`251`); обе стороны — `78 184 949,425` KGS. Величина источника независимо пересчитана
  из СЫРЫХ тел ответов по формуле `reference/code/cf-facts/fetch_purchases.py:141/147` и совпала со
  всеми 251 значением нашей стороны, то есть подтверждена независимо от разбора сессии-исполнителя.
  Полукопейка между `,42` и `,43` внутри артефакта-измерения есть направление округления половины
  тыйына на агрегате; сноска ¹ того артефакта называет механизм неточно, числа верны, артефакт
  датирован и не переписывается (`ADR-043`). **accepted** (архитектор).

  §2 **Утверждение `ADR-120 §2` о причине подтверждено измерением; его собственная оговорка
  («достаточное объяснение, а не измеренная причина») СНИМАЕТСЯ.** Основания независимы, каждого
  достаточно. (1) Прямое: оба заказа прежнего одностороннего хвоста — `08bfe70e-…`
  («PDMKGFRU26070711») и `e7d7a18b-…` («HOLI260708 (TOR)») — несут статус «Прибыл» на ОБЕИХ сторонах,
  то есть на свежих данных наша сторона воспроизводит статус источника по тем самым заказам, из-за
  которых расхождение и возникло; альтернатива «структурный дефект соответствия» этим опровергнута,
  а не признана менее правдоподобной. (2) Арифметическое: прежняя разность в точности равна сумме
  этих двух заказов (`1 197 000,00 + 1 037 268,75 = 2 234 268,75`), а прежнее пересечение пятнадцати
  заказов воспроизводится в сегодняшнем замере ТОЧНО — сегодняшние `251` позиция минус единственный
  вошедший в периметр заказ `04a41f62-…` дают `241` позицию и `76 514 274,300` KGS, разность `0,000`.
  Пятнадцать заказов не шевельнулись ни на тыйын за 25,5 часов, поэтому гипотеза «две встречные
  поправки случайно скомпенсировались» исключена измерением, а не правдоподобием — прецеденты класса
  `ADR-097 §1`, `ADR-103`. **accepted** (архитектор).

  §3 **Мост строки 22 достраивается до пяти элементов `ADR-079 §2`; наша сторона моста —
  `core.fact_purchases`.** `ADR-120 §3` назвал объект, зерно и периметр, но не назвал поле
  документа-источника и правило конвертации в KGS, тогда как `ADR-079 §2` считает мост выведенным
  только при пяти. Пробел закрывается фактами, не предположением: документ-источник —
  `entity/purchaseorder` плюс `entity/purchaseorder/<id>/positions` отдельным вызовом (`expand=positions`
  в списочном режиме не работает, `fetch_purchases.py:5`), отбор по `state.meta.href`; поля —
  `positions[].inTransit`/`price`/`discount` и заголовочный `rate.value`; конвертация (`ADR-010`) —
  `price_kgs = price/100 × rate.value`, `in_transit_sum_kgs = round(price_kgs × inTransit ×
  (1 − discount/100), 4)`; фильтр периода отсутствует по построению (величина «на момент») и
  заменяется правилом одновременности §4; зерно — позиция заказа, ключ
  `(purchase_order_id, position_id)`. Слой нашей стороны фиксируется как `core.fact_purchases`, а не
  `marts.in_transit`: `ADR-079 §2` прямо отказывает снимку март-SQL в статусе моста, и тот же выбор
  уже сделан для соседней снимочной пары (`ADR-123 §1`). Оговорка `ADR-079 §4` о ненадёжности
  `is_in_transit` к этому мосту неприменима — периметр опирается на `status_name`, как и предписывает
  `02_ERP_CONTRACTS.md:408` п.5. Позиции с количеством в пути при нулевой цене (`13` в замере)
  отбрасываются фильтром симметрично на обеих сторонах: это граница объекта паритета, не расхождение.
  Значений `ADR-120 §3` настоящий параграф не двигает. **accepted** (архитектор).

  §4 **Критерий сходимости пары — инвариант разложения, не порог на `Δ`** (форма `ADR-119 §2` и
  `ADR-123 §4`, основание `05_CONVENTIONS` Часть I ★ «Критерий приёмки выводится из формулы, не из
  свойства данных», `ADR-075 §3`). Число `0,00` в критерий не записывается: величина есть снимок «на
  момент», и порог на `Δ` был бы порогом на момент съёма, а не на качество соответствия — то же
  возражение, которым `ADR-123 §6` отклонил допуск-по-элапсу у соседней пары. Пара считается
  сошедшейся в смысле `ADR-068 §1`, пока выполняются одновременно: (1) обе стороны сняты внутри
  ОДНОГО часового цикла `cf-facts`, элапс от `MAX(_loaded_at)` до живого `GET` назван числом, окно
  записано в UTC (при остановленной загрузке пара не измерима в принципе — `ADR-120 §4` в силе без
  изменений); (2) `Δ = Σ(движение) + Σ(сироты)` раскладывается без свободного остатка, где природа
  каждой сироты установлена, а знак движения однороден. Переоткрытие — только при появлении
  свободного остатка. **Почему часовое окно допустимо там, где суточное окно строки 21 недопустимо:**
  критерий здесь самопроверяем — движение внутри элапса не прячется в агрегате, оно проявляется либо
  односторонним ключом (смена статуса выводит заказ из периметра), либо расхождением на совпавшем
  ключе (изменение `inTransit`); в замере при элапсе `16-18` минут обе категории ПУСТЫ, то есть
  тишина интервала измерена, а не предположена. Оговорка, обязательная к чтению: это факт об ЭТОМ
  интервале, а не свойство часового окна вообще. **accepted** (архитектор).

  §5 **Судьба всех четырёх заказов прежнего расхождения закрыта измерением; остатка нет.** Два заказа
  хвоста (`08bfe70e-…`, `e7d7a18b-…`) вышли из целевых статусов на обеих сторонах; `04a41f62-…` вошёл
  в периметр на обеих сторонах (`10` позиций, `1 670 675,125`); `aab67ac6-…` остался в целевом статусе
  с нулевым остатком на обеих сторонах и исключается фильтром симметрично. Каждое положение измерено
  прямым запросом, а не выведено вычитанием (`ADR-120 §5 (v)`: вычитание в этой ветке работы дважды
  давало ошибку). Наблюдение «`in_transit_sum_kgs` не обнуляется у заказов со статусом „Прибыл“» на
  измерение не влияет (заказы вне периметра на обеих сторонах) и дефектом НЕ объявляется: позиции
  этих заказов у источника не запрашивались, поэтому неизвестно, отдаёт ли МойСклад по ним
  `inTransit = 0`; вопрос передаётся в `INTRANSIT-SURFACE-LAG`, отдельной строки не получает
  (`ADR-051 §2`). **accepted** (архитектор).

  §6 **Клиентская витрина «в пути» отстаёт от ядра на сутки; заводится `INTRANSIT-SURFACE-LAG`.**
  `sq_marts_in_transit` имеет расписание `every 24 hours` с якорем около `13:09Z`
  (`reference/sq_cadence_2026-07-27.md:83`), тогда как `core.fact_purchases` перезаписывается
  ежечасно; scorecard «Сумма KGS в пути» страниц «Склад» и «Закупки в пути» читает
  `marts.in_transit.in_transit_sum_kgs` (`reference/ls_surface_fields_sweep_2026-08-04.md §Пара 2`) —
  то есть клиент видит снимок возрастом до ~24 часов у величины с семантикой «на сейчас», и именно
  этим механизмом страницы двое суток показывали `2 234 268,75` KGS идущим товаром, которого источник
  таковым уже не числил (`ADR-120`, клиентское следствие). Прежняя запись `ADR-120 §Последствия`
  `[без правок] sq_marts_in_transit.sql` с обоснованием «множество заказов марта совпадает с ядром в
  обе стороны (проверено)» была верна как наблюдение и вводит в заблуждение как основание: совпадение
  держалось на том, что ядро стояло замороженным, — заморозка МАСКИРОВАЛА отставание витрины. Третий
  инстанс класса «утверждение о МЕХАНИЗМЕ прочитано как утверждение о СОСТОЯНИИ» внутри истории одной
  пары (первые два — `ADR-120 §1`, `§2`); правило метода под него не пишется (`ADR-107 §1/§4`).
  Задача `INTRANSIT-SURFACE-LAG` несёт два вопроса: (1) подтвердить замером, что расхождение множеств
  объясняется суточной каденцией (`_mart_refreshed_at` витрины против `MAX(_loaded_at)` ядра), и
  предъявить величину эффекта на клиентской странице; (2) обнуляется ли `in_transit_sum_kgs` у
  полностью прибывших заказов и что отдаёт по ним источник. **Форма фикса НЕ назначается** — два
  кандидата (поднять каденцию витрины до часовой, правка живого `transferConfig`, класс B; либо
  опубликовать момент пересборки на странице, владелец) несопоставимы по цене без замера эффекта.
  Диагностика и подготовка — класс A, мандат постоянный; правка живой конфигурации — класс B, мандат
  НЕ выдан. **Вердикта §1 это не отменяет:** объект паритета адъюдицирован слоем ядра (§3), отставание
  витрины есть дефект переупаковки `core` в витрину — отдельный слой и фикс-форвард туда
  (`00_CHARTER §закон-следствие`), а не повод объявить пару несошедшейся или, наоборот, замолчать
  разницу. **accepted** (архитектор).

  §7 **Названные пределы вердикта.** (i) Сходимость измерена на ОДНОМ окне; регресс возможен и
  обнаруживается повторным замером по §4. (ii) Ноль НЕ доказывает корректность формулы курса:
  `fetch_purchases.py:141` несёт `(currency_rate or 1.0)` — форму, уже названную нарушением
  `ADR-029 §1` в scope `INGEST-CURRENCY-ASSERT`; в замере `n_missing_rate_value = 0`, подстановка не
  срабатывала ни разу, то есть ноль есть свойство ДАННЫХ замера (`05_CONVENTIONS` Часть I ★). Новой
  строки это не заводит — объект уже назван (`ADR-051 §2`). (iii) Момент перехода двух заказов хвоста
  в статус «Прибыл» внутри МойСклада не снимался; на разложение не влияет. (iv) Строка 21 настоящим
  решением не двигается ни в какую сторону: допустимого измерения по ней не было, шаг 2
  `PARITY-STOCK-SNAPSHOT-SYNC` не исполнен. (v) `7/7` упирается не в один шаг, а в замер ПЛЮС вердикт
  по нему — объявление пары сошедшейся остаётся адъюдикацией (`ADR-076 §4`, `ADR-091 §8`,
  `ADR-123 §8`) и живёт остатком строки `PARITY-STOCK-INTRANSIT-CLOSE`; учёт назван явно, чтобы
  счётчик снова не разошёлся с планом очереди при исправном исполнении (`ADR-119 §4`).
  **accepted** (архитектор).

  **Последствия:**
  - [сейчас] `reference/parity_registry.md` строка 22, колонки «мост» и «статус сходимости» —
    внесено сессией в ветке `s/PARITY-STOCK-INTRANSIT-CLOSE`, слито в `main` до сборки.
  - [сейчас] `reference/parity_intransit_close_2026-08-05.md` плюс
    `reference/_scratch_PARITY-STOCK-INTRANSIT-CLOSE_2026-08-05/` — артефакт адъюдикации и провенанс
    трёх офлайн-проверок; внесены сессией в ветке.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_ARCHIVE.md` — `Q-81` `PARTIAL` →
    `CLOSED`, строка уезжает в архив дословно. Строки в таблице `§Мандат Claude Code: класс задач` у
    `Q-81` нет и не было (факт-строка, не задача-строка), парность `ADR-090 §1` убирать ничего не
    требует, и это не расхождение.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` — строка `PARITY-STOCK-INTRANSIT-CLOSE`
    остаётся `PARTIAL`; её текст и гейт сужаются до вердикта по строке 21 после
    `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 (упоминание строки 22 переводится в закрытое).
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
    задач` — заводится строка `INTRANSIT-SURFACE-LAG` (`OPEN`, класс A, параллель да, мандат
    постоянный на диагностику и подготовку; правка расписания живого `transferConfig` — класс B,
    мандат НЕ выдан; Пишет: `reference/intransit_surface_lag_<date>.md`, `11_INFRA_FACTS.md` §SQ),
    парность `ADR-090 §1` соблюдена.
  - [сейчас] `07_STATE §Стенд-ап` — заменяется целиком (`ADR-084 §1`) текстом из `STATE_PATCH`, если
    настоящий блок последний по порядку `_ASSEMBLER.md §5`; иначе применяется стенд-ап последнего
    блока.
  - [сейчас] `06_INDEX.md` — строка этого ADR.
  - [задачей `PARITY-STOCK-SNAPSHOT-SYNC`] допустимое измерение строки 21 в окне `21:00Z` и природа
    семи сирот; вердикт по нему — остатком `PARITY-STOCK-INTRANSIT-CLOSE`.
  - [задачей `INTRANSIT-SURFACE-LAG`] замер отставания витрины от ядра и величины эффекта на
    клиентской странице; вопрос об `in_transit_sum_kgs` у прибывших заказов; побочно — заполнение
    отсутствующей строки `sq_marts_in_transit` в `11_INFRA_FACTS §SQ` (в таблице шесть конфигураций
    из тринадцати живых).
  - [без правок] `06_DECISIONS_LOG.md` тексты `ADR-067`, `ADR-068`, `ADR-079`, `ADR-084`, `ADR-119`,
    `ADR-120`, `ADR-123` — не редактируются (append-only); настоящее решение их не отменяет и
    значений не двигает: §3 достраивает мост по уже действующему требованию `ADR-079 §2`, §4
    переносит форму `ADR-119 §2`/`ADR-123 §4` на третью пару, §2 снимает оговорку, которую
    `ADR-120 §2` поставил сам себе и сам назвал условием снятия.
  - [без правок] `reference/parity_registry.md` строки 17-21 и 23 — не трогались.
  - [без правок] `reference/parity_intransit_recheck_2026-08-05.md` и
    `reference/parity_intransit_close_2026-08-04.md` — датированы, не переписываются (`ADR-043`);
    поправка о полукопейке и поправка к обоснованию `[без правок]` живут в артефакте этой сессии.
    Сплошной поиск утверждения о совпадении витрины с ядром по репо (`ADR-083 §2`, форма `ADR-044`)
    дал ровно два совпадения с номерами строк — `06_DECISIONS_LOG.md:3628` и
    `reference/parity_intransit_close_2026-08-04.md:60`, оба в неизменяемых текстах; правится ни
    одно, назван каждый.
  - [без правок] `reference/sql/sq_marts_in_transit.sql`, `reference/code/cf-facts/**`,
    `03_PIPELINE_SPEC.md`, `02_ERP_CONTRACTS.md` — решение ничего не деплоит, патча не готовит и
    фактов о схемах не двигает.
  - [без правок] `00_CHARTER §критерии успеха` — формулировка финишной черты не меняется: закрытие
    пары нулём ею предусмотрено дословно.
  - [без правок] `07_GAPS.md` строки `Q-80` и `PARITY-STOCK-SNAPSHOT-SYNC` — вне scope сессии,
    статусы не меняются.

  Введено сессией `PARITY-STOCK-INTRANSIT-CLOSE` (первый ход, 2026-08-05, класс A, архитектор, без
  брифа, `ADR-086 §1`).

- ADR-125: Счётчик реестра измеряет «объяснено», а не «совпадает» — два разных расстояния до финиша;
  наводки клиента по `sales_overview` за июль ложатся на уже установленный механизм периметра и на
  неадъюдицированную половину `Q-78`; ревизия актуальности реестра

  **Статус: accepted** (архитектор — §1-§5; §6 фактура архитектор, решение по деплою и по
  `04_ROADMAP` — владелец).

  **Контекст.** Владелец задал три вопроса (что осталось до закрытия, что потеряло актуальность, что
  брать следующим) и передал числа со звонка с клиентом: единственная оставшаяся претензия — витрина
  продаж, расхождение по июлю-2026 на Executive summary и в разрезе по сотрудникам. Полный разбор —
  `reference/finish_calibration_2026-08-05.md`.

  **Решение:**

  §1 **Числа владельца фиксируются как датированный снимок эталона уровня (iii)** (`ADR-067 §2`):
  период `2026-07-01…2026-07-31`, снят 2026-08-05. Executive summary «Выручка KGS» (`revenue_kgs`):
  дашборд `109,2` млн, МойСклад без фильтра каналов `112,35` млн, МойСклад с каналами «опт»+«офис»
  `109,16111` млн. Операционка, разрез по сотрудникам, та же витрина: дашборд `73,6` млн против
  МойСклада `74,745` млн по одному сотруднику, условие по каналам на стороне МойСклада не названо.
  Основание фиксации: без датированной записи эталон живёт только в чате, и последующая сверка
  сравнивала бы движущуюся сторону с движущейся (`ADR-098 §7`). **accepted** (архитектор).

  §2 **Счётчик реестра и приёмка клиентом суть разные расстояния, и их несовпадение есть следствие
  принятого решения, а не дефект.** `ADR-119 §2` сознательно отверг порог на разность в пользу
  инварианта разложения, потому что обе назначенные правки двигают `Δ` — сначала вверх, потом вниз, —
  и всякий порог объявил бы верную правку регрессом. Отсюда прямо следует: **счётчик дойдёт до `7/7`,
  а клиент по-прежнему будет видеть разрыв на странице продаж, пока патч периметра не задеплоен и
  история не пересобрана.** Формальный остаток финиша — одна строка 21 (замер плюс вердикт);
  клиентский остаток — деплой `SALES-INGEST-PATCH`, пересборка истории и неадъюдицированная
  атрибуция сотрудника. Настоящий параграф критерия финиша НЕ меняет и `ADR-119 §2` не
  пересматривает — он называет следствие, которое иначе всплывает при сдаче. **accepted**
  (архитектор).

  §3 **Июльский разрыв ложится на уже установленный механизм, но переносить майское разложение на
  июль без замера запрещено.** Ведущая гипотеза — периметр типов документов (`ADR-103`, `ADR-104`):
  ингест читает только `entity/demand`, розница и продажи комиссионера не грузятся вовсе. Совпадение
  дашборда с источником, суженным до «опт»+«офис», с точностью `0,036 %` есть сильнейшее
  свидетельство в её пользу, какое можно получить без запроса. Но соотношение механизмов между
  месяцами не постоянно: в мае чистый эффект периметра `1 341 450,08` KGS на `93` млн при нашей
  стороне ВЫШЕ эталона, в июле разрыв `3,19` млн на `112` млн при нашей стороне НИЖЕ. **Июль не
  измерялся ни разу ни одной сессией**, поэтому разложение июля есть работа, а не вывод. Заводится
  `PARITY-SALES-JULY-DIFF`: шаги 1-4 — четыре read-only запроса к нашим таблицам (класс A, мандат
  постоянный, ни секретов, ни нового решения), шаг 5 — живой `GET` за июль (класс B, мандат НЕ
  выдан) и только если после шагов 1-4 остаётся неотнесённая величина. Порядок обязателен: замер
  ПОСЛЕ деплоя невоспроизводим, потому что деплой двигает нашу сторону. **accepted** (архитектор).

  §4 **Разрыв по сотруднику имеет вторую, независимую гипотезу, и она структурная.** Атрибуция
  сотрудника в витрине идёт через `dim_counterparties.owner_employee_id` — менеджера КОНТРАГЕНТА
  (`reference/sql/sq_marts_sales_overview.sql:53`, `:117-118`), а не через сотрудника документа;
  отчёт МойСклада «по сотрудникам» почти наверняка режет по документу, и это разные популяции
  всякий раз, когда документ заведён не менеджером контрагента. Дефектом это НЕ объявляется —
  гипотеза выведена из текста витрины, не измерена. Различитель стоит один запрос и назван в
  `§4` артефакта: переатрибуция сохраняет общий итог, периметр его теряет. Предмет уже назван
  строкой `Q-78` дословно («группировка по менеджеру/стране не сопоставлена с полями отчёта»),
  поэтому отдельной строки не заводится (`ADR-051 §2`) — наводка клиента ставит цену на давно
  открытой строке, а не открывает новую тему. **accepted** (архитектор).

  §5 **Ревизия актуальности: шесть строк с опровергнутыми посылками, три полуустаревшие, один
  устаревший документ.** Проверено чтением полного текста каждой строки, не по памяти; основания —
  `§6` артефакта. Закрываются: `PARITY-CHECK-SALES-RETURNS` (мандат выдан `ADR-092`, задача
  исполнена, обе пары закрыты `ADR-114`/`ADR-119`), `Q-93` (различитель снят пятью контрольными
  окнами), `Q-82` (мост выведен `ADR-118`, загрузчик построен и задеплоен),
  `INVOICES-PERIMETER-DIFF` (недостачи `468` документов больше нет — `4526` против `4527`,
  `ADR-118`), `Q-79` (снапшот SQL есть, оракул уровня (i) найден `ADR-098`, пара закрыта `ADR-114`),
  блокер `B-7` (держит `E1-T3-MECH-FX`, закрытую `DONE` решением `ADR-095`). Сужаются, но не
  закрываются: `FACTS-BACKFILL` (продажная половина закрыта фактом, закупки и возвраты не
  проверялись), `Q-3` (посылка изменилась качественно с появлением код-репо), `RQ-3` (требует одной
  сверки с `MANIFEST.md` после деплоя 2026-08-03, в любую сторону не двигается без неё). Не
  устарели вопреки виду: четырнадцать строк под заморозкой `ADR-107 §3` (снимутся автоматически на
  `7/7`), `Q-11`…`Q-14` (факты добыты, осталась перепись), `MARTS-BUILD-STAMP` (наоборот подорожал —
  находка о суточной каденции витрин есть ровно его класс). **accepted** (архитектор).

  §6 **`04_ROADMAP` перестал описывать очередь и правится отдельным решением, не этим.**
  Декомпозиция `M-P4` целиком исторична, раздел Epic-1 кончается фразой «Первая задача: E1-T1 (Трек
  B). Остальные — после», тогда как Epic-1 прошёл шесть пар из семи; фактическая очередь живёт в
  `07_STATE` + `07_GAPS`. Практическое следствие: документ, названный планом работ, при чтении даёт
  неверную картину стадии проекта. Правка требует ADR (раздел несёт финишную формулировку
  `ADR-067`) и выносится владельцу как развилка, а не делается попутно. **accepted** (архитектор —
  констатация; решение о правке — владелец).

  **Последствия:**
  - [сейчас] `reference/finish_calibration_2026-08-05.md` — артефакт калибровки, ревизии и плана
    расследования; внесён сессией в ветке.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_ARCHIVE.md` — `FINISH-CALIBRATION`
    заводится и архивируется тем же проходом; строка мандата не заводится (`ADR-090 §2` по смыслу).
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат Claude Code: класс
    задач` — заводится `PARITY-SALES-JULY-DIFF` (`READY`, класс A, параллель да, мандат постоянный
    на шаги 1-4; шаг 5 — класс B, мандат НЕ выдан; Пишет:
    `reference/parity_sales_july_diff_<date>.md`, `reference/parity_registry.md` только строки
    18/19), парность `ADR-090 §1` соблюдена.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_ARCHIVE.md` + `07_STATE §Мандат
    Claude Code: класс задач` — закрытие и архивация шести строк `§5`; парные строки мандата
    убираются у `PARITY-CHECK-SALES-RETURNS` и `INVOICES-PERIMETER-DIFF`, у `Q-79`/`Q-82`/`Q-93` и
    `B-7` их нет и не было (факт-строки и блокер), и это не расхождение.
  - [сейчас] `07_STATE §Блокеры` — `B-7` снимается, раздел остаётся пустым; пустой раздел есть
    верное состояние.
  - [сейчас] `07_GAPS.md` строки `FACTS-BACKFILL`, `Q-3`, `RQ-3` — дописки о сужении по `§5`;
    статусы не меняются.
  - [сейчас] `06_INDEX.md` — строка этого ADR.
  - [задачей `PARITY-SALES-JULY-DIFF`] разложение июльского разрыва, различение четырёх гипотез,
    ответ по атрибуции сотрудника и закрытие остатка `Q-78`.
  - [задачей `SALES-INGEST-PATCH` (деплой)] фактическое устранение периметра; мандат класса B не
    выдан, окно пересборки июля истекает `2026-09-29`.
  - [без правок] `reference/parity_registry.md` строки 18/19 — пара «Продажи» остаётся закрытой
    правилом (`ADR-119 §2`); июльский разрыв правилом предусмотрен и свободным остатком не является,
    пока разложение июля не измерено и не показало обратного. Правка строк — предмет
    `PARITY-SALES-JULY-DIFF`, не настоящего решения.
  - [без правок] `04_ROADMAP.md` — устарел, правка выносится владельцу отдельно (`§6`).
  - [без правок] `06_DECISIONS_LOG.md` тексты `ADR-092`, `ADR-095`, `ADR-098`, `ADR-103`, `ADR-104`,
    `ADR-114`, `ADR-118`, `ADR-119` — не редактируются (append-only); настоящее решение их не
    отменяет, а констатирует, что описанные ими работы исполнены и строки реестра под них устарели.
  - [без правок] `reference/sql/sq_marts_sales_overview.sql`, `reference/code/**` — решение ничего не
    правит и не деплоит; `§4` есть чтение текста, не патч.

  Введено сессией `PARITY-STOCK-INTRANSIT-CLOSE` (второй ход `FINISH-CALIBRATION`, 2026-08-05, класс
  A, архитектор, без брифа, `ADR-086 §1`; строка заведена этим же блоком, `ADR-086 §3`).

- ADR-126: Июльское расхождение витрины продаж разложено — причин ДВЕ, и вторая (переатрибуция по
  сотруднику) патчем периметра не чинится; деплойная часть готового патча получает собственную
  строку, которой у неё не было

  **Статус: accepted** (архитектор — §1-§5; §6 постановка и форма архитектор, мандаты класса B — владелец).

  **Контекст.** Владелец передал числа со звонка с клиентом и выдал мандат на разбор (чат 2026-08-05,
  «Сможешь сделать? Даю мандат»), дополнив снимок эталона отфильтрованной величиной по сотруднику:
  Турдалиева, каналы «Оптовая торговля + ОФИС», `01.07.2026 00:00 … 31.07.2026 23:59` —
  `74 745 820,29` KGS. Замер исполнен третьим ходом сессии, класс A, шесть read-only запросов; полный
  ход — `reference/parity_sales_july_diff_2026-08-05.md`.

  **Решение:**

  §1 **Дефекта переупаковки нет: ядро, витрина и страница совпадают.** `core.fact_sales_profit` за
  июль — `2 935` строк / `109 167 858,30` KGS; `marts.sales_overview` — те же `2 935` строк /
  `109 167 858,32`; страница — `109,2` млн со слов клиента. Целый класс объяснений («теряется при
  переупаковке», «режется фильтром страницы») закрыт измерением, а не рассуждением. **accepted**
  (архитектор).

  §2 **Первая наводка — периметр типов документов, подтверждён с НАШЕЙ стороны.** В ядре за июль
  присутствует ровно один канал продаж, «Оптовая торговля» (`2 895` строк / `108 346 335,30`), плюс
  `40` строк с незаполненным каналом (`821 523,00`); строк розничного, маркетплейсного или
  комиссионного канала нет ни одной. Это ровно предсказание `ADR-103`/`ADR-104` и оно проверено без
  единого обращения к МойСкладу. Разность эталона «все каналы» и «опт+офис» — `3 188 890` KGS
  (`2,84 %`) — и есть непогруженные типы документов. Неотнесённый остаток по итогу — `6 748,30` KGS
  (`0,006 %`), он воспроизводится двумя независимыми разложениями и природе не приписывается:
  установить её можно только эталоном уровня (i) за июль, это класс B. Совпадение знака с механизмом
  строк-сирот отмечено и измерением НЕ является. **accepted** (архитектор).

  §3 **Вторая наводка — НЕ периметр, а переатрибуция, и это отдельный дефект.** Внутри тех же каналов
  «опт+офис» наш итог сходится с эталоном на `0,006 %`, а по одному сотруднику расходится на
  `−1 177 927,90` KGS (`−1,58 %`). Разрез по менеджерам не теряет ни сома: сумма семи корзин в
  точности равна итогу ядра, число строк после джойна равно числу до него (размножения нет), строк
  без контрагента нет, контрагентов с двумя текущими SCD2-строками нет. Механизм, теряющий документы,
  уронил бы и итог; наблюдаемое поведение даёт только перераспределение между корзинами. Ведущее
  объяснение: наша витрина режет по менеджеру КОНТРАГЕНТА
  (`reference/sql/sq_marts_sales_overview.sql:53`, `:117-118`), отчёт МойСклада — по документу.
  Дефектом конкретное правило НЕ объявляется: это гипотеза, различитель назван (разрез эталона по
  контрагентам внутри корзины сотрудника) и стоит один экран у клиента. Корзина «менеджер не
  назначен» (`1 964 248,90` на трёх контрагентах) разрыв не объясняет — ни одно её подмножество не
  даёт `1 177 927,90`, а атрибуция устроена «всё или ничего». **accepted** (архитектор).

  §4 **Структурное ограничение, определяющее цену второй правки.** `core.fact_sales_profit` не несёт
  сотрудника документа вообще (полная схема — `02_ERP_CONTRACTS.md §схемы core`; состав загружаемых
  полей — `reference/sales_ingest_patch_2026-08-03.md §1`). Следовательно, если МойСклад режет по
  документу, воспроизвести его разрез **нельзя ни при какой правке витрины** — требуется правка
  загрузчика с добавлением поля. Это меняет оценку работы с «поправить запрос марта» на «править
  ингест», то есть ставит её в один ряд с патчем периметра, а не в разряд косметики. Заводится
  `SALES-EMPLOYEE-ATTRIBUTION` (класс A на диагностику и проект; деплой — класс B, мандат НЕ выдан).
  **accepted** (архитектор).

  §5 **Сутки `2026-07-06` отсутствуют в ядре законно, задачи под себя не получают.** Вывод сделан
  арифметикой итога, а не предположением: пропуск суток с реальными продажами уронил бы наш итог на
  `3–5 %`, фактически итог ВЫШЕ эталона «опт+офис» на `0,006 %`. То же поведение уже наблюдалось
  (`ADR-121 §4`: `8,9 %` суток этого бизнеса не несут ни одного документа продажи). Названо, чтобы
  следующая сессия не завела работу на пустом месте. Побочно: `40` строк приходят с пустым
  `sales_channel_name` и на витрине становятся «Не указан», то есть фильтр по каналу «ОФИС» их не
  найдёт; пусто ли поле в источнике или теряется загрузчиком — с нашей стороны неразличимо, вывод не
  выносится. **accepted** (архитектор).

  §6 **Деплойная часть готового патча периметра получает собственную строку `SALES-INGEST-PATCH-DEPLOY`.**
  Причина, по которой патч не задеплоен, установлена и она процессная, а не техническая: гейт
  `ADR-065` снят для всех трёх функций (`ADR-115 §1`), форма деплой-процедуры выбрана (`ADR-115 §4`,
  вариант Б), патч написан 2026-08-03 — но **строки под деплой не существует ни в реестре, ни в
  таблице мандата**, тогда как у пяти других готовящихся правок такие строки есть. Работа жила
  только фразой внутри плана очереди (`ADR-116 §5`), где была намеренно поставлена ПОСЛЕ финишного
  счётчика, потому что в тот момент конвейер стоял (блок DQ Gate) и деплой в него не принимался;
  конвейер восстановился `2026-08-04T18:00:02Z`, и это основание истекло. Инстанс `ADR-084 §5`
  («работа без идентификатора есть незакрытая постановка»). Строка `SALES-INGEST-PATCH` закрывается
  `CLOSED` (подготовка исполнена), деплойная часть выделяется — прецедент разреза
  `DQ-GATE-SCOPE-SPLIT` → `DQ-GATE-SCOPE-SPLIT-DEPLOY` (`ADR-122 §3`); дополнительная причина
  разреза в том, что статус `READY` подготовительной строки читался как «готово к деплою», против
  чего прямо предостерегал `ADR-115 §2`. Мандат класса B настоящим решением НЕ выдаётся — выдаётся
  поимённо владельцем после объявления действия (`ADR-077 §6`). **Срочность названа числом:** окно
  `WEEKLY_WINDOW_DAYS = 90` выпускает июль-2026 к `2026-09-29`, после чего пересобрать июль штатным
  путём будет нечем. **accepted** (архитектор — постановка и форма; мандат — владелец).

  §7 **Пара «Продажи» ни сошедшейся, ни переоткрытой этим решением не объявляется.** Оба измеренных
  механизма правилу `ADR-119 §2` не противоречат: периметр есть механизм `M2`, названный самим
  правилом, а переатрибуция итога не двигает вовсе и потому в разложение `Δ` не входит по построению.
  Правка строк 18/19 реестра — предмет деплоя и последующей пересверки, не настоящего решения.
  **accepted** (архитектор).

  **Последствия:**
  - [сейчас] `reference/parity_sales_july_diff_2026-08-05.md` плюс
    `reference/_scratch_PARITY-SALES-JULY-DIFF_2026-08-05/` — артефакт и провенанс, внесены сессией
    в ветке.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_ARCHIVE.md` + `07_STATE §Мандат` —
    `PARITY-SALES-JULY-DIFF` заводится и закрывается `DONE` одним проходом; `SALES-INGEST-PATCH`
    `READY` → `CLOSED`, архив, строка мандата убирается (`ADR-090 §1`).
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` + `07_STATE §Мандат` — заводятся
    `SALES-INGEST-PATCH-DEPLOY` (`READY`, класс B, параллель нет, мандат НЕ выдан; Пишет:
    `reference/code/cf-facts/MANIFEST.md`, `reference/sales_ingest_patch_deploy_<date>.md`,
    `reference/_scratch_SALES-INGEST-PATCH-DEPLOY_<date>/`) и `SALES-EMPLOYEE-ATTRIBUTION` (`OPEN`,
    класс A, параллель да, мандат постоянный на диагностику и проект; Пишет:
    `reference/sales_employee_attribution_<date>.md`, `reference/code/cf-facts/`), парность
    `ADR-090 §1` соблюдена.
  - [сейчас] `07_GAPS.md` строки `SALES-CONSIGNMENT-REVENUE` и `SALES-PERIMETER-EXTEND` — гейт
    переводится на `SALES-INGEST-PATCH-DEPLOY`; статусы `READY` не меняются.
  - [сейчас] `07_STATE §Открытые вопросы` + `07_GAPS.md` — `Q-78` `OPEN` → `PARTIAL`, остаток —
    правило соответствия разреза по сотруднику, гейт `SALES-EMPLOYEE-ATTRIBUTION`.
  - [задачей `SALES-INGEST-PATCH-DEPLOY`] устранение периметра и пересверка итога июля; перед
    деплоем — одна проверка класса B, названная самим патчем (живой `GET` одной позиции
    `entity/retaildemand` для подтверждения состава полей).
  - [задачей `SALES-EMPLOYEE-ATTRIBUTION`] правило разреза по сотруднику и закрытие остатка `Q-78`.
  - [без правок] `reference/parity_registry.md` строки 18/19 — правка после деплоя и пересверки
    (`§7`), не этим решением.
  - [без правок] `reference/sql/sq_marts_sales_overview.sql`, `reference/code/**` — читались, не
    правились; форма правильной атрибуции не назначается (`§3`).
  - [без правок] `06_DECISIONS_LOG.md` тексты `ADR-103`, `ADR-104`, `ADR-115`, `ADR-116`, `ADR-119` —
    не редактируются (append-only); настоящее решение их подтверждает измерением и не двигает.
  - [отдельным коммитом] `tools/inbox_manifest.sh` — починка границы региона «Последствия»
    (терминатор на начале следующего решения) плюс
    `reference/_scratch_INBOX-MANIFEST-BOUNDARY_2026-08-05/`. Коммит сессии, вне буфера
    (`ADR-081 §8`); сборщик его в репо НЕ ищет и на отсутствии НЕ останавливается — перечисляет
    названной задолженностью в теле своего коммита. Основание выхода из-под заморозки
    `ADR-107`: это механическая проверка, а не решение о методе, и без неё встаёт сборка, которая
    блокирует старт `PARITY-STOCK-SNAPSHOT-SYNC` — единственной работы, оставшейся до `7/7`
    (`ADR-107 §2` требует назвать задачу паритета поимённо, она названа).

  Введено сессией `PARITY-STOCK-INTRANSIT-CLOSE` (третий ход `PARITY-SALES-JULY-DIFF`, 2026-08-05,
  класс A, архитектор, мандат владельца в чате).

- ADR-127: Разрез продаж по сотруднику — гипотеза `ADR-126 §3` подтверждена частично данными
  клиента; простая перестановка контрагентов не закрывает разрыв в ноль; необходим разрез на
  уровне документа

  **Статус: accepted** (архитектор — §1-§4 фактура и проект; §5 развилка не решена, остаётся
  открытой).

  **Контекст.** `ADR-126 §3/§4` оставил версию «источник режет по документу, мы — по контрагенту»
  гипотезой с названным различителем (сверка отчёта Турдалиевой по контрагентам). Владелец
  предоставил этот отчёт 2026-08-06/07.

  **Решение:** (1) множество контрагентов у источника и у нас совпадает на 23 из 30 точно, семь
  расходятся по закреплению менеджера — гипотеза подтверждена НАПРАВЛЕНИЕМ; (2) перестановка
  семи расхождений по деньгам даёт перелёт на `1 194 360,00` KGS против недостачи
  `1 177 927,90` KGS — правило источника режет тоньше уровня контрагента, воспроизвести его без
  данных документа нельзя; (3) состав правки загрузчика (поле `owner.meta.href` документа
  `entity/demand`, паттерн идентичен уже читаемому `agent_id`) спроектирован, не применён;
  (4) офлайн-проверка на мае частично исполнена, поле `owner` подтверждено населённым, но
  сравнение по сумме упёрлось в новый неразрешённый gap (заведён отдельной строкой); (5) выбор
  правила разреза (правка загрузчика / легитимное отступление моста / гибрид) — развилка
  владельца, этим решением НЕ закрывается.

  **Последствия:**
  - [сейчас] `reference/oracle_sales_july_2026-08-05.md`, `reference/sales_employee_attribution_2026-08-07.md`,
    `reference/_scratch_SALES-EMPLOYEE-ATTRIBUTION_2026-08-07/` — внесены сессией в ветке.
  - [сейчас] `07_GAPS.md` строка `SALES-EMPLOYEE-ATTRIBUTION` — дописка о частичном подтверждении
    гипотезы и ссылка на артефакт (снята следующим решением `ADR-128` тем же коммитом сборки).
  - [сейчас] `07_GAPS.md` строка `Q-78` — дописка: различитель `ADR-126 §3` исполнен, направление
    гипотезы подтверждено, точного схождения в ноль на уровне контрагента не существует по
    построению; статус остаётся `PARTIAL` до решения развилки.
  - [сейчас] `07_STATE.md §Открытые вопросы` — новая строка `SALES-EMPLOYEE-ATTRIBUTION-MAY-SUM-GAP`
    (факт, не решение; закрывается независимым замером `expand=positions` либо подтверждением
    сравнимости `sum`/`Σ revenue_kgs`).
  - [задачей владельца] Выбор варианта развилки §5 артефакта — не заводится отдельной задачей
    этим решением; следующая сессия, которой владелец сообщит выбор, заводит либо правку
    загрузчика, либо правку реестра паритета (легитимное отступление), по факту выбора.

  Введено сессией `SALES-EMPLOYEE-ATTRIBUTION` (2026-08-07, архитектор, без брифа, `ADR-086 §1`,
  определение процитировано дословно из `07_GAPS.md` в тексте запуска).

- ADR-128: Развилка разреза по сотруднику закрыта владельцем — вариант 1 (правка загрузчика);
  задача `SALES-DOCUMENT-OWNER-INGEST` заведена с полной фактурой для генерации брифа

  **Статус: accepted** (архитектор — постановка и форма задачи; выбор варианта — владелец, чат
  2026-08-07).

  **Контекст.** `reference/sales_employee_attribution_2026-08-07.md §5` предъявил владельцу три
  варианта (правка загрузчика / документирование как легитимного отступления / гибрид) с
  рисками каждого и рекомендацией архитектора (вариант 1, не срочно). Владелец выбрал вариант 1
  в чате 2026-08-07.

  **Решение:** (1) вариант 1 принят: правка загрузчика `cf-facts`, добавляющая
  `document_owner_employee_id` в `core.fact_sales_profit` по полю `owner` документа
  `entity/demand`; (2) следующая задача называется `SALES-DOCUMENT-OWNER-INGEST` (класс A — текст
  патча, не деплой; деплой получает отдельную строку по факту готовности патча, урок `ADR-126 §6`
  применяется превентивно, не постфактум); (3) `SALES-EMPLOYEE-ATTRIBUTION` закрывается `DONE` —
  её мандат (диагностика + проект) исполнен полностью первым ходом этой же сессии; (4) вся
  фактура для сессии-генератора брифа уже лежит в
  `reference/sales_employee_attribution_2026-08-07.md §3`, повторный discovery не требуется,
  `SALES-DOCUMENT-OWNER-INGEST-GEN` при старте читает эту секцию и строку `07_GAPS.md`;
  (5) правка `sq_marts_sales_overview.sql` явно исключена из scope новой задачи — форма разреза
  на марте есть решение ПОСЛЕ появления поля в `core`, не часть этой правки.

  **Последствия:**
  - [сейчас] `07_GAPS.md` — строка `SALES-EMPLOYEE-ATTRIBUTION` переносится в `07_ARCHIVE.md`
    дословно с пометкой «закрыта решением владельца по развилке §5, продолжение —
    `SALES-DOCUMENT-OWNER-INGEST`»; новая строка `SALES-DOCUMENT-OWNER-INGEST` заводится текстом
    из `STATE_PATCH §Подробности для модели` этого блока.
  - [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — строка `SALES-EMPLOYEE-ATTRIBUTION`
    убирается (`ADR-090 §1`); новая строка `SALES-DOCUMENT-OWNER-INGEST` (класс A, да, постоянный,
    описание — как в `STATE_PATCH`) добавляется тем же коммитом (парность `ADR-090 §1` соблюдена).
  - [сейчас] `07_STATE.md §Открытые вопросы` — строка `SALES-EMPLOYEE-ATTRIBUTION` (была `OPEN`)
    заменяется строкой `SALES-DOCUMENT-OWNER-INGEST` (`OPEN`), текст — из `STATE_PATCH`.
  - [задачей SALES-DOCUMENT-OWNER-INGEST-GEN] Сборка брифа задачи `SALES-DOCUMENT-OWNER-INGEST` из
    фактуры, названной выше — следующий шаг метода (`_METHOD.md §9`, механический бриф либо бриф с
    суждением по усмотрению генератора), не заводится этим ADR как отдельная задача реестра.

  Введено сессией `SALES-EMPLOYEE-ATTRIBUTION` (второй ход, 2026-08-07, архитектор, база —
  `2fa9c8e` первого хода этой же сессии, многоходовая сессия `ADR-021 §1`).

- ADR-129: Пара строки 21 реестра паритета объявлена сошедшейся: разложение `ADR-123 §4` замкнуто точным нулём; оговорка о нерасследованном дрейфе 27 строк не создаёт свободного остатка

  **Статус: accepted**

  **Контекст.** Строка 21 реестра паритета («Остатки товаров», `Q-80`) висела как
  `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2 после трёх предыдущих ходов («не сошлась» → отрицательная
  проба момента → принудительный прогон). Замер `2026-08-06`
  (`reference/parity_stock_snapshot_sync_2026-08-06.md`) дал построчный полный джойн `299`/`299`
  без единого расхождения на совпавшем ключе и без единой сироты с любой стороны; вердикт о
  сходимости пары этот замер явно оставил за архитектором.

  **Решение.** Архитектор (`ADR-076 §4`, `ADR-091 §8`) адъюдицировал приёмку `ADR-123 §8` как
  исполненную дословно: `Δ = Σ(движение) + Σ(сироты) = 0,00 + 0,00 = 0,00`, свободного остатка
  нет. Владелец апрувнул ВАРИАНТ 2 (чат, 2026-08-07): пара объявляется СОШЕДШЕЙСЯ с явной
  оговоркой в тексте строки 21 о нерасследованном дрейфе 27 строк источника за трое суток
  (`326`→`299`) — дрейф на два порядка крупнее семи исходных сирот, причина не установлена, но
  обе стороны согласны, что он не создаёт свободного остатка на данном `Δ`. Отдельно
  зафиксировано: отсутствие семи прежних сирот на обеих сторонах снимает вопрос присутствия, но
  не является установлением причины их исчезновения в широком смысле (прямой
  `GET entity/product/<uuid>`, как планировал `ADR-123 §8` шаг 3 буквально, не выполнялся).
  Переоткрытие строки 21 возможно только при появлении свободного остатка (критерий `ADR-123 §4`),
  не при появлении новых наблюдений о дрейфе.

  **Последствия:**
  - [сейчас] `reference/parity_registry.md`, строка 21: текст вердикта внесён этой сессией, ADR
    фиксирует его как принятое решение задним числом (адъюдикация была раньше правки).
  - [сейчас] `07_STATE.md`: строки `Q-80` и `PARITY-STOCK-SNAPSHOT-SYNC` переводятся в `DONE` и
    переносятся в `07_ARCHIVE.md` дословно тем же коммитом сборки (`ADR-064`); остаток строки
    `PARITY-STOCK-INTRANSIT-CLOSE` закрывается.
  - [сейчас] `06_INDEX.md`: строка на этот ADR добавляется сборкой тем же коммитом.
  - [задачей <дрейф-задача, если владелец заведёт>] причина дрейфа 27 строк источника
    (`326`→`299` за трое суток) остаётся нерасследованной; отдельной задачи на момент этой сессии
    не заведено — если владелец решит расследовать, номер задачи присвоит он или сборка.
  - [без правок] `reference/parity_stock_snapshot_sync_2026-08-06.md`,
    `reference/parity_stock_snapshot_sync_2026-08-04.md` и прочие прежние артефакты серии
    `PARITY-STOCK-SNAPSHOT-SYNC` — не переписываются (`ADR-043`), остаются провенансом.

  Введено сессией `PARITY-STOCK-SNAPSHOT-SYNC-CLOSE` (2026-08-07, исполнитель, адъюдикация
  архитектора цитируется дословно); применено сборкой буфера 2026-08-07.

- ADR-130: Адъюдикация review-очереди `INTRANSIT-SURFACE-LAG` (2026-08-07): форма фикса отставания
  клиентской витрины «в пути» и находка по необнулению «Прибыл»
  Контекст: `ADR-124 §6` завёл задачу `INTRANSIT-SURFACE-LAG` с двумя вопросами и явно не назначил
  форму фикса отставания витрины (два кандидата «несопоставимы по цене без замера»). Сессия-исполнитель
  измерила эффект (`reference/intransit_surface_lag_2026-08-07.md`) и оставила review-очередь
  (`reference/architect_review_queue_2026-08-07-1.md`) с двумя пунктами архитектору — адъюдикация
  решений сессии-исполнителю не отдаётся (`ADR-076 §4`, `ADR-091 §8`).
  Решение:
  §1 **Форма фикса — варианты (A)/(B) НЕ взаимоисключающие; принимается (B) немедленно, (A)
  гейтится замером цены.** Вариант (B) (подпись момента пересборки на странице Looker Studio) снимает
  вводящую в заблуждение семантику «на сейчас» немедленно, дёшево и без класса B — это интерфейсное
  действие владельца, комбинация с (A) архитектурно не конфликтует: (B) раскрывает staleness клиенту,
  (A) её устраняет в источнике, слои независимы. Рекомендация исполнителя «(B) первым шагом, не
  исключая (A)» принимается буквально — они не альтернатива, а последовательность. Класс B мандат на
  вариант (A) (правка `transferConfig` `sq_marts_in_transit` на часовую каденцию) этой сессией НЕ
  открывается: цена в BigQuery slot-часах не измерена ни разу (замер `reference/intransit_surface_lag_2026-08-07.md`
  прямо называет это вне scope сессии-исполнителя), а выдача поимённого мандата на правку живой
  конфигурации без цены есть решение вслепую — то самое, что `05_CONVENTIONS` Часть I ★ «Критерий
  приёмки выводится из формулы» и общий закон anti-improvisation запрещают по духу (цена — вход
  решения, не следствие). Заводится `INTRANSIT-CADENCE-COST-PROBE` (класс A, read-only) как
  предусловие: замеряет BigQuery slot-время/стоимость часовой пересборки `sq_marts_in_transit`
  (`INFORMATION_SCHEMA.JOBS_BY_PROJECT` за представительное окно суточных прогонов текущей
  конфигурации, экстраполяция на часовую каденцию, либо `--dry_run` пробный расчёт). Задача класса A,
  параллельна, НЕ блокирует финиш Epic-1 (реестр паритета уже `7/7`, это работа по качеству витрины
  вне периметра сверки). **accepted** (архитектор).
  §2 **Находка по необнулению `in_transit_sum_kgs`/`quantity_in_transit` у статуса «Прибыл» —
  DEFER, заводится `Q-104`.** Рекомендация исполнителя принимается: находка (`94,5%` строк,
  `957 627 883,14` KGS, `reference/intransit_surface_lag_2026-08-07.md §4`) не меняет ни один вердикт
  паритета (периметр `ADR-124 §3` статус «Прибыл» исключает по построению) и не имеет известного
  потребителя вне scorecard «Сумма KGS в пути» (которая сама фильтрует по статусу). Различитель
  «источник vs загрузчик» требует живого `GET entity/purchaseorder/<id>/positions` (класс B, секрет
  `msklad-token`) — цена мандата не оправдана без конкретного потребителя данных без фильтра по
  статусу. `Q-104` заводится задача-строкой GAP-реестра (не факт-строкой, в отличие от `Q-81`,
  потому что здесь остаётся открытый различитель с явным триггером, а не закрытое наблюдение),
  статус DEFER, триггер — появление такого потребителя. Строка в таблице мандата не заводится: мандат
  никому не выдан и не запрашивается до триггера. **accepted** (архитектор).
  §3 **Задача `INTRANSIT-SURFACE-LAG` закрывается DONE.** Оба вопроса `ADR-124 §6` отвечены: (1)
  механизм и эффект отставания измерены (`~5,88` ч элапс, `~6,12` ч возраст снимка на момент замера,
  KGS-эффект `0,00` в этом окне, структурный риск до `~24` ч остаётся неисчерпан на худшей фазе цикла
  — это ограничение измерения, не открытый вопрос задачи); (2) вопрос об обнулении отвечен фактом о
  ядре и явным DEFER остатка (`§2`). Форма фикса решена (`§1`). Остаток («опубликовать подпись» —
  действие владельца; «замерить цену часовой каденции» — новая задача `INTRANSIT-CADENCE-COST-PROBE`)
  не является незакрытым хвостом ЭТОЙ задачи — это её потомки. **accepted** (архитектор).
  Последствия:
  - [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — `INTRANSIT-SURFACE-LAG` `OPEN` → `DONE`,
    строка уезжает в `07_ARCHIVE.md` дословно; заводятся `Q-104` (DEFER) и
    `INTRANSIT-CADENCE-COST-PROBE` (OPEN) — обе строки полным текстом, `Q-104` без парной строки в
    таблице мандата (обоснование — `§2`).
  - [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — строка `INTRANSIT-CADENCE-COST-PROBE`
    (класс A, параллель да, мандат постоянный, основание — read-only BigQuery-замер, `Пишет:
    reference/intransit_cadence_cost_probe_<date>.md`).
  - [сейчас] `07_STATE.md §Стенд-ап` — заменяется целиком (`ADR-084 §1`) текстом из `STATE_PATCH`,
    если этот блок последний по порядку сборки; иначе применяется стенд-ап последнего блока.
  - [сейчас] `06_INDEX.md` — строка этого ADR.
  - [без правок] `reference/parity_registry.md` — пункты вне реестра паритета (`ADR-124 §1` вердикт
    `Δ = 0` не двигается), строки не трогаются.
  - [без правок] `06_DECISIONS_LOG.md` текст `ADR-124` — не редактируется (append-only); настоящее
    решение достраивает `§6` фактом о форме фикса, не отменяет его.
  - [без правок] `11_INFRA_FACTS.md`, `02_ERP_CONTRACTS.md`, `03_PIPELINE_SPEC.md` — фактов не двигает.
  - [без правок] `04_ROADMAP.md` — задача вне декомпозиции M-P4/Epic-1, роудмап не адресует
    GAP-реестровые задачи поимённо (прецедент — прочие ADR закрытия GAP-строк).
  Статус: accepted

  Введено сессией `INTRANSIT-SURFACE-LAG-ADJ` (2026-08-07, архитектор, без брифа, `ADR-086 §1`);
  применено сборкой буфера 2026-08-07.

- ADR-131: Очередь после деплоя периметра продаж заведена четырьмя задачами; счётчик реестра —
  `7/7`; эскалация по расхождению на клиентской странице приземлена задачами, а не наблюдением

  **Статус: accepted** (владелец подтвердил заведение строк прямым решением в чате 2026-08-07).

  **Контекст.** Деплой `cf-facts-00008-zen` (2026-08-07) состоялся, периметр промоутнут разово
  (`+5 467` строк, `+12 702 994,13` KGS за 90 суток, `reference/code/cf-facts/MANIFEST.md` шаг 9б).
  Владелец сообщил, что картина расхождения на странице продаж не изменилась, и запросил разбор.
  Одновременно исполнитель остановился на задаче, определение которой существовало только в
  разговоре, — по `ADR-086 §3` это правильная остановка, а не сбой.

  Установлено чтением репо, без единого облачного вызова:

  (i) **Счётчик реестра паритета — `7/7`, а не `6/7`.** Строка 21 закрыта `ADR-129` тем же проходом
  сборки 2026-08-07; расхождение стенд-апа уже названо в `07_STATE.md:1041` как содержание чужого
  блока. Задач, гейтящих седьмую пару, не существует. Открыт не счётчик, а клиентская приёмка —
  разрез, который `ADR-125 §1` назвал заранее.

  (ii) **Режимы `perimeter` и `perimeter_promote` не вызываются ни одним расписанием.** Сплошной
  поиск подстроки `perimeter` даёт 0 совпадений в `reference/code/cf-facts/workflow_hourly.yaml`,
  `workflow_weekly.yaml` и `deploy_and_workflow.sh`; инвентарь Cloud Scheduler
  (`11_INFRA_FACTS.md:27`) называет пять джобов, ни один из них периметр не вызывает. Деплой
  добавил способность, но не поведение.

  (iii) **У строк периметра нет канала продаж** — `CAST(NULL AS STRING) AS sales_channel_name`
  (`bq_ops.py:483-486`), витрина превратит их в «Не указан» (`sq_marts_sales_overview.sql:58`) и
  смешает с 40 строками опта без канала (`ADR-126 §7`).

  (iv) **Разрез по сотруднику от этого деплоя не сдвигается по построению** — разрыв есть
  переатрибуция, а не потеря (`ADR-126 §3`, подтверждено данными клиента `ADR-127`); чинится только
  патчем загрузчика и его деплоем.

  (v) **Дефект парности `ADR-090 §1`:** бриф `briefs/SALES-PERIMETER-PARITY-RECHECK.md` закоммичен
  `82137c3`, строк задачи нет ни в `07_GAPS.md`, ни в таблице мандата; бриф сам предсказывал их
  заведение сборкой, заведения не произошло.

  **Решение.**

  §1 Заводятся четыре задачи — `SALES-PERIMETER-LANDING-CHECK`, `SALES-PERIMETER-CADENCE`,
  `SALES-PERIMETER-CHANNEL-DECIDE`, `SALES-DOCUMENT-OWNER-DEPLOY` — полным текстом определения и
  приёмки в `07_GAPS.md`, строкой индекса в `07_STATE`, строкой мандата в той же таблице, одним
  коммитом (`ADR-090 §1`). Восстанавливается парность пятой, `SALES-PERIMETER-PARITY-RECHECK`.

  §2 **Эскалация по расхождению на клиентской странице приземляется задачами, а не наблюдением.**
  Каждая гипотеза о причине получает задачу-различитель, и ни одна не остаётся в тексте ответа:
  «смотрели до пересборки витрины либо на кэш» и «периметр лёг не на июль» закрывает
  `SALES-PERIMETER-LANDING-CHECK` величинами (1)-(4) его приёмки; «новые строки не видны в разрезе
  по каналу» — величина (5) той же задачи плюс `SALES-PERIMETER-CHANNEL-DECIDE`; «эффект разовый и
  завтра разойдётся снова» — `SALES-PERIMETER-CADENCE`; «разрез по сотруднику не сдвинулся» — уже
  объяснён (iv) и закрывается парой `SALES-DOCUMENT-OWNER-INGEST` + `SALES-DOCUMENT-OWNER-DEPLOY`.
  Гипотеза без задачи-различителя в этом проекте есть та же догадка, только длиннее.

  §3 **Порядок.** `SALES-PERIMETER-LANDING-CHECK` и `SALES-DOCUMENT-OWNER-INGEST` идут первыми и
  параллельно (обе класс A; перед одновременным запуском обязателен
  `bash tools/parallel_check.sh SALES-PERIMETER-LANDING-CHECK SALES-DOCUMENT-OWNER-INGEST`,
  `ADR-082 §1`). Затем `SALES-PERIMETER-CADENCE` и `SALES-PERIMETER-CHANNEL-DECIDE`. Затем два
  класса B: `SALES-PERIMETER-PARITY-RECHECK` и `SALES-DOCUMENT-OWNER-DEPLOY`. Основание порядка:
  замер, показывающий, где обрывается периметр, меняет содержание всех последующих правок, а
  стоит один заход read-only.

  §4 **Учёт `ADR-119 §4` соблюдён:** ни одна из четырёх задач не объявляет пару реестра сошедшейся
  и счётчика не двигает — реестр уже `7/7`. Задача-замер и задача-вердикт остаются разными
  работами; вердикт по итогам `SALES-PERIMETER-PARITY-RECHECK` (если он понадобится) есть
  адъюдикация архитектора, сессии-исполнителю не отдаваемая (`ADR-076 §4`, `ADR-091 §8`).

  §5 **Заморозка `ADR-107` не нарушена:** все пять строк предметные, ни одна не вводит и не меняет
  правило метода. Стоп исполнителя на задаче из разговора — четвёртый инстанс уже
  зарегистрированного класса (`07_STATE.md:221`), строки реестра под себя не получает
  (`ADR-051 §2`, `ADR-107 §4`).

  **Последствия:**
  - [сейчас] `07_GAPS.md`: append пять строк (Патч 1 этого блока, дословно).
  - [сейчас] `07_STATE.md` §Открытые вопросы: append пять строк индекса (Патч 2, дословно).
  - [сейчас] `07_STATE.md` §Мандат Claude Code: append семь строк — пять задач, из которых две
    разрезаны по шагам (Патч 3, дословно); парность `ADR-090 §1` соблюдена.
  - [сейчас] `07_STATE.md` §Стенд-ап: заменяется целиком, счётчик `7/7`.
  - [сейчас] `07_STATE.md` §Подробности для модели: append абзац этого блока.
  - [сейчас] `06_INDEX.md`: три строки на три ADR этого блока.
  - [задачей SALES-PERIMETER-LANDING-CHECK] проверка, доехал ли периметр до июля, витрины и страницы.
  - [задачей SALES-PERIMETER-CADENCE] подключение периметра к недельному конвейеру.
  - [задачей SALES-PERIMETER-CHANNEL-DECIDE] метка канала для розницы и комиссии.
  - [задачей SALES-DOCUMENT-OWNER-DEPLOY] деплой поля сотрудника-владельца документа.
  - [без правок] `reference/code/cf-facts/*` — этой сессией не трогались; `reference/parity_registry.md`
    правит только `SALES-PERIMETER-PARITY-RECHECK`; `04_ROADMAP.md` — расхождение с фактической
    очередью уже названо (`ADR-125 §6`), настоящим решением не чинится.

  Введено сессией `SALES-PERIMETER-QUEUE-ADJ` (2026-08-07, архитектор).

- ADR-132: Мандат класса B на деплой Cloud Workflows в рамках `SALES-PERIMETER-CADENCE`

  **Статус: accepted** (владелец выдал мандат прямым решением в чате 2026-08-07).

  **Контекст.** Деплой периметра дал способность без поведения: режимы существуют, вызывать их
  некому (первый ADR блока, (ii)). Подключение — правка YAML недельного pipeline, то есть деплой
  Cloud Workflows, класс B. Форма «один поимённый ADR на один деплой» действует
  (`DEPLOY-ORDER-ADJ`, 2026-08-03 §iv), пакетный мандат отклонён и не переоткрывается.

  **Решение.** Мандат выдан поимённо на задачу `SALES-PERIMETER-CADENCE`, шаг 3.

  §1 **Разрешено ровно одно действие:** развёртывание нового текста `msklad-pipeline-weekly`
  (`gcloud workflows deploy`) с двумя добавленными шагами — `step_perimeter` между `step_facts` и
  `step_dq`, `step_perimeter_promote` после `step_promote`. Никаких иных объектов, никаких правок
  `msklad-pipeline-hourly`, никаких новых джобов Cloud Scheduler, никаких правок самой функции
  `cf-facts` этим мандатом не разрешено.

  §2 **Предусловие исполнения:** шаг 1 задачи (read-only съём живых объектов) исполнен и записан;
  при обнаружении уже подключённой каденции мандат не используется вовсе.

  §3 **Обязательный порядок и откат.** Процедура — `05_CONVENTIONS` Часть II вариант Б: снимок
  задеплоенного текста как seed, отдельная ветка, слияние после read-back. Read-back для Workflows
  делается через `--format=json` и программное извлечение `sourceContents` — форматтер `gcloud`
  добавляет свой перевод строки и даёт ложное расхождение в один байт
  (`DQ-GATE-SCOPE-SPLIT-DEPLOY`, 2026-08-05). Откат — повторный деплой снятого до правки текста;
  текст обязан быть сохранён в scratch до деплоя, а не восстанавливаться из памяти.

  §4 **Оговорка `ADR-077 §6` обязательна:** перед исполнением сессия отдельным сообщением в чат
  называет, что именно будет развёрнуто, на каком объекте и чем откатывается. Отсутствие такого
  сообщения делает подтверждение владельца неотличимым от рутинного запуска скрипта.

  §5 **Мандат не покрывает первый прогон нового шага вручную.** Каденция включается расписанием;
  принудительный внеплановый вызов `mode=perimeter_promote` есть запись в живую `core.fact_*` и
  требует отдельного решения — прецедент формы `PARITY-STOCK-SNAPSHOT-SYNC` шаг 2.

  **Последствия:**
  - [сейчас] `07_STATE.md` §Мандат Claude Code: строка `SALES-PERIMETER-CADENCE, шаг 3` несёт
    «выдан поимённо» со ссылкой на этот ADR.
  - [задачей SALES-PERIMETER-CADENCE] исполнение деплоя и запись провенанса в
    `reference/code/cf-facts/MANIFEST.md`.
  - [без правок] `reference/code/cf-facts/workflow_hourly.yaml` — часовой pipeline этим решением не
    затрагивается; `11_INFRA_FACTS.md §SQ` — пополняется задачей, не этим ADR.

  Введено сессией `SALES-PERIMETER-QUEUE-ADJ` (2026-08-07, архитектор).

- ADR-133: Мандат класса B на живые вызовы `report/profit/*` в рамках `SALES-PERIMETER-PARITY-RECHECK`

  **Статус: accepted** (владелец выдал мандат прямым решением в чате 2026-08-07).

  **Контекст.** Пара «Продажи» закрыта правилом (`ADR-119`), но после деплоя периметра ни одна
  сторона не пересчитывалась: предсказанный рост видимой разности до `≈1,75 %` остаётся ожиданием,
  не измерением (`07_STATE.md:1080-1083`). Бриф задачи готов, строк реестра не имел — заводятся
  первым ADR блока.

  **Решение.** Мандат выдан поимённо на задачу `SALES-PERIMETER-PARITY-RECHECK`.

  §1 **Разрешённый объём вызовов, шире которого сессия не идёт:** `GET` к
  `report/profit/byproduct` и `report/profit/bycounterparty` с границами `momentFrom` /
  `momentTo`, покрывающими зону паритета мая-2026, с пагинацией `limit ≤ 100`
  (`02_ERP_CONTRACTS §поведение API`). Ни один иной endpoint, ни один `POST`/`PUT`/`DELETE`, ни
  одна запись в любую таблицу проекта этим мандатом не разрешены.

  §2 **Эталон датируется** (`ADR-067 §3`): сырые тела ответов сохраняются в scratch задачи и
  коммитятся как провенанс; сравнение идёт со снятым снимком, а не с повторным запросом в момент
  вывода. Наша величина берётся из `core.fact_sales_profit` и НЕ перевычисляется из API в момент
  сравнения — иначе проверка вырождается в сравнение источника с самим собой (`ADR-098 §7`).

  §3 **Правило суток обеих сторон называется явно** (`ADR-088 §3`) вместе с окном UTC; окно
  запроса выводится из правила, а не назначается на глаз.

  §4 **Критерий вывода — инвариант разложения, не порог** (`ADR-119 §2`): разность обязана
  разложиться на строки-сироты и периметр типов документов без свободного остатка. Рост видимой
  разности до `≈1,75 %` есть предсказанное поведение (риск `R1`, `ADR-101`); он не является
  регрессом, не переоткрывает строки 18/19 и не служит основанием откатывать деплой.
  **Объявление пары сошедшейся либо её переоткрытие сессии-исполнителю не отдаётся**
  (`ADR-076 §4`, `ADR-091 §8`) — сессия предъявляет разложение, вердикт выносит архитектор.

  §5 **Оговорки `ADR-076 §5` обязательны;** перед первым живым вызовом — отдельное сообщение в чат
  по `ADR-077 §6`. Форма логирования — `ADR-079 §6`.

  §6 **Ловушка, снятая заранее:** `curl` с заголовком `Accept-Encoding: gzip` без флага
  `--compressed` отдаёт нераспакованные байты и ломает разбор ответа
  (`PARITY-STOCK-SNAPSHOT-SYNC`, 2026-08-04); `bq query` без явного `--max_rows` тихо режет вывод
  до 100 строк (`PARITY-STOCK-INTRANSIT`, 2026-08-04).

  **Последствия:**
  - [сейчас] `07_STATE.md` §Мандат Claude Code: строка `SALES-PERIMETER-PARITY-RECHECK` несёт
    «выдан поимённо» со ссылкой на этот ADR.
  - [задачей SALES-PERIMETER-PARITY-RECHECK] измерение и правка строк 18-19
    `reference/parity_registry.md`.
  - [без правок] `briefs/SALES-PERIMETER-PARITY-RECHECK.md` — бриф не переписывается, мандат
    добавляется строкой реестра и этим ADR.

  Введено сессией `SALES-PERIMETER-QUEUE-ADJ` (2026-08-07, архитектор).

- ADR-134: Метка канала периметра продаж — константа по типу документа для обоих типов

  **Статус: accepted** (владелец, чат 2026-08-07).

  **Контекст.** У строк периметра продаж (`entity/retaildemand`/`entity/commissionreportin`,
  `SALES-PERIMETER-EXTEND`) поле `sales_channel_name` было жёстко `NULL`. Офлайн-разбор дампов
  показал смешанный источник — `entity/retaildemand` не несёт `salesChannel` вовсе,
  `entity/commissionreportin` несёт его на всех строках выборки. Развилка (читать реальное
  значение там, где оно есть, или метить константой оба типа единообразно) была явно
  зарезервирована за владельцем (`07_STATE.md` «Развилки на владельце», `07_GAPS.md:87`
  «Рекомендация архитектора, если решает владелец»).

  **Решение.** Владелец выбрал константу-метку типа документа для ОБОИХ типов («Розница» для
  `entity/retaildemand`, «Комиссия» для `entity/commissionreportin`); реальный `salesChannel`
  из `entity/commissionreportin` НЕ читается патчем.

  **Последствия:**
  - [сейчас] `reference/code/cf-facts/fetch_perimeter.py` — `_fetch_positions_for` получает
    параметр `sales_channel_name`, обе вызывающие функции передают константу.
  - [сейчас] `reference/code/cf-facts/bq_ops.py` — `PERIMETER_STAGING_SCHEMA` несёт
    `sales_channel_id`/`sales_channel_name`; `_build_perimeter_merge_sql` читает их из staging
    (не `CAST(NULL AS STRING)`), `WHEN MATCHED` тоже обновляет оба поля.
  - [задачей SALES-PERIMETER-CHANNEL-DECIDE (деплой)] Деплой `cf-facts` с этой правкой — мандат
    класса B не выдан этим ADR, заводится отдельным поимённым ADR по факту готовности (уже
    зафиксированной этим коммитом), прецедент `ADR-109 §1`.
  - [без правок] `reference/sql/sq_marts_sales_overview.sql` — `COALESCE(f.sales_channel_name,
    'Не указан')` корректен при любом исходе развилки, вне scope (`00_CHARTER §главный принцип`).

  Введено сессией `SALES-PERIMETER-CHANNEL-DECIDE` (2026-08-07, executor).

---

### ADR-135: мандат класса B на деплой `cf-facts` с меткой канала периметра продаж; готовность патча подтверждена архитектором по существу; дефект досева соседнего патча адъюдицирован

  **Статус: accepted** (владелец выдал мандат прямым решением в чате 2026-08-07, запросив решение
  для закрытия задачи).

  **Контекст.** Сессия `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY` исполнила шаги 1-4 брифа и
  остановилась перед шагом 5, назвав отсутствие поимённого мандата. Остановка предписана самим
  брифом (шаг 5, дословно) и `_GENERATOR.md §4a`; это исправное поведение, а не сбой. Подготовлено:
  побайтовая сверка живой ревизии `cf-facts-00008-zen` с `master` код-репо (совпадение, дрейфа нет),
  ветка `deploy/cf-facts-2026-08-07-channel` (коммит `84d6f71`) запушена в `holika-prod`, патч
  перенесён по diff.

  **Решение.**

  §1 **Мандат класса B выдан поимённо** на шаги 6-10 брифа
  `briefs/SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY.md`.

  §2 **Разрешён ровно один деплой ровно одного объекта:** Cloud Function `cf-facts`, регион
  `asia-east1`, `--source` = каталог `cf-facts/` выкаченной ветки `deploy/cf-facts-2026-08-07-channel`
  и ничто иное. Никакие другие функции, никакие Workflows, никакие джобы Cloud Scheduler, никакие
  правки живых `transferConfig` этим мандатом не разрешены. Функциональные прогоны, разрешённые
  тем же мандатом: `hourly` (шаг 8), `perimeter` (шаг 9а, пишет только в staging) и
  `perimeter_promote` (шаг 9б, `MERGE` в `core.fact_sales_profit`) — строго в этом порядке, шаг 9б
  только после напечатанной проверки шага 9а.

  §3 **Откат назван до исполнения:** повторный деплой ревизии `cf-facts-00008-zen` по имени
  (`generation 1786093276804812`). Данные откатывать не требуется — патч меняет только значение
  двух колонок у строк периметра, `NULL` есть восстановимое состояние; при необходимости оно
  возвращается прогоном `perimeter_promote` с откаченного кода. Ветка НЕ сливается в `master` до
  успешного read-back (шаг 7); при неуспехе любого из шагов 6-9 неудачная попытка фиксируется в
  `MANIFEST.md` наравне с удачной.

  §4 **Адъюдикация дефекта досева в соседней подготовке — `SALES-DOCUMENT-OWNER-INGEST`.**
  Прочитан снапшот: `document_owner_employee_id` внесён в `STAGING_SCHEMA`, в `SELECT` внутри
  `USING` и в явный `INSERT (колонки)`, но НЕ внесён в `WHEN MATCHED THEN UPDATE SET`
  (`bq_ops.py`, `_build_merge_sql`; факт назван самой сессией подготовки, `07_STATE.md:1088-1090`).
  Обоснование «консистентно с уже принятым поведением `agent_id`» **принимается как верное для
  колонки, существовавшей с первой загрузки, и отклоняется для колонки, добавляемой задним
  числом**: строка, уже лежащая в `core.fact_sales_profit`, матчится по `transaction_id` и уходит
  в ветку обновления, где новой колонки нет, — значит июль-2026 не получит значение ни при одном
  числе повторных прогонов. Это тот же механизм, что `ADR-100 §2` уже описал на `transaction_date`.
  Следствие прямое: деплой патча в текущем виде **не двигает клиентскую цифру, ради которой он
  заводился** (`ADR-128`), и приёмка «разрез по сотрудникам сошёлся» была бы невыполнима по
  построению (`05_CONVENTIONS` Часть I ★ «критерий приёмки выводится из формулы»).
  **Предусловие деплоя, добавляемое к строке `SALES-DOCUMENT-OWNER-DEPLOY`:** колонка вносится в
  `WHEN MATCHED THEN UPDATE SET` до деплоя, и первый прогон идёт режимом с окном, покрывающим
  июль-2026 (`weekly`, `window_days = 90`; июль внутри окна до `2026-09-29`, `ADR-125 §5`).
  **Названная цена решения, а не скрытая:** семантика поля перестаёт быть «владелец на момент
  первой загрузки» и становится «владелец на момент последнего прогона». Какое из двух значений
  режет отчёт МойСклада «по сотрудникам», репо не устанавливает — это открытый вопрос, уже
  названный строкой `Q-105` (версионирование `owner`), и настоящий ADR его не закрывает. Выбор
  сделан в пользу текущего значения по одному доводу: снимочная семантика оставляет историю пустой,
  то есть не даёт вообще никакого разреза, а неверная семантика даёт разрез, расхождение которого
  измеримо и объяснимо. Если замер покажет, что источник режет по значению на момент документа, —
  это правка одной ветки `MERGE`, а не потеря работы.

  §5 **Имена.** Задача-строка и строка мандата существуют под именем родительской задачи
  (`SALES-PERIMETER-CHANNEL-DECIDE`, строка мандата «…, деплой»). Имя
  `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY` есть task-ID сессии, её ветки, дерева и брифа — то же
  дело, не вторая задача. **Дубликат строки не заводится**, правится ячейка «Мандат» существующей
  строки; парность `ADR-090 §1` этим сохраняется. Побочное следствие, названное и не чинимое:
  `tools/parallel_check.sh` по имени `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY` строк в таблице
  мандата не найдёт и вернёт `RC 3`; на вердикт это не влияет — класс B не параллелится никогда
  (`ADR-082 §1`), и запуск запрещён независимо от вывода скрипта.

  §6 **Смешанный патч в снапшоте — наблюдение, не новое правило.** Снапшот
  `reference/code/cf-facts/` несёт два независимых недеплоенных патча в одних и тех же файлах.
  Действующая процедура это уже покрывает: перенос по diff против `master` код-репо (шаг 3 брифа,
  `05_CONVENTIONS` Часть II вариант Б) — именно он поймал смешение до коммита. Нового правила
  метода не вводится (`ADR-107 §1`): ни одна задача паритета без него не встаёт.

  §7 **Оговорка `ADR-077 §6` в силе:** перед исполнением шага 6 сессия отдельным сообщением в чат
  называет, что исполняется, на каком объекте и чем откатывается. Шаги 1-4 повторно не исполняются
  — они выполнены и закоммичены; шаг 2 повторяется ТОЛЬКО если между сессиями появилась новая
  ревизия `cf-facts` (различитель — `describe`, один read-only вызов).

  **Последствия:**
  - [сейчас] `07_STATE.md` §Мандат Claude Code, строка `SALES-PERIMETER-CHANNEL-DECIDE, деплой`:
    ячейка «Мандат» — `НЕ выдан` → `выдан поимённо ADR-135`.
  - [сейчас] `07_STATE.md` §Открытые вопросы, строка `SALES-DOCUMENT-OWNER-DEPLOY`: в колонку
    «Гейт» дописывается «плюс правка `WHEN MATCHED THEN UPDATE SET` до деплоя (§4 `ADR-135`)».
  - [сейчас] `07_GAPS.md`, строка `SALES-DOCUMENT-OWNER-DEPLOY`: то же предусловие дословно.
  - [сейчас] `07_STATE.md` §Стенд-ап и §Подробности для модели — по настоящему блоку.
  - [сейчас] `06_INDEX.md`: строка на этот ADR.
  - [задачей SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY] шаги 5-10 брифа поверх подготовленной ветки;
    по их завершении строка `SALES-PERIMETER-CHANNEL-DECIDE` закрывается целиком и уезжает в
    `07_ARCHIVE.md` вместе с обеими строками мандата (`ADR-064`, `ADR-090 §1`).
  - [задачей SALES-DOCUMENT-OWNER-INGEST либо SALES-DOCUMENT-OWNER-DEPLOY] правка
    `WHEN MATCHED THEN UPDATE SET` — кем именно, решает постановка; работа одной строки.
  - [без правок] `briefs/SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY.md` — бриф верен и не
    переписывается; `reference/code/cf-facts/*` — этой сессией не трогались;
    `reference/sales_perimeter_channel_decide_2026-08-07.md` и артефакт сессии подготовки —
    провенанс, не переписываются (`ADR-043`).

  Введено сессией `SALES-PERIMETER-CHANNEL-DEPLOY-ADJ` (2026-08-07, архитектор).

### ADR-136: строка `SALES-DOCUMENT-OWNER-DEPLOY` разрезана на доработку (класс A) и деплой (класс B); предписанная `ADR-135 §4` правка становится исполнимой

**Статус: accepted** (форма организации работы класса A; мандата владельца не требует —
`ADR-076 §1`, класс A идёт постоянно).

**Контекст.** `ADR-135 §4` предписал внести `document_owner_employee_id` в ветку
`WHEN MATCHED THEN UPDATE SET` до деплоя. Сессия, которая взялась бы за эту правку сегодня,
остановилась бы, и правильно:

(i) Родительская задача `SALES-DOCUMENT-OWNER-INGEST` закрыта DONE и уехала в `07_ARCHIVE.md`
(строка 205) — живой строки у неё нет, а `ADR-086 §3` запрещает брать определение задачи из
разговора.

(ii) Единственная живая строка — `SALES-DOCUMENT-OWNER-DEPLOY`, и она класса B с невыданным
мандатом. Правка снапшота есть класс A (`ADR-076 §1`, ничего не деплоит); расхождение класса между
тем, что делает сессия, и тем, что говорит таблица мандата, есть стоп-условие сессии
(`CLAUDE.md §Мандат`).

(iii) Последствие `ADR-135` было записано как `[задачей SALES-DOCUMENT-OWNER-INGEST либо
SALES-DOCUMENT-OWNER-DEPLOY] — кем именно, решает постановка`. Постановка не состоялась, и вилка
осталась открытой: это ровно тот класс «решение принято, приземление не назначено», который уже
один раз стоил проекту непришедшей адъюдикации fail-open (`ADR-122`, признак готовности
`ADR-109 §1` ловит отсутствие подготовки и не ловит отсутствие приземления).

**Решение.**

§1 Строка таблицы мандата `SALES-DOCUMENT-OWNER-DEPLOY` заменяется ДВУМЯ строками по образцу
уже применённого в этой же таблице разреза (`INGEST-CURRENCY-ASSERT`, `DQ-GATE-FAIL-OPEN-FIX`,
`SALES-PERIMETER-CADENCE`):

- `SALES-DOCUMENT-OWNER-DEPLOY, доработка патча` — класс **A**, параллель **да**, мандат
  **постоянный**. Основание: правка снапшота `reference/code/cf-facts/bq_ops.py`, ничего не
  деплоит, живых вызовов нет. Пишет: `reference/code/cf-facts/`,
  `reference/sales_document_owner_deploy_prep_<date>.md`,
  `reference/_scratch_SALES-DOCUMENT-OWNER-DEPLOY_<date>/`.
- `SALES-DOCUMENT-OWNER-DEPLOY, деплой` — класс **B**, параллель **нет**, мандат **НЕ выдан**.
  Основание: деплой `cf-facts` плюс `ALTER TABLE` живой `core.fact_sales_profit`; выдаётся
  отдельным поимённым ADR по факту готовности доработки (`ADR-109 §1`).

§2 **Содержание доработки названо дословно, чтобы сессия не переизобретала scope.** В
`_build_merge_sql` (`reference/code/cf-facts/bq_ops.py`), ветка `WHEN MATCHED THEN UPDATE SET`,
добавляется одна строка `T.document_owner_employee_id = S.document_owner_employee_id`. Ничто
другое в патче `SALES-DOCUMENT-OWNER-INGEST` не правится: `STAGING_SCHEMA`, `SELECT` внутри
`USING`, явный `INSERT (колонки)` (`C1`, `ADR-030`) и `fetch_demands.py` уже готовы и
подтверждены. `_build_perimeter_merge_sql` не трогается — источники периметра на предмет поля
`owner` не проверялись, это отдельный вопрос и он этой правкой не решается.
**Приёмка:** напечатанный `git diff` снапшота (ровно одна добавленная строка),
`python3 -m py_compile` свежим прогоном, и явное подтверждение в артефакте, что список колонок
ветки `UPDATE SET` и список колонок `INSERT` после правки различаются ровно на те колонки,
которые различались до неё, плюс новая.

§3 **Задача-строка GAP-реестра остаётся ОДНА.** Парность `ADR-090 §1` требует, чтобы строка
мандата и задача-строка заводились и убирались одним коммитом, но не требует их
один-к-одному: в таблице уже живут задачи с двумя и тремя строками мандата на одну строку
реестра. Уточняется только колонка «Гейт» строки `SALES-DOCUMENT-OWNER-DEPLOY` (`07_STATE`
индекс и `07_GAPS.md` полный текст): «сначала доработка патча — класс A, мандат постоянный
(§1-§2 настоящего ADR); затем деплой — класс B, мандат выдаётся отдельным поимённым ADR по
факту готовности доработки».

§4 **Порядок первого прогона после будущего деплоя фиксируется здесь, а не откладывается на
момент деплоя:** режим `weekly`, `window_days = 90` — иначе июль-2026 не попадает в окно
`MERGE` и правка §2 не даёт наблюдаемого эффекта. Окно закрывается `2026-09-29`
(`ADR-125 §5`), после чего июль штатным путём не досеется.

§5 **Заморозка `ADR-107` не нарушена:** решение предметное, правил метода не вводит; оно
организует уже принятую работу, а без него встаёт названная задача паритета
(`SALES-DOCUMENT-OWNER-DEPLOY`), то есть условие `ADR-107 §2` выполнено явно.

**Последствия:**
- [сейчас] `07_STATE.md` §Мандат Claude Code: строка `SALES-DOCUMENT-OWNER-DEPLOY` заменяется
  двумя строками §1 дословно.
- [сейчас] `07_STATE.md` §Открытые вопросы и `07_GAPS.md`, строка `SALES-DOCUMENT-OWNER-DEPLOY`:
  колонка «Гейт» / «Как закрывается» получает текст §3 и требование §4.
- [сейчас] `07_STATE.md` §Подробности для модели: append абзац настоящего блока.
- [сейчас] `06_INDEX.md`: строка на этот ADR.
- [задачей SALES-DOCUMENT-OWNER-DEPLOY, доработка патча] сама правка одной строки.
- [без правок] `reference/code/cf-facts/*` — этой сессией не трогались;
  `07_ARCHIVE.md` — закрытая строка `SALES-DOCUMENT-OWNER-INGEST` не возвращается в оборот
  (`ADR-064`), работа продолжается под именем задачи деплоя.

Введено сессией `SALES-DOCUMENT-OWNER-SPLIT-ADJ` (2026-08-07, архитектор).

### ADR-137: эталон июля-2026 принят в двух разрезах; вклад патча периметра подтверждён точным совпадением; остаток локализован в ветке `entity/demand`; две посылки прежних решений скорректированы фактом

**Статус: accepted** (владелец предоставил обе выгрузки прямым сообщением в чате 2026-08-07,
сняв их сам в интерфейсе МойСклад).

**Контекст.** До этой сессии эталон июля существовал только округлёнными числами со звонка
клиента (`ADR-125 §1`), на которых разрыв в сотые доли процента неразличим, а вопрос «пусто ли
поле канала в источнике или теряется загрузчиком» был объявлен неразличимым с нашей стороны
(`ADR-126 §7`). Владелец снял оба разреза сам.

**Решение.**

§1 **Обе выгрузки принимаются как снимок уровня (iii) (`ADR-067 §2`)** и фиксируются артефактом
`reference/oracle_july_2026_2026-08-07.md` (таблица каналов — дословно; PDF отчёта по
сотрудникам — рядом, `reference/oracle_july_2026_employees_2026-08-07.pdf`). Артефакт
датирован и не переписывается (`ADR-043`).

§2 **Вклад патча периметра подтверждён точным совпадением, а не порогом.** Прирост нашей
стороны `112 502 581,88 − 109 167 858,30 = 3 334 723,58` KGS в точности равен сумме двух
маркетплейс-каналов источника (`3 097 648,79 + 237 074,79`). Совпадение до копейки на величине
в три миллиона случайным быть не может. Это подтверждение НЕ заменяет
`SALES-PERIMETER-PARITY-RECHECK` (май, построчно, другой endpoint) и мандат `ADR-133` не
расширяет; необходимость расширять его на июль снимается.

§3 **Остаток `178 124,42` KGS локализован целиком в ветке `entity/demand`.** Механизм не
установлен. Кандидат — строки-сироты `MERGE` без ветки удаления (`ADR-100 §1`): знак сходится
(наша сторона выше), но согласие знака доказательством не является. Прежняя редакция этого
решения (черновик в буфере) объясняла остаток «движением источника с 2026-08-05» через
разность `171 376,12 + 6 748,30`; **это объяснение отзывается** как построенное на округлённом
до десятка числе со звонка (`ADR-125 §1`) и не имеющее механизма. Различитель — задача §5.

§4 **`ADR-126 §7` различён фактом.** У источника за июль корзины «канал не указан» НЕТ: три
канала замыкают итог до копейки (`112 324 457,46`); канал «ОФИС» существует как значение (назван
в фильтре по умолчанию отчёта по сотрудникам), но оборота за июль не имеет. Следовательно наши
40 строк с пустым `sales_channel_name` (`821 523,00` KGS) теряют поле на нашей стороне. Оговорка
названа и не спрятана: отчёт мог бы не показывать пустую группу вовсе — тогда вывод не следует;
снимается одним взглядом владельца на тот же разрез. Заводится `SALES-CHANNEL-NULL-DEMAND`.

§5 **Заводится `SALES-JULY-RESIDUAL-SPLIT`** — класс A, read-only, брифа не требует
(`ADR-086 §1`); полный текст определения и приёмки — строка `07_GAPS.md`.

§6 **Посылка `ADR-134` скорректирована в части, касающейся источника.** Формулировка «реальный
`salesChannel` из `commissionreportin` патчем не читается» верна как описание патча и неверна
как утверждение об источнике: у источника те же документы лежат в поимённых каналах «UMAI WB» и
«К Глобал», то есть поле заполнено. Решение `ADR-134` (константы «Розница»/«Комиссия») **не
отменяется** — оно задеплоено, работает и на итог не влияет; но клиент на разрезе по каналам
увидит наши метки вместо имён МойСклада. Заводится `SALES-PERIMETER-CHANNEL-REAL` со статусом
`DEFER`, триггер — решение владельца: это вопрос совпадения ИМЁН на клиентской поверхности, а
не величин, и финиш он не гейтит.

§7 **Отчёт по сотрудникам ПРИГОДЕН как полный построчный эталон по выручке.** Постраничный
разбор: страницы `1–7` несут полную таблицу (`Наименование · Тип документа · Номер · Время ·
Сумма`); страница `8` начинается заголовком «Сумма себест.», страницы `8–14` — одна отставшая
колонка себестоимости, вынесенная экспортом на отдельные листы. Владелец решил (чат 2026-08-07)
колонку себестоимости отбросить и читать файл до её начала; решение принимается — себестоимость
объектом паритета не является (§10), а её позиционное восстановление дало бы величину, чья
привязка к строке не проверяется ничем. **Повторная выгрузка НЕ требуется**, прежнее требование
снимается. Итоговой строки в файле нет, поэтому **два контроля полноты обязательны перед
использованием файла как эталона:** число строк страниц `1–7` обязано дать `243` документа
продажи плюс `1` возврата (состав канала «Оптовая торговля» из §1 артефакта, отчёт отфильтрован
теми же каналами), а сумма отгрузок обязана дать `108 989 733,88` KGS. Расхождение любого из
двух есть стоп, а не повод округлить (`ADR-021 §2`). Независимое подтверждение единства фильтра
уже есть: возврат `00008` на `62 709,00` KGS присутствует и в отчёте, и в строке возвратов того
же канала.

§8 **Границы периода РАЗРЕШЕНЫ фактом: окна источника и нашей стороны за июль совпадают.**
Последняя строка отчёта — `Возврат покупателя № 00008, 01.07.2026 18:33:00` — есть ТОТ ЖЕ
документ, на котором `ADR-088 §2` уже провёл замер (`entity/salesreturn` №00008, API
`2026-07-01 15:33:00` UTC против интерфейса `01.07.2026 18:33`). Совпадают номер, тип, дата и
минута, следовательно отчёт рендерит время в `UTC+3`, а шапка `30.06.2026 21:00:00 …
31.07.2026 20:59:00` есть `2026-06-30T18:00Z … 2026-07-31T17:59Z`, то есть дословно
`DATE(M, 'Asia/Bishkek') = июль` — правило суток нашей стороны (`bq_ops.py:158`, `ADR-088 §4`).
**Следствие:** полоса `M ∈ [18:00; 21:00)` (`ADR-088 §5`) к июльскому сравнению неприменима и
из кандидатов на остаток §3 вычёркивается; задача `SALES-JULY-RESIDUAL-SPLIT` её не проверяет.
Планировавшийся различитель класса B **не потребовался** — он оказался уже исполненным и
лежащим в репо; мандат на него не запрашивается. Оговорка названа: отождествление документа
сделано по номеру, типу, дате и минуте, не по UUID (его в PDF нет).

§9 **Числа документов сторон не сопоставляются** (`249` против `2 936`/`2 870` — разные отчёты;
`4 707` нашей стороны есть строки позиций, число документов из `core.fact_sales_profit` не
восстанавливается в принципе, `ADR-104 vi`).

§10 **Себестоимость (`87 361 338,27` итог, `85 701 177,07` по «Оптовой») записана как факт,
объектом паритета НЕ является** и в реестр не вносится: реестр закрыт `7/7`, расширение состава
есть решение владельца (`ADR-067 §4`, `ADR-069 §4`).

§11 **Развилка имён каналов закрыта владельцем (чат 2026-08-07): оставить как есть до
завершения текущей очереди.** Обоснование владельца принято и совпадает с рекомендацией
архитектора: финиш это не гейтит, а деплойный цикл дороже сходства подписей. Возврат к вопросу
— при появлении второго значимого маркетплейса. Строка `SALES-PERIMETER-CHANNEL-REAL` заводится
сразу со статусом `DEFER` с этим триггером, а не как открытая работа.

**Последствия:**
- [сейчас] `reference/oracle_july_2026_2026-08-07.md` и
  `reference/oracle_july_2026_employees_2026-08-07.pdf` — коммитятся вместе с блоком.
- [сейчас] `07_GAPS.md` + `07_STATE.md` §Открытые вопросы + §Мандат Claude Code: три строки
  (Патчи ниже), парность `ADR-090 §1`.
- [сейчас] `07_STATE.md` §Подробности для модели: append абзац настоящего блока.
- [сейчас] `06_INDEX.md`: строка на этот ADR.
- [задачей SALES-JULY-RESIDUAL-SPLIT] различитель механизма §3.
- [задачей SALES-CHANNEL-NULL-DEMAND] локализация 40 строк без канала.
- [задачей SALES-PERIMETER-CHANNEL-REAL] имена каналов периметра, после решения владельца.
- [без правок] `reference/parity_registry.md` — реестр закрыт `7/7`, июль в его зону не входит
  (`ADR-115 §11`); `briefs/SALES-PERIMETER-PARITY-RECHECK.md` и мандат `ADR-133` — по маю, не
  расширяются; `reference/finish_calibration_2026-08-05.md` — округлённые числа со звонка
  остаются провенансом (`ADR-043`), настоящий ADR их уточняет, а не заменяет; `ADR-134` —
  append-only, текст не редактируется, коррекция посылки живёт в §6.

Введено сессией `SALES-JULY-ORACLE-ADJ` (2026-08-07, архитектор).

---

## ADR-138 · Замер июльского остатка признан частичным по дефекту посылки; названы четыре кандидатных механизма и офлайн-различитель; заводятся `SALES-JULY-DAILY-RECON` и `INGEST-DEMAND-APPLICABLE-FILTER`

**Дата:** 2026-08-08 · **Статус:** accepted (архитектор — §1-§4 фактура и форма; владелец — апрув всех
четырёх пунктов, чат 2026-08-08)

**Контекст.** Сессия `SALES-JULY-RESIDUAL-SPLIT` (2026-08-07) объяснила `12,73 %` июльского
остатка `+178 124,42` KGS механизмом строк-сирот (`ADR-100 §1`) и оставила `155 456,4092` KGS
архитектору как `CONTEXT GAP`, явно не называя следующего кандидата (anti-improvisation).
Адъюдикация этого гэпа не отдаётся сессии-исполнителю (`ADR-076 §4`, уточнено `ADR-091 §8`).

**Решение.**

§1 **Замер `SALES-JULY-RESIDUAL-SPLIT` признаётся исполненным частично и с дефектом посылки.**
Идентификация «последнего массового прогона» опирается на утверждение «все прочие сутки после
него несут `30–230` строк» (`reference/sales_july_residual_split_2026-08-07.md`, раздел
идентификации), которое опровергается собственной таблицей (1) того же артефакта: строка
`2026-08-07 | 1 830` превышает пик `2026-07-19 | 1 113`. По `07_STATE.md:1106` прогон
`2026-08-07` был разовым ручным прогоном периметра (`+5 467` строк за 90 суток) — другая ветка
со своим `MERGE`, то есть идентификация сделана на смешанной популяции. Дополнительно критерий
«`_loaded_at` старше последней полной перезагрузки» односторонний: он ловит только сирот,
возникших ДО неё, тогда как между `2026-07-19` и датой эталона `2026-08-07` лежат 19 суток, а
сутки `07-01…07-18` не покрыты ни одним 7-суточным окном после `07-19`. Следствие: `22 668,0108`
KGS есть НИЖНЯЯ граница вклада сирот, `155 456,4092` KGS — ВЕРХНЯЯ граница вклада прочих
механизмов; раскладка «12,73 % / 87,27 %» разбиением не является. Артефакт не переписывается
(`ADR-043`), поправка живёт здесь. **accepted** (архитектор).

§2 **Названы четыре кандидатных механизма, все выведены из репо.**
(K1) сироты в слепом окне 19 суток — `ADR-100 §1`, `bq_ops.py:237-305` (у `MERGE` нет ветки
удаления); знак совпадает; не измерен.
(K2) черновики: `entity/demand` грузится без фильтра `applicable` — `fetch_demands.py:68-76`
фильтрует только по `moment`, тогда как `fetch_returns.py:48` несёт `applicable=true`
(«TD-11: defensive — исключаем черновики») и `fetch_perimeter.py:178`/`:218` несут его же;
спека требует исключать черновики (`03_PIPELINE_SPEC.md:364`). Асимметрия есть ФАКТ; считает ли
отчёт «Прибыльность» непроведённые документы — репо не устанавливает, это названный
`CONTEXT GAP`, а не подставленная посылка.
(K3) `transaction_date` не обновляется — `ADR-100 §2`, `bq_ops.py:240-256` (колонки нет в
`WHEN MATCHED THEN UPDATE SET`); в мае вклад в месячный итог был нулевым (обе даты внутри мая),
в июле нулевым быть не обязан — перенос `moment` через границу месяца даёт ненулевой вклад.
(K4) список `COMMISSION_CHANNEL_AGENT_IDS` (`config.py:56-60`) измерен исчерпывающе только по
маю-2026 (`ADR-104`); контрагент, ставший комиссионным в июле, останется в нашей «Оптовой»
выручкой, которой у источника нет.
**Порядок работ — K1 первым по исполнению** (дешевле и обязателен: новый механизм не называется,
пока названный недоизмерен), **K2 первым по существу** (объясняет две вещи одним механизмом —
и невхождение в оборот источника, и незаполненный канал у 40 строк, из которых 38 суть один
контрагент, одни сутки `2026-07-21`, один заброс загрузчика,
`reference/sales_channel_null_demand_2026-08-07.md §2`). Владелец принял эту рекомендацию
(чат 2026-08-08). **accepted** (архитектор — фактура; владелец — принятие порядка).

§3 **Заводится `SALES-JULY-DAILY-RECON` — класс A, без брифа (`ADR-086 §1`).** Приёмка дословно,
цитируется текстом запуска без пересказа:
«(1) Пересъём распределения `_loaded_at` по июлю-2026 С ФИЛЬТРОМ ВЕТКИ
(`sales_channel_name IS NULL OR sales_channel_name NOT IN ('Розница','Комиссия')`); назвать
последний массовый прогон ветки `entity/demand` и указать числом слепое окно между ним и датой
эталона `2026-08-07`; пересчитать кандидатов в сироты на отфильтрованной популяции.
(2) Контроль полноты построчного эталона `reference/oracle_july_2026_employees_2026-08-07.pdf`
(стр. 1–7): число строк против `243` отгрузок + `1` возврата и сумма отгрузок против
`108 989 733,88` KGS. Расхождение любого из двух контролей есть СТОП с предъявлением чисел, а не
повод округлить или продолжить.
(3) Суточный ряд эталона, собранный из PDF по колонке «Время» с пересчётом `−3 часа` в UTC
(`ADR-137 §8`, правило пересчёта — `reference/oracle_july_2026_2026-08-07.md §3`), против нашего
суточного ряда по «Оптовая + строки без канала» (`sales_channel_name IS NULL OR = 'Оптовая
торговля'`); возврат `№ 00008` из эталона исключается, объект сверки — только продажи.
(4) Вердикт: на какие сутки приходится `+178 124,42` KGS, какой из кандидатов K1–K4 этой
локализацией подтверждается и какой опровергается, и необъяснённый остаток назван числом. Если
разность на сутках равна сумме одного-двух документов PDF — назвать их номера.
Границы: живых вызовов к МойСкладу НЕТ, эталон принят числами; в `core.*`/`marts.*` не пишет; код
не правится; `bq query --format=json|prettyjson` без явного `-n`/`--max_rows` тихо режет вывод до
100 строк (`BQ-QUERY-MAX-ROWS`). Пишет: `reference/sales_july_daily_recon_<date>.md`,
`reference/_scratch_SALES-JULY-DAILY-RECON_<date>/`.»
**accepted** (архитектор — форма и приёмка; владелец — апрув заведения).

§4 **Заводится `INGEST-DEMAND-APPLICABLE-FILTER` — OPEN, гейт: результат `SALES-JULY-DAILY-RECON`.**
Предмет: ветка продаж грузит документы без фильтра `applicable`, три соседние ветки его несут.
Форма фикса НЕ назначается до замера (`_METHOD §6`): величина вклада неизвестна, а добавление
фильтра при отрицательном результате замера есть правка вслепую. Подготовка — класс A (правка
снапшота `reference/code/cf-facts/`, ничего не деплоит); деплой — класс B, дополнительно гейтится
`ADR-065`. **accepted** (архитектор — форма; владелец — апрув заведения).

**Последствия:**
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — заводятся строки
  `SALES-JULY-DAILY-RECON` (READY) и `INGEST-DEMAND-APPLICABLE-FILTER` (OPEN), полный текст
  второй несёт §2(K2) дословно; гейт строки `SALES-JULY-RESIDUAL-SPLIT` переводится на
  `SALES-JULY-DAILY-RECON`, её статус остаётся PARTIAL, числа переклассифицируются по §1.
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — парные строки (`ADR-090 §1`):
  `SALES-JULY-DAILY-RECON` — класс A, параллель да, мандат постоянный, «Пишет:
  `reference/sales_july_daily_recon_<date>.md`, `reference/_scratch_SALES-JULY-DAILY-RECON_<date>/`»;
  `INGEST-DEMAND-APPLICABLE-FILTER, подготовка` — класс A, параллель нет, мандат постоянный, гейт
  результата замера, «Пишет: `reference/code/cf-facts/`,
  `reference/ingest_demand_applicable_filter_<date>.md`»;
  `INGEST-DEMAND-APPLICABLE-FILTER, деплой` — класс B, мандат НЕ выдан, гейт `ADR-065`.
- [сейчас] `06_INDEX.md` — строка на настоящий ADR тем же коммитом.
- [без правок] `reference/sales_july_residual_split_2026-08-07.md`,
  `reference/oracle_july_2026_2026-08-07.md` — датированные артефакты не переписываются
  (`ADR-043`), поправка §1 живёт в настоящем ADR и в `07_STATE §Подробности для модели`.
- [без правок] `00_CHARTER.md` — финишная черта не меняется: июльский остаток вне зоны паритета
  (`ADR-115 §11`) и финиш не гейтит.
- [без правок] `reference/code/cf-facts/` — эта сессия кода не правит; правка адресована §4.
- [задачей SALES-JULY-DAILY-RECON] локализация `+178 124,42` KGS по суткам и вердикт по K1–K4.
- [задачей INGEST-DEMAND-APPLICABLE-FILTER] форма фикса фильтра черновиков.

Введено сессией `SALES-JULY-RESIDUAL-ADJ` (2026-08-08, архитектор), применён сборкой буфера
2026-08-08.

---

## ADR-139 · Мандат класса B на остаток `SALES-CHANNEL-NULL-DEMAND` выдан поимённо

**Дата:** 2026-08-08 · **Статус:** accepted (владелец — мандат выдан в чате 2026-08-08; архитектор —
состав вызовов, различитель и границы)

**Контекст.** `SALES-CHANNEL-NULL-DEMAND` (2026-08-07) локализовала механизм внутри нашего кода:
у всех 40 строк `sales_channel_id` тоже `NULL`, что опровергает промах кэша `channel_map` и
сводит потерю к пустому `salesChannel` уже во входном ответе списка (`fetch_demands.py:104`).
Остаток — один бит: «list-режим МойСклада систематически не отдаёт `salesChannel`» против
«канал назначен ПОСЛЕ первой загрузки». Нашими данными не различим.

**Решение.**

§1 **Мандат класса B ВЫДАН поимённо** (владелец, чат 2026-08-08). Единственное основание класса —
доступ к секрету `msklad-token` (`ADR-076 §1`). Задача только читает; в `core.*`/`marts.*` и в
живые конфигурации не пишет; отката не требует по построению, потому что состояние не меняется.
Шесть оговорок `ADR-076 §5` и форма логирования `ADR-079 §6` обязательны без изменений.

§2 **Состав вызовов ограничен и назван, объём — не более шести `GET`.** Прямой
`GET entity/demand/{id}` по нашим данным невозможен: `core.fact_sales_profit` не несёт
`demand_id`, ключ строки есть `MD5(demand_id + position_id)` (`ADR-104 vi`). Поэтому
(1) списочный `GET entity/demand?filter=moment>=<сутки> 00:00:00;moment<=<сутки> 23:59:59` за
`2026-07-02` и за `2026-07-21`, из ответа берутся `id` документов нужных контрагентов
(`178ba7b4-87c0-11ef-0a80-1568002f4222` и `13564a08-87c0-11ef-0a80-1568002f3f8d`) и фиксируется,
присутствует ли в ЭТИХ ответах ключ `salesChannel`; (2) одиночный `GET entity/demand/{id}` по
найденным `id` (не более трёх документов) и фиксируется тот же ключ.

§3 **Различитель — сравнение двух режимов на ОДНОМ документе, а не наличие поля само по себе.**
Поле есть в одиночном ответе и отсутствует в списочном ⇒ list-режим теряет `salesChannel`,
фикс адресуется загрузчику (запрос поля явно либо дозапрос документа). Поле отсутствует в обоих ⇒
канал у документа не проставлен и сейчас, при том что отчёт источника за июль показывает оборот
только по трём заполненным каналам — тогда открытым остаётся вопрос о моменте назначения канала,
и он адресуется владельцу, не коду. Форма фикса настоящим ADR НЕ назначается.

§4 **Бесплатная альтернатива не отменяется и остаётся предпочтительной при равном результате:**
владелец открывает в интерфейсе МойСклад отгрузку от `2026-07-02` контрагента
`178ba7b4-87c0-11ef-0a80-1568002f4222` (сумма позиции `61 938,00` KGS) и смотрит поле «Канал
продаж». Это закрывает вторую половину §3 без единого вызова.

**Последствия:**
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md`, строка `SALES-CHANNEL-NULL-DEMAND` —
  гейт «мандат не выдан» → «мандат класса B ВЫДАН поимённо (`ADR-139 §1`, владелец, чат
  2026-08-08)»; статус остаётся PARTIAL.
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — строка
  `SALES-CHANNEL-NULL-DEMAND, остаток` (класс B, параллель нет, мандат выдан поимённо, «Пишет:
  `reference/sales_channel_null_demand_remainder_<date>.md`,
  `reference/_scratch_SALES-CHANNEL-NULL-DEMAND_<date>/`»); существующая строка класса A
  сохраняется — она описывает уже исполненную часть.
- [сейчас] `06_INDEX.md` — строка на настоящий ADR тем же коммитом.
- [без правок] `reference/code/cf-facts/fetch_demands.py` — форма фикса не назначена (§3).
- [задачей SALES-CHANNEL-NULL-DEMAND, остаток] исполнение §2 и вердикт §3.

Введено сессией `SALES-JULY-RESIDUAL-ADJ` (2026-08-08, архитектор), применён сборкой буфера
2026-08-08.

---

## ADR-140 · Финиш Epic-1 объявлен по выполненному критерию `00_CHARTER`; передача клиенту вводится как ОТДЕЛЬНАЯ фаза с собственным критерием отсечения задач

**Дата:** 2026-08-08 · **Статус:** accepted (владелец — объявление финиша, чат 2026-08-08; архитектор
— оговорки §2, критерий отсечения §3, состав очереди §4)

**Контекст.** `00_CHARTER §критерии успеха` определяет финиш Epic-1 так: каждая пара реестра
паритета сходится `0,00` либо расхождение закрыто явным правилом/ADR, на глубине истории с
`2026-05-01`. Счётчик достиг `7/7` `2026-08-07` (`ADR-129`, подтверждено `ADR-130 §1`), но
объявления не произошло, и репо продолжало вести себя так, будто финиш впереди. Одновременно
`ADR-107 §7` («снятие заморозки автоматическое при сходимости реестра паритета») сработал и
остался неприменённым. Расхождение факта и записи есть тот же класс дефекта, который проект чинит
весь месяц, и оно не лечится ожиданием.

**Решение.**

§1 **Финиш Epic-1 объявлен состоявшимся `2026-08-07`** — по дате закрытия седьмой пары
(`ADR-129`), не по дате настоящего решения. Основание — буквальное выполнение записанного
критерия. Откладывание объявления было бы применением незаписанного критерия, то есть тихим
переносом финишной черты; менять её можно, но решением уровня `ADR-067`, а не умолчанием.
**accepted** (владелец).

§2 **Три оговорки объявления, входящие в его текст, а не сопровождающие его.**
(i) **Зона паритета есть ДАТИРОВАННЫЙ СНИМОК мая-2026, а не непрерывно обновляемая область**
(`ADR-115 §11`, выбор владельца — вариант 1). Финиш утверждает сходимость на этой зоне.
(ii) **Вне финиша по построению:** DERIVED-величины — USD и всё производное от конвертации
(`ADR-067 §4`); поверхности вне реестра паритета (`00_CHARTER §критерии успеха`, расширение
реестра есть новое решение владельца); история до `2026-05-01` (`ADR-069`, помечена плашкой).
(iii) **Финиш не равен трём вещам, с которыми его легко спутать:** он не есть клиентская
картина на витринах (`ADR-125 §1`), не есть работоспособность регламента пересъёма (§3 ниже) и
не есть отсутствие техдолга. Каждая из трёх адресована отдельно.
**accepted** (архитектор).

§3 **Вводится ВТОРОЙ критерий, отдельный от финиша: «регламент пересъёма работает».** Июль-2026
— первый пересъём зоны по выбранной каденции (вариант 1); его приёмка есть `SALES-JULY-DAILY-RECON`.
Основание разделения: критерий, при котором каждый следующий месяц обязан сойтись до объявления
финиша, не наступает никогда — остаток свежего месяца есть свойство живого источника, а не
дефект сходимости. Смешение двух критериев в один и было причиной, по которой финиш не объявили
при `7/7`. **accepted** (архитектор).

§4 **Фаза передачи клиенту вводится явно, с критерием отсечения задач.** Задача входит в
передачу тогда и только тогда, когда её невыполнение даёт одно из трёх:
**(i)** неверное ЧИСЛО или неверную ПОДПИСЬ на поверхности, которую клиент смотрит;
**(ii)** ТИХИЙ отказ — данные встают или портятся, и об этом никто не узнаёт;
**(iii)** неремонтопригодность без нас — нет кода, нет процедуры, нет документа «где смотреть».
Строка реестра, не попавшая ни под один из трёх признаков, в передачу НЕ входит и не
выбрасывается: она переезжает в документ «известные ограничения», отдаваемый клиенту текстом.
Критерий проверяемый, а не тематический — тот же приём, которым `ADR-091 §1` резал задачи по
линии исполнимости действия, а `ADR-107 §2` резал исключения из заморозки.
Следствие-ограничитель: **до передачи новая задача-строка заводится, только если проходит
(i)/(ii)/(iii)**; прочие наблюдения фиксируются строкой в артефакте сессии и строкой реестра не
становятся (форма `ADR-107 §4`, действие которой этим восстанавливается на новом основании и на
новый срок — до подписания передачи). **accepted** (архитектор).

§5 **Риск `R1` (`ADR-101`) обязан быть назван владельцу ДО первого деплоя фазы передачи и
повторён в документе передачи.** Расхождение по продажам есть разность двух встречных смещений;
починка ОДНОГО из них временно УХУДШИТ видимую цифру. Не сказанный заранее, этот эффект будет
прочитан как регресс, и первую же верную правку откатят. **accepted** (архитектор).

**Последствия:**
- [сейчас] `07_STATE.md §Статусы задач (Epic 1)` — строка итога: финиш Epic-1 объявлен
  `2026-08-07` (`ADR-140 §1`), критерий `00_CHARTER §критерии успеха` выполнен, счётчик `7/7`.
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — строка `PARITY-REGISTRY` (PARTIAL,
  гейт «построчные сверки пар не проведены») закрывается DONE и уезжает в `07_ARCHIVE.md`
  дословно (`ADR-064`): все семь пар сверены и адъюдицированы, гейт исчерпан.
- [сейчас] `06_INDEX.md` — строка на настоящий ADR тем же коммитом.
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — стейл-гейты шести строк снимаются
  как факт, а не как решение: `SALES-CONSIGNMENT-REVENUE` и `SALES-PERIMETER-EXTEND` (гейт
  `SALES-INGEST-PATCH-DEPLOY` архивирован DONE `2026-08-05`); `FACTS-BACKFILL` (гейт
  `FACTS-WORKFLOW-STOP-DIAG` плюс восстановление — закрыты, `ADR-121`); `DQ-FRESHNESS-COVERAGE`,
  `DQ-GATE-METRIC-REDESIGN`, `DQ-GATE-SCOPE-CONFIRM` (гейт `DQ-GATE-SCOPE-SPLIT-DEPLOY`
  архивирован DONE); `Q-78` (гейт `SALES-DOCUMENT-OWNER-INGEST` закрыт, фактический гейт —
  `SALES-DOCUMENT-OWNER-DEPLOY`).
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` + `07_STATE.md §Мандат Claude Code` —
  заводится `HANDOVER-PACKAGE` (OPEN; класс A, параллель да, мандат постоянный, «Пишет:
  `reference/handover_package_<date>.md`»), парность `ADR-090 §1` соблюдена. Гейт — исполнение
  очереди передачи; сам документ собирается последним, иначе фиксирует состояние, которое ещё
  меняется.
- [задачей HANDOVER-PACKAGE] содержание документа передачи: реестр известных ограничений (§4),
  клиентский runbook «где смотреть», текст риска `R1` (§5).
- [без правок] `00_CHARTER.md` — критерий не переписывается: он выполнен, а не изменён.
  Фаза передачи вводится настоящим ADR, а не правкой конституции.
- [без правок] `_METHOD.md`, `05_CONVENTIONS.md` — решение о фазе и критерии отсечения правил
  метода не требует.

Введено сессией `SALES-JULY-RESIDUAL-ADJ` (2026-08-08, архитектор), применён сборкой буфера
2026-08-08. Механическое применение `ADR-107 §7` (снятие заморозки процессного слоя со статусом
`DEFER` и общим гейтом «сходимость реестра паритета» у четырнадцати строк `Q-35`, `Q-40`, `Q-62`,
`Q-68`, `Q-75`, `Q-76`, `Q-84`, `Q-85`, `Q-94`, `LAUNCH-RUNBOOK`, `CC-READONLY-PRINCIPAL`,
`METHOD-PROMOTE-LOCAL-RULES`, `STATE-DETAILS-BUDGET`, `REGISTRY-COLUMN-SPLIT`) идёт этим же
коммитом сборки как следствие уже принятого `ADR-107 §7`, отдельного решения не требует.

---

## ADR-141 · Гейт `INGEST-DEMAND-APPLICABLE-FILTER` не снят, а перенаправлен: результат `SALES-JULY-DAILY-RECON` исчерпал триггер, но не причину; посылка «спека требует» снята замером; знак эффекта фикса и его второй компонент названы; заводится `SALES-JULY-K2K3-DISCRIMINATE` (класс B, мандат выдан поимённо)

**Контекст.** Строка `INGEST-DEMAND-APPLICABLE-FILTER` (`ADR-138 §4`) гейтилась результатом
`SALES-JULY-DAILY-RECON`. Задача завершена всеми четырьмя пунктами приёмки и архивирована
(`07_ARCHIVE.md:211`), результат частичный: К1 (строки-сироты) подтверждён дважды независимо
(`22 668,01` KGS), остаток `155 456,37` KGS локализован на пяти сутках
(`07-20`/`07-21`/`07-27`/`07-29`/`07-30`, `56,5 %` — на `2026-07-30`), но механизм не различён:
`core.fact_sales_profit` не несёт `applicable`/`moment`/`demand_id`. Адъюдикация гэпа не
отдаётся сессии-исполнителю (`ADR-076 §4`, уточнено `ADR-091 §8`).

**Решение.**

§1 **Гейт НЕ снимается целиком и не остаётся как есть — он перенаправляется.** `ADR-138 §4`
записал в одну фразу триггер («результат `SALES-JULY-DAILY-RECON`») и причину («форма фикса не
назначается до замера: величина вклада неизвестна, добавление фильтра при отрицательном
результате есть правка вслепую»). Триггер сработал, причина жива: вклад фильтра остался в
интервале `[0; 155 456,37]` KGS. Снятие по формальному прочтению триггера санкционировало бы
ровно то действие, которое та же фраза запретила. Оставить текст гейта как есть тоже нельзя:
гейт, указывающий на закрытую задачу, есть стейл-гейт — тот самый класс, шесть экземпляров
которого сняты сплошным проходом сутками ранее (`ADR-140 §последствия`). accepted (архитектор —
фактура и форма; владелец — апрув, чат 2026-08-08, вариант А).

§2 **Посылка «спека требует исключать черновики (`03_PIPELINE_SPEC.md:364`)» снимается как
несостоятельная; `ADR-138 §2(K2)` superseded-in-part в этой клаузе.** Замер: `applicable`
встречается во всей `03_PIPELINE_SPEC.md` ровно один раз — строка `364`, лежащая внутри секции
`## §fact_payments — семантика загрузки (Ghost Records)` (`361`–`378`). Раздела про загрузку
продаж в спеке нет, требования к `entity/demand` нет. Строка, описывающая другую таблицу,
была распространена на ветку продаж — тот же род дефекта, что `ADR-138 §1` нашёл у предыдущего
замера (утверждение, не выдерживаемое собственным источником). **Что остаётся фактом:**
асимметрия загрузчиков (`fetch_returns.py:48`, `fetch_perimeter.py:178/218` несут
`applicable=true`; `fetch_demands.py:72` несёт только `moment`). **Что падает:** «спека требует».
Следствие для веса довода: у фикса остаётся одна фактическая нога вместо двух, причём соседний
фильтр поставлен превентивно по собственному комментарию кода («TD-11: defensive»), то есть
большинство не измеряло поведение отчёта. Строка реестра правится соответственно.
accepted (архитектор).

§3 **Названо то, чего в деле не было: знак эффекта фикса не установлен.** `ADR-138 §2(K2)`
назвал `CONTEXT GAP` («считает ли отчёт «Прибыльность» непроведённые документы — репо не
устанавливает»), но не вывел из него следствия. Следствие таково: при одном исходе фильтр
приближает нашу сторону к эталону, при другом — уводит и создаёт расхождение на клиентской
витрине там, где его сейчас нет. Знак июльского остатка (наша сторона выше) совместим с обоими
кандидатами K2 и K3 и различителем не является — подтверждено исчерпывающим поиском одиночных
и парных документов эталона (`C(243,2) ≈ 29 тыс. комбинаций`, ни одного совпадения на пяти
датах). Поэтому гейт защищает не только от неизвестного размера, но и от неизвестного НАПРАВЛЕНИЯ
правки; второе весомее первого. accepted (архитектор).

§4 **Названо то, чего в деле не было: у фикса есть второй компонент.** У `MERGE`, кладущего
строки в `core.fact_sales_profit`, только ветви `WHEN MATCHED THEN UPDATE SET`
(`reference/code/cf-facts/bq_ops.py:303`, `:515`) и `WHEN NOT MATCHED THEN INSERT` (`:322`, `:531`);
ветки удаления нет, что согласуется с `ADR-100 §1`. Следовательно добавление `applicable=true`
прекратит загрузку черновиков вперёд, но НЕ удалит уже загруженные — июльское число от одного
фильтра не изменится. Форма фикса, когда её будут назначать, обязана нести два компонента:
фильтр в загрузчике и явный `DELETE` по уже лежащим строкам. `ADR-138 §4` этого не называл.
accepted (архитектор).

§5 **Заводится `SALES-JULY-K2K3-DISCRIMINATE` — класс B, только чтение; мандат выдан поимённо
владельцем (чат 2026-08-08).** Основание мандата единственное — живые вызовы к МойСкладу и
доступ к секрету `msklad-token`; задача только читает, не пишет ни в `core.*`/`marts.*`, ни в
staging, ничего не деплоит, откатывать нечего. Форма мандата — прецедент `ADR-139 §1`/`ADR-092 §1`;
шесть оговорок `ADR-076 §5` и форма логирования `ADR-079 §6` обязательны без изменений.
Приёмка дословно, текст запуска обязан процитировать её без пересказа:
«(1) Списочный вызов `GET entity/demand` с фильтром по `moment` за каждые из пяти суток
`2026-07-20`, `2026-07-21`, `2026-07-27`, `2026-07-29`, `2026-07-30` (границы суток — по правилу
`ADR-088`/`ADR-137 §8`, то же окно, что режет нашу сторону), **БЕЗ фильтра `applicable`** — иначе
черновики останутся невидимы, и замер потеряет предмет. По каждому документу записать: `id`,
номер (`name`), `moment`, `applicable`, `sum`, контрагент. Прямой `GET` по идентификатору
невозможен: `core.fact_sales_profit` не несёт `demand_id`, ключ строки необратим (`ADR-104 vi`) —
идентификатор добывается только списочным вызовом (`ADR-139 §2`).
(2) Контроль совпадения популяции: наша сторона за те же пятеро суток из `core.fact_sales_profit`
(фильтр «Оптовая + без канала», как в `SALES-JULY-DAILY-RECON`) обязана воспроизвести суточные
величины Diff из `reference/sales_july_daily_recon_2026-08-08.md` раздел (3). Расхождение есть
СТОП с предъявлением чисел, а не повод продолжить.
(3) Сопоставление с построчным эталоном `reference/oracle_july_2026_employees_2026-08-07.pdf`
(`243` отгрузки; разбор текстового слоя уже лежит в
`reference/_scratch_SALES-JULY-DAILY-RECON_2026-08-08/`, переснимать PDF не нужно, `ADR-043`):
назвать документы, которые есть у нас и отсутствуют в эталоне, и по каждому — `applicable` и `moment`.
(4) Вердикт ЧИСЛАМИ, не словами: сколько из `155 456,37` KGS приходится на непроведённые
документы (K2), сколько — на документы с `moment` вне июля (K3), сколько не объясняется ни тем
ни другим. Отдельным пунктом — ответ на `CONTEXT GAP` `ADR-138 §2`: считает ли отчёт
«Прибыльность» непроведённые документы; устанавливается фактом «документ непроведён И отсутствует
в эталоне» против «документ непроведён И присутствует в эталоне». Если ни один непроведённый
документ на пяти сутках не найден — сказать прямо: K2 опровергнут, и гейт фильтра снимается
без правки.
Границы: только `GET`, ни одной операции записи и ни одного деплоя; код не правится; `date -u` и
`gcloud auth list` первой И последней командой скрипта (`ADR-055`/`ADR-063`); один скрипт на ШАГ,
не на команду (`ADR-077 §6`); `bq query --format=json|prettyjson` без явного `-n`/`--max_rows`
тихо режет вывод до 100 строк (`BQ-QUERY-MAX-ROWS`). Пишет:
`reference/sales_july_k2k3_discriminate_<date>.md`,
`reference/_scratch_SALES-JULY-K2K3-DISCRIMINATE_<date>/`.»
accepted (архитектор — форма и приёмка; владелец — мандат класса B и апрув заведения,
чат 2026-08-08, вариант А).

§6 **`INGEST-DEMAND-APPLICABLE-FILTER` параллельно НЕ стартует; цена обоих вариантов названа.**
Выигрыш параллели — одна сессия (подготовка есть правка одной строки `fetch_demands.py:72` плюс
артефакт). Цена — патч пишется при неустановленном направлении (§3) и при неполной форме (§4);
сверх того готовый патч создаёт давление на деплой: прецедент `SALES-PERIMETER-CHANNEL-DECIDE` →
`ADR-135` показывает, что «правка готова» превращается в поимённый мандат за один ход. Патч
неизвестного направления, лежащий наготове, есть риск на живой клиентской поверхности. Ошибка в
сторону ожидания стоит одной сессии, ошибка в другую сторону — расхождения там, где его нет.
accepted (архитектор — фактура и рекомендация; владелец — выбор варианта А, чат 2026-08-08).

§7 **Квалификация обеих задач по критерию отсечения `ADR-140 §4`, которым владелец объявил фазу
передачи клиенту.** Различитель проходит по первому пункту критерия прямо: витрина продаж
показывает клиенту июльскую выручку на `178 124,42` KGS (`0,159 %`) выше той, что показывает ему
МойСклад, — это неверное число на клиентской поверхности; и если механизм есть K2, то это не
разовая июльская история, а тихий ежемесячный перебор. `INGEST-DEMAND-APPLICABLE-FILTER` проходит
критерий УСЛОВНО — её вход зависит от результата различителя. Тем самым развилка, стоявшая в
`07_STATE §Стенд-ап` («заводить ли новую задачу-discovery на различитель сейчас, или отложить»),
снимается критерием репо, а не возвращается владельцу вопросом (`ADR-056 §язык`: вопрос,
снимаемый чтением репо, не задаётся). accepted (архитектор).

**Последствия (blast radius):**
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — у строки
  `INGEST-DEMAND-APPLICABLE-FILTER` заменяется колонка гейта (§1) и клауза про спеку (§2);
  заводится новая строка `SALES-JULY-K2K3-DISCRIMINATE` (READY) в ОБА файла одним коммитом
  (`ADR-106 §1`).
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — парная строка
  `SALES-JULY-K2K3-DISCRIMINATE` (класс B, параллель нет, мандат ВЫДАН поимённо, поле «Пишет:»
  заполнено) по `ADR-090 §1`.
- [сейчас] `07_STATE.md §Стенд-ап` — замена целиком; `§Подробности для модели` — четыре абзаца
  по `STATE_PATCH` выше.
- [сейчас] `06_INDEX.md` — строка на настоящий ADR тем же коммитом.
- [без правок] `reference/sales_july_daily_recon_2026-08-08.md`,
  `reference/sales_july_residual_split_2026-08-07.md` — датированные артефакты не переписываются
  (`ADR-043`); поправки §2/§3/§4 живут здесь.
- [без правок] `03_PIPELINE_SPEC.md` — §2 НЕ добавляет в спеку раздел про загрузку продаж:
  заведение такого раздела есть правка SEMI-STABLE-дока и требует собственного ADR по существу
  предмета, а не побочным продуктом адъюдикации гейта.
- [без правок] `reference/code/cf-facts/` — эта сессия кода не правит; правка адресована
  `INGEST-DEMAND-APPLICABLE-FILTER` и остаётся гейтованной.
- [без правок] `00_CHARTER.md`, `04_ROADMAP.md` — финиш Epic-1 объявлен (`ADR-140`), июльский
  остаток вне зоны паритета (`ADR-115 §11`) и финиш не гейтит.
- [задачей `SALES-JULY-K2K3-DISCRIMINATE`] вклад K2 и K3 числами, ответ по знаку эффекта.
- [задачей `INGEST-DEMAND-APPLICABLE-FILTER`] форма фикса из двух компонентов (§4), после снятия
  гейта.

Статус: §1 accepted (архитектор — фактура и форма; владелец — апрув, чат 2026-08-08, вариант А) ·
§2-§4 accepted (архитектор) · §5 accepted (архитектор — форма и приёмка; владелец — мандат
класса B выдан поимённо, чат 2026-08-08, вариант А) · §6 accepted (архитектор — фактура;
владелец — выбор варианта) · §7 accepted (архитектор).

Введён сессией `SALES-JULY-K-DISCRIMINATE-ADJ` (2026-08-08, архитектор, класс A, без брифа —
`ADR-086 §1`), применён сборкой буфера 2026-08-08 (четырнадцатый проход).

---

## ADR-142 · Очередь до финиша Epic-1 — фазы передачи клиенту: зафиксирована артефактом `26` задач в пяти уровнях по критерию отсечения `ADR-140 §4`, `9` из них блокируют передачу; назван единственный жёсткий срок очереди (`2026-09-29`); заводится `INFRA-FACTS-LANDING`

**Контекст.** `ADR-140` объявил финиш Epic-1 (реестр паритета `7/7`, дата `2026-08-07`), ввёл
ВТОРОЙ критерий «регламент пересъёма работает» (`§3`) и фазу передачи клиенту с критерием
отсечения задач (`§4`). Сам критерий был введён, но НИ ОДНА открытая строка реестра по нему не
квалифицирована — то есть инструмент отсечения существовал, а результат его применения нигде не
записан. Владелец запросил счёт: сколько задач отделяет проект от передачи клиенту без
техдолга, который вернёт клиента через недели.

**Решение.**

§1 **Очередь фиксируется артефактом `reference/handover_queue_2026-08-08.md`, а не текстом
решения.** Список из `26` задач с ID для тегирования, наличием брифа, гейтами и однострочной
сутью каждой живёт в `/reference` по общему правилу («полный ход — в артефакт, в `07_STATE` —
следствие плюс ссылка», `ADR-105 §1`). Основание отдельного артефакта, а не абзаца деталей:
список длиннее любого разумного абзаца, обязан читаться целиком перед планированием и
переживает несколько сборок без переписывания. accepted (архитектор).

§2 **Каждая открытая строка реестра квалифицирована по `ADR-140 §4`; результат — пять уровней.**
Уровень 1 (`9` задач) — неверное число на клиентской поверхности либо тихий отказ: блокирует
передачу. Уровень 2 (`3`) — второй критерий финиша `ADR-140 §3`. Уровень 3 (`3`) — июльский
хвост. Уровень 4 (`8`) — тихие мины и гигиена данных. Уровень 5 (`3`) — документы передачи.
Итого `26`. Вне передачи — ≈`30` строк, по каждой корзине названа причина невыполнения критерия.
Статусы, гейты и классы взяты из реестра дословно: настоящий ADR НЕ переоценивает ни одной
строки, он их СОРТИРУЕТ. accepted (архитектор).

§3 **Назван единственный жёсткий срок очереди: `2026-09-29`.** `WEEKLY_WINDOW_DAYS = 90`
(`reference/code/cf-facts/config.py`) выпускает июль-2026 из окна пересборки; после этой даты
правка, обязанная изменить ИЮЛЬСКИЕ числа задним числом, штатным путём не применима. Факт был
записан при одной строке (`SALES-INGEST-PATCH-DEPLOY`, `07_ARCHIVE.md:204`), а касается двух —
`SALES-DOCUMENT-OWNER-DEPLOY` и будущего фикса `INGEST-DEMAND-APPLICABLE-FILTER`. Поднят на
уровень очереди, чтобы не был потерян вместе с архивированной строкой. accepted (архитектор).

§4 **Заводится `INFRA-FACTS-LANDING`, сводящая `Q-11`/`Q-12`/`Q-13`.** Критерий `ADR-140 §4`
выполнен по пункту (iii): три узла живой инфраструктуры (бизнес-метрики, служба алертов, состав
недельного прогона) не описаны в живых доках, притом что факты под них добыты
(`reference/infra_facts_sweep_2026-08-01.md`) и лежат неперенесёнными с `2026-08-01` — причина
названа там же: та сессия была класса A и писала только в `/reference`. Три отдельных захода за
одним и тем же артефактом есть трижды оплаченный вход в контекст. accepted (архитектор).

§5 **Три строки объявлены подлежащими переквалификации, а не исполнению:**
`SALES-CONSIGNMENT-REVENUE`, `SALES-PERIMETER-EXTEND`, `Q-78`. Их гейты сняты фактом проходом
`2026-08-08`, но статусы тем проходом намеренно не переоценивались
(`ADR-140 §последствия`: «снят только устаревший текст гейта»). Держать их в очереди как работу
до проверки — значит считать сделанное несделанным. accepted (архитектор).

§6 **Рекомендация к немедленному запуску (выбор за владельцем, не решение архитектора):**
`SALES-JULY-K2K3-DISCRIMINATE` (мандат выдан, приёмка написана), `DQ-ALERT-FILTER-FIX` (бриф
готов, нужен мандат класса B — самая дешёвая из девяти блокирующих),
`SALES-DOCUMENT-OWNER-DEPLOY` (патч готов, прямая претензия клиента, единственный жёсткий срок).
Проверка совместимости обязательна перед любым одновременным запуском (`ADR-082 §1`/`ADR-102`);
две последние — класс B и не параллелятся по построению. **proposed** — ждёт выбора владельца.

**Последствия (blast radius):**
- [отдельным коммитом] `reference/handover_queue_2026-08-08.md` — НОВЫЙ артефакт; в пятёрку
  файлов сборки не входит и коммитится САМОЙ сессией поимённо (`ADR-081 §8`), уже лежит в
  репозитории на момент сборки — сборщику проверять только наличие файла, не создавать его.
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — заводится строка
  `INFRA-FACTS-LANDING` (READY) в ОБА файла одним коммитом (`ADR-106 §1`); у строк `Q-11`,
  `Q-12`, `Q-13` заменяется колонка гейта на «закрывается задачей `INFRA-FACTS-LANDING`».
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — парная строка `INFRA-FACTS-LANDING`
  (`ADR-090 §1`).
- [сейчас] `07_STATE.md §Стенд-ап` — замена целиком (стенд-ап настоящего блока побеждает по
  `_ASSEMBLER.md §5` пункт 3); `§Подробности для модели` — три абзаца по `STATE_PATCH` выше.
- [сейчас] `06_INDEX.md` — строка на настоящий ADR тем же коммитом.
- [без правок] `00_CHARTER.md`, `04_ROADMAP.md` — очередь не меняет ни финишную черту, ни
  критерии успеха: `ADR-140` уже ввёл и второй критерий, и фазу передачи, настоящий ADR лишь
  применяет их к строкам реестра.
- [без правок] статусы всех перечисленных строк — §2 сортирует, не переоценивает.
- [задачей `INFRA-FACTS-LANDING`] перенос трёх групп фактов в `01_ARCHITECTURE.md`/
  `11_INFRA_FACTS.md`, закрытие `Q-11`/`Q-12`/`Q-13`.
- [задачей `HANDOVER-PACKAGE`] сам пакет передачи, собирается последним по очереди §2.

Статус: §1-§5 accepted (архитектор) · §6 **proposed** (рекомендация; выбор первых задач и два
мандата класса B — за владельцем).

Введён сессией `HANDOVER-QUEUE-ADJ` (2026-08-08, архитектор, второй ход сессии
`SALES-JULY-K-DISCRIMINATE-ADJ`, класс A, без брифа — `ADR-086 §1`), применён сборкой буфера
2026-08-08 (четырнадцатый проход).

## ADR-143: Гейт `INGEST-DEMAND-APPLICABLE-FILTER` закрыт без правки по выполненному предусловию; остаток июля разложен на два подслучая на данных, уже лежащих в репо; `DEFER` отклонён ложностью его посылки — снимок источника на момент загрузки существует; заводится `SALES-JULY-STALE-COHORT`

**Контекст.** `SALES-JULY-K2K3-DISCRIMINATE` исполнена (`ADR-141 §5`, артефакт
`reference/sales_july_k2k3_discriminate_2026-08-08.md`, строка архивирована DONE сборкой буфера
2026-08-08, пятнадцатый проход). Живой `GET entity/demand` без фильтра `applicable` по пяти
суткам вернул `69` документов, все `applicable=true`; все `69` найдены в построчном эталоне;
контроль совпадения популяции с `core.fact_sales_profit` воспроизвёлся точно. Сессия-исполнитель
адъюдикацию не производит (`ADR-076 §4`, уточнено `ADR-091 §8`) и передала архитектору два
пункта: формальное закрытие гейта и судьбу K3.

**Решение.**

§1 **`INGEST-DEMAND-APPLICABLE-FILTER` закрывается БЕЗ правки кода — предусловие выполнено
дословно.** `ADR-141 §5(4)` записал условие ДО замера и получил апрув владельца: «Если ни один
непроведённый документ на пяти сутках не найден — сказать прямо: K2 опровергнут, и гейт фильтра
снимается без правки». Найдено `0` из `69`. Условие, написанное до замера, исполняется, а не
пересматривается задним числом под новый вкус — иначе критерий приёмки перестаёт быть критерием
(`05 Часть I ★ Критерий приёмки выводится из формулы`). Правка `fetch_demands.py` не
производится, деплой `cf-facts` по этому основанию не запрашивается, обе парные строки мандата
(подготовка и деплой) уходят вместе со строкой. **accepted** (архитектор).

§2 **Охват закрытия назван, чтобы его нельзя было расширить молча.** Опровергнуто: НА
ИЗМЕРЕННОЙ ПОПУЛЯЦИИ (`69` документов, пять суток, `5` из `31` и `69` из `243` июльских
отгрузок) непроведённых документов нет, поэтому фильтр не исключил бы там ни одной строки —
вклад K2 равен `0,00` KGS. НЕ утверждается «черновиков в `entity/demand` не бывает»: из этой
выборки такое не следует (`_METHOD §6`). **Триггер переоткрытия:** появление хотя бы одного
документа `entity/demand` с `applicable=false` в любом последующем живом замере (в частности в
помесячном пересъёме — `ORACLE-AUTOMATION`, `SALES-PERIMETER-PARITY-RECHECK`); вместе с ним
возвращается и вопрос `ADR-138 §2` о поведении отчёта «Прибыльность» на непроведённых
документах, который остаётся неустановленным по существу и больше ничего не гейтит.
**accepted** (архитектор).

§3 **Асимметрия загрузчиков не исчезает и адресуется документу передачи, а не реестру.** Факт
(`fetch_returns.py:48`, `fetch_perimeter.py:178/218` несут `applicable=true`,
`fetch_demands.py:72` не несёт) критерий отсечения `ADR-140 §4` не проходит: неверного числа на
клиентской поверхности он не даёт (вклад измерен, `0,00`), тихого отказа не даёт,
неремонтопригодности не создаёт. `ADR-140 §4` для этого случая прямо предписывает форму —
строка «не выбрасывается: она переезжает в документ „известные ограничения“». Адресат —
`HANDOVER-PACKAGE`. **accepted** (архитектор).

§4 **Механизм остатка `155 456,37` KGS назван и разложен; K3 сужен, а не признан неизмеримым.**
Из снапшота кода следует, чего в репо не было записано одной фразой: строка с
`transaction_date = D` обновляется только прогонами `R ∈ [D, D+7]` (окно выборки
`main.py:135-136` при `HOURLY_WINDOW_DAYS = 7` и тот же предикат в `ON` у `MERGE`), `_loaded_at`
обновляется при совпадении и потому показывает ПОСЛЕДНЕЕ попадание строки в ответ источника, а
ветки удаления нет (`ADR-100 §1`). Значит после `D+7` строка заморожена ЦЕЛИКОМ — не только
`transaction_date`, как записано в `ADR-100 §2`, — до следующего 90-суточного пересбора, а
исчезнувшая строка не удаляется никогда, в том числе пересбором. Разложение по когортам
`_loaded_at`, полученное из двух агрегатных таблиц `sales_july_daily_recon_2026-08-08.md`:
подслучай **A** (строка пропала из источника) — сутки `07-21` (`8` строк / `27 019,20` KGS,
когорта `2026-07-22`) и `07-30` (`3` строки / `100 602,00` KGS, когорта `2026-08-01`), итого
`127 621,20` KGS (`82,1 %`); подслучай **B** (источник изменился после заморозки) — сутки
`07-20`/`07-27`/`07-29`, итого `27 835,20` KGS (`17,9 %`). Модель проверена на уже подтверждённой
части: когорты `C(07-09)` и `C(07-15)` дают ровно известные K1-сироты (`8` строк /
`22 668,0108` KGS). Подслучай A есть тот же дефект, что описан `ADR-100 §1` и записан
конвенцией `05 Часть II C2`, измеренный на втором месяце; критерий `ADR-138` его не видел по
построению, поскольку все пять суток лежат ПОСЛЕ `2026-07-19` — то есть утверждение
`sales_july_daily_recon_2026-08-08.md` §(4) «пять оставшихся суток НЕ являются сиротами»
БЕЗОСНОВАТЕЛЬНО (не опровергнуто и не подтверждено); артефакт не переписывается (`ADR-043`),
поправка живёт здесь и в `reference/sales_july_k2k3_adj_2026-08-08.md`. Разложение есть
ГИПОТЕЗА с точным арифметическим подтверждением, а не измерение (`05 Часть I ★ Успех
инструмента ≠ факт`); её фальсифицируемое предсказание записано ДО замера
(`sales_july_k2k3_adj_2026-08-08.md §2.7`). **accepted** (архитектор).

§5 **Заводится `SALES-JULY-STALE-COHORT` — класс A, гейтов нет, мандата не требует, без брифа
(`ADR-086 §1`).** Критерий отсечения `ADR-140 §4` проходит по (i) и (ii): июльская выручка на
клиентской витрине завышена, и `82,1 %` завышения есть строки, которые не уберёт ни один
штатный прогон. Приёмка дословно, текст запуска обязан процитировать её без пересказа:
«(0) `gcloud storage ls -l gs://msklad-raw-msklad-bi-prod/demand/incremental/` с печатью
листинга и дат создания; назвать, какие объекты покрывают прогоны `2026-07-22`, `2026-08-01`,
`2026-08-06`, и есть ли объекты старше `2026-07-19`. Отсутствие июльских объектов есть законный
РЕЗУЛЬТАТ, а не провал: печатается как есть, шаг 2 отменяется, шаги 1 и 3 исполняются. Пустая
выдача при `rc=0` фактом не является (`05 Часть I ★ Успех инструмента ≠ факт`).
(1) Один `bq query` (текст дословно — `reference/sales_july_k2k3_adj_2026-08-08.md §2.7`,
явный `-n`, `BQ-QUERY-MAX-ROWS`): разрез пяти суток по `DATE(_loaded_at)`. Построчно сравнить с
предсказанной таблицей §2.7 и назвать КАЖДОЕ расхождение числом. Расхождение опровергает модель
`§2.2` и есть результат, а не сбой: тогда разложение `127 621,20 / 27 835,20` снимается и
вопрос возвращается архитектору с новыми числами.
(1b) Той же выдачей — БЕСПЛАТНО, отдельного запроса не требует — ответить на вопрос «прошёл ли
90-суточный пересбор после `2026-07-19`»: пересбор обновил бы `_loaded_at` у ВСЕХ пяти суток
одним значением. Если все пять суток несут одну свежую дату `_loaded_at` — пересбор прошёл,
предсказание §2.7 отменяется этим фактом, а не ошибкой, и подслучай B считается излеченным;
назвать дату. Если нет — назвать прямо: «90-суточного пересбора между `2026-07-19` и <дата
замера> не было», и это есть вход задачи `SALES-DOCUMENT-OWNER-DEPLOY` (её первый прогон —
`weekly`, `window_days=90`) со сроком `2026-09-29`. Новая строка реестра по этому поводу НЕ
заводится без отрицательного результата (`ADR-140 §4`).
(2) Только при положительном шаге 0: скачать в scratch объекты прогонов, покрывающих пять суток;
вычислить `TO_HEX(MD5(CONCAT(demand_id,'|',position_id)))` по записям архива и сопоставить с
`transaction_id` одиннадцати строк подслучая A (сами `transaction_id` берутся отдельным
read-only запросом к `core.fact_sales_profit`). По каждой строке сказать ровно одно из трёх:
(а) `demand_id` присутствует в БОЛЕЕ ПОЗДНЕМ архиве с ДРУГИМ `transaction_date_raw` — `moment`
переехал, K3 подтверждён поимённо; (б) `demand_id` присутствует в более позднем архиве, а
`position_id` — нет: удалена позиция; (в) `demand_id` в более поздних архивах отсутствует
вовсе — документ удалён либо `moment` уехал за окно запроса, различается сверкой с живым
списком `69` документов (`reference/sales_july_k2k3_discriminate_2026-08-08.md §1`).
(3) Вердикт ЧИСЛАМИ: сколько из `127 621,20` KGS приходится на каждый из трёх исходов и сколько
осталось неразличённым; отдельной строкой — подтвердился ли подслучай B величиной `27 835,20`
KGS как остаток.
Границы: живых вызовов к МойСкладу НЕТ; в `core.*`/`marts.*` и в staging не пишет; код не
правится; `date -u` и `gcloud auth list` первой И последней командой каждого скрипта
(`ADR-055`/`ADR-063`); один скрипт на ШАГ, не на команду (`ADR-077 §6`); диагностика не убирает
за собой, путь печатается последней строкой (`ADR-043`). Пишет:
`reference/sales_july_stale_cohort_<date>.md`, `reference/_scratch_SALES-JULY-STALE-COHORT_<date>/`.»
**accepted** (архитектор).

§6 **Варианты (a) и (b) вопроса владельца отклонены, цена каждого названа.** (a) новый
discovery-бриф под другой метод различения — избыточен: метод искать не нужно, он найден и лежит
в репо (`cf-facts` кладёт сырой NDJSON каждого прогона в GCS, `main.py:151-152`/`:191-192`,
запись несёт `demand_id`/`position_id`/`transaction_date_raw`, `fetch_demands.py:153-170`);
задача класса A с дословной приёмкой брифа не требует (`ADR-086 §1`, прецеденты
`SALES-JULY-DAILY-RECON`, `SALES-ORPHAN-CONFIRM`). (b) `DEFER` остатка с причиной «метод
исчерпан дважды, K3 не измерим текущими средствами» — отклонён ЛОЖНОСТЬЮ ПОСЫЛКИ, а не
предпочтением: посылка «нужна история `moment` либо снимок на момент первой загрузки, ни того
ни другого нет» неверна — снимок пишется каждым прогоном; оба прежних захода смотрели на
сторону источника и на агрегаты ядра, ни один не смотрел на `_loaded_at` в разрезе суток и на
GCS-архив. Цена ошибки в эту сторону выше остальных: `DEFER` выглядит как решение и снимает
вопрос с повестки, оставляя `127 621,20` KGS необъяснённых денег на клиентской поверхности.
**accepted** (архитектор).

§7 **Побочный факт, установленный тем же разложением: 90-суточного пересбора июля не было.**
Недельный режим обновил бы `_loaded_at` у ВСЕХ июльских строк одним значением; в таблице (1)
`sales_july_daily_recon_2026-08-08.md` они распределены по `20` датам, массовой когорты после
`2026-07-19` нет — факт выводится из отсутствия когорты, а не из отсутствия наблюдения.
Следствия: подслучай B (`27 835,20` KGS) самоизлечим ближайшим 90-суточным пересбором, если тот
пройдёт до `2026-09-29` (после этой даты июль выходит из окна `WEEKLY_WINDOW_DAYS = 90`);
подслучай A (`127 621,20` KGS) не самоизлечим никогда — нужен явный `DELETE` по образцу второй
половины фикса `ADR-141 §4`. Форма этого `DELETE` здесь НЕ назначается (`_METHOD §6`): состав
строк ещё не измерен, назначение формы до шага 1 есть правка вслепую — тот же довод, которым
`ADR-138 §4` гейтил фильтр черновиков. **accepted** (архитектор).

§8 **Новая строка реестра под сам механизм НЕ заводится, и это ограничение соблюдается
намеренно.** Механизм покрыт двумя уже существующими носителями: `SALES-REFRESH-WINDOW`
(«окно обновления не покрывает якорь паритета, режима досева нет», уровень 2 очереди передачи)
и конвенция `05 Часть II C2`. Настоящий разбор даёт им измеренное число вместо общих слов.
`ADR-140 §4` ограничивает заведение новых строк до передачи — заводится ровно одна (§5), и она
проходит критерий. **accepted** (архитектор).

§9 **Финиш Epic-1 не пересматривается.** Зона паритета есть датированный снимок мая-2026
(`ADR-140 §2(i)`, `ADR-115 §11`); майские сироты того же класса уже входят в правило-мост пары
«Продажи» разложением `Δ = M1 − |M2| + ε` (`ADR-119`). Настоящее решение про июль, который вне
зоны паритета, и про ВТОРОЙ критерий (`ADR-140 §3`, «регламент пересъёма работает»), а не про
первый. **accepted** (архитектор).

§10 **Фикс подслучая A новой строки НЕ требует — у него уже есть дом, но ЦЕНА выбранного в нём
варианта изменилась и подлежит переопределению владельцем после замера.** Дефект «у `MERGE` нет
ветки удаления» есть пункт (2) из трёх дефектов, объединённых в `SALES-REFRESH-WINDOW`
(`ADR-100 §7`, объединение принято владельцем 2026-08-01); подготовка закрыта DONE, три варианта
патча написаны, бриф собран (`briefs/SALES-REFRESH-WINDOW.md`). Владелец выбрал `ADR-115 §7`
ВАРИАНТ 3 — плашка «датированный снимок» без правки кода, вариант 1 (настоящий фикс) отнесён на
после `CODE-REPO-STANDUP` шага 2b. Выбор делался, когда известная цена бездействия равнялась
семи майским строкам `1 630 240,60` KGS, уже входящим в правило-мост пары «Продажи»
(`ADR-119`), то есть цена лежала ВНУТРИ объяснённого. Настоящий разбор добавляет к ней
одиннадцать июльских строк `127 621,20` KGS, лежащих НА клиентской витрине и не входящих ни в
какое правило, плюс механизм ежемесячного повторения. Это переоценка цены уже принятого решения,
а не новое решение и не новая задача: `SALES-REFRESH-WINDOW` возвращается владельцу вопросом
«остаётся ли вариант 3» по факту шага 1 `SALES-JULY-STALE-COHORT`, а не сейчас — до замера
переоценка опиралась бы на гипотезу (§4). **accepted** (архитектор).

**Последствия (blast radius):**
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — строка
  `INGEST-DEMAND-APPLICABLE-FILTER` закрывается DONE и переносится ДОСЛОВНО в `07_ARCHIVE.md`
  (`ADR-064`), тем же коммитом снимается её строка индекса; полный текст в архиве дополняется
  вердиктом §1/§2 и триггером переоткрытия §2.
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — убираются ОБЕ парные строки
  `INGEST-DEMAND-APPLICABLE-FILTER, подготовка` и `INGEST-DEMAND-APPLICABLE-FILTER, деплой`
  (`ADR-090 §1`).
- [сейчас] `07_STATE.md §Открытые вопросы` + `07_GAPS.md` — заводится строка
  `SALES-JULY-STALE-COHORT` (READY) в ОБА файла одним коммитом (`ADR-106 §1`); полный текст —
  §5 дословно.
- [сейчас] `07_STATE.md §Мандат Claude Code: класс задач` — парная строка
  `SALES-JULY-STALE-COHORT`: класс A, параллель да, мандат постоянный, «Пишет:
  `reference/sales_july_stale_cohort_<date>.md`,
  `reference/_scratch_SALES-JULY-STALE-COHORT_<date>/`» (`ADR-090 §1`).
- [сейчас] `07_STATE.md §Стенд-ап` — замена целиком; `§Подробности для модели` — шесть абзацев
  по `STATE_PATCH` выше.
- [сейчас] `06_INDEX.md` — строка на настоящий ADR тем же коммитом.
- [задачей `SALES-JULY-STALE-COHORT`] проверка разложения §4 запросом и различение трёх исходов
  подслучая A по GCS-архиву.
- [задачей `HANDOVER-PACKAGE`] строка «известных ограничений» об асимметрии фильтра `applicable`
  в загрузчиках (§3) и о заморозке строки после `D+7` с невозвратностью исчезнувших строк (§4/§7).
- [без правок] `reference/sales_july_daily_recon_2026-08-08.md`,
  `reference/sales_july_k2k3_discriminate_2026-08-08.md`,
  `reference/handover_queue_2026-08-08.md` — датированные артефакты не переписываются
  (`ADR-043`); поправка §4 живёт в настоящем ADR и в `reference/sales_july_k2k3_adj_2026-08-08.md`.
- [без правок] `reference/code/cf-facts/` — эта сессия кода не правит; §1 отменяет правку
  фильтра, §7 форму `DELETE` не назначает.
- [без правок] `03_PIPELINE_SPEC.md` — раздел про загрузку продаж в спеку не заводится
  (основание неизменно: `ADR-141 §последствия`, правка SEMI-STABLE-дока требует собственного ADR
  по существу предмета).
- [без правок] `00_CHARTER.md`, `04_ROADMAP.md`, `05_CONVENTIONS.md` — §9; `C2` уже несёт
  правило, новая конвенция не предлагается.
- [без правок] `SALES-REFRESH-WINDOW`, `FACTS-BACKFILL` — строки не переоцениваются этой
  сессией; §7/§8 дают им фактуру, статус меняет их собственная работа.
- [без правок] `SALES-REFRESH-WINDOW` — строка не переоценивается этой сессией (§10): вопрос
  «остаётся ли вариант 3» возвращается владельцу по факту шага 1 `SALES-JULY-STALE-COHORT`.

Статус: §1-§10 accepted (архитектор).

Введён сессией `SALES-JULY-K2K3-ADJ` (2026-08-08, архитектор, класс A, без брифа —
`ADR-086 §1`), применён сборкой буфера 2026-08-08 (шестнадцатый проход).

--- END 06_DECISIONS_LOG.md ---
