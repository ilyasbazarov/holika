# 01 · ARCHITECTURE — Топология и порядок прогона

**Версия:** 0.1 (скелет, M-P3a) · **Статус:** STABLE
**Назначение:** топология слоёв МойСклад → GCS/BQ (`stg`/`core`/`marts`) → Looker Studio; DAG и последовательность прогона; слой потребителей.
Секции — скелет: заголовок + указатель трассировки. Прод-наполнение прозой — P4.

---

## §топология

<!-- P4: из RB-03 (ASCII-схема, первичный источник топологии) + PR-07 колонка «ответственность». ⚠ GAP Q-12: cf-alert есть на схеме RB-03, отсутствует в таблице CF PR-01/§1 — не примирять, discovery отдельно. -->

## §DAG — последовательность прогона

<!-- P4: из PR-20 (порядок шагов msklad-pipeline-hourly, расписание) + RB-19 (маппинг raise_*-шагов, имена шагов workflow). -->

## §потребители — слой Looker Studio

<!-- P4: из PR-39 (страницы Looker Studio ↔ источники) + PR-30 (назначение страниц LS для мартов). -->
