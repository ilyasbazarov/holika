# FILE: 07_ARCHIVE.md

# 07_ARCHIVE · Архив закрытых строк STATE (ADR-064)

**Статус:** LIVING (append при закрытии строк в `07_STATE.md`).
**Назначение:** полный текст закрытых Q/задач/блокеров и исторических подстрок задач. НЕ входит в
обязательный контекст ролей; поднимается точечно по ID. Решения-первоисточники — в `06_DECISIONS_LOG.md`.
**Перенесено сессией PIPELINE-AUDIT 2026-07-27 из `07_STATE.md` @ SHA 9350263 — дословно, без правок.**

---

## Epic M — построчные статусы подзадач M-P4 (все DONE)

| Задача | Название | Статус |
|---|---|---|
| ↳ M-P4-11a | Скаффолд `11_INFRA_FACTS.md` + перенос 11a | DONE (см. сессию M-P4-B-11; была BLOCKED — нет 11) |
| ↳ M-P4-01a | 01 §топология | DONE (была READY; см. сессию M-P4-A-01) |
| ↳ M-P4-01b | 01 §DAG | DONE (была READY; см. сессию M-P4-A-01) |
| ↳ M-P4-01c | 01 §потребители (LS) | DONE (была READY; см. сессию M-P4-A-01) |
| ↳ M-P4-01d | 01 §топология — узел `audit` | DONE (была READY; см. сессию M-P4-A-01) |
| ↳ M-P4-02a | 02 §схемы stg | DONE (была READY; см. сессию M-P4-A-02) |
| ↳ M-P4-02b | 02 §схемы core | DONE (была READY; см. сессию M-P4-A-02; PR-27 не перенесён — вне scope брифа, см. 04_ROADMAP «Отклонения от плана») |
| ↳ M-P4-02c | 02 §схемы dim | DONE (была READY; см. сессию M-P4-A-02) |
| ↳ M-P4-02d | 02 §оракул | DONE (наполнена в рамках M-P3c, ADR-006; отдельного M-P4-брифа не было по дизайну — явно исключена из scope брифа M-P4-A-02: «НЕ в scope (уже готово): §семантика П&Л, §оракул (02d)») |
| ↳ M-P4-02e | 02 §справочные-данные (UUID) | DONE (была READY; см. сессию M-P4-A-02) |
| ↳ M-P4-02f | 02 §валюты | DONE (была READY; см. сессию M-P4-A-02) |
| ↳ M-P4-02g | 02 §поведение API | DONE (была READY; см. сессию M-P4-A-02) |
| ↳ M-P4-02h | 02 §семантика данных | DONE (была READY; см. сессию M-P4-A-02) |
| ↳ M-P4-03a | 03 §режимы cf-facts | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, PR-09+PR-17) |
| ↳ M-P4-03b | 03 §cf-finance | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, PR-14+PR-13+PR-35п41; конфиг → 11 §CF) |
| ↳ M-P4-03c | 03 §cf-fx | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, PR-18; URL/секрет → 11 §секреты, Q-7 не в прозе) |
| ↳ M-P4-03d | 03 §DQ — чеки и пороги | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, PR-31+PR-19+PR-34п25+ADR-007; 3 порога в одной таблице, не сведены) |
| ↳ M-P4-03e1 | 03 §marts — описание/пороги + SQL in_transit | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, PR-29+PR-30; SQL in_transit дословно) |
| ↳ M-P4-03e2-legacy | 03 §marts — легаси SQL fact_payments-марта (AS-IS) | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`; AS-IS/legacy-пометка + кросс-ссылка на TO-BE ADR-006 подтверждены) |
| ↳ M-P4-03e2-sq | 03 §marts — канонический SQL ~11 SQ | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`; 8 март-SQ, Q-5 CLOSED, in_transit не задублирован, 3 audit-SQ корректно в 11 §SQ) |
| ↳ M-P4-03f | 03 §fact_payments (Ghost Records) | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, PR-27+PR-34п26; счётчики внесены этим же патчем) |
| ↳ M-P4-03g | 03 §операционные инварианты | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, PR-33 п.18–20) |
| ↳ M-P4-03h | 03 §инварианты данных | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, RB-20+RB-30+RB-22) |
| ↳ M-P4-03i | 03 §конвертация валют | DONE (см. сессию M-P4-A-03, верифицировано @ SHA `46c1340`, RB-38 п.26.6) |
| ↳ M-P4-09a | 09 §конвертация валют вх/исх | DONE (см. сессию M-P4-A-09, трассировка RB-38 §26.6; сверено с 03i, расхождений нет) |

---

## Закрытые вопросы / GAP-реестр (полный текст)

| ID | Вопрос | Тип | Как закрывается |
|---|---|---|---|
| Q-1 | Точный состав эталонных цифр ERP (что именно = ground-truth приёмки) | референс-артефакт | M-P3b DONE (ADR-005 accepted): seed #1 П&Л май-2026 посеян в `/reference/pnl_2026-05.md`; граница оракула = line-level МойСклада (ORACLE), exact-match по цифрам; RECON+иные периоды → P5 |
| Q-2 | Что в источнике — DROP по определению (устаревшее/разовое/секреты) | решение | адъюдицировано в P2: 28 ед. (9 DROP + 19 DROP-DUP) |
| Q-5 | Канонические SQL мартов: только `in_transit` в доках; остальные ~11 SQ — только в живых transferConfigs | референс-артефакт | **CLOSED**: референс `/reference/sql/*` + README посеяны 2026-07-07, трассируемо (D5 done @ `805450a`); цепочка D5 → 03e2-sq пройдена → 03e2-sq разблокирован |
| Q-10 | Полный список `EXCLUDE_EXPENSE_IDS` (в доках — 4 UUID, канон — в коде) | факт | закрывается выгрузкой кода (Q-3) |
| Q-15 | Легаси `sq_marts_expenses` (из `fact_payments`, ~33% разрыв) vs. ADR-006 raw-март — что канон для марта расходов | решение | **CLOSED (ADR-012, supersedes ADR-009):** канон = `sq_marts_expenses` (fact_payments-based) — прод-витрина НЕ ретайрится, а ПРАВИТСЯ точечно (добавление `loss`+`commissionreportin` по ADR-006 там, где расхождение подтверждено); TO-BE raw-март ADR-006 не строится |
| Q-16 | Фильтрация статей по ИМЕНИ (ADR-006) vs. UUID (Q-9/Q-10) — безопасно ли стартовать по имени | решение | **CLOSED (ADR-009):** старт по имени (валидирован оракулом 0.00 diff 2026-07-06); UUID-хардинг — future defense-in-depth, не блокер Epic-1 |
| Q-17 | Undocumented `sq_audit_dim_*_snapshot` — где живут / нужна ли доменная спека | решение | **CLOSED (ADR-008):** инструментация; дом = `11`/`/reference`; НЕ заводить `§audit` в `03`/`09`; промоушен в consumer-facing спеку — по требованию (новый ADR) |
| Q-18 | Совпадает ли то, что реально питает страницу «Расходы» (прод), с методологией ADR-006? | решение | **CLOSED (ADR-012):** заведён и закрыт в одну сессию (M-P5-A-01 v3), факт из `/reference/sql/sq_marts_expenses.sql` @ снапшот 2026-07-07 — прод = `marts.expenses` ← `sq_marts_expenses` (fact_payments-based, ADR-006 raw-март НЕ реализован); TO-BE raw-март не строится; Epic-1 = верификация+правки |
| Q-19 | Реально ли `sq_marts_sales_overview` (Config `69ff34b4-0000-2b2b-a390-14c14ef7af10`) применяет входящую конвертацию по `document.rate.value` (канон ADR-010), или в цепочке подмешан курс Bakai/`dim_fx_rates` (канонически зарезервирован под исходящую конвертацию KGS→USD для инвестора) — то есть смешаны входящая и исходящая конвертации на sales-стороне. Владелец не помнит точно, какой курс использовался при историческом фиксе sales_overview (была системная недооценка валютных сумм из-за `÷100` без `×rate`, аналог TD-RECON-01 на expense-стороне) | факт | живая выгрузка `/reference/sql/sq_marts_sales_overview.sql` + сверка формулы конвертации против `document.rate.value` (ADR-010). Приор повышен ADR-016 §2 — тот же класс (÷100 без ×rate) на sales-стороне; рекомендован к приоритизации вместе с Q-30. Промоушен «факт, когда актуально» → СТРУКТУРНЫЙ пре-cutover гейт `E1-T3-MECH-FX`: снять `sq_marts_sales_overview.sql`; определить, применяет ли sales свою конвертацию (компенсирует ядро) или доверяет `core.sum_kgs` — если компенсирует, core-фикс двойным-исправит sales → cutover обязан учесть. Квантификация — НЕ в этом гейте. **Структурная половина — CAPTURED (сессия E1-T3-D-CFSRC, 2026-07-14):** конвертер cf-finance локален (inline в `main.py::run_etl()`), не shared-модуль; blast radius бага ограничен `core.fact_payments` и тем, что от него зависит. Магнитудный замер — остаётся DEFER. **CLOSED (сессия E1-T3-MECH-FX, 2026-07-14):** структурный гейт снят фактом — `sq_marts_sales_overview.sql` снят полностью (128 строк, завершающий `SELECT * FROM main` подтверждён), `core.fact_payments` нигде не упоминается; единственные `sum_kgs` — колонка `core.fact_returns`, не связана с cf-finance/converter; двойная коррекция исключена фактом. Снятый SQL закоммичен в `/reference/sql/` этой сессией |
| Q-22 | «Вывод прибыли» — дашборд-категория или движение капитала | решение | **CLOSED (ADR-013 §1):** движение капитала; блок «Капитал/распределение», вне opex-тотала |
| Q-23 | 5 понижённых статей — причина занижения | факт | **CLOSED:** декомпозиция закрыта количественно, captured `/reference/recon_expenses_sources_2026-05.md §1`; разрыв_после_loss=0,00 по 7 статьям; ADR-012 §4 CONFIRMED |
| Q-24 | «Налоги и сборы» — cash или accrued; природа | решение | **CLOSED по направлению (ADR-013 §3):** accrued (начисление П&Л); показ строкой-исключением на «Расходах»; форма механики → E1-T2-MECH-TAX |
| Q-25 | `pnl_2026-05.md` «31 статья» при 32 строках | референс-артефакт | **CLOSED:** опечатка метки, значения корректны (Σ=79 308 742,62); патч 31→32 применён |
| Q-26 | split исключений «Возврат»↔«Вывод прибыли» (02 vs 03/06) | решение | **CLOSED (ADR-013 §2):** канон-6 с «Вывод прибыли»; «Возврат» — фантом, удалён из populated-доков |
| Q-27 | «Вывод прибыли»: прод `marts.expenses` (BQ, май) = 3 355 976,16, live API/оракул = 4 246 163,66. Разрыв 890 187,50 | факт | **CLOSED** (сессия E1-T3, 2026-07-14): root cause — currency-conversion баг в cf-finance/`core.fact_payments` (`rate.value` не применяется для не-KGS валют — неисполнение ADR-010 для `paymentout`). Разрыв 890 187,50 закрыт полностью без остатка (Δ(A↔B)=890 187,50 + Δ(B↔C)=0,00). Фикс-форвард и приоритизация → ADR-016 (accepted). **Реконфирмация (сессия E1-T1-MECH-PREP, 2026-07-26):** факт `core.fact_payments` за май по статье = 4 246 163,66, совпадает с эталоном. Разрыв 0,00 |
| **E1-T3** | Discovery (Epic-1, Трек B, read-only): root-cause Q-27. Scope: diff `core.fact_payments` vs live API по `paymentout` «Вывод прибыли» май; last-successful-run vs набор майских документов (staleness); граница `DATE(moment)` UTC vs локальная TZ | задача | **DONE** (сессия 2026-07-14): root cause подтверждён + системность доказана (38/38 подлежащих загрузке не-KGS документов мая — 100% расхождение, ≥7 категорий, Σ 1 108 075,75 KGS). Q-27 → CLOSED, Q-29 → CLOSED (открыт и закрыт в этой же сессии). Return: ADR-016 (accepted) + `/reference/recon_vyvod_pribyli_2026-05.md §8` + `/reference/architect_review_queue_2026-07-14-2.md` (п.2, п.4). Приоритизация currency-fix vs E1-T1-MECH — открыто, решение за архитектором. Пункты 1–4 review-очереди адъюдицированы (ADR-016 §4–§5); currency-fix → отдельная задача E1-T3-MECH-FX |
| **E1-T3-D-CFSRC** | Read-only discovery: извлечение/версионирование кода cf-finance, точная локализация конвертера (движет Q-3, разблокирует E1-T3-MECH-FX) | задача | **DONE** (сессия 2026-07-14): инвентаризация on-disk источника (5 файлов, sha256-дерево); `gcloud functions describe` → ревизия `cf-finance-00006-piv` подтверждена; disk-vs-deployed diff — код (`main.py`/`patch_main_finance.py`/`requirements.txt`) байт-в-байт идентичен деплою, расходится только `.gcloudignore` (build-файл, не код); локус конвертера — `main.py::run_etl()`, `sum_kgs = сумма÷100`, поле `rate` нигде не используется. Снапшот → `/reference/code/cf-finance/` + `MANIFEST.md`; findings → `/reference/cf_finance_converter_findings_2026-07-14.md`. Побочная находка: локальный `.git` в `/home/ilyasbazarov4/cf-finance/` (не исследован, вне scope этого брифа) → вынесено в review-очередь (RQ-1) и ADR-017 |
| Q-31 | Маршрутизирует ли E1-T1-MECH источники `loss`/`commissionreportin` ЧЕРЕЗ cf-finance (тогда cutover E1-T1-MECH обязан идти ПОСЛЕ E1-T3-MECH-FX, иначе импорт бага) или это чистый SQ-SQL (cutover'ы независимы) | факт/решение | **CLOSED** (вердикт: новый независимый ингест, НЕ через cf-finance; вердикт внесён как `ADR-024` accepted, сессия RQ-6-E1T1-ADJ 2026-07-22). Grounding: полная диагностика 4 датасетов проекта (`bq ls`/`bq show`) — `raw.moysklad_loss`/`raw.moysklad_commissionreportin` не существуют нигде ни в одном из датасетов; оба варианта исходной развилки (α — через cf-finance, β — прямое чтение готовых raw-таблиц) не реализованы и негодны. Провенанс: `/reference/recon_expenses_2026-05.md`, `/reference/design_sq_marts_expenses_E1-T1-MECH.md` (staging-first) — закоммичены (наблюдаемы @ ffd87ec8). |
| **E1-T1-MECH-INGEST** | Ингест entity/loss + entity/commissionreportin → core.fact_loss/core.fact_commissionreportin (поддиагностика Q-31: источники не готовы, требуется cf-ингест) | задача | см. таблицу выше |
| RQ-1 | Каноническое место хранения исходников кода пайплайна | решение | **CLOSED (ADR-017):** развёл провенанс (снапшот `/reference/code/`, остаётся) vs живой VC (отдельный код-репо, вариант a; (b) отклонён). De-gate: НЕ гейтит `E1-T3-MECH-FX`. Живой VC → задача `CODE-REPO-STANDUP` (низкий приор, вкл. `.git`-discovery, сид из zip). Прецедент — все CF, исполнение per-CF |
| RQ-2 | `expand=rate` — приходит ли `rate.value` от МойСклад API без явного `expand` | факт | **CLOSED (разведка 2026-07-20):** форма `(row.get("rate") or {}).get("value")` в рабочем коде `main.py:69` на диске = API отдаёт `rate` вложенным объектом с полем `value`. Фикс написан под факт, не догадку. Финальность ревизии (sha256) подтверждается Шагом B брифа `E1-T3-MECH-FX-CUTOVER` перед cutover |
| RQ-4 | Нейминг-коллизия файлов `architect_review_queue_2026-07-14*.md` | решение | **CLOSED (ADR-018):** обязательный ординал `-N`≥1 (bare-date запрещён); существующие `-2`/`-3` grandfather (не переименовывать); клауза нейминга ADR-015 → superseded-in-part; правка `05 Часть III` |
| Q-32 | Точная дельта колонок `core.dim_products` ↔ `audit.dim_products_snapshots` (состав + позиция) | факт | **CLOSED (ADR-019 §1, 2026-07-17):** дельта = `weight` FLOAT64 (core поз.14, в цели отсутствует); поз.1–13 идентичны по имени/типу/позиции; `snapshot_at` TIMESTAMP = поз.14 цели. Sub-scope Q-4 закрыт в части `audit.dim_products_snapshots`. Провенанс `/reference/sq_audit_dim_products_drift_2026-07-17.md` §Ф-1 |
| Q-33 | (а) дата ПЕРВОГО падения `69fc93d1-…`; (б) состояние прогонов всех 13 transferConfig | факт | **CLOSED (ADR-019 §2 / ADR-020 §1, 2026-07-17).** (а) 2026-06-03; дыра 45 суток, подтверждена трижды независимо. (б) 13/13 сняты: **12 зелёных**, красный один — известный (45/50). Ограничение: `0/50` = «нет падений в последних 50 прогонах»; ~50 сут у 8, вся история у `weight_flow`/`expenses`/`customer_invoices_ar`, **~4 сут у `sales_overview`**. Провенанс `/reference/sq_fleet_health_2026-07-17.md` |
| Q-36 | `audit.dim_products_snapshots`: 27 `distinct_snapshot_days` при 26 успешных сутках по run history | факт | **CLOSED (ADR-019 §2, 2026-07-17):** лишние сутки = 2026-05-07, 13 389 строк = ровно 3 × 4 463, окно 13:19:53…13:27:29, вне расписания и вне run history ⇒ 3 ручных выполнения SQL при заводке конфига, не прогоны DTS (инструмент не установлен, не домысливается). Σ по суткам = 129 905 = `rows_total` ровно ⇒ иных вставок нет. Провенанс `/reference/sq_audit_dim_products_drift_2026-07-17.md` §Ф-2 |
| Q-41 | Статусные метки `DONE/PASS` в `07_STATE`, выставленные без возвращённого артефакта возврата (лог/файл/число) — факт или claim? Прецедент: `E1-T3-MECH-FX` нёс «Step 3 DONE» + «Step 4 DONE/PASS», при этом фикс Step 3 нашёлся только на диске (не в репо), а артефакт Step 4 не найден нигде | решение (процессный инвариант) | **CLOSED применением ADR-021 §2** («успех инструмента/метки ≠ факт»): метка `DONE` без артефакта возврата — **claim, не факт**; трактуется как открытый gate до предъявления артефакта. Не новый ADR — прямое следствие принятого. Операционализировано в брифе `E1-T3-MECH-FX-CUTOVER` (Return-this: «STATE_PATCH фиксирует статус только при наличии артефакта возврата»). Дальнейшего действия не требует |
| Q-42 | (security) `cf-finance` был задеплоен с `--trigger-http --allow-unauthenticated` (TD-SEC-01) — дало подтверждённый инцидент 2026-07-20 (анонимный трафик перезаписывал `core.fact_payments` независимо от паузы Scheduler), устранённый и ратифицированный `ADR-022`. Не проверено: задеплоены ли тем же паттерном остальные CF проекта — `cf-fx`, `cf-facts`, `cf-dim`, `cf-dq`, `cf-inventory`, `cf-alert` — и являются ли они write-эндпоинтами в прод-таблицы | решение | **ОТКРЫТ, discovery-бриф УТВЕРЖДЁН** (`E1-T3-CUTOVER-REVIEW-ADJ`, 2026-07-20): read-only аудит `--allow-unauthenticated` по 6 CF — (а) режим auth; (б) write-в-прод или read/utility; (в) штатный вызыватель + не ломает ли lockdown. Приоритет: выше DEFER-инфрастрок, НЕ на крит.пути Epic-1 — независим и дёшев, стартует немедленно параллельно, не блокирует Q-31/E1-T1-MECH. Эскалация в крит.путь — только если найден публичный write-CF, питающий поверхность Трека A/B. Основание — `ADR-022` + структурный паттерн per-CF (ссылка на `ADR-017 §6` из исходного патча снята архитектором как ошибочная по существу — §6 покрывает хранение кода, не IAM/аутентификацию). **Дополнение (сессия E1-T1-MECH-PREP, 2026-07-26):** `cf-loss-commission` проверен — `run.invoker` только у `etl-sa`, привязки `allUsers` нет, ingress `ALLOW_ALL` как у `cf-finance` после lockdown ADR-022. Новая функция дыры не открыла; в список 6 CF её добавлять как риск не нужно, факт зафиксирован |
| Q-43 | (инфраструктура) Scheduler-джоб `finance-daily-update` имеет `attemptDeadline: 180s`; серверный `--timeout` самой `cf-finance` = `1800s` (факт, `11 §CF`) — сервер укладывается с огромным запасом. Открыт узкий вопрос: обрывает ли истечение клиентского `attemptDeadline` серверную обработку на середине `MERGE`, или это чисто клиентский таймаут Scheduler (в связке с `retryConfig.maxRetryDuration=0s` — молча проглоченный «сбой», как класс молчания из ADR-020 §Q-34) | факт (требует проверки) | **CLOSED (факт-половина, сессия Q43-D-DEADLINE-PROBE, 2026-07-21):** триангуляция A/B/C по первому штатному прогону после IAM-lockdown ADR-022 (прогон 2026-07-21 03:00 Asia/Bishkek) — Source A (Scheduler): `DEADLINE_EXCEEDED`/504 в T+180s; Source B (Cloud Run лог `cf-finance`): `200`, latency 807s, все 5 маркеров `run_etl()` присутствуют; Source C (BQ): MERGE-джоб в `core.fact_payments` дошёл до `DONE` (inserted=1449, updated=4937) в T+~13м25с. Вердикт: `attemptDeadline=180s` — чисто клиентский таймаут Scheduler, серверную обработку НЕ обрывает; сервер доводит `run_etl()`/MERGE независимо от истечения клиентского дедлайна. Провенанс: `/reference/fin_sched_deadline_probe_2026-07-21.md`. Decision-половина CLOSED: **ADR-023 accepted** — raise attemptDeadline = гигиена/шумоподавление (не safety-гейт). **Значение = 1800s** (владелец адаптировал рекомендацию архитектора, 2026-07-21; = серверный timeout = потолок attemptDeadline ⇒ ложный 504 исключён by construction). Исполняет человек: `gcloud scheduler jobs update http finance-daily-update --attempt-deadline=1800s` (Scheduler-side, не редеплой; OIDC сохраняется), read-back верификация. Патч `11 §CF` (180s→1800s) — ПОСЛЕ подтверждения `jobs describe` (ADR-021 §2). Вердикт УЗКИЙ (снимает статус гейта, не утверждает невозможность частичной записи); несущая гарантия — Cloud-Run-no-abort + атомарность MERGE + идемпотентный ghost-DELETE (ADR-011). Q-43 → CLOSED (fact + decision) |
| Q-44 | (инфраструктура, обнаружено попутно в Q43-D-DEADLINE-PROBE) `11_INFRA_FACTS §CF` документирует ревизию `cf-finance` как `00006-piv`; фактически наблюдаемая в этой сессии (Source B, лог вызова) — `00012-cik`. Природа расхождения (сколько деплоев между `00006-piv` и `00012-cik`, что в них менялось) этой сессией не исследована | факт (расхождение дока и факта) | **ЗАКРЫТ** (сессия E1-T1-MECH-PREP, 2026-07-26). Факт: `serviceConfig.revision = cf-finance-00012-cik`, `updateTime = 2026-07-20T15:21:20Z`. Задокументированная ревизия 6 устарела; `11 §CF` патчится значением 00012-cik |
| Q-45 | (инфраструктура, заведён попутно в RQ-5-ADR023-ADJ) Несёт ли фактически задеплоенная ревизия `cf-finance-00012-cik` currency-fix (`× rate.value`, ADR-016) или это до-фиксовый конвертер (`sum_kgs = сумма÷100` без `rate`) — не установлено; observation gap между зафиксированным в `07 §E1-T3-MECH-FX`/`ADR-016` состоянием фикса и фактически исполняющимся на проде кодом | факт (observation gap, elevated — граница контракта) | **ЗАКРЫТ, ДА** (сессия E1-T1-MECH-PREP, 2026-07-26). Два независимых доказательства: (1) в `main.py` умножение на `rate.value`, строки 66–68; (2) в архиве лежит `main.py.pre-e1t3-mech-fx.bak` с sha256 `7a5d8dcd7866a887eb940accab709487eb27cba42a3446478b83678c67fa181d` — ранее зафиксированный хеш до-фиксовой версии. Подтверждено данными: все пять проверенных статей за май сходятся с эталоном. sha256 текущего `main.py` = `0cb4f6698a61cee96c90f9a14973ada38f5123418b54ff901f24ea2026ddab59` |
| Q-46 | (E1-T1-MECH-INGEST, сессия 2026-07-23) Способ коммита в holika для этой сессии не установлен — в Cloud Shell не найден клон holika (найден только несвязанный ~/cf-finance/.git без remote и без коммитов). Где должны жить файлы E1-T1-MECH-INGEST в `/reference`? Как получить write-access? | факт (процессный) | **CLOSED применением** (`E1-T1-INGEST-ADJ`, 2026-07-23): (1) путь = `/reference/code/cf-loss-commission/{main.py, requirements.txt, MANIFEST.md}` — `ADR-017 §2` (снапшот-per-revision + MANIFEST) + `ADR-024 §3` + Шаг 5 брифа; прецедент структуры `/reference/code/cf-finance/`. (2) Write-access модели НЕ полагается и не делегируется: `_METHOD §10` («запись обратно — не прямым коммитом модели, только session-блок → человек/applier») + `_ARCHITECT §1`; вариант «access-delegation архитектору» из B-9 отклонён по методу. (3) Отсутствие клона holika в Cloud Shell — корректное состояние, не дефект: `ADR-017 §4` намеренно разводит docs-репо и код-среду; клонировать holika в Cloud Shell не требуется. (4) Перенос — байт-точно из `function-source.zip`/on-disk, не транскрипция (`ADR-017 §2`); коммит из рабочей копии человека, формат `_ARCHITECT §8`. Нового ADR не требует (прецедент закрытия применением — `Q-41`). **Артефакт фактически закоммичен** в `holika` (ревизия `cf-loss-commission-00007-kuh`, сессия 2026-07-25) — закрытие подтверждено артефактом, не только применением решения (`ADR-031 §1`). |
| Q-48 | (E1-T1-MECH-INGEST, сессия 2026-07-23) Источник ставок конвертации в МойСклад API: есть ли ревью-процесс корректности курсов (сравнение с CBR/реальным курсом)? Брать как есть (на веру) или требуется верификация? | факт (policy) | **CLOSED (ADR-028)**: `rate.value` документа МойСклада — часть референс-оракула (`00_CHARTER §главный принцип`, `ADR-002`), берётся как есть; внешняя сверка курсов в inbound-конвертации не вводится (второй конкурирующий оракул + нарушение «слой не чинит чужой слой», `_METHOD §4`). Некорректный курс — дефект слоя ERP, правится в ERP. Приоритет источника — `rate.value` документа, иначе текущий курс (`ADR-010`). Разграничение: касается ТОЛЬКО входящей конвертации; исходящая (KGS→USD, `cf-fx`/`dim_fx_rates`) — вне scope, класс `Q-19`. |
| Q-49 | (E1-T1-MECH-INGEST, заведён в адъюдикации `E1-T1-INGEST-ADJ` 2026-07-23) Два контракт-факта ингеста, не снятые артефактом: **(а)** литеральная форма document-level поля — `commissionOverhead` (скаляр, форма пропозала сессии) или `commissionOverhead.sum` (объект, форма `02 §семантика П&Л`); **(б)** профиль запроса ингеста (`expand`, `limit`) — при `expand=` с `limit > 100` API молча игнорирует `expand` и отдаёт `NULL` (`02 §поведение API`), т.е. `rate`/`rate.currency.isoCode` деградируют тихо | факт | **CLOSED (ADR-031 §4)**: закрыт фактом из закоммиченного `main.py` — (а) `commissionOverhead` читается как объект (`.get("sum")`), подтверждает канон `02 §семантика П&Л`, не скалярную форму; (б) expand-профиль (`loss: expenseItem,agent,rate.currency`; `commission: agent,rate.currency,positions`) с `limit=100` — в безопасных границах `02 §поведение API`, `expand` не деградирует. Ссылки: `ADR-026 §4`, `ADR-027 §3`. |
| Q-50 | (E1-T1-MECH-INGEST, Шаг 6, 2026-07-24) `_rate_and_currency` при `rate.value is None` использует хардкод `1.0`, а не «текущий курс валюты» (`ADR-010`/`ADR-028`). На загруженных данных не проявилось (100% выборки — KGS), но расходится со спекой буквально | решение | **CLOSED (ADR-029, accepted-директива):** фикс-форвард — fallback-ветка обязана фетчить фактический текущий курс валюты вместо хардкода `1.0`; обязательство не гейтится наличием не-KGS данных в выборке (аналогия `ADR-016 §3`/`Q-47`). Реализация → задача-строка `E1-T1-MECH-RATE-FALLBACK` |
| Q-51 | (E1-T1-MECH-INGEST, Шаг 6, 2026-07-24) Приёмочный критерий «конвертация проверена на ≥1 не-KGS документе» не закрываем эмпирически: 0/128 loss, 0/7 commissionreportin-май — не-KGS данных нет физически | факт (прецедент Q-47) | **CLOSED решением владельца (ADR-031 §5, 2026-07-25)** — не DEFER-ожиданием: код-ревью признан достаточным закрытием приёмочного критерия «конвертация проверена на ≥1 не-KGS документе», живой тест не требуется как условие. Основание — 128+191 документов за всю историю аккаунта, ни одного не-KGS. Прецедент того же класса, что Q-47. Триггер пересмотра прежний: первый реальный не-KGS документ |
| Q-52 | (E1-T1-MECH-INGEST, Шаг 6, находка, 2026-07-24) «Маркетинг и реклама» (loss, 751 438,54) численно совпадает с историческим нераскрытым остатком §Входы брифа (`architect_review_queue_2026-07-24-1.md` п.3) | факт | **ЗАКРЫТ, задвоения нет** (сессия E1-T1-MECH-PREP, 2026-07-26). Эталон «Маркетинг и реклама» за май = 1 026 901,54 = 275 463,00 (платежи) + 751 438,54 (списания). Совпадение объяснено: это недостающая половина статьи, а не дубль |
| E1-T1-MECH-RATE-FALLBACK | Форвард-фикс `_rate_and_currency`: явный fetch текущего курса валюты вместо хардкода `1.0` в fallback-ветке (`rate.value is None`) (`ADR-029`) | задача (decision, ADR-029) | **DONE (сессия 2026-07-25, `ADR-031 §3`)**: fallback-ветка реализована и задеплоена — `entity/currency` вместо хардкода `1.0` при `rate.value is None` |
| Q-54 | (E1-T1-MECH-PREP, 2026-07-26) Полный список `EXCLUDE_EXPENSE_IDS` — где живёт в коде | факт | **ЗАКРЫТ (снят).** Константы `EXCLUDE_EXPENSE_IDS` в коде нет — список зашит литералами прямо в `delete_sql`. Q-10 тем самым ЗАКРЫТ: четыре идентификатора зафиксированы в ADR-037. Остаточное: названия этих статей неизвестны, `stg_msklad.fact_payments_stg` не существует. Достаётся запросом к `entity/expenseitem/<id>` МойСклада — с обязательным заголовком `Accept-Encoding: gzip` |
| Q-56 | (E1-T1-MECH-PREP, 2026-07-26) Живой SQL марта делит суммы всех месяцев на **последний** курс из `dim_fx_rates` (`ORDER BY date DESC LIMIT 1`), то есть `total_sum_usd` за исторические месяцы считается по сегодняшнему курсу | факт (класс — исходящая конвертация) | **СУЖЕН (ADR-039, 2026-07-26).** Подтверждено артефактом: `/reference/sql/sq_marts_expenses.sql` стр.19–26. `ADR-039 §1`: в scope `E1-T1-MECH` формула не трогается (accepted). `ADR-039 §2`: развилка «штатно / дефект» — политика инвесторской отчётности, не архитектурный инвариант (`total_sum_usd` = DERIVED, границу `ADR-002`/`ADR-005` не нарушает ни одна форма) → **owner-gated, ожидает решения владельца** (`/reference/architect_review_queue_2026-07-26-2.md` п.1). Два новых факта: (1) тот же паттерн `latest_fx` задокументирован как КАНОНИЧЕСКИЙ в `03 §marts` для `marts.in_transit` (стр.126–134) — поведение репо-широкое, и для метрики состояния оно содержательно корректно, в отличие от исторического потока; (2) `core.dim_fx_rates` наполняется из Bakai с 2026-06-03 (`02 §core.dim_fx_rates`), покрытие ранних дат НЕ замерено — без майских курсов вариант (b) для мая-2026 технически неисполним. Замер покрытия + скан 13 SQ на `latest_fx` → строка `FX-OUTBOUND-COVERAGE`. **Сессия `E1-T1-MECH-PREP-ADJ-2` (2026-07-26):** архитектурного открытого решения не осталось — `ADR-039 §2` (owner-gated) и `§4` (замер = предусловие) уже accepted; открыт был только исполнительский шаг, он закрыт: discovery-бриф сгенерирован (`briefs/FX-OUTBOUND-COVERAGE.md`). Фактура к развилке владельца, добавленная этой сессией: цена варианта (a) бьёт в `00_CHARTER §миссия` (нельзя назвать инвестору воспроизводимую USD-величину периода); цена варианта (c) ниже оценки очереди — разметка «поток/состояние» ложится в существующий `03 §marts`, где `latest_fx` для `marts.in_transit` уже описан как канонический. **Предусловие `ADR-039 §4` СНЯТО фактом** (`/reference/fx_outbound_coverage_2026-07-26.md §3`: май-2026 покрыт 31/31). **`§5` замерен** (`§2` артефакта): основной `latest_fx` — `abc_xyz`, `expenses`, `gmroi`, `in_transit`, `inventory_health` (5); fallback — `supplier_price_history`; косвенно — `gmroi_by_folder`; `sales_overview` конвертирует **по дате** (стр.120–121). **`ADR-050`:** развилка переформулирована в трёхчленную ((b′) привести 5 к дате / (a′) узаконить `latest_fx` / (c′) по типу метрики); `gmroi_by_folder` — не пункт политики ⇒ цена (b′) = 5 правок, не 7. **`ADR-053`:** решение владельца НЕ принимается до замера доли forward-fill (`Q-66`), порог интерпретации зафиксирован до замера (`§5`). **Замер выполнен; решение владельца УШЛО В DEFER (`ADR-057 §3`, 2026-07-26)** с новым предусловием — различить причину малоподвижности курса (`Q-69`, бриф `briefs/FX-RATE-FIDELITY.md`). Основание: `_METHOD §4` и `00_CHARTER §закон-следствие` — ни один вариант конвертации не исправляет вход, который не меняется; выбор формулы поверх непроверенного входа есть костыль у потребителя вместо фикс-форварда в слой-источник. Владелец подтвердил приоритет явно. **Ставка решения снижена фактом `ADR-057 §4`:** для мая-2026 «по курсу на дату операции» и «по последнему курсу на конец мая» дают одно и то же число (в мае одно значение), различается только «по сегодняшнему курсу» **CLOSED (`ADR-062`, владелец, 2026-07-26).** Политика исходящей конвертации выбрана: вариант `(c′)` — форма курса по типу метрики, критерий отнесения = наличие дневного зерна в строке витрины, выведен из живого SQL (`ADR-062 §2/§3`). `sq_marts_expenses` → курс по дате с деградацией на последний курс (форма-эталон `supplier_price_history`, `ADR-050 §4`); `in_transit`, `inventory_health` → последний курс УЗАКОНЕН (метрики состояния); `gmroi`, `abc_xyz` → последний курс + пометка качества, «по дате» неисполнимо (дневного зерна нет); `gmroi_by_folder` наследует. **Цена (b′) «5 правок SQL» из `ADR-050 §2` опровергнута кодом — `superseded-in-part` (`ADR-062 §4`).** Исполнение → задача-строка `FX-POLICY-EXPENSES`, ПОСЛЕ cutover `E1-T1-MECH` (`ADR-062 §9`). |
| Q-57 | (E1-T1-MECH-PREP, 2026-07-26, гигиена) В задеплоенном архиве `cf-finance` лежат `main.py.bak`, `main.py.pre-e1t3-mech-fx.bak`, `patch_main_finance.py` и `__pycache__/`. Задеплоенный артефакт не чист | факт (гигиена деплоя) | **CLOSED (ADR-040, 2026-07-26)** — закрыт ПАТТЕРН, не инстанс: `RQ-3` тем же ADR → CLOSED. Внеплановый деплой ради чистки НЕ назначается (риск гигиенический; редеплой `cf-finance` не-идемпотентен и имеет прецедент переоткрытия IAM-дыры, `ADR-022`). Следующий деплой любой CF по любой причине обязан идти с очищенным `.gcloudignore` (`*.bak`, `__pycache__/`, разовые patch-скрипты); проверка — read-back содержимого архива после деплоя, результат в `MANIFEST.md` ревизии (`ADR-017 §2`). Диагноз провала `RQ-3`: правило жило только строкой в `07` и не было ничьим условием ⇒ условие вынесено в `05 Часть II`. Осознанная издержка (`ADR-040 §5`): после чистки в архиве не останется до-фиксовых `.bak`, давших второе доказательство `Q-45` — замена штатная, снапшот-per-revision `/reference/code/` |
| Q-58 | (E1-T1-MECH-PREP, 2026-07-26, процесс) В `07_STATE` каждый task-ID существует в двух написаниях — с кириллической `М` (U+041C) и латинской `M` (U+004D) | факт (процесс) | **CLOSED в части нормализации (ADR-041, 2026-07-26).** Замер выполнен заново архитектором @ SHA `d063a8c` (`ADR-021 §2`, цифры review-очереди уточнены): `07` — 42 кириллических / 29 латинских; `06` — 5 / 43. Разбивка `07`: `E1-T1-MECH-INGEST` 15/4 · `E1-T1-MECH` 10/4 · `E1-T3-MECH-FX` 9/5 · `E1-T3-MECH-FX-CUTOVER` 3/0 · `E1-T1-MECH-RATE-FALLBACK` 3/0 · `E1-T2-MECH-TAX` 2/1 · `E1-T1-MECH-PREP` 15 (целиком латиница). Полный скан смешанных токенов: единственный класс гомоглифных ИДЕНТИФИКАТОРОВ — семейство `*-MECH*`; прочие смешанные токены (`GAP-реестр`, `session-блок`, `не-KGS`) — легитимные композиты. `04_ROADMAP`/`05_CONVENTIONS` вхождений не имеют — blast radius = `07` + `06`. `07` нормализован под латиницу механической заменой; `06` не тронут (append-only, `ADR-018 §3`), 5 кириллических вхождений (стр.373/377/378/393) читаются как латинские ID. Конвенция на будущее — принята владельцем, внесена в `05 Часть III` (`ADR-041 §6`) |
| Q-59 | (E1-T1-MECH-PREP, 2026-07-26, ошибка сессии) Запрос к МойСкладу в диагностическом скрипте №2 вернул не-JSON и упал на разборе | факт (ошибка сессии, зафиксировать) | **ЗАКРЫТ.** Причина — отсутствовал заголовок `Accept-Encoding: gzip`, который оба прод-загрузчика передают (`cf-finance` строка 39). Любой будущий диагностический вызов API МойСклада обязан его нести |
| Q-60 | (E1-T1-MECH-PREP-ADJ, 2026-07-26) `11_INFRA_FACTS §CF` стр.22 @ SHA `d063a8c` утверждает «доку. зафиксирована `cf-finance-00006-piv`» и «содержит ли `00012-cik` currency-fix — не установлено», притом что `07_STATE` на ТОМ ЖЕ SHA закрывает `Q-44` (ревизия = `00012-cik`) и `Q-45` (currency-fix ДА). Два дока проекта в одном коммите противоречили друг другу | факт (расхождение доков) | **ЗАКРЫТ в этой же сессии** (прецедент закрытия в одну сессию — `Q-18`, `Q-29`): `11 §CF` стр.22 приведена в соответствие с закрытыми `Q-44`/`Q-45` тем же коммитом. Причина расхождения — патч `11` из сессии `E1-T1-MECH-PREP` не был применён, тогда как патч `07` был. Отдельного правила из этого НЕ выводится (на двух точках процесс не изобретается, `_METHOD §6`) |
| FX-OUTBOUND-COVERAGE | (заведена `ADR-039 §4/§5`, 2026-07-26) Замер покрытия `core.dim_fx_rates` по датам (наполнение из Bakai с 2026-06-03; есть ли курсы за май-2026 и ранее) + скан всех 13 SQ флота на паттерн `latest_fx` (`ORDER BY date DESC LIMIT 1`) | задача (discovery) | **READY** (владелец подтвердил запуск, 2026-07-26). Read-only, ничего не гейтит, идёт параллельно `E1-T1-MECH`. Return-this = обогащение репо: покрытие `dim_fx_rates` по датам + список SQ с паттерном → `/reference`. Выход разблокирует решение владельца по `ADR-039 §2`. **Discovery-бриф СГЕНЕРИРОВАН** (`briefs/FX-OUTBOUND-COVERAGE.md`, сессия `E1-T1-MECH-PREP-ADJ-2`, 2026-07-26). Ждёт запуска человеком; статус остаётся READY до возвращённого артефакта (`ADR-021 §2`: метка без артефакта — claim, не факт, прецедент `Q-41`). **DONE (2026-07-26).** Артефакт возвращён и закоммичен: `/reference/fx_outbound_coverage_2026-07-26.md` ⇒ `ADR-021 §2` удовлетворён. Выход адъюдицирован сессией `FX-OUTBOUND-COVERAGE-ADJ`: `ADR-049` (п.1 — не аномалия, `02` патчится, forward-fill), `ADR-050` (п.2 — развилка переформулирована), `ADR-051` (п.3 — `Q-68`), `ADR-053` (замер как предусловие). Побочные находки сессии: `B-12`, `ADR-054`, `ADR-055`, `ADR-056` |
| Q-63 | (E1-T1-MECH-PREP-ADJ, 2026-07-26, ошибка сессии) Архитектор рассчитал слоты Scheduler от календарной даты своего контекста (26 июля), тогда как фактическое UTC-время среды было `2026-07-25T19:44:52Z`; Бишкек = UTC+6 ⇒ локальная дата опережала UTC на шесть часов. Результат: слот `2026-07-25T21:00Z` объявлен пропущенным, хотя он ещё не наступил | факт (ошибка сессии, зафиксировать) | **ЗАКРЫТ.** На ложном расчёте были построены `Q-61` (мнимый пропуск прогонов обоими джобами) и переквалификация `B-10` — **оба отозваны в той же сессии, в репо не вносились**. Опровергнуто дважды: собственным листингом (`msklad-pipeline-hourly`, `0 * * * *`, `scheduleTime = 2026-07-25T20:00:01Z` при `lastAttemptTime = 2026-07-25T19:00:02Z` ⇒ текущее время строго раньше `20:00Z`) и прямым `date -u`. Смежное: репо датирует сессии по локальному времени Бишкека, конвенция нигде не зафиксирована — вопрос вынесен в review-очередь п.6. Предложенное правило (UTC-якорь первой командой) — `proposed`, свёрнуто с п.3 очереди в один пункт `NEW_CONVENTIONS` сессии `E1-T1-MECH-PREP-ADJ-2`, ожидает явного апрува владельца (`_METHOD §8`). **Смежная конвенция ЗАКРЫТА (`ADR-046`, 2026-07-26):** датировка сессий и артефактов = локальная дата Бишкека (канон, ретро-правок нет); любой временно́й ФАКТ в прозе — UTC с явным суффиксом `Z`. Переход на UTC в датировке отклонён по механике `ADR-018 §3` (ретро-переименование запрещено ⇒ смешанный набор дат без права выравнивания). Прецедент класса: `ADR-044`, `Q-59` |
| Q-66 | (FX-OUTBOUND-COVERAGE-ADJ, 2026-07-26) Доля строк `core.dim_fx_rates`, являющихся forward-fill (копией курса предыдущего дня), а не наблюдённым курсом своего дня, не замерена | факт (магнитуда; структура установлена) | **ОТКРЫТ, замер назначен.** Структурная половина CAPTURED (`ADR-049 §3`): покрытие 454/454 без дыр при непубликации источника в выходные (`RUNBOOK_v8` журнал 2026-05-13 «FX lag > 3 дня (выходные)») + механизм (авто-forward-fill `cf-fx` при 401, `PROJECT_REFERENCE_v6` стр.186/757; ручной `INSERT`, `RUNBOOK_v8 §13.4`) + зафиксированные применения (`М-19` 7 строк, `М-24`, `М-28` 7 строк) ⇒ forward-fill в таблице есть. Форма квалификации — прецедент `Q-30`. **`ADR-053`:** замер = один read-only запрос (не бриф, прецедент формы `ADR-048 §2`), свёрнут в один прогон с read-back'ом `ADR-052 §9`; **жёсткое предусловие** решения владельца по `ADR-039 §2` (`§3`); метрика — число дней-копий, выпавших на РАБОЧИЕ дни мая-2026 (май-2026 = 10 суббот и воскресений, 1 мая — пятница); порог рекомендации зафиксирован ДО замера (`§5`): 0–2 ⇒ (b′), ≥5 ⇒ (c′), 3–4 ⇒ выбор владельца. Ограничение метода (`§6`, `ADR-044 §1/§2`): равенство курса двум соседним дням есть верхняя граница forward-fill, не доказательство ⇒ трактовка по построчной выдержке мая, не по проценту. Не гейтит `E1-T1-MECH`. **CLOSED замером (`ADR-057 §1`, 2026-07-26):** артефакт `/reference/fx_forward_fill_2026-07-26.md`, прогон `2026-07-26T14:46:56Z`. Дней, в которые значение не менялось: 63,3 % (апрель), **96,8 %** (май), 86,7 % (июнь), 73,1 % (июль 1–26); копий в рабочие дни 13–20/мес. **Май-2026 — одно значение `87.4205` на все 31 день** (вошло 2026-05-01, внутримесячных изменений ноль). Миграция 2026-06-03 режим качественно не изменила. Порог `ADR-053 §5` формально пройден в сторону `(c′)` (20 ≥ 5), но **механически не применён** — измеренная величина вне модели, под которую порог строился (`ADR-057 §2`): порог не сдвинут и не переписан, вместо ответа по нему поднят `Q-69`. Побочный факт без интерпретации (`ADR-057 §6`): в апреле и июле часть выходных несёт новое значение, что не согласуется с формулировкой `ADR-049 §3` — уходит в `Q-69` |
| Q-67 | (FX-OUTBOUND-COVERAGE-ADJ, 2026-07-26) `03 §marts` стр.126–134 документирует `latest_fx` (`ORDER BY date DESC LIMIT 1`) как **канонический** паттерн исходящей конвертации, тогда как живой `sq_marts_sales_overview` стр.120–121 конвертирует **по дате** (`LEFT JOIN dim_fx_rates fx ON f.transaction_date = fx.date`). Док и код расходятся | факт (расхождение док/код) | **ОТКРЫТ, не блокер.** Класс `Q-60`. `ADR-050 §6`: настоящим не решается — какая из двух форм канон, есть ровно `ADR-039 §2` (owner-gated). Правка `03 §marts` НЕ назначается до решения владельца (`ADR-039 §Последствия`: «документировать нечего»). Закрывается тем же решением, которым закрывается `Q-56`. **Сдвинут вместе с `Q-56` (`ADR-057 §7`, 2026-07-26):** закрывается решением по `ADR-039 §2`, а оно ушло в DEFER до закрытия `Q-69`. Факт, снижающий практическую остроту: для мая-2026 `sq_marts_sales_overview`, джойнящий по дате, умножает выручку на ту же константу, что и `latest_fx`-файлы — в мае одно значение курса (`ADR-057 §4`) ⇒ действующая неоднородность флота на майских числах не проявляется **CLOSED (`ADR-062 §10`, 2026-07-26) — и НЕ как расхождение.** `03 §marts` стр.126–136 содержит `latest_fx` ВНУТРИ канонического SQL витрины состояния `in_transit`; репо-широкого утверждения «`latest_fx` — канон для всего» в `03` нет (стр.374 говорит о классе конвертации, не о форме курса). При выбранной политике `(c′)` этот фрагмент верен и правке не подлежит. Формулировка `ADR-050 §6` («док и код расходятся») была НАД-ПРОЧТЕНИЕМ — `superseded-in-part`. `03 §marts` патчится ДОБАВЛЕНИЕМ явной политики (патч 5), а не исправлением `in_transit`. |
| INFRA-FACTS-RESTORE | (заведена `ADR-052 §5`, 2026-07-26) Восстановление `11_INFRA_FACTS.md`: файл @ `98097a5` содержал копию `05_CONVENTIONS.md` (редакция до `ADR-046`), инфра-фактов ноль | задача (восстановление; исполнитель — человек) | **DONE (2026-07-26), верифицировано артефактом** (`ADR-021 §2`). Восстановлено владельцем @ SHA `206244dfab3035d4e86e30a893feb983868bb0eb`: 12 158 байт, 75 строк, версия 0.2 (M-P4-A-03). Причина порчи установлена владельцем — некорректный коммит обновления, НЕ промах applier'а (`ADR-052 §4`). Полный дифф дерева `98097a5 → 206244d`: изменён ровно один файл ⇒ якоря прочих патчей сессии действительны. Зависимости `ADR-052 §2` проверены поимённо: `§SQ` стр.49/50 (номера совпадают с цитатой `ADR-045 §6`), `§CF` стр.25 регион `asia-east1`, полные Config ID `6a22a243-…cad4` / `6a23f3ea-…7ecc`, `§секреты` `msklad-token`/`bakai-fx-token`; исправление `Q-60` (стр.22) на месте. Восстановление из замороженных источников как основной путь остаётся отклонённым и после факта (`ADR-052 §6`) — прецедент для LIVING-доков. Остаток стр.25 (висячий провенанс) закрывается фикс-форвардом в прогоне `ADR-053 §2`, отдельный `Q` не заводится (`ADR-052 §9`) |
| FX-RATE-FIDELITY | (заведена `ADR-058 §5`, 2026-07-26) Различить гипотезы `Q-69`: сверка GCS-архива ответа источника против содержимого `core.dim_fx_rates` за июль-2026 + логи `cf-fx` за те же даты + явный ответ о наличии внутренних свидетельств по май-2026 | задача (discovery) | **READY** (владелец подтвердил приоритет явно — вариант 1, 2026-07-26). Read-only, идёт параллельно `E1-T1-MECH`, живых витрин не касается. **Бриф СГЕНЕРИРОВАН:** `briefs/FX-RATE-FIDELITY.md` (сессия `FX-FORWARD-FILL-ADJ`, SHA context-to-load `5c10dc0b63aa5e0d25e11655b0d6a4af7d2f72c8`). Return-this = `/reference/fx_rate_fidelity_<date>.md` + session-блок; **ADR не предлагать** — квалификация за архитектором (`ADR-058 §7`). Статус остаётся READY до возвращённого артефакта (`ADR-021 §2`: метка без артефакта — claim, не факт; прецедент `Q-41`). Замер каденции `ADR-045 §5` с этим прогоном НЕ сворачивается — другой объект и другой гейт (`ADR-058 §5`). **READY → DONE (2026-07-26).** Артефакт возвращён: `/reference/fx_rate_fidelity_2026-07-26.md`, три захода (`run.log`/`run2.log`/`run3.log`). Return-this выполнен по форме брифа (§1–§7). Ход и вердикт — см. обновлённую строку `Q-69`. ADR не предложен разработчиком (`ADR-058 §7`) — квалификация и решение по `ADR-039 §2` за архитектором |

| Q-71 | (PIPELINE-AUDIT, 2026-07-27) Критерии «готово к инвестору» в `00_CHARTER §критерии успеха` — CONTEXT GAP чартера, не промоутнутый в реестр: без них у проекта нет финишной черты (причина неконтролируемого расширения scope) | решение (owner) | owner-сессия: зафиксировать 3–5 критериев готовности через ADR → патч `00 §критерии успеха` **CLOSED (ADR-067, та же сессия):** критерий зафиксирован владельцем — числовой паритет с интерфейсом МойСклад; чартер пропатчен; остатки-развилки → Q-73/Q-74 |

| Q-73 | (ADR-067 §6, 2026-07-27) Уровень паритета с МойСклад UI: (a) «пиксель» — страницы BI рисуют ровно то же (влечёт ревизию ADR-006/ADR-013) · (b) «сверяемые величины + опубликованный мост соответствий» · (c) гибрид — (b) + режим-зеркало на чувствительных страницах | решение (owner) | owner-выбор; рекомендация архитектора — (b), при повторных претензиях клиента к конкретной странице — точечно (c) **CLOSED (ADR-068, владелец):** выбран (b); отступления моста = только уже принятые ADR-006/013; новые пожелания = новый ADR |
| Q-74 | (ADR-067 §6, 2026-07-27) Глубина истории паритета: (a) инвесторский период (напр. последние 12 мес) · (b) вся история аккаунта · (c) граница по дате из ADR, ранняя история помечена «вне паритета» в LS | решение (owner) | owner-выбор даты/варианта; рекомендация — (c) с явной датой; учесть эпохи FX (`ADR-049`/`ADR-059`: курсы до 2025-04-29 и окно мая-2026) **CLOSED (ADR-069):** выбран (c), якорь 2026-05-01 (обоснование фактами: единственный оракул-месяц 0,00; Q-30 не замерен; FX-эпохи); расширение назад — явным ADR |

| Q-21 | `sq_marts_customer_invoices_ar` (Config `6a23f3ea-…`) в снапшоте D5 имеет тот же пустой-schedule рендер, что был у `sq_marts_expenses` (оказался дефолтным 24h). Вероятно тоже ежедневный автозапуск, но `bq show` по нему не снят | факт | **partial-close (ADR-020 §6, 2026-07-17):** фактическая каденция = **ежедневная**, якорь ~10:00 UTC (43 прогона подряд, 0 не-OK) — гипотеза подтверждена фактом, внесено в `11 §SQ`. **Остаток:** поле `schedule` в transferConfig не снято (`bq show --transfer_config` по `6a23f3ea-…` не выполнялся) — DEFER, не блокер. **CLOSED (сессия `E1-T1-MECH-CUTOVER`, 2026-07-27):** остаток закрыт — поле `schedule` у `6a23f3ea-…` в выдаче ОТСУТСТВУЕТ (не пусто), расписание в `scheduleOptionsV2`, `nextRunTime 2026-07-28T10:00:00Z` |
| E1-T1-MECH | Механика точечной правки `sq_marts_expenses`: править прод `transferConfig` (`6a22a243-…`, WRITE_TRUNCATE, **ежедневный автозапуск ~11:10 UTC** — исправлено M-P5-AP-2) in-place, или сверка на staging/копии SQ с cutover | решение | **Шаги 0–5 DONE, все критерии приёмки выполнены. Стоп-гейт владельца (Шаг 5) достигнут. Прод не тронут. Ожидает решения владельца по `E1-T1-MECH-CUTOVER`** (сессия `E1-T1-MECH`, 2026-07-27). **DONE (cutover исполнен 2026-07-27T15:52Z, приёмка 0,00):** закрыта cutover'ом 2026-07-27: прод `sq_marts_expenses` несёт union трёх источников, май-2026 = 10 232 903,20, разрыв 0,00, прод==staging по всем 28 месяцам |

---

## Снятые блокеры (полный текст)

- **B-1 (держит старт P2):** оба исходных файла и BQ-выгрузка → **СНЯТ** (источник залит в репо @ 8a17b84; MIGRATION_MAP.md готов)
- **B-2 (нужен до брифа P3):** `08_TASK_BRIEF_TEMPLATE.md` → **СНЯТ** (в репо)
- **B-3 (держит приёмку по цифрам P4):** ADR-005 граница оракула → **СНЯТ** (ADR-005 accepted; приёмка «по цифрам» включена на ORACLE-строках)
- **B-4 (пороги свежести contradictions):** ADR-007 (три механизма, не примиряются) → **СНЯТ** (ADR-007 → accepted; ветка примирения `03d` закрыта, см. `04_ROADMAP` B6)
- **B-5 (держал промоушен ADR-012):** → **СНЯТ** (bq show 2026-07-08: FROM=core.fact_payments подтверждён; расхождение по расписанию исправлено в ADR-012 §Контекст; провенанс /reference/bq_transferconfig_sq_marts_expenses_2026-07-08.txt). ADR-012 → accepted.
- **B-6 (Roadmap-последовательность, сессия E1-T3 2026-07-14):** приоритизация currency-fix vs E1-T1-MECH РЕШЕНА (ADR-016 §5) → **СНЯТ** по приоритету; остаётся Q-31 (порядок cutover) как под-гэп последовательности roadmap — путь закрытия назначен (`E1-T3-CUTOVER-REVIEW-ADJ` 2026-07-20): grounding-чтение `/reference/sql/sq_marts_expenses.sql`, первым шагом брифа `E1-T1-MECH`.
- **B-8 (держал исполнение решения Q-31 / заведение `E1-T1-MECH-INGEST`):** → **СНЯТ** (RQ-6-E1T1-ADJ, 2026-07-22): апрув архитектора дан, `ADR-0XX` внесён как `ADR-024` (accepted). `E1-T1-MECH-INGEST` разблокирован к заведению в `04_ROADMAP` (по факту генерации брифа, anti-improvisation).
- **B-9 (СНЯТ, сессия 2026-07-25, `ADR-031 §1`):** провенанс-снапшот `/reference/code/cf-loss-commission/{main.py, requirements.txt, MANIFEST.md}` закоммичен человеком в `holika` (ревизия `cf-loss-commission-00007-kuh`, sha256-верифицирован disk==deployed, дважды за сессию — ревизии `00006-dip` и `00007-kuh`). Закрытие Шага 5 в приёмке разблокировано; `Q-49(б)` закрыт (`ADR-031 §4`). `Q-46` — CLOSED (путь и процесс назначены, теперь подтверждено артефактом).
- **B-10 (сессия E1-T1-MECH-PREP, 2026-07-26) → СНЯТ фактом (E1-T1-MECH-PREP-ADJ, 2026-07-25T19:45Z):** `core.fact_loss` = **128** строк, `core.fact_commissionreportin` = **191** — совпадает до единицы с полнотой аккаунта, зафиксированной в самом B-10 («128 и 191 документов») и в `Q-47`/`ADR-031 §5`. История залита целиком; гранулярность обеих таблиц — документ (для комиссий это не было очевидно, ср. `ADR-026`: `reward` берётся с уровня `positions[]`). Заливка произошла двумя ручными прогонами `gcloud scheduler jobs run loss-commission-daily-update` (`19:35:22Z` и `19:42:52Z`, см. `Q-62`), а не по расписанию — плановый слот `2026-07-25T21:00Z` на момент замера ещё не наступил. Наложение двух полных прогонов задвоения не дало: `MERGE` с явными колонками (`ADR-030`/C1) отработал идемпотентно (n=1, в правило не возводится). **Гейт Шага 0 брифа `E1-T1-MECH` открыт.** Остаточные проверки Шага 0, ставшие возможными только теперь: регрессия майских контрольных сумм (loss-часть = 1 193 254,77 — источник числа: `briefs/E1-T1-MECH.md` стр.49, НЕ текст `ADR-033`, который несёт только итог 10 232 903,20; сходится как 411 838,94 + 751 438,54 + 29 977,29, см. `ADR-047 §7`), наличие `applicable = FALSE` (`ADR-034`), попадание четырёх системных `expense_item_id` в историю (`ADR-037`), варианты написания статей (`ADR-035`), непустые `project_id`/`sales_channel_id`/`agent_name` (`ADR-036`/`Q-55`).
- **B-11 (заведён и СНЯТ одновременно, ADR-042, 2026-07-26):** смешанное состояние качества марта (`ADR-024 §5` — платежная часть несла валютный баг `ADR-016`) → **СНЯТ**. Валютная починка подтверждена в проде двумя независимыми доказательствами (`Q-45`) и в данных: три источника за май дают 10 232 903,20 = целевая величина эталона, разрыв 0,00 (`ADR-033`). Номер `B-11` присвоен `ADR-042`: `ADR-033 §Последствия` говорит «B-9 снят», но `B-9` в `07` занят провенанс-снапшотом `cf-loss-commission` (`ADR-031 §1`, ссылки `Q-46`/`Q-3`); текст `ADR-033` в append-only `06` не редактируется — упоминание «B-9» там по факту нумерации есть настоящий `B-11` (прецедент формы — `ADR-018 §4`).
- **B-12 (заведён и СНЯТ в одной сессии, `FX-OUTBOUND-COVERAGE-ADJ`, 2026-07-26, `ADR-052`; форма — прецедент `ADR-042 §4`):** `11_INFRA_FACTS.md` @ SHA `98097a5d06803504d5e15d9362c4772b4bd837c8` содержал **копию `05_CONVENTIONS.md`** в редакции до вставки `ADR-046` (20 520 байт, 224 строки; строка 1 `# FILE: 05_CONVENTIONS.md`; побайтовое расхождение с `05` со строки 209). Инфра-фактов — ноль. Класс — СТРУКТУРНОЕ НЕСООТВЕТСТВИЕ (`_METHOD §8`), отказ **тихий** (`HTTP 200` + валидный markdown), то есть хуже `Q-35` (404). **Держал:** (i) адресат патча `ADR-045 §6` — `11 §SQ` стр.49–50, при том что замер каденции есть жёсткое предусловие генерации брифа `E1-T1-MECH-CUTOVER` (`ADR-045 §3`) ⇒ критический путь Epic-1; (ii) строку верификации `ADR-048 §2` (регион `asia-east1`); (iii) приёмку `E1-T1-MECH` п.5 (полный `transferConfig` ID, `briefs/E1-T1-MECH.md` стр.230); (iv) `ADR-014 §1` (имена секретов). **НЕ держал:** Шаг 0/0б `E1-T1-MECH`, `FX-OUTBOUND-COVERAGE`. → **СНЯТ (2026-07-26):** причина — некорректный коммит обновления владельцем, НЕ промах applier'а (`ADR-052 §4`); файл восстановлен владельцем @ `206244dfab3035d4e86e30a893feb983868bb0eb`, все четыре зависимости проверены поимённо (`ADR-052 §5`), номера строк `§SQ` 49/50 совпали с цитатой `ADR-045 §6`. Смежное: `Q-60` — второй тихий промах по тому же файлу, механизм другой (пропуск патча против подмены содержимого). **Защита от повтора введена `ADR-054`** по прямому запросу владельца: любая роль обязана сверить первую строку файла с его именем ДО работы и остановиться на расхождении; форма git pre-commit hook владельцем не выбрана (`ADR-054 §5`, путь не закрыт)
