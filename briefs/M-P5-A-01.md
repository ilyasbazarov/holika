# TASK BRIEF · M-P5-A-01 — Декомпозиция M-P5 (процессный слой) + адъюдикация ретро-ADR

> Тип: **архитекторский декомпозиционный** (аналог M-P4-A-01). Один бриф = одна задача = один чат.
> Пиннить context-to-load по **SHA, который человек даёт в начале сессии** (ниже — плейсхолдер `<SHA>`;
> факты входов зафиксированы на `585f613853a5fcf0ad00cf2ea5ae46aafdb61805`).

## Роль
Ты — **АРХИТЕКТОР** проекта (НЕ разработчик). Сначала прочитай `_METHOD` (особ. §11 discovery-петля) +
`05_CONVENTIONS` + `_ARCHITECT`, затем действуй. Ты ПРОЕКТИРУЕШЬ (декомпозиция, черновые ретро-ADR,
приоритизация), НЕ пишешь прод-код. Человек апрувит решения и коммитит. Выход = патчи доков + черновые ADR
(`proposed`), не код и не исполнение подзадач.

## Цель
Декомпозировать фазу **M-P5 (процессный слой)** в проверяемую таблицу подзадач; адъюдицировать отложенные
на P5 ретро-ADR-кандидаты (черновики `proposed`); приоритизировать discovery-очередь по блокирующему влиянию
на Epic-1; задать заголовок+scope Epic-1 и цель/приёмку/context-to-load **первого боевого брифа** (сам боевой
бриф — отдельной задачей). НЕ исполнять подзадачи — только спроектировать фазу и вынести человеку на апрув.

## Context-to-load (обязательно; re-pin по SHA сессии)
- `_METHOD` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/_METHOD.md  *(особ. §11 discovery-петля; определение фазы P5)*
- `_ARCHITECT` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/_ARCHITECT.md
- `00_CHARTER` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/00_CHARTER.md
- `05_CONVENTIONS` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/05_CONVENTIONS.md  *(Часть III — формат session-блока)*
- `04_ROADMAP` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/04_ROADMAP.md  *(шапка Epic M; строка M-P5; запрет пред-детализации будущих эпиков)*
- `06_DECISIONS_LOG` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/06_DECISIONS_LOG.md  *(ADR-005…009; блок «Ожидающие апрува»)*
- `07_STATE` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/07_STATE.md  *(Текущий фокус; GAP-реестр; блокеры; контрольные цифры)*
- `08_TASK_BRIEF_TEMPLATE` — https://raw.githubusercontent.com/ilyasbazarov/holika/<SHA>/08_TASK_BRIEF_TEMPLATE.md
- Замороженный источник для трассировки ретро-ADR (читать, НЕ выдумывать): `PROJECT_REFERENCE_v6.md`, `RUNBOOK_v8.md`, `MIGRATION_MAP.md` (те же raw-URL, тот же SHA).

Если чего-то из списка нет в контексте → выдай `CONTEXT GAP` и остановись.

## Входы (факты на SHA 585f613)
- **M-P4 = DONE** (22/22 подзадач). Блокеры **B-1…B-4 сняты**. `06` § «Ожидающие апрува» — пуст.
- **Отложенные на P5 ретро-ADR-кандидаты** (из `04_ROADMAP`, трассируемы — НЕ изобретать сверх):
  - **02f · валюты** — минорные единицы + `rate.value` документа. В M-P4-02f прямо: «ретро-ADR-кандидат — P5 (не здесь)».
  - **03f · Ghost Records** — `applicable=False`, DELETE после MERGE (PR-27 + PR-34 п.26). В M-P4-03f: «ретро-ADR → P5».
  - Свипнуть источник + session-логи на ПРОЧИЕ отложенные «→ P5»; каждому — адрес-трассировка; не добавлять от себя.
- **Открытая discovery-очередь** (`07` GAP-реестр, статус НЕ CLOSED): **Q-3, Q-4, Q-6, Q-7, Q-9, Q-10, Q-11, Q-12, Q-13, Q-14**.
  *(Q-1 = partial; Q-2/Q-5/Q-8/Q-15/Q-16/Q-17 = CLOSED.)*
- **Epic-1** (ADR-009 §1): построить **TO-BE raw-март `marts.expenses`** (по ADR-006, двухисточниковая методология,
  фильтр по ИМЕНИ статьи) + ретайр легаси `sq_marts_expenses`. Фильтр по имени развязан от Q-9/Q-10, валидирован
  оракулом (0.00 diff 2026-07-06) → **discovery Epic-1 НЕ гейтит**.

## Шаги
1. Прочитать context-to-load. Свипнуть замороженный источник + session-логи на ВСЕ отложенные «→ P5» ретро-ADR-кандидаты; каждому — трассировка к PR/RB/ADR. Не изобретать.
2. **Ретро-ADR (черновики).** Для каждого подтверждённого кандидата (минимум: валюты-02f, Ghost-03f) выдать черновой ADR по формату `06` (контекст → решение → последствия → статус `proposed`). Ретро = фиксируем УЖЕ принятое в коде решение с провенансом к источнику; молчаливого примирения нет. Номера/`accepted` — только после апрува человека.
3. **07 «полон».** Перечислить, чего не хватает `07_STATE` для статуса «полный» (синхронизация контрольных цифр, снятые заметки-противоречия, актуализация `updated_at`, перевод discovery-очереди в приоритизированный вид). Оформить как пункты `STATE_PATCH`, не переписывать `07` вручную вне формата.
4. **Discovery-очередь.** Приоритизировать Q-3/4/6/7/9/10/11/12/13/14 по: (а) блокирует ли Epic-1, (б) тип (факт / референс-артефакт / решение), (в) стоимость добычи. Дать явный порядок + пометку «параллельно / блокер». Явно зафиксировать: **Epic-1 не блокируется ни одним Q** (обоснование ADR-006/009).
5. **Первый боевой бриф.** Задать в `04_ROADMAP` заголовок+scope **Epic-1** — БЕЗ пред-детализации сверх первой задачи (anti-improvisation). Определить первую задачу Epic-1 для боевого брифа (кандидат: построение TO-BE raw-март `marts.expenses` + приёмка против оракула `/reference/pnl_2026-05.md`, целевой расход 11 132 303,89). Зафиксировать только цель/приёмку/context-to-load боевого брифа — сам бриф здесь НЕ писать.
6. **Собрать декомпозицию M-P5** в таблицу подзадач: `ID · что · тип(a/b/c) · источник/адрес · приёмка · зависимости` — по образцу таблицы M-P4 в `04_ROADMAP`.

## Критерии приёмки (Acceptance)
- Таблица подзадач M-P5 в `04_ROADMAP`: каждая строка — проверяемая приёмка + трассировка; 0 выдуманных задач.
- Черновые ретро-ADR (≥2: валюты, Ghost Records) в `06`, статус `proposed`, каждый трассируем к PR/RB.
- Discovery-очередь Q-3…Q-14 приоритизирована: явный порядок + пометка блокер/параллельно; зафиксировано «Epic-1 не гейтится discovery» с обоснованием.
- `04_ROADMAP`: Epic-1 получил заголовок+scope; определены цель/приёмка/context-to-load первого боевого брифа.
- Всё вынесено человеку на апрув до `accepted`; ничего не примирено молча.

## Что вернуть человеку (Return-this)
- Патч `04_ROADMAP.md`: секция «M-P5 — детальная декомпозиция» (таблица подзадач) + блок «Epic-1 — заголовок+scope».
- Патч `06_DECISIONS_LOG.md`: черновые ретро-ADR (`proposed`), append-only.
- `STATE_PATCH` для `07_STATE.md`: пункты «что дополнить для полноты» + приоритизированная discovery-очередь.
- Отдельным блоком: цель / приёмка / context-to-load первого боевого брифа Epic-1 (заготовка под следующий GENERATOR-прогон).
- Точная инструкция человеку: что и в каком порядке коммитить.

## Вне scope этой задачи
- Исполнение подзадач M-P5 (финализация ADR сверх `proposed`, ручное наполнение `07` вне `STATE_PATCH`).
- Написание самого первого боевого брифа Epic-1 (отдельная задача / отдельный GENERATOR-прогон).
- Любой прод-код и построение марта `marts.expenses` — это Epic-1.
- Добыча/закрытие discovery-гэпов (Q-3…Q-14) — здесь только приоритизация, не разведка.
- Присвоение ретро-ADR статуса `accepted` без апрува человека.

## В конце сессии
Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
