# TASK BRIEF · T-SALES-REFRESH-WINDOW-DEPLOY (пересобран под патч варианта 1, второй заход)

> **Цитата `07_STATE.md` §Мандат Claude Code (строка `SALES-REFRESH-WINDOW, деплой`, база сессии
> `bd8c733`), дословно:** «SALES-REFRESH-WINDOW, деплой | B | нет | ВЫДАН, ПРИОСТАНОВЛЕН (`ADR-161`) |
> деплой исполнен `2026-08-11` (ревизия `cf-facts-00012-ber`) и в тот же день откачен на
> `cf-facts-00011-mab`; разрешено одно действие — откат (исполнено); новый мандат на деплой патча
> варианта 1 не выдан, гейт — решение владельца по `Q-107` (текст страховки против кода)».
>
> **Цитата `07_STATE.md` §Открытые вопросы, строка `Q-108` (та же база), дословно:** «Артефакт
> `reference/sales_refresh_window_rollback_adj_2026-08-11.md §7` п.4 утверждает, что
> `workflow_weekly.yaml:70` передаёт `window_days=90` явно для `mode="weekly"` — независимая проверка
> это опровергла... OPEN».
>
> **Внимание для читателя этих двух цитат — расхождение названо, не скрыто.** На базе `bd8c733` обе
> строки читаются как «мандата нет, `Q-108` открыт». Это устарело ФАКТОМ, зафиксированным ДРУГОЙ
> сессией: `SALES-REFRESH-WINDOW-VERIFY-ADJ` (архитектор, класс A, без брифа, `ADR-086 §1`) закрыла
> `Q-108` и выдала новый мандат класса B двумя коммитами (`8ce95c1`, `99d2db0`) на ветке
> `s/SALES-REFRESH-WINDOW-VERIFY-ADJ` — **эта ветка ещё НЕ слита в `main` и не прошла проход сборки**
> на момент генерации этого брифа. Данный бриф собран поверх её текста намеренно — стартовый промт,
> которым была вызвана эта сессия, воспроизводит дословно `reference/sales_refresh_window_deploy_mandate_2026-08-11.md
> §6` (сама архитекторская сессия предписала именно такой следующий шаг). Расхождение между `07_STATE`
> и текстом мандата закрывается не этим брифом, а проходом сборки буфера — и именно поэтому
> предусловие **П1** ниже требует его перед деплоем и несёт собственную механическую проверку,
> а не полагается на то, что сборка уже прошла к моменту исполнения этого брифа.

---

**Класс задачи (ADR-076):** B
<Живой деплой Cloud Function `cf-facts` (патч варианта 1: область удаления по происхождению строки +
параметризация недельного окна) плюс не-идемпотентный прогон против прод-таблицы
`core.fact_sales_profit`. Мандат выдан поимённо архитектором — текст:
`reference/sales_refresh_window_deploy_mandate_2026-08-11.md §4` (сессия
`SALES-REFRESH-WINDOW-VERIFY-ADJ`, коммит `99d2db0`, номер ADR присваивается проходом сборки).
Дополнительно гейтится `ADR-065` («деплой CF только из код-репо»).>

**Параллель (ADR-082 §1, уточнён `ADR-083 §1`):** нет
<Класс B не параллелится никогда.>

**Файлы на запись** (полный список; на нём МЕХАНИЧЕСКИ проверяется пересечение при параллельном
запуске — `tools/parallel_check.sh`, `ADR-083 §1`):
- `reference/sales_refresh_window_deploy_v2_<date>.md` — провенанс деплоя: сверка дрейфа, снимок
  таблицы (П2), sha256 ветки, обе строки предохранителя, замер трёх чисел приёмки, время travel
  окно (замер)
- `reference/code/cf-facts/MANIFEST.md` — новая секция ревизии по факту read-back (`ADR-040`,
  `ADR-017 §2`), включая неудачную попытку, если она случится
- `reference/_scratch_SALES-REFRESH-WINDOW-DEPLOY_<date>/` — рабочие скрипты/логи сессии (один файл
  на шаг, `ADR-076 §6`)
- `reference/_inbox/session_SALES-REFRESH-WINDOW-DEPLOY_<date>.md` — session-блок

## Роль
Ты — исполнитель проекта. Законы — `CLAUDE.md`, стандарты — `05_CONVENTIONS.md` Части I и II.
Модель исполнения: ты исполняешь сам (`ADR-082 §2`). Не-идемпотентное — `deploy`, `run`
(`mode=weekly` с расширенным окном, следующий за ним `mode=promote`), прод-`bq query` (снимок,
замеры), `git push` (код-репо) — только отдельным действием после явного подтверждения владельца,
никогда в связке с диагностикой (`ADR-055 §2`). **Перед КАЖДЫМ таким ask — отдельное сообщение
владельцу: что именно исполняется, на каком объекте, чем откатывается, с именем ревизии отката**
(`ADR-077 §6`, промежуточная мера в силе). `Done` — только по подтверждённому логу, не по `rc=0`
(`★ Успех инструмента ≠ факт`). Работаешь в СВОЁМ рабочем дереве и коммитишь в СВОЮ ветку
(`ADR-081 §6`). `07_STATE`, `06_DECISIONS_LOG` и `06_INDEX` не правишь: session-блок кладёшь файлом
в `reference/_inbox/`.

## Цель
Задеплоить в `cf-facts` патч варианта 1 (`reference/sales_refresh_window_rollback_adj_2026-08-11.md §7`:
единый источник меток периметра, `COALESCE`-предикат по происхождению строки в обеих ветках удаления
`MERGE`, параметризованное недельное окно; правка докстринга `main.py:175-176` из адъюдикации `Q-108`)
из НОВОЙ ветки код-репо `holika-prod`, пройдя пять предусловий `§3` мандата, снять свежий снимок
таблицы непосредственно перед прогоном, исполнить первый боевой прогон одним заходом РЕЖИМОМ
`mode=weekly` (единственным принимающим `window_days`), измерить три числа приёмки — весь объём,
откат и приёмка ровно по `reference/sales_refresh_window_deploy_mandate_2026-08-11.md §3/§4`, ничего
сверх.

## Context-to-load (обязательно прочитать перед работой)
- `_METHOD`, `00_CHARTER`, `05_CONVENTIONS`, `07_STATE` (всегда, читаются с диска по именам)
- `reference/sales_refresh_window_deploy_mandate_2026-08-11.md` — полный текст целиком: §1 (что
  архитектор сверил по существу), §2 (вердикт готовности), §3 (пять предусловий — обязательны ДО
  деплоя), §4 (текст мандата: объект, что разрешено/не разрешено, откат, приёмка — три числа)
- `reference/sales_refresh_window_verify_adj_2026-08-11.md` — адъюдикация `Q-108`: почему текст
  докстринга правится, а не `workflow_weekly.yaml`; находка §7 про `mode=hourly`/`mode=purchases`
  (парсят, логируют и выбрасывают `window_days` — не использовать эти режимы для расширенного окна)
- `reference/sales_refresh_window_scope_verify_2026-08-11.md` — независимая проверка предиката
  удаления (восемь пунктов, все пройдены); что именно уже проверено и не требует повторной проверки
  этой сессией
- `reference/sales_refresh_window_rollback_adj_2026-08-11.md` §§1-4, §7 — природа дефекта, которым
  был откачен прошлый деплой (ветка удаления слепа к происхождению строки при двух независимых
  загрузчиках `core.fact_sales_profit`), и точный состав патча варианта 1 по файлам
- `reference/sales_refresh_window_deploy_2026-08-11.md` — как шёл прошлый (откаченный) заход: что
  было исполнено (шаги 1-6 старого брифа, штатно), где он встал (шаг 8, структурный гэп `weekly`),
  как был откачен (три числа приёмки отката — все нули)
- `05_CONVENTIONS.md` §«Процедура деплоя Cloud Function — только из код-репо, вариант Б» — восемь
  обязательных элементов
- `reference/deploy_procedure_2026-08-03.md` — чек-лист процедуры (шаги/команды)
- `reference/code/cf-facts/config.py`, `reference/code/cf-facts/bq_ops.py`,
  `reference/code/cf-facts/fetch_perimeter.py`, `reference/code/cf-facts/main.py` — четыре файла
  патча варианта 1 (состав по мандату `§4`); `main.py` несёт и патч, и правку докстринга `Q-108`
- `reference/code/cf-facts/MANIFEST.md` — текущая живая ревизия `cf-facts-00011-mab`, провенанс
  предыдущих деплоев
- `02_ERP_CONTRACTS.md` §core.fact_sales_profit — схема целевой таблицы (для снимка и замера)
- `07_GAPS.md` строка `SALES-REFRESH-WINDOW` — полный провенанс задачи; `07_STATE.md` §Мандат
  Claude Code, строка `SALES-REFRESH-WINDOW, деплой`
Если чего-то из списка нет в контексте → выдай `CONTEXT GAP` и остановись.

## Входы
- Снапшот `reference/code/cf-facts/` несёт патч варианта 1 (четыре файла) плюс правку докстринга
  `Q-108`, но эти правки лежат на НЕСЛИТОЙ ветке `s/SALES-REFRESH-WINDOW-VERIFY-ADJ`. Код в этой
  сессии не патчится — только переносится в ветку деплоя ПОСЛЕ того, как предусловие П1 подтвердит,
  что он уже в `main`.
- Живая ревизия базы деплоя — `cf-facts-00011-mab` (мандат `§4` фиксирует её как исходную и как
  цель отката кода).
- Старый снимок `core.fact_sales_profit_snap_20260811` — НЕ точка восстановления для этого деплоя
  (устарел, П2), НЕ удалять.
- Старая ветка код-репо `deploy/cf-facts-2026-08-11-refresh-window` — provenance откаченного кода,
  НЕ база для новой ветки, НЕ цель слияния (П3).

## Шаги

### Предусловия (`§3` мандата) — все пять ДО шага деплоя, каждое своим скриптом

1. **П1 — ветка адъюдикации слита.** Read-only: `grep -c "передаёт 90 явно" reference/code/cf-facts/main.py`
   на актуальном `main` (после `git pull`/чтения текущего HEAD). **Стоп-условие:** результат ≠ `0` →
   правка `Q-108` ещё не в `main` (проход сборки буфера не прошёл, либо прошёл без слияния ветки
   `s/SALES-REFRESH-WINDOW-VERIFY-ADJ`) — остановиться, доложить владельцу, не продолжать шаги 2-14.
   Ожидаемый результат при выполненном предусловии — `0`.

2. **П2 — свежий снимок таблицы.** Read-only замер: `SELECT COUNT(*) FROM core.fact_sales_profit`
   с UTC-якорем времени замера. Зафиксировать число и время — это ориентир, не сам снимок (снимок
   создаётся позже, на шаге объявленного действия, непосредственно перед прогоном, не здесь).
   **Стоп-условие:** повторное использование `core.fact_sales_profit_snap_20260811` как точки
   восстановления для этого цикла запрещено — он старше и не отражает состояние ПЕРЕД этим прогоном.
   Старый снимок не трогать (не удалять, не переиспользовать).

3. **П3 — новая ветка деплоя от `master`.** Read-only проверка ДО заведения ветки: `git log
   deploy/cf-facts-2026-08-11-refresh-window -1` (существующая ветка) и `git log
   deploy/cf-facts-2026-08-11-refresh-window-v2 -1` (новая, ожидается «не существует»).
   **Стоп-условие:** если `deploy/cf-facts-2026-08-11-refresh-window-v2` уже существует — остановиться,
   не переиспользовать её вслепую (могла быть заведена другой сессией с другим содержимым), спросить
   владельца. Имя новой ветки — `deploy/cf-facts-2026-08-11-refresh-window-v2`, заводится от
   актуального `master` код-репо `holika-prod`, не от старой ветки.

4. **П4 — сверка дрейфа заново, живым съёмом.** Read-only: снять текущую задеплоенную ревизию
   `cf-facts` (ожидание — `cf-facts-00011-mab`, `100%` трафика) и сверить `sha256` её архива против
   `master` код-репо на МОМЕНТ ЭТОЙ сессии (не против архива `2026-08-09`, использованного мандатом
   как провенанс, — тот замер устарел по построению, `§3 п.4` мандата). **Стоп-условие:**
   расхождение любого файла — деплой поверх дрейфа затирает чужую правку молча, остановиться и
   доложить владельцу.

5. **П5 — расширенный первый прогон только `mode=weekly`.** Не read-only команда, а проверка
   понимания перед объявлением действия (шаг 10 ниже): зафиксировать в артефакте, что вызов с
   `mode=hourly` либо `mode=purchases` и `window_days` шире умолчания **не является заменой**
   расширенного прогона — оба режима парсят поле, печатают его в лог (`main.py:90`) и отбрасывают
   (`main.py:94`/`:100`), окно останется `7`/полным независимо от переданного значения. Единственный
   режим, реально принимающий `window_days`, — `mode=weekly`. Явного стоп-условия у этого шага нет,
   он документальный — но нарушение (вызов `hourly`/`purchases` с намерением расширить окно) на
   шаге 10 есть провал приёмки, не мелочь.

### Деплой

6. **Ветка деплоя.** Завести `deploy/cf-facts-2026-08-11-refresh-window-v2` от актуального `master`
   (П3 подтвердил её отсутствие); перенести в неё из снапшота `reference/code/cf-facts/` ровно
   четыре файла патча — `bq_ops.py`, `config.py`, `main.py`, `fetch_perimeter.py`. Сверить `sha256`
   перенесённых файлов против снапшота репозитория `holika` — они обязаны совпасть (снапшот к этому
   моменту сам синхронизирован с `main`, П1).

7. **Гигиена и секреты.** Проверить `.gcloudignore` (чистый, `ADR-040`: `*.bak`, `__pycache__/`,
   `patch_*.py` исключены) и прогнать сплошной поиск секретов по патчу с печатью совпавших строк
   (`ADR-044`, пустая выдача не есть факт отсутствия).

8. **Объявление действия (`ADR-077 §6`) + подтверждение владельца, затем снимок.** Отдельным
   сообщением назвать: что исполняется (`CREATE SNAPSHOT TABLE core.fact_sales_profit_snap_<YYYYMMDD_HHMMSS>
   CLONE core.fact_sales_profit`), на каком объекте (`core.fact_sales_profit`, прод), чем
   откатывается (сам снимок; операция сама по себе безопасна и обратима, но пишет в прод). Ординал
   времени в имени снимка обязателен — старый `snap_20260811` без времени уже занят и не годится
   как имя. После явного «да» — выполнить, напечатать число строк и `SUM(revenue_kgs)` по каналам
   (тот же разрез, что в `rollback_adj §3`: оптовая торговля / комиссия / розница / без канала) —
   это и есть точка сравнения П2 для приёмки шага 12.

9. **Объявление действия + подтверждение, затем деплой.** Отдельным сообщением: деплой `cf-facts`
   из ветки `deploy/cf-facts-2026-08-11-refresh-window-v2` (`--source` только на `cf-facts/`), откат
   — повторный деплой/перевод трафика на именованную ревизию `cf-facts-00011-mab`. После «да» —
   деплой; зафиксировать новую ревизию.

10. **Read-back архива.** Сверить содержимое задеплоенного архива (`ADR-021 §2`: успех команды
    деплоя ≠ факт). Проверить: `sha256` четырёх файлов совпадает с веткой; в архиве нет `*.bak`,
    `__pycache__`, `patch_*.py` (это первое из трёх чисел приёмки мандата `§4`). Записать секцию в
    `reference/code/cf-facts/MANIFEST.md`.

11. **Замер окна time travel (read-only, второй путь отката данных).** `bq show
    --format=prettyjson core`, поле `maxTimeTravelHours` — замерить, не предполагать (мандат `§4`
    отката, пункт 2). Записать число в артефакт как второй путь восстановления данных, дополнительно
    к снимку шага 8.

12. **Объявление действия + подтверждение, затем первый боевой прогон.** Отдельным сообщением:
    режим и объект — `mode=weekly` с расширенным `window_days`, покрывающим `2026-05-01` (формула
    мандата: `window_days >= (дата прогона − 2026-05-01 в сутках) + 3`, вычислить на ФАКТИЧЕСКУЮ
    дату прогона), staging + `MERGE` обоих промоутов (продажи и периметр) ОДНИМ прогоном с ОДНИМ
    значением `window_days`; чем откатывается — снимок шага 8 плюс time travel шага 11 (данные), код
    — ревизия `cf-facts-00011-mab` (шаг 1 отката). **Ни `mode=hourly`, ни `mode=purchases` для этой
    цели не вызывать (П5)** — окно будет проигнорировано молча. После «да» — исполнить. В логе
    обязана присутствовать строка «предохранитель пройден» с обеими датами для КАЖДОГО из двух
    промоутов — отсутствие есть провал приёмки, не мелочь для разбора постфактум.

13. **Три числа приёмки (мандат `§4`, дословно, не переписываются):**
    1. Read-back архива задеплоенной ревизии: `sha256` четырёх файлов совпадает с веткой; в архиве
       нет `*.bak`, `__pycache__`, `patch_*.py` (исполнено шагом 10).
    2. Разбиение таблицы после прогона: сумма частей (периметр + продажи, тем же предикатом, что в
       `scope_verify §6`) точно равна `COUNT(*)`, зазор и пересечение `0` — инвариант, не конкретное
       число.
    3. Сопоставление со свежим снимком П2 (шаг 8) по четырём каналам: удалённых строк оптовой
       торговли `0` штук / `0,00` KGS. **Ненулевое значение — немедленный откат по пункту 1 отката
       (`§4`), ветка в `master` не сливается.**
    Дополнительно — по аналогии со старым брифом, но не частью трёх чисел мандата: измерить
    `COUNT(*)`/`SUM(revenue_kgs)` за май-2026 и июль-2026 до/после для провенанса; расхождение с
    ожиданием (`sales_refresh_window_rollback_adj_2026-08-11.md` не называет числового ожидания для
    этого цикла — прошлый ориентир относился к режиму `topoff`, который здесь не строится) печатается
    как факт, не примиряется исполнителем.

14. **Слияние ветки в `master` код-репо** — ТОЛЬКО после успешного read-back (шаг 10) и всех трёх
    чисел приёмки (шаг 13) в ожидаемых значениях. При любом неуспехе — ветка не сливается, откат на
    `cf-facts-00011-mab`, неудачная попытка записывается в `MANIFEST.md` наравне с удачной.

15. Собрать `reference/sales_refresh_window_deploy_v2_<date>.md`: результаты пяти предусловий,
    снимок (число/сумма по каналам), sha256 ветки, обе строки «предохранитель пройден», три числа
    приёмки, замер time travel, майский/июльский замер до-после, ссылки на коммиты код-репо (ветка +
    merge либо запись о неудаче).

## Критерии приёмки (Acceptance)
- Все пять предусловий `§3` пройдены с зафиксированным результатом; ни одно не пропущено и не
  заменено памятью/предположением.
- Ревизия `cf-facts` обновлена, read-back подтверждён `sha256` четырёх файлов, в архиве нет
  `*.bak`/`__pycache__`/`patch_*.py`.
- Свежий снимок `core.fact_sales_profit_snap_<YYYYMMDD_HHMMSS>` существует, создан НЕПОСРЕДСТВЕННО
  перед прогоном (не переиспользован `snap_20260811`), число строк/сумма по каналам зафиксированы.
- Лог первого прогона несёт строку «предохранитель пройден» с обеими датами для ОБОИХ промоутов.
- Три числа приёмки мандата (шаг 13) измерены и напечатаны; третье число (сопоставление со свежим
  снимком) равно нулю по оптовой торговле — иначе сессия не закрывается `DONE`, исполнен откат.
- Окно `maxTimeTravelHours` датасета `core` замерено (не взято из провенанса прошлой сессии).
- Ветка код-репо слита в `master` ТОЛЬКО при выполнении условий выше; при любом неуспехе — запись в
  `MANIFEST.md`, откат на `cf-facts-00011-mab`.
- Ни один шаг из §1-§15 не объединяет диагностику и не-идемпотентное действие в одном скрипте/пасте
  (`ADR-055 §2`); каждый скрипт несёт `date -u` и `gcloud auth list` первой И последней командой
  (`ADR-055 §1/§4`).
- Перед каждым не-идемпотентным действием (шаги 8, 9, 12) — отдельное сообщение владельцу с именем
  ревизии/снимка отката, ДО ask (`ADR-077 §6`).

## Что вернуть человеку (Return-this)
- `reference/sales_refresh_window_deploy_v2_<date>.md` — полный провенанс деплоя и прогона.
- Обновлённый `reference/code/cf-facts/MANIFEST.md`.
- Session-блок в `reference/_inbox/session_SALES-REFRESH-WINDOW-DEPLOY_<date>.md` по
  `05_CONVENTIONS` Часть III, включая статус (`DONE`/`PARTIAL`/провал приёмки — по факту лога, не по
  намерению) и любые новые открытые вопросы.

## Вне scope этой задачи
- Правка `05_CONVENTIONS.md` под `Q-107` (proposed, апрув владельца) — не эта сессия, отдельным
  поимённым коммитом после апрува (`ADR-081 §8`).
- Правка `workflow_weekly.yaml`/`workflow_hourly.yaml` — `Q-108` закрыт решением НЕ трогать эти
  файлы (`verify_adj §3`); расширение объёма патча этой сессией запрещено.
- Режим `topoff` не строится (узкая форма, `ADR-144 §8`).
- Источник COGS шире 90 суток — открытый гэп (`SALES-MERGE-DRYRUN`), эту задачу не гейтит.
- Правка `07_STATE.md`/`06_DECISIONS_LOG.md`/`06_INDEX.md`/`07_GAPS.md` — делает проход сборки.
- Адъюдикация ненулевого расхождения любого из трёх чисел приёмки — если третье число ненулевое,
  действие уже названо (откат), решение по причине — архитектору постфактум, не этой сессии.

## В конце сессии
Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
