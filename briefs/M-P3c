# TASK BRIEF · M-P3c — Discovery Q-8 (методология агрегации П&Л МойСклада)

## Роль
Ты — рабочий чат в **discovery-режиме** (`_METHOD.md` §11). Сначала прочитай `_METHOD` + `05_CONVENTIONS`
(правила агента), затем действуй. Задача с суждением (интерпретация методологии внешней системы + черновой
ADR) → модель архитекторского уровня.
Модель исполнения: **ты пишешь/интерпретируешь и драфтишь обогащение репо; человек добывает источник из UI
МойСклада, запускает API-запросы и коммитит.** Ты не исполняешь сам.

## Цель
Установить **методологию агрегации расходной части П&Л МойСклада** — по какому правилу
`fact_payments (+ entity/loss)` обязаны сходиться к целевой величине `11 132 303,89` (KGS, май-2026),
и чем объясняется остаток разрыва `523 654,81` (14,4%), «размазанный по статьям в обе стороны».
Return-this — обогащение репо (черновой ADR + референс-артефакт + предложение патча `02`), НЕ прод-код.

## Context-to-load (обязательно прочитать перед работой; SHA-пин)
Base = `https://raw.githubusercontent.com/ilyasbazarov/holika/e02064f0a99f4ea3fdaa12960918beafe18eaac0`
- `{Base}/_METHOD.md`
- `{Base}/00_CHARTER.md`  — §главный принцип (источник истины сверки = ТОЛЬКО прямой запрос к API МойСклада)
- `{Base}/05_CONVENTIONS.md`  — Часть III (формат session-блока)
- `{Base}/07_STATE.md`  — Q-8/Q-9/Q-10 в GAP-реестре
- `{Base}/06_DECISIONS_LOG.md`  — ADR-002 (граница-оракул), ADR-005 (правило сверки расходов, п.4)
- `{Base}/02_ERP_CONTRACTS.md`  — §семантика П&Л (несёт ⚠ GAP Q-8), §оракул
- `{Base}/reference/pnl_2026-05.md`  — seed #1, разбивка разрыва и оговорки источника
Если чего-то из списка нет в контексте → выдай `CONTEXT GAP` и остановись.

## Входы (уже зафиксированы в репо)
- Целевая величина сверки расходов: **11 132 303,89** (`ORACLE`).
- `fact_payments` май на 2026-06-25: **7 492 843,26** (`PIPELINE_SNAPSHOT`).
- Разрыв **3 639 460,63**: из него **3 115 805,82** = 4 статьи в `entity/loss` (→ Q-9/Q-10, вне этого брифа);
  **523 654,81** = остаток → **Q-8**.
- Оговорка: прямая арифметика `entity/loss` тоже не бьётся 1:1 с П&Л → методология агрегации неизвестна.
- Указанный источник (из `07_STATE` Q-8): документ **`00029-00001`** в UI МойСклад + возможный запрос в поддержку.

## Шаги
1. Переформулируй задачу своими словами; перечисли полученные входы; проверь полноту → нет → `CONTEXT GAP`.
2. **Сформулируй человеку точечный запрос на добычу источника:** что именно открыть/выгрузить из UI МойСклад
   (документ `00029-00001`: как строится строка расходов П&Л — состав статей, знак, неттинг, момент признания,
   что включается/исключается), и какой API-скоуп `entity/loss` за май-2026 нужен для сверки методологии.
   Источник истины сверки — прямой запрос к `api.moysklad.ru` (`00_CHARTER` §главный принцип), не BQ-таблицы.
3. По возвращённым человеком данным/скринам — **реконструируй правило агрегации**: покажи арифметику,
   объясняющую остаток `523 654,81` (или явно переклассифицируй его, если методология вскрывает иную причину).
4. Зафиксируй методологию как **референс-артефакт** и **черновой ADR**; предложи патч `02` (через NEW_DECISIONS,
   не прямой правкой STABLE-дока).
5. Что не разрешается источником — не дотягивай догадкой: `DEFER` с фиксацией в `07_STATE`, либо новый под-гэп.

## Критерии приёмки (Acceptance)
- Методология агрегации расходной П&Л **зафиксирована в репо и трассируема к источнику** (id документа
  `00029-00001` / ответ поддержки + дата добычи).
- Остаток `523 654,81` **объяснён методологией** ИЛИ явно переклассифицирован/`DEFER` с причиной — без тихой
  подгонки (`_METHOD.md` §4b).
- Правило сверки расходной строки сформулировано **проверяемо** (в цифрах): при каком составе и правиле
  `fact_payments + entity/loss` обязаны дать `11 132 303,89`.
- Q-8 в `07_STATE` помечен на закрытие (closed/partial); при partial — остаточный под-вопрос назван.

## Что вернуть человеку (Return-this — обогащение репо, куда коммитить)
- `/reference/pnl_methodology.md` — методология агрегации (правило + арифметика + провенанс источника).
- **Черновой ADR** (в session-блоке, `proposed`) — фиксация правила сверки расходов / методологии
  (уточняет ADR-005 п.4). Номер = следующий по факту.
- **Предложение патча** `02_ERP_CONTRACTS` §семантика П&Л (заменить «⚠ GAP Q-8» на зафиксированную
  методологию) — через `NEW_DECISIONS`/`NEW_CONVENTIONS`, не прямой правкой.
- Точные команды человеку: что открыть в UI и какой `curl` к `api.moysklad.ru` (метод по образцу PR РБ
  строки 711–721) выполнить, и какой вывод прислать назад.

## Вне scope этой задачи
- Добавление `entity/loss` в `cf-finance`, резолв UUID статей, полный `EXCLUDE_EXPENSE_IDS` — это **Q-9/Q-10**
  (прод-код/факты, отдельные задачи; Q-10 зависит от Q-3).
- Любой прод-код пайплайна.
- RECON-класс (выручка / «В пути») — `DEFER` по ADR-005.
- Широкое наполнение прочих доков — это M-P4.

## В конце сессии
Выдай ОДИН SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
В `STATE_PATCH`: Q-8 → closed/partial; «Текущий фокус» → M-P4 (или следующий discovery); updated_at.
