# TASK BRIEF · T-FACTS-FLOW-RESUME-CONFIRM

**Класс задачи (ADR-076):** A
<Read-only проверка одним `bq query`; ничего не пишется в `core.*`/`stg_msklad.*`/живые конфиги.
Совпадает с `07_STATE.md:747` таблицы «Мандат Claude Code: класс задач».>

**Параллель (ADR-082 §1, уточнён `ADR-083 §1`):** да
<Совпадает с `07_STATE.md:747`. Перед любым парным запуском — `bash tools/parallel_check.sh
FACTS-FLOW-RESUME-CONFIRM <TASK2>` (`ADR-082 §1`).>

**Файлы на запись** (полный список; на нём МЕХАНИЧЕСКИ проверяется пересечение при параллельном
запуске — `tools/parallel_check.sh`, `ADR-083 §1`):
- `reference/facts_flow_resume_confirm_2026-08-05.md` — артефакт замера (провенанс, запрос, вывод, вердикт)
- `reference/_inbox/session_FACTS-FLOW-RESUME-CONFIRM_2026-08-05.md` — session-блок (буфер, `ADR-080`)

## Роль
Ты — исполнитель проекта. Законы — `CLAUDE.md`, стандарты — `05_CONVENTIONS.md` Части I и II.
Модель исполнения: ты исполняешь сам (`ADR-082 §2`). Не-идемпотентное — `deploy`, `run`, `update`,
прод-`bq query` на запись, `git push` — только отдельным действием после явного подтверждения
владельца, никогда в связке с диагностикой. Эта задача не содержит не-идемпотентных операций —
только `bq query` на чтение. `Done` — только по подтверждённому логу, не по `rc=0`
(`05_CONVENTIONS.md ★ Успех инструмента ≠ факт`). Работаешь в СВОЁМ рабочем дереве и коммитишь в
СВОЮ ветку (`ADR-081 §6`). `07_STATE`, `06_DECISIONS_LOG` и `06_INDEX` не правишь: session-блок
кладёшь файлом в `reference/_inbox/`.

## Цель
Проверить контрольную точку `ADR-121 §7` («утро `2026-08-05`»): появились ли в
`core.fact_sales_profit` строки за сутки `2026-08-01…2026-08-04` (Бишкек) и сдвинулся ли
`MAX(_loaded_at)` этой таблицы относительно замера `FACTS-FLOW-RESTORE-ADJ` (`2026-08-04T07:41…07:49Z`).
Зафиксировать read-only вердикт «прошёл» / «не прошёл» и записать его в `/reference` — без
исполнения любого из вариантов (а)/(б)/(в) `ADR-121 §6`, они не входят в эту задачу.

## Context-to-load (обязательно прочитать перед работой)
- `_METHOD`, `00_CHARTER`, `05_CONVENTIONS`, `07_STATE` (всегда, читаются с диска по именам)
- `06_DECISIONS_LOG.md` — `ADR-121` целиком (контекст решения, формула чека, ожидаемый исход,
  крайний срок вмешательства `2026-08-07`)
- `06_DECISIONS_LOG.md` — `ADR-113` (источники `target_rev`/`ma7`/`freshness`, staging vs core)
- `reference/facts_flow_restore_adj_2026-08-04.md` — полный провенанс замера, который эта задача
  проверяет по факту: формула гейта (`§2`), прогноз по датам (`§3`), состояние staging и core на
  момент замера (`§4`, `§5`), рекомендация и точная формулировка контрольной точки (`§7`)
- `reference/_scratch_FACTS-FLOW-RESTORE-ADJ_2026-08-04/step1_gate_arithmetic.sh` и
  `step1_run.log` — образец запросов к `core.fact_sales_profit` (шаги 1b/1c/1d), переиспользуй их
  форму вместо изобретения новой
- `07_STATE.md` §«Открытые вопросы» — строки `FACTS-FLOW-RESUME-CONFIRM` и `FACTS-BACKFILL`
  (крайний срок вмешательства `2026-08-07`, вердикт «досев не требуется» условен на этом замере)
Если чего-то из списка нет в контексте → выдай `CONTEXT GAP` и остановись.

## Входы
- Проект BigQuery: `msklad-bi-prod`, таблица `core.fact_sales_profit` (поле `transaction_date`,
  поле `_loaded_at` — использовалось соседним замером `step2_staging_liveness.sh` на staging, для
  core применить аналогично).
- Ожидание из `ADR-121 §4/§7`: прогон `2026-08-04T18:00:02Z` должен был провести чек `drift_check`
  (`ma7=5 558 128,17` против `08-04` в staging `≥1 136 030,01`, отношение `0,2044` на момент
  замера, порог будней `0,10`) и, при успехе, промоутнуть `MERGE` окном 7 суток от `2026-07-28`,
  занося пропущенные `2026-08-01` и `2026-08-03` тем же прогоном.
- Личность для `bq`/`gcloud`: та же, что использовалась `FACTS-FLOW-RESTORE-ADJ`
  (`ilyasbazarov4@gmail.com`) — подтвердить `gcloud auth list` в начале и в конце скрипта
  (`05_CONVENTIONS.md §Доставка — условие, не рекомендация`, п.4).

## Шаги
1. Один bash-скрипт, доставка файлом (`bash script.sh > run.log 2>&1; cat run.log`,
   `05_CONVENTIONS.md §Доставка`), UTC-якорь и подтверждение личности первой И последней командой.
2. Запрос 1 — присутствие строк за проверяемые сутки в `core.fact_sales_profit`:
   ```sql
   SELECT transaction_date, COUNT(*) AS n_rows, ROUND(SUM(revenue_kgs), 2) AS revenue_kgs
   FROM `msklad-bi-prod.core.fact_sales_profit`
   WHERE transaction_date BETWEEN '2026-08-01' AND '2026-08-04'
   GROUP BY transaction_date
   ORDER BY transaction_date
   ```
   Печатать выданные строки с номерами, не только `rc`/агрегат (`ADR-044`, `★ Успех инструмента ≠ факт`).
3. Запрос 2 — сдвиг `MAX(_loaded_at)` относительно замера `FACTS-FLOW-RESTORE-ADJ`:
   ```sql
   SELECT MAX(_loaded_at) AS max_loaded_at, COUNT(DISTINCT _loaded_at) AS n_distinct_loaded_at
   FROM `msklad-bi-prod.core.fact_sales_profit`
   ```
   Если у `core.fact_sales_profit` нет колонки `_loaded_at` под этим именем — проверить фактическую
   схему (`bq show`) и не подставлять правдоподобное имя вместо гэпа (`★ Anti-improvisation`).
4. Сравнить вывод с ожиданием шага «Входы»: все четыре даты присутствуют с ненулевым `n_rows`,
   `MAX(_loaded_at)` позже `2026-08-04T07:41:07Z` (якорь замера `FACTS-FLOW-RESTORE-ADJ`) →
   вердикт «прошёл». Любая из дат отсутствует или `n_rows=0` → вердикт «не прошёл» — зафиксировать
   ЧТО именно отсутствует, не интерпретировать причину (диагностика следующего шага не входит сюда).
5. Записать `reference/facts_flow_resume_confirm_2026-08-05.md`: провенанс (UTC-якорь, личность,
   путь к скрипту/логу в `/reference/_scratch_FACTS-FLOW-RESUME-CONFIRM_2026-08-05/`, `ADR-043` —
   не убирать), таблица результата запроса 1, значение запроса 2, явный вердикт «прошёл»/«не
   прошёл» одной строкой в начале артефакта.

## Критерии приёмки (Acceptance)
- Оба запроса выполнены read-only против `msklad-bi-prod`, вывод показан построчно с номерами
  (не только `PASS`/`FAIL`).
- Вердикт «прошёл»/«не прошёл» зафиксирован явно и обоснован сравнением с ожиданием из «Входы»,
  а не догадкой.
- Артефакт `reference/facts_flow_resume_confirm_2026-08-05.md` самодостаточен: следующая сессия
  (постановка (а)/(б) при отрицательном исходе, либо закрытие строки при положительном) не обязана
  перечитывать `FACTS-FLOW-RESTORE-ADJ`, чтобы понять результат ЭТОЙ проверки.
- Личность (`gcloud auth list`) и UTC-якорь (`date -u`) — первой и последней командой скрипта.

## Что вернуть человеку (Return-this)
- Скрипт + `run.log` в `reference/_scratch_FACTS-FLOW-RESUME-CONFIRM_2026-08-05/`.
- `reference/facts_flow_resume_confirm_2026-08-05.md` с вердиктом.
- Точная команда запуска: `bash reference/_scratch_FACTS-FLOW-RESUME-CONFIRM_2026-08-05/check.sh > reference/_scratch_FACTS-FLOW-RESUME-CONFIRM_2026-08-05/run.log 2>&1; cat reference/_scratch_FACTS-FLOW-RESUME-CONFIRM_2026-08-05/run.log`

## Вне scope этой задачи
- Исполнение любого из вариантов `ADR-121 §6` (ручной промоут, правка порога/логики
  `drift_check`, деплой) — при отрицательном исходе постановка (а)+(б) уходит отдельным
  архитекторским ходом, не этой сессией.
- Установление ПРИЧИНЫ тихих суток `2026-08-01…08-03` — вопрос владельцу закрыт гипотезой
  (`ADR-121 §5`), эта задача её не проверяет.
- Правка `03_PIPELINE_SPEC.md §DQ`, `reference/code/cf-dq/`, любой деплой.
- `PARITY-STOCK-ROWWISE` и любая другая параллельная задача очереди — не эта сессия.

## В конце сессии
Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
STATE_PATCH обязан закрыть строку `FACTS-FLOW-RESUME-CONFIRM` в `07_STATE §Открытые вопросы`
(вердикт → DONE при «прошёл», либо статус с указанием крайнего срока `2026-08-07` и постановкой
следующей задачи при «не прошёл») и обновить стенд-ап пятью строками (`ADR-084 §1`).
