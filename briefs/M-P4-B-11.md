# TASK BRIEF · M-P4-B-11 — скаффолд 11_INFRA_FACTS + перенос 11a

## Роль
Ты — разработчик проекта **holika**. Прочитай `_METHOD` + `05_CONVENTIONS`, затем действуй.
Ты ПИШЕШЬ док-патчи; человек РЕВЬЮИТ и КОММИТИТ. Ничего «от себя» — только по источнику.

## Цель
(1) Создать НОВЫЙ файл `11_INFRA_FACTS.md` — пустой скелет по составу из `00_CHARTER §карта документов`.
(2) Наполнить `11 §SQ` тремя audit-SQ (11a) из `reference/sql/README` + ADR-008.

## Context-to-load (человек пиннит SHA)
- `_METHOD` / `00_CHARTER` / `05_CONVENTIONS` / `07_STATE` — .../<SHA>/{_METHOD,00_CHARTER,05_CONVENTIONS,07_STATE}.md
- `04_ROADMAP` — .../<SHA>/04_ROADMAP.md  (M-P4-11a)
- `06_DECISIONS_LOG` — .../<SHA>/06_DECISIONS_LOG.md  (ADR-004 узел 11; ADR-008 §Решение 1)
- `reference/sql/README.md` — .../<SHA>/reference/sql/README.md  (Config ID / schedule / состояние 3 audit-SQ)
Нет чего-то → `CONTEXT GAP`, стоп.

## Входы
- `00_CHARTER §карта документов` стр.53: состав 11 = «URL/ревизии CF, Config ID SQ, расписания, IAM, секреты (имена)».
- `reference/sql/README` таблица: Config ID + schedule + состояние для `sq_audit_dim_{products,counterparties,employees}_snapshot`.

## Шаги
1. НОВЫЙ ФАЙЛ `11_INFRA_FACTS.md`: док-заголовок (LIVING) + ПУСТЫЕ секции строго по составу стр.53:
   `§CF (URL/ревизии)`, `§SQ (Config ID + расписания)`, `§IAM`, `§секреты (имена)`. Каждая — с пометкой источника-адреса, без выдуманного содержимого.
2. `11 §SQ` (11a): внести 3 audit-SQ — `Config ID · displayName · целевая таблица · schedule · состояние` из `reference/sql/README` (трассировка: ADR-008 §Решение 1 + PR-21). `sq_audit_dim_products_snapshot` — зафиксировать состояние **FAILED как есть** (не чинить, вне scope).
3. НЕ вносить прочие факты (CF-ревизии, cf-fx URL/секрет) — они придут из брифа A-03.

## Приёмка
- `11_INFRA_FACTS.md` существует, 4 пустые секции по составу стр.53, каждая с адресом-источником.
- `11 §SQ` содержит 3 audit-SQ с Config ID/schedule/состоянием; трассировка к ADR-008/PR-21; products-snapshot помечен FAILED as-is.
- 0 выдуманных значений (нет факта в источнике → секция остаётся пустым слотом с адресом).

## Return-this
- Новый файл `11_INFRA_FACTS.md` (drop-in) + SESSION-блок.

## Вне scope
- CF-ревизии/URL/секреты cf-fx (→ бриф A-03, приземляются в уже созданный 11).
- `10_OPS_PLAYBOOK` (не эта фаза). Схема датасета `audit` (Q-4), SQL audit-SQ в `/reference` (уже есть).

## В конце сессии
SESSION-блок по `05_CONVENTIONS` Часть III. STATE_PATCH: 11a → DONE; 11 заведён.
