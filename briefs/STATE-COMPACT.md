# TASK BRIEF · T-STATE-COMPACT

**Класс задачи (ADR-076):** A
<Правка `07_STATE.md`/`07_ARCHIVE.md`, локальный коммит, облака не касается — совпадает со строкой
`07_STATE.md:81` таблицы `§Мандат Claude Code: класс задач» (`Класс А, мандат постоянный`).>

**Параллель (ADR-082 §1, уточнён `ADR-083 §1`):** нет
<Совпадает со строкой `07_STATE.md:81`: «пересекается с любой пишущей сессией; идёт хвостом, не
параллелью». `tools/parallel_check.sh` для этой задачи не запускается — она по определению не идёт
рядом ни с одной другой пишущей сессией, а после неё/до неё.>

**Файлы на запись** (полный список; на нём МЕХАНИЧЕСКИ проверяется пересечение при параллельном
запуске — `tools/parallel_check.sh`, `ADR-083 §1`):
- `07_STATE.md` — удаление полностью закрытых строк GAP-реестра (после переноса), правка счётчика в
  «Стенд-ап», уборка строки `LS-QUERY-SNAPSHOT` из таблицы `§Мандат Claude Code: класс задач`
- `07_ARCHIVE.md` — append перенесённых строк дословно
- `reference/_inbox/` — session-блок этой сессии (файл `session_STATE-COMPACT_<date>.md`)

## Роль
Ты — исполнитель проекта. Законы — `CLAUDE.md`, стандарты — `05_CONVENTIONS.md` Части I и II.
Модель исполнения: ты исполняешь сам (`ADR-082 §2`). Не-идемпотентное — `deploy`, `run`, `update`,
прод-`bq query`, `git push` — только отдельным действием после явного подтверждения владельца,
никогда в связке с диагностикой. `Done` — только по подтверждённому логу, не по `rc=0`.
Работаешь в СВОЁМ рабочем дереве и коммитишь в СВОЮ ветку (`ADR-081 §6`). `07_STATE`,
`06_DECISIONS_LOG` и `06_INDEX` не правишь напрямую как «источник истины сборки» — правки этой сессии в
`07_STATE.md`/`07_ARCHIVE.md` являются рабочим содержимым самой задачи (её и только её тип задачи это
разрешает, `ADR-064`/`07_STATE.md:81`), но окончательный коммит в основную ветку и слияние всё равно
делает проход сборки (`_ASSEMBLER.md`), как и для любой другой сессии — ты коммитишь в СВОЮ ветку
`s/STATE-COMPACT`, не в `main`. Session-блок кладёшь файлом в `reference/_inbox/` как обычно.

## Цель
Уборка GAP-реестра `07_STATE.md §Открытые вопросы / GAP-реестр»: перенести в `07_ARCHIVE.md` дословно
каждую строку, которая на момент этой сессии полностью закрыта (CLOSED/DONE без открытого остатка,
привязанного к её собственному ID), и синхронно поправить зависимые места — счётчик в блоке
«Стенд-ап» и строку `LS-QUERY-SNAPSHOT` в таблице «Мандат Claude Code: класс задач» (обе правки
поимённо востребованы текстом `07_STATE.md`, см. «Входы»).

## Context-to-load (обязательно прочитать перед работой)
- `_METHOD`, `00_CHARTER`, `05_CONVENTIONS`, `07_STATE`  (всегда, читаются с диска по именам)
- `07_STATE.md` целиком — «Текущий фокус», «Мандат Claude Code: класс задач», «Открытые вопросы /
  GAP-реестр» (все 58 строк на момент генерации брифа — число обязано быть перечитано заново на старте
  сессии, не взято отсюда как факт), «Контрольные цифры», «Блокеры»
- `07_ARCHIVE.md` — формат переноса (append-секции по эпикам/темам, дословный перенос без перефразирования)
- `06_INDEX.md` строка `ADR-064` (правило компактности) и строка `ADR-084` (форма блока «Стенд-ап»,
  обязательный счётчик с названным знаменателем)
- Полный текст `ADR-064` и `ADR-084` из `06_DECISIONS_LOG.md` по номеру, если формулировка правила
  в `06_INDEX.md` недостаточна для конкретного случая переноса
Если чего-то из списка нет в контексте → выдай `CONTEXT GAP` и остановись.

## Входы
- Строка задачи в GAP-реестре, дословно (`07_STATE.md:167`):
  > | STATE-COMPACT | (`ADR-084`, 2026-07-30) Уборка реестра: полностью закрытые строки уходят в
  > `07_ARCHIVE.md` дословно по `ADR-064`. Подтверждённо закрыты `Q-53`, `Q-72`, `SOURCE-MAP-SALES`;
  > требуют квалификации `CC-SESSION-ISOLATION`, `Q-76`, `LAUNCH-RUNBOOK` (у последних двух есть
  > названные остатки, переносу не подлежат) | задача (класс A, механическая) | **READY.** Реестр на
  > 2026-07-30 несёт 49 строк, часть неживые. Приёмка: число строк реестра равно числу действительно
  > открытых, каждая перенесённая строка присутствует в `07_ARCHIVE.md` дословно. Идёт хвостом пишущей
  > сессии, не параллелью (пересекается по `07_STATE` с любой) |
- Строка `Q-86` (`07_STATE.md:170`), тоже адресована этой задаче: «Уезжает в `07_ARCHIVE.md` задачей
  `STATE-COMPACT`».
- Замечание в «Подробности для модели» (`07_STATE.md:41`): «Строка `LS-QUERY-SNAPSHOT` в таблице
  `§Мандат Claude Code: класс задач` осталась после закрытия задачи и подлежит уборке вместе с реестром
  (`STATE-COMPACT`)» — сама строка сейчас на `07_STATE.md:79`.
- Счётчик в «Стенд-ап» (`07_STATE.md:25`) на момент генерации брифа: «реестр открытых вопросов 58 строк
  до уборки `STATE-COMPACT`» — число в счётчике этой строки после переноса обязано отражать реальный
  остаток, посчитанный после правки, не переписано вручную «на глаз».
- **Число строк реестра на 2026-07-30, названное в самой строке задачи («49 строк»), устарело**: на
  момент генерации этого брифа (2026-07-31) реестр несёт 58 строк (счётчик пересчитан построчно этой
  сессией генерации). Расхождение 49 vs 58 — не ошибка задачи, а следствие того, что реестр рос между
  2026-07-30 и 2026-07-31 (новые строки `Q-86`…`Q-89` и задачи `LS-*`/`PARITY-SCD2-ATTRIBUTION`
  появились после того, как текст строки `STATE-COMPACT` был написан). Не примирять эти два числа —
  считать заново по живому файлу на старте сессии.
- Три строки, названные «подтверждённо закрыты» в тексте задачи (`Q-53`, `Q-72`, `SOURCE-MAP-SALES`) и
  одна дополнительная (`Q-86`) — это НЕ исчерпывающий список: текст задачи написан 2026-07-30, реестр с
  тех пор менялся. Задача обязана пройти ВСЕ 58 (на момент старта сессии — актуальное число) строк
  реестра заново и определить статус каждой по её собственному тексту в живом `07_STATE.md`, а не
  ограничиться четырьмя названными.
- Три строки, названные «требуют квалификации» (`CC-SESSION-ISOLATION`, `Q-76`, `LAUNCH-RUNBOOK`) —
  текст задачи прямо говорит «есть названные остатки, переносу не подлежат»: эти три НЕ переносятся
  этой сессией без дополнительного обоснования. Если при чтении живых строк остаток исчерпан фактом
  (как это уже случилось однажды с `Q-90`/`PIPE-BASE-SYNC`, см. `07_STATE.md` «Сборка (2026-07-31)»)
  — CONTEXT GAP владельцу, не самостоятельное решение переносить.

## Шаги
1. Перечитать `07_STATE.md §Открытые вопросы / GAP-реестр» целиком, построчно. Для каждой строки
   определить: несёт ли её текст явный маркер полного закрытия без открытого остатка (`CLOSED`, `DONE`,
   «снят», «закрыт фактом» — с указанием ADR/сессии, закрывшей вопрос) — критерий `ADR-064`: «Полностью
   закрытые Q/задачи/блокеры переезжают в `07_ARCHIVE.md` однострочной выжимкой... Полностью закрытые
   строки (CLOSED/DONE/СНЯТ без открытого остатка)».
2. Строку, где закрытие подтверждено, но её текст называет остаток/незакрытую часть (по аналогии с
   `CC-SESSION-ISOLATION`/`Q-76`/`LAUNCH-RUNBOOK` из текста задачи) — НЕ переносить; зафиксировать в
   session-блоке пунктом «Отклонения от плана» или отдельным пунктом, почему конкретная строка осталась.
3. Для каждой строки, подлежащей переносу: скопировать её текст ДОСЛОВНО (без перефразирования) в
   `07_ARCHIVE.md`, добавив её в существующую тематическую секцию либо заведя новую по аналогии с
   действующей структурой файла (проверить на месте, куда логично класть — секции по эпикам/темам уже
   есть в `07_ARCHIVE.md`, читать перед правкой). Убрать перенесённую строку из `07_STATE.md`.
4. После переноса всех подлежащих строк — пересчитать итоговое число строк GAP-реестра и обновить
   «Счётчик» в блоке «Стенд-ап» новым значением (замена, не дописывание — `ADR-084 §1`, форма пяти строк
   `05_CONVENTIONS.md` Часть III).
5. Убрать строку `LS-QUERY-SNAPSHOT` из таблицы `07_STATE.md §Мандат Claude Code: класс задач»
   (`07_STATE.md:79` на момент чтения) — задача `LS-QUERY-SNAPSHOT` DONE, её мандатная строка не несёт
   действующего смысла (сама задача не в GAP-реестре, а в статусах — проверить, куда она делась, прежде
   чем удалять таблицу-строку; если она уже в `07_ARCHIVE.md`, удаление мандатной строки безопасно; если
   нет — CONTEXT GAP).
6. Прогнать `bash tools/session_status.sh` перед коммитом, чтобы подтвердить дерево «чисто» относительно
   обычных проверок (буфер/деревья/ветки/origin) — сама эта задача не трогает `reference/_inbox/` до
   собственного session-блока, так что до его выдачи буфер должен оставаться пустым.

## Критерии приёмки (Acceptance)
- Число строк GAP-реестра в `07_STATE.md` после правки равно числу строк без явного маркера полного
  закрытия (все закрытые без остатка перенесены; ни одна открытая/DEFER/IN PROGRESS/READY строка не
  тронута).
- Каждая перенесённая строка присутствует в `07_ARCHIVE.md` ДОСЛОВНО (побайтово совпадающий текст,
  кроме служебной обвязки секции).
- Строки, названные в задаче как «требуют квалификации» (`CC-SESSION-ISOLATION`, `Q-76`,
  `LAUNCH-RUNBOOK`), либо остались нетронутыми, либо перенесены с явным обоснованием в session-блоке,
  почему остаток снят (не тихо).
- Блок «Стенд-ап» несёт актуальный счётчик реестра (замена, не дописывание, ровно пять строк формы).
- Строка `LS-QUERY-SNAPSHOT` убрана из таблицы «Мандат Claude Code: класс задач» ЛИБО оставлена с
  явным обоснованием, если проверка Шага 5 дала CONTEXT GAP.
- `bash tools/session_status.sh` перед коммитом сессии подтверждает «чисто» (RC 0) кроме собственного
  session-блока, который сессия кладёт в буфер последним действием.

## Что вернуть человеку (Return-this)
- Диф `07_STATE.md`/`07_ARCHIVE.md`, закоммиченный в свою ветку `s/STATE-COMPACT`.
- Session-блок файлом в `reference/_inbox/session_STATE-COMPACT_<date>.md` по форме `05_CONVENTIONS`
  Часть III, с явным списком перенесённых ID и явным списком оставленных (с обоснованием) в
  `SESSION_LOG` → «Сделано».

## Вне scope этой задачи
- Не адъюдицировать содержательно ни один открытый вопрос (эта задача не решает `Q-76`/`Q-84`/`Q-85`
  и т.п. по существу — только определяет, закрыты они формально или нет).
- Не трогать `06_DECISIONS_LOG.md`/`06_INDEX.md` (эта задача не заводит и не закрывает ADR).
- Не запускать `bash tools/parallel_check.sh` — задача идёт хвостом, не параллельно ни с чем.
- Не переносить строки таблиц «Статусы задач» (Epic M / Epic 1) — правило `ADR-025 §2` уже развело их
  от GAP-реестра, эта задача касается только `§Открытые вопросы / GAP-реестр` и указанной точечно
  мандатной строки `LS-QUERY-SNAPSHOT`.

## В конце сессии
Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
