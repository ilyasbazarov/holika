# TASK BRIEF · T-INFRA-FACTS-SWEEP

**Класс задачи (ADR-076):** A
<Чтение репо и облака (read-only), запись только в `/reference`, коммит без push.>

**Параллель (ADR-082 §1, уточнён `ADR-083 §1`):** да
<Класс A и полный набор файлов на запись объявлен ниже. Перед фактическим одновременным запуском
с другой сессией обязателен `bash tools/parallel_check.sh INFRA-FACTS-SWEEP <TASK2>`.>

**Файлы на запись** (полный список; на нём МЕХАНИЧЕСКИ проверяется пересечение при параллельном
запуске — `tools/parallel_check.sh`, `ADR-083 §1`):
- `reference/infra_facts_sweep_2026-08-01.md` — сводный артефакт: по одному вопросу на раздел, факт + напечатанная выдержка вывода
- `reference/_scratch_INFRA-FACTS-SWEEP_2026-08-01/` — сырые логи команд (SQL, `gcloud … describe`, `bq query`), провенанс

## Роль
Ты — исполнитель проекта. Законы — `CLAUDE.md`, стандарты — `05_CONVENTIONS.md` Части I и II.
Модель исполнения: ты исполняешь сам (`ADR-082 §2`). Не-идемпотентное — здесь таких шагов нет
(всё read-only `describe`/`SELECT`); тем не менее любую команду, дающую пустой/непарсимый вывод,
трактуй как гэп наблюдения, не как факт (`★ Успех инструмента ≠ факт`, `05 Часть I`).
Работаешь в СВОЁМ рабочем дереве и коммитишь в СВОЮ ветку (`ADR-081 §6`). `07_STATE`,
`06_DECISIONS_LOG` и `06_INDEX` не правишь: session-блок кладёшь файлом в `reference/_inbox/`.

## Цель
Закрыть одной сессией хвост из семи мелких read-only гэпов (`Q-6`, `Q-11`, `Q-12`, `Q-13`, `Q-14`,
`Q-47`, `Q-55`), зафиксированных в `07_STATE §Открытые вопросы / GAP-реестр`. По каждому — либо
факт с напечатанной выдержкой вывода в `reference/infra_facts_sweep_2026-08-01.md`, либо явно
названная причина, почему факт снять не удалось (метка без выдержки не засчитывается,
`ADR-021 §2`). Форма задачи прецедентна `ADR-076 §3c` и заведена `ADR-091 §10`.

## Context-to-load (обязательно прочитать перед работой)
- `_METHOD`, `00_CHARTER`, `05_CONVENTIONS`, `07_STATE`  (всегда, читаются с диска по именам)
- `06_DECISIONS_LOG.md` — `ADR-091 §10` (текст задачи целиком) точечно по номеру
- `11_INFRA_FACTS.md` — существующие записи по `cf-dq` (строка «актуальная ревизия… → GAP Q-6»),
  инвентарь Cloud Scheduler/Workflows, SQ-флот (location `asia-east1`, проект `420804682491`)
- `01_ARCHITECTURE.md` — узел `cf-alert` (GAP Q-12), узел `msklad-pipeline-weekly` (GAP Q-13),
  топология GCS raw → BQ (§«raw JSON (immutable)», бакет `msklad-raw-msklad-bi-prod`)
- `03_PIPELINE_SPEC.md §marts — логика мартов и ABC/XYZ` — текущие цифры X=39/Y=148/Z=439,
  волатильные, дом `07_STATE §контрольные цифры`
- `RUNBOOK_v8.md §11 «Полная пересборка core из raw»` — п.11.2/11.3 являются ЗАГОЛОВКАМИ БЕЗ
  КОМАНД (`«Очистить core → Запустить пересборку из GCS»`, дословно вся строка) — источник Q-14,
  реальной процедуры в замороженном источнике нет
- `07_ARCHIVE.md` — строка `B-10`/`Q-47`-соседство (полнота `core.fact_loss` = 128 строк) для
  контекста Q-55/Q-47
Если чего-то из списка нет в контексте → выдай `CONTEXT GAP` и остановись.

## Входы
Семь строк `07_STATE.md §Открытые вопросы / GAP-реестр` (цитируются дословно):

- **Q-6** (`07_STATE.md:164`): «Актуальная ревизия cf-dq после T-1-фикса (TD-DQ-REVISION)» — тип
  «факт» — метод «`gcloud functions describe cf-dq`». `11_INFRA_FACTS.md:72` уже несёт
  последнюю ИЗВЕСТНУЮ ревизию `cf-dq-00006-lac` с пометкой «дата предшествует T-1-фиксу
  2026-06-24, канон не зафиксирован, не выдавать за актуальную» — актуальную ревизию нужно снять
  живым `describe` и сравнить с этой.
- **Q-11** (`07_STATE.md:167`): «Бизнес-метрики (X/Y/Z распределение, покрытие) датированы
  2026-06-05 — устарели» — тип «референс-артефакт» — метод «свежая выгрузка метрик из BQ →
  `/reference`». Текущие цифры в `07_STATE §контрольные цифры`: X=39, Y=148, Z=439
  (`marts.abc_xyz`, схема — `03_PIPELINE_SPEC.md §ABC/XYZ`).
- **Q-12** (`07_STATE.md:168`): «`cf-alert` на схеме архитектуры, отсутствует в PR; URL/конфиг/
  Telegram — не документированы» — тип «факт» — метод «`gcloud functions describe cf-alert` +
  конфиг алертинга».
- **Q-13** (`07_STATE.md:169`): «`msklad-pipeline-weekly`: расписание известно, состав шагов — не
  задокументирован» — тип «факт» — метод «`gcloud workflows describe msklad-pipeline-weekly`».
  Расписание уже известно (`11_INFRA_FACTS.md:27`): `0 1 * * 0`, регион `asia-east1`.
- **Q-14** (`07_STATE.md:170`): «Процедура полной пересборки core из GCS raw (RB §11, шаги
  11.2–11.3) — заглушки» — тип «факт» — метод «восстановить процедуру (связано с Q-3)».
- **Q-47** (`07_STATE.md:183`): требование — «положить SQL/лог замера в `/reference` вместе с
  провенанс-коммитом» для утверждения «Loss: 0 из 128 документов за всю историю аккаунта в
  не-KGS валютах» (сессия `E1-T1-MECH-INGEST`, 2026-07-23). Задача НЕ переоткрывает DEFER-решение
  Q-47 — только укладывает артефакт под уже принятую метку.
- **Q-55** (`07_STATE.md:184`): три `COUNTIF` по `core.fact_loss` (128 строк) — уже точно названы
  дословно в тексте строки: `COUNTIF(project_id IS NOT NULL)`, `COUNTIF(sales_channel_id IS NOT
  NULL)`, `COUNTIF(agent_name IS NOT NULL)`. Условие переоткрытия `ADR-036` названо в этой же
  строке (любое ненулевое значение ⇒ `ADR-036` переоткрывается НОВЫМ ADR — этой сессии
  переоткрывать нельзя, только замерить и зафиксировать число).

Проект/регион (из `11_INFRA_FACTS.md`): проект `msklad-bi-prod`, регион Cloud Functions/Workflows/SQ
— `asia-east1`.

## Шаги
1. **Q-6.** `gcloud functions describe cf-dq --project=msklad-bi-prod --region=asia-east1 --gen2`
   (gen2-флаг сверить с формой соседних CF в `11_INFRA_FACTS.md`, если функция gen1 — без флага).
   Снять `updateTime`/ревизию, сравнить с `cf-dq-00006-lac` (предшествует T-1-фиксу 2026-06-24).
2. **Q-12.** `gcloud functions describe cf-alert --project=msklad-bi-prod --region=asia-east1`.
   Снять URL, env vars (Telegram chat_id/webhook, если видны как имена, не значения секретов),
   ревизию.
3. **Q-13.** `gcloud workflows describe msklad-pipeline-weekly --project=msklad-bi-prod
   --location=asia-east1`. Если поле `sourceContents` присутствует — распечатать состав шагов
   (имена вызываемых CF/URL в порядке).
4. **Q-14.** Read-only разведка (НЕ восстанавливать процедуру догадкой): проверить
   `reference/code/*/` на наличие загрузчика raw→core; проверить `01_ARCHITECTURE.md` на
   упоминание механизма записи `GCS: msklad-raw-msklad-bi-prod/` → `core`; при отсутствии кода —
   `bq query` по `INFORMATION_SCHEMA.JOBS_BY_PROJECT` на предмет исторических `LOAD`/`QUERY`
   заданий, пишущих в `core.*` из источника с признаком raw/GCS (по аналогии с приёмом из
   `reference/source_map_invoices_2026-07-30.md`, не забыть `--location`, `ADR-021 §2`). Если
   процедура так и не находится — зафиксировать явно как «процедура не документирована нигде в
   репо и не восстановлена этим замером» (не гейт, не догадка), а не оставить как есть.
5. **Q-47.** Заново выполнить (или найти в `reference/_scratch_*` прежней сессии `E1-T1-MECH-INGEST`
   и перепроверить свежим прогоном) `SELECT COUNT(*) FROM core.fact_loss WHERE currency_code !=
   'KGS'` (имя колонки валюты сверить со схемой `core.fact_loss` перед запуском, не угадывать) и
   `SELECT COUNT(*) FROM core.fact_loss`. Положить точный SQL и полный вывод в
   `reference/_scratch_INFRA-FACTS-SWEEP_2026-08-01/`, сослаться на них из сводного артефакта.
6. **Q-55.** Три `COUNTIF` из текста строки одним запросом:
   `SELECT COUNTIF(project_id IS NOT NULL) AS project_id_nn, COUNTIF(sales_channel_id IS NOT NULL)
   AS sales_channel_id_nn, COUNTIF(agent_name IS NOT NULL) AS agent_name_nn, COUNT(*) AS total FROM
   core.fact_loss`. Напечатать числа. НЕ трогать `ADR-036` и SQL марта независимо от результата —
   переоткрытие названо явно как «не эта задача» (`07_STATE.md:184`).
7. **Q-11.** Свежая выгрузка распределения X/Y/Z из `marts.abc_xyz`:
   `SELECT xyz_class, COUNT(*) FROM marts.abc_xyz GROUP BY xyz_class ORDER BY xyz_class` (плюс
   `abc_class` тем же приёмом, если колонка присутствует по схеме `03_PIPELINE_SPEC.md`). Сравнить
   с текущими цифрами `07_STATE` (X=39, Y=148, Z=439) — совпадение или расхождение фиксируется как
   факт, не сглаживается.
8. Собрать все семь результатов в `reference/infra_facts_sweep_2026-08-01.md`: по одному разделу
   на вопрос (`Q-6`…, `Q-55`), в каждом — команда/запрос дословно + выдержка вывода + однострочный
   вывод (факт снят / гэп остаётся и почему).

## Критерии приёмки (Acceptance)
- По каждому из семи вопросов (`Q-6`, `Q-11`, `Q-12`, `Q-13`, `Q-14`, `Q-47`, `Q-55`) в
  `reference/infra_facts_sweep_2026-08-01.md` есть либо факт с напечатанной выдержкой реального
  вывода команды/запроса, либо явно названная причина, почему факт не снят. Метка вида
  «сделано»/«проверено» без выдержки не засчитывается (`ADR-021 §2`).
- Все сырые логи и SQL, на которые ссылается сводный артефакт, лежат в
  `reference/_scratch_INFRA-FACTS-SWEEP_2026-08-01/`.
- `Q-47` не переоткрывает DEFER-решение (`E1-T1-INGEST-ADJ`), только кладёт артефакт.
- `Q-55` не переоткрывает `ADR-036`, даже если `COUNTIF` дал ненулевое значение — переоткрытие
  фиксируется как факт для владельца/архитектора, SQL марта этой сессией не правится.

## Что вернуть человеку (Return-this)
- `reference/infra_facts_sweep_2026-08-01.md` — сводный артефакт по семи вопросам.
- `reference/_scratch_INFRA-FACTS-SWEEP_2026-08-01/` — сырые логи/SQL, провенанс.
- Session-блок по `05_CONVENTIONS` Часть III файлом в `reference/_inbox/`.

## Вне scope этой задачи
- `Q-39` (не входит в хвост, `ADR-091 §10`) — форма фикса owner-gated, не трогать.
- Переоткрытие `ADR-036` по итогам `Q-55` — только фиксация факта, решение архитектора отдельно.
- Переоткрытие DEFER-статуса `Q-47` — только укладка артефакта.
- Восстановление/реализация процедуры пересборки core (Q-14) — только разведка «что уже есть»; не
  писать новый скрипт пересборки, не деплоить, не исполнять `bq query` над `core` за пределами
  read-only `SELECT`.
- Любая правка `11_INFRA_FACTS.md`/`01_ARCHITECTURE.md` — эта сессия пишет только в `/reference`
  (класс A, `07_STATE §Мандат`); перенос фактов в 11/01 — отдельным шагом сборки/архитектора.

## В конце сессии
Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
