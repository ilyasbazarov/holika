# TASK BRIEF · SALES-DOCUMENT-OWNER-DEPLOY

**Класс задачи (ADR-076):** B — деплой Cloud Function `cf-facts` (доработанный патч сотрудника-владельца
документа, `document_owner_employee_id`) плюс `ALTER TABLE core.fact_sales_profit ADD COLUMN`.

**Мандат:** НЕ выдан на момент генерации этого брифа. `07_STATE.md §Мандат Claude Code` несёт строку
`SALES-DOCUMENT-OWNER-DEPLOY, деплой | B | нет | НЕ выдан | деплой cf-facts плюс ALTER TABLE живой
core.fact_sales_profit; выдаётся отдельным поимённым ADR по факту готовности доработки (ADR-109 §1)`;
`07_STATE.md §Открытые вопросы`, строка `SALES-DOCUMENT-OWNER-DEPLOY` (`OPEN`), несёт тот же статус
дословно. Готовность ДОРАБОТКИ (класс A, `SALES-DOCUMENT-OWNER-DEPLOY, доработка патча`) САМА
подтверждена (`ADR-136 §2`, применена — обе ветки `MERGE` несут колонку), но это НЕ мандат на деплой —
это предусловие, по факту которого мандат заводится ОТДЕЛЬНЫМ поимённым ADR (форма — прецедент
`ADR-092`/`ADR-108`/`ADR-117`/`ADR-118`/`ADR-135`). **Этот бриф ГЕНЕРИРУЕТСЯ по прямому предписанию
`_GENERATOR.md §4a` («класс B без указанного ADR, выдавшего мандат» → бриф генерируется, но с явным
гейтом в шапке), но исполнение блокировано до появления такого ADR.** Шаги 1–4 (класс A/read-only,
диагностика/подготовка ветки, ниже) мандата не требуют; шаг 5 и далее (объявление действия, `ALTER
TABLE`, сам деплой) не стартуют без поимённого мандата.

**Параллель (ADR-082 §1):** нет и не может быть — класс B не параллелится никогда.

**Файлы на запись** (полный список; форма `08`, машиночитаемая — `tools/parallel_check.sh`):
- `reference/code/cf-facts/MANIFEST.md` — запись ревизия ↔ коммит по результату read-back
- `reference/sales_document_owner_deploy_<date>.md` — артефакт задачи
- `reference/_scratch_SALES-DOCUMENT-OWNER-DEPLOY_<date>/` — скрипты и сырые логи (провенанс, `ADR-043`)
- `reference/_inbox/session_SALES-DOCUMENT-OWNER-DEPLOY_<date>.md` — session-блок
- код-репозиторий `holika-prod` (вне репо `holika`): ветка `deploy/cf-facts-<дата>-document-owner`
- `core.fact_sales_profit` (вне репо `holika`, живая таблица BigQuery): `ALTER TABLE … ADD COLUMN`

## Роль

Ты — исполнитель проекта. Законы — `CLAUDE.md`, стандарты — `05_CONVENTIONS.md` Части I и II.
Не-идемпотентное (`push` ветки/`master`, `ALTER TABLE`, `deploy`, `MERGE`/`weekly`-прогон в
`core.fact_sales_profit`) — только отдельным действием после явного подтверждения владельца, никогда
в связке с диагностикой (`ADR-055 §2`). Перед КАЖДЫМ таким подтверждением — ОТДЕЛЬНОЕ сообщение в чат:
что исполняется, на каком объекте, чем откатывается (`ADR-077 §6`, `deploy_procedure §3 Шаг 4`). `Done`
— только по подтверждённому логу, не по `rc=0` (`★ Успех инструмента ≠ факт`, `ADR-021 §2`). Работаешь
в СВОЁМ рабочем дереве, коммитишь в СВОЮ ветку; `07_STATE`/`06_DECISIONS_LOG`/`06_INDEX` не правишь —
session-блок кладёшь файлом в `reference/_inbox/`.

## Цель

Задеплоить готовый (не задеплоенный) патч сотрудника-владельца документа (`document_owner_employee_id`
в обеих ветках `MERGE` — `SELECT`/`INSERT`/`UPDATE SET`, `ADR-136 §2`) в `cf-facts` по процедуре
`reference/deploy_procedure_2026-08-03.md` («Процедура деплоя Cloud Function — только из код-репо,
вариант Б», `05_CONVENTIONS.md` Часть II), подтвердить read-back'ом, что развёрнутый архив равен ветке,
и функционально проверить оба затронутых режима (`hourly`/основной ежедневный, и — ОБЯЗАТЕЛЬНО первым
прогоном после деплоя — `weekly` с `window_days=90`) БЕЗ порчи остальных режимов (`promote`/`returns`/
`purchases`/`perimeter`/`perimeter_promote`). До деплоя кода — `ALTER TABLE core.fact_sales_profit ADD
COLUMN document_owner_employee_id STRING` (предусловие `ADR-136 §4`, `1`). Это ТРЕТЬЕ применение
процедуры вариант Б к `cf-facts` (первое — `SALES-INGEST-PATCH-DEPLOY`, ревизия `cf-facts-00008-zen`,
2026-08-07; второе — `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY`, ревизия `cf-facts-00009-tul`, 2026-08-07)
и деплой ПОВЕРХ этой же базы, а не с нуля.

## Context-to-load (обязательно прочитать перед работой)

- `_METHOD`, `00_CHARTER`, `05_CONVENTIONS` (обе Части, включая процедуру деплоя), `07_STATE`
- `reference/deploy_procedure_2026-08-03.md` — полный текст процедуры, шаги 0–9 и чек-лист
- `reference/sales_document_owner_deploy_prep_2026-08-07.md` — что именно построено доработкой
  (`ADR-136 §2`): точный `git diff` (ровно одна добавленная строка `UPDATE SET`), приёмка колонок
- `reference/sales_document_owner_ingest_2026-08-07.md` — базовый патч (`STAGING_SCHEMA`, `SELECT`,
  `INSERT`), провенанс поля-источника (`entity/demand.owner.meta.href`), открытый вопрос `Q-105`
  (версионирование `owner`) — НЕ закрывается этой задачей
- `reference/code/cf-facts/MANIFEST.md` — текущий снапшот; БАЗОВАЯ ревизия для этого деплоя —
  `cf-facts-00009-tul` (`generation 1786115536540209`, `updateTime = 2026-08-07T15:13:10.253085704Z`,
  `master` код-репо на коммите `7e039bd`) — НЕ `cf-facts-00007-xir`, названная в `11_INFRA_FACTS.md`
  (устарела дважды: сначала `SALES-INGEST-PATCH-DEPLOY`, затем `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY`,
  обе 2026-08-07)
- `11_INFRA_FACTS.md` §cf-facts — параметры живого деплоя (регион `asia-east1`, service-account
  `etl-sa@msklad-bi-prod.iam.gserviceaccount.com`, `--memory=2048MB --timeout=540s`, секрет
  `MSKLAD_TOKEN`); ревизия/`generation`, названные там, УСТАРЕЛИ — брать текущие из `MANIFEST.md`
- `07_GAPS.md` точечно: `SALES-DOCUMENT-OWNER-DEPLOY`
- `06_DECISIONS_LOG.md` точечно: `ADR-136` (разрез задачи на доработку/деплой, предусловие `ALTER
  TABLE`, режим первого прогона `weekly`/`window_days=90`, окно `2026-09-29`), `ADR-135 §4` (адъюдикация
  дефекта досева, откуда пришло предусловие `UPDATE SET`), `ADR-125 §5` (окно `window_days=90`
  закрывается `2026-09-29`), `ADR-109 §1` (прецедент «готовность зафиксирована коммитом» ≠ мандат),
  `ADR-065` (деплой только из код-репо), `ADR-040` (гигиена архива), `ADR-030`/`ADR-101 §7` (C1/C2)
- `02_ERP_CONTRACTS.md` — новая колонка `core.fact_sales_profit` этой задачей НЕ документируется
  контрактом (решение после появления поля в проде, вне scope, см. «Вне scope»)

Чего-то нет в контексте → `CONTEXT GAP` и стоп.

## Входы — что уже установлено и не переустанавливается

1. **Доработка построена, применена и подтверждена приёмкой (`ADR-136 §2`, класс A, DONE):** в
   `reference/code/cf-facts/bq_ops.py`, `_build_merge_sql`, ветка `WHEN MATCHED THEN UPDATE SET`,
   добавлена ровно одна строка `T.document_owner_employee_id = S.document_owner_employee_id`. Список
   колонок `UPDATE SET` теперь 17 (было 16), список `INSERT` не менялся (22, колонка уже была внесена
   базовым патчем `SALES-DOCUMENT-OWNER-INGEST`). `STAGING_SCHEMA`, `SELECT` внутри `USING`, явный
   `INSERT (колонки) VALUES (…)` (`C1`, `ADR-030`) и `fetch_demands.py` этой доработкой не тронуты.
   `_build_perimeter_merge_sql` не тронута — источники периметра на предмет поля `owner` не
   проверялись, это отдельный вопрос вне scope.
2. **Три требования к приёмке названы заранее (`ADR-136 §4`, дословно):**
   (1) `ALTER TABLE core.fact_sales_profit ADD COLUMN document_owner_employee_id STRING` — **ДО**
       деплоя кода (тип колонки — `STRING`, из `STAGING_SCHEMA`, `bq_ops.py:57`);
   (2) доработка `UPDATE SET` уже применена ДО деплоя — условие выполнено (Вход 1);
   (3) первый прогон ПОСЛЕ деплоя — режим `weekly`, `window_days=90` (иначе июль-2026 не попадает в
       окно `MERGE`); окно закрывается `2026-09-29` (`ADR-125 §5`), после чего июль штатным путём не
       досеется.
3. **Семантика поля после доработки — «владелец на момент последнего прогона», не «на момент первой
   загрузки» (`ADR-135 §4`, названная и не скрытая цена решения).** Какое из двух значений режет отчёт
   МойСклада «по сотрудникам» — репо не устанавливает (`Q-105`, открыт, эта задача его не закрывает).
4. **База патча — актуальная живая ревизия, уже несущая ДВА предыдущих деплоя (`SALES-INGEST-PATCH-
   DEPLOY` + `SALES-PERIMETER-CHANNEL-DECIDE-DEPLOY`):** `cf-facts-00009-tul`, архив
   `generation 1786115536540209`, `updateTime = 2026-08-07T15:13:10.253085704Z`
   (`reference/code/cf-facts/MANIFEST.md`). Снапшот `reference/code/cf-facts/` после доработки несёт
   **ровно один** недеплоенный патч — сотрудник документа; патч метки канала уже деплоен той же
   ревизией. Патч этой задачи ложится ПОВЕРХ уже задеплоенных режимов, это инкрементальная правка тех
   же файлов (`bq_ops.py`, `fetch_demands.py`), не новый файл.
5. **Код-репозиторий:** `https://github.com/ilyasbazarov/holika-prod`, ветка по умолчанию `master`,
   раскладка `cf-facts/`. `master` после прошлого деплоя несёт коммит слияния `7e039bd`. Актуальный
   `HEAD` снимается заново на шаге 2 (не берётся из этого брифа буквально —
   `deploy_procedure_2026-08-03.md §1` требует свежей сверки).
6. **`ALTER TABLE` — операция над живой прод-таблицей `core.fact_sales_profit`, вне репозитория
   `holika`, не-идемпотентна по духу правила `05_CONVENTIONS` (даже если сам DDL безопасен на
   повторный прогон — `ADD COLUMN IF NOT EXISTS` не гарантирован BigQuery DDL без явной проверки):
   read-only проверка ОТСУТСТВИЯ колонки — обязательный шаг ДО `ALTER TABLE`, не после.**
7. **Ожидаемый эффект — новая ненулевая колонка у СТАРЫХ и НОВЫХ строк июля-2026,** после чего разрез
   продаж по сотруднику через `document_owner_employee_id` становится физически возможным (предмет
   `ADR-128`/`Q-78`); эта задача НЕ проверяет числовое схождение разреза с эталоном — это отдельная
   задача после деплоя (не входит в scope, см. «Вне scope»).

## Шаги

**Шаг 1 (класс A, read-only). Свежая проверка чистоты дерева и актуальности патча.** Подтвердить, что
`reference/code/cf-facts/bq_ops.py` в РАБОЧЕМ дереве этой сессии несёт ровно одну добавленную строку
`UPDATE SET` (Вход 1, приёмка `reference/sales_document_owner_deploy_prep_2026-08-07.md`) и синтаксически
валиден (`python3 -m py_compile`, свежим прогоном).

**Шаг 2 (класс A). Свежий снапшот живой ревизии и сверка с `master`.** По процедуре, шаг 1:
`describe` живой ревизии `cf-facts` → `gsutil cp` архива по `generation` → распаковка →
`shasum -a 256` → побайтовая сверка с `cf-facts/` в `master` код-репо. Ожидаемая база — ревизия
`cf-facts-00009-tul` (Вход 4); расхождение с ожиданием — СТОП, разбирается до любого деплоя.

**Шаг 3 (класс A). Ветка и перенос патча.** `deploy/cf-facts-<дата>-document-owner` от `master`; патч
переносится **по diff** против `master` (не копированием файлов из этого репо — снапшот
`reference/code/cf-facts/` мог отклониться от того, что реально снято шагом 2). `.gcloudignore`
обязан исключать `*.bak`, `__pycache__/`, `.DS_Store`, `src.zip`, разовые patch-скрипты (`ADR-040`).
Сплошным поиском по патчу подтвердить отсутствие секретов (репозиторий публичный) — печатать совпавшие
строки, пустая выдача фактом не является.

**Шаг 4 (класс A → подтверждение). Коммит в ветку, `push` ветки.** `master` не трогается.

**Шаг 5 (подтверждение владельца, ДО `ALTER TABLE` и ДО деплоя). Объявление действия отдельным
сообщением (`ADR-077 §6`).** Что именно исполняется («`ALTER TABLE core.fact_sales_profit ADD COLUMN
document_owner_employee_id STRING`, затем деплой ревизии `cf-facts` с доработанным патчем сотрудника-
владельца документа»), на каком объекте (`core.fact_sales_profit`; `cf-facts`, `asia-east1`), чем
откатывается (`ALTER TABLE … DROP COLUMN document_owner_employee_id` — новая колонка без потребителей,
откат безопасен; для кода — деплой предыдущей ревизии `cf-facts-00009-tul` по имени). Это ТОТ САМЫЙ
момент, где даётся поимённый мандат класса B на сам деплой — до него шаги 1–4 не требуют мандата, шаг 6
и далее требуют. **Без предварительно выданного поимённого ADR (см. шапку брифа) этот шаг не наступает
вовсе — сессия останавливается ДО шага 5 и называет отсутствие мандата, не пытается получить его
явочным порядком в моменте.**

**Шаг 6a (класс B, read-only ДО `ALTER TABLE`). Подтвердить отсутствие колонки.**
`bq show --schema core.fact_sales_profit` (или `INFORMATION_SCHEMA.COLUMNS`) — печатается полный список
колонок; `document_owner_employee_id` в нём отсутствовать ОБЯЗАН (различитель для не-идемпотентности
Входа 6). Присутствует — СТОП, кто-то уже исполнил `ALTER TABLE` без записи в репо, разбирается прежде
чем продолжать.

**Шаг 6b (класс B). `ALTER TABLE`.** Одна команда, отдельным пастом, `date -u` и `gcloud auth list`
(или эквивалент для `bq`) первой И последней командой скрипта (`ADR-055 §4`). После — повторный
`bq show --schema`, колонка присутствует, тип `STRING`, напечатано.

**Шаг 7 (класс B). Деплой кода.** Одна команда, отдельным пастом, скриптом-файлом с логом в файл;
`date -u`/`gcloud auth list` первой И последней командой. `--source` указывает на `cf-facts/` выкаченной
ветки, и ни на что другое. Остальные параметры — из `describe` живой конфигурации (шаг 2 +
`11_INFRA_FACTS.md §cf-facts`, помня об устаревшей ревизии/`generation` в этом доке — Вход 4).

**Шаг 8 (класс B). Read-back архива.** `describe` новой ревизии → скачать архив по `generation` →
`shasum -a 256` всех файлов → сверить с веткой → убедиться, что грязи в новом архиве нет. Расхождение =
деплой неуспешен, ветка НЕ сливается, откат на `cf-facts-00009-tul` (код) — колонка БД откатом кода не
трогается, её откат отдельная команда шага 5, исполняется только если владелец решит откатывать целиком.

**Шаг 9 (класс B). Функциональная проверка — незатронутые режимы.** Прогон `hourly` (или ближайший
штатный вызов) и чтение лога: подтвердить, что режимы `promote`/`returns`/`purchases`/`perimeter`/
`perimeter_promote` не изменили поведение (эти файлы патчем не тронуты — `fetch_purchases.py`/
`fetch_returns.py`/`fetch_perimeter.py`/`helpers.py`/`config.py`/`main.py`, если их sha256 не менялся
против шага 2, отдельно не прогонять).

**Шаг 10 (класс B). Функциональная проверка — обязательный первый прогон `weekly`
(`window_days=90`).** Это НЕ опциональная проверка, а предусловие приёмки (`ADR-136 §4 (3)`): без него
июль-2026 не попадает в окно `MERGE`, и колонка у июльских строк останется `NULL`. Прогон, чтение лога.
До и после — прямой запрос `SELECT COUNT(*) FROM core.fact_sales_profit WHERE document_owner_employee_id
IS NOT NULL AND DATE(transaction_date) BETWEEN '2026-07-01' AND '2026-07-31'` (уточнить фильтр по
периметру/ветке `entity/demand`, если нужно исключить строки периметра, у которых поле не читалось,
Вход 1) — число печатается с арифметикой, рост от `0` (колонка только что создана) до измеримого
значения подтверждает эффект.

**Шаг 11 (класс A). Слияние и запись соответствия.** `merge --no-ff` ветки в `master`, `push`
(подтверждение владельца); в `reference/code/cf-facts/MANIFEST.md` — имя новой ревизии, `generation`
архива, sha256 файлов, время в UTC, SHA коммита код-репо, по образцу секции «Деплой `cf-facts` — метка
канала периметра продаж (2026-08-07)».

**При неуспехе любого шага 6b–10** — ветка НЕ сливается, откат кода на `cf-facts-00009-tul` по имени,
неудачная попытка фиксируется в `MANIFEST.md` наравне с удачной; откат колонки БД — отдельное решение
владельца (Вход 6, шаг 5).

## Критерии приёмки

- Шаг 6a: `document_owner_employee_id` отсутствовала в схеме `core.fact_sales_profit` ДО `ALTER TABLE`
  — напечатан полный список колонок.
- `ALTER TABLE` исполнен, колонка присутствует, тип `STRING`, подтверждено повторным `bq show`.
- Побайтовая сверка снапшота с `master` (шаг 2) напечатана — совпало/не совпало, по файлам; база
  подтверждена как `cf-facts-00009-tul`.
- `.gcloudignore` перед коммитом — подтверждено отсутствие `.bak`/`__pycache__`/`.DS_Store`/`src.zip`.
- Read-back (шаг 8): имя ревизии, `generation`, sha256 по файлам, отсутствие грязи — напечатаны.
- Незатронутые режимы (шаг 9) не изменили поведение — подтверждено сверкой sha256 нетронутых файлов
  либо прогоном лога.
- Обязательный `weekly` (`window_days=90`) прогнан ПОСЛЕ деплоя (шаг 10, не опущен, не заменён `hourly`);
  число строк июля-2026 с непустым `document_owner_employee_id` напечатано ДО и ПОСЛЕ с арифметикой.
- `MANIFEST.md` несёт запись ревизия ↔ коммит по результату read-back, не по сообщению команды деплоя.
- `master` код-репо содержит патч ТОЛЬКО после успешного read-back (шаг 8).
- Session-блок называет фактическое число заполненных строк июля-2026 после `weekly`-прогона как
  измеренный факт, не домысленный по ожиданию; отдельно называет открытым `Q-105` (эта задача его не
  закрывает).

## Что вернуть человеку

- Артефакт `reference/sales_document_owner_deploy_<date>.md`; первой фразой — задеплоено или нет, стоит
  ли колонка в `core.fact_sales_profit`, прогнан ли `weekly` и сколько июльских строк получили значение.
- Session-блок в `reference/_inbox/`, закрывающий `SALES-DOCUMENT-OWNER-DEPLOY` (обе строки —
  `доработка патча` уже DONE, эта сессия закрывает `деплой`); строка реестра `07_GAPS.md`
  `SALES-DOCUMENT-OWNER-DEPLOY` правится проходом сборки по факту session-блока, не этой сессией
  напрямую.

## Вне scope этой задачи

- Числовое схождение разреза продаж по сотруднику (через `document_owner_employee_id`) с эталоном
  МойСклада — отдельная задача после этого деплоя, не эта.
- Решение по `Q-105` (версионируется ли `owner` документа отдельно от документа) — этой задачей не
  снимается ни замером, ни решением.
- Правка `02_ERP_CONTRACTS.md` документированием новой колонки контрактом — отдельное решение владельца
  после деплоя.
- Правка `reference/sql/sq_marts_sales_overview.sql` или любой клиентской витрины под новое поле —
  отдельная задача, решение после появления поля в `core` (Вход 1).
- `_build_perimeter_merge_sql` и источники периметра на предмет поля `owner` — не проверяются, не
  правятся.
- `SALES-PERIMETER-PARITY-RECHECK` / любая пересверка реестра паритета — отдельная задача с отдельным
  мандатом, не эта; численный итог по сумме `core.fact_sales_profit` этим деплоем НЕ меняется (меняется
  только заполненность колонки), реестр паритета не переоткрывается.
- Любые правки `cf-finance`/`cf-dq`/`cf-inventory`.

## В конце сессии

Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
