# TASK BRIEF · E1-T1-MECH-CUTOVER

**Тип:** задача переключения прода (не-идемпотентная). Процедура наследуется от `ADR-038`.
**Протокол исполнения:** прежний — модель пишет скрипты, владелец запускает и возвращает логи. Мандат
Claude Code (`ADR-071 §1`) на эту задачу не распространяется; расширение не запрашивается.
**Сгенерирован:** генератор-чат по `briefs/_GENERATOR.md`, `curl` по SHA `e20f229ba0853ffe67541826c418ad2eb06cbbbe`.
**Предусловие `ADR-045 §3` ИСПОЛНЕНО** замером 2026-07-27T15:13:14Z…15:13:35Z (два прогона, лог у владельца;
Шаг 0 ниже кладёт его в репо). Верхняя граница окна переключения снята фактом.
**Апрув владельца на переключение получен 2026-07-27.** Он НЕ отменяет требования `ADR-055 §2`:
не-идемпотентная команда идёт отдельным пастом после подтверждения состояния, с повторным «да» в тот момент.

---

## ★ CONTEXT GAP, который обязан быть закрыт до Шага 2

Текст нового SQL, прошедшего приёмку в сессии `E1-T1-MECH`, **в репозитории отсутствует**: в
`/reference/sql/sq_marts_expenses.sql` @ `e20f229b` лежит ДЕЙСТВУЮЩАЯ (до-правочная) версия — 1359 байт,
`fact_payments` без `fact_loss`/`fact_commissionreportin`. Новый текст существует только как выдача той
сессии. Он подаётся владельцем в чат исполнения и **не принимается на слово**: пригодность доказывается
воспроизведением чисел на staging (Шаг 1), а не заявлением сессии, его породившей (`ADR-021 §2`).
Текста нет на руках → `CONTEXT GAP`, стоп, не реконструировать по описанию.

## Роль

Ты — исполнитель одной задачи. Пишешь скрипты, владелец запускает. Каждый шаг с записью — отдельным пастом
после подтверждённого состояния. Диагностику и действие в один блок не объединять (`ADR-055 §2`).

## Цель

Заменить SQL в живой конфигурации `sq_marts_expenses` на версию, объединяющую `fact_payments` +
`fact_loss` + `fact_commissionreportin`, с сохранённым откатом, немедленной проверкой и нетронутым
расписанием. Приёмка — сходимость прода с `marts.expenses_staging` в 0,00 и май-2026 = 10 232 903,20.

## Context-to-load (обязательно прочитать перед работой)

`curl` по SHA `e20f229ba0853ffe67541826c418ad2eb06cbbbe`:

- `https://raw.githubusercontent.com/ilyasbazarov/holika/e20f229ba0853ffe67541826c418ad2eb06cbbbe/_METHOD.md`
- `.../05_CONVENTIONS.md` (Часть I ★, Часть II — доставка)
- `.../07_STATE.md` (строки `E1-T1-MECH`, `Q-64`, `Q-21`, `FX-POLICY-EXPENSES`, контрольные цифры)
- `.../11_INFRA_FACTS.md` (§SQ стр.44–52)
- `.../briefs/E1-T1-MECH.md` (критерии приёмки, стр.224–231)
- `.../reference/sql/sq_marts_expenses.sql` (действующий текст — эталон отката)
- `.../06_INDEX.md`
- Точечно из `06_DECISIONS_LOG.md`: `ADR-032`, `ADR-033`, `ADR-034`, `ADR-036`, `ADR-037`, `ADR-038`,
  `ADR-039 §1`, `ADR-043`, `ADR-045 §4/§6`, `ADR-046`, `ADR-055 §1/§2/§3`, `ADR-062 §9`, `ADR-063 §4`
- `/reference/pnl_2026-05.md` — референс-оракул мая-2026

Чего-то нет → `CONTEXT GAP`, стоп. Первая строка дока не совпала с именем → стоп по `ADR-054`.

## Входы — факты замера 2026-07-27 (использовать, не перепроверять)

- Полное имя конфигурации: `projects/420804682491/locations/asia-east1/transferConfigs/6a22a243-0000-20fd-a458-883d24f4cad4`
  · `displayName: sq_marts_expenses` · `destinationDatasetId: marts` · `dataSourceId: scheduled_query`
  · владелец `ilyasbazarov4@gmail.com` · `userId 8522307959247193051` · `updateTime 2026-06-05T11:10:23.201634Z`.
- **`schedule` в выдаче ОТСУТСТВУЕТ** — это не «пустое поле», как записано в `11 §SQ` стр.49. Набор ключей
  выдачи: `dataSourceId, datasetRegion, destinationDatasetId, displayName, emailPreferences, name,
  nextRunTime, ownerInfo, params, scheduleOptions, scheduleOptionsV2, state, updateTime, userId`.
  `scheduleOptions: {}`; расписание живёт в `scheduleOptionsV2`. Отсюда — Шаг 3 (read-back).
- **`nextRunTime: 2026-07-28T11:10:00Z`** — верхняя граница окна.
- Фактическая каденция за окно `2026-07-26T15:13Z … 2026-07-27T15:13Z`: `marts.expenses` пересобиралась
  **ровно один раз** — `2026-07-27T11:10:01Z`, `WRITE_TRUNCATE`, задание
  `scheduled_query_6a66c595-0000-22d8-883f-883d24f52624`, под `ilyasbazarov4@gmail.com`. Задокументированный
  якорь ~11:10 UTC подтверждён фактом.
- **Второй путь пересборки (`cf-finance.trigger_marts()` ~21:00 UTC, `ADR-038`) в окне НЕ наблюдён:** ни
  одного задания с целью `marts.expenses` около 21:00Z 26 июля. Наблюдение НЕ интерпретируется (`ADR-045 §1`):
  «пути нет» не утверждается, «путь есть» тоже. Для тайминга это означает лишь одно — рассчитывать окно по
  наблюдённому якорю 11:10 UTC и не полагаться на предположение о ночном пинке.
- `marts.expenses_staging` собиралась `2026-07-26T19:39:37Z` заданием `bqjob_r4a62e606d0d607b2_…` (ручной
  запрос, не DTS) — то есть текущее содержимое staging относится к тому прогону.
- Побочный факт (закрывает аномалию `Q-64` по `sales_overview`): `sq_marts_sales_overview`
  `69ff34b4-0000-2b2b-a390-14c14ef7af10` имеет `schedule: every 2 hours`. Двухчасовая каденция — конфиг, а не
  ручные прогоны. Наблюдение `Q-64` про `marts.expenses` в 14:41 UTC относится к 26 июля ДО начала окна ⇒
  этим замером не покрыто, остаётся как есть.
- Контрольные цифры приёмки (`07_STATE §Контрольные цифры`, `ADR-006` seed #2): май-2026 расходы
  **10 232 903,20** при разрыве **0,00**; постатейно — `Списания` 411 838,94, `Маркетинг и реклама`
  1 026 901,54, `Прочие расходы` 83 985,29, `Расходы маркетплейсов` + `Комиссия` = 580 674,98.

## Окно переключения

- Не начинать позже, чем за час до `2026-07-28T11:10:00Z`.
- Оптимально — сразу после подтверждённой пересборки, чтобы до следующей было максимум времени
  (`ADR-038 §Решение(2)`). Подтверждение пересборки берётся заданием в листинге, а не расчётом от расписания.
- Класс риска ограничен `ADR-038`/`ADR-045 §4`: `WRITE_TRUNCATE` пересобирает витрину целиком, промежуточное
  состояние не накапливает порчу и самоисправляется следующим корректным прогоном; падение запроса оставляет
  предыдущую таблицу нетронутой. Риск — временно неверный дашборд, не потеря данных.
- Пауза расписания защитной мерой НЕ считается и не используется (`ADR-038 §Решение(4)`).

## Шаги

Доставка — только через файл (`ADR-055 §1`): heredoc-запись, `bash script.sh > run.log 2>&1; cat run.log`.
`date -u` и `gcloud auth list --filter=status:ACTIVE` — первой И последней командой каждого скрипта
(`ADR-055 §3`, `ADR-063 §4`). Временные файлы не удалять, путь печатать последней строкой (`ADR-043`).

**Шаг 0 — положить замер в репо (провенанс, до всего остального).**
Собрать из двух логов владельца артефакт `/reference/sq_cadence_2026-07-27.md`: `date -u` и аккаунт начала и
конца обоих прогонов, версия `bq` (2.1.35), сработавший флаг (`--all_jobs`; `--all_users` этой установкой не
поддерживается — `bq help ls` печатает «DEPRECATED. Use --all instead»), перечень заданий с целью в `marts.*`
за окно, расписания всех 13 конфигураций, полные выдачи двух целевых `bq show`. Это провенанс для патча
`11 §SQ` (`ADR-045 §6`) — без него патч вносить нельзя.

**Шаг 1 — снять откат и доказать staging, прод не касаясь (read-only).**
- Выгрузить действующую конфигурацию целиком, включая `params.query`, в файл:
  `bq show --format=prettyjson --transfer_config '<полное имя>' > live_config_before.json`;
  оттуда `params.query` — в отдельный файл `rollback_query.sql`. **Это делается ДО первого касания**
  (`ADR-038 §Решение(1)`).
- Сравнить `rollback_query.sql` с `/reference/sql/sq_marts_expenses.sql`. Расхождение — не блокер, но факт:
  снапшот в репо мог отстать. Записать результат сравнения.
- Проверить, что `marts.expenses_staging` существует и всё ещё несёт принятые числа: май-2026 итог по всем
  статьям минус `Перемещение исходящий` плюс `Налоги и сборы` = 10 232 903,20; постатейная разбивка мая; и
  разница `expenses_staging` минус `expenses` по месяцам. Не сошлось ⇒ **стоп**, cutover не начинается:
  значит staging изменился с 26 июля, и приёмка `E1-T1-MECH` требует переподтверждения.
- Сравнить набор и порядок колонок: `bq show --schema` по `marts.expenses` и `marts.expenses_staging` —
  должны совпадать побайтно (`ADR-032`: 17 колонок сохраняются).

**Шаг 2 — подмена. Отдельный паст, одна команда, после подтверждения состояния.**
Перед пастом: показать владельцу свежее состояние (последняя пересборка из листинга заданий, `nextRunTime`) и
получить его «да» в этот момент (`ADR-055 §2`). Затем ровно одна команда:
`bq update --transfer_config --params='{"query":"<новый SQL>"}' '<полное имя>'`.
Ничего кроме `--params` не передавать: `schedule` в выдаче отсутствует, расписание живёт в
`scheduleOptionsV2`, и лишний флаг может его сбить.

**Шаг 3 — read-back до любого прогона.**
`bq show --format=prettyjson --transfer_config '<полное имя>' > live_config_after.json`. Проверить:
`params.query` побайтно равен поданному тексту; `nextRunTime` по-прежнему `2026-07-28T11:10:00Z`;
`scheduleOptionsV2` не изменился против `live_config_before.json`; `state`, `destinationDatasetId`,
`ownerInfo` не изменились. Любое расхождение по расписанию ⇒ откат (Шаг 6) и разбор.

**Шаг 4 — самому запустить одну пересборку и сверить** (`ADR-038 §Решение(3)`).
Запуск ручного прогона по конфигурации — не-идемпотентно, значит снова отдельным пастом:
`bq mk --transfer_run --run_time=<UTC сейчас> '<полное имя>'`. Дождаться завершения, забрать `state` прогона.

**Шаг 5 — приёмка по цифрам.**
На прод-таблице `marts.expenses`: май-2026 = 10 232 903,20 при разрыве 0,00; постатейно совпадает с
контрольными цифрами; разница `marts.expenses` минус `marts.expenses_staging` по месяцам = 0,00 везде.
Формула `total_sum_usd` остаётся как была (`ADR-039 §1`) — если она изменилась, это выход за scope.

**Шаг 6 — откат (только если Шаг 3 или 5 не сошёлся).**
`bq update --transfer_config --params='{"query":"<текст из rollback_query.sql>"}' '<полное имя>'`, затем
ручной прогон и сверка с числами до правки. Откат тоже не-идемпотентен — отдельный паст, отдельное «да».

**Шаг 7 — обновить репо по факту.**
- `/reference/sql/sq_marts_expenses.sql` — заменить на новый действующий текст **только после** зелёного
  Шага 5 (`ADR-021 §2`: снапшот отражает факт, а не намерение).
- `11_INFRA_FACTS.md §SQ` стр.49–50 — патч строго по замеру: у обеих конфигураций поле `schedule`
  отсутствует (не пусто), расписание в `scheduleOptionsV2`, якоря `11:10` и `10:00` UTC подтверждены
  `nextRunTime`; добавить строку `sq_marts_sales_overview` с `every 2 hours`; провенанс — артефакт Шага 0.

## Критерии приёмки (Acceptance)

1. `rollback_query.sql` создан ДО первой команды записи, содержит действующий текст, лежит в логе прогона.
2. `params.query` после подмены побайтно равен поданному новому SQL.
3. `nextRunTime` = `2026-07-28T11:10:00Z` и `scheduleOptionsV2` не изменились против состояния до подмены.
4. Ручной прогон завершился успешно; `marts.expenses` за май-2026 = **10 232 903,20**, разрыв **0,00**.
5. Постатейно за май совпадает с контрольными цифрами `07_STATE`, включая `Списания` 411 838,94.
6. `marts.expenses` минус `marts.expenses_staging` по месяцам = 0,00 без остатка.
7. Набор и порядок колонок `marts.expenses` не изменился (17 колонок, `ADR-032`).
8. Артефакт замера `/reference/sq_cadence_2026-07-27.md` в репо; патч `11 §SQ` не выходит за снятые поля.
9. Ноль транспортных инцидентов класса `Q-62`/`Q-68`; каждая команда записи прошла отдельным пастом с
   явным «да» владельца в тот момент.

## Что вернуть человеку (Return-this)

- Скрипты по шагам, каждый одним heredoc-блоком; команды записи — отдельными пастами, не в общем скрипте.
- Короткий отчёт приёмки: девять пунктов выше, по каждому фактическое значение и «сошлось / не сошлось».
- `/reference/sq_cadence_2026-07-27.md`, обновлённый `/reference/sql/sq_marts_expenses.sql`, diff-патч
  `11_INFRA_FACTS.md §SQ`.
- SESSION-блок (`05` Часть III). `STATE_PATCH`: `E1-T1-MECH` → DONE (строка целиком переносится в
  `07_ARCHIVE.md` тем же коммитом, `ADR-064`); `Q-21` остаток → закрыт; `Q-64` → закрыт в части
  `sales_overview`, остаток по наблюдению 14:41 UTC сформулирован явно; `FX-POLICY-EXPENSES` → гейт cutover
  снят (`ADR-062 §9`); `Текущий фокус` → `FX-POLICY-EXPENSES` (пилот Claude Code, брифа нет — генерация по
  `_GENERATOR`); `updated_at`. `NEW_DECISIONS` — только если по ходу возникло решение, иначе «нет».

## Вне scope этой задачи

- **Формула `total_sum_usd` и курс** — не трогать (`ADR-039 §1`); это `FX-POLICY-EXPENSES`, стартует после.
- **Код загрузчиков** `cf-finance` / `cf-loss-commission` — не трогать (`ADR-034`).
- **Прочие 12 конфигураций флота** — не трогать. Аномалия `sales_overview` объяснена конфигом, правок не
  требует; `Q-38` (март-на-март зависимость `gmroi_by_folder`) остаётся DEFER.
- **Пауза расписания** как защитная мера (`ADR-038 §Решение(4)`).
- **Догон `project_id`/`sales_channel_id`/`agent_name` для списаний** — `'Не указан'` по `ADR-036`.
- **Дашборд**, слияние `Комиссия` с `Расходы маркетплейсов` на уровне отчёта.
- **Генерация брифа `FX-POLICY-EXPENSES`** — отдельная сессия после этой.

## В конце сессии

Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
