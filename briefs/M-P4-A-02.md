# TASK BRIEF · M-P4-A-02 — наполнение 02_ERP_CONTRACTS (a-batch)

## Роль
Ты — разработчик **holika**. Прочитай `_METHOD` + `05_CONVENTIONS`. Ты ПИШЕШЬ док-патчи, человек РЕВЬЮИТ и КОММИТИТ.
`02` — **STABLE**: переносишь ТОЛЬКО прозу в предразмеченные P3a-слоты (HTML-адреса уже стоят) по `MIGRATION_MAP`. Ничего «от себя».
Наполненные §семантика П&Л и §оракул — **НЕ трогать**. Противоречие источника → `NEW_DECISIONS` (proposed ADR), не тихая правка.

## Цель
Наполнить оставшиеся пустые слоты `02`: §схемы stg/core/dim, §поведение API, §справочные данные (UUID), §семантика данных, §валюты — прозой из замороженного источника.

## Context-to-load (человек пиннит SHA)
- `_METHOD` / `00_CHARTER` / `05_CONVENTIONS` / `07_STATE` — .../<SHA>/{…}.md
- `04_ROADMAP` — .../<SHA>/04_ROADMAP.md  (строки 02a–02h)
- `06_DECISIONS_LOG` — .../<SHA>/06_DECISIONS_LOG.md
- `02_ERP_CONTRACTS` — .../<SHA>/02_ERP_CONTRACTS.md  (цель; слоты уже с HTML-адресами)
- `MIGRATION_MAP.md` / `PROJECT_REFERENCE_v6.md` / `RUNBOOK_v8.md` — .../<SHA>/{…}
Нет чего-то → `CONTEXT GAP`, стоп. Если слот в `02` уже наполнен (§семантика П&Л, §оракул) — не трогать.

## Шаги (по строке; трассировка `(PR-xx/RB-xx)` обязательна)
| ID | § | Источник | Спец-правило |
|---|---|---|---|
| 02a | §схемы stg | PR-23 | `stg_msklad.fact_sales_staging`, `transaction_date_raw = STRING` |
| 02b | §схемы core | PR-24 (+PR-25) | `fact_sales_profit` перенести (a); `fact_purchases` — пометка **«не исчерпывающая» → Q-4** (перенести что есть, полноту НЕ выдумывать) |
| 02c | §схемы dim | PR-26 | `dim_products`/`dim_counterparties`(SCD2,country)/`dim_fx_rates`; цифру покрытия weight → **07**, не в прозу |
| 02e | §справочные данные (UUID) | PR-32 | поля/статусы/каналы/проекты/статьи (a); UUID статей `entity/loss` — **→ Q-9** (блок, не резолвить) |
| 02f | §валюты | PR-33 правило 6 (+RB-38 факт) | минорные единицы, `rate.value` документа; ретро-ADR — **P5**, не здесь |
| 02g | §поведение МойСклад API | PR-16 + PR-33 (22–24) + PR-34 (29–30) + RB-18 | expand/limit≤100, sleep 0.25 при 429, timeout=90, 415-на-GET |
| 02h | §семантика данных | PR-33 правила 1–5, 7 | факты о полях/типах |

**НЕ в scope (уже готово):** §семантика П&Л, §оракул (02d).

## Приёмка
- 7 слотов наполнены прозой источника; каждая несёт `(PR/RB)`-трассировку.
- `fact_purchases` (02b) и UUID `entity/loss` (02e) — помечены блоком Q-4/Q-9, а не додуманы.
- Покрытие weight — ушло в STATE_PATCH, не в прозу.
- §семантика П&Л и §оракул — байт-в-байт без изменений.

## Return-this
Док-патчи по слотам 02 + SESSION-блок. Любое противоречие источника → proposed-ADR в NEW_DECISIONS.

## Вне scope
Q-4/Q-9 discovery (отдельные брифы); ветки 01/03/09/11; ретро-ADR валют (P5).

## В конце сессии
SESSION-блок по `05_CONVENTIONS` Часть III (статусы 02a…02h → DONE/частично; weight-покрытие; блоки Q-4/Q-9).
