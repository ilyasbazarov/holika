# TASK BRIEF · T-INVOICES-PARITY-RECHECK

**Класс задачи (ADR-076):** B
Основание класса B: живой `GET entity/invoiceout` к API МойСклада требует секрета `msklad-token`
(`ADR-076 §1`). Совпадает с `07_STATE.md §Мандат Claude Code: класс задач`, строка
`INVOICES-PARITY-RECHECK` (`07_STATE.md:554`) — расхождения нет.

> **ГЕЙТ 1 — мандат не выдан (`_GENERATOR.md §«Что сделать» п.4a`).** Строка мандата
> (`07_STATE.md:554`) несёт «НЕ выдан»: для класса B без указанного ADR, выдавшего мандат,
> задача внутри Claude Code НЕ исполнима. Требуется отдельное решение владельца/архитектора,
> называющее объём разрешённых вызовов и оговорки (прецедент формы — `ADR-092`, `ADR-100 §10`,
> `ADR-104 §1`, `ADR-108`, `ADR-115 §5/§6`: один поимённый ADR на одно исполнение класса B,
> пакетная выдача отклонена `ADR-115 §5`).
>
> **ГЕЙТ 2 — предметная зависимость, снимается не мандатом.** GAP-реестр (`07_STATE.md:661`,
> `07_GAPS.md:82`) называет гейтом задачу `INVOICES-BACKFILL`, которая сама открыта
> (`07_STATE.md:660`, `07_GAPS.md:81`) и ждёт своего мандата класса B. Программа
> `INVOICES-PROGRAMME` (`ADR-110 §3`) фиксирует единственную жёсткую последовательность
> `T1→T2→T3→T4→T5→T6`, где эта задача — `T6`, а `INVOICES-BACKFILL` — `T5`
> (`reference/invoices_loader_programme_2026-08-02.md:78-79`). Досев `T5` ещё не исполнен
> (замер `PARITY-COARSE-TOTALS`, зафиксированный в `reference/parity_registry.md` строка 23,
> снят ДО досева, `2026-08-02`) — сверка ПОСЛЕ досева бессмысленна, пока досева нет.
>
> Оба гейта независимы: даже если владелец выдаст мандат класса B на эту задачу сейчас,
> исполнять её раньше завершения `INVOICES-BACKFILL` означает сверяться с тем же неполным
> состоянием, что уже измерено `PARITY-COARSE-TOTALS`, — без дополнительной ценности.

**Параллель (ADR-082 §1):** нет
<Класс B не параллелится никогда (`ADR-082 §1`, подтверждено `ADR-115 §5`).>

**Файлы на запись** (полный список; на нём МЕХАНИЧЕСКИ проверяется пересечение при параллельном
запуске — `tools/parallel_check.sh`, `ADR-083 §1`):
- `reference/parity_registry.md` — правка строки 23 (правило-моста + статус сходимости после досева)
- `reference/invoices_parity_recheck_<date>.md` — артефакт замера (сырые числа источника/BI, разность, вывод)
- `reference/_scratch_INVOICES-PARITY-RECHECK_<date>/` — рабочие файлы сессии (`ADR-076 §6`)

## Роль
Ты — исполнитель проекта. Законы — `CLAUDE.md`, стандарты — `05_CONVENTIONS.md` Части I и II.
Модель исполнения: ты исполняешь сам (`ADR-082 §2`). Не-идемпотентное — здесь не применимо (задача
только читает источник и правит документ репо); тем не менее исполнение самого мандата класса B
(живой запрос с `msklad-token`) начинается только ПОСЛЕ явного подтверждения владельца и после
объявления действия отдельным сообщением (`ADR-077 §6`). `Done` — только по подтверждённому логу,
не по `rc=0`. Работаешь в СВОЁМ рабочем дереве и коммитишь в СВОЮ ветку (`ADR-081 §6`). `07_STATE`,
`06_DECISIONS_LOG` и `06_INDEX` не правишь: session-блок кладёшь файлом в `reference/_inbox/`.

## Цель
После завершения досева `INVOICES-BACKFILL` (`T5`) повторно сверить пару реестра паритета «Счета
покупателям» (`entity/invoiceout` ↔ `marts.customer_invoices_ar`) и записать в строку 23
`reference/parity_registry.md` актуальное правило-мост и статус сходимости — либо `0,00`, либо
расхождение, закрытое явным правилом/ADR (`ADR-068 §1`). Это условие 4 из пяти условий готовности
программы «Загрузчик счетов покупателям» (`reference/invoices_loader_programme_2026-08-02.md §4`).

## Context-to-load (обязательно прочитать перед работой)
- `_METHOD`, `00_CHARTER`, `05_CONVENTIONS`, `07_STATE`  (всегда, читаются с диска по именам)
- `06_DECISIONS_LOG.md` — `ADR-067` (иерархия источников эталона), `ADR-068` (уровень моста b),
  `ADR-069` (глубина `2026-05-01`), `ADR-076 §1/§5` (класс B, шесть оговорок), `ADR-088`
  (правило суток документа по UTC), `ADR-100` (строки-сироты/ветка удаления, релевантно при
  оценке полноты), `ADR-110` (контракт загрузчика, определение готовности §4), `ADR-115 §5/§6`
  (форма выдачи мандата класса B — один поимённый ADR на исполнение)
- `02_ERP_CONTRACTS.md §оракул` — регистрация `reference/parity_registry.md`
- `reference/invoices_loader_programme_2026-08-02.md` — контракт загрузчика, T6 в декомпозиции,
  условие 4 готовности
- `reference/parity_registry.md` строка 23 — текущее состояние пары («Требуется» по правилу-мосту,
  грубый итог ДО досева)
- `reference/parity_coarse_totals_2026-08-02.md` — метод и числа замера ДО досева (различитель:
  что именно должно измениться после `T5`)
- session-блок/артефакт `INVOICES-BACKFILL` (появится после её исполнения) — объём и период
  досева, число загруженных строк
Если чего-то из списка нет в контексте → выдай `CONTEXT GAP` и остановись.

## Входы
- Строка 23 `reference/parity_registry.md`: источник эталона — (ii) подтверждённый живым `GET`
  `entity/invoiceout` (bulk-отчёта задолженности в report-API нет, оба `report/counterparty/debt*`
  дают `404`); поверхность BI — `marts.customer_invoices_ar` → LS «Операционка»
  (`msklad_customer_invoices_ar`); правило-мост агрегации в `reference/sql/sq_marts_customer_invoices_ar.sql`
  описано, но соответствие полям `entity/invoiceout` (`sum`, `payedSum`, `state.name`) построчно
  не проверено.
- Грубый замер ДО досева (`PARITY-COARSE-TOTALS`, 2026-08-02): источник — 4526 документов, наша
  сторона — 4058 счетов; недостача `468` документов; `43,4 %` документов источника не в KGS
  (конвертация не проведена этим замером). Эти числа — точка сравнения «было», не текущее
  состояние после досева.
- Артефакт `INVOICES-BACKFILL` (создаётся задачей `T5`, гейтящей эту) — фактически загруженный
  период и число строк.

## Шаги
1. Проверить, что мандат класса B на эту задачу выдан (номер и параграф ADR) и что `INVOICES-BACKFILL`
   закрыта — по строке `07_STATE.md §Открытые вопросы`/`07_GAPS.md`, не по памяти чата. Любое из
   двух отсутствует → `CONTEXT GAP`, стоп.
2. Прочитать артефакт `INVOICES-BACKFILL` — период и объём досева, дату снятия.
3. Датированный снимок источника (`ADR-067 §3`): один живой `GET entity/invoiceout` (пагинация
   по `limit`≤100, форма авторизации — из снапшота `reference/code/cf-finance/main.py`, не
   изобретается), agregировать по правилу, применённому в `PARITY-COARSE-TOTALS`, плюс явно
   учесть валюту (`43,4 %` документов не в KGS — привести к KGS через `rate.value`, не пропустить,
   как было пропущено в замере ДО досева).
4. Снять нашу сторону: `marts.customer_invicies_ar` (число счетов, выставлено/оплачено/не оплачено
   в KGS) в том же окне снимка.
5. Построчно проверить мост: соответствие `sum`→`sum_kgs`, `payedSum`→`payed_sum_kgs`,
   `state.name`→статус (текст SQL — `reference/sql/sq_marts_customer_invoices_ar.sql`), правило
   суток документа по UTC (`ADR-088`) для полей, зависящих от даты (если такие есть в разрезе).
6. Посчитать разность по количеству документов и по трём суммам; для каждой ненулевой разности —
   назвать причину, если она устанавливается этим объёмом работы (класс документов/статус/валюта),
   или явно оставить как остаточный `CONTEXT GAP`/новый `Q-` с указанием, чем закрывается.
7. Записать в строку 23 `reference/parity_registry.md`: правило-мост (заполнить «Требуется» фактом
   проверки), новый статус сходимости с датой снимка и ссылкой на артефакт шага 3-6.
8. Токен `msklad-token` не печатать; `grep -rF -- "$MSKLAD_TOKEN" <каталог сессии>` перед
   `git add`, любое совпадение — стоп (`ADR-076 §5.1/§5.3`). `date -u` и `gcloud auth list` первой
   И последней командой каждого скрипта, обращающегося к секрету (`ADR-079 §6`, `ADR-055 §4`).

## Критерии приёмки (Acceptance)
- Строка 23 `reference/parity_registry.md` несёт заполненное правило-мост (не «Требуется») и
  статус сходимости с явной датой снимка обеих сторон.
- Либо разность по количеству и по трём суммам равна `0,00`, либо каждая ненулевая разность
  закрыта поимённым правилом/причиной, либо явно оставлена открытым `Q-`/`CONTEXT GAP` с адресом,
  чем закрывается дальше — тихое примирение (ADR-002/`00_CHARTER §главный принцип`) запрещено.
- Условие 4 готовности программы (`reference/invoices_loader_programme_2026-08-02.md §4`, п.4)
  прямо процитировано как выполненное или невыполненное с названной причиной.
- Токен не попал ни в один закоммиченный файл (лог `grep -rF` приложен).

## Что вернуть человеку (Return-this)
- Артефакт `reference/invoices_parity_recheck_<date>.md` (метод замера, сырые числа, разности,
  причины/остатки) + правка `reference/parity_registry.md` строка 23 + точная команда снятия
  живого `GET` (без значения токена) и её вывод (объём/статус-код, не содержимое с ПДн контрагентов
  сверх нужного для сверки).

## Вне scope этой задачи
- Построчная (подокументная) сверка сверх количества/сумм по статусам — если нужна, заводится
  отдельной задачей после этой.
- Правка `sq_marts_customer_invoices_ar.sql` под найденные расхождения — фикс-форвардом в SQL,
  отдельной задачей поверх подтверждённой причины (`00_CHARTER §главный принцип`, слой не лезет
  через границу чинить чужой слой).
- Повторный запуск/диагностика `INVOICES-BACKFILL` — эта задача только читает её результат.
- Мандат на саму задачу — эта задача не выдаёт себе мандат, ждёт владельца/архитектора.

## В конце сессии
Выдай SESSION-блок по формату `05_CONVENTIONS` Часть III
(`SESSION_LOG` / `STATE_PATCH` / `NEW_DECISIONS` / `NEW_CONVENTIONS`).
