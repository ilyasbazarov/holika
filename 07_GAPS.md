# FILE: 07_GAPS.md

# 07_GAPS · GAP-реестр discovery-петли — полный текст строк

**Статус:** LIVING · **Заведён:** `ADR-106 §1` (2026-08-02), перенос из `07_STATE` ДОСЛОВНЫЙ.

> Индекс этих же строк (`ID · статус · вопрос одной фразой · гейт`) живёт в
> `07_STATE §Открытые вопросы` и входит в обязательный контекст сессии. Этот файл читается
> ТОЧЕЧНО по ID и в обязательный контекст НЕ входит (приём `ADR-064`).
> Новая строка заводится в ОБА файла одним коммитом. Полностью закрытая строка уезжает
> отсюда в `07_ARCHIVE.md` дословно, и тем же коммитом снимается её строка индекса.

---

| ID | Вопрос | Тип | Как закрывается |
|---|---|---|---|
| Q-3 | **Исходный код пайплайна нигде не версионирован**: cf-finance на persistent-диске Cloud Shell, `load_invoices.py` в эфемерном `/tmp`, остальные CF — не названы | референс-артефакт + решение | **partial-close (cf-finance-часть, сессия E1-T3-D-CFSRC 2026-07-14):** код cf-finance извлечён, sha256-верифицирован, disk==deployed для кода подтверждён (снапшот `/reference/code/cf-finance/` + `MANIFEST.md`; расходится только build-файл `.gcloudignore`); decision-половина (каноническое место хранения) — остаётся `proposed` (ADR-017, review-очередь Пункт 1). Структурный охват converter'а не-KGS — CAPTURED в рамках этой же сессии (см. Q-30/Q-19: локальный, не shared-модуль). Residual Q-3 (не тронуто, следующий discovery-бриф): `load_invoices.py`, `cf-facts`, `cf-dim`, `cf-fx`, `cf-dq`, `cf-inventory`, `cf-alert`. **decision-половина (cf-finance) CLOSED (ADR-017):** провенанс = снапшот `/reference/code/`; живой VC = отдельный код-репо (a), execution → `CODE-REPO-STANDUP`. Residual Q-3 (прочие CF) — остаётся. **E1-T1-MECH-INGEST дополнение (2026-07-23):** cf-loss-commission провенанс захвачен (снапшот `/reference/code/cf-loss-commission/` готов, SHA256 верифицирован disk==deployed), **требует коммита в holika** (см. B-9, Q-46). **Дописка (`PARITY-STOCK-INTRANSIT-CLOSE`, второй ход, 2026-08-05, `ADR-125 §5`): посылка «код нигде не версионирован» изменилась качественно с появлением код-репо, остаток (`cf-dim`, `cf-fx`, `cf-alert`) жив.** |
| Q-7 | TTL JWT-токена Bakai (TD-BAKAI-01) | факт | запрос банку / эмпирическая фиксация (рабочая политика DEFER есть в RB §17) |
| Q-9 | UUID статей расходов на `entity/loss` (ре-скоуп: декомпозиция вынесена в Q-23/CLOSED — остаётся ЧИСТЫЙ UUID-резолвинг) | факт | **DEFER** (rationale ADR-009 §3 — future defense-in-depth, не блокер Epic-1; E1-T2 работал по имени, UUID не резолвлены); API-запрос по методу PR (РБ, строки 711–721), когда актуально |
| Q-14 | Процедура полной пересборки core из GCS raw (RB §11, шаги 11.2–11.3) — заглушки | факт | восстановить процедуру (связано с Q-3) |
| Q-20 | DQ-порог `currency_normalization` (`avg_revenue_kgs < 10M`, `03_PIPELINE_SPEC.md` §DQ) — только верхняя граница (ловит «забыли `/100`, остались тыйыны»); НЕ ловит регрессию «забыли `×rate`» (сумма занижена ~90x, а не завышена) — тот же класс бага, что и TD-RECON-01. Нужен ли нижний порог / отдельный чек на этот класс ошибки | решение | решение владельца + (при подтверждении) патч DQ-конфигурации `cf-dq`; coverage-gap подтверждён архитектором (M-P5-AP), конкретное значение порога — не назначается заранее (anti-improvisation), требует эмпирической калибровки **Дописка `ADR-101 §5` (2026-08-02, архитектор): ПЕРЕФОРМУЛИРОВАН.** Вопрос «каким должен быть нижний порог» получает ответ «порог не нужен и не помог бы». Ни `core.fact_sales_profit`, ни `core.fact_payments` не несут ни валюты документа, ни суммы в минорных единицах, ни применённого курса — конвертация выполняется в загрузчике (`fetch_demands.py:137-141`), в таблицу ложится только результат в KGS, поэтому внутри хранилища класс ошибки непроверяем ПО ПОСТРОЕНИЮ, а не по нехватке калибровки: гейт «эмпирическая калибровка» не имел исполнителя потому, что калибровка на этой схеме невозможна. Проверка переносится в слой загрузки, где валюта и курс есть на руках (`05_CONVENTIONS §граница контракта`, `_METHOD §4`): для каждой позиции либо валюта документа есть валюта аккаунта, либо применён `rate.value` (при отсутствии — курс по `ADR-010`); ответ бинарный, порога и owner-gated числа не требует. Работа адресуется задачей `INGEST-CURRENCY-ASSERT`. Действующий агрегатный порог не снимается, но перестаёт считаться покрытием этого класса. Условная арифметика (при прочтении «средняя выручка на строку»): май `93 522 995,53` на 1363 строках даёт около `68 616` KGS на строку, регрессия «забыли поделить на сто» даёт около `6,86` млн — НИЖЕ порога `10` млн, то есть проверка не сработала бы и на собственной цели; утверждение условное — текста проверки в репо нет (`CONTEXT GAP`, снимается шагом 1 той же задачи). |
| E1-T2-MECH-TAX | Инъекция 2-го источника «Налоги» (начисление) + подавление cash-строки | решение | форма staging-first (как E1-T1-MECH); гейт E1-T2 шаг-5; ops — владелец числа + каденс помесячно **Дописка сборки 2026-08-01 (`ADR-091 §9`):** блокирует не класс задачи, а отсутствующий вход владельца (число начисления и каденс); после его получения staging-часть становится классом A. |
| Q-28 | `Regional Access Boundary` ошибка при `gcloud secrets versions access` в логе прогона E1-T2 (не заблокировала выполнение) | факт | **DEFER** — задокументирована в `11_INFRA_FACTS.md §IAM`. Триггер промоушена из DEFER: повтор ИЛИ появление в не-интерактивном/service-account прогоне (ошибка ссылается на личный gmail `ilyasbazarov4@…`, не на `etl-sa@…`) |
| Q-30 | Полный масштаб currency-conversion бага (весь март / другие месяцы / `cashout`) не измерен сессией E1-T3, вне scope E1-T3 | факт | МАГНИТУДНЫЙ замер (месяцы × все core.fact_* × cashout) — DEFER до после структурной картины (Q-3) и посадки expense-фикса; СТРУКТУРНЫЙ охват снимается в Q-3, не отдельной кампанией. Owner-gated; бласт-радиус потенциально project-wide, если converter общий для `core.fact_*` (не подтверждено — Q-3). **Структурная половина — CAPTURED (сессия E1-T3-D-CFSRC, 2026-07-14):** конвертер локальный для cf-finance, blast radius ограничен `core.fact_payments`; project-wide риск для прочих `core.fact_*` не подтверждён (иные CF не исследованы, остаётся в residual Q-3). Магнитудный замер (месяцы/cashout/sales) — по-прежнему DEFER **Дополнение триггера (ADR-069 §3):** промоушен из DEFER при расширении паритета на период до 2026-05 — замер месяца входит в процедуру включения |
| RQ-3 | Задеплоенная ревизия `cf-finance-00006-piv` содержит `main.py.bak` | факт (гигиена деплоя) | не адъюдицировано этой сессией; DEFER — не блокер, убрать при следующем деплое. **Дописка (`PARITY-STOCK-INTRANSIT-CLOSE`, второй ход, 2026-08-05, `ADR-125 §5`): требует одной сверки с `reference/code/cf-finance/MANIFEST.md` после деплоя 2026-08-03, в любую сторону не двигается без неё.** |
| Q-34 | Наблюдается ли состояние прогонов SQ-флота (покрывает ли `cf-alert`/мониторинг падения BQ DTS) и нужен ли постоянный механизм | решение | **DEFER (ADR-020 §3).** Гэп реален (дефект прожил 45 сут невидимым; `03 §DQ` и ADR-007 наблюдают только `staging`/`core`; прецедент RB-42), но: эмпирика опровергла риск для инвесторской поверхности (`sq_marts_expenses` — 0 падений за всю историю); механизм Q-34 не поймал бы Q-38 (отказ выглядит как `SUCCEEDED`); Epic-1 не закрыт. «Алерта нет вообще» НЕ утверждается — связан с Q-12. **Триггер промоушена:** любой новый красный в SQ-флоте ИЛИ закрытие Epic-1 ИЛИ появление первого потребителя у `audit.*` |
| Q-35 | `10_OPS_PLAYBOOK.md` объявлен в `00_CHARTER §карта документов` стр.52 и `MIGRATION_MAP` §Целевой набор; на него ссылаются `11 §секреты`, `03 §DQ`, `ADR-008 §4`, `02:241` — файла в репо @ `4fce6a8` НЕТ (404); M-P4 закрыт DONE 22/22 без него | референс-артефакт | **DEFER (ADR-020 §Последствия)** — не блокер. Висячий адресат: ответы Q-34 и `ADR-008 §4` (retention `audit`) класть некуда. Триггер промоушена: выход Q-34 из DEFER ЛИБО первая правка, требующая §инцидентный слой **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-37 | Латентный дефект актива: по `DATE(snapshot_at) = 2026-05-07` лежат ТРИ идентичных среза (Q-36) — наивный запрос по этой дате утроит строки | факт | **DEFER (ADR-019 §6)** — не блокер, потребителя нет (`ADR-008 §2`). Срабатывает при промоушене таблицы в consumer-facing роль (`ADR-008 §3`): тогда либо дедуп по `MIN(snapshot_at)` в пределах суток, либо `DELETE` двух лишних срезов — решать ADR того момента. Не чинить превентивно; `AUDIT-SNAPSHOT-FIX` этого не касается |
| Q-38 | `sq_marts_gmroi_by_folder` (Config `6a004e88-…`) читает `marts.gmroi` (Config `6a006664-…`) — единственная март-на-март зависимость флота (проверены все 13 SQL @ `4fce6a8`). Порядок 08:03 → 12:02 UTC верный, но не задан ничем: оба `every 24 hours` от собственного `creationTime`, зависимость не объявлена, оркестрации нет; Config ID зависимого МЕНЬШЕ ⇒ заведён раньше источника | факт (зафиксирован) + решение (форма) | **DEFER (ADR-020 §4)** — не блокер Epic-1, `gmroi_by_folder` вне Трека A/B. Риск: нарушение порядка даёт `SUCCEEDED` на вчерашних данных; `03 §DQ` не видит; `_mart_refreshed_at = CURRENT_TIMESTAMP()` штампует свежесть агрегации, а не данных. Форма фикса (оркестрация · assert на `_mart_refreshed_at` источника · слияние в один SQ) НЕ назначается — anti-improvisation. Триггер промоушена: включение `gmroi`/`gmroi_by_folder` в инвесторскую сверку ИЛИ первое расхождение по GMROI |
| Q-39 | `sq_audit_dim_counterparties_snapshot` / `sq_audit_dim_employees_snapshot`: тот же паттерн `INSERT … SELECT *, CURRENT_TIMESTAMP()`, что сломал `dim_products` — две взведённые мины того же класса (зелены лишь потому, что источники ещё не получали новых колонок) | решение | **DEFER (ADR-020 §5)** — оптовое распространение фикса ADR-019 ОТКЛОНЕНО: паттерн один, ценность активов разная. `core.dim_counterparties` несёт SCD2 нативно (`02 §схемы dim`) ⇒ история уже в ядре, довод `ADR-008 §3` неприменим, вопрос смещается с «как чинить» на «зачем он» (ближе к ретайру). Решать отдельно и по каждому, не оптом по совпадению формы SQL. **Триггер промоушена НАСТУПИЛ (закрытие Q-4, 2026-07-28):** `core.dim_employees` теперь документирован в `02` (`/reference/schema_dump_2026-07-28.md`) — основание «решать нечего» снято. Само решение по форме фикса `sq_audit_dim_employees_snapshot` этой сессией НЕ производится (owner-gated / архитектор, `_METHOD §11` DISCOVER≠CAPTURE≠решение) **Дописка `ADR-101 §3` (2026-08-02, архитектор + решение владельца): РАЗДЕЛЁН по объектам, как и требовал `ADR-020 §5`.** Сотрудники — чинить формой E (`ADR-019 §3`), задача `AUDIT-SNAPSHOT-FIX-EMPLOYEES`: `core.dim_employees` версионирования не несёт (7 колонок, проверено живой схемой), значит снимок есть единственный носитель истории и довод `ADR-008 §3` применим; основание «решать нечего» снято закрытием `Q-4`; `audit.dim_employees_snapshots` несёт `snapshot_at` ПОСЛЕДНЕЙ колонкой, то есть заряжена та же мина, что сломала снимок товаров. Контрагенты — чинить нечего, история в ядре нативно (`scd2_*`); **владелец выбрал ретайр (чат 2026-08-02)**, задача `AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE`. Отсутствие потребителя у `audit.*` переподтверждено фактом: сплошной поиск чтений `audit.` по `reference/sql/*.sql` даёт совпадения только в трёх самих снапшот-запросах (там `audit.*` — цель `INSERT`), в инвентаре Custom Query дашборда слово `audit` не встречается; ограничение метода — проверены оба известных репо инвентаря, потребитель вне них таким поиском не виден. Строка закрывается по исполнении ОБЕИХ задач. `Q-37` не трогается (`ADR-019 §6`). |
| Q-40 | Промоушен правила «успех инструмента ≠ факт» (`05` Часть I, ADR-021 §2) в канон `_METHOD §6` | решение | **DEFER (ADR-021 §4)** — `05` Часть I есть доменно-нейтральное зеркало `_METHOD §6`; правило введено как holika-локальное уточнение, что создаёт контролируемое расхождение с каноном. Правка `_METHOD` задевает проекты за пределами holika (методология переносимая) — owner-gated, blast radius вне этого репо. Триггер промоушена: правило сработало ≥1 раз вне этой сессии ЛИБО тот же класс ошибки воспроизведён в другом проекте. **ТРИГГЕР НАСТУПИЛ (ADR-044 §5, 2026-07-26):** правило сработало вне исходной сессии — ложный греп-вердикт «валютной починки НЕТ» в диагностике `E1-T1-MECH-PREP`, опровергнутый фактом (`Q-45`). Промоушен НЕ назначается автоматически: blast radius за пределами holika, owner-gated. Вынесен на решение — `/reference/architect_review_queue_2026-07-26-2.md` п.2 (предложенная форма — помеченное расширение `_METHOD §6` по образцу `§11 [РАСШИРЕНИЕ]`, эталон `cb33fb4` не правится). **Сессия `E1-T1-MECH-PREP-ADJ-2` (2026-07-26):** архитектор рекомендует (a) и выдал готовый текст ADR как `proposed` в `NEW_DECISIONS` своего session-блока; в `06` не заносится до апрува (`_METHOD §8`). Остаётся owner-gated (`ADR-021 §4`). Фактура, добавленная этой сессией: цена варианта (b) «оставить локальным» РАСТЁТ — `ADR-044` добавил к правилу второй абзац (греп-вердикт), которого в каноне нет, то есть расхождение уже не одна фраза **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-47 | (E1-T1-MECH-INGEST, сессия 2026-07-23) Loss: 0 из 128 документов за всю историю аккаунта в не-KGS валютах. Это известный факт о бизнесе (loss всегда KGS) или потенциальный гэп в данных, требующий переспроверки? | факт (бизнес-правило) | **DEFER** (`E1-T1-INGEST-ADJ`, 2026-07-23). Бизнес-факт не назначается — дока, утверждающего «loss всегда KGS», в репо нет (догадка запрещена, `_METHOD §6`). НИЧЕГО НЕ ГЕЙТИТ: по прецеденту `ADR-016 §3` («охват `cashout` — из глобального scope `ADR-010`, forward-корректность, НЕ из замера») ингест обязан нести `× rate.value` независимо от наличия не-KGS документов в выборке; отсутствие не-KGS в замере не сужает scope конвертации (`ADR-010`, `ADR-024 §2`). Замер «0 из 128» пока — метка без артефакта (`ADR-021 §2` / `05 Часть I ★ Успех инструмента ≠ факт`): положить SQL/лог замера в `/reference` вместе с провенанс-коммитом. **Триггер промоушена:** появление первого не-KGS `loss`-документа ЛИБО расхождение по «Списаниям»/«Прочим расходам» против оракула. |
| Q-55 | (E1-T1-MECH-PREP, 2026-07-26) В `core.fact_loss` поля `project_id`, `sales_channel_id`, `agent_name` = NULL во всех строках | факт | **Механизм установлен фактом 2026-07-26:** `cf-finance` строка 38 запрашивает `expand=expenseItem,agent,project,salesChannel` и проект с каналом получает; `cf-loss-commission` запрашивает `expand=expenseItem,agent,rate.currency` — без `project` и `salesChannel`, поэтому читает NULL из неразвёрнутых ссылок. Один и тот же API, разный запрос, разный результат. Остаётся непроверенным одно: проставлены ли проект и канал у документов списания в самом МойСкладе. Догон — две строки в URL плюс подтягивание названий из справочников, не расследование. Для `agent_name` вероятен содержательный ответ (у списания товара нет контрагента). Заполнение → `'Не указан'` (ADR-036), догон не назначен. **Условие переоткрытия назначено (`ADR-047 §3`/`§5`, 2026-07-26):** проверка входит в остаток Шага 0 — `COUNTIF(project_id IS NOT NULL)`, `COUNTIF(sales_channel_id IS NOT NULL)`, `COUNTIF(agent_name IS NOT NULL)` по `core.fact_loss` (128 строк). Любое ненулевое значение ⇒ `ADR-036` переоткрывается НОВЫМ ADR **до** правки SQL: `'Не указан'` перестаёт быть «честным отражением источника» и становится затиранием данных. Переоткрытие УЗКОЕ — `ADR-032` (17 колонок, имена, порядок) не затрагивается |
| Q-62 | (E1-T1-MECH-PREP-ADJ, 2026-07-26, доставка артефактов) Диагностический блок, доставленный как многострочный `bash -c '…'` с вложенным квотингом и `sleep 900` внутри, при вставке в интерактивный Cloud Shell наложился строками: задвоенный блок, оборванный SQL, непарные кавычки ⇒ шелл встал на продолжении строки | факт (процесс, доставка) | **ОТКРЫТ.** `ADR-014 §3` (редирект в файл) присутствовал и не помог: он лечит наложение echo, а причина здесь — глубина вложенности квотинга и длина блока. **Последствие подтверждено фактом, а не осталось риском:** из искажённого паста успела уйти строка `gcloud scheduler jobs run loss-commission-daily-update` — `lastAttemptTime` джоба сдвинулся на `2026-07-25T19:35:22Z`, тогда как осознанный запуск владельца произошёл в `19:42:52Z`. Плановым прогон быть не мог (расписание `0 3 * * *`), анонимным HTTP-вызовом тоже (`Q-42`, дополнение 2026-07-26: у `cf-loss-commission` `run.invoker` только `etl-sa`). То есть не-идемпотентное действие исполнилось раньше, чем человек увидел состояние джоба. Ущерба нет (ингест идемпотентен, марта не касается), но выяснилось постфактум. Предложенное правило — `proposed`, review-очередь п.3. **Путь закрытия НАЗНАЧЕН (`ADR-055`, 2026-07-26):** доставка через файл стала условием, а не рекомендацией; не-идемпотентное действие — отдельным пастом после подтверждения состояния. Не закрывается автоматически — ждёт первой сессии, прошедшей по новому правилу без инцидента. Кросс: `Q-68` (тот же транспорт, отказ громкий вместо тихого), `ADR-051 §3` (`Q-62` приоритетнее — последствие подтверждено фактом), `ADR-051 §4` (триггер перевода `ADR-014 §3` в условие). **Новый случай 2026-07-27:** блок из нескольких команд вставлен подряд в два разных терминала (Cloud Shell и локальный macOS); приглашение чужого терминала и текст сообщений об ошибках исполнились как команды (`command not found`, `syntax error`). Разрушительных действий не произошло — все строки упали. Смягчение, применённое дальше в той же сессии: подача команд по одной строке за раз. `Q-62` НЕ закрывается **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-68 | (FX-OUTBOUND-COVERAGE-ADJ, 2026-07-26, доставка артефактов) Прямая вставка copy-paste bash-блока в **интерактивный Cloud Shell** дважды подряд обрывала терминал целиком — потеря сессии без следа, не наложение строк как в `Q-62`. Обход (heredoc-файл + `nohup`) сработал | факт (процесс, транспорт) | **ОТКРЫТ, путь закрытия назначен, приоритет ниже `Q-62`.** Причина НЕ установлена и не выводится (`ADR-051 §1`): наблюдения изнутри консоли нет, гипотезы (объём паста, глубина блока, лимит буфера терминала, обрыв WebSocket Cloud Shell) не различены. Класс — «интерактивный Cloud Shell как ненадёжный транспорт», НЕ под-класс наложения строк (`ADR-051 §2`: общий признак с `Q-62` и общий сработавший обход ⇒ две реестровые строки дали бы два DEFER об одном). `ADR-051 §3`: последствий нет (сессия умерла целиком, ничего не исполнив) ⇒ строго менее опасен, чем `Q-62`. **Закрывается `ADR-055 §1`** (доставка через файл — условие), по факту первой сессии, прошедшей по новому правилу без инцидента **ВТОРОЙ ИНСТАНС (`ADR-063`, 2026-07-26), Q остаётся ОТКРЫТ.** Сессия `FX-RATE-FIDELITY` деградировала до анонимного вызова НА СЕРЕДИНЕ первого захода: все запросы после `2026-07-13`, весь Step 4 и Step 5 вернули `401`/`no active account`, что читается как «данных нет». Ложное «архива за эти даты нет» поймано только случайным противоречием Step 1 ↔ Step 3 внутри того же лога. Отдельный `Q` не заводится (`ADR-051 §2`). **Класс опасности ПЕРЕСМОТРЕН (`ADR-063 §2`):** основание `ADR-051 §3` («последствий нет — сессия умерла целиком») опровергнуто — отказ ТИХИЙ, последствие класса подтверждено фактом ⇒ `ADR-051 §3` `superseded-in-part`, приоритет выровнен с `Q-62`. **Условие закрытия УТОЧНЕНО (`ADR-063 §3`):** «без инцидента» читается как «без инцидента ЭТОГО КЛАССА»; настоящая сессия условию не удовлетворяет — доставка прошла по `ADR-055 §1/§3` (артефакт §6), но инцидент класса был. **Защита внесена условием (`ADR-063 §4/§5`):** `05` Часть II п.4 — `date -u` первой И последней командой плюс переподтверждение личности вызывающего. **Дополнение 2026-07-28 (`ADR-073 §9`):** при замере `Q-64` два запроса, повторённые вручную россыпью в Cloud Shell (вне скрипта), дали `ERROR: (bq) You do not currently have an active account selected` дважды подряд — отказ ГРОМКИЙ, в отличие от тихой деградации до `401` в инциденте 2026-07-26. Ущерба нет: данные уже были получены скриптовым прогоном с якорями `date -u`. Провенанс: `/reference/bq_jobs_2026-07-28.md §7`. **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-69 | (FX-FORWARD-FILL-ADJ, 2026-07-26) Значение курса в `core.dim_fx_rates` меняется в 3–37 % дней месяца; май-2026 — ни одного изменения за 31 день (`87.4205`). Причина НЕ установлена: **(A)** `cf-fx`/его источник теряет изменения либо **(B)** официальный курс НБКР действительно малоподвижен | факт (две неразличённые гипотезы) | **ОТКРЫТ, различитель назначен.** Замер `/reference/fx_forward_fill_2026-07-26.md §7` между гипотезами не выбирает — смотрит только внутрь таблицы. Догадкой не закрывается (`_METHOD §6`; прецедент `ADR-045 §1`). `ADR-058 §2`: различитель — GCS-архив ответа источника `gs://msklad-raw-msklad-bi-prod/fx-rates/bakai_{YYYY-MM-DD}.json` (`03 §cf-fx` стр.59; бакет — `01_ARCHITECTURE` стр.29), сверка «что источник отдал» против «что легло в таблицу». `ADR-058 §3`: второй независимый различитель того же прогона — логи `cf-fx` (`RUNBOOK_v8 §13.2`) на `bakai_token_expired`/`degraded`/forward-fill в дни-копии. `ADR-058 §4`: до-миграционный период (май-2026, НБКР XLS) архивом не покрыт — внешняя добыча курсов ВНЕ scope; применимость довода `ADR-028` (второй конкурирующий оракул) к **исходящей** конвертации не адъюдицирована и есть отдельное owner-gated решение. Включает побочный факт `ADR-057 §6` (новые значения в выходные в апреле/июле против формулировки `ADR-049 §3`). **Гейтит:** решение владельца по `ADR-039 §2` (`ADR-057 §3`) и через него `Q-56`/`Q-67`. НЕ гейтит `E1-T1-MECH`. Исходы не предрешены (`ADR-058 §7`): (A) ⇒ фикс-форвард в `cf-fx` отдельной задачей, политика выбирается после; (B) ⇒ вопрос точности снят, развилка `ADR-050 §2` возвращается владельцу с известной ценой вариантов. **Различение ПРОДВИНУТО сессией `FX-RATE-FIDELITY` (2026-07-26), артефакт `/reference/fx_rate_fidelity_2026-07-26.md`, не закрыт полностью:** Bakai-эра (2026-06-03…) — **(B) подтверждён** прямой сверкой архив/таблица, 26/26 дат июля, включая дни изменения курса. До-миграционный гранулярный период `2025-04-29…2026-04-30` — склоняется к (B) по характеру распределения значений (49 различных значений за 365 дней, не похоже на forward-fill), но без архива источника — метод слабее июльского. **Найден и сужен критический участок `2026-05-01…2026-06-02` (33 дня, весь май-2026 внутри):** идеально плоское значение `87.4205`, резкий переход в обе стороны (гранулярно `04-29`/`04-30` → плоско `05-01` → гранулярно/Bakai `06-03`). Структурный сигнал в пользу **(A)** для этого окна конкретно, но архивного снапшота источника для дифференциации тем же методом, что сработал для июля, нет и, по всей видимости, никогда не было. Побочно проверена и ОТВЕРГНУТА гипотеза о `nbkr_cache.json` как о механизме заполнения таблицы (отдельный `Q` не заводится — гипотеза закрыта отрицательным результатом, не открытым вопросом) **АДЪЮДИЦИРОВАН `ADR-059` (2026-07-26): СУЖЕН, не CLOSED.** (1) Эпоха Bakai `2026-06-03…` — **(B) accepted**; `ADR-021 §2` удовлетворён по существу (два независимых ИСТОЧНИКА, не два инструмента над одним). (2) Побочный факт `ADR-057 §6` **CLOSED для эпохи Bakai**: источник меняет значение в выходные (`07-04` Сб, `07-11` Сб — новые значения, архив совпадает); формулировка `ADR-049 §3` эпоха-зависима, на Bakai не распространяется. (3) `2025-04-29…2026-04-30` — DEFER, склоняется к (B) по распределению, источником не подтверждено; **триггер промоушена:** первая долларовая величина за период ранее `2026-05` в инвесторской поверхности. (4) Остаток окна `2026-05-01…2026-06-02` вынесен в `Q-70`. (5) `ADR-058 §3` — ИСЧЕРПАН (`ADR-059 §5`). (6) `ADR-058 §2` — ИСПОЛНЕН. (7) `Q-69` больше НЕ гейтит `ADR-039 §2` (`ADR-061 §2`); политика выбрана `ADR-062`. |
| Q-70 | (FX-RATE-FIDELITY-ADJ, 2026-07-26; продвинут сессией FX-MAY-WINDOW, 2026-07-28) Механизм застывшего значения `87.4205` в окне `2026-05-01…2026-06-02` (33 дня, весь май-2026 внутри) не установлен полностью. forward-fill как объяснение окна ОПРОВЕРГНУТ (`ADR-059 §3`) | факт (гипотеза (A) переформулирована, различители D1–D3 частично исполнены) | **СУЖЕН, НЕ закрыт (сессия FX-MAY-WINDOW, 2026-07-28, `/reference/fx_may_window_2026-07-28.md`).** (D2) ИСПОЛНЕН фактом: `INFORMATION_SCHEMA.JOBS_BY_PROJECT` за окно вернула 13 заданий с полным текстом SQL — 6 forward-fill `INSERT` между `2026-05-07` и `2026-06-02`, буквально копирующих последний известный курс; 5 из 6 исполнены вручную (`ilyasbazarov4@gmail.com`), 1 — `etl-sa`. Вход в окно (`2026-05-01`/`05-02`) трассируется к отдельному `LOAD`-заданию `bootstrap-sa` от `2026-05-03` (`WRITE_APPEND`, 4 строки, не из GCS) — конкретные значения этих 4 строк из метаданных задания не восстановлены, остаётся неразличённым куском. (D3) ИСПОЛНЕН, отрицательный результат: read-only инвентари (`functions`/`run`/`scheduler`/`workflows`) не нашли ни одного именованного fx-сервиса кроме `cf-fx` (нерелевантен для эпохи до `2026-06-03`); согласуется с D2 — все записи окна ручные/`etl-sa`, не от именованного автоматического загрузчика. (D1) **ИСПОЛНЕН фактом, ВНЕ сессии Claude Code (владелец, чат, 2026-07-28).** Гэп из сессии `FX-MAY-WINDOW` снят: владелец поставил `xlrd` (`pip3 install --user xlrd`) и прочитал `nbkr_dailyrus_raw.xls` — файл оказался многолистовым (лист на год, `sheet_by_index(0)` в сессии агента читал лист `'2010'`, отсюда пустой результат при фильтре по 2026 году; нужный лист — `'2026'`, назван явно `wb.sheet_by_name('2026')`). Лист `'2026'` за `2026-04-25…2026-05-10`: `04-25`…`04-27` = `87.4032`; `04-28` = `87.4307`; `04-29` = `87.4296`; `04-30` = `87.4293`; **`05-01`…`05-10` = `87.4205`** (сплошь, включая ровно те даты, что D2 не смог восстановить из метаданных `LOAD`-задания `bootstrap-sa`). **Значение независимого архива источника на `2026-05-01`/`05-02` СОВПАДАЕТ с тем, что легло в `core.dim_fx_rates`.** Провенанс: чтение выполнено вне Claude Code, командой владельца, без нового прогона агента; сырой файл и скрипт чтения — `reference/_scratch_fx_may_window_2026-07-28/nbkr_dailyrus_raw.xls` (перенесён туда владельцем с `/tmp` после сессии `FX-MAY-WINDOW`, содержимое не менялось). **Свёрнутые сюда лиды (`ADR-059 §6`, отдельные `Q` не заводятся, прецедент формы `ADR-051 §2`):** `nbkr_cache.json` (365 записей `2025-04-29…2026-04-28`, все `87.4293`, создан `2026-04-29T15:04:57Z`, Update = Creation; источником данных таблицы НЕ является — 49 различных значений за то же окно); остаток `ADR-049 §5` (почему `min_date` = `2025-04-29`) — адресуется здесь. **Граница `ADR-059 §7` ПЕРЕСЧИТАНА сессией FX-MAY-WINDOW (Шаг 4):** MIN/MAX по всей таблице `core.dim_fx_rates` (456 строк) = `[87.2319; 87.45]`, ШИРЕ прежней `[87.4205; 87.45]`; разброс на майском эталоне (10 232 903,20 KGS) = **292,56 USD (0,25 %)**, не 39,49 USD (0,0337 %) — прежняя оценка веса вопроса устарела. Новый минимум лежит вне окна мая, само окно остаётся плоским. Если дефект подтвердится — фикс-форвард в слой курсов, НЕ в марты (`ADR-062 §7`). Квалификация (закрыт/остаётся сужен) — за архитектором, этой сессией не производится (`ADR-058 §7`). **С учётом D1 (владелец, вне сессии, 2026-07-28):** все три различителя D1–D3 теперь исполнены и указывают в одну сторону для входа в окно — архив источника, факт таблицы и (по D2) единственная неразличённая деталь метаданных `LOAD`-задания совпадают на `87.4205`. Формальное закрытие `Q-70` (а не только сужение) — по-прежнему решение архитектора, не автоматическое следствие этой находки **Дописка `ADR-101 §4` (2026-08-02, архитектор): НЕ ЗАКРЫТ, и причина конкретна.** Различитель D1 покрывает `2026-04-25…2026-05-10`, окно застывания — `2026-05-01…2026-06-02`; отрезок `2026-05-11…2026-06-02` (23 суток) источником не сверялся вовсе, и именно в нём D2 нашёл шесть ручных forward-fill `INSERT`, копирующих последний известный курс. Закрытие сейчас было бы распространением совпадения, установленного на ВХОДЕ в окно, на его СЕРЕДИНУ, то есть выбором правдоподобной половины (`_METHOD §6`, прецедент `ADR-045 §1`). Остаток адресуется задачей `FX-MAY-WINDOW-D1-TAIL` (класс A, гейтов нет): файл источника уже лежит в репо, читатель известен. |
| ORACLE-AUTOMATION | (ADR-067 §5c, 2026-07-27) Скрипт помесячного посева эталона (report-API/entity-API → `/reference/pnl_<YYYY-MM>.md` и далее по реестру паритета) вместо ручной выгрузки | задача | бриф после PARITY-REGISTRY (форма эталона определяется реестром); приёмка: эталон нового месяца сеется одной командой, формат идентичен ручному seed **Дописка сборки 2026-08-01 (`ADR-091 §6`):** разрезана — написание скрипта посева есть класс A и доступно сейчас; первый живой прогон с секретом есть класс B, мандат НЕ выдан, выдаётся поимённо по прецеденту `ADR-079 §9`. |
| CC-READONLY-PRINCIPAL | (`ADR-077`, 2026-07-28) Перевод класс-A сессий Claude Code на принципал с правами по факту использования: снять подтверждение с каждого запуска скрипта, не превращая ask-гейты в декорацию | задача (замер + инфра) | **READY, шаг 1 (класс A).** Замер трёх неизвестных (`ADR-077 §3`): D1 механизм трения (`/permissions` + один наблюдённый запуск); D2 уважает ли `bq` impersonation, иначе цена варианта растёт на управление файлом-ключом; D3 отделима ли запись в `marts.expenses_staging` от живой `marts.expenses` при общем датасете `marts`, и если нет, то цена выноса staging в отдельный датасет. Состав ролей выводится из провенанса `reference/_scratch_*` (`ADR-077 §4`), не назначается заранее. Артефакт → `/reference/cc_readonly_principal_<date>.md`. Шаги 2-3 (создание принципала, правка `.claude/settings.json`) — класс B, отдельный апрув; верификация обязана нести контр-прогон (`ADR-077 §5`). Приоритет: ниже `PARITY-REGISTRY`, не гейтит её **Дописка сборки 2026-08-01 (`ADR-091 §5`):** шаги 2-3 разрезаны — подготовка патча `.claude/settings.json` и скрипта выдачи ролей есть класс A, исполнение обоих за владельцем; основание не класс операции, а совпадение исполнителя и бенефициара. Отдельно: различитель D1 («вывод `/permissions`») в десктопном режиме неисполним как записан, два пути названы в `reference/mandate_table_audit_2026-08-01.md §4a`, выбор за сессией, берущей задачу. **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-76 | (CC-HOOK-REVIEW, 2026-07-27) Охват механических проверок хука не замерялся: (а) набор гомоглифов `[МСЕАРОТНКВХ]` в проверке №2 — какие кириллические подделки НЕ ловятся, не установлено; (б) фильтр проверки №1 `^(_[A-Z]\|[0-9]{2}_).*\.md$` не покрывает `CLAUDE.md`, `MIGRATION_MAP.md`, `PROJECT_REFERENCE_v6.md`, `RUNBOOK_v8.md`, `TZ-MIGRATE-TO-DOCS-METHOD.md` | факт (охват не замерен) | **ОТКРЫТ, НЕ блокер (`ADR-072 §7`).** Дефекта на текущем содержимом нет — прогон логики проверки №1 по всем 18 подпадающим документам @ `1680524` дал 17 OK и 1 FAIL (`_GENERATOR.md`, закрыт `ADR-072 §5`); первая строка всех пяти непокрытых файлов фактически корректна. Расширение охвата — предмет `ADR-054`/`ADR-041`, без замера есть импровизация (`_METHOD §6`). Закрывается: инвентаризация кириллических гомоглифов латинских букв, встречающихся в ID проекта, + решение о фильтре проверки №1. **Триггер промоушена:** первая подделка, прошедшая хук, ЛИБО первый документ вне фильтра с разъехавшимся заголовком. **(в) BSD grep не замерен:** все измерения `CC-HOOK-REVIEW` — GNU grep 3.11; корректность паттерна и достаточность отсутствия закрепления локали на BSD подлежат подтверждению одной командой на macOS владельца: `bash tools/hooks/selftest.sh` → ожидание `провалено 0`. Пустой/противоречивый результат = гэп наблюдения, не факт (`ADR-072 §9`) **(в) ОСНОВАНИЕ ИЗМЕНЕНО (`ADR-074 §2/§5`):** закрепление локали по-прежнему ОТКЛОНЕНО (`ADR-072 §1` в этой части в силе), но класс гомоглифов переведён на альтернацию целых символов и от локали больше не зависит (замер: 0 ложных в `C`, `C.UTF-8` и несуществующей локали, 3 подделки из 3 ловятся во всех трёх). Замер `ADR-072 §1` о чистоте легитимной строки «в четырёх режимах» ОПРОВЕРГНУТ на реальном содержимом репо: в `LC_ALL=C` ложные совпадения в шести файлах, включая `07_STATE.md` и `07_ARCHIVE.md`. **ЗАКРЫТ ФАКТОМ (2026-07-28, машина владельца, macOS, BSD grep):** `bash tools/hooks/selftest.sh` дал «пройдено 28, провалено 0»; функциональная проба локали нашла `en_US.UTF-8` как многобайтную, набор прошёл ПОЛНОСТЬЮ в обеих локалях, включая `C`. Тем самым закрыт и остаток `ADR-072 §7` («поведение паттерна под BSD grep не замерено»): паттерн `ADR-074 §2` ведёт себя на BSD grep идентично GNU. Первый прогон на macOS дал два ПРОВАЛА, но не в хуке и не в фикстуре, а в формах верификации самотеста (`od`-формат и подбор локали по имени) — это отдельный факт, зафиксирован `ADR-074 §9`, к охвату проверки не относится. Пункты (а)/(б) без изменений **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-75 | (CC-PILOT-BOOTSTRAP, 2026-07-27) В `.claude/settings.json` правило `Bash(bq query:*)` стоит в `ask`, `Bash(bq query --dry_run*)` — в `allow`. По документации инструмента правила проверяются в порядке deny → ask → allow, первое совпадение решает, и `ask` побеждает более точный `allow` ⇒ пробные запросы без записи предположительно всё равно требуют подтверждения | факт (не подтверждён замером) + решение (форма) | **ОТКРЫТ, решение отложено до замера (`ADR-071 §6`, выбор владельца), НЕ блокер.** Утверждение взято из документации, не из наблюдения на этой установке ⇒ фактом не является (`ADR-021 §2`, `05` Часть I ★). Различитель: Шаг 0 — вывод `/permissions` плюс первый фактический `bq query --dry_run` в сессии. Риск не в безопасности (перекос в сторону лишнего вопроса), а в привыкании владельца подтверждать механически — что подрывает `ADR-070 §2(d)`. Форма фикса НЕ назначается; правка состава прав меняет `ADR-070 §1` ⇒ требует отдельного ADR. **Триггер решения:** ≥10 лишних подтверждений на dry-run за одну сессию **ФАКТ ДОБАВЛЕН (владелец, 2026-07-28, `ADR-075 §7`):** в пилотной сессии подтверждения запрашивались на КАЖДЫЙ запуск скрипта, включая read-only шаги. Механизм не замерен: гипотеза — команда вида `bash script.sh` не покрыта ни одним правилом `.claude/settings.json`, а `ADR-055 §1` обязывает доставлять исполняемое именно файлом-скриптом, то есть правило доставки и список прав противоречат друг другу по построению. Из документации не берётся (`ADR-021 §2`), различитель — вывод `/permissions` плюс один наблюдённый запуск. На read-only шагах подтверждение защитной функции не несёт и работает только на привыкание, то есть ровно на риск, ради которого `Q-75` заведён. Правка состава прав меняет `ADR-070 §1` ⇒ отдельный ADR (`ADR-071 §6`). **НАПРАВЛЕНИЕ РЕШЕНО (`ADR-077`, 2026-07-28, владелец):** ограничение переносится на слой личности, а не на список команд; вариант «разрешить запуск скриптов» отклонён навсегда (`ADR-077 §2`); переход гейтится замером трёх неизвестных (`ADR-077 §3`, задача `CC-READONLY-PRINCIPAL`), до замера действует промежуточная мера (`ADR-077 §6`). `Q-75` остаётся ОТКРЫТЫМ: состав прав не изменён. **Триггер закрытия переформулирован:** исполнение замера и последующая правка прав, ЛИБО отрицательный результат D2/D3, при котором владелец пересматривает объём **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-84 | (`PROCESS-DEBT`, 2026-07-29) Промоушен правила «пункт блока последствий несёт маркер времени прихода» (`ADR-083 §3`) из holika-локального `05` Часть III в канон `_METHOD §8` | решение | **DEFER, owner-gated.** `_METHOD.md` доменно-нейтрален и дистиллирован из эталона `ilyasbazarov/docs` @ `cb33fb4`; его правка задевает проекты за пределами holika — тот же blast radius и то же основание, что у `Q-40` (`ADR-021 §4`). Настоящим ADR правится ТОЛЬКО `05` Часть III, расхождение с каноном названо сознательно, а не оставлено незамеченным. **Триггер промоушена:** правило сработало ≥1 раза вне holika ЛИБО владелец решает выровнять канон общим проходом вместе с `Q-40` **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| LAUNCH-RUNBOOK | (`ADR-082`, 2026-07-29) Пусковые сценарии сессий: чат-архитектор, генерация брифа, одна задача, параллель, сборка. До этого решения порядок запуска нигде не был записан, ссылки на роли владелец прикладывал вручную каждый раз, а `08_TASK_BRIEF_TEMPLATE` требовал raw-URL и запрещал сессии исполнять самой, что противоречило агенту `executor` | задача (процесс) | **DONE фаза «правила» (2026-07-29).** Введён `09_RUNBOOK.md` (пять режимов, готовые тексты первого сообщения, шпаргалка); в `08` добавлены поля «Параллель» и «Файлы на запись», убраны требование raw-URL и запрет на самостоятельное исполнение; в `_GENERATOR.md` разведены запуск с диска и запуск в чате; заведены агенты `assembler` и `generator`. **Остаток:** выбор роли через механизм агентов на десктопном приложении не проверен наблюдением (`ADR-082 §3`), закрывается первым прогоном; тексты первого сообщения работают и без агентов. **Гейтит:** ничего. Семь дефектов, обнаруженных при первом прогоне рюнбука, разобраны `ADR-083`: парная природа признака параллелимости (`§1`), самопротиворечие шаблона брифа (`§2`), время прихода пунктов блока последствий (`§3`), ненадёжная проверка буфера (`§4`), скрытый каталог деревьев (`§5`), режим Г в своём дереве (`§6`), коллизия имён генерации и исполнения (`§7`). Остаток `ADR-082 §3` (агенты не проверены наблюдением) настоящим ADR не закрывается **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-85 | (`STATE-STANDUP`, 2026-07-30) Правило «стенд-ап заменяется, детали дописываются» введено как holika-локальное в `05_CONVENTIONS` Часть III, тогда как канон `_METHOD §8` по-прежнему несёт одну строку «Текущий фокус» | решение | **DEFER, owner-gated.** `_METHOD.md` доменно-нейтрален и дистиллирован из эталона `ilyasbazarov/docs` @ `cb33fb4`; его правка задевает проекты за пределами holika. Основание и форма идентичны `Q-40` и `Q-84`. **Триггер промоушена:** тот же дефект (дописывание вместо замены) воспроизведён в другом проекте ЛИБО владелец выравнивает канон общим проходом вместе с `Q-40` и `Q-84` **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| LS-PERIOD-CONTRACT | (`ADR-087`, 2026-07-30) Контракт периода Custom Query: параметры дашборда по умолчанию, снимок «на момент» единственное исключение и объявляется в инвентаре плюс подписью на странице, скользящее окно литералом запрещено | задача (класс B, инструменту не отдаётся) | **READY.** Правило принято `ADR-087`, осталось исполнение: (а) объявить исключения для `msklad_customer_invoices_ar` (страница «Операционка») и `msklad_inventory_latest` (страница «Склад») строкой в инвентаре — исполнимо сейчас; (б) подписи на этих двух страницах дашборда, страницы известны; (в) `fact_returns` в этой задаче НЕ чинится, свёрнут в `LS-RETURNS-FX-HARDCODE` (`ADR-087 §5`). Исполняет владелец в интерфейсе Looker Studio **Дописка сборки 2026-08-01 (`ADR-091 §3`):** разрезана — пункт (а) (строки-исключения в `reference/ls_custom_queries_2026-07-30.md`) есть класс A и отдаётся инструменту; пункт (б) (подписи на страницах) за владельцем. |
| Q-92 | (`PARITY-MOMENT-ZONE`, 2026-07-31) Эталонные сутки паритета: московские сутки интерфейса, бишкекские сутки бизнеса, либо объявленное отступление (`ADR-068 §2`) | решение (owner-gated) | **ОТКРЫТ, гейт `ADR-088 §6` СНЯТ фактом** (ночные полосы за май-2026 пусты по обеим парам: 0 из 127 документов `entity/demand`, 0 из 8 `entity/retailsalesreturn`), прежняя формулировка «НЕ задаётся до замера» устарела и вводила в заблуждение. **Вопрос сужен до одного бита — исключается ли вариант (a)** (московские сутки интерфейса). Основание сужения (`ADR-101 §6`): при вариантах (b) и (c) целевое правило суток для четырёх дефектных веток загрузки ОДНО И ТО ЖЕ — бишкекские сутки `DATE(M+6ч)`, ровно то, что ветка продаж уже делает (`ADR-088 §3/§4`); только (a) требует `DATE(M+3ч)` и пересчёта `core.fact_sales_profit`. Рекомендация архитектора — исключить (a). **Переквалифицирован в предложение клиенту (решение владельца, чат 2026-08-02):** правом менять настройки в аккаунте клиента владелец не располагает, поэтому вариант (b) передаётся клиенту предложением — задача `MSKLAD-TZ-PROPOSAL`, текст готов (`reference/msklad_timezone_proposal_2026-08-02.md`), включает обратимую пятиминутную проверку ретроактивности, попутно закрывающую несделанный второй замер сдвига `ADR-088 §10`. Fallback при отказе клиента — вариант (c). **Ничего больше не гейтит:** подготовка `INGEST-MOMENT-ZONE-FIX` разгейчена независимо от ответа клиента. Открытость сохраняется: пустота полос установлена для мая-2026, а не навсегда — первый документ в полосе `[18:00;24:00)` UTC внутри зоны паритета возвращает вопросу численную цену. Варианты и их цена без изменений в `ADR-088 §7` (статус там `proposed`) |
| INGEST-MOMENT-ZONE-FIX | (`ADR-088 §4`, 2026-07-31) Приведение четырёх веток — возвраты и закупки в `cf-facts`, списания и комиссии в `cf-finance` — к явному правилу суток; ветка продаж НЕ трогается, она корректна | задача (класс B) | **READY, мандат НЕ выдан, гейт `Q-92` СНЯТ.** Про `core.fact_payments.moment` (уже DATE на приёме) правило суток не установлено и не предполагается — устанавливается отдельно при постановке задачи. **Гейт снят (`ADR-101 §6` плюс решение владельца 2026-08-02):** целевое правило суток известно при обоих оставшихся вариантах и одинаково — бишкекские сутки `DATE(M+6ч)`; ответ клиента по часовому поясу (`MSKLAD-TZ-PROPOSAL`) подготовку не гейтит. Подготовка патча в снапшоте `reference/code/` есть класс A и доступна сейчас; прежняя формулировка `ADR-091 §7` («ПОСЛЕ `Q-92`») superseded-in-part. Деплой без изменений класс B, гейт `ADR-065`. **Бриф подготовки собран (сессия `INGEST-MOMENT-ZONE-FIX-GEN`, 2026-08-03):** `briefs/INGEST-MOMENT-ZONE-FIX.md`, не взят; уточнена локализация дефекта `ADR-088 §4` для «списания/комиссии» — несущий CF называется `cf-loss-commission`, дефекта в его ингесте нет, настоящий дефект сидит в `reference/sql/sq_marts_expenses.sql:20,44`. **Подготовка патча DONE, READY к деплою (сессия `INGEST-MOMENT-ZONE-FIX`, 2026-08-08):** три диффа готовы и проверены числово — `fetch_returns.py::_parse_moment_kgt`/`fetch_purchases.py::_parse_date_kgt` переведены на разбор UTC + сдвиг `+6ч` через общую `helpers.py::parse_moment_to_bishkek_date`; `sq_marts_expenses.sql:20,44` — `CAST(... AS DATE)` → `DATE(..., 'Asia/Bishkek')`. Сплошной поиск паттерна по `reference/code/`/`reference/sql/` (пять совпадений) закрыт без новых находок вне трёх известных мест. Артефакт `reference/ingest_moment_zone_fix_2026-08-08.md`. Деплой не выполнялся, гейт `ADR-065` в силе. Сопутствующее предложение документирования правила суток в `02_ERP_CONTRACTS.md` вынесено отдельной строкой `ERP-CONTRACTS-DAY-RULE-DOC` (`proposed`, `_ASSEMBLER.md §7`). |
| ERP-CONTRACTS-DAY-RULE-DOC | (`INGEST-MOMENT-ZONE-FIX`, 2026-08-08, `NEW_DECISIONS` proposed) Документирование правила суток `DATE(M+6ч)`, Asia/Bishkek, для `order_date` (`core.fact_purchases`) и `return_date` (`core.fact_returns`) в `02_ERP_CONTRACTS.md`; новая строка для `core.fact_loss.moment`/`core.fact_commissionreportin.moment` (хранятся UTC без изменений при `MERGE`, местное правило суток применяется не при загрузке, а в `sq_marts_expenses.sql`); уточнение формулировки `ADR-088 §4` — несущий CF расходной ветки называется `cf-loss-commission`, не `cf-finance` | решение | **OPEN, proposed (требует апрува владельца).** `02_ERP_CONTRACTS.md` — `STABLE`, правится только через ADR; сборка не апрувит (`_ASSEMBLER.md §7`). Полный текст предложения (четыре пункта `[сейчас]`) — блок сессии `reference/_inbox/session_INGEST-MOMENT-ZONE-FIX_2026-08-08.md` в истории git (`ADR-080 §4ii`). |
| Q-101 | (`PARITY-GAPS-ADJ`, 2026-08-01, побочный дефект §5 `ADR-098`) `fetch_returns.py:70` определяет `has_basis` по наличию ключа `demand`, а не по его значению; у всех восьми розничных возвратов мая ключ присутствует, поэтому колонка `core.fact_returns.has_basis` = `true` вопреки собственному докстрингу `fetch_returns.py:13` («retailsalesreturn никогда не имеет demand») | факт (дефект классификации, денежных величин не трогает) | **DEFER, ничего не гейтит.** Закрывается вместе с деплоем `INGEST-MOMENT-ZONE-FIX` — отдельной задачи и отдельной строки мандата не заводится, потому что отдельного деплоя не требует. Триггер промоушена: любая задача, опирающаяся на `has_basis` как на признак. **Уточнение мандата (`INGEST-MOMENT-ZONE-FIX-MANDATE`, 2026-08-09, `ADR-147 §6`): выданный мандат на деплой `INGEST-MOMENT-ZONE-FIX` эту строку НЕ закрывает** — `fetch_returns.py:70` патчем, вошедшим в объём мандата, не тронут. Рекомендация архитектора — дополнить патч одной строкой в той же ветке деплоя; при отказе обещание из этой строки снимается. Выбор за сессией-исполнителем, молчаливое закрытие строки запрещено |
| SALES-CONSIGNMENT-REVENUE | (`ADR-103 §7`, 2026-08-02) Отгрузка комиссионеру признаётся выручкой BI: товар, переданный на реализацию, попадает в `core.fact_sales_profit.revenue_kgs` как выручка месяца отгрузки, тогда как МойСклад продажей его не считает. За май-2026 — один документ `entity/demand` `04235` (`moment 2026-05-12 14:44:00.000`, `1 980 000,00` KGS, `2,1 %` месячной выручки, больше всего розничного канала). Поверхность инвесторская (`marts.sales_overview` → страница LS «Инвестор») | задача | **READY, гейт замера снят (`ADR-104 §5`): масштаб дефекта замерен по всей зоне паритета и оказался единичным** — ровно один документ, доля `0,675 %` зоны паритета. Форма фикса не назначена: зависит от того, есть ли у отгрузок комиссионерам различающий признак в уже загружаемых полях. Класс подготовки — A (правка снапшота `reference/code/cf-facts/`, ничего не деплоит); деплой — B, гейтится `ADR-065` **Подготовка DONE (сессия `SALES-INGEST-PATCH`, 2026-08-03):** форма фикса определена — различитель `agent_id` (список трёх комиссионных контрагентов, `ADR-104 §2/§5`), исключение позиций комиссионного канала внесено в `fetch_demands.py`; артефакт `reference/sales_ingest_patch_2026-08-03.md`. Деплой — класс B, гейт переведён на задачу `SALES-INGEST-PATCH-DEPLOY` (`PARITY-STOCK-INTRANSIT-CLOSE`, третий ход, `ADR-126 §6`); прежний гейт `ADR-065`/`DEPLOY-PROCEDURE` снят фактом (обе строки закрываются одним и тем же деплоем). **Гейт снят фактом (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, `ADR-140 §последствия`): `SALES-INGEST-PATCH-DEPLOY` исполнена и архивирована DONE (2026-08-05/07); статус строки этим не переоценивается — остаётся владельцу.** |
| SALES-PERIMETER-EXTEND | (`ADR-103 §8`, 2026-08-02) Расширение периметра ингеста продаж на `entity/retaildemand` и на продажи по `entity/commissionreportin` — следствие выбора владельцем варианта (a) объекта паритета продаж | задача | **READY, оба гейта сняты.** Гейт решения снят выбором владельца (a) (`ADR-103 §8`); гейт подтверждающего замера снят (`ADR-104 §2`). Тем же выбором адресована правка правила-моста `reference/parity_registry.md` строки 18-19: объект паритета продаж = число `report/profit/*`. Форма — ОТДЕЛЬНЫЙ ингест по прецеденту `ADR-024`, НЕ расширение патча `MERGE` из `ADR-100 §7`. Класс подготовки — A, деплой — B **Подготовка DONE (сессия `SALES-INGEST-PATCH`, 2026-08-03):** текст патча (`reference/code/cf-facts/fetch_perimeter.py`, `config.py`, `bq_ops.py`, `main.py`, различитель — `agent_id`) и правка `reference/parity_registry.md` строки 18-19 готовы против снапшота, не задеплоены; артефакт `reference/sales_ingest_patch_2026-08-03.md`. Деплой — класс B, гейт переведён на задачу `SALES-INGEST-PATCH-DEPLOY` (`PARITY-STOCK-INTRANSIT-CLOSE`, третий ход, `ADR-126 §6`); прежний гейт `ADR-065`/`DEPLOY-PROCEDURE` снят фактом (обе строки закрываются одним и тем же деплоем). **Гейт снят фактом (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, `ADR-140 §последствия`): `SALES-INGEST-PATCH-DEPLOY` исполнена и архивирована DONE (2026-08-05/07); статус строки этим не переоценивается — остаётся владельцу.** |
| SALES-REFRESH-WINDOW | (`ADR-097 §5`, 2026-08-01) Окно обновления `cf-facts` (7/90 суток скользящим) не покрывает якорь паритета `2026-05-01`, режима досева у продаж нет | задача | **Класс A:** подготовка ОДНИМ патчем снапшота `reference/code/cf-facts/` по трём дефектам одной и той же операции `MERGE` (`ADR-100 §7`, объединение принято владельцем 2026-08-01): (1) окно — досев отдельным режимом с раздельными окнами выборки и `MERGE`; (2) отсутствие ветки удаления (`bq_ops.py:237-305`) — строки, исчезнувшие в источнике, остаются в `core` навсегда; (3) `transaction_date` не обновляется при матче (`bq_ops.py:240-256`) — документ, перенесённый по дате задним числом, остаётся на дате первой записи. **Класс B, НЕ выдан:** деплой `cf-facts`, дополнительно гейтится `ADR-065`. **Три ловушки, названные заранее, каждая обязана быть снята dry-run до деплоя, а не рассуждением:** (i) при досеве окно выборки и окно `MERGE` совпадают по построению, поэтому строка старше окна уйдёт в `INSERT` и задвоится (`ADR-097 §5`); (ii) ветка удаления опасна ровно тем же условием окна внутри `ON` (`bq_ops.py:238`): строка вне окна не матчится и под «не найдено в источнике» подпадает формально, поэтому ветка удаления без собственного явного ограничения по дате рискует снести историю за пределами окна; (iii) допустимость обновления `transaction_date` (колонка партиционирования) внутри `MERGE` этой адъюдикацией НЕ установлена и предположением не закрывается — сессия обязана проверить это первой командой и, если ограничение есть, назвать альтернативную форму (удаление плюс вставка либо включение даты в ключ сопоставления) вместо того, чтобы обходить молча. Пишет: `reference/sales_refresh_window_<date>.md` **Бриф собран (сессия `SALES-REFRESH-WINDOW-GEN`, 2026-08-01):** `briefs/SALES-REFRESH-WINDOW.md`, три варианта текста патча (не выбор одного) — досев отдельным режимом с раздельными окнами; разовый пересев мая явными датами; плашка «датированный снимок» без правки кода. Форма явного `INSERT (колонки)` (C1, `ADR-030`) сохраняется; источник `cogs_source` для диапазона шире 7/90 суток назван открытым вопросом, не домысливается. Готов к исполнению отдельной сессией, не взят. **Подготовка DONE (сессия `SALES-REFRESH-WINDOW`, 2026-08-01).** Артефакт `reference/sales_refresh_window_2026-08-01.md`: три варианта патча (досев отдельным режимом / разовый пересев мая явными датами / плашка-снимок без кода), каждый разбирает три ловушки задвоения/удаления/даты на уровне конкретного SQL, форма `INSERT (колонки)` сохранена во всех ветках включая ветку удаления; сравнительная таблица без ранжирования. `cogs_source` для диапазона шире 7/90 суток и допустимость `UPDATE transaction_date` в `MERGE` остаются открытыми, не домысливались. Выбор варианта и деплой — класс B, мандат не выдан, гейт `ADR-065`. **Выбор варианта СДЕЛАН (`ADR-115 §7`, 2026-08-03):** вариант 3 — к исполнению (плашка «датированный снимок», без правки кода), вариант 2 отклонён. Вопрос допустимости `UPDATE` партиционирующей колонки внутри `MERGE` ЗАКРЫТ прошедшим dry-run (`SALES-MERGE-DRYRUN`). **Переоценка (`ADR-144`, 2026-08-08):** отсрочка варианта 1 («цель после `CODE-REPO-STANDUP` шага 2b») СНЯТА — обе её посылки опровергнуты (цена бездействия измерена, `127 621,20` KGS/месяц, `SALES-JULY-STALE-COHORT`; названный деплойный гейт `cf-facts` не существует с `2026-08-03`, снят и дважды пройден). Владелец решил объём и форму (`ADR-144 §8`): узкая форма — только ветка удаления, режим `topoff` не строится; ОБА `MERGE` функции `cf-facts` (`_build_merge_sql` и `_build_perimeter_merge_sql`, второй найден без строки в реестре и адресуется этим же объёмом, `ADR-144 §7`) одним патчем; окно выкладки `2026-08-25`…`2026-09-29`, риск `R1` (`ADR-101`, `ADR-140 §5`) объявляется до выкладки. Источник COGS шире 90 суток остаётся открытым, но перестаёт быть гейтом этой задачи — он гейтит только режим `topoff`, который этой формой не строится. Статус — `READY`. **Предусловие деплоя, которого у задачи не было (`ADR-145 §4/§5`, 2026-08-08):** до запроса мандата класса B патч обязан получить защиту от рассогласования окна staging и окна `MERGE`. Посылка ветки удаления («строки внутри окна, отсутствующие в свежей выборке за то же окно») названа верно, но её выполнение не обеспечено ничем: `window_days` приходит из тела HTTP-запроса (`main.py:88`) и доходит до `MERGE` без проверок (`main.py:98`/`:213-224`), единственный предохранитель промоута — непустота staging (`bq_ops.py:396-402`), а в `STG_FACT_SALES` пишут `WRITE_TRUNCATE`-ом оба режима, часовой и недельный (`bq_ops.py:136-159`). Следствие: `mode=promote` с `window_days` шире фактического покрытия staging удалит порядка 83 суток продаж. Триггеры реальны: ручной первый прогон, которого требуют оба патча, и общий staging у двух расписаний (`msklad-pipeline-hourly` `0 * * * *`, `11_INFRA_FACTS.md:27`; недельный пайплайн — три отдельных вызова, `workflow_weekly.yaml:61/166/207`, ничем не сериализованные). Свойства защиты: сравнить покрытие staging по `transaction_date` с окном `MERGE`, ОТКАЗАТЬ при недостаточном покрытии (не предупредить), сработать ДО `MERGE`; форма не назначена (`_METHOD §6`). Основание уровня предусловия, а не пункта мандата: у этого патча единственного в очереди нет работающего отката — повторный деплой ревизии вернёт код, но не удалённые строки (`ADR-145 §5`). **Порядок (`ADR-145 §3`): деплоится ВТОРОЙ**, после `SALES-DOCUMENT-OWNER-DEPLOY`. **Деплой исполнен и ОТКАЧЕН тем же днём (`ADR-161`, 2026-08-11).** Ревизия `cf-facts-00012-ber` задеплоена, дошла до шага 8 (первый боевой прогон), где найден структурный гэп: `mode=weekly`/`mode=hourly` игнорируют `window_days` тела запроса, окно жёстко `90`/`7` суток. Прогон НЕ запускался. Проверка этого факта архитектором (`SALES-REFRESH-WINDOW-ROLLBACK-ADJ`) вскрыла ВТОРОЙ, более тяжёлый дефект уже задеплоенного кода: обе ветки удаления `MERGE` (`bq_ops.py:379`, `:647`) ограничены только датой, а `core.fact_sales_profit` пишут два независимых загрузчика без признака происхождения строки — слияние периметра считает «исчезнувшими» все оптовые строки, слияние продаж — все строки периметра. Под удалением в окне 90 суток — `269 440 872,10` KGS оптовой торговли плюс `12 698 192,29` KGS периметра/без канала; срок срабатывания — недельный конвейер `2026-08-16 01:00 UTC`. Ущерба нет (снимок `core.fact_sales_profit_snap_20260811` против таблицы — нули по всем каналам). Обслуживание возвращено на `cf-facts-00011-mab`, откат подтверждён тремя read-back (100% трафика, нулевое сопоставление сразу после отката, нулевое сопоставление после первого естественного `promote`). **Владелец выбрал вариант 1 настоящего фикса** (ограничение области удаления по происхождению строки — единый источник меток периметра, `COALESCE` по паре полей в обеих ветках, параметризация недельного окна): патч подготовлен в снапшоте (`s/SALES-REFRESH-WINDOW-ROLLBACK-ADJ`, коммит `a336700`) и **независимо проверен** (`SALES-REFRESH-WINDOW-SCOPE-VERIFY`, 2026-08-11): восемь пунктов проверки закрыты доказательством (импорты + `py_compile`, направление предиката, `COALESCE`, отрендеренный SQL, `--dry_run` обоих `MERGE`, разбиение таблицы без зазора/пересечения, умолчание `_run_weekly_load` сохраняет `90`, сплошной поиск — третьего писателя `core.fact_sales_profit` нет). Две находки для архитектора остались открытыми: `Q-107` (текст патча ссылается на несуществующую явную передачу `window_days` в `workflow_weekly.yaml` для `mode=weekly`) и числовой ориентир (устарел на 20 строк живого прироста, разбиение переподтверждено — не требует правки, отдельной строки не заводит). **Новый мандат класса B на деплой патча варианта 1 этим циклом НЕ выдан** — решение владельца по `Q-107` предшествует запросу мандата. Снимок `core.fact_sales_profit_snap_20260811` и ветка `deploy/cf-facts-2026-08-11-refresh-window` код-репо сохраняются как provenance/откат. Полный разбор — `reference/sales_refresh_window_deploy_2026-08-11.md`, `reference/sales_refresh_window_rollback_adj_2026-08-11.md`, `reference/sales_refresh_window_scope_verify_2026-08-11.md`. **Гейт снят (сессия `SALES-REFRESH-WINDOW-VERIFY-ADJ`, 2026-08-11, `ADR-162`/`ADR-163`):** «`Q-108` закрыт (`VERIFY-ADJ`, 2026-08-11); полезная нагрузка деплоя атрибутирована полностью, чужого не несёт; мандат класса B выдан заново владельцем. Гейтов не осталось, форма запуска — пересборка брифа (`SALES-REFRESH-WINDOW-DEPLOY-GEN`), затем деплой. `Q-107` (`proposed`) деплой технически не блокирует». Мандат — `reference/sales_refresh_window_deploy_mandate_2026-08-11.md`. Бриф под новый мандат пересобран (сессия `SALES-REFRESH-WINDOW-DEPLOY-GEN`, 2026-08-11) — `briefs/SALES-REFRESH-WINDOW-DEPLOY.md`. **Дописка (сессии `SALES-REFRESH-WINDOW-DEPLOY`/`SALES-REFRESH-WINDOW-DELETE-ADJ`, 2026-08-12, `ADR-164`):** «Деплой варианта 1 исполнен `2026-08-11`, ревизия `cf-facts-00014-doh`, трафик `100 %`; откат НЕ исполнен по решению владельца. Боевой прогон (`106` суток) удалил `36` строк, из них `7` опознаны как сироты `ADR-100 §1` по трём совпавшим признакам — по ним патч сработал штатно. Третье число приёмки признано мис-специфицированным (`DELETE-ADJ`), верный критерий — «каждая удалённая строка отсутствует в источнике». Остаток `29` строк / `2 407 220,42` KGS разбирается задачей `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`. Предохранитель периметра сработал штатно и отказал в `MERGE` периметра (`500`) — периметрийные строки не тронуты. Ветка код-репо `…-v2` (`9ae9a84`) НЕ слита. Срок: недельный конвейер `2026-08-16T01:00Z`». Полный разбор опознания — `reference/sales_refresh_window_delete_adj_2026-08-12.md`. **Дописка (`SALES-REFRESH-WINDOW-DEFECT-ADJ`, 2026-08-12, `ADR-165`):** «Вывод различителя о `19` строках дефекта СНЯТ: критерий («документ в архиве есть, позиции нет ⇒ дефект») прогнан по размеченному набору из `7` строк с доказанной законностью удаления (`ADR-100 §1`) и метит дефектом `4` из `7` — подпись принимает оба значения на размеченном наборе, информации о дефекте не несёт. Причина — механизм сирот `ADR-100 §1` ПОЗИЦИОННЫЙ (документ остаётся, позиция исчезает), наблюдение одинаково порождается потерей позиции выгрузкой и законным удалением позиции в источнике. Все `29` строк (`2 407 220,42` KGS) возвращаются в неквалифицированные, `7` строк (`1 630 239,81` KGS) остаются доказанно законными. Квалифицирует только `SALES-REFRESH-WINDOW-SOURCE-PROBE` (класс B, мандат выдан `ADR-166`). Найден структурный гэп: предохранитель `_assert_staging_covers_merge_window` проверяет края окна, не полноту внутри него — проверка полноты выгрузки в патче отсутствует и объявлена предусловием повторного деплоя. Рекомендован откат кода до `2026-08-16T01:00Z` по непроверяемости инварианта полноты выгрузки, а не по доказанному дефекту — мандат выдан `ADR-166 §1`.» Полный разбор — `reference/sales_refresh_window_defect_adj_2026-08-12.md`. |
| Q-94 | (`MANDATE-TABLE-AUDIT`, 2026-08-01) Можно ли проверить язык ответа владельцу машинно. У инструмента есть механизм перехвата на конце хода, который в принципе способен прочитать текст ответа и заругаться на внутренний идентификатор без расшифровки (`ADR-056 §1`). Работает ли он так на этой установке — НЕ замерено | факт (не измерено) | **DEFER, не гейтит ничего.** Из документации не берётся (`ADR-021 §2`, `05` Часть I ★): нужен замер на этой установке. Правило `ADR-093 §1-§3` от замера не зависит и держится на ревью владельца. Основание держать вопрос открытым — `ADR-093 §5`: три причины из четырёх сняты мерами, конкуренцию проверяемого правила с непроверяемым снимает только измерение. **Триггер промоушена:** повтор нарушения `ADR-056 §1` после внедрения `ADR-093 §1-§3` **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| Q-97 | (`E1-T3-CLOSE`, 2026-08-01) Единственный документ зоны паритета, не переписанный после деплоя валютной правки: №05662, `23167c6f-76d1-11f1-0a80-0556001964ca`, `moment 2026-07-03`, `_loaded_at 2026-07-07 21:01:30`, контрагент ООО «ВАЛЕН СЕРВИС», статья «Бухгалтерские услуги», `sum_kgs = 30 000,00`. Все соседние июльские документы переписаны 2026-07-22…31, этот — нет | факт (две неразличённые гипотезы) | **ОТКРЫТ, оба различителя названы, цена мала.** Гипотеза (A): документ не-KGS и его сумма занижена — величина ошибки равна `30 000 × (rate−1)`; при рублёвом курсе порядка 1,25 это около 7 500 KGS, при KGS ошибки нет вовсе. Гипотеза (B): документ перестал приходить из источника (удалён либо изменён в МойСкладе), то есть перед нами строка класса Ghost Records (`ADR-011`) — тогда вопрос не в валюте, а в том, что таблица держит документ, которого в источнике уже нет. Гипотезы НЕ смешивать: (A) чинится пересчётом, (B) — удалением строки, выбор без наблюдения есть догадка (`_METHOD §6`). **Различитель, доступный владельцу без инструмента:** открыть документ №05662 в интерфейсе МойСклад — видны и валюта, и факт существования. **Различитель для инструмента:** один живой `GET` по `entity/paymentout/23167c6f-76d1-11f1-0a80-0556001964ca` — класс B (`msklad-token`), мандат НЕ выдан. **Гейтит:** ничего. Величина максимум порядка 7 500 KGS против майского эталона 10 232 903,20, то есть менее 0,08 % |
| AUDIT-SNAPSHOT-FIX-EMPLOYEES | (`ADR-101 §3`, 2026-08-02) `audit.dim_employees_snapshots` несёт `snapshot_at` последней колонкой при `INSERT … SELECT *, CURRENT_TIMESTAMP()` — та же конструкция, что сломала снимок товаров и прожила 45 суток невидимой (`ADR-019`); `core.dim_employees` версионирования не несёт, поэтому снимок есть единственный носитель истории (`ADR-008 §3`) | задача | **READY, форма фикса задана и не изобретается: вариант E `ADR-019 §3`** — пересоздать `audit.dim_employees_snapshots` с `snapshot_at` ПЕРВОЙ колонкой, запрос переписать на `SELECT CURRENT_TIMESTAMP() AS snapshot_at, * FROM core.dim_employees`. Подготовка DDL и текста запроса — класс A. Исполнение (пересоздание таблицы, правка живого `transferConfig`) — класс B, мандат НЕ выдан. Осознанный размен варианта E назван в `ADR-019 §3` и здесь не переоткрывается. Пишет: `reference/sql/sq_audit_dim_employees_snapshot.sql`, `reference/audit_snapshot_fix_employees_<date>.md` |
| AUDIT-COUNTERPARTIES-SNAPSHOT-RETIRE | (`ADR-101 §3`, решение владельца 2026-08-02) Ежедневная копия `core.dim_counterparties` в `audit.dim_counterparties_snapshots` дублирует историю, которая в ядре уже есть нативно (SCD2), потребителя не имеет и несёт ту же взведённую мину позиционного `INSERT … SELECT *`, что сломала снимок товаров | задача | **READY, решение принято — ретайр.** Приёмка: `transferConfig` снапшота контрагентов отключён либо удалён, факт подтверждён read-back; отсутствие потребителя переподтверждено НА МОМЕНТ ИСПОЛНЕНИЯ (не наследуется из сессии `UNGATED-QUEUE-ADJ`) сплошным поиском чтений `audit.` по `reference/sql/*.sql` и по инвентарю Custom Query с печатью совпавших строк (`ADR-044`); существующие данные таблицы НЕ удаляются — ретайр останавливает дальнейшее накопление, а не стирает накопленное (решение об удалении данных отдельное и здесь не принимается). Класс B (правка живой конфигурации), мандат НЕ выдан |
| INGEST-CURRENCY-ASSERT | (`ADR-101 §5`, 2026-08-02) Класс ошибки «забыли умножить на курс» непроверяем в хранилище по построению: таблицы фактов не несут ни валюты документа, ни суммы в минорных единицах, ни применённого курса — проверка обязана стоять в слое загрузки, где эти величины есть на руках | задача | **READY по шагам 1-2.** Шаг 1 (класс A): снять исходник и актуальную ревизию `cf-dq`, напечатать фактический текст проверки `currency_normalization` — закрывает названный `CONTEXT GAP` и `Q-6`, попутно сужает residual `Q-3`. Шаг 2 (класс A): подготовить проверку в снапшоте `reference/code/` — для каждой позиции либо валюта документа есть валюта аккаунта, либо применён `rate.value` документа (при отсутствии — курс по правилу `ADR-010`); ответ бинарный, порога нет и калибровки не требуется. Шаг 3 (класс B, мандат НЕ выдан): деплой, дополнительно гейтится `ADR-065`. Действующий агрегатный порог `avg_revenue_kgs < 10M` не снимается, но перестаёт считаться покрытием этого класса. Пишет: `reference/ingest_currency_assert_<date>.md`, `reference/code/` **Бриф шагов 1-2 собран (сессия `INGEST-CURRENCY-ASSERT-GEN`, 2026-08-03):** `briefs/INGEST-CURRENCY-ASSERT.md`, не взят; шаг 1 фактически уже закрыт `DQ-SOURCE-CAPTURE` (снапшот `cf-dq`/текст проверки в репо, требуется переподтверждение свежести); шаг 2 расширен до шести точек конвертации в `cf-facts`/`cf-finance`. |
| METHOD-PROMOTE-LOCAL-RULES | (`ADR-101 §9`, решение владельца 2026-08-02) Продвижение трёх holika-локальных правил в `_METHOD.md` помеченным расширением: «успех инструмента ≠ факт» (`Q-40`, в `§6`), «маркер времени прихода пункта последствий» (`Q-84`, в `§8`), «стенд-ап заменяется целиком» (`Q-85`, в `§8`) | задача (класс A) | **READY, гейтов нет.** Форма задана прецедентом `_METHOD.md §11 [РАСШИРЕНИЕ]`: помеченный блок плюс провенанс-строка «за пределами эталонного baseline». Эталон `ilyasbazarov/docs @ cb33fb4` НЕ правится, чужие пины не ломаются. Приёмка: три правила присутствуют в `_METHOD.md` с пометкой расширения; `05_CONVENTIONS` Часть I/III и `_ASSEMBLER.md` не переписываются (локальные редакции остаются, расхождение с каноном исчезает); строки `Q-40`, `Q-84`, `Q-85` закрываются этой задачей. Правка идёт отдельным именованным коммитом сессии (`ADR-081 §8`) **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| MSKLAD-TZ-PROPOSAL | (`ADR-101 §6`, решение владельца 2026-08-02) Передача клиенту предложения переключить часовой пояс аккаунта МойСклад с московского на бишкекский; исполняет владелец, инструменту недоступно ни в каком классе | задача (владелец) | **READY, текст готов** — `reference/msklad_timezone_proposal_2026-08-02.md`. Включает обратимую пятиминутную проверку ретроактивности (пункт 5), которая попутно закрывает несделанный второй замер сдвига `ADR-088 §10`. Исходы: согласие означает закрытие `Q-92` вариантом (b), пояс аккаунта переключён, единственная сошедшаяся пара «Расходы» перепроверяется; отказ означает закрытие `Q-92` вариантом (c) с плашкой в отчётах. Подготовку `INGEST-MOMENT-ZONE-FIX` не гейтит ни при каком исходе |
| STATE-DETAILS-BUDGET | (`STATE-DETAILS-ADJ`, 2026-08-02, `ADR-105 §4`) Назначить числовой бюджет полю «Подробности для модели» в session-блоке и максимальную длину строки GAP-реестра, плюс механическую проверку в `tools/hooks/pre-commit` с кейсами в `tools/hooks/selftest.sh`. **Гейт намеренный:** не раньше чем через 3–5 сборок под критерием `ADR-105 §1` — число обязано быть замером фактического объёма, а не назначением (`★ Anti-improvisation`). Метод замера снят и воспроизводим: `reference/_scratch_STATE-DETAILS-ADJ_2026-08-02/measure_state.py`. Распространение на реестр обязательно в том же решении: лимит только на раздел создаёт канал перетекания текста в строки реестра, у которых лимита нет | задача (класс A, документная + правка хука) | закрывается ADR с числом, выведенным из замера, плюс правки хука и его самотестов **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| `REGISTRY-COLUMN-SPLIT` | (`REGISTRY-COMPACT-ADJ`, 2026-08-02, `ADR-106 §3`) Разрез колонки «Как закрывается» в `07_GAPS.md`: остаются метод, статус и гейт, лента дописок уезжает в артефакт вопроса либо в архив, в строке — ссылка. Основание замером: колонка даёт 74,7 % реестра, 23 строки из 62 несут маркеры дописки и это 59 % объёма колонки. Попутно: привести колонку «Тип» к закрытому словарю (32 различных значения на 62 строки, 23 однократных) и починить разметку строки `Q-82`. **Гейтов нет**, но порядок назначен: только после применения `ADR-106 §1` (разрез на индекс/полный текст), чтобы разрез шёл по одной строке и не блокировал Epic-1 | задача (класс A, документная) | закрывается ADR плюс правкой `07_GAPS.md`; каждая переформулированная строка уезжает старой редакцией в `07_ARCHIVE.md` дословно тем же коммитом (`ADR-106 §5iii`) **`DEFER` заморозкой процессного слоя (`PROCESS-FREEZE`, 2026-08-02, `ADR-107 §3`); триггер — сходимость реестра паритета.** **Заморозка СНЯТА (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, механическое применение `ADR-107 §7`, триггер — сходимость реестра паритета `ADR-129`, `7/7`, `2026-08-07`): статус и гейт строки восстановлены к состоянию до `PROCESS-FREEZE` — см. текущее значение в `07_STATE.md §Открытые вопросы`.** |
| SALES-MERGE-DRYRUN | (`PARITY-COARSE-ADJ`, 2026-08-02, `ADR-109 §4`) Два технических неизвестных патча продаж (`SALES-REFRESH-WINDOW`): допустим ли `UPDATE` партиционирующей колонки внутри `MERGE`; источник COGS для диапазона шире 7/90 суток не назван | задача | **PARTIAL (сессия `SALES-MERGE-DRYRUN`, 2026-08-02).** Вопрос (а) закрыт фактом: тестовый `MERGE` с добавленной `T.transaction_date = S.transaction_date` в `WHEN MATCHED THEN UPDATE SET` прошёл `bq query --dry_run` против реальной `core.fact_sales_profit` без ошибки. Вопрос (б) остаётся `CONTEXT GAP`: `fetch_byvariant_cogs` (`fetch_byvariant.py:54-58`) технически принимает произвольный диапазон, но единственный вызывающий код (`main.py:154,173`) жёстко привязан к `WEEKLY_WINDOW_DAYS=90` — источник COGS шире 90 суток в коде не назван. Артефакт `reference/sales_merge_dryrun_2026-08-02.md`. Решение владельца/архитектора по гэпу (б) — перед выбором варианта `SALES-REFRESH-WINDOW`. |
| MARTS-BUILD-STAMP | (`FACTS-STOP-ADJ`, 2026-08-02, `ADR-111 §4/§6`) Пять таблиц не несут отметки времени и методом свежести не проверяемы; две из них пересобираются ежедневно поверх источников, которые могут стоять, то есть выглядят свежими независимо от возраста данных — та же маскировка, что два месяца прятала замороженные счета | задача | **OPEN.** Гейт: решение о форме (отметка сборки плюс возраст источника) — предметное, не методное. Мандат класса A: подготовка правки текстов витрин, ничего не применяет. Пишет `reference/marts_build_stamp_<date>.md`. **Форма подготовлена для 3 из 5 таблиц (сессия `MARTS-BUILD-STAMP`, 2026-08-10):** `customer_invoices_ar`/`expenses`/`weight_flow` разобраны, режим записи подтверждён `WRITE_TRUNCATE`/`CREATE OR REPLACE` (не `MERGE`, `C1`/`C2` неприменимы), два независимых варианта правки на таблицу («две колонки» / «одна производная»), выбор — owner-gated. Полный разбор — `reference/marts_build_stamp_2026-08-10.md`. Остаток — два CONTEXT GAP, заведены отдельными строками `MARTS-BUILD-STAMP-FX-CODE` и `MARTS-BUILD-STAMP-EXPSTG-SQ`. |
| MARTS-BUILD-STAMP-FX-CODE | (`MARTS-BUILD-STAMP`, 2026-08-10, CONTEXT GAP) Код `cf-fx` не найден в `reference/code/` — разбор SQL/логики маскировки свежести для `core.dim_fx_rates` невозможен по определению (не Custom Query) | референс-артефакт | **OPEN.** Discovery-задача снять исходники `cf-fx` по прецеденту `SOURCE-MAP-REST`/`SOURCE-MAP-SALES`; форма правки для `core.dim_fx_rates` не спроектирована до закрытия. |
| MARTS-BUILD-STAMP-EXPSTG-SQ | (`MARTS-BUILD-STAMP`, 2026-08-10, CONTEXT GAP) Ни один живой Custom Query/CF, строящий `marts.expenses_staging`, не найден — три источника проверены (`03_PIPELINE_SPEC.md §marts — SQL`, `11_INFRA_FACTS.md §SQ`, `reference/sql/`), 0 совпадений | референс-артефакт | **OPEN.** Discovery-задача найти адрес вне репо (`bq show`/`bq ls --transfer_config` по имени таблицы); форма правки не спроектирована до закрытия. |
| FACT-SALES-BYVARIANT-BACKUP-OWNER | (`CORE-FRESHNESS-SWEEP`, 2026-08-02; заведена сборкой буфера 2026-08-02 по прямому решению владельца — не попала ни в одну из четырёх задач `ADR-111`, см. «Дополнение сборки» там же) Таблица `core.fact_sales_profit_byvariant_backup` заморожена с `2026-05-03` (91 сутки), владелец и ожидаемая частота обновления нигде не названы — решить, это разовый бэкап или пропущенный загрузчик | факт | **OPEN.** Гейт: нет, приоритет ниже `FACTS-WORKFLOW-STOP-DIAG`. Мандат не назначен. |
| PARALLEL-CHECK-BLOCK-END | (`INVOICES-PROGRAMME`, 2026-08-02, `ADR-110 §4`, заведена по исключению `ADR-107 §5(i)` — починка сломанной механической проверки из-под заморозки) Извлекатель «Файлы на запись» `tools/parallel_check.sh:27-32` не закрывает блок на структурном заголовке `##` (шаблон закрывает следующим `## Роль`, не строкой `**`) — на реальном брифе (`briefs/SALES-REFRESH-WINDOW.md`) два объявленных файла дают четырнадцать извлечённых; проверка параллели не разрешает НИКОГДА | задача (инструмент, класс A) | **READY, гейтов нет.** Приёмка: (1) на `briefs/SALES-REFRESH-WINDOW.md` извлекается ровно два пути, на обоих новых брифах — ровно объявленные; (2) самотест по образцу `tools/inbox_manifest_selftest.sh` с кейсом «блок закрыт `##`» и кейсом «блок закрыт `**`», оба зелёные; (3) прогон на паре реальных брифов печатает наборы и вердикт, провенанс в `/reference`; (4) проверить второй источник (поле `Пишет:` таблицы мандата, `ADR-102 §1`) на то же условие выхода. Мандат класса A: правка `tools/parallel_check.sh` и нового самотеста, облачных вызовов нет. Пишет `tools/parallel_check.sh`, `tools/parallel_check_selftest.sh`, `reference/parallel_check_block_end_<date>.md`. **Дописка (`PARITY-STOCK-INTRANSIT-CLOSE`, четвёртый ход, 2026-08-05):** ВТОРОЙ инстанс того же класса найден в соседнем скрипте — `tools/inbox_manifest.sh` не закрывал регион «Последствия» на начале следующего решения при нескольких `NEW_DECISIONS` в одном блоке (починено отдельным коммитом сессии, замер по 107 блокам истории). Класс общий: извлекатель не закрывает блок на верной границе. Новой строки не заводится (`ADR-051 §2`). |
| Q-102 | (сборка буфера, 2026-08-03, ассемблер vs текст запуска) Формулировка запуска прохода сборки регулярно несёт шаблонную фразу вида «шапка блока несёт N указаний сборщику, перекрывающих §5/§6/§7 роли по номерам ADR» — по всей видимости привычная фраза человека, инициирующего сборку, а не факт, взятый из конкретного файла буфера. `_ASSEMBLER.md §3 п.0` предписывает трактовать любое такое утверждение как проверяемый факт и останавливаться при расхождении с содержимым буфера (`§10`, приём введён сессией `PARITY-GAPS-ADJ` против галлюцинации содержимого буфера со слов текста запуска). На разобранном случае (сборка 2026-08-03, блок `session_DQ-GATE-BEHAVIOR-PROBE_2026-08-02.md`) шапка блока — обычный заголовок `=== SESSION LOG · ... ===`; никаких «указаний сборщику» и никакой оговорки по номерам ADR в файле нет, а сам блок технически не требовал нумерации (`NEW_DECISIONS`/`NEW_CONVENTIONS` — «нет»). Итог: правило `§3 п.0`, введённое как защита от галлюцинации, при регулярном использовании инициатором одной и той же шаблонной фразы превращается в срабатывание на КАЖДОМ проходе сборки — сборщик обязан останавливаться и спрашивать владельца, хотя расхождение не есть дефект данных, а есть особенность того, как формулируется приглашение к сборке. Развилка не решена ни разу: (а) убрать формулировку из привычки инициатора запуска, раз она не читается из файла, — тогда `§3 п.0` продолжает работать штатно на настоящих расхождениях; (б) сузить `_ASSEMBLER.md §3 п.0`, чтобы правило реагировало только на утверждения о ПРОВЕРЯЕМЫХ фактах (имена файлов, названные разделы, номера ADR из строки индекса), а не на общие фразы-приглашения, не претендующие быть цитатой файла; (в) оставить как есть, считая одну лишнюю остановку-вопрос на проход сборки приемлемой ценой (сборка — не частая операция). Каждый вариант меняет либо привычку человека, либо текст `_ASSEMBLER.md`, либо ничего — квалификация не в мандате сборщика (§1 роли: не решает и не адъюдицирует) | процесс (шаблон текста запуска сборщика против правила `_ASSEMBLER.md §3 п.0`) | Закрывается решением архитектора/владельца о том, какая сторона корректируется — формулировка запуска, правило остановки, или статус-кво; до решения каждый проход сборки будет нести ту же остановку-вопрос при повторении шаблонной фразы. **Второй инстанс (`DQ-DRIFT-SOURCE-CORRECTION`, 2026-08-03):** `tools/inbox_manifest.sh` считает файлами сборки четыре и помечает пункт `[сейчас]`, адресованный `07_GAPS.md`, как находку с остановкой (`RC 1`), тогда как `ADR-106 §1` требует правки `07_GAPS.md` тем же коммитом, а проверка 9 хука её вынуждает. Наблюдено на живом проходе `2026-08-03`, снято обходом (переформулировкой пункта), не починкой. Фикс — одна строка в списке файлов скрипта; из-под заморозки выведено `ADR-107 §5`. Отдельная строка не заводится (`ADR-051 §2`) |
| CODE-REPO-VISIBILITY | (`CODE-REPO-2B-CONFIRM`, 2026-08-03) Код-репозиторий `holika-prod` ПУБЛИЧНЫЙ (`isPrivate: false`, замер `2026-08-02T20:24:31Z`) при решении `ADR-109 §7` «приватный GitHub `holika-prod`» | решение | **DEFER.** Значений секретов в выложенном `main.py` нет (читаются из Secret Manager по имени); публично видны идентификатор проекта GCP, адреса служебных аккаунтов, имена бакетов/датасетов, имя секрета `msklad-token` и логика конвейера. Закрывается ОДНИМ из двух явных путей: владелец переключает видимость, либо `ADR-109 §7` пересматривается новым ADR. Тихое приведение доков к факту запрещено (`00_CHARTER §главный принцип`). Настройка репозитория владельца — инструменту не отдаётся (`ADR-090 §2`). **Решение владельца (`LS-RETURNS-SURFACE-ADJ`, чат 2026-08-03):** «пока живём так, закрою потом, когда закончим». Триггер — завершение проекта либо решение владельца. Расхождение с `ADR-109 §7` остаётся зарегистрированным, не примирённым. |
| GATE-STATE-VERIFIABILITY | (`CODE-REPO-2B-CONFIRM`, 2026-08-03, `NEW_DECISIONS` proposed) Строка реестра, чьё ДЕЙСТВИЕ за владельцем, не несёт способа проверки СОСТОЯНИЯ — два разных свойства слиты в одну формулировку, и сессии читают «исполняет владелец» как «состояние проверке недоступно» | решение | **OPEN, proposed (требует апрува владельца).** Живой прецедент с измеримой ценой: шаг 2b `CODE-REPO-STANDUP` исполнен `2026-08-01T16:35:55Z`, зафиксирован `2026-08-03`; `ADR-109 §5` объявлял этот гейт единственным гейтом всего деплойного потока, то есть очередь правок числилась заблокированной двое суток гейтом, которого не было. Проверка стоила одного read-only вызова без секретов (`gh repo view`) — внутри класса A. Предложение из четырёх пунктов: (1) разделить в реестре «кто исполняет действие» и «как проверяется состояние»; (2) строка с действием владельца обязана нести способ проверки состояния либо явное «проверке инструментом не поддаётся» с причиной; (3) сессия, чей гейт есть действие владельца, обязана СНАЧАЛА выполнить названную проверку — «гейт объявлен» не есть «гейт стоит»; (4) ретроспективное применение сплошным поиском (`ADR-083 §2`) с решением по каждому совпадению. Тем же признаком помечены как минимум правки в интерфейсе Looker Studio (результат проверяется запросом к витрине; ср. `LS-INVENTORY-EXPLICIT-COLUMNS`, где read-back остался устным) и `MSKLAD-TZ-PROPOSAL`. **Второй инстанс (`LS-RETURNS-SURFACE-ADJ`, 2026-08-03):** реестр паритета называл поверхностью пары возвратов Custom Query `msklad_counterparty_returns`, то есть источник данных вместо нарисованного графика; состав полей графика ни разу не снимался, и пять суток пара числилась несошедшейся по признаку, который никто не проверял. **Заявлено исключение из заморозки `ADR-107 §2`:** без правила встаёт не метод, а предметная очередь — `INVOICES-LOADER-DEPLOY`, `INGEST-MOMENT-ZONE-FIX`, `SALES-REFRESH-WINDOW`, `INGEST-CURRENCY-ASSERT`, `DQ-GATE-SCOPE-SPLIT`. Полный текст предложения — блок сессии `reference/_inbox/session_CODE-REPO-2B-CONFIRM_2026-08-03.md` в истории git (`ADR-080 §4ii`) и `reference/code_repo_2b_confirm_2026-08-03.md` §4. **Третий инстанс (`DQ-GATE-DEPLOY-ADJ`, 2026-08-04, `ADR-122 §9`):** признак готовности `ADR-109 §1` («артефакт в `reference/` с именем задачи») ловит отсутствие ПОДГОТОВКИ и не ловит отсутствие ПРИЗЕМЛЕНИЯ — адъюдикация `reference/dq_gate_fail_open_adj_2026-08-03.md` несла артефакт, но её ADR-кандидат номера не получил и в `06_INDEX.md` не значился; признак показывал зелёное, решение в `06` отсутствовало. |
| DQ-FRESHNESS-COVERAGE | (`ADR-115 §14б`, 2026-08-03) Покрытие проверкой свежести шести фактовых таблиц ядра без наблюдателя (`fact_returns`, `fact_purchases`, `fact_inventory`, `fact_payments`, `fact_customer_invoices`, `fact_commissionreportin`) | задача | **OPEN.** Основание: `DQ-SOURCE-CAPTURE` установил, что гейт проверяет ТОЛЬКО домен продаж; счета простояли замороженными 58 суток и найдены случайно. Порог у каждой таблицы выводится из её собственной каденции, текст проверки для счетов уже готов (`invoices_loader_design §11`, техническая свежесть по `_loaded_at`, порог 48 часов) и служит образцом формы. **Гейт: `DQ-GATE-SCOPE-SPLIT`** — добавление проверок в текущий гейт до разделения периметра расширит уже известный радиус поражения. Подготовка — класс A, деплой — класс B. Пишет: `reference/code/cf-dq/`, `reference/dq_freshness_coverage_<date>.md`. **Гейт перенаправлен (`DQ-GATE-DEPLOY-ADJ`, 2026-08-04, `ADR-122 §8`): `DQ-GATE-SCOPE-SPLIT-DEPLOY`**, не `DQ-GATE-SCOPE-SPLIT` (подготовка того закрыта DONE). Дописано требование: свежесть измеряется на ПРОМОУТНУТОЙ таблице (`core.*`), а не на staging — иначе проверка не отличает остановку промоута от остановки источника. **Гейт снят фактом (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, `ADR-140 §последствия`): `DQ-GATE-SCOPE-SPLIT-DEPLOY` архивирована DONE (2026-08-05); статус строки этим не переоценивается.****Бриф собран (сессия `DQ-FRESHNESS-COVERAGE-GEN`, 2026-08-08):** `briefs/DQ-FRESHNESS-COVERAGE.md`, не взят. Класс — A, параллель — нет, мандат — постоянный (строка «DQ-FRESHNESS-COVERAGE, подготовка»); опираться на актуальный `07_GAPS.md` (гейт снят фактом), а не на устаревшую формулировку колонки «Основание» таблицы мандата. Бриф ставит задачу спроектировать проверки свежести (техническая по `_loaded_at` + бизнес-диагностическая по бизнес-дате, без деплоя) для шести таблиц, из которых `fact_customer_invoices` уже полностью спроектирована (`reference/invoices_loader_design_2026-08-02.md §9.2`, порог 48ч) и переносится без переделки; для остальных пяти порог технической проверки выводится по формуле «`2 × период каденции`» (`03_PIPELINE_SPEC.md:86`): часовая (`fact_purchases`) → `2ч`; суточная (`fact_inventory`, `fact_payments`, `fact_commissionreportin`) → `48ч`; недельная (`fact_returns`) → `336ч`. Бриф явно требует от исполнителя ПЕРЕД написанием проверки для каждой из пяти новых таблиц подтвердить чтением кода загрузчика инвариант «один стамп `_loaded_at` на прогон», не подставлять правдоподобное. Деплой и подключение к списку `CHECKS`/`workflow.yaml` — вне scope, отдельная строка мандата «DQ-FRESHNESS-COVERAGE, деплой» (класс B, мандат не выдан). **Подготовка продвинута (сессия `DQ-FRESHNESS-COVERAGE`, 2026-08-09):** 4 из 6 таблиц (`fact_purchases`, `fact_returns`, `fact_inventory`, `fact_customer_invoices`) получили обе готовые проверки; для 2 из 6 (`fact_payments`, `fact_commissionreportin`) написана только диагностика (B) — инвариант «один стамп `_loaded_at` на прогон» ОПРОВЕРГНУТ (`cf-finance/main.py:72`, `cf-loss-commission/main.py:149` вызывают `datetime.now()`/`utcnow()` внутри цикла постранично). Все 12 SQL провалидированы `bq query --dry_run` (RC=0). Решение о форме/приоритете фикс-форварда и о закрытии строки — за архитектором. Полный ход — `reference/dq_freshness_coverage_2026-08-09.md §7-§8`. **Дописка (`ADR-152 §3`, 2026-08-10):** этот код физически лежит в том же снапшоте `reference/code/cf-dq/{main.py,config.py}`, что и деплоящаяся сейчас правка `DQ-GATE-FAIL-OPEN-FIX` — в мандат и в ветку деплоя `deploy/cf-dq-2026-08-10-fail-open` этой задачи НЕ входит (разделение по хункам: `main.py` ветки строится как задеплоенный `main.py` плюс ровно хунк `ma7`, `config.py` не переносится вовсе). Своего мандата на деплой строка по-прежнему не имеет. **Остаток разблокирован (`DQ-GATE-FORM-ADJ`, 2026-08-10, `ADR-155`):** формулировка «2/6 блокированы дефектом `_loaded_at`» опровергнута чтением кода — обе недостающие технические проверки (`fact_payments`, `fact_commissionreportin`) пишутся классом A с порогом `48` часов по формуле `MAX(_loaded_at)`; `MAX` по построчным стампам равен моменту окончания прогона, отклонение от единого стампа безопасно (минуты против порога 48ч); поле `distinct_load_stamps` для этих двух таблиц неинформативно. Дефект самого построчного `_loaded_at` выносится строкой известных ограничений (не заводится задачей `LOADER-LOADED-AT-STAMP`, `ADR-156 §1`). |
| BQ-QUERY-MAX-ROWS | (`PARITY-STOCK-INTRANSIT`, 2026-08-04, `NEW_CONVENTIONS` proposed) Любой `bq query --format=json|prettyjson`, чей результат может превышать 100 строк, обязан нести явный `-n`/`--max_rows` с запасом либо предварённый `COUNT(*)` тем же условием — дефолтный лимит `bq` (100 строк) даёт синтаксически валидный, но тихо неполный результат (тот же класс, что `★ Успех инструмента ≠ факт`, но специфичен для `bq`, не назван в существующем тексте правила) | решение | **OPEN, proposed.** Живой прецедент: первый `bq query` без `-n` в этой сессии молча обрезал результат `entity/purchaseorder`/`core.fact_purchases` до дефолтных 100 строк (`341`/`241` фактически) — пойман только потому, что число строк источника было известно независимо и не совпало со 100; при отсутствии независимого счётчика результат ушёл бы в отчёт как факт. Session-блок не заявляет исключения из заморозки `ADR-107 §2` (прецедент формы — `GATE-STATE-VERIFIABILITY`) — закрывается апрувом владельца плюс заявленным исключением из заморозки, названным поимённо. Полный текст предложения и провенанс — `reference/_inbox/session_PARITY-STOCK-INTRANSIT_2026-08-04.md` в истории git (`ADR-080 §4ii`) и `reference/parity_stock_intransit_2026-08-04.md`. |
| DQ-GATE-BLOCK-BOUNDED | (`DQ-GATE-FAIL-OPEN-FIX`, 2026-08-10, `ADR-152 §7`, требование `dq_gate_fail_open_adj_2026-08-03.md §7` пункт 1) Блокирующее действие DQ Gate ничем не ограничено по длительности и не имеет пути подтверждения-и-пропуска — до правки `DQ-GATE-FAIL-OPEN-FIX` это компенсировалось тем, что сторож саморазоружался сам через восемь суток (fail-open); после правки ограничения по времени по-прежнему нет, но пропуска тоже нет | задача | **READY.** У требования не было и до сих пор нет строки-адресата в реестре (проверено `grep`; родительская `DQ-GATE-SCOPE-SPLIT` закрыта DONE деплоем периметра, но это конкретное ограничение предметно не вносилось). Принимаемый риск деплоя `DQ-GATE-FAIL-OPEN-FIX` (`ADR-152 §7`) назван явно, не умолчан: главное возражение против неограниченной блокировки снято фактом — адресный сигнал человеку работает (`ADR-149`, 2026-08-09, read-back подтверждён). Не гейтит и не гейтится `DQ-GATE-FAIL-OPEN-FIX`. **Форма назначена (`DQ-GATE-FORM-ADJ`, 2026-08-10, `ADR-154`):** ack-механизм в коде и автоснятие по времени НЕ строятся (четыре основания — см. `ADR-154`); длительность блока извлекается запросом к `audit.dq_runs`, порог эскалации `24` часовых прогона назначается человеку, ручное снятие получает написанную процедуру; задача переопределена как документная, класс A. Строка `8` блока B списка закрытия (`reference/scope_close_freeze_2026-08-10.md §3b`); часть про параметры ручного снятия гейтится предохранителем `ADR-145 §4/§5`. Строка мандата заведена (класс A, параллель да, постоянный). Пишет: `reference/dq_gate_block_bounded_<date>.md`. |
| DQ-GATE-METRIC-REDESIGN | (`DQ-GATE-DEPLOY-ADJ`, 2026-08-04, `ADR-122 §4`) `drift_check` отождествляет «почти не продавали» с «данные испорчены»: `8,9 %` суток этого бизнеса (`40` из `451`) не имеют ни одного документа продажи вовсе, на ВСЕХ днях недели, не только на выходных — метрика с одним значением на нормальный и аварийный вход не калибруется, а переделывается | задача (форма принята) | **READY.** Проектирование — класс A, Пишет: `reference/dq_gate_metric_redesign_<date>.md`. **Гейт снят фактом (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, `ADR-140 §последствия`): `DQ-GATE-SCOPE-SPLIT-DEPLOY` архивирована DONE (2026-08-05).** **Форма принята (`DQ-GATE-FORM-ADJ`, 2026-08-10, `ADR-153`):** кандидат 1 — `target_rev == 0` даёт исход `notify` (не блокирует промоут), `target_rev > 0` при `ratio < threshold` остаётся блокирующим, `freshness` остаётся независимым блокирующим бэкстопом; механизм — две функции (не tri-state); канал `notify` — существующий телеграм; пороги `0,10`/`0,03` не меняются; кандидат 3 (persistence-gating) — `DEFER`, триггер: два и более подряд идущих провала блокирующего класса, разобранных человеком и признанных ложными. Остаток: подготовка патча `reference/code/cf-dq/main.py` (класс A, исполнима сейчас); деплой (класс B, мандат не выдан, один выезд вместе с `DQ-FRESHNESS-COVERAGE`); вторая лог-метрика для доставки исхода `notify` в телеграм-канал (класс B, отдельный объект, мандат не выдан); `03_PIPELINE_SPEC.md §DQ` дописывается второй строкой `drift_check` при деплое. |
| DQ-GATE-SCOPE-CONFIRM | (`DQ-GATE-DEPLOY-ADJ`, 2026-08-04, `ADR-122 §8`, требование (2) неприземлённой адъюдикации `reference/dq_gate_fail_open_adj_2026-08-03.md`) Подтверждение на первом реальном провале `drift_check` после разделения периметра, что закупки и возвраты исполнились, а промоут продаж — нет | задача (класс A) | **OPEN, гейт `DQ-GATE-SCOPE-SPLIT-DEPLOY`, класс A, параллель да.** Форма запроса берётся из скриптов `DQ-SOURCE-CAPTURE`; там же закрывается гэп наблюдения по логам гейта (`calibration §5` — фильтр Cloud Logging не достаёт тела записей приложения, вернулась только HTTP-запись). Пишет: `reference/dq_gate_scope_confirm_<date>.md`. **Гейт снят фактом (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, `ADR-140 §последствия`): `DQ-GATE-SCOPE-SPLIT-DEPLOY` архивирована DONE (2026-08-05); статус строки этим не переоценивается.** |
| Q-104 | `94,5%` строк `core.fact_purchases` со статусом «Прибыл» (`957 627 883,14` KGS) не обнуляют `in_transit_sum_kgs`/`quantity_in_transit`; неизвестно, лежит причина в источнике или в загрузчике `fetch_purchases.py` (`INTRANSIT-SURFACE-LAG-ADJ`, 2026-08-07, `ADR-130 §2`) | факт (открытый различитель) | **DEFER.** Не меняет ни один вердикт паритета (периметр `ADR-124 §3` статус «Прибыл» исключает по построению), потребителя вне scorecard «Сумма KGS в пути» (сама фильтрует по статусу) нет. Различитель «источник vs загрузчик» требует живого `GET entity/purchaseorder/<id>/positions` (класс B, секрет `msklad-token`) — не оправдан без конкретного потребителя. Триггер — появление такого потребителя. `reference/intransit_surface_lag_2026-08-07.md §4`. Строки в таблице мандата нет (мандат не выдан и не запрашивается до триггера). |
| INTRANSIT-CADENCE-COST-PROBE | (`INTRANSIT-SURFACE-LAG-ADJ`, 2026-08-07, `ADR-130 §1`) Класс A, read-only замер стоимости (BigQuery slot-время) часовой пересборки `sq_marts_in_transit` против текущей суточной — предусловие для повторного рассмотрения варианта (A) правки каденции витрины «в пути» | задача | **OPEN.** Не блокирует финиш Epic-1 (реестр паритета уже `7/7`, работа по качеству витрины вне периметра сверки). Замер через `INFORMATION_SCHEMA.JOBS_BY_PROJECT` за представительное окно суточных прогонов текущей конфигурации, экстраполяция на часовую каденцию, либо `--dry_run` пробный расчёт. Пишет: `reference/intransit_cadence_cost_probe_<date>.md`. |
| SALES-EMPLOYEE-ATTRIBUTION-MAY-SUM-GAP | (`SALES-EMPLOYEE-ATTRIBUTION`, 2026-08-07) Офлайн-проверка разреза по сотруднику на мае наткнулась на нестыковку: Σ поля `sum` 127 майских документов `entity/demand` (`52 647 606,84` KGS) не пропорциональна Σ`revenue_kgs` тех же документов на уровне позиций (`93 522 995,53` KGS) — коэффициент различается по сотрудникам-владельцам документа от `23%` до `127%`, единым множителем (курс/налог/округление) не объясняется | факт | **OPEN.** Вероятная причина — `sum` документа и Σ его позиций считаются по разным основаниям (в сохранённых JSON `positions` не развёрнуты, только ссылка), не подтверждено измерением. Блокирует только майскую офлайн-проверку разреза по сотруднику, июльский вывод (`ADR-127`) не затрагивает — тот подтверждён иначе, по множеству контрагентов. Закрывается снимком с `expand=positions` (класс B) либо иным подтверждением сравнимости `sum`/`Σ revenue_kgs`. |
| Q-105 | (`SALES-DOCUMENT-OWNER-INGEST`, 2026-08-07) Версионируется ли `owner` документа `entity/demand` отдельно от самого документа (как `dim_counterparties.owner_employee_id` через SCD2, `ADR-089`), или правится вместе с `updated` документа? | факт | **OPEN.** Не проверено ни замером, ни документацией API (требует живого запроса, класс B, либо документации API МойСклада — вне мандата класса A). Полный текст — `reference/sales_document_owner_ingest_2026-08-07.md §Открытый технический вопрос`. |
| SALES-PERIMETER-CHANNEL-REAL | (`SALES-JULY-ORACLE-ADJ`, 2026-08-07, `ADR-137 §6`) Метки канала у строк периметра — константы «Розница»/«Комиссия» (`ADR-134`), тогда как у источника те же документы лежат в поимённых каналах «UMAI WB», «К Глобал»; на разрезе по каналам клиент видит наши метки вместо имён МойСклада | задача | **DEFER.** Триггер — решение владельца: вопрос совпадения ИМЁН на клиентской поверхности, не величин; итог и вклад периметра от него не зависят (подтверждены точным совпадением, `ADR-137 §2`), финиш он не гейтит. При положительном решении работа — чтение реального `salesChannel` в `fetch_perimeter.py` по образцу `fetch_demands.py`, класс A подготовка плюс класс B деплой; `ADR-134` при этом не отменяется, а уточняется новым решением (append-only). Строки в таблице мандата нет и не будет, пока задача не открыта (прецедент трактовки `Q-81`/`Q-104`). |
| HANDOVER-PACKAGE | (`SALES-JULY-RESIDUAL-ADJ`, 2026-08-08, `ADR-140 §4/§последствия`) Документ передачи клиенту: реестр известных ограничений (строка реестра, не попавшая под критерий отсечения `ADR-140 §4`, переезжает сюда текстом), клиентский runbook «где смотреть», текст риска `R1` (`ADR-101`, обязателен к объявлению до первого деплоя фазы передачи, `ADR-140 §5`) | задача (класс A, документная) | **OPEN, гейт — исполнение очереди передачи; документ собирается последним**, иначе фиксирует состояние, которое ещё меняется. Критерий отсечения задач фазы передачи (`ADR-140 §4`): задача входит в передачу только если её невыполнение даёт (i) неверное число/подпись на клиентской поверхности, (ii) тихий отказ, (iii) неремонтопригодность без нас. Пишет: `reference/handover_package_<date>.md`. |
| Q-106 | (`SALES-JULY-K2K3-ADJ`, 2026-08-08, `NEW_CONVENTIONS` proposed) Хвост ответа владельцу, за которым следует работа, обязан кончаться таблицей с фиксированными колонками `Задача (имя строкой реестра) · Статус · Класс · Бриф (есть/нужен/не нужен) · Гейт · Кто делает`; задачи без собственного имени строкой в `07_STATE §Открытые вопросы` в таблице быть не может — прозаические обороты («дальше один запрос», «форму починки назначим позже») формой не являются | решение | **OPEN, proposed (требует апрува владельца).** Правка `05_CONVENTIONS.md` Часть I `★ Язык коммуникации с владельцем`, новый пункт 9, зеркально — в файлы всех четырёх ролей (`ADR-093 §1`). Механическая половина предложена отдельно: проверка в `tools/hooks/pre-commit`, что каждое имя задачи в блоке `### Стенд-ап` (`07_STATE.md`, строки «Следующий шаг»/«Развилки на владельце») существует строкой в `07_STATE §Открытые вопросы` либо в `07_ARCHIVE.md`; расхождение = RC 1. Полный текст предложения — `reference/_inbox/session_SALES-JULY-K2K3-ADJ_2026-08-08.md` в истории git (`ADR-080 §4ii`). Правки `05_CONVENTIONS.md`, файлов ролей и `tools/hooks/*` приходят отдельным поимённым коммитом после апрува (`ADR-081 §8`), в буфер не попадают. |
| Q-107 | (`SALES-REFRESH-WINDOW-ROLLBACK-ADJ`, 2026-08-11, `NEW_CONVENTIONS` proposed) В список проверки готовности деплоя перед выдачей мандата класса B добавить пункт: «для любой операции, УДАЛЯЮЩЕЙ строки, назвать, чьи строки представляет источник, и доказать, что он полон для окна операции» | решение | **OPEN, proposed (требует апрува владельца).** Основание — инцидент `ADR-161`: три независимые проверки (`--dry_run`, проверка архитектора по существу, предохранитель `ADR-158`) пропустили дефект (ветка удаления `MERGE` сносит строки чужого загрузчика), потому что ни одна не задавала этого вопроса. Полный текст предложения — `reference/_inbox/session_SALES-REFRESH-WINDOW-ROLLBACK-ADJ_2026-08-11.md` в истории git (`ADR-080 §4ii`). Правка `05_CONVENTIONS.md` (список проверки готовности деплоя) приходит отдельным поимённым коммитом после апрува (`ADR-081 §8`), в буфер не попадает. |
| SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE | (заведена `SALES-REFRESH-WINDOW-DELETE-ADJ`, 2026-08-12, `ADR-164 §6`) Из `36` строк, удалённых боевым прогоном `SALES-REFRESH-WINDOW-DEPLOY` (окно `106` суток), `7` опознаны как подтверждённые сироты `ADR-100 §1` — по ним удаление штатно. Остаток `29` строк / `2 407 220,42` KGS не объяснён: исчезли ли они из источника (удаление корректно) или потеряны выгрузкой на длинном окне (дефект патча) | задача | **READY, класса A целиком кроме возможной третьей ступени.** Форма — точный трёхступенчатый различитель, весь на GCS-архиве СЫРОГО ответа источника (снимок нашей таблицы источник не подменяет, `00_CHARTER §главный принцип`), прецедент опоры на архив — `ADR-143`: (1) пересчитать `transaction_id = TO_HEX(MD5(CONCAT(demand_id,'|',position_id)))` по каждой записи архива, проверить присутствие всех `29` удалённых `transaction_id` в этом множестве — найден → выгрузка вернула, дефект между архивом и staging (гипотеза b) подтверждена без обращения к складу; не найден → шаг 2; (2) для ненайденных — искать записи того же `agent_id`/`transaction_date` в архиве — есть → потеря позиций (b); нет ни одной → документ отсутствует в выгрузке целиком, гипотеза (a) правдоподобна, шаг 3; (3) только для остатка и обязательно для пары маркетплейса (`1 980 000,00` KGS) — прямой `GET` к `api.moysklad.ru`, класс B, мандат выдаётся поимённо, объём — единицы чтений. Полная форма и приблизительная перекрёстная проверка по группам дата+контрагент (`13` групп, `5` многострочных) — `reference/sales_refresh_window_delete_adj_2026-08-12.md §4/§4a`. Пишет: `reference/sales_refresh_window_delete_discriminate_<date>.md`, `reference/_scratch_SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE_<date>/`. Страховка по времени (`ADR-164 §7`): если вопрос не закрыт к `2026-08-15T12:00Z` — откат КОДА без дальнейшего обсуждения, данные не трогаются ни при одном исходе. **Ступени 0-2 исполнены классом A целиком (сессия `SALES-REFRESH-WINDOW-DELETE-DISCRIMINATE`, 2026-08-12):** GCS-архив сырого ответа найден (`generation=1786510926867012`, создан до `MERGE`); пересчёт `transaction_id` по `7690` записям — ни один из `29` идентификаторов не найден; группировка по `agent_id`+бишкекская дата дала в `11` из `13` групп (`19` строк) подпись «документ есть, позиции нет», в `2` группах (`10` строк) документ отсутствует целиком; перекрёстная проверка снимок/таблица дала численно идентичный разрез. Ступень 3 (класс B) не исполнена — вне мандата этой сессии. Полный разбор — `reference/sales_refresh_window_delete_discriminate_2026-08-12.md`. **Вердикт «`19` строк — подтверждённый дефект» СНЯТ адъюдикацией (`SALES-REFRESH-WINDOW-DEFECT-ADJ`, 2026-08-12, `ADR-165`):** критерий («документ в архиве есть, позиции нет ⇒ дефект») прогнан по размеченному набору из `7` строк, чья законность удаления доказана `ADR-100 §1`, и метит дефектом `4` из `7` — подпись принимает оба значения на размеченном наборе, информации не несёт. Причина — позиционная природа механизма сирот `ADR-100 §1`; наблюдение одинаково порождается потерей позиции выгрузкой и законным удалением позиции в источнике. Все `29` строк возвращаются в неквалифицированные; `7` строк (`1 630 239,81` KGS) остаются доказанно законными. Ступени 0-2 и перекрёстная проверка остаются в силе и переиспользуются. Статус — `PARTIAL`; строка не архивируется до закрытия `SALES-REFRESH-WINDOW-SOURCE-PROBE`, которая квалифицирует остаток прямым запросом к источнику. |
| SALES-REFRESH-WINDOW-SOURCE-PROBE | (заведена `SALES-REFRESH-WINDOW-DEFECT-ADJ`, 2026-08-12, `ADR-165 §5`) Квалификация `29` неатрибутированных удалённых строк прямым запросом к источнику МойСклад — жив ли каждый удалённый идентификатор в текущих данных склада | задача | **READY, класс B, мандат ВЫДАН поимённо (владелец, чат 2026-08-12, `ADR-166 §2`).** Форма — ВПЕРЁД: по каждой из `13` пар дата+контрагент (`16` включая `3` контрольные) — `GET entity/demand` с `expand=positions`, пересчёт `transaction_id` по найденным позициям, проверка вхождения `29` идентификаторов; обратно из хеша `demand_id` не достаётся (`bq_ops.py:261`), поэтому запрос идёт только вперёд. Ловушка суток обязательна (`ADR-088`): фильтр по `moment` для бишкекской даты `D` — `[D−1 18:00:00Z; D 18:00:00Z)`, наивный фильтр даёт ложное «документа нет». Пагинация позиций обязательна (`limit ≤ 100`, `02_ERP_CONTRACTS`). Оба контроля — условие доверия, не пожелание: положительный (семь доказанных сирот `ADR-100 §1` обязаны вернуться «не найдено»), отрицательный (не менее `10` заведомо живых позиций обязаны найтись); провал любого ⇒ вердикт по `29` не выносится. Пишет: `reference/sales_refresh_window_source_probe_<date>.md`, `reference/_scratch_SALES-REFRESH-WINDOW-SOURCE-PROBE_<date>/`. |
| SALES-REFRESH-WINDOW-CODE-ROLLBACK | (заведена `SALES-REFRESH-WINDOW-DEFECT-ADJ`, 2026-08-12, `ADR-166 §1`) Инвариант полноты выгрузки, от которого зависит ветка удаления патча `SALES-REFRESH-WINDOW`, не проверяется ни одним инструментом; ближайший широкий прогон (`2026-08-16T01:00Z`) идёт без человека | задача | **READY, класс B, мандат ВЫДАН поимённо (владелец, чат 2026-08-12).** Объект — одна идемпотентная команда перевода трафика `cf-facts` на ревизию `cf-facts-00011-mab`. Приёмка — три read-back формы прецедента `2026-08-11`, третий на естественном часовом прогоне. Откат отката — та же команда на ревизию `cf-facts-00014-doh`, пересборка не требуется ни в одну сторону. Основание — не доказанный дефект и не наступивший срок, а непроверяемость инварианта полноты плюс безнадзорный широкий прогон плюс нулевая цена отката (отработан `2026-08-11`) плюс асимметрия ошибок (`ADR-165 §8`). Пишет: `reference/sales_refresh_window_code_rollback_<date>.md`, `reference/_scratch_SALES-REFRESH-WINDOW-CODE-ROLLBACK_<date>/`. |
| Q-109 | (`NEW_CONVENTIONS`, `SALES-REFRESH-WINDOW-DEFECT-ADJ`, 2026-08-12, proposed) Критерий, отделяющий дефект от нормы, обязан быть прогнан по РАЗМЕЧЕННОМУ набору обеих категорий ДО того, как по нему выносится вердикт; в вердикт печатается результат этого прогона | решение | **OPEN, proposed (требует апрува владельца).** Основание — измеренная цена: критерий `…delete_adj…§4a` п.2 дал `4` ложных срабатывания из `7` на единственном размеченном наборе, который существовал заранее и стоил одного прогона скрипта; на нём была построена и доложена владельцу картина «дефект подтверждён на двух третях остатка». Правило смежно `05_CONVENTIONS` Часть I ★ «Критерий приёмки выводится из формулы, не из свойства данных» (`ADR-075 §3`) и ★ «Успех инструмента ≠ факт» (`ADR-044`): те про вердикт инструмента, это про вердикт КРИТЕРИЯ. Адрес — `05_CONVENTIONS` Часть I, отдельный пункт ★. Полный текст — `reference/_inbox/session_SALES-REFRESH-WINDOW-DEFECT-ADJ_2026-08-12.md` в истории git (`ADR-080 §4ii`). |

--- END 07_GAPS.md ---
